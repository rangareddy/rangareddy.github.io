<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="3.9.2">Jekyll</generator><link href="https://rangareddy.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://rangareddy.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-09-26T20:02:57+05:30</updated><id>https://rangareddy.github.io/feed.xml</id><title type="html">Ranga Reddy</title><subtitle>Hands-on notes, production troubleshooting and interactive tools for Apache Hudi, Iceberg, Spark, Kafka and the lakehouse stack underneath them.</subtitle><author><name>Ranga Reddy</name></author><entry><title type="html">Run every demo on this blog locally: one compose stack for Hudi, Iceberg and Delta</title><link href="https://rangareddy.github.io/DatalakeDockerDemos/" rel="alternate" type="text/html" title="Run every demo on this blog locally: one compose stack for Hudi, Iceberg and Delta" /><published>2026-09-19T10:00:00+05:30</published><updated>2026-09-19T10:00:00+05:30</updated><id>https://rangareddy.github.io/DatalakeDockerDemos</id><content type="html" xml:base="https://rangareddy.github.io/DatalakeDockerDemos/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#why-a-local-stack&quot; id=&quot;markdown-toc-why-a-local-stack&quot;&gt;Why a local stack&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-is-in-it&quot; id=&quot;markdown-toc-what-is-in-it&quot;&gt;What is in it&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#getting-it-running&quot; id=&quot;markdown-toc-getting-it-running&quot;&gt;Getting it running&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing-a-spark-line&quot; id=&quot;markdown-toc-choosing-a-spark-line&quot;&gt;Choosing a Spark line&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#running-the-demos&quot; id=&quot;markdown-toc-running-the-demos&quot;&gt;Running the demos&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#demos-map-to-posts&quot; id=&quot;markdown-toc-demos-map-to-posts&quot;&gt;The demos map to the posts&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#the-numbers-come-out-the-same&quot; id=&quot;markdown-toc-the-numbers-come-out-the-same&quot;&gt;The numbers come out the same&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#things-that-cost-time&quot; id=&quot;markdown-toc-things-that-cost-time&quot;&gt;Things that cost me time&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#resetting&quot; id=&quot;markdown-toc-resetting&quot;&gt;Resetting and cleaning up&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-it-does-not-cover&quot; id=&quot;markdown-toc-what-it-does-not-cover&quot;&gt;What this does not cover&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;a href=&quot;https://github.com/rangareddy/datalake-docker&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;datalake-docker&lt;/code&gt;&lt;/a&gt; brings up MinIO, a Hive Metastore, Spark, Kafka and Trino as one Compose project, so the demos need no cloud account.&lt;/li&gt;
    &lt;li&gt;It builds on &lt;strong&gt;two Spark lines&lt;/strong&gt;: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SPARK_VERSION=3.5.9&lt;/code&gt; (Scala 2.12) and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SPARK_VERSION=4.1.3&lt;/code&gt; (Scala 2.13). Everything else follows from that choice.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;demos/&lt;/code&gt; holds scripts that reproduce the posts’ measurements. The copy-on-write versus merge-on-read figures came out identical here to the numbers in that post.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The Spark 4.1 image ships Iceberg only.&lt;/strong&gt; Hudi and Delta have no build that runs on Spark 4.1 yet, and both failures are concrete rather than theoretical — measured below.&lt;/li&gt;
    &lt;li&gt;Hudi’s first write to a new table bootstraps its metadata table and takes around twenty seconds. A smoke test run immediately after &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;start&lt;/code&gt; can read that as a failure.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;why-a-local-stack&quot;&gt;Why a local stack&lt;/h2&gt;

&lt;p&gt;Every measured claim in these posts — the file counts in
&lt;a href=&quot;/IcebergLocalPlayground/&quot;&gt;copy-on-write versus merge-on-read&lt;/a&gt;,
the snapshot arithmetic, the spill numbers — came from running something, not from
recalling it. That is only sustainable if running something is cheap.&lt;/p&gt;

&lt;p&gt;A cloud account is not cheap in the way that matters here: it is slow to reset, it
costs money to leave running, and a demo that depends on your IAM setup is not a demo
anyone else can reproduce.&lt;/p&gt;

&lt;p&gt;The stack below runs on a laptop, resets with one command, and gives the three table
formats a shared catalog and a shared object store, which is exactly the environment
where the interesting differences between them show up.&lt;/p&gt;

&lt;h2 id=&quot;what-is-in-it&quot;&gt;What is in it&lt;/h2&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
    subgraph ingest[&quot;ingestion&quot;]
      PG[(Postgres)] --&amp;gt; DBZ[Debezium / Kafka Connect]
      DBZ --&amp;gt; K[Kafka]
    end
    subgraph compute[&quot;compute&quot;]
      SM[Spark master] --- SW[Spark worker]
      TR[Trino]
      FL[Flink]
    end
    subgraph meta[&quot;catalog&quot;]
      HMS[Hive Metastore]
    end
    subgraph store[&quot;storage&quot;]
      MO[(MinIO, S3 API)]
    end
    K --&amp;gt; SM
    SM --&amp;gt; HMS
    TR --&amp;gt; HMS
    FL --&amp;gt; HMS
    SM --&amp;gt; MO
    TR --&amp;gt; MO
    HMS --&amp;gt; MO
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;MinIO is the S3 endpoint, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;s3a://warehouse/...&lt;/code&gt; in a demo is the same path shape you
would write against real object storage. One Hive Metastore is shared by Spark, Trino
and Flink, which is what makes a table written by one engine readable by another without
extra registration.&lt;/p&gt;

&lt;h2 id=&quot;getting-it-running&quot;&gt;Getting it running&lt;/h2&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;git clone https://github.com/rangareddy/datalake-docker.git
&lt;span class=&quot;nb&quot;&gt;cd &lt;/span&gt;datalake-docker

&lt;span class=&quot;c&quot;&gt;# build the images you need - this is the slow part, once&lt;/span&gt;
&lt;span class=&quot;nv&quot;&gt;IMAGES&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;spark &lt;span class=&quot;nv&quot;&gt;SPARK_VERSION&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;4.1.3 ./docker_build/build_docker_images.sh

&lt;span class=&quot;c&quot;&gt;# start the stack on the same pin&lt;/span&gt;
&lt;span class=&quot;nv&quot;&gt;SPARK_VERSION&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;4.1.3 sh docker_run/run_datalake.sh start
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;IMAGES&lt;/code&gt; limits the build to a subset. Building all eight images takes a long time
because the XTable image compiles from source, and most demos need only Spark.&lt;/p&gt;

&lt;p&gt;Check what came up:&lt;/p&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;sh docker_run/run_datalake.sh status
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;minio           Up (healthy)
hive-metastore  Up (healthy)
spark-master    Up (healthy)
spark-worker    Up (healthy)
postgres        Up (healthy)
kafka           Up (healthy)
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;choosing-a-spark-line&quot;&gt;Choosing a Spark line&lt;/h2&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SPARK_VERSION&lt;/code&gt; selects a profile, and the rest of the matrix is derived from it, because
the Spark line dictates the Scala binary, the bundled Hadoop, and which builds of the
three connectors exist at all:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SPARK_VERSION&lt;/code&gt;&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Scala&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Hadoop&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Hudi&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Iceberg&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Delta&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;3.5.9&lt;/code&gt; (default)&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2.12&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3.3.4&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1.1.1&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1.11.0&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3.3.2&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.1.3&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2.13&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3.4.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;—&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1.11.0&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;—&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Any other Spark line is refused rather than built against a guessed Scala binary.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The Spark 4.1 image ships Iceberg only&lt;/strong&gt;, and that is not a version-table inference —
both formats were built against Spark 4.1.3 and run:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Hudi 1.2.0    NoClassDefFoundError: org/apache/parquet/variant/VariantConverters
Delta 4.0.0   NoSuchMethodError: org.apache.spark.internal.LogKey.$init$
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Spark 4.1.3 ships &lt;strong&gt;Parquet 1.16.0&lt;/strong&gt;, which dropped &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VariantConverters&lt;/code&gt; — it is in no
Spark jar — while &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi-spark4.1-bundle&lt;/code&gt; is compiled against 1.15.x and bundles parquet
classes that still reference it. Delta’s failure is Spark 4.1 changing an internal trait,
and Delta publishes no Scala 2.13 build past 4.0.0.&lt;/p&gt;

&lt;p&gt;Rather than ship jars that fail on the first statement, the 4.1 image leaves them out and
the smoke test reports them as skipped. &lt;strong&gt;Use the 3.5.9 profile when you need all three
formats.&lt;/strong&gt; When Hudi and Delta publish Spark 4.1 builds, filling in two version numbers is
the whole change.&lt;/p&gt;

&lt;p&gt;One consequence of the Spark 4 line is worth knowing if you build images yourself:
&lt;strong&gt;Hadoop 3.4 switched S3A from AWS SDK v1 to v2&lt;/strong&gt;. Hadoop 3.3.x wants
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;com.amazonaws:aws-java-sdk-bundle&lt;/code&gt;; 3.4.x wants &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;software.amazon.awssdk:bundle&lt;/code&gt;.
Shipping the wrong one gives a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ClassNotFoundException&lt;/code&gt; on the first &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;s3a://&lt;/code&gt; call. The
build script now caches the jars per Hadoop version and stages a clean directory for the
image, so switching profiles does not leave the previous SDK behind on the classpath.&lt;/p&gt;

&lt;h2 id=&quot;running-the-demos&quot;&gt;Running the demos&lt;/h2&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;docker &lt;span class=&quot;nb&quot;&gt;cp &lt;/span&gt;demos spark-master:/opt/demos
docker &lt;span class=&quot;nb&quot;&gt;exec&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;-it&lt;/span&gt; spark-master bash /opt/demos/smoke_test_formats.sh
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;On the Spark 4.1 profile:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;== environment ==
  spark        version 4.1.3
  scala        2.13
  hudi         unset
  iceberg      1.11.0
  delta        unset
  java         &quot;17.0.15&quot;
== iceberg ==
  PASS  iceberg write/update/delete: ROWS=1 DEPT=Analytics
  PASS  iceberg snapshot history: SNAPS=3
== hudi ==
  SKIP  hudi write/update: not shipped on this Spark line
== delta ==
  SKIP  delta write/update/delete: not shipped on this Spark line

== summary ==
  passed 2, failed 0, skipped 2
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is the check to run after building an image. On the 3.5.9 profile the same script
exercises all three formats instead, with no skips.&lt;/p&gt;

&lt;p&gt;Individual demos go through a wrapper that supplies the jars and catalog configuration
each format needs:&lt;/p&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;docker &lt;span class=&quot;nb&quot;&gt;exec&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;-it&lt;/span&gt; spark-master bash /opt/demos/run_demo.sh iceberg/cow_vs_mor.py
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The wrapper infers the format from the first path component, so a demo script contains
only its own logic, and jar names are resolved with globs rather than pinned versions —
which is what lets the same demo run on both Spark profiles unchanged.&lt;/p&gt;

&lt;h2 id=&quot;demos-map-to-posts&quot;&gt;The demos map to the posts&lt;/h2&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Demo&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Post&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;iceberg/cow_vs_mor.py&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;/IcebergCopyOnWriteMergeOnRead/&quot;&gt;Copy-on-write or merge-on-read&lt;/a&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;iceberg/time_travel.py&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;/IcebergTimeTravel/&quot;&gt;Time travel and rollback&lt;/a&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;iceberg/schema_and_partition_evolution.py&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;/IcebergSchemaEvolution/&quot;&gt;Schema evolution&lt;/a&gt; and &lt;a href=&quot;/IcebergPartitioning/&quot;&gt;hidden partitioning&lt;/a&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;smoke_test_formats.sh&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;/OpenTableFormats/&quot;&gt;Open table formats in practice&lt;/a&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h3 id=&quot;the-numbers-come-out-the-same&quot;&gt;The numbers come out the same&lt;/h3&gt;

&lt;p&gt;This is the part worth checking rather than trusting. The copy-on-write versus
merge-on-read post measured its file accounting on a standalone Spark 4.1.3 with Iceberg
1.11.0. Running the same demo inside the stack:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;  copy-on-write before        data=2f/4000r                            query returns 4000
  copy-on-write after DELETE  data=2f/3900r                            query returns 3900
  snapshot ops                [&apos;append&apos;, &apos;overwrite&apos;]
  merge-on-read before        data=2f/4000r                            query returns 4000
  merge-on-read after DELETE  data=2f/4000r  position-delete=1f/100r   query returns 3900
  snapshot ops                [&apos;append&apos;, &apos;delete&apos;]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Identical to the post: copy-on-write rewrote to 3,900 records under an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt;
snapshot, merge-on-read kept 4,000 records and added a 100-record delete file under a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt; snapshot, and both queries return 3,900 rows. The same demo produces the same
numbers on the 3.5.9 profile, which is the useful property — the claims are about Iceberg,
not about one Spark build.&lt;/p&gt;

&lt;p&gt;The evolution demo reproduces its post the same way:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;== schema evolution ==
  rows still readable   9000
  NULL currency rows    9000
  data files 3 -&amp;gt; 3  (unchanged = no rewrite)
== partition evolution ==
  spec_id per data file [1, 0, 0]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Three data files before the schema change and three after, and two of the three files
still carrying the old partition spec.&lt;/p&gt;

&lt;h2 id=&quot;things-that-cost-time&quot;&gt;Things that cost me time&lt;/h2&gt;

&lt;p&gt;Four, each found by running it rather than reading about it.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Hudi’s first write is slow, and it looks like a hang.&lt;/strong&gt; On the 3.5 profile, creating a
table and inserting into it for the first time bootstraps Hudi’s metadata table, which
took about twenty seconds on a cold stack against MinIO. A smoke test run immediately
after &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;start&lt;/code&gt; read that as a failure; the same test passed on a second run with no change.
If you are timing anything Hudi-related, warm the table first.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;A connector that “has a 4.1 build” is not the same as one that runs on 4.1.&lt;/strong&gt;
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi-spark4.1-bundle_2.13&lt;/code&gt; exists on Maven Central and installs cleanly, and it still
fails at the first statement against Spark 4.1.3. Checking that an artifact exists is not
the same test as running it, which is the whole reason the smoke test exists.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;minio/minio&lt;/code&gt; no longer resolves on Docker Hub.&lt;/strong&gt; The stack could not start at all
until the compose files were pointed at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;quay.io/minio/minio&lt;/code&gt;. If you have an older
checkout and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;start&lt;/code&gt; fails with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pull access denied for minio/minio&lt;/code&gt;, that is why.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;A failed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;apt&lt;/code&gt; can still produce an image.&lt;/strong&gt; The Spark image’s package layer was
failing against a Debian mirror whose index advertised package versions its pool no
longer had. The build carried on, because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;wget&lt;/code&gt; already exists in the base image and
every later layer kept working — and the breakage only surfaced at runtime as
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;java: command not found&lt;/code&gt;. The layer now verifies &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;java -version&lt;/code&gt; before it finishes, so
the failure happens where it is diagnosable.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Version-agnostic jar globs matter more than they look.&lt;/strong&gt; The README said
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ls $HUDI_HOME/hudi-spark3.5-bundle_*.jar&lt;/code&gt;, which resolves to nothing on a Spark 4 image
where the bundle is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi-spark4.0-bundle_2.13&lt;/code&gt;. Every demo now globs
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi-spark*-bundle_*.jar&lt;/code&gt;.&lt;/p&gt;

&lt;h2 id=&quot;resetting&quot;&gt;Resetting and cleaning up&lt;/h2&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;sh docker_run/run_datalake.sh stop
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;stop&lt;/code&gt; does a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;down&lt;/code&gt; without &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-v&lt;/code&gt;, so the Postgres data directory and the MinIO object
store under &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;docker_run/data/&lt;/code&gt; survive. That is usually what you want between runs and
occasionally a surprise — for a genuinely clean slate, delete that directory.&lt;/p&gt;

&lt;p&gt;Individual demos are safe to re-run: each drops and recreates its own tables, so a
second run is not polluted by the first.&lt;/p&gt;

&lt;h2 id=&quot;what-it-does-not-cover&quot;&gt;What this does not cover&lt;/h2&gt;

&lt;p&gt;The stack is a single node with one writer. It will not show you concurrent-writer
behaviour at scale, S3 throttling, cross-region latency, or anything about IAM, because
MinIO has none of them. The Spark-internals measurements in these posts —
&lt;a href=&quot;/SparkShuffleInternals/&quot;&gt;shuffle internals&lt;/a&gt; and
&lt;a href=&quot;/SparkPerformanceOptimization/&quot;&gt;performance tuning&lt;/a&gt; — were
taken against a plain Spark container rather than this stack, because they are about the
engine rather than the lakehouse around it.&lt;/p&gt;

&lt;p&gt;What it does give you is a correct, reproducible environment for the table-format
behaviour, which is where most of the surprises live.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/rangareddy/datalake-docker&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;datalake-docker&lt;/code&gt;&lt;/a&gt; — the stack, the demos and the build script&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/&quot;&gt;Apache Iceberg documentation&lt;/a&gt; for the Spark procedures the demos call&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://hudi.apache.org/docs/quick-start-guide&quot;&gt;Apache Hudi Spark guide&lt;/a&gt; for the table properties Hudi requires&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.delta.io/latest/index.html&quot;&gt;Delta Lake documentation&lt;/a&gt; for the Scala 2.13 build matrix&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergLocalPlayground/&quot;&gt;A local Iceberg playground&lt;/a&gt; for a smaller three-service alternative when you only need Iceberg&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Hudi, Apache Spark, Apache Hive, Apache Kafka, Apache Flink,
Apache Parquet, Apache and the Apache feather logo are either registered trademarks or
trademarks of The Apache Software Foundation in the United States and other countries.
Delta Lake is a trademark of the Linux Foundation. MinIO is a trademark of MinIO, Inc.
Trino is a trademark of the Trino Software Foundation.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Lakehouse" /><category term="Docker" /><category term="Iceberg" /><category term="Hudi" /><category term="Delta" /><summary type="html">Why a local stack What is in it Getting it running Choosing a Spark line Running the demos The demos map to the posts The numbers come out the same Things that cost me time Resetting and cleaning up What this does not cover References Trademarks TL;DR datalake-docker brings up MinIO, a Hive Metastore, Spark, Kafka and Trino as one Compose project, so the demos need no cloud account. It builds on two Spark lines: SPARK_VERSION=3.5.9 (Scala 2.12) and SPARK_VERSION=4.1.3 (Scala 2.13). Everything else follows from that choice. demos/ holds scripts that reproduce the posts’ measurements. The copy-on-write versus merge-on-read figures came out identical here to the numbers in that post. The Spark 4.1 image ships Iceberg only. Hudi and Delta have no build that runs on Spark 4.1 yet, and both failures are concrete rather than theoretical — measured below. Hudi’s first write to a new table bootstraps its metadata table and takes around twenty seconds. A smoke test run immediately after start can read that as a failure. Why a local stack Every measured claim in these posts — the file counts in copy-on-write versus merge-on-read, the snapshot arithmetic, the spill numbers — came from running something, not from recalling it. That is only sustainable if running something is cheap. A cloud account is not cheap in the way that matters here: it is slow to reset, it costs money to leave running, and a demo that depends on your IAM setup is not a demo anyone else can reproduce. The stack below runs on a laptop, resets with one command, and gives the three table formats a shared catalog and a shared object store, which is exactly the environment where the interesting differences between them show up. What is in it flowchart TB subgraph ingest[&quot;ingestion&quot;] PG[(Postgres)] --&amp;gt; DBZ[Debezium / Kafka Connect] DBZ --&amp;gt; K[Kafka] end subgraph compute[&quot;compute&quot;] SM[Spark master] --- SW[Spark worker] TR[Trino] FL[Flink] end subgraph meta[&quot;catalog&quot;] HMS[Hive Metastore] end subgraph store[&quot;storage&quot;] MO[(MinIO, S3 API)] end K --&amp;gt; SM SM --&amp;gt; HMS TR --&amp;gt; HMS FL --&amp;gt; HMS SM --&amp;gt; MO TR --&amp;gt; MO HMS --&amp;gt; MO MinIO is the S3 endpoint, so s3a://warehouse/... in a demo is the same path shape you would write against real object storage. One Hive Metastore is shared by Spark, Trino and Flink, which is what makes a table written by one engine readable by another without extra registration. Getting it running git clone https://github.com/rangareddy/datalake-docker.git cd datalake-docker # build the images you need - this is the slow part, once IMAGES=spark SPARK_VERSION=4.1.3 ./docker_build/build_docker_images.sh # start the stack on the same pin SPARK_VERSION=4.1.3 sh docker_run/run_datalake.sh start IMAGES limits the build to a subset. Building all eight images takes a long time because the XTable image compiles from source, and most demos need only Spark. Check what came up: sh docker_run/run_datalake.sh status minio Up (healthy) hive-metastore Up (healthy) spark-master Up (healthy) spark-worker Up (healthy) postgres Up (healthy) kafka Up (healthy) Choosing a Spark line SPARK_VERSION selects a profile, and the rest of the matrix is derived from it, because the Spark line dictates the Scala binary, the bundled Hadoop, and which builds of the three connectors exist at all: SPARK_VERSION Scala Hadoop Hudi Iceberg Delta 3.5.9 (default) 2.12 3.3.4 1.1.1 1.11.0 3.3.2 4.1.3 2.13 3.4.2 — 1.11.0 — Any other Spark line is refused rather than built against a guessed Scala binary. The Spark 4.1 image ships Iceberg only, and that is not a version-table inference — both formats were built against Spark 4.1.3 and run: Hudi 1.2.0 NoClassDefFoundError: org/apache/parquet/variant/VariantConverters Delta 4.0.0 NoSuchMethodError: org.apache.spark.internal.LogKey.$init$ Spark 4.1.3 ships Parquet 1.16.0, which dropped VariantConverters — it is in no Spark jar — while hudi-spark4.1-bundle is compiled against 1.15.x and bundles parquet classes that still reference it. Delta’s failure is Spark 4.1 changing an internal trait, and Delta publishes no Scala 2.13 build past 4.0.0. Rather than ship jars that fail on the first statement, the 4.1 image leaves them out and the smoke test reports them as skipped. Use the 3.5.9 profile when you need all three formats. When Hudi and Delta publish Spark 4.1 builds, filling in two version numbers is the whole change. One consequence of the Spark 4 line is worth knowing if you build images yourself: Hadoop 3.4 switched S3A from AWS SDK v1 to v2. Hadoop 3.3.x wants com.amazonaws:aws-java-sdk-bundle; 3.4.x wants software.amazon.awssdk:bundle. Shipping the wrong one gives a ClassNotFoundException on the first s3a:// call. The build script now caches the jars per Hadoop version and stages a clean directory for the image, so switching profiles does not leave the previous SDK behind on the classpath. Running the demos docker cp demos spark-master:/opt/demos docker exec -it spark-master bash /opt/demos/smoke_test_formats.sh On the Spark 4.1 profile: == environment == spark version 4.1.3 scala 2.13 hudi unset iceberg 1.11.0 delta unset java &quot;17.0.15&quot; == iceberg == PASS iceberg write/update/delete: ROWS=1 DEPT=Analytics PASS iceberg snapshot history: SNAPS=3 == hudi == SKIP hudi write/update: not shipped on this Spark line == delta == SKIP delta write/update/delete: not shipped on this Spark line == summary == passed 2, failed 0, skipped 2 That is the check to run after building an image. On the 3.5.9 profile the same script exercises all three formats instead, with no skips. Individual demos go through a wrapper that supplies the jars and catalog configuration each format needs: docker exec -it spark-master bash /opt/demos/run_demo.sh iceberg/cow_vs_mor.py The wrapper infers the format from the first path component, so a demo script contains only its own logic, and jar names are resolved with globs rather than pinned versions — which is what lets the same demo run on both Spark profiles unchanged. The demos map to the posts Demo Post iceberg/cow_vs_mor.py Copy-on-write or merge-on-read iceberg/time_travel.py Time travel and rollback iceberg/schema_and_partition_evolution.py Schema evolution and hidden partitioning smoke_test_formats.sh Open table formats in practice The numbers come out the same This is the part worth checking rather than trusting. The copy-on-write versus merge-on-read post measured its file accounting on a standalone Spark 4.1.3 with Iceberg 1.11.0. Running the same demo inside the stack: copy-on-write before data=2f/4000r query returns 4000 copy-on-write after DELETE data=2f/3900r query returns 3900 snapshot ops [&apos;append&apos;, &apos;overwrite&apos;] merge-on-read before data=2f/4000r query returns 4000 merge-on-read after DELETE data=2f/4000r position-delete=1f/100r query returns 3900 snapshot ops [&apos;append&apos;, &apos;delete&apos;] Identical to the post: copy-on-write rewrote to 3,900 records under an overwrite snapshot, merge-on-read kept 4,000 records and added a 100-record delete file under a delete snapshot, and both queries return 3,900 rows. The same demo produces the same numbers on the 3.5.9 profile, which is the useful property — the claims are about Iceberg, not about one Spark build. The evolution demo reproduces its post the same way: == schema evolution == rows still readable 9000 NULL currency rows 9000 data files 3 -&amp;gt; 3 (unchanged = no rewrite) == partition evolution == spec_id per data file [1, 0, 0] Three data files before the schema change and three after, and two of the three files still carrying the old partition spec. Things that cost me time Four, each found by running it rather than reading about it. Hudi’s first write is slow, and it looks like a hang. On the 3.5 profile, creating a table and inserting into it for the first time bootstraps Hudi’s metadata table, which took about twenty seconds on a cold stack against MinIO. A smoke test run immediately after start read that as a failure; the same test passed on a second run with no change. If you are timing anything Hudi-related, warm the table first. A connector that “has a 4.1 build” is not the same as one that runs on 4.1. hudi-spark4.1-bundle_2.13 exists on Maven Central and installs cleanly, and it still fails at the first statement against Spark 4.1.3. Checking that an artifact exists is not the same test as running it, which is the whole reason the smoke test exists. minio/minio no longer resolves on Docker Hub. The stack could not start at all until the compose files were pointed at quay.io/minio/minio. If you have an older checkout and start fails with pull access denied for minio/minio, that is why. A failed apt can still produce an image. The Spark image’s package layer was failing against a Debian mirror whose index advertised package versions its pool no longer had. The build carried on, because wget already exists in the base image and every later layer kept working — and the breakage only surfaced at runtime as java: command not found. The layer now verifies java -version before it finishes, so the failure happens where it is diagnosable. Version-agnostic jar globs matter more than they look. The README said ls $HUDI_HOME/hudi-spark3.5-bundle_*.jar, which resolves to nothing on a Spark 4 image where the bundle is hudi-spark4.0-bundle_2.13. Every demo now globs hudi-spark*-bundle_*.jar. Resetting and cleaning up sh docker_run/run_datalake.sh stop stop does a down without -v, so the Postgres data directory and the MinIO object store under docker_run/data/ survive. That is usually what you want between runs and occasionally a surprise — for a genuinely clean slate, delete that directory. Individual demos are safe to re-run: each drops and recreates its own tables, so a second run is not polluted by the first. What this does not cover The stack is a single node with one writer. It will not show you concurrent-writer behaviour at scale, S3 throttling, cross-region latency, or anything about IAM, because MinIO has none of them. The Spark-internals measurements in these posts — shuffle internals and performance tuning — were taken against a plain Spark container rather than this stack, because they are about the engine rather than the lakehouse around it. What it does give you is a correct, reproducible environment for the table-format behaviour, which is where most of the surprises live. References datalake-docker — the stack, the demos and the build script Apache Iceberg documentation for the Spark procedures the demos call Apache Hudi Spark guide for the table properties Hudi requires Delta Lake documentation for the Scala 2.13 build matrix A local Iceberg playground for a smaller three-service alternative when you only need Iceberg Trademarks Apache Iceberg, Apache Hudi, Apache Spark, Apache Hive, Apache Kafka, Apache Flink, Apache Parquet, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries. Delta Lake is a trademark of the Linux Foundation. MinIO is a trademark of MinIO, Inc. Trino is a trademark of the Trino Software Foundation.</summary></entry><entry><title type="html">Running Iceberg in production: file sizes, object-storage paths, and the jobs you must schedule</title><link href="https://rangareddy.github.io/IcebergProductionGuide/" rel="alternate" type="text/html" title="Running Iceberg in production: file sizes, object-storage paths, and the jobs you must schedule" /><published>2026-09-17T10:00:00+05:30</published><updated>2026-09-17T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergProductionGuide</id><content type="html" xml:base="https://rangareddy.github.io/IcebergProductionGuide/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#what-degrades&quot; id=&quot;markdown-toc-what-degrades&quot;&gt;What actually degrades&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#object-storage-paths&quot; id=&quot;markdown-toc-object-storage-paths&quot;&gt;Object-storage paths&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#target-file-size&quot; id=&quot;markdown-toc-target-file-size&quot;&gt;Target file size&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#sizing-files&quot; id=&quot;markdown-toc-sizing-files&quot;&gt;Sizing files against your query shape&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#monitoring-queries&quot; id=&quot;markdown-toc-monitoring-queries&quot;&gt;A monitoring query set&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#where-cost-goes&quot; id=&quot;markdown-toc-where-cost-goes&quot;&gt;Where the cost actually goes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-checklist&quot; id=&quot;markdown-toc-production-checklist&quot;&gt;A production checklist&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-three-jobs&quot; id=&quot;markdown-toc-the-three-jobs&quot;&gt;The three jobs&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#metadata-retention&quot; id=&quot;markdown-toc-metadata-retention&quot;&gt;Metadata retention, which is separate&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-catalog-decision&quot; id=&quot;markdown-toc-the-catalog-decision&quot;&gt;The catalog decision&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#properties-at-create-time&quot; id=&quot;markdown-toc-properties-at-create-time&quot;&gt;Properties worth setting at create time&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#automating-it&quot; id=&quot;markdown-toc-automating-it&quot;&gt;Automating it&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.object-storage.enabled&lt;/code&gt; changes data paths to carry a &lt;strong&gt;hashed prefix&lt;/strong&gt; — the measured table wrote to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0111/0110/0011/10101101/...&lt;/code&gt;. That distributes keys across object-store partitions instead of concentrating them under one date prefix.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.target-file-size-bytes&lt;/code&gt; is a target the writer aims at, not a guarantee. A 1 MiB target produced files of &lt;strong&gt;124,966&lt;/strong&gt; and &lt;strong&gt;496,738&lt;/strong&gt; bytes from one write.&lt;/li&gt;
    &lt;li&gt;Three jobs must be scheduled from day one: compaction, delete-file compaction, and snapshot expiry. Each fixes something the others do not.&lt;/li&gt;
    &lt;li&gt;The catalog is the production decision that is hardest to reverse. A path-based catalog cannot commit safely on plain object storage.&lt;/li&gt;
    &lt;li&gt;Metadata files accumulate independently of snapshots, and are bounded by their own settings.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;what-degrades&quot;&gt;What actually degrades&lt;/h2&gt;

&lt;p&gt;An Iceberg table rarely fails. It degrades, in four ways that have different
causes and different fixes, and telling them apart is most of production
operation.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Symptom&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Cause&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scans slow, data volume flat&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;too many small data files&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scans slow on a CDC table&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;delete files accumulating&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_position_delete_files&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Planning slow before any task starts&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;manifests and metadata accumulating&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_manifests&lt;/code&gt;, metadata retention&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Storage grows, row count flat&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;snapshots never expired&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;expire_snapshots&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Throttling or 503s from object storage&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;key prefixes concentrated&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.object-storage.enabled&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Notice that “the query got slow” maps to three different causes. The way to tell
them apart is where the time goes: planning time before tasks are scheduled
points at metadata; task time points at data files or delete files.&lt;/p&gt;

&lt;h2 id=&quot;object-storage-paths&quot;&gt;Object-storage paths&lt;/h2&gt;

&lt;p&gt;The default layout writes data under a directory derived from partition values:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;data/pickup_day=2026-02-10/00000-2-....parquet
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is readable, and on S3 it is a problem at scale. Object stores partition
their keyspace by prefix, so a table whose keys all begin
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;data/pickup_day=2026-02-10/&lt;/code&gt; concentrates a day’s requests onto one internal
partition. The symptom is throttling — 503 responses under load — on a table that
looks perfectly healthy.&lt;/p&gt;

&lt;p&gt;Setting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.object-storage.enabled&lt;/code&gt; changes the path:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;prod&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;BIGINT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;STRING&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;write.object-storage.enabled&apos;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;true&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The measured result:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;0111/0110/0011/10101101/00000-66-7fc327bf-3edb-40a3-b09f-686852b...
1011/1010/0010/00000001/00000-66-7fc327bf-3edb-40a3-b09f-686852b...
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Those leading binary components are a hash of the file path&lt;/strong&gt;, inserted before
the rest of the location. Two files written in the same operation landed under
completely different prefixes, which is the point: requests spread across the
keyspace instead of queuing behind one.&lt;/p&gt;

&lt;p&gt;The cost is that the layout is no longer human-navigable. You cannot list a
partition’s directory to see its files, because the files are not in one. The
metadata knows where everything is, which is the only consumer that matters, but
it does change how you debug.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Turn this on for tables on S3 or GCS that take real query concurrency.&lt;/strong&gt; Leave
it off for small tables, local filesystems, and anywhere a human browsing the
layout is worth more than the throughput.&lt;/p&gt;

&lt;h2 id=&quot;target-file-size&quot;&gt;Target file size&lt;/h2&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;write.target-file-size-bytes&apos;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;1048576&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Setting a 1 MiB target and writing 400,000 rows produced:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;files written = 2
   124966 bytes    81000 rows
   496738 bytes   319000 rows
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Neither file hit the target, and they differ from each other by a factor of
four.&lt;/strong&gt; This is worth internalising: the property is an input to the writer’s
decisions, not a contract. Actual sizes depend on how the data was partitioned
coming in, how well it compressed, and where the writer chose to split.&lt;/p&gt;

&lt;p&gt;The practical reading: &lt;strong&gt;target file size shapes compaction more reliably than it
shapes ingestion.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; bin-packs toward the target and gets
close, because it is reading existing files and has freedom to choose boundaries.
An ingesting writer is bounded by the partitions it was handed.&lt;/p&gt;

&lt;p&gt;For production the usual target is 128 MiB to 512 MiB — large enough that
per-file overhead is amortised, small enough for useful parallelism. The default
is 512 MiB, and the reason to lower it is a table that is read with very
selective filters, where smaller files prune better.&lt;/p&gt;

&lt;h2 id=&quot;sizing-files&quot;&gt;Sizing files against your query shape&lt;/h2&gt;

&lt;p&gt;The default target is 512 MiB, and the right value depends on how selectively
your queries read.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Query shape&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Target&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Full scans and large aggregations&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;512 MiB – 1 GiB&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;per-file overhead amortised&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Selective filters with good pruning&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;128 – 256 MiB&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;smaller files prune more precisely&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Frequent row-level deletes under copy-on-write&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;128 MiB&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;a delete rewrites less&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming ingestion, compacted hourly&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;256 MiB&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;compaction hits the target reliably&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Measure what you actually have rather than what you configured, since the
property is a target and not a guarantee:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;round&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;min&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;file_size_in_bytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;/&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1048576&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;  &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;min_mb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;round&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;avg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;file_size_in_bytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;/&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1048576&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;  &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;avg_mb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;round&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;file_size_in_bytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;/&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1048576&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;  &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;max_mb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;round&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;percentile_approx&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;file_size_in_bytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;/&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1048576&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p50_mb&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A median far below the target means compaction is not keeping pace with
ingestion; a wide spread between min and max means partitions are receiving very
different volumes and a single global target is the wrong instrument.&lt;/p&gt;

&lt;h2 id=&quot;monitoring-queries&quot;&gt;A monitoring query set&lt;/h2&gt;

&lt;p&gt;Five queries cover the failure modes. Run them on a schedule and alert on trends
rather than absolutes.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- 1. file health: small files and delete accumulation in one row&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;n&quot;&gt;round&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;avg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;file_size_in_bytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;/&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1048576&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;avg_mb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;records&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- 2. metadata health: what planning must read&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;manifests&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;round&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;length&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;/&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1048576&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;manifest_mb&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;manifests&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- 3. history: is expiry running?&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;min&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;oldest&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;newest&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- 4. commit rate: the predictor of every metadata problem&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;date_trunc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;day&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;day&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;commits&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DESC&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;LIMIT&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;14&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- 5. metadata file accumulation, which expiry does not bound&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;metadata_files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;metadata_log_entries&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Query 4 is the leading indicator.&lt;/strong&gt; Commits per day predicts manifests,
metadata files and snapshot count; watching it catches problems weeks before
anything is slow.&lt;/p&gt;

&lt;h2 id=&quot;where-cost-goes&quot;&gt;Where the cost actually goes&lt;/h2&gt;

&lt;p&gt;Object storage bills on three axes and Iceberg touches all three differently.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Storage&lt;/strong&gt; is dominated by unexpired snapshots, not by live data. A table
compacting nightly and never expiring holds every pre-compaction generation.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;expire_snapshots&lt;/code&gt; is the only lever, and its retention is your recovery window.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Request count&lt;/strong&gt; is dominated by file count and by listing. Iceberg never lists
directories during planning, which removes the Hive-era cost entirely, but small
files still mean more GETs per scan. This is what &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.object-storage.enabled&lt;/code&gt;
addresses for throttling and what compaction addresses for volume.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Egress and compute&lt;/strong&gt; follow bytes scanned, which is what pruning controls —
partition transforms matching your filters, and sort orders narrowing column
bounds.&lt;/p&gt;

&lt;p&gt;A concrete shape: a table with 10,000 small files scanned fully costs 10,000
GETs; compacted to 200 files it costs 200. Neither changes the bytes, and both
change the bill and the latency.&lt;/p&gt;

&lt;h2 id=&quot;production-checklist&quot;&gt;A production checklist&lt;/h2&gt;

&lt;p&gt;Before a table carries real traffic:&lt;/p&gt;

&lt;ul class=&quot;task-list&quot;&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;Catalog&lt;/strong&gt; is not path-based if storage is an object store&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;format-version&lt;/code&gt;&lt;/strong&gt; set deliberately, and every reader supports it&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;Partition transform&lt;/strong&gt; matches the filters queries actually use&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.target-file-size-bytes&lt;/code&gt;&lt;/strong&gt; set for the query shape&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.object-storage.enabled&lt;/code&gt;&lt;/strong&gt; on, for S3 or GCS&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.*.mode&lt;/code&gt;&lt;/strong&gt; chosen per operation, not left to the default&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;Sort order&lt;/strong&gt; declared if filters go beyond the partition column&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.metadata.delete-after-commit.enabled&lt;/code&gt;&lt;/strong&gt; on, with a version cap&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;Compaction&lt;/strong&gt; scheduled, scoped with a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;where&lt;/code&gt; clause&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_position_delete_files&lt;/code&gt;&lt;/strong&gt; scheduled if merge-on-read&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;expire_snapshots&lt;/code&gt;&lt;/strong&gt; scheduled, retention = recovery window&lt;/li&gt;
  &lt;li class=&quot;task-list-item&quot;&gt;&lt;input type=&quot;checkbox&quot; class=&quot;task-list-item-checkbox&quot; disabled=&quot;disabled&quot; /&gt;&lt;strong&gt;Monitoring&lt;/strong&gt; on commit rate and the delete-to-data ratio&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The ordering matters: the first six are decisions that are expensive to change
later, and the last six are jobs that are cheap to add at any time but costly to
have omitted for months.&lt;/p&gt;

&lt;h2 id=&quot;the-three-jobs&quot;&gt;The three jobs&lt;/h2&gt;

&lt;p&gt;These are not optional and they are not the same job.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Compaction&lt;/strong&gt; merges small data files:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_data_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;options&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;min-input-files&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;5&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;target-file-size-bytes&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;536870912&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;));&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Delete-file compaction&lt;/strong&gt; applies and clears delete files, which data compaction
does not fully do:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_position_delete_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Expiry&lt;/strong&gt; is the only one that reclaims storage:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;expire_snapshots&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;older_than&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;TIMESTAMP&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;2026-09-17 00:00:00&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;retain_last&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Compaction without expiry grows storage while improving query time, because the
pre-compaction files stay referenced by older snapshots. That accounting, measured
file by file, is in
&lt;a href=&quot;/TableMaintenance/&quot;&gt;table maintenance for Iceberg and Hudi&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;A fourth, run when planning is slow rather than on a schedule:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_manifests&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Retention is a recovery decision, not a cleanup decision.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;older_than&lt;/code&gt; sets
how far back you can time travel and therefore how far back you can undo a bad
write. Seven days of snapshots is seven days of insurance and seven days of
storage for every file those snapshots reference. Decide it as a recovery policy
and let the storage follow.&lt;/p&gt;

&lt;h2 id=&quot;metadata-retention&quot;&gt;Metadata retention, which is separate&lt;/h2&gt;

&lt;p&gt;Every commit writes a new &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;metadata.json&lt;/code&gt;. Expiring snapshots does not remove
them; they have their own settings:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.metadata.delete-after-commit.enabled&apos;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;true&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.metadata.previous-versions-max&apos;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;100&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Without these, a table committing every minute accumulates metadata files
indefinitely — the directory grows even though snapshots are being expired on
schedule. This is the specific fix for a metadata directory with tens of
thousands of JSON files, and it is easy to miss because it looks like the
snapshot problem and is not.&lt;/p&gt;

&lt;h2 id=&quot;the-catalog-decision&quot;&gt;The catalog decision&lt;/h2&gt;

&lt;p&gt;The hardest thing to change later.&lt;/p&gt;

&lt;p&gt;A catalog’s job is to answer &lt;em&gt;which metadata file is current&lt;/em&gt; and to make the
swap atomically. That atomicity is what stops two writers from losing each
other’s commits.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Catalog&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Suitable for&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Hadoop / path-based&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;local experiments, single-writer, filesystems with atomic rename&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Hive Metastore&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;existing Hive estates, single-region&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;JDBC&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;simple deployments with a database already in place&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;REST&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;the standard interface; decouples engines from the metadata backend&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;strong&gt;A path-based catalog on plain object storage is the configuration to avoid.&lt;/strong&gt; It
depends on atomic rename semantics that object stores do not offer, which turns
concurrent commits into lost commits — and it fails silently, not loudly.&lt;/p&gt;

&lt;p&gt;The REST catalog is the direction the ecosystem has settled on, because it puts
the commit protocol behind an API that any engine can speak, rather than each
engine reimplementing filesystem conventions. A local walkthrough is in
&lt;a href=&quot;/IcebergLocalPlayground/&quot;&gt;a local Iceberg playground&lt;/a&gt;,
and a production-shaped open-source implementation in
&lt;a href=&quot;/SparkIcebergPolaris/&quot;&gt;running Iceberg on Apache Polaris&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;properties-at-create-time&quot;&gt;Properties worth setting at create time&lt;/h2&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Property&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Typical&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;format-version&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;2&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;delete files; the default in current versions&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.target-file-size-bytes&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;128–512 MiB&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;amortises per-file overhead&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.object-storage.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; on S3/GCS&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;spreads keys, avoids throttling&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.parquet.compression-codec&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;zstd&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;the default; better ratio than snappy&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.delete.mode&lt;/code&gt; / &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;update&lt;/code&gt; / &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;merge&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;per workload&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;see the COW/MOR comparison&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.metadata.delete-after-commit.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;bounds metadata files&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.metadata.previous-versions-max&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;100&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;how many to keep&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Set these when the table is created. Several can be changed later, but the ones
that affect layout only apply to data written afterwards, so a table that ran for
six months with the wrong settings needs a rewrite, not an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ALTER&lt;/code&gt;.&lt;/p&gt;

&lt;h2 id=&quot;automating-it&quot;&gt;Automating it&lt;/h2&gt;

&lt;p&gt;Maintenance that depends on someone remembering is maintenance that stops. The
shape that works:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;hourly   rewrite_data_files on hot tables
         rewrite_position_delete_files on CDC tables
daily    expire_snapshots  (retention = your recovery window)
weekly   rewrite_manifests where planning is slow
monthly  remove_orphan_files with a conservative age threshold
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Two operational notes. &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;remove_orphan_files&lt;/code&gt; deletes files no snapshot
references, and a file being written right now looks exactly like an orphan&lt;/strong&gt; —
use an age threshold of days, and never run it while a long write is in flight.
And &lt;strong&gt;maintenance jobs are writers&lt;/strong&gt;: they commit, they can conflict with your
pipelines, and they need their own compute rather than competing with the
ingestion they are meant to help.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Target file size guarantees file size.”&lt;/strong&gt; The measured write produced 124,966
and 496,738 bytes against a 1 MiB target.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Compaction is maintenance.”&lt;/strong&gt; It is one of three jobs, and the only one that
reclaims storage is expiry.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Object storage mode is just a layout preference.”&lt;/strong&gt; It is the fix for prefix
throttling, and it changes the failure mode under concurrency.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Expiring snapshots is cleanup.”&lt;/strong&gt; It is deleting your recovery window. Choose
the retention as a recovery policy.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“The catalog can be swapped later.”&lt;/strong&gt; It can, with a migration. It is much
cheaper to choose correctly at the start.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;An Iceberg table has two clocks. One counts data files and delete files, and
compaction resets it. The other counts snapshots, manifests and metadata files,
and expiry and manifest rewriting reset it.&lt;/p&gt;

&lt;p&gt;Production operation is keeping both wound, choosing a catalog that can commit
safely, and setting the layout properties before the first write rather than
after the first incident.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/configuration/&quot;&gt;Iceberg configuration&lt;/a&gt; for every write property and its default&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-procedures/&quot;&gt;Iceberg Spark procedures&lt;/a&gt; for the maintenance calls above&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/aws/&quot;&gt;Iceberg AWS integration&lt;/a&gt; for object-storage paths and S3 behaviour&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/TableMaintenance/&quot;&gt;Table maintenance for Iceberg and Hudi&lt;/a&gt; for the compaction-versus-expiry accounting&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergCdcPipelines/&quot;&gt;CDC into Iceberg&lt;/a&gt; for the delete-file growth that drives most of this schedule&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Spark, Apache Hive, Apache Parquet, Apache Polaris, Apache
and the Apache feather logo are either registered trademarks or trademarks of The
Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Iceberg" /><category term="Iceberg" /><category term="Production" /><category term="Tuning" /><category term="Storage" /><summary type="html">What actually degrades Object-storage paths Target file size Sizing files against your query shape A monitoring query set Where the cost actually goes A production checklist The three jobs Metadata retention, which is separate The catalog decision Properties worth setting at create time Automating it Common misconceptions A model worth keeping References Trademarks TL;DR write.object-storage.enabled changes data paths to carry a hashed prefix — the measured table wrote to 0111/0110/0011/10101101/.... That distributes keys across object-store partitions instead of concentrating them under one date prefix. write.target-file-size-bytes is a target the writer aims at, not a guarantee. A 1 MiB target produced files of 124,966 and 496,738 bytes from one write. Three jobs must be scheduled from day one: compaction, delete-file compaction, and snapshot expiry. Each fixes something the others do not. The catalog is the production decision that is hardest to reverse. A path-based catalog cannot commit safely on plain object storage. Metadata files accumulate independently of snapshots, and are bounded by their own settings. What actually degrades An Iceberg table rarely fails. It degrades, in four ways that have different causes and different fixes, and telling them apart is most of production operation. Symptom Cause Fix Scans slow, data volume flat too many small data files rewrite_data_files Scans slow on a CDC table delete files accumulating rewrite_position_delete_files Planning slow before any task starts manifests and metadata accumulating rewrite_manifests, metadata retention Storage grows, row count flat snapshots never expired expire_snapshots Throttling or 503s from object storage key prefixes concentrated write.object-storage.enabled Notice that “the query got slow” maps to three different causes. The way to tell them apart is where the time goes: planning time before tasks are scheduled points at metadata; task time points at data files or delete files. Object-storage paths The default layout writes data under a directory derived from partition values: data/pickup_day=2026-02-10/00000-2-....parquet That is readable, and on S3 it is a problem at scale. Object stores partition their keyspace by prefix, so a table whose keys all begin data/pickup_day=2026-02-10/ concentrates a day’s requests onto one internal partition. The symptom is throttling — 503 responses under load — on a table that looks perfectly healthy. Setting write.object-storage.enabled changes the path: CREATE TABLE local.db.prod (id BIGINT, v STRING) USING iceberg TBLPROPERTIES (&apos;write.object-storage.enabled&apos;=&apos;true&apos;) The measured result: 0111/0110/0011/10101101/00000-66-7fc327bf-3edb-40a3-b09f-686852b... 1011/1010/0010/00000001/00000-66-7fc327bf-3edb-40a3-b09f-686852b... Those leading binary components are a hash of the file path, inserted before the rest of the location. Two files written in the same operation landed under completely different prefixes, which is the point: requests spread across the keyspace instead of queuing behind one. The cost is that the layout is no longer human-navigable. You cannot list a partition’s directory to see its files, because the files are not in one. The metadata knows where everything is, which is the only consumer that matters, but it does change how you debug. Turn this on for tables on S3 or GCS that take real query concurrency. Leave it off for small tables, local filesystems, and anywhere a human browsing the layout is worth more than the throughput. Target file size TBLPROPERTIES (&apos;write.target-file-size-bytes&apos;=&apos;1048576&apos;) Setting a 1 MiB target and writing 400,000 rows produced: files written = 2 124966 bytes 81000 rows 496738 bytes 319000 rows Neither file hit the target, and they differ from each other by a factor of four. This is worth internalising: the property is an input to the writer’s decisions, not a contract. Actual sizes depend on how the data was partitioned coming in, how well it compressed, and where the writer chose to split. The practical reading: target file size shapes compaction more reliably than it shapes ingestion. rewrite_data_files bin-packs toward the target and gets close, because it is reading existing files and has freedom to choose boundaries. An ingesting writer is bounded by the partitions it was handed. For production the usual target is 128 MiB to 512 MiB — large enough that per-file overhead is amortised, small enough for useful parallelism. The default is 512 MiB, and the reason to lower it is a table that is read with very selective filters, where smaller files prune better. Sizing files against your query shape The default target is 512 MiB, and the right value depends on how selectively your queries read. Query shape Target Why Full scans and large aggregations 512 MiB – 1 GiB per-file overhead amortised Selective filters with good pruning 128 – 256 MiB smaller files prune more precisely Frequent row-level deletes under copy-on-write 128 MiB a delete rewrites less Streaming ingestion, compacted hourly 256 MiB compaction hits the target reliably Measure what you actually have rather than what you configured, since the property is a target and not a guarantee: SELECT count(*) AS files, round(min(file_size_in_bytes)/1048576, 1) AS min_mb, round(avg(file_size_in_bytes)/1048576, 1) AS avg_mb, round(max(file_size_in_bytes)/1048576, 1) AS max_mb, round(percentile_approx(file_size_in_bytes, 0.5)/1048576, 1) AS p50_mb FROM local.db.events.files WHERE content = 0; A median far below the target means compaction is not keeping pace with ingestion; a wide spread between min and max means partitions are receiving very different volumes and a single global target is the wrong instrument. A monitoring query set Five queries cover the failure modes. Run them on a schedule and alert on trends rather than absolutes. -- 1. file health: small files and delete accumulation in one row SELECT content, count(*) AS files, round(avg(file_size_in_bytes)/1048576, 1) AS avg_mb, sum(record_count) AS records FROM local.db.events.files GROUP BY content ORDER BY content; -- 2. metadata health: what planning must read SELECT count(*) AS manifests, round(sum(length)/1048576, 1) AS manifest_mb FROM local.db.events.manifests; -- 3. history: is expiry running? SELECT count(*) AS snapshots, min(committed_at) AS oldest, max(committed_at) AS newest FROM local.db.events.snapshots; -- 4. commit rate: the predictor of every metadata problem SELECT date_trunc(&apos;day&apos;, committed_at) AS day, count(*) AS commits FROM local.db.events.snapshots GROUP BY 1 ORDER BY 1 DESC LIMIT 14; -- 5. metadata file accumulation, which expiry does not bound SELECT count(*) AS metadata_files FROM local.db.events.metadata_log_entries; Query 4 is the leading indicator. Commits per day predicts manifests, metadata files and snapshot count; watching it catches problems weeks before anything is slow. Where the cost actually goes Object storage bills on three axes and Iceberg touches all three differently. Storage is dominated by unexpired snapshots, not by live data. A table compacting nightly and never expiring holds every pre-compaction generation. expire_snapshots is the only lever, and its retention is your recovery window. Request count is dominated by file count and by listing. Iceberg never lists directories during planning, which removes the Hive-era cost entirely, but small files still mean more GETs per scan. This is what write.object-storage.enabled addresses for throttling and what compaction addresses for volume. Egress and compute follow bytes scanned, which is what pruning controls — partition transforms matching your filters, and sort orders narrowing column bounds. A concrete shape: a table with 10,000 small files scanned fully costs 10,000 GETs; compacted to 200 files it costs 200. Neither changes the bytes, and both change the bill and the latency. A production checklist Before a table carries real traffic: Catalog is not path-based if storage is an object store format-version set deliberately, and every reader supports it Partition transform matches the filters queries actually use write.target-file-size-bytes set for the query shape write.object-storage.enabled on, for S3 or GCS write.*.mode chosen per operation, not left to the default Sort order declared if filters go beyond the partition column write.metadata.delete-after-commit.enabled on, with a version cap Compaction scheduled, scoped with a where clause rewrite_position_delete_files scheduled if merge-on-read expire_snapshots scheduled, retention = recovery window Monitoring on commit rate and the delete-to-data ratio The ordering matters: the first six are decisions that are expensive to change later, and the last six are jobs that are cheap to add at any time but costly to have omitted for months. The three jobs These are not optional and they are not the same job. Compaction merges small data files: CALL local.system.rewrite_data_files( table =&amp;gt; &apos;db.events&apos;, options =&amp;gt; map(&apos;min-input-files&apos;,&apos;5&apos;,&apos;target-file-size-bytes&apos;,&apos;536870912&apos;)); Delete-file compaction applies and clears delete files, which data compaction does not fully do: CALL local.system.rewrite_position_delete_files(table =&amp;gt; &apos;db.events&apos;); Expiry is the only one that reclaims storage: CALL local.system.expire_snapshots( table =&amp;gt; &apos;db.events&apos;, older_than =&amp;gt; TIMESTAMP &apos;2026-09-17 00:00:00&apos;, retain_last =&amp;gt; 10); Compaction without expiry grows storage while improving query time, because the pre-compaction files stay referenced by older snapshots. That accounting, measured file by file, is in table maintenance for Iceberg and Hudi. A fourth, run when planning is slow rather than on a schedule: CALL local.system.rewrite_manifests(table =&amp;gt; &apos;db.events&apos;); Retention is a recovery decision, not a cleanup decision. older_than sets how far back you can time travel and therefore how far back you can undo a bad write. Seven days of snapshots is seven days of insurance and seven days of storage for every file those snapshots reference. Decide it as a recovery policy and let the storage follow. Metadata retention, which is separate Every commit writes a new metadata.json. Expiring snapshots does not remove them; they have their own settings: ALTER TABLE local.db.events SET TBLPROPERTIES ( &apos;write.metadata.delete-after-commit.enabled&apos;=&apos;true&apos;, &apos;write.metadata.previous-versions-max&apos;=&apos;100&apos;); Without these, a table committing every minute accumulates metadata files indefinitely — the directory grows even though snapshots are being expired on schedule. This is the specific fix for a metadata directory with tens of thousands of JSON files, and it is easy to miss because it looks like the snapshot problem and is not. The catalog decision The hardest thing to change later. A catalog’s job is to answer which metadata file is current and to make the swap atomically. That atomicity is what stops two writers from losing each other’s commits. Catalog Suitable for Hadoop / path-based local experiments, single-writer, filesystems with atomic rename Hive Metastore existing Hive estates, single-region JDBC simple deployments with a database already in place REST the standard interface; decouples engines from the metadata backend A path-based catalog on plain object storage is the configuration to avoid. It depends on atomic rename semantics that object stores do not offer, which turns concurrent commits into lost commits — and it fails silently, not loudly. The REST catalog is the direction the ecosystem has settled on, because it puts the commit protocol behind an API that any engine can speak, rather than each engine reimplementing filesystem conventions. A local walkthrough is in a local Iceberg playground, and a production-shaped open-source implementation in running Iceberg on Apache Polaris. Properties worth setting at create time Property Typical Why format-version 2 delete files; the default in current versions write.target-file-size-bytes 128–512 MiB amortises per-file overhead write.object-storage.enabled true on S3/GCS spreads keys, avoids throttling write.parquet.compression-codec zstd the default; better ratio than snappy write.delete.mode / update / merge per workload see the COW/MOR comparison write.metadata.delete-after-commit.enabled true bounds metadata files write.metadata.previous-versions-max 100 how many to keep Set these when the table is created. Several can be changed later, but the ones that affect layout only apply to data written afterwards, so a table that ran for six months with the wrong settings needs a rewrite, not an ALTER. Automating it Maintenance that depends on someone remembering is maintenance that stops. The shape that works: hourly rewrite_data_files on hot tables rewrite_position_delete_files on CDC tables daily expire_snapshots (retention = your recovery window) weekly rewrite_manifests where planning is slow monthly remove_orphan_files with a conservative age threshold Two operational notes. remove_orphan_files deletes files no snapshot references, and a file being written right now looks exactly like an orphan — use an age threshold of days, and never run it while a long write is in flight. And maintenance jobs are writers: they commit, they can conflict with your pipelines, and they need their own compute rather than competing with the ingestion they are meant to help. Common misconceptions “Target file size guarantees file size.” The measured write produced 124,966 and 496,738 bytes against a 1 MiB target. “Compaction is maintenance.” It is one of three jobs, and the only one that reclaims storage is expiry. “Object storage mode is just a layout preference.” It is the fix for prefix throttling, and it changes the failure mode under concurrency. “Expiring snapshots is cleanup.” It is deleting your recovery window. Choose the retention as a recovery policy. “The catalog can be swapped later.” It can, with a migration. It is much cheaper to choose correctly at the start. A model worth keeping An Iceberg table has two clocks. One counts data files and delete files, and compaction resets it. The other counts snapshots, manifests and metadata files, and expiry and manifest rewriting reset it. Production operation is keeping both wound, choosing a catalog that can commit safely, and setting the layout properties before the first write rather than after the first incident. References Iceberg configuration for every write property and its default Iceberg Spark procedures for the maintenance calls above Iceberg AWS integration for object-storage paths and S3 behaviour Table maintenance for Iceberg and Hudi for the compaction-versus-expiry accounting CDC into Iceberg for the delete-file growth that drives most of this schedule Trademarks Apache Iceberg, Apache Spark, Apache Hive, Apache Parquet, Apache Polaris, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Migrating Hive tables to Iceberg: snapshot first, migrate second</title><link href="https://rangareddy.github.io/IcebergHiveMigration/" rel="alternate" type="text/html" title="Migrating Hive tables to Iceberg: snapshot first, migrate second" /><published>2026-09-15T10:00:00+05:30</published><updated>2026-09-15T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergHiveMigration</id><content type="html" xml:base="https://rangareddy.github.io/IcebergHiveMigration/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#two-procedures&quot; id=&quot;markdown-toc-two-procedures&quot;&gt;Two procedures, two intentions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#snapshot-migration-measured&quot; id=&quot;markdown-toc-snapshot-migration-measured&quot;&gt;Snapshot migration, measured&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#in-place-migration&quot; id=&quot;markdown-toc-in-place-migration&quot;&gt;In-place migration, and what I could not demonstrate&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#before-you-migrate&quot; id=&quot;markdown-toc-before-you-migrate&quot;&gt;Before you migrate anything&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#migrating-in-pieces&quot; id=&quot;markdown-toc-migrating-in-pieces&quot;&gt;Migrating in pieces&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#after-the-cutover&quot; id=&quot;markdown-toc-after-the-cutover&quot;&gt;After the cutover&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#rolling-back&quot; id=&quot;markdown-toc-rolling-back&quot;&gt;Rolling back a migration&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-migration-does-not-change&quot; id=&quot;markdown-toc-what-migration-does-not-change&quot;&gt;What migration does not change&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-migration-sequence&quot; id=&quot;markdown-toc-a-migration-sequence&quot;&gt;A migration sequence that works&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#verification-worth-doing&quot; id=&quot;markdown-toc-verification-worth-doing&quot;&gt;Verification worth doing&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Neither procedure copies data.&lt;/strong&gt; Both write Iceberg metadata that points at the Parquet files already on disk, so a terabyte table converts in about the time it takes to read its file listing.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;system.snapshot&lt;/code&gt; creates a &lt;strong&gt;parallel Iceberg table&lt;/strong&gt; over the same data files. The measured run produced a 6,000-row Iceberg table while the source stayed Parquet and intact — the safe way to test before cutting over.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;system.migrate&lt;/code&gt; converts the table &lt;strong&gt;in place&lt;/strong&gt;, replacing it. It is the cutover, not the rehearsal.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; requires a catalog whose table locations it can take over. Against a &lt;strong&gt;path-based&lt;/strong&gt; session catalog it refused: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Cannot set a custom location for a path-based table&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Writes to the original table after a snapshot do &lt;strong&gt;not&lt;/strong&gt; appear in the copy. The copy is a point-in-time view of the files, not a replica.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;two-procedures&quot;&gt;Two procedures, two intentions&lt;/h2&gt;

&lt;p&gt;Converting a Hive table to Iceberg sounds like it should mean rewriting the data.
It does not. The Parquet files a Hive table points at are already the format
Iceberg uses; what is missing is the metadata tree — the manifests, the manifest
list, the snapshot, the schema with field IDs.&lt;/p&gt;

&lt;p&gt;Both procedures build that tree over the existing files. They differ in what
happens to the original.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
    H[(Hive table&amp;lt;br/&amp;gt;parquet files)] --&amp;gt;|&quot;system.snapshot&quot;| C[&quot;new Iceberg table&amp;lt;br/&amp;gt;same files, original untouched&quot;]
    H --&amp;gt;|&quot;system.migrate&quot;| M[&quot;same name, now Iceberg&amp;lt;br/&amp;gt;original replaced&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;system.snapshot&lt;/code&gt; is the rehearsal.&lt;/strong&gt; It creates a second table, under a new
name, whose metadata references the same data files. The original keeps working,
keeps serving queries, and keeps accepting writes. You point a test workload at
the copy and compare.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;system.migrate&lt;/code&gt; is the cutover.&lt;/strong&gt; The table keeps its name and becomes an
Iceberg table. Existing readers now get Iceberg semantics whether or not they
were ready for them.&lt;/p&gt;

&lt;p&gt;The order in the title is the recommendation: snapshot, validate, then migrate.&lt;/p&gt;

&lt;h2 id=&quot;snapshot-migration-measured&quot;&gt;Snapshot migration, measured&lt;/h2&gt;

&lt;p&gt;A partitioned Parquet table with 6,000 rows, created the ordinary way:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark_catalog&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshot&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;legacy_a&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;ice_copy&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;source is parquet      : Provider = parquet
source rows            = 6000
snapshot-table rows    = 6000
ice_copy provider      : Provider = iceberg
ice_copy snapshots     = 1
source still intact    = 6000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Both tables report 6,000 rows. One is Parquet, one is Iceberg, and &lt;strong&gt;there is
only one copy of the data on disk.&lt;/strong&gt; The Iceberg table has exactly one snapshot,
because its history begins at conversion.&lt;/p&gt;

&lt;p&gt;This is the state you want before a cutover. Run your real queries against
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ice_copy&lt;/code&gt;, compare results and plans against &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;legacy_a&lt;/code&gt;, and find out what
breaks while the original is still authoritative.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;What the copy does not do is track the original.&lt;/strong&gt; Rows written to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;legacy_a&lt;/code&gt;
after the snapshot will not appear in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ice_copy&lt;/code&gt; — the copy’s manifests list the
files that existed at conversion time. For a table under active write, that makes
the copy a validation target, not a mirror, and the divergence starts
immediately.&lt;/p&gt;

&lt;p&gt;That also means a snapshot copy is not a rollback plan for a migration that
happened later. It is a rollback plan only for the moment it was taken.&lt;/p&gt;

&lt;h2 id=&quot;in-place-migration&quot;&gt;In-place migration, and what I could not demonstrate&lt;/h2&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark_catalog&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;migrate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;legacy_b&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;This converts the table in place. Iceberg reads the existing partition structure
and file listing, writes the metadata tree, and swaps the table definition. In
most configurations it leaves the previous definition behind under a name
suffixed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_BACKUP_&lt;/code&gt;, so the original can be restored.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;I was not able to run this successfully, and would rather say so than describe
behaviour I did not observe.&lt;/strong&gt; Against a path-based session catalog it failed:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;IllegalArgumentException: Cannot set a custom location for a path-based table.
Expected /work/wh2/default/legacy_b but got file:/work/wh2/legacy_b
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The cause is a location convention. A Hadoop, path-based catalog derives a
table’s location from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;&amp;lt;warehouse&amp;gt;/&amp;lt;namespace&amp;gt;/&amp;lt;table&amp;gt;&lt;/code&gt;, while Spark’s own
warehouse places &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;default&lt;/code&gt;-database tables directly under the warehouse root. The
two disagree about where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;legacy_b&lt;/code&gt; lives, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; refuses rather than
guess.&lt;/p&gt;

&lt;p&gt;Configuring the session catalog as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;type=hive&lt;/code&gt; instead — the realistic setup,
since the tables being migrated are Hive tables — failed in my container for a
different reason: no metastore was available to it.&lt;/p&gt;

&lt;p&gt;So the honest summary is: &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshot&lt;/code&gt; verified end to end; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; verified
only to the point of its catalog requirement.&lt;/strong&gt; What the error does establish, and
it is the practically useful part, is that &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; needs a catalog that owns
its tables’ locations. A path-based catalog pointed at a Spark warehouse is not
that, and this is the first thing to check when the procedure refuses.&lt;/p&gt;

&lt;h2 id=&quot;before-you-migrate&quot;&gt;Before you migrate anything&lt;/h2&gt;

&lt;p&gt;Migration converts metadata, so the things that go wrong are things the old table
tolerated and the new one will not. Four checks, each cheap.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Schema compatibility.&lt;/strong&gt; Hive is permissive about types that Iceberg is strict
about, and a column typed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;string&lt;/code&gt; in the metastore but holding numbers in the
files is a problem you want to find now:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;DESCRIBE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;FORMATTED&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;legacy_sales&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;legacy_sales&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;LIMIT&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;File formats in the table.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; supports Parquet, ORC and Avro. A table
whose partitions were written in different formats over the years converts, but
verify rather than assume:&lt;/p&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;aws s3 &lt;span class=&quot;nb&quot;&gt;ls&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--recursive&lt;/span&gt; s3://warehouse/legacy_sales/ | &lt;span class=&quot;nb&quot;&gt;awk&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;{print $4}&apos;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  | &lt;span class=&quot;nb&quot;&gt;sed&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;s/.*\.//&apos;&lt;/span&gt; | &lt;span class=&quot;nb&quot;&gt;sort&lt;/span&gt; | &lt;span class=&quot;nb&quot;&gt;uniq&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;-c&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Partition column types.&lt;/strong&gt; Hive partition values are strings in the metastore.
A partition column that is logically a date arrives as a string, and you may want
it typed properly — which is a post-migration change, not something migration
does.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;File count.&lt;/strong&gt; Migration reads every Parquet footer for statistics, so time
scales with file count rather than data volume:&lt;/p&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;aws s3 &lt;span class=&quot;nb&quot;&gt;ls&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--recursive&lt;/span&gt; s3://warehouse/legacy_sales/ | &lt;span class=&quot;nb&quot;&gt;wc&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;-l&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A million small files is a long job and a strong argument for compacting &lt;em&gt;after&lt;/em&gt;
migration, not before — compaction on the Iceberg side is a single procedure.&lt;/p&gt;

&lt;h2 id=&quot;migrating-in-pieces&quot;&gt;Migrating in pieces&lt;/h2&gt;

&lt;p&gt;For a table too large or too important to convert in one step, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;add_files&lt;/code&gt; moves
partitions into a new Iceberg table incrementally, leaving the Hive table serving
throughout:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;order_id&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;BIGINT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;DECIMAL&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;STRING&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;dt&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;DATE&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;PARTITIONED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;dt&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;add_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.sales&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;source_table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;legacy_sales&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;partition_filter&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;dt&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;2026-01-06&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;));&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Loop over partitions, verify as you go, and cut readers over only when the new
table is complete. The data files are shared, so this costs metadata and not
storage.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The caution with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;add_files&lt;/code&gt;&lt;/strong&gt;: it does not check that the files match the
declared schema. A partition whose files have an extra column, or a differently
typed one, is added and fails at read time. Verify per partition:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;dt&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;date&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;2026-01-06&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;legacy_sales&lt;/span&gt;   &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;dt&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;2026-01-06&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;after-the-cutover&quot;&gt;After the cutover&lt;/h2&gt;

&lt;p&gt;Migration gives you Iceberg metadata over a Hive-shaped table. The things that
make Iceberg fast are separate jobs, and doing them after the cutover keeps each
step diagnosable.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- 1. the layout properties the table should have had&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.target-file-size-bytes&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;268435456&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.object-storage.enabled&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;true&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.parquet.compression-codec&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;zstd&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.metadata.delete-after-commit.enabled&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;true&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- 2. a sort order, if queries filter on something other than the partition&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WRITE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;ORDERED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;dt&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- 3. then a compaction that applies both&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_data_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.sales&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;options&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;min-input-files&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;5&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;target-file-size-bytes&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;268435456&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;));&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- 4. and only now, a transform-based partition layout for new data&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;REPLACE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;PARTITION&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;FIELD&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;dt&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WITH&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;days&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Step 4 is worth resisting until the rest is stable. Hidden partitioning on a
migrated table applies to new data; existing files keep the spec they were
adopted with, and a table carrying three specs during a migration is harder to
reason about than one carrying two.&lt;/p&gt;

&lt;h2 id=&quot;rolling-back&quot;&gt;Rolling back a migration&lt;/h2&gt;

&lt;p&gt;In-place &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; leaves the original table definition behind, conventionally
suffixed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_BACKUP_&lt;/code&gt;. That is the undo, and it is worth confirming it exists
&lt;em&gt;before&lt;/em&gt; you need it:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SHOW&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TABLES&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;LIKE&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;*BACKUP*&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;To revert, drop the Iceberg table &lt;strong&gt;without purging&lt;/strong&gt; and rename the backup back:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;DROP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;legacy_sales&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;                       &lt;span class=&quot;c1&quot;&gt;-- metadata only; keeps the data files&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;legacy_sales_BACKUP_&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;RENAME&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TO&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;legacy_sales&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DROP TABLE ... PURGE&lt;/code&gt; would delete the data files&lt;/strong&gt; that both the Iceberg
table and the backup point at, which turns a reversible migration into a
restore-from-backup. This is the single most expensive mistake available in this
whole procedure.&lt;/p&gt;

&lt;p&gt;Keep the backup until you are confident — it costs a metastore entry and nothing
else, since no data is duplicated.&lt;/p&gt;

&lt;h2 id=&quot;what-migration-does-not-change&quot;&gt;What migration does not change&lt;/h2&gt;

&lt;p&gt;Worth being explicit, because expectations here cause post-cutover surprises.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;File layout stays as it was.&lt;/strong&gt; A Hive table partitioned into
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;region=r0/region=r1&lt;/code&gt; directories keeps those directories. Iceberg records them
as partition values in its metadata; it does not reorganise storage. You get
Iceberg’s metadata benefits immediately and its &lt;em&gt;layout&lt;/em&gt; benefits only after you
rewrite data.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Hidden partitioning does not appear retroactively.&lt;/strong&gt; The converted table has an
explicit partition spec matching the Hive columns. Transform-based partitioning —
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;days(ts)&lt;/code&gt; rather than a physical &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dt&lt;/code&gt; column — is a later change, applied to new
data, with old files keeping the original spec.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;File sizes stay as they were.&lt;/strong&gt; If the Hive table had a small-file problem, the
Iceberg table has the same small-file problem, now with accurate statistics that
let you see it. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; is the fix and it is a separate, data-moving
job.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Statistics come from the files.&lt;/strong&gt; Iceberg records column bounds by reading
Parquet footers during conversion. That is what makes the conversion fast, and
what makes conversion of a table with millions of files not fast.&lt;/p&gt;

&lt;h2 id=&quot;a-migration-sequence&quot;&gt;A migration sequence that works&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;strong&gt;Snapshot into a copy.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;system.snapshot(&apos;sales&apos;, &apos;sales_iceberg&apos;)&lt;/code&gt;. The original keeps serving.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Validate.&lt;/strong&gt; Row counts, aggregate checksums per partition, and the queries that matter, run against both. Compare results, not just counts — a schema mismatch shows up in values long before it shows up in a count.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Test the write path.&lt;/strong&gt; Run your actual writers against the copy. This is where engines that cannot handle Iceberg reveal themselves, and it is much cheaper to learn now.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Freeze writes to the original&lt;/strong&gt;, briefly.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Migrate in place.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;system.migrate(&apos;sales&apos;)&lt;/code&gt;, which preserves the table name so downstream references keep resolving.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Verify, then keep the backup&lt;/strong&gt; until you are confident. The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_BACKUP_&lt;/code&gt; table is the undo.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Then improve the layout.&lt;/strong&gt; Compaction, sort order, partition spec changes — all of it after the cutover, none of it during.&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;&lt;strong&gt;Do not combine steps 5 and 7.&lt;/strong&gt; A cutover that also reorganises storage cannot
be reasoned about when something looks wrong afterwards, and it turns a metadata
operation into a data-movement operation.&lt;/p&gt;

&lt;h2 id=&quot;verification-worth-doing&quot;&gt;Verification worth doing&lt;/h2&gt;

&lt;p&gt;Counts agreeing is necessary and not sufficient. Three checks catch most real
problems:&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Per-partition counts&lt;/strong&gt;, which catch a partition that failed to map:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sales_iceberg&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Aggregate checksums on numeric columns&lt;/strong&gt;, which catch type-mapping errors that
preserve row count while changing values:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;min&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sales_iceberg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;File accounting&lt;/strong&gt;, which confirms nothing was copied:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;file_size_in_bytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sales_iceberg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Compare that byte total against the original directory. They should match,
because the files are the same files.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Migration rewrites the data.”&lt;/strong&gt; Neither procedure moves data. Both write
metadata over the files already present.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“A snapshot copy stays in sync.”&lt;/strong&gt; It reflects the files at conversion time.
Later writes to the source do not appear in it.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Migration fixes small files.”&lt;/strong&gt; It inherits them, and gives you the statistics
to see them clearly.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Migration is instant regardless of table size.”&lt;/strong&gt; It scales with the number of
files, because every Parquet footer is read for statistics. Millions of files is
a long job.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“I can migrate any table.”&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; requires a catalog that owns table
locations. The measured failure above is the common first obstacle.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;Migration is a metadata operation over data that already exists. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshot&lt;/code&gt; gives
you a copy to be wrong against; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; gives you the cutover. Doing them in
that order costs one extra step and converts an irreversible change into a
rehearsed one.&lt;/p&gt;

&lt;p&gt;Everything that makes Iceberg fast — compaction, sort orders, transform
partitioning — comes after the cutover, not during it.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-procedures/#table-migration&quot;&gt;Iceberg Spark procedures&lt;/a&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshot&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;add_files&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/hive-migration/&quot;&gt;Iceberg Hive migration&lt;/a&gt; for the supported source formats and caveats&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-configuration/#catalog-configuration&quot;&gt;Iceberg configuration&lt;/a&gt; for session catalog setup, which decides whether &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; is available&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheIcebergArchitecture/&quot;&gt;Apache Iceberg architecture&lt;/a&gt; for the metadata tree these procedures construct&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/TableMaintenance/&quot;&gt;Table maintenance for Iceberg and Hudi&lt;/a&gt; for the compaction that should follow a migration&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Spark, Apache Hive, Apache Parquet, Apache and the Apache
feather logo are either registered trademarks or trademarks of The Apache
Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Iceberg" /><category term="Iceberg" /><category term="Migration" /><category term="Hive" /><category term="Parquet" /><summary type="html">Two procedures, two intentions Snapshot migration, measured In-place migration, and what I could not demonstrate Before you migrate anything Migrating in pieces After the cutover Rolling back a migration What migration does not change A migration sequence that works Verification worth doing Common misconceptions A model worth keeping References Trademarks TL;DR Neither procedure copies data. Both write Iceberg metadata that points at the Parquet files already on disk, so a terabyte table converts in about the time it takes to read its file listing. system.snapshot creates a parallel Iceberg table over the same data files. The measured run produced a 6,000-row Iceberg table while the source stayed Parquet and intact — the safe way to test before cutting over. system.migrate converts the table in place, replacing it. It is the cutover, not the rehearsal. migrate requires a catalog whose table locations it can take over. Against a path-based session catalog it refused: Cannot set a custom location for a path-based table. Writes to the original table after a snapshot do not appear in the copy. The copy is a point-in-time view of the files, not a replica. Two procedures, two intentions Converting a Hive table to Iceberg sounds like it should mean rewriting the data. It does not. The Parquet files a Hive table points at are already the format Iceberg uses; what is missing is the metadata tree — the manifests, the manifest list, the snapshot, the schema with field IDs. Both procedures build that tree over the existing files. They differ in what happens to the original. flowchart TB H[(Hive table&amp;lt;br/&amp;gt;parquet files)] --&amp;gt;|&quot;system.snapshot&quot;| C[&quot;new Iceberg table&amp;lt;br/&amp;gt;same files, original untouched&quot;] H --&amp;gt;|&quot;system.migrate&quot;| M[&quot;same name, now Iceberg&amp;lt;br/&amp;gt;original replaced&quot;] system.snapshot is the rehearsal. It creates a second table, under a new name, whose metadata references the same data files. The original keeps working, keeps serving queries, and keeps accepting writes. You point a test workload at the copy and compare. system.migrate is the cutover. The table keeps its name and becomes an Iceberg table. Existing readers now get Iceberg semantics whether or not they were ready for them. The order in the title is the recommendation: snapshot, validate, then migrate. Snapshot migration, measured A partitioned Parquet table with 6,000 rows, created the ordinary way: CALL spark_catalog.system.snapshot(&apos;legacy_a&apos;, &apos;ice_copy&apos;); source is parquet : Provider = parquet source rows = 6000 snapshot-table rows = 6000 ice_copy provider : Provider = iceberg ice_copy snapshots = 1 source still intact = 6000 Both tables report 6,000 rows. One is Parquet, one is Iceberg, and there is only one copy of the data on disk. The Iceberg table has exactly one snapshot, because its history begins at conversion. This is the state you want before a cutover. Run your real queries against ice_copy, compare results and plans against legacy_a, and find out what breaks while the original is still authoritative. What the copy does not do is track the original. Rows written to legacy_a after the snapshot will not appear in ice_copy — the copy’s manifests list the files that existed at conversion time. For a table under active write, that makes the copy a validation target, not a mirror, and the divergence starts immediately. That also means a snapshot copy is not a rollback plan for a migration that happened later. It is a rollback plan only for the moment it was taken. In-place migration, and what I could not demonstrate CALL spark_catalog.system.migrate(&apos;legacy_b&apos;); This converts the table in place. Iceberg reads the existing partition structure and file listing, writes the metadata tree, and swaps the table definition. In most configurations it leaves the previous definition behind under a name suffixed _BACKUP_, so the original can be restored. I was not able to run this successfully, and would rather say so than describe behaviour I did not observe. Against a path-based session catalog it failed: IllegalArgumentException: Cannot set a custom location for a path-based table. Expected /work/wh2/default/legacy_b but got file:/work/wh2/legacy_b The cause is a location convention. A Hadoop, path-based catalog derives a table’s location from &amp;lt;warehouse&amp;gt;/&amp;lt;namespace&amp;gt;/&amp;lt;table&amp;gt;, while Spark’s own warehouse places default-database tables directly under the warehouse root. The two disagree about where legacy_b lives, and migrate refuses rather than guess. Configuring the session catalog as type=hive instead — the realistic setup, since the tables being migrated are Hive tables — failed in my container for a different reason: no metastore was available to it. So the honest summary is: snapshot verified end to end; migrate verified only to the point of its catalog requirement. What the error does establish, and it is the practically useful part, is that migrate needs a catalog that owns its tables’ locations. A path-based catalog pointed at a Spark warehouse is not that, and this is the first thing to check when the procedure refuses. Before you migrate anything Migration converts metadata, so the things that go wrong are things the old table tolerated and the new one will not. Four checks, each cheap. Schema compatibility. Hive is permissive about types that Iceberg is strict about, and a column typed string in the metastore but holding numbers in the files is a problem you want to find now: DESCRIBE FORMATTED legacy_sales; SELECT * FROM legacy_sales LIMIT 5; File formats in the table. migrate supports Parquet, ORC and Avro. A table whose partitions were written in different formats over the years converts, but verify rather than assume: aws s3 ls --recursive s3://warehouse/legacy_sales/ | awk &apos;{print $4}&apos; \ | sed &apos;s/.*\.//&apos; | sort | uniq -c Partition column types. Hive partition values are strings in the metastore. A partition column that is logically a date arrives as a string, and you may want it typed properly — which is a post-migration change, not something migration does. File count. Migration reads every Parquet footer for statistics, so time scales with file count rather than data volume: aws s3 ls --recursive s3://warehouse/legacy_sales/ | wc -l A million small files is a long job and a strong argument for compacting after migration, not before — compaction on the Iceberg side is a single procedure. Migrating in pieces For a table too large or too important to convert in one step, add_files moves partitions into a new Iceberg table incrementally, leaving the Hive table serving throughout: CREATE TABLE local.db.sales ( order_id BIGINT, amount DECIMAL(10,2), region STRING, dt DATE) USING iceberg PARTITIONED BY (dt); CALL local.system.add_files( table =&amp;gt; &apos;db.sales&apos;, source_table =&amp;gt; &apos;legacy_sales&apos;, partition_filter =&amp;gt; map(&apos;dt&apos;, &apos;2026-01-06&apos;)); Loop over partitions, verify as you go, and cut readers over only when the new table is complete. The data files are shared, so this costs metadata and not storage. The caution with add_files: it does not check that the files match the declared schema. A partition whose files have an extra column, or a differently typed one, is added and fails at read time. Verify per partition: SELECT count(*), sum(amount) FROM local.db.sales WHERE dt = date&apos;2026-01-06&apos;; SELECT count(*), sum(amount) FROM legacy_sales WHERE dt = &apos;2026-01-06&apos;; After the cutover Migration gives you Iceberg metadata over a Hive-shaped table. The things that make Iceberg fast are separate jobs, and doing them after the cutover keeps each step diagnosable. -- 1. the layout properties the table should have had ALTER TABLE local.db.sales SET TBLPROPERTIES ( &apos;write.target-file-size-bytes&apos; = &apos;268435456&apos;, &apos;write.object-storage.enabled&apos; = &apos;true&apos;, &apos;write.parquet.compression-codec&apos; = &apos;zstd&apos;, &apos;write.metadata.delete-after-commit.enabled&apos; = &apos;true&apos;); -- 2. a sort order, if queries filter on something other than the partition ALTER TABLE local.db.sales WRITE ORDERED BY region, dt; -- 3. then a compaction that applies both CALL local.system.rewrite_data_files( table =&amp;gt; &apos;db.sales&apos;, options =&amp;gt; map(&apos;min-input-files&apos;,&apos;5&apos;,&apos;target-file-size-bytes&apos;,&apos;268435456&apos;)); -- 4. and only now, a transform-based partition layout for new data ALTER TABLE local.db.sales REPLACE PARTITION FIELD dt WITH days(event_ts); Step 4 is worth resisting until the rest is stable. Hidden partitioning on a migrated table applies to new data; existing files keep the spec they were adopted with, and a table carrying three specs during a migration is harder to reason about than one carrying two. Rolling back a migration In-place migrate leaves the original table definition behind, conventionally suffixed _BACKUP_. That is the undo, and it is worth confirming it exists before you need it: SHOW TABLES LIKE &apos;*BACKUP*&apos;; To revert, drop the Iceberg table without purging and rename the backup back: DROP TABLE legacy_sales; -- metadata only; keeps the data files ALTER TABLE legacy_sales_BACKUP_ RENAME TO legacy_sales; DROP TABLE ... PURGE would delete the data files that both the Iceberg table and the backup point at, which turns a reversible migration into a restore-from-backup. This is the single most expensive mistake available in this whole procedure. Keep the backup until you are confident — it costs a metastore entry and nothing else, since no data is duplicated. What migration does not change Worth being explicit, because expectations here cause post-cutover surprises. File layout stays as it was. A Hive table partitioned into region=r0/region=r1 directories keeps those directories. Iceberg records them as partition values in its metadata; it does not reorganise storage. You get Iceberg’s metadata benefits immediately and its layout benefits only after you rewrite data. Hidden partitioning does not appear retroactively. The converted table has an explicit partition spec matching the Hive columns. Transform-based partitioning — days(ts) rather than a physical dt column — is a later change, applied to new data, with old files keeping the original spec. File sizes stay as they were. If the Hive table had a small-file problem, the Iceberg table has the same small-file problem, now with accurate statistics that let you see it. rewrite_data_files is the fix and it is a separate, data-moving job. Statistics come from the files. Iceberg records column bounds by reading Parquet footers during conversion. That is what makes the conversion fast, and what makes conversion of a table with millions of files not fast. A migration sequence that works Snapshot into a copy. system.snapshot(&apos;sales&apos;, &apos;sales_iceberg&apos;). The original keeps serving. Validate. Row counts, aggregate checksums per partition, and the queries that matter, run against both. Compare results, not just counts — a schema mismatch shows up in values long before it shows up in a count. Test the write path. Run your actual writers against the copy. This is where engines that cannot handle Iceberg reveal themselves, and it is much cheaper to learn now. Freeze writes to the original, briefly. Migrate in place. system.migrate(&apos;sales&apos;), which preserves the table name so downstream references keep resolving. Verify, then keep the backup until you are confident. The _BACKUP_ table is the undo. Then improve the layout. Compaction, sort order, partition spec changes — all of it after the cutover, none of it during. Do not combine steps 5 and 7. A cutover that also reorganises storage cannot be reasoned about when something looks wrong afterwards, and it turns a metadata operation into a data-movement operation. Verification worth doing Counts agreeing is necessary and not sufficient. Three checks catch most real problems: Per-partition counts, which catch a partition that failed to map: SELECT region, count(*) FROM sales_iceberg GROUP BY region; Aggregate checksums on numeric columns, which catch type-mapping errors that preserve row count while changing values: SELECT sum(amount), min(amount), max(amount) FROM sales_iceberg; File accounting, which confirms nothing was copied: SELECT count(*), sum(file_size_in_bytes) FROM sales_iceberg.files; Compare that byte total against the original directory. They should match, because the files are the same files. Common misconceptions “Migration rewrites the data.” Neither procedure moves data. Both write metadata over the files already present. “A snapshot copy stays in sync.” It reflects the files at conversion time. Later writes to the source do not appear in it. “Migration fixes small files.” It inherits them, and gives you the statistics to see them clearly. “Migration is instant regardless of table size.” It scales with the number of files, because every Parquet footer is read for statistics. Millions of files is a long job. “I can migrate any table.” migrate requires a catalog that owns table locations. The measured failure above is the common first obstacle. A model worth keeping Migration is a metadata operation over data that already exists. snapshot gives you a copy to be wrong against; migrate gives you the cutover. Doing them in that order costs one extra step and converts an irreversible change into a rehearsed one. Everything that makes Iceberg fast — compaction, sort orders, transform partitioning — comes after the cutover, not during it. References Iceberg Spark procedures for snapshot, migrate and add_files Iceberg Hive migration for the supported source formats and caveats Iceberg configuration for session catalog setup, which decides whether migrate is available Apache Iceberg architecture for the metadata tree these procedures construct Table maintenance for Iceberg and Hudi for the compaction that should follow a migration Trademarks Apache Iceberg, Apache Spark, Apache Hive, Apache Parquet, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">One table, three formats: Iceberg, Delta and Hudi interoperability</title><link href="https://rangareddy.github.io/IcebergInteroperability/" rel="alternate" type="text/html" title="One table, three formats: Iceberg, Delta and Hudi interoperability" /><published>2026-09-13T10:00:00+05:30</published><updated>2026-09-13T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergInteroperability</id><content type="html" xml:base="https://rangareddy.github.io/IcebergInteroperability/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#why-possible&quot; id=&quot;markdown-toc-why-possible&quot;&gt;Why this is possible at all&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#xtable&quot; id=&quot;markdown-toc-xtable&quot;&gt;Apache XTable&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#uniform&quot; id=&quot;markdown-toc-uniform&quot;&gt;Delta UniForm&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-a-job-looks-like&quot; id=&quot;markdown-toc-what-a-job-looks-like&quot;&gt;What a translation job looks like&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#verifying&quot; id=&quot;markdown-toc-verifying&quot;&gt;Verifying a translation&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#staleness&quot; id=&quot;markdown-toc-staleness&quot;&gt;The staleness window, stated&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#when-not-to-translate&quot; id=&quot;markdown-toc-when-not-to-translate&quot;&gt;When not to translate&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-it-cannot-carry&quot; id=&quot;markdown-toc-what-it-cannot-carry&quot;&gt;What translation cannot carry&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing&quot; id=&quot;markdown-toc-choosing&quot;&gt;Choosing a strategy&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#before-reaching&quot; id=&quot;markdown-toc-before-reaching&quot;&gt;Before reaching for any of this&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;All three formats store &lt;strong&gt;ordinary Parquet&lt;/strong&gt; and differ in the metadata describing it. Translation is therefore a metadata problem, not a data problem.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Apache XTable&lt;/strong&gt; reads one format’s metadata and writes another’s over the same files, so a table becomes readable as Iceberg, Delta or Hudi with one copy of the data.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Delta UniForm&lt;/strong&gt; does the same from the write side: a Delta table generates Iceberg metadata as it commits.&lt;/li&gt;
    &lt;li&gt;The data is shared, so &lt;strong&gt;storage does not double&lt;/strong&gt; — but the metadata is generated, not live, and it is as current as the last sync.&lt;/li&gt;
    &lt;li&gt;One writer, many readers is the pattern that works. Writing through two formats to the same files is how you corrupt both.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;why-possible&quot;&gt;Why this is possible at all&lt;/h2&gt;

&lt;p&gt;Iceberg, Delta Lake and Hudi disagree about metadata and agree about data. All
three write Parquet, and all three record which Parquet files are in the table,
what their schema is, and what changed in each commit.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
    D[&quot;the same Parquet files&quot;] --&amp;gt; I[&quot;Iceberg metadata&amp;lt;br/&amp;gt;metadata.json + manifests&quot;]
    D --&amp;gt; DL[&quot;Delta metadata&amp;lt;br/&amp;gt;_delta_log&quot;]
    D --&amp;gt; H[&quot;Hudi metadata&amp;lt;br/&amp;gt;.hoodie timeline&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If the data files are already acceptable to all three, then making a table
readable by another engine means writing a second description of files that
already exist. No bytes move.&lt;/p&gt;

&lt;p&gt;That is the entire idea behind both tools below, and understanding it tells you
where the limits are: anything expressible in one metadata model and not another
is where translation gets lossy.&lt;/p&gt;

&lt;h2 id=&quot;xtable&quot;&gt;Apache XTable&lt;/h2&gt;

&lt;p&gt;XTable (incubating) reads the source format’s metadata and writes target metadata
alongside it. The data files stay exactly where they are; the table gains a second
— or third — way to be read.&lt;/p&gt;

&lt;p&gt;The practical shape is a sync job: run it after writes, and readers of the target
format see the table as of that sync. Incremental sync translates only what
changed rather than re-reading the whole table, which is what makes it viable on
a schedule rather than as a one-off conversion. The mechanics and the cost of
getting that wrong are in
&lt;a href=&quot;/XTableIncrementalSync/&quot;&gt;Apache XTable incremental sync&lt;/a&gt;,
and the command surface in
&lt;a href=&quot;/ApacheXTableCheatSheet/&quot;&gt;the XTable cheat sheet&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;What it is good for&lt;/strong&gt;: an engine that only speaks one format, a migration where
both old and new readers must work during the transition, a downstream consumer
you do not control.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;What it is not&lt;/strong&gt;: a way to have two writers. Metadata is generated from a source
of truth, and the source of truth is the format you write.&lt;/p&gt;

&lt;h2 id=&quot;uniform&quot;&gt;Delta UniForm&lt;/h2&gt;

&lt;p&gt;UniForm moves the same idea to write time. A Delta table configured for it
generates Iceberg metadata as part of committing, so Iceberg readers see the table
without a separate sync job.&lt;/p&gt;

&lt;p&gt;The trade against XTable is directness versus generality. UniForm is built into
the writer, so there is no extra job and less staleness, but it works from Delta
outward. XTable is a separate process covering more source and target
combinations, at the cost of running it.&lt;/p&gt;

&lt;p&gt;Either way the shape is the same: &lt;strong&gt;one writer, one set of data files, several
metadata descriptions.&lt;/strong&gt;&lt;/p&gt;

&lt;h2 id=&quot;what-a-job-looks-like&quot;&gt;What a translation job looks like&lt;/h2&gt;

&lt;p&gt;XTable runs as a job with a small YAML config, which makes the staleness window
an explicit operational choice rather than an accident.&lt;/p&gt;

&lt;div class=&quot;language-yaml highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;na&quot;&gt;sourceFormat&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;ICEBERG&lt;/span&gt;
&lt;span class=&quot;na&quot;&gt;targetFormats&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt;
  &lt;span class=&quot;pi&quot;&gt;-&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;DELTA&lt;/span&gt;
  &lt;span class=&quot;pi&quot;&gt;-&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;HUDI&lt;/span&gt;
&lt;span class=&quot;na&quot;&gt;datasets&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt;
  &lt;span class=&quot;pi&quot;&gt;-&lt;/span&gt; &lt;span class=&quot;na&quot;&gt;tableBasePath&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;s3://lake/warehouse/db/events&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;tableName&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;events&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;java &lt;span class=&quot;nt&quot;&gt;-jar&lt;/span&gt; xtable-utilities.jar &lt;span class=&quot;nt&quot;&gt;--datasetConfig&lt;/span&gt; config.yaml
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Run it after writes, on whatever cadence your consumers can tolerate. The output
is additional metadata directories beside the existing data:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;s3://lake/warehouse/db/events/
├── data/                  # unchanged, shared by all three
├── metadata/              # Iceberg
├── _delta_log/            # written by the sync
└── .hoodie/               # written by the sync
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Nothing under &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;data/&lt;/code&gt; is touched&lt;/strong&gt;, which is the property that makes this cheap
and also the property that makes two writers catastrophic.&lt;/p&gt;

&lt;h2 id=&quot;verifying&quot;&gt;Verifying a translation&lt;/h2&gt;

&lt;p&gt;Row counts agreeing is necessary and not sufficient — the failure modes here
preserve counts while changing values or losing pruning.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- counts, the cheap check&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg_cat&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;delta&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;`s3://lake/warehouse/db/events`&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- aggregates, which catch representation differences&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;min&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg_cat&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;min&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;delta&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;`s3://lake/warehouse/db/events`&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- per-partition, which catches a partition that failed to map&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg_cat&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Then check the thing most likely to be silently lost:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;EXPLAIN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;delta&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;`s3://lake/warehouse/db/events`&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;=&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;date&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;2026-01-06&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;If the translated table’s plan reads every file where the Iceberg table pruned to
one partition, the rows are correct and the performance is not — which is the
usual outcome when a transform-partitioned table is translated into a model that
has no equivalent concept.&lt;/p&gt;

&lt;h2 id=&quot;staleness&quot;&gt;The staleness window, stated&lt;/h2&gt;

&lt;p&gt;A translated table is a point-in-time copy of metadata. Between syncs, readers of
the target format see an older table than readers of the source.&lt;/p&gt;

&lt;p&gt;That is fine for hourly analytics and wrong for anything that joins the two
representations expecting consistency. Make it explicit:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- how far behind is the translated view?&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;source_latest&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg_cat&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;timestamp&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;    &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;target_latest&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;delta&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;`...`&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;history&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Alert on the gap rather than on the sync job’s exit code — a job that succeeds
while doing nothing is the failure that matters.&lt;/p&gt;

&lt;h2 id=&quot;when-not-to-translate&quot;&gt;When not to translate&lt;/h2&gt;

&lt;p&gt;Three situations where the honest answer is to fix something else.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The engine gained support since you last checked.&lt;/strong&gt; Iceberg support across
Trino, Flink, Dremio, Snowflake and BigQuery has moved quickly, and translation
layers outlive the constraint that motivated them.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The consumer reads once a day.&lt;/strong&gt; A scheduled export to Parquet is simpler to
operate and reason about than a metadata translation, and it has no ambiguity
about who owns the table.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Two teams both want to write.&lt;/strong&gt; No amount of translation makes that safe. The
fix is a conversation about ownership, not a sync job.&lt;/p&gt;

&lt;h2 id=&quot;what-it-cannot-carry&quot;&gt;What translation cannot carry&lt;/h2&gt;

&lt;p&gt;The formats are similar enough for this to work and different enough for it to be
lossy at the edges.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Deletes are represented differently.&lt;/strong&gt; Iceberg has position and equality delete
files and, in v3, deletion vectors. Delta has deletion vectors. Hudi has log files
in merge-on-read tables. A table relying on one format’s row-level delete
mechanism does not always translate cleanly into another’s, and merge-on-read
tables are where translation is most likely to disappoint.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Partitioning models differ.&lt;/strong&gt; Iceberg’s hidden partitioning is a transform on a
column; Hive-style partitioning in other models is a physical column. A
transform-partitioned Iceberg table translated outward may lose the pruning that
made it fast, even though the rows are all present.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;History does not fully transfer.&lt;/strong&gt; Time travel depends on each format’s own
snapshot or version history. A translated table typically starts its history at
translation, so the target format cannot travel back before that.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Table properties do not map one to one.&lt;/strong&gt; Compaction settings, sort orders,
retention policies are expressed differently and mostly do not survive.&lt;/p&gt;

&lt;p&gt;The rule of thumb: &lt;strong&gt;the current state of the data translates well; the history
and the optimizations translate poorly.&lt;/strong&gt;&lt;/p&gt;

&lt;h2 id=&quot;choosing&quot;&gt;Choosing a strategy&lt;/h2&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Situation&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Approach&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;One engine, one team&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;pick a format, skip all of this&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Migrating, both readers live&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;XTable during the transition&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Delta writer, Iceberg readers&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;UniForm&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Many engines, indefinitely&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;translate, and accept a staleness window&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Two teams both wanting to write&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;fix the ownership question instead&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;That last row is the important one. Interoperability solves &lt;em&gt;reading&lt;/em&gt;. Two writers
through two formats against the same files means two metadata layers each
believing they own the table, and neither will notice the other’s commits. The
result is silent corruption. &lt;strong&gt;One writer, many readers&lt;/strong&gt; is the only safe shape.&lt;/p&gt;

&lt;h2 id=&quot;before-reaching&quot;&gt;Before reaching for any of this&lt;/h2&gt;

&lt;p&gt;The most common reason to translate is an engine that supports only one format.
That is worth checking rather than assuming: Iceberg support has broadened
considerably across Trino, Flink, Dremio, Snowflake and BigQuery, and the
constraint that motivated a translation layer a year ago may no longer exist.&lt;/p&gt;

&lt;p&gt;The differences between the formats themselves — why they made different choices
rather than which is better — are in
&lt;a href=&quot;/OpenTableFormats/&quot;&gt;open table formats in practice&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Interoperability means the data is copied.”&lt;/strong&gt; The Parquet files are shared.
Only metadata is written.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“A translated table is a live view.”&lt;/strong&gt; It is as current as the last sync.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“I can write through both formats.”&lt;/strong&gt; You can, and you will corrupt both.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Everything translates.”&lt;/strong&gt; Current state translates well. Deletes, history and
optimizations translate poorly.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“UniForm and XTable are competitors.”&lt;/strong&gt; They solve the same problem from
different ends — one at write time from Delta, one as a job across many
combinations.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;The data files are the table. The metadata is a description of them, and a set of
files can carry more than one description.&lt;/p&gt;

&lt;p&gt;So interoperability is cheap for reading and impossible for writing, and the
sensible arrangement is always the same: one format owns the writes, every other
description is generated, and the staleness window is something you chose rather
than discovered.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://xtable.apache.org/&quot;&gt;Apache XTable&lt;/a&gt; for supported source and target formats&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.delta.io/latest/delta-uniform.html&quot;&gt;Delta UniForm&lt;/a&gt; for generating Iceberg metadata from Delta&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/OpenTableFormats/&quot;&gt;Open table formats in practice&lt;/a&gt; for how the three formats differ&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/XTableIncrementalSync/&quot;&gt;Apache XTable incremental sync&lt;/a&gt; for keeping translation fast&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheXTableCheatSheet/&quot;&gt;Apache XTable cheat sheet&lt;/a&gt; for the commands&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Hudi, Apache XTable, Apache Spark, Apache Parquet, Apache
Flink, Apache and the Apache feather logo are either registered trademarks or
trademarks of The Apache Software Foundation in the United States and other
countries. Delta Lake is a trademark of the Linux Foundation.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Lakehouse" /><category term="Iceberg" /><category term="Delta" /><category term="Hudi" /><category term="XTable" /><summary type="html">Why this is possible at all Apache XTable Delta UniForm What a translation job looks like Verifying a translation The staleness window, stated When not to translate What translation cannot carry Choosing a strategy Before reaching for any of this Common misconceptions A model worth keeping References Trademarks TL;DR All three formats store ordinary Parquet and differ in the metadata describing it. Translation is therefore a metadata problem, not a data problem. Apache XTable reads one format’s metadata and writes another’s over the same files, so a table becomes readable as Iceberg, Delta or Hudi with one copy of the data. Delta UniForm does the same from the write side: a Delta table generates Iceberg metadata as it commits. The data is shared, so storage does not double — but the metadata is generated, not live, and it is as current as the last sync. One writer, many readers is the pattern that works. Writing through two formats to the same files is how you corrupt both. Why this is possible at all Iceberg, Delta Lake and Hudi disagree about metadata and agree about data. All three write Parquet, and all three record which Parquet files are in the table, what their schema is, and what changed in each commit. flowchart TB D[&quot;the same Parquet files&quot;] --&amp;gt; I[&quot;Iceberg metadata&amp;lt;br/&amp;gt;metadata.json + manifests&quot;] D --&amp;gt; DL[&quot;Delta metadata&amp;lt;br/&amp;gt;_delta_log&quot;] D --&amp;gt; H[&quot;Hudi metadata&amp;lt;br/&amp;gt;.hoodie timeline&quot;] If the data files are already acceptable to all three, then making a table readable by another engine means writing a second description of files that already exist. No bytes move. That is the entire idea behind both tools below, and understanding it tells you where the limits are: anything expressible in one metadata model and not another is where translation gets lossy. Apache XTable XTable (incubating) reads the source format’s metadata and writes target metadata alongside it. The data files stay exactly where they are; the table gains a second — or third — way to be read. The practical shape is a sync job: run it after writes, and readers of the target format see the table as of that sync. Incremental sync translates only what changed rather than re-reading the whole table, which is what makes it viable on a schedule rather than as a one-off conversion. The mechanics and the cost of getting that wrong are in Apache XTable incremental sync, and the command surface in the XTable cheat sheet. What it is good for: an engine that only speaks one format, a migration where both old and new readers must work during the transition, a downstream consumer you do not control. What it is not: a way to have two writers. Metadata is generated from a source of truth, and the source of truth is the format you write. Delta UniForm UniForm moves the same idea to write time. A Delta table configured for it generates Iceberg metadata as part of committing, so Iceberg readers see the table without a separate sync job. The trade against XTable is directness versus generality. UniForm is built into the writer, so there is no extra job and less staleness, but it works from Delta outward. XTable is a separate process covering more source and target combinations, at the cost of running it. Either way the shape is the same: one writer, one set of data files, several metadata descriptions. What a translation job looks like XTable runs as a job with a small YAML config, which makes the staleness window an explicit operational choice rather than an accident. sourceFormat: ICEBERG targetFormats: - DELTA - HUDI datasets: - tableBasePath: s3://lake/warehouse/db/events tableName: events java -jar xtable-utilities.jar --datasetConfig config.yaml Run it after writes, on whatever cadence your consumers can tolerate. The output is additional metadata directories beside the existing data: s3://lake/warehouse/db/events/ ├── data/ # unchanged, shared by all three ├── metadata/ # Iceberg ├── _delta_log/ # written by the sync └── .hoodie/ # written by the sync Nothing under data/ is touched, which is the property that makes this cheap and also the property that makes two writers catastrophic. Verifying a translation Row counts agreeing is necessary and not sufficient — the failure modes here preserve counts while changing values or losing pruning. -- counts, the cheap check SELECT count(*) FROM iceberg_cat.db.events; SELECT count(*) FROM delta.`s3://lake/warehouse/db/events`; -- aggregates, which catch representation differences SELECT sum(amount), min(event_ts), max(event_ts) FROM iceberg_cat.db.events; SELECT sum(amount), min(event_ts), max(event_ts) FROM delta.`s3://lake/warehouse/db/events`; -- per-partition, which catches a partition that failed to map SELECT region, count(*) FROM iceberg_cat.db.events GROUP BY region ORDER BY region; Then check the thing most likely to be silently lost: EXPLAIN SELECT * FROM delta.`s3://lake/warehouse/db/events` WHERE event_ts &amp;gt;= date&apos;2026-01-06&apos;; If the translated table’s plan reads every file where the Iceberg table pruned to one partition, the rows are correct and the performance is not — which is the usual outcome when a transform-partitioned table is translated into a model that has no equivalent concept. The staleness window, stated A translated table is a point-in-time copy of metadata. Between syncs, readers of the target format see an older table than readers of the source. That is fine for hourly analytics and wrong for anything that joins the two representations expecting consistency. Make it explicit: -- how far behind is the translated view? SELECT max(committed_at) AS source_latest FROM iceberg_cat.db.events.snapshots; SELECT max(timestamp) AS target_latest FROM delta.`...`.history; Alert on the gap rather than on the sync job’s exit code — a job that succeeds while doing nothing is the failure that matters. When not to translate Three situations where the honest answer is to fix something else. The engine gained support since you last checked. Iceberg support across Trino, Flink, Dremio, Snowflake and BigQuery has moved quickly, and translation layers outlive the constraint that motivated them. The consumer reads once a day. A scheduled export to Parquet is simpler to operate and reason about than a metadata translation, and it has no ambiguity about who owns the table. Two teams both want to write. No amount of translation makes that safe. The fix is a conversation about ownership, not a sync job. What translation cannot carry The formats are similar enough for this to work and different enough for it to be lossy at the edges. Deletes are represented differently. Iceberg has position and equality delete files and, in v3, deletion vectors. Delta has deletion vectors. Hudi has log files in merge-on-read tables. A table relying on one format’s row-level delete mechanism does not always translate cleanly into another’s, and merge-on-read tables are where translation is most likely to disappoint. Partitioning models differ. Iceberg’s hidden partitioning is a transform on a column; Hive-style partitioning in other models is a physical column. A transform-partitioned Iceberg table translated outward may lose the pruning that made it fast, even though the rows are all present. History does not fully transfer. Time travel depends on each format’s own snapshot or version history. A translated table typically starts its history at translation, so the target format cannot travel back before that. Table properties do not map one to one. Compaction settings, sort orders, retention policies are expressed differently and mostly do not survive. The rule of thumb: the current state of the data translates well; the history and the optimizations translate poorly. Choosing a strategy Situation Approach One engine, one team pick a format, skip all of this Migrating, both readers live XTable during the transition Delta writer, Iceberg readers UniForm Many engines, indefinitely translate, and accept a staleness window Two teams both wanting to write fix the ownership question instead That last row is the important one. Interoperability solves reading. Two writers through two formats against the same files means two metadata layers each believing they own the table, and neither will notice the other’s commits. The result is silent corruption. One writer, many readers is the only safe shape. Before reaching for any of this The most common reason to translate is an engine that supports only one format. That is worth checking rather than assuming: Iceberg support has broadened considerably across Trino, Flink, Dremio, Snowflake and BigQuery, and the constraint that motivated a translation layer a year ago may no longer exist. The differences between the formats themselves — why they made different choices rather than which is better — are in open table formats in practice. Common misconceptions “Interoperability means the data is copied.” The Parquet files are shared. Only metadata is written. “A translated table is a live view.” It is as current as the last sync. “I can write through both formats.” You can, and you will corrupt both. “Everything translates.” Current state translates well. Deletes, history and optimizations translate poorly. “UniForm and XTable are competitors.” They solve the same problem from different ends — one at write time from Delta, one as a job across many combinations. A model worth keeping The data files are the table. The metadata is a description of them, and a set of files can carry more than one description. So interoperability is cheap for reading and impossible for writing, and the sensible arrangement is always the same: one format owns the writes, every other description is generated, and the staleness window is something you chose rather than discovered. References Apache XTable for supported source and target formats Delta UniForm for generating Iceberg metadata from Delta Open table formats in practice for how the three formats differ Apache XTable incremental sync for keeping translation fast Apache XTable cheat sheet for the commands Trademarks Apache Iceberg, Apache Hudi, Apache XTable, Apache Spark, Apache Parquet, Apache Flink, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries. Delta Lake is a trademark of the Linux Foundation.</summary></entry><entry><title type="html">CDC into Iceberg: the delete files nobody counted</title><link href="https://rangareddy.github.io/IcebergCdcPipelines/" rel="alternate" type="text/html" title="CDC into Iceberg: the delete files nobody counted" /><published>2026-09-09T10:00:00+05:30</published><updated>2026-09-09T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergCdcPipelines</id><content type="html" xml:base="https://rangareddy.github.io/IcebergCdcPipelines/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#the-shape&quot; id=&quot;markdown-toc-the-shape&quot;&gt;The shape of a CDC pipeline&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-three-merges-cost&quot; id=&quot;markdown-toc-what-three-merges-cost&quot;&gt;What three merges cost&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-compaction-fixes&quot; id=&quot;markdown-toc-what-compaction-fixes&quot;&gt;What compaction does, and does not, fix&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#building-the-merge&quot; id=&quot;markdown-toc-building-the-merge&quot;&gt;Building the merge correctly&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#measuring-the-debt&quot; id=&quot;markdown-toc-measuring-the-debt&quot;&gt;Measuring the debt&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#equality-deletes&quot; id=&quot;markdown-toc-equality-deletes&quot;&gt;Equality deletes, and why Flink pipelines differ&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-schedule-that-keeps-up&quot; id=&quot;markdown-toc-a-schedule-that-keeps-up&quot;&gt;A maintenance schedule that keeps up&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing-the-write-mode&quot; id=&quot;markdown-toc-choosing-the-write-mode&quot;&gt;Choosing the write mode for a CDC table&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-to-monitor&quot; id=&quot;markdown-toc-what-to-monitor&quot;&gt;What to monitor&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#getting-the-merge-right&quot; id=&quot;markdown-toc-getting-the-merge-right&quot;&gt;Getting the merge right&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Three &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt; statements against a merge-on-read table left &lt;strong&gt;5 data files holding 5,960 records and 3 delete files holding 1,120&lt;/strong&gt; — for a table that returns &lt;strong&gt;4,840 rows&lt;/strong&gt;. Over a fifth of what is stored is dead weight the reader filters out.&lt;/li&gt;
    &lt;li&gt;Every merge adds a delete file. The count grows with the number of &lt;strong&gt;merges&lt;/strong&gt;, not the number of changed rows, so a frequent CDC pipeline accumulates them fastest.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; collapsed the data side to &lt;strong&gt;1 file of 4,840 records&lt;/strong&gt; — the live rows exactly — but &lt;strong&gt;2 delete files survived&lt;/strong&gt;. Compaction of data is not compaction of deletes.&lt;/li&gt;
    &lt;li&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;files&lt;/code&gt; metadata table with its &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content&lt;/code&gt; column is the monitor. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content = 1&lt;/code&gt; growing over time is the early warning.&lt;/li&gt;
    &lt;li&gt;A CDC table without scheduled maintenance does not fail. It gets slower every hour, with no change in row count to explain it.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;the-shape&quot;&gt;The shape of a CDC pipeline&lt;/h2&gt;

&lt;p&gt;Change data capture lands a stream of row-level events — inserts, updates,
deletes — from a source database into a table. The usual path is Debezium
reading the database log, Kafka carrying the changes, and a Spark job applying
them.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
    DB[(source DB)] --&amp;gt;|&quot;log&quot;| DBZ[Debezium]
    DBZ --&amp;gt; K[Kafka]
    K --&amp;gt; S[&quot;Spark micro-batch&quot;]
    S --&amp;gt;|&quot;MERGE INTO&quot;| I[(Iceberg table)]
    I -.-&amp;gt;|&quot;accumulates delete files&quot;| M[&quot;compaction&amp;lt;br/&amp;gt;+ expiry&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The interesting part is the last arrow, and it is the one most pipelines omit.&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt; is the statement that applies a changelog, because it expresses all
three operations against a key in one pass:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;MERGE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;INTO&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cdc&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;chg&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ON&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;MATCHED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AND&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;op&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;D&apos;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DELETE&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;MATCHED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;UPDATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;updated_at&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;updated_at&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;NOT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;MATCHED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AND&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;op&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;lt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;D&apos;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;INSERT&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;updated_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VALUES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;updated_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That statement is correct, idempotent when keyed properly, and the reason CDC on
Iceberg is straightforward to build. What it does to the files underneath is the
part worth measuring.&lt;/p&gt;

&lt;h2 id=&quot;what-three-merges-cost&quot;&gt;What three merges cost&lt;/h2&gt;

&lt;p&gt;A merge-on-read table loaded with 5,000 rows, then three changelog batches
applied, each touching 400 rows with a fifth of them deletes. Reading the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;files&lt;/code&gt;
metadata table after each step, where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content&lt;/code&gt; is 0 for data and 1 for position
deletes:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;After&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;data files / records&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;delete files / records&lt;/th&gt;
      &lt;th style=&quot;text-align: right&quot;&gt;rows the query returns&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;initial load&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2 / 5,000&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;—&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;5,000&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;merge #1&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3 / 5,320&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1 / 400&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;4,920&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;merge #2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4 / 5,640&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2 / 760&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;4,880&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;merge #3&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;5 / 5,960&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3 / 1,120&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;4,840&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;img src=&quot;/assets/images/iceberg/cdc-accumulation.png&quot; alt=&quot;Three MERGE statements against a merge-on-read table: data records climb from 5,000 to 5,960 across five files while delete files grow to three holding 1,120 records, for a table returning 4,840 rows; after rewrite_data_files the data collapses to one file of 4,840 records but two delete files survive&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Read the last row carefully.&lt;/strong&gt; The table answers queries with 4,840 rows. To do
that it stores 5,960 records across five data files, and applies 1,120 delete
records across three delete files to filter them down. Roughly 19% of the stored
records are dead, and every scan pays to exclude them.&lt;/p&gt;

&lt;p&gt;Two patterns are visible in that table and both matter.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Data records only ever go up.&lt;/strong&gt; An update in merge-on-read is a delete plus an
insert: the new version is appended, the old one is marked deleted. Nothing
shrinks at write time.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Delete files grow per merge, not per row.&lt;/strong&gt; Merge #1 wrote one delete file,
merge #2 a second, merge #3 a third. A pipeline running a merge every minute
accumulates 1,440 delete files a day regardless of whether each batch changed ten
rows or ten thousand.&lt;/p&gt;

&lt;p&gt;That second point is the one that makes CDC different from occasional updates.
The cost tracks your &lt;em&gt;trigger interval&lt;/em&gt;, not your data volume.&lt;/p&gt;

&lt;h2 id=&quot;what-compaction-fixes&quot;&gt;What compaction does, and does not, fix&lt;/h2&gt;

&lt;p&gt;Running the standard data compaction:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_data_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.cdc&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;options&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;min-input-files&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;2&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;after rewrite_data_files: data=1f/4840r   pos-del=2f/960r   rows=4840
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The data side is now exactly right: &lt;strong&gt;one file holding 4,840 records&lt;/strong&gt;, which is
precisely the live row count. The rewrite read the data files, applied the delete
files, and wrote out only surviving rows. The 1,120 dead records are gone from
the data.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;But two delete files remain, holding 960 records.&lt;/strong&gt; Data compaction did not
clear the delete side. Iceberg has a separate procedure for that:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_position_delete_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.cdc&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The distinction is easy to miss and expensive to miss for a long time. A pipeline
that runs &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; nightly and calls it maintenance still accumulates
delete files, and the read path still applies them.&lt;/p&gt;

&lt;p&gt;Add the third job, the one that actually reclaims storage:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;expire_snapshots&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.cdc&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;retain_last&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Because none of the above deletes anything from disk. Compaction writes new files
and leaves the old ones referenced by older snapshots. That accounting is the
subject of
&lt;a href=&quot;/TableMaintenance/&quot;&gt;table maintenance for Iceberg and Hudi&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;building-the-merge&quot;&gt;Building the merge correctly&lt;/h2&gt;

&lt;p&gt;The deduplication step is where CDC pipelines go wrong, so here it is in full
rather than as a fragment.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;functions&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Window&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;apply_changelog&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;batch_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;batch_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;c1&quot;&gt;# 1. collapse to one row per key, newest wins, using the SOURCE&apos;s ordering
&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;#    column - never wall-clock arrival time, which reorders under retry.
&lt;/span&gt;    &lt;span class=&quot;n&quot;&gt;w&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Window&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orderBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;log_seq&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;desc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;latest&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;batch_df&lt;/span&gt;
              &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;row_number&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;over&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;w&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
              &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn = 1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
              &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;drop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

    &lt;span class=&quot;c1&quot;&gt;# 2. a tombstone for a key that never existed is a no-op, not an error
&lt;/span&gt;    &lt;span class=&quot;n&quot;&gt;latest&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createOrReplaceTempView&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;changes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

    &lt;span class=&quot;n&quot;&gt;batch_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
        MERGE INTO local.db.cdc t USING changes s ON t.id = s.id
        WHEN MATCHED AND s.op = &apos;D&apos; THEN DELETE
        WHEN MATCHED AND s.log_seq &amp;gt; t.log_seq THEN UPDATE SET *
        WHEN NOT MATCHED AND s.op &amp;lt;&amp;gt; &apos;D&apos; THEN INSERT *&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeStream&lt;/span&gt;
       &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;foreachBatch&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;apply_changelog&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
       &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;checkpointLocation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;/work/ckpt_cdc&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
       &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trigger&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;availableNow&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;True&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
       &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;start&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Three details earn their place.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHEN MATCHED AND s.log_seq &amp;gt; t.log_seq&lt;/code&gt;&lt;/strong&gt; makes the merge idempotent against
replay. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreachBatch&lt;/code&gt; is at-least-once, so the same batch can be applied twice
after a failure; without that guard, a re-applied batch can overwrite a newer
row with an older one. Storing the source sequence number in the target is what
makes the comparison possible, and it is worth the column.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Ordering by the source’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log_seq&lt;/code&gt;&lt;/strong&gt;, not by ingestion timestamp. Debezium
emits a monotonic position per source transaction; Kafka delivery order is not
guaranteed to match it after a retry or a partition rebalance.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHEN NOT MATCHED AND s.op &amp;lt;&amp;gt; &apos;D&apos;&lt;/code&gt;&lt;/strong&gt; stops a delete for an unknown key from
inserting a tombstone row.&lt;/p&gt;

&lt;h2 id=&quot;measuring-the-debt&quot;&gt;Measuring the debt&lt;/h2&gt;

&lt;p&gt;One query, on a schedule, tells you whether compaction is keeping up:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;CASE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ELSE&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;END&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;data_records&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;CASE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ELSE&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;END&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pos_delete_records&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;CASE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ELSE&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;END&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;eq_delete_records&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;round&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;CASE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ELSE&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;END&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;nullif&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;CASE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ELSE&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;END&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pct_dead&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cdc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pct_dead&lt;/code&gt; is the fraction of stored records that every scan filters out. On the
measured table it reached &lt;strong&gt;19%&lt;/strong&gt; after three merges. Two thresholds worth
alerting on: above ~10% sustained means compaction is behind; a value that climbs
every day means it is not running at all.&lt;/p&gt;

&lt;p&gt;Pair it with a file-count trend, because record share and file count degrade
differently:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cdc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Many small delete files hurt even when they hold few records, because each is
opened per scan.&lt;/p&gt;

&lt;h2 id=&quot;equality-deletes&quot;&gt;Equality deletes, and why Flink pipelines differ&lt;/h2&gt;

&lt;p&gt;Spark writes position deletes: this file, these row positions. Flink CDC writers
commonly write &lt;strong&gt;equality deletes&lt;/strong&gt;: rows where these columns equal these values.&lt;/p&gt;

&lt;p&gt;The difference is not academic. A position delete is applied by skipping known
offsets in one named file. An equality delete is a predicate the reader must
evaluate against &lt;strong&gt;every data file in scope with a lower sequence number&lt;/strong&gt; — it
does not know where the matching rows live.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- which kind is this table accumulating?&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cdc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content = 2&lt;/code&gt; rows are equality deletes. A table mixing both, because two engines
write to it, pays both costs and is the hardest case to reason about. If you
control the topology, &lt;strong&gt;pick one writer per table&lt;/strong&gt;.&lt;/p&gt;

&lt;h2 id=&quot;a-schedule-that-keeps-up&quot;&gt;A maintenance schedule that keeps up&lt;/h2&gt;

&lt;p&gt;CDC tables need maintenance proportional to merge frequency, not data volume:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- hourly: collapse the files the last 60 merges produced&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_data_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt;   &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.cdc&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;where&lt;/span&gt;   &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;updated_at &amp;gt;= current_timestamp() - INTERVAL 2 HOURS&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;options&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;min-input-files&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;5&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;));&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- hourly: and clear the delete files, which the above does not&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_position_delete_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.cdc&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- daily: reclaim, with retention equal to your recovery window&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;expire_snapshots&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.cdc&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;retain_last&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;24&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;where&lt;/code&gt; clause matters on a CDC table.&lt;/strong&gt; Rewriting the whole table hourly
costs more than the merges did. Scope compaction to the partitions the merges
actually touched.&lt;/p&gt;

&lt;h2 id=&quot;choosing-the-write-mode&quot;&gt;Choosing the write mode for a CDC table&lt;/h2&gt;

&lt;p&gt;The table above is merge-on-read, which is the usual choice for CDC and worth
justifying rather than assuming.&lt;/p&gt;

&lt;p&gt;Under copy-on-write, each merge rewrites every data file containing an affected
row. A changelog touching 400 scattered keys could rewrite most of the table,
every minute. That does not keep up, and it is why CDC pipelines choose
merge-on-read.&lt;/p&gt;

&lt;p&gt;The trade is the one measured above: writes stay cheap, reads carry the delete
files until compaction runs. Set it deliberately, per operation:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cdc&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(...)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.merge.mode&apos;&lt;/span&gt;  &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;merge-on-read&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.delete.mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;merge-on-read&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.update.mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;merge-on-read&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The full comparison, with the same DELETE measured under both modes, is in
&lt;a href=&quot;/IcebergCopyOnWriteMergeOnRead/&quot;&gt;copy-on-write or merge-on-read&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;One caveat for Flink-based CDC.&lt;/strong&gt; Spark writes &lt;em&gt;position&lt;/em&gt; deletes, which name a
file and row positions. Flink CDC writers commonly produce &lt;em&gt;equality&lt;/em&gt; deletes,
which are predicates on key columns. Equality deletes are more expensive to apply,
because the reader evaluates them against every data file in scope rather than one
named file. A pipeline mixing engines accumulates both kinds, and the read cost is
not the same.&lt;/p&gt;

&lt;h2 id=&quot;what-to-monitor&quot;&gt;What to monitor&lt;/h2&gt;

&lt;p&gt;One query, run on a schedule, answers whether the pipeline is healthy:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;records&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cdc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content = 0&lt;/code&gt; is data, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1&lt;/code&gt; is position deletes, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;2&lt;/code&gt; is equality deletes.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Watch the ratio, not the absolute numbers.&lt;/strong&gt; Delete records as a fraction of
data records is the fraction of read work that is wasted. On the measured table
that ratio reached 19% after three merges. A steady-state pipeline with
maintenance keeps it low; one without maintenance climbs until queries hurt.&lt;/p&gt;

&lt;p&gt;Two more signals worth alerting on. &lt;strong&gt;Delete file count climbing across days&lt;/strong&gt;
means compaction is not keeping pace with the merge frequency, and the fix is
usually to compact more often rather than to merge less often. &lt;strong&gt;Query planning
time growing while row count is flat&lt;/strong&gt; points at metadata rather than data, which
is snapshot and manifest accumulation, not delete files.&lt;/p&gt;

&lt;h2 id=&quot;getting-the-merge-right&quot;&gt;Getting the merge right&lt;/h2&gt;

&lt;p&gt;Two correctness issues matter more than the performance ones.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Deduplicate the changelog before merging.&lt;/strong&gt; If a batch contains two events for
the same key, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt; raises an error rather than picking one, because the
result would be ambiguous. Collapse to the latest event per key first, typically
with a window function on the source’s log sequence number or timestamp. Using
wall-clock arrival time here is a common bug: out-of-order delivery then applies
an older version over a newer one.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Make the match condition the real key.&lt;/strong&gt; Matching on a column that is not
unique in the target produces the same ambiguity error, or worse, silently
updates more rows than intended when it happens to be unique in the sample you
tested with.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“MERGE INTO updates rows in place.”&lt;/strong&gt; In merge-on-read it appends new versions
and writes delete records for the old ones. Storage grows on every merge.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Compaction cleans up deletes.”&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; applies them to the data
it rewrites; delete files can survive it. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_position_delete_files&lt;/code&gt; is the
dedicated procedure.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“The row count tells me the table is healthy.”&lt;/strong&gt; The measured table returned
4,840 rows while storing 5,960 data records and 1,120 delete records. Row count
is exactly the number that hides this.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“More frequent merges are strictly better for freshness.”&lt;/strong&gt; They are also
strictly worse for delete-file accumulation, because the file count follows the
merge count.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Copy-on-write would avoid all this.”&lt;/strong&gt; It avoids delete files by rewriting data
files instead, which for a scattered changelog can mean rewriting most of the
table per batch.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;A CDC pipeline on Iceberg is a debt arrangement. Every merge borrows read
performance to keep write latency low, and compaction is the repayment.&lt;/p&gt;

&lt;p&gt;The borrowing is automatic and the repayment is not, which is why the failure mode
is a table that works perfectly for a month and then gets slow for no visible
reason. Schedule &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_position_delete_files&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;expire_snapshots&lt;/code&gt; when you build the pipeline, and monitor the delete-to-data
ratio so you find out before your users do.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-procedures/&quot;&gt;Iceberg maintenance procedures&lt;/a&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_position_delete_files&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;expire_snapshots&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-writes/&quot;&gt;Iceberg writes with Spark&lt;/a&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt; semantics and its restrictions&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/spec/#delete-formats&quot;&gt;Iceberg spec: delete formats&lt;/a&gt; for position versus equality deletes&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergCopyOnWriteMergeOnRead/&quot;&gt;Copy-on-write or merge-on-read&lt;/a&gt; for the mode this pipeline depends on&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergStreamingIngestion/&quot;&gt;Streaming into Iceberg&lt;/a&gt; for the trigger that decides how often merges happen&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Spark, Apache Flink, Apache Kafka, Apache and the Apache
feather logo are either registered trademarks or trademarks of The Apache
Software Foundation in the United States and other countries. Debezium is a
trademark of Red Hat, Inc.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Iceberg" /><category term="Iceberg" /><category term="CDC" /><category term="Deletes" /><category term="Compaction" /><summary type="html">The shape of a CDC pipeline What three merges cost What compaction does, and does not, fix Building the merge correctly Measuring the debt Equality deletes, and why Flink pipelines differ A maintenance schedule that keeps up Choosing the write mode for a CDC table What to monitor Getting the merge right Common misconceptions A model worth keeping References Trademarks TL;DR Three MERGE INTO statements against a merge-on-read table left 5 data files holding 5,960 records and 3 delete files holding 1,120 — for a table that returns 4,840 rows. Over a fifth of what is stored is dead weight the reader filters out. Every merge adds a delete file. The count grows with the number of merges, not the number of changed rows, so a frequent CDC pipeline accumulates them fastest. rewrite_data_files collapsed the data side to 1 file of 4,840 records — the live rows exactly — but 2 delete files survived. Compaction of data is not compaction of deletes. The files metadata table with its content column is the monitor. content = 1 growing over time is the early warning. A CDC table without scheduled maintenance does not fail. It gets slower every hour, with no change in row count to explain it. The shape of a CDC pipeline Change data capture lands a stream of row-level events — inserts, updates, deletes — from a source database into a table. The usual path is Debezium reading the database log, Kafka carrying the changes, and a Spark job applying them. flowchart LR DB[(source DB)] --&amp;gt;|&quot;log&quot;| DBZ[Debezium] DBZ --&amp;gt; K[Kafka] K --&amp;gt; S[&quot;Spark micro-batch&quot;] S --&amp;gt;|&quot;MERGE INTO&quot;| I[(Iceberg table)] I -.-&amp;gt;|&quot;accumulates delete files&quot;| M[&quot;compaction&amp;lt;br/&amp;gt;+ expiry&quot;] The interesting part is the last arrow, and it is the one most pipelines omit. MERGE INTO is the statement that applies a changelog, because it expresses all three operations against a key in one pass: MERGE INTO local.db.cdc t USING chg s ON t.id = s.id WHEN MATCHED AND s.op = &apos;D&apos; THEN DELETE WHEN MATCHED THEN UPDATE SET t.v = s.v, t.updated_at = s.updated_at WHEN NOT MATCHED AND s.op &amp;lt;&amp;gt; &apos;D&apos; THEN INSERT (id, v, updated_at) VALUES (s.id, s.v, s.updated_at) That statement is correct, idempotent when keyed properly, and the reason CDC on Iceberg is straightforward to build. What it does to the files underneath is the part worth measuring. What three merges cost A merge-on-read table loaded with 5,000 rows, then three changelog batches applied, each touching 400 rows with a fifth of them deletes. Reading the files metadata table after each step, where content is 0 for data and 1 for position deletes: After data files / records delete files / records rows the query returns initial load 2 / 5,000 — 5,000 merge #1 3 / 5,320 1 / 400 4,920 merge #2 4 / 5,640 2 / 760 4,880 merge #3 5 / 5,960 3 / 1,120 4,840 Read the last row carefully. The table answers queries with 4,840 rows. To do that it stores 5,960 records across five data files, and applies 1,120 delete records across three delete files to filter them down. Roughly 19% of the stored records are dead, and every scan pays to exclude them. Two patterns are visible in that table and both matter. Data records only ever go up. An update in merge-on-read is a delete plus an insert: the new version is appended, the old one is marked deleted. Nothing shrinks at write time. Delete files grow per merge, not per row. Merge #1 wrote one delete file, merge #2 a second, merge #3 a third. A pipeline running a merge every minute accumulates 1,440 delete files a day regardless of whether each batch changed ten rows or ten thousand. That second point is the one that makes CDC different from occasional updates. The cost tracks your trigger interval, not your data volume. What compaction does, and does not, fix Running the standard data compaction: CALL local.system.rewrite_data_files( table =&amp;gt; &apos;db.cdc&apos;, options =&amp;gt; map(&apos;min-input-files&apos;,&apos;2&apos;)) after rewrite_data_files: data=1f/4840r pos-del=2f/960r rows=4840 The data side is now exactly right: one file holding 4,840 records, which is precisely the live row count. The rewrite read the data files, applied the delete files, and wrote out only surviving rows. The 1,120 dead records are gone from the data. But two delete files remain, holding 960 records. Data compaction did not clear the delete side. Iceberg has a separate procedure for that: CALL local.system.rewrite_position_delete_files(table =&amp;gt; &apos;db.cdc&apos;) The distinction is easy to miss and expensive to miss for a long time. A pipeline that runs rewrite_data_files nightly and calls it maintenance still accumulates delete files, and the read path still applies them. Add the third job, the one that actually reclaims storage: CALL local.system.expire_snapshots(table =&amp;gt; &apos;db.cdc&apos;, retain_last =&amp;gt; 10) Because none of the above deletes anything from disk. Compaction writes new files and leaves the old ones referenced by older snapshots. That accounting is the subject of table maintenance for Iceberg and Hudi. Building the merge correctly The deduplication step is where CDC pipelines go wrong, so here it is in full rather than as a fragment. from pyspark.sql import functions as F, Window def apply_changelog(batch_df, batch_id): # 1. collapse to one row per key, newest wins, using the SOURCE&apos;s ordering # column - never wall-clock arrival time, which reorders under retry. w = Window.partitionBy(&quot;id&quot;).orderBy(F.col(&quot;log_seq&quot;).desc()) latest = (batch_df .withColumn(&quot;rn&quot;, F.row_number().over(w)) .filter(&quot;rn = 1&quot;) .drop(&quot;rn&quot;)) # 2. a tombstone for a key that never existed is a no-op, not an error latest.createOrReplaceTempView(&quot;changes&quot;) batch_df.sparkSession.sql(&quot;&quot;&quot; MERGE INTO local.db.cdc t USING changes s ON t.id = s.id WHEN MATCHED AND s.op = &apos;D&apos; THEN DELETE WHEN MATCHED AND s.log_seq &amp;gt; t.log_seq THEN UPDATE SET * WHEN NOT MATCHED AND s.op &amp;lt;&amp;gt; &apos;D&apos; THEN INSERT *&quot;&quot;&quot;) (stream.writeStream .foreachBatch(apply_changelog) .option(&quot;checkpointLocation&quot;, &quot;/work/ckpt_cdc&quot;) .trigger(availableNow=True) .start()) Three details earn their place. WHEN MATCHED AND s.log_seq &amp;gt; t.log_seq makes the merge idempotent against replay. foreachBatch is at-least-once, so the same batch can be applied twice after a failure; without that guard, a re-applied batch can overwrite a newer row with an older one. Storing the source sequence number in the target is what makes the comparison possible, and it is worth the column. Ordering by the source’s log_seq, not by ingestion timestamp. Debezium emits a monotonic position per source transaction; Kafka delivery order is not guaranteed to match it after a retry or a partition rebalance. WHEN NOT MATCHED AND s.op &amp;lt;&amp;gt; &apos;D&apos; stops a delete for an unknown key from inserting a tombstone row. Measuring the debt One query, on a schedule, tells you whether compaction is keeping up: SELECT sum(CASE WHEN content = 0 THEN record_count ELSE 0 END) AS data_records, sum(CASE WHEN content = 1 THEN record_count ELSE 0 END) AS pos_delete_records, sum(CASE WHEN content = 2 THEN record_count ELSE 0 END) AS eq_delete_records, round(100.0 * sum(CASE WHEN content &amp;gt; 0 THEN record_count ELSE 0 END) / nullif(sum(CASE WHEN content = 0 THEN record_count ELSE 0 END), 0), 1) AS pct_dead FROM local.db.cdc.files; pct_dead is the fraction of stored records that every scan filters out. On the measured table it reached 19% after three merges. Two thresholds worth alerting on: above ~10% sustained means compaction is behind; a value that climbs every day means it is not running at all. Pair it with a file-count trend, because record share and file count degrade differently: SELECT content, count(*) AS files FROM local.db.cdc.files GROUP BY content; Many small delete files hurt even when they hold few records, because each is opened per scan. Equality deletes, and why Flink pipelines differ Spark writes position deletes: this file, these row positions. Flink CDC writers commonly write equality deletes: rows where these columns equal these values. The difference is not academic. A position delete is applied by skipping known offsets in one named file. An equality delete is a predicate the reader must evaluate against every data file in scope with a lower sequence number — it does not know where the matching rows live. -- which kind is this table accumulating? SELECT content, count(*) FROM local.db.cdc.files WHERE content &amp;gt; 0 GROUP BY content; content = 2 rows are equality deletes. A table mixing both, because two engines write to it, pays both costs and is the hardest case to reason about. If you control the topology, pick one writer per table. A maintenance schedule that keeps up CDC tables need maintenance proportional to merge frequency, not data volume: -- hourly: collapse the files the last 60 merges produced CALL local.system.rewrite_data_files( table =&amp;gt; &apos;db.cdc&apos;, where =&amp;gt; &apos;updated_at &amp;gt;= current_timestamp() - INTERVAL 2 HOURS&apos;, options =&amp;gt; map(&apos;min-input-files&apos;,&apos;5&apos;)); -- hourly: and clear the delete files, which the above does not CALL local.system.rewrite_position_delete_files(table =&amp;gt; &apos;db.cdc&apos;); -- daily: reclaim, with retention equal to your recovery window CALL local.system.expire_snapshots(table =&amp;gt; &apos;db.cdc&apos;, retain_last =&amp;gt; 24); The where clause matters on a CDC table. Rewriting the whole table hourly costs more than the merges did. Scope compaction to the partitions the merges actually touched. Choosing the write mode for a CDC table The table above is merge-on-read, which is the usual choice for CDC and worth justifying rather than assuming. Under copy-on-write, each merge rewrites every data file containing an affected row. A changelog touching 400 scattered keys could rewrite most of the table, every minute. That does not keep up, and it is why CDC pipelines choose merge-on-read. The trade is the one measured above: writes stay cheap, reads carry the delete files until compaction runs. Set it deliberately, per operation: CREATE TABLE local.db.cdc (...) USING iceberg TBLPROPERTIES ( &apos;write.merge.mode&apos; = &apos;merge-on-read&apos;, &apos;write.delete.mode&apos; = &apos;merge-on-read&apos;, &apos;write.update.mode&apos; = &apos;merge-on-read&apos;) The full comparison, with the same DELETE measured under both modes, is in copy-on-write or merge-on-read. One caveat for Flink-based CDC. Spark writes position deletes, which name a file and row positions. Flink CDC writers commonly produce equality deletes, which are predicates on key columns. Equality deletes are more expensive to apply, because the reader evaluates them against every data file in scope rather than one named file. A pipeline mixing engines accumulates both kinds, and the read cost is not the same. What to monitor One query, run on a schedule, answers whether the pipeline is healthy: SELECT content, count(*) AS files, sum(record_count) AS records FROM local.db.cdc.files GROUP BY content; content = 0 is data, 1 is position deletes, 2 is equality deletes. Watch the ratio, not the absolute numbers. Delete records as a fraction of data records is the fraction of read work that is wasted. On the measured table that ratio reached 19% after three merges. A steady-state pipeline with maintenance keeps it low; one without maintenance climbs until queries hurt. Two more signals worth alerting on. Delete file count climbing across days means compaction is not keeping pace with the merge frequency, and the fix is usually to compact more often rather than to merge less often. Query planning time growing while row count is flat points at metadata rather than data, which is snapshot and manifest accumulation, not delete files. Getting the merge right Two correctness issues matter more than the performance ones. Deduplicate the changelog before merging. If a batch contains two events for the same key, MERGE INTO raises an error rather than picking one, because the result would be ambiguous. Collapse to the latest event per key first, typically with a window function on the source’s log sequence number or timestamp. Using wall-clock arrival time here is a common bug: out-of-order delivery then applies an older version over a newer one. Make the match condition the real key. Matching on a column that is not unique in the target produces the same ambiguity error, or worse, silently updates more rows than intended when it happens to be unique in the sample you tested with. Common misconceptions “MERGE INTO updates rows in place.” In merge-on-read it appends new versions and writes delete records for the old ones. Storage grows on every merge. “Compaction cleans up deletes.” rewrite_data_files applies them to the data it rewrites; delete files can survive it. rewrite_position_delete_files is the dedicated procedure. “The row count tells me the table is healthy.” The measured table returned 4,840 rows while storing 5,960 data records and 1,120 delete records. Row count is exactly the number that hides this. “More frequent merges are strictly better for freshness.” They are also strictly worse for delete-file accumulation, because the file count follows the merge count. “Copy-on-write would avoid all this.” It avoids delete files by rewriting data files instead, which for a scattered changelog can mean rewriting most of the table per batch. A model worth keeping A CDC pipeline on Iceberg is a debt arrangement. Every merge borrows read performance to keep write latency low, and compaction is the repayment. The borrowing is automatic and the repayment is not, which is why the failure mode is a table that works perfectly for a month and then gets slow for no visible reason. Schedule rewrite_data_files, rewrite_position_delete_files and expire_snapshots when you build the pipeline, and monitor the delete-to-data ratio so you find out before your users do. References Iceberg maintenance procedures for rewrite_data_files, rewrite_position_delete_files and expire_snapshots Iceberg writes with Spark for MERGE INTO semantics and its restrictions Iceberg spec: delete formats for position versus equality deletes Copy-on-write or merge-on-read for the mode this pipeline depends on Streaming into Iceberg for the trigger that decides how often merges happen Trademarks Apache Iceberg, Apache Spark, Apache Flink, Apache Kafka, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries. Debezium is a trademark of Red Hat, Inc.</summary></entry><entry><title type="html">Time travel and rollback in Iceberg: undoing a bad write in one statement</title><link href="https://rangareddy.github.io/IcebergTimeTravel/" rel="alternate" type="text/html" title="Time travel and rollback in Iceberg: undoing a bad write in one statement" /><published>2026-09-07T10:00:00+05:30</published><updated>2026-09-07T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergTimeTravel</id><content type="html" xml:base="https://rangareddy.github.io/IcebergTimeTravel/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#every-commit-is-a-restore-point&quot; id=&quot;markdown-toc-every-commit-is-a-restore-point&quot;&gt;Every commit is a restore point&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#reading-an-old-state&quot; id=&quot;markdown-toc-reading-an-old-state&quot;&gt;Reading an old state&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#undoing-a-bad-write&quot; id=&quot;markdown-toc-undoing-a-bad-write&quot;&gt;Undoing a bad write&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#a-rollback-is-itself-recorded&quot; id=&quot;markdown-toc-a-rollback-is-itself-recorded&quot;&gt;A rollback is itself recorded&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-tables&quot; id=&quot;markdown-toc-the-tables&quot;&gt;The tables that answer questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#retention-is-recovery&quot; id=&quot;markdown-toc-retention-is-recovery&quot;&gt;Retention is your recovery window&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#finding-the-snapshot&quot; id=&quot;markdown-toc-finding-the-snapshot&quot;&gt;Finding the snapshot you want&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#incremental-reads&quot; id=&quot;markdown-toc-incremental-reads&quot;&gt;Incremental reads between snapshots&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#write-audit-publish&quot; id=&quot;markdown-toc-write-audit-publish&quot;&gt;Write-audit-publish&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#retention-policy&quot; id=&quot;markdown-toc-retention-policy&quot;&gt;Retention, as a policy&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#branches-and-tags&quot; id=&quot;markdown-toc-branches-and-tags&quot;&gt;Branches and tags&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Reading an old state is a query: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VERSION AS OF &amp;lt;snapshot_id&amp;gt;&lt;/code&gt; returned &lt;strong&gt;2 rows&lt;/strong&gt; on a table whose current state had 3.&lt;/li&gt;
    &lt;li&gt;Undoing a bad write is one call. After two accidental rows took a table from 3 to &lt;strong&gt;5&lt;/strong&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_snapshot&lt;/code&gt; returned it to &lt;strong&gt;3&lt;/strong&gt;.&lt;/li&gt;
    &lt;li&gt;A rollback does not erase history. The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;history&lt;/code&gt; table kept the abandoned snapshot with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;is_current_ancestor=false&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Your recovery window is exactly your snapshot retention. Expiring snapshots deletes the ability to go back, which is why expiry is a recovery policy, not a cleanup chore.&lt;/li&gt;
    &lt;li&gt;Snapshots are cheap because they share metadata, not because they are free — each commit still writes three metadata files.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;every-commit-is-a-restore-point&quot;&gt;Every commit is a restore point&lt;/h2&gt;

&lt;p&gt;Iceberg never edits a snapshot. A commit writes new metadata describing the new
state and leaves the previous metadata intact, so every commit the table has ever
made is still described somewhere, as long as it has not been expired.&lt;/p&gt;

&lt;p&gt;Two things follow: you can &lt;strong&gt;read&lt;/strong&gt; any retained state, and you can &lt;strong&gt;return&lt;/strong&gt; to
it.&lt;/p&gt;

&lt;h2 id=&quot;reading-an-old-state&quot;&gt;Reading an old state&lt;/h2&gt;

&lt;p&gt;A table with two commits — two rows, then a third:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;snapshot_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tt&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;snapshots: [5422029406646937356, 9044140986145286505]
current rows        = 3
VERSION AS OF first = 2
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tt&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VERSION&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;OF&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;5422029406646937356&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Two rows, because that is what the table held at that snapshot. The same works by
time:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tt&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;TIMESTAMP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;OF&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;2026-09-24 10:00:00&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;which resolves to the snapshot current at that moment.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;This is a normal query.&lt;/strong&gt; It plans the same way against different metadata, so
there is no special slow path — the read-path funnel just starts from a different
snapshot.&lt;/p&gt;

&lt;h2 id=&quot;undoing-a-bad-write&quot;&gt;Undoing a bad write&lt;/h2&gt;

&lt;p&gt;The case this exists for. Two rows land that should not have:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;after bad write = 5
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rollback_to_snapshot&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;db.tt&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;9044140986145286505&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;after rollback  = 3
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;One statement, back to three rows.&lt;/strong&gt; No restore from backup, no rewriting, no
downtime — the catalog pointer now names the earlier snapshot, and readers see
that state immediately.&lt;/p&gt;

&lt;p&gt;There is a companion for the timestamp case,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_timestamp&lt;/code&gt;, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;set_current_snapshot&lt;/code&gt; for moving to an arbitrary
snapshot rather than strictly backwards.&lt;/p&gt;

&lt;h3 id=&quot;a-rollback-is-itself-recorded&quot;&gt;A rollback is itself recorded&lt;/h3&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;history rows:
    snap=5422029406646937356  is_current_ancestor=True
    snap=9044140986145286505  is_current_ancestor=True
    snap=5042529038394299375  is_current_ancestor=False
    snap=9044140986145286505  is_current_ancestor=True
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Four entries for three snapshots. The bad snapshot &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;5042529038394299375&lt;/code&gt; is still
listed, flagged &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;is_current_ancestor=false&lt;/code&gt; — it happened, and it is no longer in
the current table’s lineage. The final entry records the table being pointed back
at the earlier snapshot.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Rollback does not erase history; it changes which history is current.&lt;/strong&gt; That
matters for auditing: the record that a bad write occurred, and was reverted,
survives.&lt;/p&gt;

&lt;p&gt;It also means a rollback is itself reversible — until the abandoned snapshot is
expired, you can roll forward again.&lt;/p&gt;

&lt;h2 id=&quot;the-tables&quot;&gt;The tables that answer questions&lt;/h2&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Table&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Question&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.snapshots&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;what commits exist, what operation, what changed&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.history&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;what the current state’s lineage is, including abandoned branches&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.files&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;which data and delete files the current snapshot references&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.manifests&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;how much metadata planning must read&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.refs&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;named branches and tags&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.metadata_log_entries&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;every metadata file written&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;operation&lt;/code&gt; column on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.snapshots&lt;/code&gt; is the first place to look when a table
changed unexpectedly: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;replace&lt;/code&gt; tell you what
kind of write happened, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;summary&lt;/code&gt; carries record counts.&lt;/p&gt;

&lt;h2 id=&quot;retention-is-recovery&quot;&gt;Retention is your recovery window&lt;/h2&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;expire_snapshots&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.tt&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;older_than&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;TIMESTAMP&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;2026-09-17 00:00:00&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;retain_last&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;That statement deletes your ability to go back.&lt;/strong&gt; It removes snapshots and the
data files only they referenced, which is the only operation that reclaims
storage — and simultaneously the operation that shortens how far back you can
recover.&lt;/p&gt;

&lt;p&gt;So retention is two numbers at once: storage cost and recovery window. Seven days
of snapshots is seven days in which a bad write can be undone, and seven days of
storage for every file those snapshots reference.&lt;/p&gt;

&lt;p&gt;Decide it as a recovery policy. “How long before we would certainly have noticed a
bad write?” is the right question, and the storage follows from the answer rather
than driving it.&lt;/p&gt;

&lt;p&gt;Snapshots are affordable because manifest reuse means each one describes only
what changed — but they are not free, since each commit still writes three
metadata files. A table committing every minute accumulates fast, and its
retention needs to reflect commit frequency rather than calendar intuition.&lt;/p&gt;

&lt;h2 id=&quot;finding-the-snapshot&quot;&gt;Finding the snapshot you want&lt;/h2&gt;

&lt;p&gt;Rolling back means naming a snapshot, and “the one before the bad write” has to
be found rather than guessed.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- what happened, most recent first&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;snapshot_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;parent_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;operation&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;n&quot;&gt;summary&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;added-records&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;   &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;added&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;n&quot;&gt;summary&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;deleted-records&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;deleted&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;n&quot;&gt;summary&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;total-records&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;   &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;total&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DESC&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;LIMIT&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;20&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;operation&lt;/code&gt; column narrows it immediately: a table that should only ever be
appended to, showing an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt;, has found its culprit. The
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;summary&lt;/code&gt; map carries the record deltas, so a commit that removed far more rows
than expected is visible without reading any data.&lt;/p&gt;

&lt;p&gt;To see the damage before undoing it, diff two snapshots:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows_then&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VERSION&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;OF&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;9044140986145286505&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows_now&lt;/span&gt;  &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Or compare aggregates per partition, which localises the problem:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;partition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;rows&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;partition&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;partition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;incremental-reads&quot;&gt;Incremental reads between snapshots&lt;/h2&gt;

&lt;p&gt;Time travel’s less-known sibling: reading only what changed between two
snapshots, which is how downstream pipelines avoid rescanning a table.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;start-snapshot-id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;5422029406646937356&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;end-snapshot-id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;   &lt;span class=&quot;s&quot;&gt;&quot;9044140986145286505&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;This returns the rows &lt;strong&gt;appended&lt;/strong&gt; between those snapshots. Two caveats that
matter: it covers appends only — an incremental read spanning an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; or
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt; raises rather than silently under-reporting — and both snapshots must
still exist, so retention bounds how far behind a consumer may fall.&lt;/p&gt;

&lt;p&gt;The pattern for a downstream job is to store the last snapshot id it processed,
then read forward from it:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;last&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;read_watermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                       &lt;span class=&quot;c1&quot;&gt;# your own state store
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cur&lt;/span&gt;  &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT snapshot_id FROM local.db.events.snapshots &quot;&lt;/span&gt;
                 &lt;span class=&quot;s&quot;&gt;&quot;ORDER BY committed_at DESC LIMIT 1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;][&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;last&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;!=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cur&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
              &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;start-snapshot-id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;last&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
              &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;end-snapshot-id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cur&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
              &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;process&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;write_watermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cur&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;write-audit-publish&quot;&gt;Write-audit-publish&lt;/h2&gt;

&lt;p&gt;Branches turn “check before anyone sees it” into a mechanism rather than a
convention.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- 1. write to a branch nobody reads&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;BRANCH&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;`audit`&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;branch&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;audit&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;append&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- 2. run your checks against the branch&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VERSION&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;OF&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;audit&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- 3. publish only if they pass&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fast_forward&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;main&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;audit&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;If the checks fail, drop the branch and nothing ever reached readers:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DROP&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;BRANCH&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;`audit`&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;This is the difference between catching a bad load and rolling one back.&lt;/strong&gt; A
rollback is visible to anyone who queried in between; a failed audit branch is
not.&lt;/p&gt;

&lt;h2 id=&quot;retention-policy&quot;&gt;Retention, as a policy&lt;/h2&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;history.expire.max-snapshot-age-ms&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;604800000&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;-- 7 days&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;history.expire.min-snapshots-to-keep&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;50&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Two settings, because age alone is a poor policy for a table whose commit rate
varies. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;min-snapshots-to-keep&lt;/code&gt; guarantees a floor: a table that went quiet for a
week still has 50 restore points rather than none.&lt;/p&gt;

&lt;p&gt;Tags pin individual snapshots beyond the general policy, which is what you want
for a month-end close or a model-training input:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TAG&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;`close-2026-09`&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;OF&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VERSION&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;9044140986145286505&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;RETAIN&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;365&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;DAYS&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Check what is pinned before wondering why expiry reclaimed less than expected:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;type&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;snapshot_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;max_reference_age_in_ms&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;refs&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;branches-and-tags&quot;&gt;Branches and tags&lt;/h2&gt;

&lt;p&gt;For states that should outlive ordinary retention, name them:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tt&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TAG&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;`month-end-2026-09`&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;OF&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VERSION&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;9044140986145286505&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;RETAIN&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;365&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;DAYS&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A tag pins a snapshot with its own retention, so an audit point survives a
7-day expiry policy. A branch does the same for a line of development, letting
you write to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;audit&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;wap&lt;/code&gt; without affecting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;main&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;That is also the mechanism behind write-audit-publish: write to a branch, run
checks, then fast-forward the main reference only if they pass.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Time travel keeps a copy of the table.”&lt;/strong&gt; It keeps metadata pointing at files
that already exist. Old snapshots share files with new ones.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Rollback deletes the bad data.”&lt;/strong&gt; It changes which snapshot is current. The
abandoned snapshot stays in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;history&lt;/code&gt; until expired.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“I can always go back.”&lt;/strong&gt; Only as far as your retention. Expiry is
irreversible.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Time travel queries are slow.”&lt;/strong&gt; They start from different metadata and plan
identically.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Expiring snapshots is routine cleanup.”&lt;/strong&gt; It is deleting your recovery window
and should be set deliberately.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;Every commit leaves a snapshot; the catalog points at one of them. Time travel
reads a different one, rollback points at a different one, and expiry destroys
the ones you no longer keep.&lt;/p&gt;

&lt;p&gt;Which makes the retention setting the most consequential number on a production
table — it is simultaneously your storage bill and your undo button.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-queries/#time-travel&quot;&gt;Iceberg Spark queries&lt;/a&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VERSION AS OF&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TIMESTAMP AS OF&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-procedures/#snapshot-management&quot;&gt;Iceberg Spark procedures&lt;/a&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_snapshot&lt;/code&gt; and friends&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/branching/&quot;&gt;Iceberg branching and tagging&lt;/a&gt; for named references and write-audit-publish&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergWritePath/&quot;&gt;Life of a write query in Iceberg&lt;/a&gt; for the commits that create these snapshots&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/TableMaintenance/&quot;&gt;Table maintenance for Iceberg and Hudi&lt;/a&gt; for what expiry reclaims&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Spark, Apache and the Apache feather logo are either
registered trademarks or trademarks of The Apache Software Foundation in the
United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Iceberg" /><category term="Iceberg" /><category term="Snapshots" /><category term="Recovery" /><category term="History" /><summary type="html">Every commit is a restore point Reading an old state Undoing a bad write A rollback is itself recorded The tables that answer questions Retention is your recovery window Finding the snapshot you want Incremental reads between snapshots Write-audit-publish Retention, as a policy Branches and tags Common misconceptions A model worth keeping References Trademarks TL;DR Reading an old state is a query: VERSION AS OF &amp;lt;snapshot_id&amp;gt; returned 2 rows on a table whose current state had 3. Undoing a bad write is one call. After two accidental rows took a table from 3 to 5, rollback_to_snapshot returned it to 3. A rollback does not erase history. The history table kept the abandoned snapshot with is_current_ancestor=false. Your recovery window is exactly your snapshot retention. Expiring snapshots deletes the ability to go back, which is why expiry is a recovery policy, not a cleanup chore. Snapshots are cheap because they share metadata, not because they are free — each commit still writes three metadata files. Every commit is a restore point Iceberg never edits a snapshot. A commit writes new metadata describing the new state and leaves the previous metadata intact, so every commit the table has ever made is still described somewhere, as long as it has not been expired. Two things follow: you can read any retained state, and you can return to it. Reading an old state A table with two commits — two rows, then a third: SELECT snapshot_id, committed_at FROM local.db.tt.snapshots ORDER BY committed_at; snapshots: [5422029406646937356, 9044140986145286505] current rows = 3 VERSION AS OF first = 2 SELECT count(*) FROM local.db.tt VERSION AS OF 5422029406646937356; Two rows, because that is what the table held at that snapshot. The same works by time: SELECT * FROM local.db.tt TIMESTAMP AS OF &apos;2026-09-24 10:00:00&apos;; which resolves to the snapshot current at that moment. This is a normal query. It plans the same way against different metadata, so there is no special slow path — the read-path funnel just starts from a different snapshot. Undoing a bad write The case this exists for. Two rows land that should not have: after bad write = 5 CALL local.system.rollback_to_snapshot(&apos;db.tt&apos;, 9044140986145286505); after rollback = 3 One statement, back to three rows. No restore from backup, no rewriting, no downtime — the catalog pointer now names the earlier snapshot, and readers see that state immediately. There is a companion for the timestamp case, rollback_to_timestamp, and set_current_snapshot for moving to an arbitrary snapshot rather than strictly backwards. A rollback is itself recorded history rows: snap=5422029406646937356 is_current_ancestor=True snap=9044140986145286505 is_current_ancestor=True snap=5042529038394299375 is_current_ancestor=False snap=9044140986145286505 is_current_ancestor=True Four entries for three snapshots. The bad snapshot 5042529038394299375 is still listed, flagged is_current_ancestor=false — it happened, and it is no longer in the current table’s lineage. The final entry records the table being pointed back at the earlier snapshot. Rollback does not erase history; it changes which history is current. That matters for auditing: the record that a bad write occurred, and was reverted, survives. It also means a rollback is itself reversible — until the abandoned snapshot is expired, you can roll forward again. The tables that answer questions Table Question .snapshots what commits exist, what operation, what changed .history what the current state’s lineage is, including abandoned branches .files which data and delete files the current snapshot references .manifests how much metadata planning must read .refs named branches and tags .metadata_log_entries every metadata file written The operation column on .snapshots is the first place to look when a table changed unexpectedly: append, overwrite, delete and replace tell you what kind of write happened, and summary carries record counts. Retention is your recovery window CALL local.system.expire_snapshots( table =&amp;gt; &apos;db.tt&apos;, older_than =&amp;gt; TIMESTAMP &apos;2026-09-17 00:00:00&apos;, retain_last =&amp;gt; 10); That statement deletes your ability to go back. It removes snapshots and the data files only they referenced, which is the only operation that reclaims storage — and simultaneously the operation that shortens how far back you can recover. So retention is two numbers at once: storage cost and recovery window. Seven days of snapshots is seven days in which a bad write can be undone, and seven days of storage for every file those snapshots reference. Decide it as a recovery policy. “How long before we would certainly have noticed a bad write?” is the right question, and the storage follows from the answer rather than driving it. Snapshots are affordable because manifest reuse means each one describes only what changed — but they are not free, since each commit still writes three metadata files. A table committing every minute accumulates fast, and its retention needs to reflect commit frequency rather than calendar intuition. Finding the snapshot you want Rolling back means naming a snapshot, and “the one before the bad write” has to be found rather than guessed. -- what happened, most recent first SELECT snapshot_id, parent_id, operation, summary[&apos;added-records&apos;] AS added, summary[&apos;deleted-records&apos;] AS deleted, summary[&apos;total-records&apos;] AS total, committed_at FROM local.db.events.snapshots ORDER BY committed_at DESC LIMIT 20; The operation column narrows it immediately: a table that should only ever be appended to, showing an overwrite or delete, has found its culprit. The summary map carries the record deltas, so a commit that removed far more rows than expected is visible without reading any data. To see the damage before undoing it, diff two snapshots: SELECT count(*) AS rows_then FROM local.db.events VERSION AS OF 9044140986145286505; SELECT count(*) AS rows_now FROM local.db.events; Or compare aggregates per partition, which localises the problem: SELECT partition, sum(record_count) AS rows FROM local.db.events.files GROUP BY partition ORDER BY partition; Incremental reads between snapshots Time travel’s less-known sibling: reading only what changed between two snapshots, which is how downstream pipelines avoid rescanning a table. (spark.read .format(&quot;iceberg&quot;) .option(&quot;start-snapshot-id&quot;, &quot;5422029406646937356&quot;) .option(&quot;end-snapshot-id&quot;, &quot;9044140986145286505&quot;) .load(&quot;local.db.events&quot;) .count()) This returns the rows appended between those snapshots. Two caveats that matter: it covers appends only — an incremental read spanning an overwrite or delete raises rather than silently under-reporting — and both snapshots must still exist, so retention bounds how far behind a consumer may fall. The pattern for a downstream job is to store the last snapshot id it processed, then read forward from it: last = read_watermark() # your own state store cur = spark.sql(&quot;SELECT snapshot_id FROM local.db.events.snapshots &quot; &quot;ORDER BY committed_at DESC LIMIT 1&quot;).collect()[0][0] if last != cur: df = (spark.read.format(&quot;iceberg&quot;) .option(&quot;start-snapshot-id&quot;, last) .option(&quot;end-snapshot-id&quot;, cur) .load(&quot;local.db.events&quot;)) process(df) write_watermark(cur) Write-audit-publish Branches turn “check before anyone sees it” into a mechanism rather than a convention. -- 1. write to a branch nobody reads ALTER TABLE local.db.events CREATE BRANCH `audit`; (df.writeTo(&quot;local.db.events&quot;).option(&quot;branch&quot;, &quot;audit&quot;).append()) -- 2. run your checks against the branch SELECT count(*) FROM local.db.events VERSION AS OF &apos;audit&apos;; -- 3. publish only if they pass CALL local.system.fast_forward(&apos;db.events&apos;, &apos;main&apos;, &apos;audit&apos;); If the checks fail, drop the branch and nothing ever reached readers: ALTER TABLE local.db.events DROP BRANCH `audit`; This is the difference between catching a bad load and rolling one back. A rollback is visible to anyone who queried in between; a failed audit branch is not. Retention, as a policy ALTER TABLE local.db.events SET TBLPROPERTIES ( &apos;history.expire.max-snapshot-age-ms&apos; = &apos;604800000&apos;, -- 7 days &apos;history.expire.min-snapshots-to-keep&apos; = &apos;50&apos;); Two settings, because age alone is a poor policy for a table whose commit rate varies. min-snapshots-to-keep guarantees a floor: a table that went quiet for a week still has 50 restore points rather than none. Tags pin individual snapshots beyond the general policy, which is what you want for a month-end close or a model-training input: ALTER TABLE local.db.events CREATE TAG `close-2026-09` AS OF VERSION 9044140986145286505 RETAIN 365 DAYS; Check what is pinned before wondering why expiry reclaimed less than expected: SELECT name, type, snapshot_id, max_reference_age_in_ms FROM local.db.events.refs; Branches and tags For states that should outlive ordinary retention, name them: ALTER TABLE local.db.tt CREATE TAG `month-end-2026-09` AS OF VERSION 9044140986145286505 RETAIN 365 DAYS; A tag pins a snapshot with its own retention, so an audit point survives a 7-day expiry policy. A branch does the same for a line of development, letting you write to audit or wap without affecting main. That is also the mechanism behind write-audit-publish: write to a branch, run checks, then fast-forward the main reference only if they pass. Common misconceptions “Time travel keeps a copy of the table.” It keeps metadata pointing at files that already exist. Old snapshots share files with new ones. “Rollback deletes the bad data.” It changes which snapshot is current. The abandoned snapshot stays in history until expired. “I can always go back.” Only as far as your retention. Expiry is irreversible. “Time travel queries are slow.” They start from different metadata and plan identically. “Expiring snapshots is routine cleanup.” It is deleting your recovery window and should be set deliberately. A model worth keeping Every commit leaves a snapshot; the catalog points at one of them. Time travel reads a different one, rollback points at a different one, and expiry destroys the ones you no longer keep. Which makes the retention setting the most consequential number on a production table — it is simultaneously your storage bill and your undo button. References Iceberg Spark queries for VERSION AS OF and TIMESTAMP AS OF Iceberg Spark procedures for rollback_to_snapshot and friends Iceberg branching and tagging for named references and write-audit-publish Life of a write query in Iceberg for the commits that create these snapshots Table maintenance for Iceberg and Hudi for what expiry reclaims Trademarks Apache Iceberg, Apache Spark, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Copy-on-write or merge-on-read: choosing how an Iceberg table pays for a delete</title><link href="https://rangareddy.github.io/IcebergCopyOnWriteMergeOnRead/" rel="alternate" type="text/html" title="Copy-on-write or merge-on-read: choosing how an Iceberg table pays for a delete" /><published>2026-09-05T10:00:00+05:30</published><updated>2026-09-05T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergCopyOnWriteMergeOnRead</id><content type="html" xml:base="https://rangareddy.github.io/IcebergCopyOnWriteMergeOnRead/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#the-question&quot; id=&quot;markdown-toc-the-question&quot;&gt;The question this setting answers&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-same-delete-measured-twice&quot; id=&quot;markdown-toc-the-same-delete-measured-twice&quot;&gt;The same delete, measured twice&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-a-delete-file-is&quot; id=&quot;markdown-toc-what-a-delete-file-is&quot;&gt;What a delete file actually is&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#reproducing&quot; id=&quot;markdown-toc-reproducing&quot;&gt;Reproducing the measurement&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-the-reader-does&quot; id=&quot;markdown-toc-what-the-reader-does&quot;&gt;What the reader does with a delete file&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#tuning-each-mode&quot; id=&quot;markdown-toc-tuning-each-mode&quot;&gt;Tuning each mode&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing-per-operation&quot; id=&quot;markdown-toc-choosing-per-operation&quot;&gt;Choosing per operation, in practice&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#modes-are-per-operation&quot; id=&quot;markdown-toc-modes-are-per-operation&quot;&gt;The modes are per operation&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#which-one&quot; id=&quot;markdown-toc-which-one&quot;&gt;Which one does your workload want?&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-neither-mode-does&quot; id=&quot;markdown-toc-what-neither-mode-does&quot;&gt;What neither mode does&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#how-to-tell&quot; id=&quot;markdown-toc-how-to-tell&quot;&gt;How to tell which mode a table is using&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;The same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt; on two tables differing only in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.delete.mode&lt;/code&gt; left &lt;strong&gt;2 data files holding 3,900 records&lt;/strong&gt; under copy-on-write, and &lt;strong&gt;2 data files still holding 4,000 records plus a 100-record delete file&lt;/strong&gt; under merge-on-read. Both queries return 3,900 rows.&lt;/li&gt;
    &lt;li&gt;The snapshot’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;operation&lt;/code&gt; names the choice: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; for copy-on-write, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt; for merge-on-read.&lt;/li&gt;
    &lt;li&gt;The setting is per operation, not per table: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.delete.mode&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.update.mode&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.merge.mode&lt;/code&gt; are independent, so a table can rewrite on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt; and defer on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Merge-on-read moves cost from write time to &lt;strong&gt;every read until compaction runs&lt;/strong&gt;. A CDC table that never compacts gets slower every hour.&lt;/li&gt;
    &lt;li&gt;Neither mode reclaims space on its own. The files the old snapshot still references stay until &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;expire_snapshots&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;the-question&quot;&gt;The question this setting answers&lt;/h2&gt;

&lt;p&gt;Deleting one row from a table stored as immutable Parquet files is not obvious.
The file holding that row cannot be edited in place, so something has to give.&lt;/p&gt;

&lt;p&gt;Iceberg offers two answers, and you choose per table.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Copy-on-write&lt;/strong&gt; rewrites the whole data file without the deleted row, and the
new snapshot points at the replacement. The cost is paid now, once, by the
writer.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Merge-on-read&lt;/strong&gt; leaves the data file alone and writes a small &lt;strong&gt;delete file&lt;/strong&gt;
recording which rows no longer count. The cost is paid later, repeatedly, by
every reader that has to apply it.&lt;/p&gt;

&lt;p&gt;That is the entire trade, and it is worth seeing in the files rather than taking
on faith.&lt;/p&gt;

&lt;h2 id=&quot;the-same-delete-measured-twice&quot;&gt;The same delete, measured twice&lt;/h2&gt;

&lt;p&gt;Two tables, identical except for their mode, each loaded with 4,000 rows in 2
data files:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cow&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;BIGINT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;STRING&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;write.delete.mode&apos;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;copy-on-write&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mor&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;BIGINT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;STRING&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;write.delete.mode&apos;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;merge-on-read&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Then the identical statement against each:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;DELETE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;table&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Reading the accounting from the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;files&lt;/code&gt; metadata table, where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content&lt;/code&gt; is 0 for
data and 1 for position deletes:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
      &lt;th style=&quot;text-align: right&quot;&gt;data files&lt;/th&gt;
      &lt;th style=&quot;text-align: right&quot;&gt;records in them&lt;/th&gt;
      &lt;th style=&quot;text-align: right&quot;&gt;delete files&lt;/th&gt;
      &lt;th style=&quot;text-align: right&quot;&gt;query returns&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;copy-on-write&lt;/strong&gt; before&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;2&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;4,000&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;0&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;4,000&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;copy-on-write&lt;/strong&gt; after&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;2&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;3,900&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;0&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;3,900&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;merge-on-read&lt;/strong&gt; before&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;2&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;4,000&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;0&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;4,000&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;merge-on-read&lt;/strong&gt; after&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;2&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;4,000&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;&lt;strong&gt;1&lt;/strong&gt; (100 records)&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;3,900&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;img src=&quot;/assets/images/iceberg/cow-vs-mor.png&quot; alt=&quot;Deleting 100 rows under each mode: copy-on-write rewrites the data files so two files hold 3,900 records and the snapshot operation is overwrite; merge-on-read leaves both data files holding 4,000 records and adds a 100-position delete file, with the snapshot operation delete and the reader merging both to return 3,900 rows&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;That table is the whole post.&lt;/strong&gt; Under copy-on-write the data files were
rewritten: still two files, now containing 3,900 records, the 100 deleted rows
physically gone from the current snapshot. Under merge-on-read the data files
were never touched — they still hold all 4,000 records — and a separate file
records the 100 positions to skip. Both queries return 3,900 rows, because the
reader merges the delete file on the way past.&lt;/p&gt;

&lt;p&gt;The snapshot log names what happened, without you having to count files:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;copy-on-write   snapshot ops: [&apos;append&apos;, &apos;overwrite&apos;]
merge-on-read   snapshot ops: [&apos;append&apos;, &apos;delete&apos;]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; means files were replaced. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt; means a delete file was added.
If you ever need to know which mode a past operation used, that column is the
record.&lt;/p&gt;

&lt;h2 id=&quot;what-a-delete-file-is&quot;&gt;What a delete file actually is&lt;/h2&gt;

&lt;p&gt;The merge-on-read run produced one extra file next to the data:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;00000-45-0ec67ff2-63f5-4f54-83a2-58f5ac1bdb24-00001-deletes.parquet
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;It is a Parquet file like any other, which is a detail worth internalising:
&lt;strong&gt;counting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*.parquet&lt;/code&gt; in a table directory does not tell you how many data files
there are.&lt;/strong&gt; My first attempt at this measurement did exactly that and reported
merge-on-read as having gained a data file. It had not; it had gained a delete
file. The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;files&lt;/code&gt; metadata table and its &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content&lt;/code&gt; column are the honest source.&lt;/p&gt;

&lt;p&gt;There are two kinds, and the difference matters operationally:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Kind&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What it records&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Written by&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Position delete&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;this file, these row positions&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark, and most engines&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Equality delete&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;rows where these columns equal these values&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;streaming writers, notably Flink&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;A position delete is cheap to apply, because the reader knows exactly which rows
in which file to skip. An equality delete is a predicate, so the reader must
evaluate it against rows it reads, and it applies to every data file in scope
rather than one named file. Equality deletes let a streaming writer delete
without first finding out where the row lives, which is why Flink CDC pipelines
produce them, and they are the more expensive of the two to read.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
    subgraph COW[&quot;copy-on-write&quot;]
      D1[&quot;data file&amp;lt;br/&amp;gt;4000 rows&quot;] --&amp;gt;|&quot;DELETE rewrites it&quot;| D2[&quot;new data file&amp;lt;br/&amp;gt;3900 rows&quot;]
    end
    subgraph MOR[&quot;merge-on-read&quot;]
      E1[&quot;data file&amp;lt;br/&amp;gt;4000 rows, untouched&quot;] --&amp;gt; R[&quot;reader merges&quot;]
      E2[&quot;delete file&amp;lt;br/&amp;gt;100 positions&quot;] --&amp;gt; R
      R --&amp;gt; O[&quot;3900 rows&quot;]
    end
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;reproducing&quot;&gt;Reproducing the measurement&lt;/h2&gt;

&lt;p&gt;The whole comparison is twenty lines, and running it against your own data is
more convincing than any table here.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;functions&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;build&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;DROP TABLE IF EXISTS local.db.&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; PURGE&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
        CREATE TABLE local.db.&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; (id BIGINT, v STRING) USING iceberg
        TBLPROPERTIES (
          &apos;write.delete.mode&apos; = &apos;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;,
          &apos;write.update.mode&apos; = &apos;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;,
          &apos;write.merge.mode&apos;  = &apos;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;)&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;v&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;concat&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;v&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;selectExpr&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;v&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;append&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;accounting&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;tag&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
        SELECT content, count(*) AS files, sum(record_count) AS records
        FROM local.db.&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;.files GROUP BY content ORDER BY content&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;label&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;data&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;position-delete&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;equality-delete&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;desc&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;  &quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;label&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;content&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]]&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;files&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;f/&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;records&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;r&quot;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;live&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) c FROM local.db.&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;][&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;c&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tag&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;28&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; &lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;desc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;50&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; query returns &lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;live&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;cow&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;copy-on-write&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;mor&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;merge-on-read&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;build&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;accounting&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; before&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;DELETE FROM local.db.&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; WHERE id &amp;lt; 100&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;accounting&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; after DELETE&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;ops&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;operation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
        &lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT operation FROM local.db.&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;.snapshots ORDER BY committed_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()]&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;    snapshot ops:&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;ops&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content&lt;/code&gt; is the column that matters&lt;/strong&gt; and the reason to use the metadata table
rather than counting files on disk. My first attempt at this counted
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*.parquet&lt;/code&gt; in the table directory and concluded merge-on-read had gained a data
file. It had gained a &lt;em&gt;delete&lt;/em&gt; file, which is also Parquet.&lt;/p&gt;

&lt;h2 id=&quot;what-the-reader-does&quot;&gt;What the reader does with a delete file&lt;/h2&gt;

&lt;p&gt;A position delete names a data file and the row positions to skip, so applying it
is a merge of two sorted streams — cheap, and proportional to the rows actually
read.&lt;/p&gt;

&lt;p&gt;An equality delete is a predicate. The reader has no idea which files contain
matching rows, so it must evaluate the predicate against every data file in scope
whose sequence number is older than the delete. That is why:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;equality deletes cost more per row read than position deletes,&lt;/li&gt;
  &lt;li&gt;they cost more the &lt;em&gt;wider&lt;/em&gt; the scope, not just the deeper the history,&lt;/li&gt;
  &lt;li&gt;and a table accumulating them degrades faster than the file count suggests.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Sequence numbers are what keep this correct. Every commit gets one, and a delete
file applies only to data files with a &lt;strong&gt;lower&lt;/strong&gt; sequence number. That is how a
row inserted after a delete is not wrongly removed by it, and it is why deletes
cannot simply be applied to everything.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;file_path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sequence_number&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mor&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sequence_number&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;tuning-each-mode&quot;&gt;Tuning each mode&lt;/h2&gt;

&lt;p&gt;The mode is the big decision; two properties refine it.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Under copy-on-write&lt;/strong&gt;, the cost is how much gets rewritten, which is driven by
file size. Large files mean a one-row delete rewrites a lot:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.delete.mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;copy-on-write&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.target-file-size-bytes&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;134217728&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;-- 128 MiB&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Smaller target files make deletes cheaper and scans slightly less efficient. That
is the actual trade under copy-on-write, and it is rarely discussed.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Under merge-on-read&lt;/strong&gt;, the cost is how long deletes accumulate, so the relevant
setting is not on the table at all — it is your compaction schedule. The one
table property worth setting is the isolation level, because a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; that
scans widely will conflict more often:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.merge.isolation-level&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;snapshot&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;-- from the default &apos;serializable&apos;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Relax that deliberately and knowingly: it accepts write skew in exchange for
fewer conflicts.&lt;/p&gt;

&lt;h2 id=&quot;choosing-per-operation&quot;&gt;Choosing per operation, in practice&lt;/h2&gt;

&lt;p&gt;Because the three modes are independent, the useful configurations are mixed
rather than uniform:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- a dimension table: corrections are rare, reads are constant&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;customers&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.delete.mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;copy-on-write&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.update.mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;copy-on-write&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.merge.mode&apos;&lt;/span&gt;  &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;copy-on-write&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- a fact table fed by CDC: writes are constant, compaction is scheduled&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orders&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.delete.mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;merge-on-read&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.update.mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;merge-on-read&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.merge.mode&apos;&lt;/span&gt;  &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;merge-on-read&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- a table with occasional GDPR deletes but nightly bulk merges&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.delete.mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;copy-on-write&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;-- rare, worth doing properly&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.merge.mode&apos;&lt;/span&gt;  &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;merge-on-read&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;-- large, avoid rewriting the table&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That third case is the one that justifies the split existing. A deletion request
touching a handful of rows should leave a clean table; a nightly merge touching
millions should not rewrite files it barely changes.&lt;/p&gt;

&lt;h2 id=&quot;modes-are-per-operation&quot;&gt;The modes are per operation&lt;/h2&gt;

&lt;p&gt;This is the part most summaries flatten. There is no single table-level mode;
there are three independent properties:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Property&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Governs&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.delete.mode&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE FROM&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.update.mode&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.merge.mode&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;A freshly created table reports none of them:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;current-snapshot-id = none
format = iceberg/parquet
format-version = 2
write.parquet.compression-codec = zstd
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Their absence means the format default applies rather than that no mode exists.
&lt;strong&gt;Set them explicitly on any table where the choice matters&lt;/strong&gt;, because a default
you did not choose is a default you will not remember when the table starts
behaving unexpectedly.&lt;/p&gt;

&lt;p&gt;The independence is useful. A dimension table that receives occasional small
corrections and is read constantly might use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;copy-on-write&lt;/code&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt;, while a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt; that lands a large nightly changelog uses
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;merge-on-read&lt;/code&gt; to avoid rewriting half the table in one statement.&lt;/p&gt;

&lt;p&gt;Note also &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;format-version = 2&lt;/code&gt; in that output: delete files are a format v2
feature, so merge-on-read requires v2 or later. On a v1 table the modes do not
apply, because there is no way to express a delete except by rewriting.&lt;/p&gt;

&lt;h2 id=&quot;which-one&quot;&gt;Which one does your workload want?&lt;/h2&gt;

&lt;p&gt;The decision follows from the ratio of writes to reads, and from what runs
between them.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Situation&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Mode&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Small, infrequent corrections; heavy reads&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;copy-on-write&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;pay once, never pay again&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;High-frequency upserts or CDC&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;merge-on-read&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;rewriting files per micro-batch does not keep up&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming ingestion with tight latency&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;merge-on-read&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;write time is the SLA&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Large nightly &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; into a wide table&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;merge-on-read&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;avoids rewriting files the merge barely touches&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Table read by engines with weak delete support&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;copy-on-write&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;no delete files to misapply&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No compaction job scheduled&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;copy-on-write&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;see below&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;strong&gt;That last row is the one that bites.&lt;/strong&gt; Merge-on-read is not a way to avoid the
cost of a delete; it is a way to defer it. Every delete file that has not been
compacted is applied by every subsequent read of the files it covers. A CDC
pipeline writing delete files every minute and compacting never will get
measurably slower every hour, and the symptom — queries degrading with no change
in data volume — looks nothing like its cause.&lt;/p&gt;

&lt;p&gt;If you choose merge-on-read, you are also choosing to run
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_position_delete_files&lt;/code&gt; on a schedule. The
mechanics of both, and the separate step needed to actually reclaim storage, are
in
&lt;a href=&quot;/TableMaintenance/&quot;&gt;table maintenance for Iceberg and Hudi&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;what-neither-mode-does&quot;&gt;What neither mode does&lt;/h2&gt;

&lt;p&gt;Neither reclaims space, and this catches people who expect copy-on-write to be
self-cleaning.&lt;/p&gt;

&lt;p&gt;Under copy-on-write the rewritten file replaces the old one &lt;em&gt;in the current
snapshot&lt;/em&gt;. The old file is still referenced by the previous snapshot, which time
travel still needs, so it stays on disk until &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;expire_snapshots&lt;/code&gt; removes the
snapshot that references it. A table that deletes rows daily under
copy-on-write and never expires snapshots grows on disk while shrinking
logically.&lt;/p&gt;

&lt;p&gt;Under merge-on-read the original data file stays by definition, and the delete
file joins it. Compaction merges them into a clean data file, and then &lt;em&gt;that&lt;/em&gt;
leaves the pre-compaction files behind until expiry.&lt;/p&gt;

&lt;p&gt;The rule that covers both: &lt;strong&gt;compaction changes what queries read; expiry
changes what storage costs.&lt;/strong&gt; They are different jobs and you need both.&lt;/p&gt;

&lt;h2 id=&quot;how-to-tell&quot;&gt;How to tell which mode a table is using&lt;/h2&gt;

&lt;p&gt;Three ways, in increasing order of certainty.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The table properties&lt;/strong&gt;, which tell you the configured intent:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SHOW&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orders&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;The snapshot log&lt;/strong&gt;, which tells you what actually happened:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;snapshot_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;operation&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orders&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; for a rewrite, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt; for a delete file. This is the one to trust,
because it reflects the mode in force at the time of each operation rather than
the mode configured now.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The file accounting&lt;/strong&gt;, which tells you what you are carrying:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;records&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orders&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;content&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;content = 1&lt;/code&gt; rows are position delete files. A growing count there, on a table
whose queries are slowing down, is the signal that compaction is not keeping up.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Merge-on-read is faster.”&lt;/strong&gt; It is faster to &lt;em&gt;write&lt;/em&gt;. Reads pay for it until
compaction runs, and the total cost can exceed copy-on-write if the data is read
far more often than written.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Copy-on-write rewrites the whole table.”&lt;/strong&gt; It rewrites the data files that
contain affected rows. Deleting one row from a well-partitioned table rewrites
one file, not the table.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“A delete file means the rows are gone.”&lt;/strong&gt; The rows are still in the data file.
They are filtered at read time, and they remain readable through time travel to
an earlier snapshot.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“The mode is a table-level setting.”&lt;/strong&gt; It is three settings, one per operation,
and they can disagree.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Copy-on-write frees the space immediately.”&lt;/strong&gt; The replaced file lives on until
the snapshot referencing it is expired.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;Copy-on-write pays the delete once, at write time, and leaves a clean table.
Merge-on-read pays nothing now and a little on every read, until a compaction job
settles the account.&lt;/p&gt;

&lt;p&gt;So the question is not which is faster. It is &lt;strong&gt;who should pay, and have you
scheduled the job that pays off the debt.&lt;/strong&gt; Choose merge-on-read only with
compaction already scheduled; otherwise copy-on-write is the honest default.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/configuration/#write-properties&quot;&gt;Iceberg table properties&lt;/a&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.delete.mode&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.update.mode&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.merge.mode&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/spec/#delete-formats&quot;&gt;Iceberg spec: delete files&lt;/a&gt; for position and equality delete semantics&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-procedures/&quot;&gt;Iceberg maintenance procedures&lt;/a&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_position_delete_files&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheIcebergArchitecture/&quot;&gt;Apache Iceberg architecture&lt;/a&gt; for the metadata tree these files live in&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/TableMaintenance/&quot;&gt;Table maintenance for Iceberg and Hudi&lt;/a&gt; for the compaction and expiry jobs this post assumes&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Spark, Apache Flink, Apache Parquet, Apache and the Apache
feather logo are either registered trademarks or trademarks of The Apache
Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Iceberg" /><category term="Iceberg" /><category term="Deletes" /><category term="Compaction" /><category term="Streaming" /><summary type="html">The question this setting answers The same delete, measured twice What a delete file actually is Reproducing the measurement What the reader does with a delete file Tuning each mode Choosing per operation, in practice The modes are per operation Which one does your workload want? What neither mode does How to tell which mode a table is using Common misconceptions A model worth keeping References Trademarks TL;DR The same DELETE on two tables differing only in write.delete.mode left 2 data files holding 3,900 records under copy-on-write, and 2 data files still holding 4,000 records plus a 100-record delete file under merge-on-read. Both queries return 3,900 rows. The snapshot’s operation names the choice: overwrite for copy-on-write, delete for merge-on-read. The setting is per operation, not per table: write.delete.mode, write.update.mode and write.merge.mode are independent, so a table can rewrite on DELETE and defer on MERGE. Merge-on-read moves cost from write time to every read until compaction runs. A CDC table that never compacts gets slower every hour. Neither mode reclaims space on its own. The files the old snapshot still references stay until expire_snapshots. The question this setting answers Deleting one row from a table stored as immutable Parquet files is not obvious. The file holding that row cannot be edited in place, so something has to give. Iceberg offers two answers, and you choose per table. Copy-on-write rewrites the whole data file without the deleted row, and the new snapshot points at the replacement. The cost is paid now, once, by the writer. Merge-on-read leaves the data file alone and writes a small delete file recording which rows no longer count. The cost is paid later, repeatedly, by every reader that has to apply it. That is the entire trade, and it is worth seeing in the files rather than taking on faith. The same delete, measured twice Two tables, identical except for their mode, each loaded with 4,000 rows in 2 data files: CREATE TABLE local.db.cow (id BIGINT, v STRING) USING iceberg TBLPROPERTIES (&apos;write.delete.mode&apos;=&apos;copy-on-write&apos;); CREATE TABLE local.db.mor (id BIGINT, v STRING) USING iceberg TBLPROPERTIES (&apos;write.delete.mode&apos;=&apos;merge-on-read&apos;); Then the identical statement against each: DELETE FROM local.db.&amp;lt;table&amp;gt; WHERE id &amp;lt; 100; Reading the accounting from the files metadata table, where content is 0 for data and 1 for position deletes:   data files records in them delete files query returns copy-on-write before 2 4,000 0 4,000 copy-on-write after 2 3,900 0 3,900 merge-on-read before 2 4,000 0 4,000 merge-on-read after 2 4,000 1 (100 records) 3,900 That table is the whole post. Under copy-on-write the data files were rewritten: still two files, now containing 3,900 records, the 100 deleted rows physically gone from the current snapshot. Under merge-on-read the data files were never touched — they still hold all 4,000 records — and a separate file records the 100 positions to skip. Both queries return 3,900 rows, because the reader merges the delete file on the way past. The snapshot log names what happened, without you having to count files: copy-on-write snapshot ops: [&apos;append&apos;, &apos;overwrite&apos;] merge-on-read snapshot ops: [&apos;append&apos;, &apos;delete&apos;] overwrite means files were replaced. delete means a delete file was added. If you ever need to know which mode a past operation used, that column is the record. What a delete file actually is The merge-on-read run produced one extra file next to the data: 00000-45-0ec67ff2-63f5-4f54-83a2-58f5ac1bdb24-00001-deletes.parquet It is a Parquet file like any other, which is a detail worth internalising: counting *.parquet in a table directory does not tell you how many data files there are. My first attempt at this measurement did exactly that and reported merge-on-read as having gained a data file. It had not; it had gained a delete file. The files metadata table and its content column are the honest source. There are two kinds, and the difference matters operationally: Kind What it records Written by Position delete this file, these row positions Spark, and most engines Equality delete rows where these columns equal these values streaming writers, notably Flink A position delete is cheap to apply, because the reader knows exactly which rows in which file to skip. An equality delete is a predicate, so the reader must evaluate it against rows it reads, and it applies to every data file in scope rather than one named file. Equality deletes let a streaming writer delete without first finding out where the row lives, which is why Flink CDC pipelines produce them, and they are the more expensive of the two to read. flowchart LR subgraph COW[&quot;copy-on-write&quot;] D1[&quot;data file&amp;lt;br/&amp;gt;4000 rows&quot;] --&amp;gt;|&quot;DELETE rewrites it&quot;| D2[&quot;new data file&amp;lt;br/&amp;gt;3900 rows&quot;] end subgraph MOR[&quot;merge-on-read&quot;] E1[&quot;data file&amp;lt;br/&amp;gt;4000 rows, untouched&quot;] --&amp;gt; R[&quot;reader merges&quot;] E2[&quot;delete file&amp;lt;br/&amp;gt;100 positions&quot;] --&amp;gt; R R --&amp;gt; O[&quot;3900 rows&quot;] end Reproducing the measurement The whole comparison is twenty lines, and running it against your own data is more convincing than any table here. from pyspark.sql import functions as F def build(name, mode): spark.sql(f&quot;DROP TABLE IF EXISTS local.db.{name} PURGE&quot;) spark.sql(f&quot;&quot;&quot; CREATE TABLE local.db.{name} (id BIGINT, v STRING) USING iceberg TBLPROPERTIES ( &apos;write.delete.mode&apos; = &apos;{mode}&apos;, &apos;write.update.mode&apos; = &apos;{mode}&apos;, &apos;write.merge.mode&apos; = &apos;{mode}&apos;)&quot;&quot;&quot;) (spark.range(0, 4000) .withColumn(&quot;v&quot;, F.concat(F.lit(&quot;v&quot;), F.col(&quot;id&quot;).cast(&quot;string&quot;))) .selectExpr(&quot;id&quot;, &quot;v&quot;) .writeTo(f&quot;local.db.{name}&quot;).append()) def accounting(name, tag): rows = spark.sql(f&quot;&quot;&quot; SELECT content, count(*) AS files, sum(record_count) AS records FROM local.db.{name}.files GROUP BY content ORDER BY content&quot;&quot;&quot;).collect() label = {0: &quot;data&quot;, 1: &quot;position-delete&quot;, 2: &quot;equality-delete&quot;} desc = &quot; &quot;.join(f&quot;{label[r[&apos;content&apos;]]}={r[&apos;files&apos;]}f/{r[&apos;records&apos;]}r&quot; for r in rows) live = spark.sql(f&quot;SELECT count(*) c FROM local.db.{name}&quot;).collect()[0][&quot;c&quot;] print(f&quot;{tag:&amp;lt;28} {desc:&amp;lt;50} query returns {live}&quot;) for name, mode in ((&quot;cow&quot;, &quot;copy-on-write&quot;), (&quot;mor&quot;, &quot;merge-on-read&quot;)): build(name, mode) accounting(name, f&quot;{mode} before&quot;) spark.sql(f&quot;DELETE FROM local.db.{name} WHERE id &amp;lt; 100&quot;) accounting(name, f&quot;{mode} after DELETE&quot;) ops = [r[&quot;operation&quot;] for r in spark.sql( f&quot;SELECT operation FROM local.db.{name}.snapshots ORDER BY committed_at&quot;).collect()] print(&quot; snapshot ops:&quot;, ops) content is the column that matters and the reason to use the metadata table rather than counting files on disk. My first attempt at this counted *.parquet in the table directory and concluded merge-on-read had gained a data file. It had gained a delete file, which is also Parquet. What the reader does with a delete file A position delete names a data file and the row positions to skip, so applying it is a merge of two sorted streams — cheap, and proportional to the rows actually read. An equality delete is a predicate. The reader has no idea which files contain matching rows, so it must evaluate the predicate against every data file in scope whose sequence number is older than the delete. That is why: equality deletes cost more per row read than position deletes, they cost more the wider the scope, not just the deeper the history, and a table accumulating them degrades faster than the file count suggests. Sequence numbers are what keep this correct. Every commit gets one, and a delete file applies only to data files with a lower sequence number. That is how a row inserted after a delete is not wrongly removed by it, and it is why deletes cannot simply be applied to everything. SELECT file_path, content, sequence_number, record_count FROM local.db.mor.files ORDER BY sequence_number; Tuning each mode The mode is the big decision; two properties refine it. Under copy-on-write, the cost is how much gets rewritten, which is driven by file size. Large files mean a one-row delete rewrites a lot: ALTER TABLE local.db.events SET TBLPROPERTIES ( &apos;write.delete.mode&apos; = &apos;copy-on-write&apos;, &apos;write.target-file-size-bytes&apos; = &apos;134217728&apos;); -- 128 MiB Smaller target files make deletes cheaper and scans slightly less efficient. That is the actual trade under copy-on-write, and it is rarely discussed. Under merge-on-read, the cost is how long deletes accumulate, so the relevant setting is not on the table at all — it is your compaction schedule. The one table property worth setting is the isolation level, because a MERGE that scans widely will conflict more often: ALTER TABLE local.db.events SET TBLPROPERTIES ( &apos;write.merge.isolation-level&apos; = &apos;snapshot&apos;); -- from the default &apos;serializable&apos; Relax that deliberately and knowingly: it accepts write skew in exchange for fewer conflicts. Choosing per operation, in practice Because the three modes are independent, the useful configurations are mixed rather than uniform: -- a dimension table: corrections are rare, reads are constant ALTER TABLE local.db.customers SET TBLPROPERTIES ( &apos;write.delete.mode&apos; = &apos;copy-on-write&apos;, &apos;write.update.mode&apos; = &apos;copy-on-write&apos;, &apos;write.merge.mode&apos; = &apos;copy-on-write&apos;); -- a fact table fed by CDC: writes are constant, compaction is scheduled ALTER TABLE local.db.orders SET TBLPROPERTIES ( &apos;write.delete.mode&apos; = &apos;merge-on-read&apos;, &apos;write.update.mode&apos; = &apos;merge-on-read&apos;, &apos;write.merge.mode&apos; = &apos;merge-on-read&apos;); -- a table with occasional GDPR deletes but nightly bulk merges ALTER TABLE local.db.events SET TBLPROPERTIES ( &apos;write.delete.mode&apos; = &apos;copy-on-write&apos;, -- rare, worth doing properly &apos;write.merge.mode&apos; = &apos;merge-on-read&apos;); -- large, avoid rewriting the table That third case is the one that justifies the split existing. A deletion request touching a handful of rows should leave a clean table; a nightly merge touching millions should not rewrite files it barely changes. The modes are per operation This is the part most summaries flatten. There is no single table-level mode; there are three independent properties: Property Governs write.delete.mode DELETE FROM write.update.mode UPDATE write.merge.mode MERGE INTO A freshly created table reports none of them: current-snapshot-id = none format = iceberg/parquet format-version = 2 write.parquet.compression-codec = zstd Their absence means the format default applies rather than that no mode exists. Set them explicitly on any table where the choice matters, because a default you did not choose is a default you will not remember when the table starts behaving unexpectedly. The independence is useful. A dimension table that receives occasional small corrections and is read constantly might use copy-on-write for DELETE and UPDATE, while a MERGE INTO that lands a large nightly changelog uses merge-on-read to avoid rewriting half the table in one statement. Note also format-version = 2 in that output: delete files are a format v2 feature, so merge-on-read requires v2 or later. On a v1 table the modes do not apply, because there is no way to express a delete except by rewriting. Which one does your workload want? The decision follows from the ratio of writes to reads, and from what runs between them. Situation Mode Why Small, infrequent corrections; heavy reads copy-on-write pay once, never pay again High-frequency upserts or CDC merge-on-read rewriting files per micro-batch does not keep up Streaming ingestion with tight latency merge-on-read write time is the SLA Large nightly MERGE into a wide table merge-on-read avoids rewriting files the merge barely touches Table read by engines with weak delete support copy-on-write no delete files to misapply No compaction job scheduled copy-on-write see below That last row is the one that bites. Merge-on-read is not a way to avoid the cost of a delete; it is a way to defer it. Every delete file that has not been compacted is applied by every subsequent read of the files it covers. A CDC pipeline writing delete files every minute and compacting never will get measurably slower every hour, and the symptom — queries degrading with no change in data volume — looks nothing like its cause. If you choose merge-on-read, you are also choosing to run rewrite_data_files and rewrite_position_delete_files on a schedule. The mechanics of both, and the separate step needed to actually reclaim storage, are in table maintenance for Iceberg and Hudi. What neither mode does Neither reclaims space, and this catches people who expect copy-on-write to be self-cleaning. Under copy-on-write the rewritten file replaces the old one in the current snapshot. The old file is still referenced by the previous snapshot, which time travel still needs, so it stays on disk until expire_snapshots removes the snapshot that references it. A table that deletes rows daily under copy-on-write and never expires snapshots grows on disk while shrinking logically. Under merge-on-read the original data file stays by definition, and the delete file joins it. Compaction merges them into a clean data file, and then that leaves the pre-compaction files behind until expiry. The rule that covers both: compaction changes what queries read; expiry changes what storage costs. They are different jobs and you need both. How to tell which mode a table is using Three ways, in increasing order of certainty. The table properties, which tell you the configured intent: SHOW TBLPROPERTIES local.db.orders; The snapshot log, which tells you what actually happened: SELECT snapshot_id, operation FROM local.db.orders.snapshots ORDER BY committed_at; overwrite for a rewrite, delete for a delete file. This is the one to trust, because it reflects the mode in force at the time of each operation rather than the mode configured now. The file accounting, which tells you what you are carrying: SELECT content, count(*) files, sum(record_count) records FROM local.db.orders.files GROUP BY content; content = 1 rows are position delete files. A growing count there, on a table whose queries are slowing down, is the signal that compaction is not keeping up. Common misconceptions “Merge-on-read is faster.” It is faster to write. Reads pay for it until compaction runs, and the total cost can exceed copy-on-write if the data is read far more often than written. “Copy-on-write rewrites the whole table.” It rewrites the data files that contain affected rows. Deleting one row from a well-partitioned table rewrites one file, not the table. “A delete file means the rows are gone.” The rows are still in the data file. They are filtered at read time, and they remain readable through time travel to an earlier snapshot. “The mode is a table-level setting.” It is three settings, one per operation, and they can disagree. “Copy-on-write frees the space immediately.” The replaced file lives on until the snapshot referencing it is expired. A model worth keeping Copy-on-write pays the delete once, at write time, and leaves a clean table. Merge-on-read pays nothing now and a little on every read, until a compaction job settles the account. So the question is not which is faster. It is who should pay, and have you scheduled the job that pays off the debt. Choose merge-on-read only with compaction already scheduled; otherwise copy-on-write is the honest default. References Iceberg table properties for write.delete.mode, write.update.mode and write.merge.mode Iceberg spec: delete files for position and equality delete semantics Iceberg maintenance procedures for rewrite_data_files and rewrite_position_delete_files Apache Iceberg architecture for the metadata tree these files live in Table maintenance for Iceberg and Hudi for the compaction and expiry jobs this post assumes Trademarks Apache Iceberg, Apache Spark, Apache Flink, Apache Parquet, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Streaming into Iceberg: availableNow, checkpoints, and the snapshot count nobody watches</title><link href="https://rangareddy.github.io/IcebergStreamingIngestion/" rel="alternate" type="text/html" title="Streaming into Iceberg: availableNow, checkpoints, and the snapshot count nobody watches" /><published>2026-09-03T10:00:00+05:30</published><updated>2026-09-03T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergStreamingIngestion</id><content type="html" xml:base="https://rangareddy.github.io/IcebergStreamingIngestion/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#what-a-streaming-write-commits&quot; id=&quot;markdown-toc-what-a-streaming-write-commits&quot;&gt;What a streaming write actually commits&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-run-measured&quot; id=&quot;markdown-toc-the-run-measured&quot;&gt;The run, measured&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#why-rerunning-is-safe&quot; id=&quot;markdown-toc-why-rerunning-is-safe&quot;&gt;Why re-running is safe&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing-the-trigger&quot; id=&quot;markdown-toc-choosing-the-trigger&quot;&gt;Choosing the trigger&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-cost-in-week-three&quot; id=&quot;markdown-toc-the-cost-in-week-three&quot;&gt;The cost that shows up in week three&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#configuring-the-stream&quot; id=&quot;markdown-toc-configuring-the-stream&quot;&gt;Configuring the stream&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#watching-a-running-stream&quot; id=&quot;markdown-toc-watching-a-running-stream&quot;&gt;Watching a running stream&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#recovering-a-stream&quot; id=&quot;markdown-toc-recovering-a-stream&quot;&gt;Recovering a stream&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#maintenance-is-part-of-it&quot; id=&quot;markdown-toc-maintenance-is-part-of-it&quot;&gt;Maintenance is part of the pipeline&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-trap&quot; id=&quot;markdown-toc-the-trap&quot;&gt;The trap that produces a healthy-looking no-op&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#row-level-alongside-a-stream&quot; id=&quot;markdown-toc-row-level-alongside-a-stream&quot;&gt;Row-level operations alongside a stream&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;A streaming write commits &lt;strong&gt;one Iceberg snapshot per micro-batch&lt;/strong&gt;, not per file and not per row. Three input files consumed in one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt; run produced &lt;strong&gt;1 snapshot&lt;/strong&gt;; a fourth file arriving later produced a second.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Trigger.AvailableNow&lt;/code&gt; drains everything currently available, then stops. It is the batch-shaped way to run a streaming job, and it is what you want in an orchestrator.&lt;/li&gt;
    &lt;li&gt;Re-running the same query over the same input added &lt;strong&gt;0 rows&lt;/strong&gt;. The checkpoint, not the table, is what makes that safe.&lt;/li&gt;
    &lt;li&gt;The trigger interval is therefore a metadata-growth decision. A one-minute trigger is 1,440 snapshots a day, each with its own manifest list.&lt;/li&gt;
    &lt;li&gt;The streaming file source &lt;strong&gt;does not recurse into subdirectories&lt;/strong&gt;. Files staged one directory deep are silently never read, and the query reports healthy.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;what-a-streaming-write-commits&quot;&gt;What a streaming write actually commits&lt;/h2&gt;

&lt;p&gt;Spark’s Structured Streaming writes to Iceberg through a normal sink, and the
unit of work is the micro-batch. Each batch that produces data ends in an Iceberg
commit, which means a new snapshot, a new manifest list, and a new metadata file.&lt;/p&gt;

&lt;p&gt;That single fact explains most of what goes right and wrong with streaming
Iceberg tables. It is not “streaming” in the sense of rows trickling into a file.
It is a sequence of small, atomic batch commits, and everything about the table’s
long-term health follows from how many of them you make.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
    F[new files arrive] --&amp;gt; MB[micro-batch]
    MB --&amp;gt; W[write data files]
    W --&amp;gt; C[&quot;commit: snapshot + manifest list + metadata.json&quot;]
    C --&amp;gt; CK[checkpoint records progress]
    CK --&amp;gt; MB
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;the-run-measured&quot;&gt;The run, measured&lt;/h2&gt;

&lt;p&gt;Three Parquet files staged in a directory, a table created ahead of time, and a
query with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;schema&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;StructType&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;StructField&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;LongType&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;StructField&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;v&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;StringType&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())])&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;readStream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;schema&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;schema&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;src&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeStream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;outputMode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trigger&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;availableNow&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;True&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;checkpointLocation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;/work/ckpt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toTable&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.stream&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;awaitTermination&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;rows after first availableNow run = 3000
snapshots                         = 1
snapshot ops                      = [&apos;append&apos;]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Three files, 3,000 rows, one snapshot.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt; did not create a
snapshot per file; it consumed everything available and committed once. That is
the behaviour you want for a scheduled job, and the opposite of what a short
processing-time trigger gives you.&lt;/p&gt;

&lt;p&gt;Running the identical query a second time, with no new input:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;rows after re-run, no new files   = 3000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Nothing was added and nothing was duplicated. Then a fourth file appeared:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;rows after a 4th file arrives     = 4000
snapshots now                     = 2
data files in table               = 3
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A second snapshot, for a second run that found work. The snapshot count tracks
&lt;strong&gt;runs that found data&lt;/strong&gt;, not files and not rows.&lt;/p&gt;

&lt;h2 id=&quot;why-rerunning-is-safe&quot;&gt;Why re-running is safe&lt;/h2&gt;

&lt;p&gt;The checkpoint is doing that work, and it is worth knowing what it holds:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;checkpoint dirs = [&apos;commits&apos;, &apos;metadata&apos;, &apos;offsets&apos;, &apos;sources&apos;]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;offsets&lt;/code&gt; records what each batch was told to read, before it ran. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;commits&lt;/code&gt;
records which batches finished. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sources&lt;/code&gt; tracks which files the file source has
already seen. On restart, the engine compares the two logs, re-runs anything
that started and did not finish, and skips everything already committed.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The table is not what makes this idempotent.&lt;/strong&gt; Point the same query at a new
checkpoint directory and it will happily re-ingest every file and double your
rows. The checkpoint is the record of progress; the table is just where the
output went.&lt;/p&gt;

&lt;p&gt;Two consequences follow. &lt;strong&gt;A checkpoint belongs to one query against one table&lt;/strong&gt; —
reusing it across two different sinks corrupts both notions of progress. And
&lt;strong&gt;deleting a checkpoint to “reset” a job is a data-duplication event&lt;/strong&gt;, not a
clean slate, unless you also truncate the target.&lt;/p&gt;

&lt;p&gt;The full anatomy of that directory, including why the offset log pins
configuration, is in
&lt;a href=&quot;/StructuredStreamingInternals/&quot;&gt;Structured Streaming internals&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;choosing-the-trigger&quot;&gt;Choosing the trigger&lt;/h2&gt;

&lt;p&gt;This is the decision that determines whether the table stays healthy.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Trigger&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Behaviour&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Metadata cost&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;drain everything available, then stop&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;one snapshot per run&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;processingTime=&apos;5 minutes&apos;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;a batch every 5 minutes, forever&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;288 snapshots/day&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;processingTime=&apos;1 minute&apos;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;a batch every minute&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1,440 snapshots/day&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;default (no trigger)&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;batches as fast as they complete&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;unbounded&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt; is the right default for most pipelines.&lt;/strong&gt; It turns a streaming
query into something an orchestrator can schedule: it starts, it catches up, it
exits with a status. You get the incremental-processing semantics of streaming
with the operational shape of a batch job, and you commit once per run rather
than once per micro-batch.&lt;/p&gt;

&lt;p&gt;Reach for a continuous processing-time trigger when latency genuinely matters,
and then accept that you have signed up for the maintenance in the next section.&lt;/p&gt;

&lt;h2 id=&quot;the-cost-in-week-three&quot;&gt;The cost that shows up in week three&lt;/h2&gt;

&lt;p&gt;Every commit writes a metadata file, a manifest list, and at least one manifest.
At one snapshot a minute, a table accumulates 1,440 of each per day. Nothing
fails. Queries just get slower, because planning a query means reading the
current metadata file, its manifest list, and the manifests it points at.&lt;/p&gt;

&lt;p&gt;The symptom is distinctive: &lt;strong&gt;query planning time grows while the data volume is
flat&lt;/strong&gt;, and the slowdown affects every query against the table equally, including
trivial ones.&lt;/p&gt;

&lt;p&gt;Two jobs keep it in check, and a streaming table needs both scheduled from the
day it is created:&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Compaction&lt;/strong&gt; merges the many small files each micro-batch produced into fewer
large ones. Without it, a table ingesting every minute accumulates a file per
partition per minute.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Snapshot expiry&lt;/strong&gt; removes old snapshots and the files only they reference. This
is the one that controls storage, and compaction without expiry grows the bill
while shrinking the file count. The mechanics of both are in
&lt;a href=&quot;/TableMaintenance/&quot;&gt;table maintenance for Iceberg and Hudi&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;There is a third knob worth knowing: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.metadata.previous-versions-max&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.metadata.delete-after-commit.enabled&lt;/code&gt; bound how many old metadata JSON
files are kept, which is separate from snapshot expiry and is the specific fix
for a metadata directory with tens of thousands of files in it.&lt;/p&gt;

&lt;h2 id=&quot;configuring-the-stream&quot;&gt;Configuring the stream&lt;/h2&gt;

&lt;p&gt;Three options change the shape of what a streaming write produces, and the
defaults are rarely what a production table wants.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stream&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeStream&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;outputMode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trigger&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;availableNow&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;True&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;checkpointLocation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;/work/ckpt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fanout-enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;true&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toTable&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fanout-enabled&lt;/code&gt;&lt;/strong&gt; is the one that bites. Without it, the writer requires input
sorted by partition, and unsorted micro-batch data raises rather than writing.
With it, the writer keeps a file open per partition — correct for unsorted
streaming input, at the cost of more concurrent writers and more small files. For
a stream landing into many partitions per batch, enable it and compact more
often; for a stream landing into one or two, sort upstream and leave it off.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxFilesPerTrigger&lt;/code&gt;&lt;/strong&gt; on a file source bounds how much one batch consumes,
which is what stops a backlog from being processed as one enormous batch:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;stream&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;readStream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;schema&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;schema&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;maxFilesPerTrigger&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;200&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;src&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;With &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt;, this turns a large backlog into several sequential
micro-batches rather than one, so memory stays bounded and each batch commits
separately.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The trigger&lt;/strong&gt; determines the commit rate, which is the metadata growth rate.
Worth restating as arithmetic: a one-minute trigger is 1,440 commits a day, each
writing three metadata files, so &lt;strong&gt;4,320 metadata files a day&lt;/strong&gt; on a table that
may only receive a few thousand rows.&lt;/p&gt;

&lt;h2 id=&quot;watching-a-running-stream&quot;&gt;Watching a running stream&lt;/h2&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;StreamingQueryProgress&lt;/code&gt; answers whether the stream is keeping up, and the
Iceberg side answers what it is doing to the table.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;p&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;q&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lastProgress&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;batch&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;batchId&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rows&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;numInputRows&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;batch duration ms&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;batchDuration&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sources&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;description&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;][:&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;40&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;endOffset&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sources&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]])&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- how fast is the table accumulating commits?&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;date_trunc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;hour&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;hour&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;commits&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;summary&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;added-data-files&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DESC&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;LIMIT&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;12&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That second query is the one to put on a dashboard. Commits per hour is the
number that predicts every metadata problem this table will have, and it is
visible long before anything gets slow.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Batch duration approaching the trigger interval&lt;/strong&gt; is the other early warning:
once processing takes longer than the trigger, batches queue and latency grows
without bound.&lt;/p&gt;

&lt;h2 id=&quot;recovering-a-stream&quot;&gt;Recovering a stream&lt;/h2&gt;

&lt;p&gt;Three situations, three different answers.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The query failed and the data is fine.&lt;/strong&gt; Restart it with the same checkpoint.
Offsets and commits reconcile, an incomplete batch re-runs, and nothing
duplicates.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The output is wrong and must be replayed.&lt;/strong&gt; Do &lt;em&gt;not&lt;/em&gt; simply delete the
checkpoint — that re-ingests everything into a table that still holds the bad
rows. Roll the table back first, then reset the stream to a matching position:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rollback_to_snapshot&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;9044140986145286505&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;The schema changed upstream.&lt;/strong&gt; A new column in the source is additive: add it
to the table, and restart. A type change is not, and needs the add-migrate-drop
pattern rather than a stream restart.&lt;/p&gt;

&lt;p&gt;The checkpoint’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;offsets&lt;/code&gt; directory records what each batch was told to read,
and it is readable when you need to reason about where a stream is:&lt;/p&gt;

&lt;div class=&quot;language-sh highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nb&quot;&gt;cat&lt;/span&gt; /work/ckpt/offsets/&lt;span class=&quot;si&quot;&gt;$(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;ls&lt;/span&gt; /work/ckpt/offsets | &lt;span class=&quot;nb&quot;&gt;sort&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;-n&lt;/span&gt; | &lt;span class=&quot;nb&quot;&gt;tail&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;-1&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;maintenance-is-part-of-it&quot;&gt;Maintenance is part of the pipeline&lt;/h2&gt;

&lt;p&gt;A streaming table without scheduled maintenance is a table with a deadline. Both
jobs, from the day it is created:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- compact what the micro-batches produced, scoped to recent partitions&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_data_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt;   &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;where&lt;/span&gt;   &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;event_ts &amp;gt;= current_date() - INTERVAL 1 DAYS&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;options&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;min-input-files&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;10&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;));&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- and bound the metadata, which snapshot expiry alone does not touch&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.metadata.delete-after-commit.enabled&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;true&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.metadata.previous-versions-max&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;200&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The second statement is the one people miss. Expiring snapshots removes snapshots
and their exclusive data files; the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;metadata.json&lt;/code&gt; files have a separate
retention, and a per-minute stream will otherwise leave hundreds of thousands of
them.&lt;/p&gt;

&lt;h2 id=&quot;the-trap&quot;&gt;The trap that produces a healthy-looking no-op&lt;/h2&gt;

&lt;p&gt;Staging the input files one directory deep produced this:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;rows after availableNow run = 0
snapshots created           = 0
checkpoint dirs             = [&apos;commits&apos;, &apos;metadata&apos;, &apos;offsets&apos;, &apos;sources&apos;]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The query started, created its checkpoint, ran, and terminated normally. It read
nothing, because &lt;strong&gt;the streaming file source lists files in the given path and
does not recurse into subdirectories.&lt;/strong&gt; The files were in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;src/batch0/part-*.parquet&lt;/code&gt;
and the source was watching &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;src/&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;Nothing in that output says “I found no input”. The checkpoint exists, the query
exited zero, and only the row count reveals the problem. This was my own mistake
while writing this post, and it is worth naming because a silent zero-row
ingestion is exactly the failure that survives into production unnoticed.&lt;/p&gt;

&lt;p&gt;Two defences. Stage input &lt;strong&gt;flat&lt;/strong&gt; in the watched directory, which is what the
measured run above does. And assert on the row count or the snapshot count after
the run, rather than on the exit status.&lt;/p&gt;

&lt;h2 id=&quot;row-level-alongside-a-stream&quot;&gt;Row-level operations alongside a stream&lt;/h2&gt;

&lt;p&gt;A streaming append is the simple case. Streaming upserts go through
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreachBatch&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt;, because the sink itself only appends:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;upsert&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;batch_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;batch_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;batch_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createOrReplaceTempView&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;changes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;batch_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
        MERGE INTO local.db.target t USING changes s ON t.id = s.id
        WHEN MATCHED THEN UPDATE SET *
        WHEN NOT MATCHED THEN INSERT *&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;query&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeStream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;foreachBatch&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;upsert&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trigger&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;availableNow&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;True&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;checkpointLocation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;/work/ckpt_merge&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;start&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Note that &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreachBatch&lt;/code&gt; gives at-least-once by default: the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;batch_id&lt;/code&gt; can
run twice after a failure. A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; keyed on a primary key is naturally
idempotent, which is why this pattern works — an append inside &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreachBatch&lt;/code&gt;
would not be.&lt;/p&gt;

&lt;p&gt;The delete-file accumulation that this pattern causes on a merge-on-read table is
its own problem, covered in
&lt;a href=&quot;/IcebergCopyOnWriteMergeOnRead/&quot;&gt;copy-on-write or merge-on-read&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Streaming writes row by row.”&lt;/strong&gt; It writes micro-batches, each an atomic
Iceberg commit.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“One snapshot per file.”&lt;/strong&gt; One snapshot per micro-batch that produced data.
Three files in one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt; run gave one snapshot.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Re-running will duplicate data.”&lt;/strong&gt; Not with the same checkpoint. With a &lt;em&gt;new&lt;/em&gt;
checkpoint, it certainly will.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt; is not really streaming.”&lt;/strong&gt; It uses the streaming engine,
offsets and checkpoint, and processes only what is new. It just stops when it
catches up.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Small files are the only streaming problem.”&lt;/strong&gt; Metadata files accumulate on the
same schedule and are bounded by different settings.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;A streaming write into Iceberg is a loop of small batch commits. The trigger sets
how often you commit; the checkpoint makes re-running safe; the snapshot count is
the bill.&lt;/p&gt;

&lt;p&gt;So choose &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt; unless latency requires otherwise, keep the checkpoint
as carefully as you keep the table, and schedule compaction and expiry on the
same day you create the table rather than the week queries get slow.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-structured-streaming/&quot;&gt;Iceberg Spark structured streaming&lt;/a&gt; for the sink options and write behaviour&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/structured-streaming-programming-guide.html&quot;&gt;Structured Streaming programming guide&lt;/a&gt; for triggers and checkpoint semantics&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/configuration/&quot;&gt;Iceberg table properties&lt;/a&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.metadata.previous-versions-max&lt;/code&gt; and the metadata retention settings&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/StructuredStreamingInternals/&quot;&gt;Structured Streaming internals&lt;/a&gt; for what the checkpoint directory holds&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/TableMaintenance/&quot;&gt;Table maintenance for Iceberg and Hudi&lt;/a&gt; for the compaction and expiry this post assumes&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Spark, Apache Parquet, Apache and the Apache feather logo
are either registered trademarks or trademarks of The Apache Software Foundation
in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Iceberg" /><category term="Iceberg" /><category term="Streaming" /><category term="Checkpoints" /><category term="Snapshots" /><summary type="html">What a streaming write actually commits The run, measured Why re-running is safe Choosing the trigger The cost that shows up in week three Configuring the stream Watching a running stream Recovering a stream Maintenance is part of the pipeline The trap that produces a healthy-looking no-op Row-level operations alongside a stream Common misconceptions A model worth keeping References Trademarks TL;DR A streaming write commits one Iceberg snapshot per micro-batch, not per file and not per row. Three input files consumed in one availableNow run produced 1 snapshot; a fourth file arriving later produced a second. Trigger.AvailableNow drains everything currently available, then stops. It is the batch-shaped way to run a streaming job, and it is what you want in an orchestrator. Re-running the same query over the same input added 0 rows. The checkpoint, not the table, is what makes that safe. The trigger interval is therefore a metadata-growth decision. A one-minute trigger is 1,440 snapshots a day, each with its own manifest list. The streaming file source does not recurse into subdirectories. Files staged one directory deep are silently never read, and the query reports healthy. What a streaming write actually commits Spark’s Structured Streaming writes to Iceberg through a normal sink, and the unit of work is the micro-batch. Each batch that produces data ends in an Iceberg commit, which means a new snapshot, a new manifest list, and a new metadata file. That single fact explains most of what goes right and wrong with streaming Iceberg tables. It is not “streaming” in the sense of rows trickling into a file. It is a sequence of small, atomic batch commits, and everything about the table’s long-term health follows from how many of them you make. flowchart LR F[new files arrive] --&amp;gt; MB[micro-batch] MB --&amp;gt; W[write data files] W --&amp;gt; C[&quot;commit: snapshot + manifest list + metadata.json&quot;] C --&amp;gt; CK[checkpoint records progress] CK --&amp;gt; MB The run, measured Three Parquet files staged in a directory, a table created ahead of time, and a query with availableNow: schema = StructType([StructField(&quot;id&quot;, LongType()), StructField(&quot;v&quot;, StringType())]) q = (spark.readStream.schema(schema).parquet(src) .writeStream.format(&quot;iceberg&quot;).outputMode(&quot;append&quot;) .trigger(availableNow=True) .option(&quot;checkpointLocation&quot;, &quot;/work/ckpt&quot;) .toTable(&quot;local.db.stream&quot;)) q.awaitTermination() rows after first availableNow run = 3000 snapshots = 1 snapshot ops = [&apos;append&apos;] Three files, 3,000 rows, one snapshot. availableNow did not create a snapshot per file; it consumed everything available and committed once. That is the behaviour you want for a scheduled job, and the opposite of what a short processing-time trigger gives you. Running the identical query a second time, with no new input: rows after re-run, no new files = 3000 Nothing was added and nothing was duplicated. Then a fourth file appeared: rows after a 4th file arrives = 4000 snapshots now = 2 data files in table = 3 A second snapshot, for a second run that found work. The snapshot count tracks runs that found data, not files and not rows. Why re-running is safe The checkpoint is doing that work, and it is worth knowing what it holds: checkpoint dirs = [&apos;commits&apos;, &apos;metadata&apos;, &apos;offsets&apos;, &apos;sources&apos;] offsets records what each batch was told to read, before it ran. commits records which batches finished. sources tracks which files the file source has already seen. On restart, the engine compares the two logs, re-runs anything that started and did not finish, and skips everything already committed. The table is not what makes this idempotent. Point the same query at a new checkpoint directory and it will happily re-ingest every file and double your rows. The checkpoint is the record of progress; the table is just where the output went. Two consequences follow. A checkpoint belongs to one query against one table — reusing it across two different sinks corrupts both notions of progress. And deleting a checkpoint to “reset” a job is a data-duplication event, not a clean slate, unless you also truncate the target. The full anatomy of that directory, including why the offset log pins configuration, is in Structured Streaming internals. Choosing the trigger This is the decision that determines whether the table stays healthy. Trigger Behaviour Metadata cost availableNow drain everything available, then stop one snapshot per run processingTime=&apos;5 minutes&apos; a batch every 5 minutes, forever 288 snapshots/day processingTime=&apos;1 minute&apos; a batch every minute 1,440 snapshots/day default (no trigger) batches as fast as they complete unbounded availableNow is the right default for most pipelines. It turns a streaming query into something an orchestrator can schedule: it starts, it catches up, it exits with a status. You get the incremental-processing semantics of streaming with the operational shape of a batch job, and you commit once per run rather than once per micro-batch. Reach for a continuous processing-time trigger when latency genuinely matters, and then accept that you have signed up for the maintenance in the next section. The cost that shows up in week three Every commit writes a metadata file, a manifest list, and at least one manifest. At one snapshot a minute, a table accumulates 1,440 of each per day. Nothing fails. Queries just get slower, because planning a query means reading the current metadata file, its manifest list, and the manifests it points at. The symptom is distinctive: query planning time grows while the data volume is flat, and the slowdown affects every query against the table equally, including trivial ones. Two jobs keep it in check, and a streaming table needs both scheduled from the day it is created: Compaction merges the many small files each micro-batch produced into fewer large ones. Without it, a table ingesting every minute accumulates a file per partition per minute. Snapshot expiry removes old snapshots and the files only they reference. This is the one that controls storage, and compaction without expiry grows the bill while shrinking the file count. The mechanics of both are in table maintenance for Iceberg and Hudi. There is a third knob worth knowing: write.metadata.previous-versions-max and write.metadata.delete-after-commit.enabled bound how many old metadata JSON files are kept, which is separate from snapshot expiry and is the specific fix for a metadata directory with tens of thousands of files in it. Configuring the stream Three options change the shape of what a streaming write produces, and the defaults are rarely what a production table wants. q = (stream .writeStream .format(&quot;iceberg&quot;) .outputMode(&quot;append&quot;) .trigger(availableNow=True) .option(&quot;checkpointLocation&quot;, &quot;/work/ckpt&quot;) .option(&quot;fanout-enabled&quot;, &quot;true&quot;) .toTable(&quot;local.db.events&quot;)) fanout-enabled is the one that bites. Without it, the writer requires input sorted by partition, and unsorted micro-batch data raises rather than writing. With it, the writer keeps a file open per partition — correct for unsorted streaming input, at the cost of more concurrent writers and more small files. For a stream landing into many partitions per batch, enable it and compact more often; for a stream landing into one or two, sort upstream and leave it off. maxFilesPerTrigger on a file source bounds how much one batch consumes, which is what stops a backlog from being processed as one enormous batch: stream = (spark.readStream.schema(schema) .option(&quot;maxFilesPerTrigger&quot;, 200) .parquet(src)) With availableNow, this turns a large backlog into several sequential micro-batches rather than one, so memory stays bounded and each batch commits separately. The trigger determines the commit rate, which is the metadata growth rate. Worth restating as arithmetic: a one-minute trigger is 1,440 commits a day, each writing three metadata files, so 4,320 metadata files a day on a table that may only receive a few thousand rows. Watching a running stream StreamingQueryProgress answers whether the stream is keeping up, and the Iceberg side answers what it is doing to the table. p = q.lastProgress print(&quot;batch&quot;, p[&quot;batchId&quot;], &quot;rows&quot;, p[&quot;numInputRows&quot;]) print(&quot;batch duration ms&quot;, p[&quot;batchDuration&quot;]) print(&quot;sources&quot;, [(s[&quot;description&quot;][:40], s[&quot;endOffset&quot;]) for s in p[&quot;sources&quot;]]) -- how fast is the table accumulating commits? SELECT date_trunc(&apos;hour&apos;, committed_at) AS hour, count(*) AS commits, sum(summary[&apos;added-data-files&apos;]) AS files FROM local.db.events.snapshots GROUP BY 1 ORDER BY 1 DESC LIMIT 12; That second query is the one to put on a dashboard. Commits per hour is the number that predicts every metadata problem this table will have, and it is visible long before anything gets slow. Batch duration approaching the trigger interval is the other early warning: once processing takes longer than the trigger, batches queue and latency grows without bound. Recovering a stream Three situations, three different answers. The query failed and the data is fine. Restart it with the same checkpoint. Offsets and commits reconcile, an incomplete batch re-runs, and nothing duplicates. The output is wrong and must be replayed. Do not simply delete the checkpoint — that re-ingests everything into a table that still holds the bad rows. Roll the table back first, then reset the stream to a matching position: CALL local.system.rollback_to_snapshot(&apos;db.events&apos;, 9044140986145286505); The schema changed upstream. A new column in the source is additive: add it to the table, and restart. A type change is not, and needs the add-migrate-drop pattern rather than a stream restart. The checkpoint’s offsets directory records what each batch was told to read, and it is readable when you need to reason about where a stream is: cat /work/ckpt/offsets/$(ls /work/ckpt/offsets | sort -n | tail -1) Maintenance is part of the pipeline A streaming table without scheduled maintenance is a table with a deadline. Both jobs, from the day it is created: -- compact what the micro-batches produced, scoped to recent partitions CALL local.system.rewrite_data_files( table =&amp;gt; &apos;db.events&apos;, where =&amp;gt; &apos;event_ts &amp;gt;= current_date() - INTERVAL 1 DAYS&apos;, options =&amp;gt; map(&apos;min-input-files&apos;,&apos;10&apos;)); -- and bound the metadata, which snapshot expiry alone does not touch ALTER TABLE local.db.events SET TBLPROPERTIES ( &apos;write.metadata.delete-after-commit.enabled&apos; = &apos;true&apos;, &apos;write.metadata.previous-versions-max&apos; = &apos;200&apos;); The second statement is the one people miss. Expiring snapshots removes snapshots and their exclusive data files; the metadata.json files have a separate retention, and a per-minute stream will otherwise leave hundreds of thousands of them. The trap that produces a healthy-looking no-op Staging the input files one directory deep produced this: rows after availableNow run = 0 snapshots created = 0 checkpoint dirs = [&apos;commits&apos;, &apos;metadata&apos;, &apos;offsets&apos;, &apos;sources&apos;] The query started, created its checkpoint, ran, and terminated normally. It read nothing, because the streaming file source lists files in the given path and does not recurse into subdirectories. The files were in src/batch0/part-*.parquet and the source was watching src/. Nothing in that output says “I found no input”. The checkpoint exists, the query exited zero, and only the row count reveals the problem. This was my own mistake while writing this post, and it is worth naming because a silent zero-row ingestion is exactly the failure that survives into production unnoticed. Two defences. Stage input flat in the watched directory, which is what the measured run above does. And assert on the row count or the snapshot count after the run, rather than on the exit status. Row-level operations alongside a stream A streaming append is the simple case. Streaming upserts go through foreachBatch and MERGE INTO, because the sink itself only appends: def upsert(batch_df, batch_id): batch_df.createOrReplaceTempView(&quot;changes&quot;) batch_df.sparkSession.sql(&quot;&quot;&quot; MERGE INTO local.db.target t USING changes s ON t.id = s.id WHEN MATCHED THEN UPDATE SET * WHEN NOT MATCHED THEN INSERT *&quot;&quot;&quot;) query = (stream.writeStream.foreachBatch(upsert) .trigger(availableNow=True) .option(&quot;checkpointLocation&quot;, &quot;/work/ckpt_merge&quot;).start()) Note that foreachBatch gives at-least-once by default: the same batch_id can run twice after a failure. A MERGE keyed on a primary key is naturally idempotent, which is why this pattern works — an append inside foreachBatch would not be. The delete-file accumulation that this pattern causes on a merge-on-read table is its own problem, covered in copy-on-write or merge-on-read. Common misconceptions “Streaming writes row by row.” It writes micro-batches, each an atomic Iceberg commit. “One snapshot per file.” One snapshot per micro-batch that produced data. Three files in one availableNow run gave one snapshot. “Re-running will duplicate data.” Not with the same checkpoint. With a new checkpoint, it certainly will. “availableNow is not really streaming.” It uses the streaming engine, offsets and checkpoint, and processes only what is new. It just stops when it catches up. “Small files are the only streaming problem.” Metadata files accumulate on the same schedule and are bounded by different settings. A model worth keeping A streaming write into Iceberg is a loop of small batch commits. The trigger sets how often you commit; the checkpoint makes re-running safe; the snapshot count is the bill. So choose availableNow unless latency requires otherwise, keep the checkpoint as carefully as you keep the table, and schedule compaction and expiry on the same day you create the table rather than the week queries get slow. References Iceberg Spark structured streaming for the sink options and write behaviour Structured Streaming programming guide for triggers and checkpoint semantics Iceberg table properties for write.metadata.previous-versions-max and the metadata retention settings Structured Streaming internals for what the checkpoint directory holds Table maintenance for Iceberg and Hudi for the compaction and expiry this post assumes Trademarks Apache Iceberg, Apache Spark, Apache Parquet, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Loading data into Iceberg: appends, overwrites and row-level SQL</title><link href="https://rangareddy.github.io/IcebergIngestion/" rel="alternate" type="text/html" title="Loading data into Iceberg: appends, overwrites and row-level SQL" /><published>2026-09-01T10:00:00+05:30</published><updated>2026-09-01T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergIngestion</id><content type="html" xml:base="https://rangareddy.github.io/IcebergIngestion/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#the-four-ways&quot; id=&quot;markdown-toc-the-four-ways&quot;&gt;The four ways in&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#overwrite&quot; id=&quot;markdown-toc-overwrite&quot;&gt;Overwrite, and the one that surprises people&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#row-level-sql&quot; id=&quot;markdown-toc-row-level-sql&quot;&gt;Row-level SQL&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#merge-into&quot; id=&quot;markdown-toc-merge-into&quot;&gt;MERGE INTO&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#two-ways-to-get-merge-wrong&quot; id=&quot;markdown-toc-two-ways-to-get-merge-wrong&quot;&gt;Two ways to get MERGE wrong&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#batch-patterns&quot; id=&quot;markdown-toc-batch-patterns&quot;&gt;Batch loading patterns&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#controlling-files&quot; id=&quot;markdown-toc-controlling-files&quot;&gt;Controlling the files a write produces&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#idempotent-loads&quot; id=&quot;markdown-toc-idempotent-loads&quot;&gt;Idempotent batch loads&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#writing-from-outside-spark&quot; id=&quot;markdown-toc-writing-from-outside-spark&quot;&gt;Writing from outside Spark&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-to-check&quot; id=&quot;markdown-toc-what-to-check&quot;&gt;What to check after a load&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#writing-well&quot; id=&quot;markdown-toc-writing-well&quot;&gt;Writing well&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Four ways in: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwritePartitions&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; with a filter, and row-level SQL (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt;).&lt;/li&gt;
    &lt;li&gt;A single &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt; applied inserts, updates and deletes together: a 1,000-row table plus a 1,000-row changelog produced &lt;strong&gt;init=500, upd=300, ins=500 — 1,300 rows&lt;/strong&gt; from one statement.&lt;/li&gt;
    &lt;li&gt;Each write records an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;operation&lt;/code&gt; in the snapshot log: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt; for added files, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; when files were replaced, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt; when a delete file was written.&lt;/li&gt;
    &lt;li&gt;Prefer &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;writeTo(...).append()&lt;/code&gt; over &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.format(&quot;iceberg&quot;).save(path)&lt;/code&gt; — the v2 API resolves the table through the catalog rather than a path.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; fails on duplicate keys in the source rather than picking one. Deduplicate before merging.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;the-four-ways&quot;&gt;The four ways in&lt;/h2&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# 1. append: add data, touch nothing existing
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;append&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# 2. dynamic partition overwrite: replace only the partitions present in df
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;overwritePartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# 3. filtered overwrite: replace exactly what the filter matches
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;overwrite&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;event_ts&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-01-06&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# 4. row-level SQL
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;UPDATE local.db.events SET region = &apos;r9&apos; WHERE id &amp;lt; 100&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;DELETE FROM local.db.events WHERE region = &apos;test&apos;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;MERGE INTO local.db.events t USING changes s ON t.id = s.id ...&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Use the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;writeTo&lt;/code&gt; API, not &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.format(&quot;iceberg&quot;).save(path)&lt;/code&gt;.&lt;/strong&gt; The former
resolves the table through the catalog, which is what makes the commit atomic and
the identity stable. Writing by path bypasses the thing that makes an Iceberg
table a table.&lt;/p&gt;

&lt;p&gt;Each of these records what it did:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Statement&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Snapshot &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;operation&lt;/code&gt;&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append()&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwritePartitions()&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt; under copy-on-write&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt; under merge-on-read&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;delete&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt; under copy-on-write&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;That column is the audit trail. When a table changed unexpectedly, the sequence of
operations says what kind of change happened before you go looking at data.&lt;/p&gt;

&lt;h2 id=&quot;overwrite&quot;&gt;Overwrite, and the one that surprises people&lt;/h2&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwritePartitions()&lt;/code&gt; is dynamic: it replaces the partitions &lt;strong&gt;present in the
incoming DataFrame&lt;/strong&gt; and leaves every other partition alone. Reloading one day
touches that day only.&lt;/p&gt;

&lt;p&gt;The surprise is what happens when the DataFrame is empty. No partitions are
present, so nothing is replaced — the statement succeeds and changes nothing. A
pipeline whose upstream produced no rows silently keeps yesterday’s data, and the
job is green.&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite(filter)&lt;/code&gt; is explicit: it replaces exactly what the filter matches,
whether or not the incoming data covers it. An empty DataFrame with a filter for
yesterday deletes yesterday, which is at least unambiguous.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Use the filtered form when correctness depends on the old data being gone&lt;/strong&gt;, and
the dynamic form when you are topping up partitions you know you have.&lt;/p&gt;

&lt;h2 id=&quot;row-level-sql&quot;&gt;Row-level SQL&lt;/h2&gt;

&lt;p&gt;The statements that make a lakehouse table behave like a database table.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;UPDATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;r9&apos;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;DELETE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;test&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Both work on format v1 by rewriting files, and on v2 either by rewriting
(copy-on-write) or by writing delete files (merge-on-read), governed by
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.update.mode&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.delete.mode&lt;/code&gt;. The
&lt;a href=&quot;/IcebergCopyOnWriteMergeOnRead/&quot;&gt;comparison of the two&lt;/a&gt;
has both measured.&lt;/p&gt;

&lt;h3 id=&quot;merge-into&quot;&gt;MERGE INTO&lt;/h3&gt;

&lt;p&gt;The statement that applies a changelog, doing all three operations in one pass:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;MERGE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;INTO&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tgt&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;changes&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ON&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;MATCHED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AND&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;chg&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;D&apos;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DELETE&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;MATCHED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;UPDATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;op&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;upd&apos;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHEN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;NOT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;MATCHED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;THEN&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;INSERT&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;op&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VALUES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;ins&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Against a 1,000-row table with a 1,000-row changelog overlapping half of it:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;op=init  rows=500
op=ins   rows=500
op=upd   rows=300
total rows = 1300
snapshot ops: [&apos;append&apos;, &apos;overwrite&apos;]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Five hundred untouched rows, 500 inserted, 300 updated, 200 deleted — and &lt;strong&gt;one
snapshot&lt;/strong&gt;. The whole changelog applied atomically: no reader saw the inserts
without the deletes.&lt;/p&gt;

&lt;h3 id=&quot;two-ways-to-get-merge-wrong&quot;&gt;Two ways to get MERGE wrong&lt;/h3&gt;

&lt;p&gt;&lt;strong&gt;Duplicate keys in the source.&lt;/strong&gt; If &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;changes&lt;/code&gt; contains two rows for the same
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;id&lt;/code&gt;, the statement fails rather than picking one, because the outcome would be
arbitrary. Collapse to one row per key first, using the source’s sequence number
or commit timestamp:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;w&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Window&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orderBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;log_seq&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;desc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;latest&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;changes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;row_number&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;over&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;w&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn = 1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;drop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Ordering by arrival time instead of the source’s own sequence is a common bug:
out-of-order delivery then applies an older version over a newer one, and the
table is quietly wrong.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;A match condition that is not the real key.&lt;/strong&gt; Matching on a column that is not
unique in the target updates more rows than intended, or fails ambiguously. It
usually passes testing, because the test data happens to be unique.&lt;/p&gt;

&lt;h2 id=&quot;batch-patterns&quot;&gt;Batch loading patterns&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Full reload&lt;/strong&gt;: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; with a filter covering everything, or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CREATE OR
REPLACE TABLE ... AS SELECT&lt;/code&gt;. Simple, and rewrites everything every time.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Incremental append&lt;/strong&gt;: the cheapest path, and the one that accumulates small
files fastest. Compaction is the other half of this pattern.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Partition reload&lt;/strong&gt;: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwritePartitions()&lt;/code&gt; with the partitions you regenerated.
The normal shape for a daily pipeline that reprocesses late-arriving data.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Upsert&lt;/strong&gt;: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE INTO&lt;/code&gt;, with the deduplication above.&lt;/p&gt;

&lt;p&gt;Whichever you use, &lt;strong&gt;the file-count consequence is the same&lt;/strong&gt;: every commit writes
files, and nothing consolidates them until compaction runs. A pipeline appending
hourly produces 24 files per partition per day.&lt;/p&gt;

&lt;h2 id=&quot;controlling-files&quot;&gt;Controlling the files a write produces&lt;/h2&gt;

&lt;p&gt;The most common ingestion problem is not correctness, it is file count, and it is
decided before the write by how the DataFrame is partitioned.&lt;/p&gt;

&lt;p&gt;A DataFrame with 200 Spark partitions writing into 30 table partitions can
produce up to 6,000 files in one commit. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.distribution-mode&lt;/code&gt; makes Iceberg
shuffle for you instead:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SET&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.distribution-mode&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;hash&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;            &lt;span class=&quot;c1&quot;&gt;-- shuffle by partition key&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&apos;write.target-file-size-bytes&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;268435456&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt; &lt;span class=&quot;c1&quot;&gt;-- 256 MiB&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Mode&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Behaviour&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Use when&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;none&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;write whatever partitioning the DataFrame has&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;data is already partitioned correctly&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hash&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;shuffle by partition key before writing&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;the usual choice for partitioned tables&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;range&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;range-partition, respecting sort order&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;writing with a declared sort order&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hash&lt;/code&gt; costs a shuffle and saves you from the small-file problem that shuffle was
avoiding. On a partitioned table it is almost always the right default, and the
exception is a pipeline that already repartitioned deliberately:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;24&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;event_ts&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sortWithinPartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;region&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;append&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Check what a write actually produced rather than assuming:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;partition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;n&quot;&gt;round&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;avg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;file_size_in_bytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1048576&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;avg_mb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;rows&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;partition&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DESC&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;LIMIT&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Average file sizes in the low single-digit megabytes mean the next query against
that partition pays for it.&lt;/p&gt;

&lt;h2 id=&quot;idempotent-loads&quot;&gt;Idempotent batch loads&lt;/h2&gt;

&lt;p&gt;A reload that runs twice should not double the data, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt; gives you no
help with that. Three patterns, in order of preference.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Partition overwrite&lt;/strong&gt;, when the load is naturally partition-scoped:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;daily_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;overwritePartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Re-running replaces the same partitions. Safe to repeat — with the empty-DataFrame
caveat from earlier, which is worth guarding explicitly:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;daily_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;head&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;daily_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;overwritePartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;else&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;raise&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;ValueError&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;refusing to run a load that produced no rows&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Filtered overwrite&lt;/strong&gt;, when correctness requires the old rows gone whether or
not new ones arrived:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;daily_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local.db.events&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;overwrite&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;event_ts&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;2026-01-06&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;date&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;MERGE on a key&lt;/strong&gt;, when the load overlaps arbitrary existing rows. Naturally
idempotent because it reconciles rather than adds.&lt;/p&gt;

&lt;h2 id=&quot;writing-from-outside-spark&quot;&gt;Writing from outside Spark&lt;/h2&gt;

&lt;p&gt;Two ingestion routes that are not a Spark DataFrame, and both come up.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Adopting files that already exist&lt;/strong&gt; — data written by another tool into the
right layout, added to the table without copying:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;add_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;source_table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;`parquet`.`s3://landing/events/dt=2026-01-06`&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;This reads the Parquet footers for statistics and writes manifests pointing at
the files where they are. It does not validate that the schema matches, so check
before and count after.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CREATE TABLE AS SELECT&lt;/code&gt;&lt;/strong&gt; for the initial load of a derived table:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events_summary&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;PARTITIONED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;days&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;TBLPROPERTIES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;write.target-file-size-bytes&apos;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;268435456&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;n&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount_usd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;total&lt;/span&gt;
   &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Set the table properties in the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CREATE&lt;/code&gt;, because properties applied afterwards
only affect subsequent writes — the initial load will already have produced its
files.&lt;/p&gt;

&lt;h2 id=&quot;what-to-check&quot;&gt;What to check after a load&lt;/h2&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- did the commit do what you expected?&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;operation&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;summary&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;added-data-files&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;n&quot;&gt;summary&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;added-records&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;records&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;summary&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;deleted-records&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;deleted&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;snapshots&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;committed_at&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DESC&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;LIMIT&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;deleted-records&lt;/code&gt; on a load you believed was an append is the signal that
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwritePartitions&lt;/code&gt; replaced more than intended — the fastest way to catch a
partition-scoping mistake, and it costs one query.&lt;/p&gt;

&lt;h2 id=&quot;writing-well&quot;&gt;Writing well&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Control the number of output files.&lt;/strong&gt; A DataFrame with 200 partitions writes up
to 200 files per target partition. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition&lt;/code&gt; to something proportional to the
data before writing, or set &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write.distribution-mode&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hash&lt;/code&gt; so Iceberg
shuffles by partition key for you.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Set the target file size&lt;/strong&gt; at create time rather than discovering it later, and
know that it shapes compaction more reliably than ingestion.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Do not commit more often than you need to.&lt;/strong&gt; Commit frequency drives metadata
growth — three metadata files per commit — and is the main lever on long-term
table health.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwritePartitions&lt;/code&gt; replaces the table.”&lt;/strong&gt; It replaces only the partitions
present in the incoming data. With no rows, it replaces nothing.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; is several statements.”&lt;/strong&gt; It is one commit. The measured merge produced
one snapshot for inserts, updates and deletes together.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;save(path)&lt;/code&gt; is equivalent to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;writeTo&lt;/code&gt;.”&lt;/strong&gt; Writing by path bypasses the
catalog, which is where atomicity and identity live.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Appending is free.”&lt;/strong&gt; Each append is a commit: three metadata files plus data
files, and small files until compacted.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt; needs merge-on-read.”&lt;/strong&gt; It works on any version; the mode decides
whether files are rewritten or delete files written.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;Every write is a commit, and the snapshot log records which kind. Choose the
statement by what should be true afterwards — add rows, replace these partitions,
replace what matches this filter, or reconcile against a changelog — and let the
mode properties decide how it is physically done.&lt;/p&gt;

&lt;p&gt;Then remember that ingestion is half the job: the files you write are the files
someone has to compact.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-writes/&quot;&gt;Iceberg Spark writes&lt;/a&gt; for the write API and row-level SQL&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/configuration/#write-properties&quot;&gt;Iceberg configuration&lt;/a&gt; for distribution mode and file sizing&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergCopyOnWriteMergeOnRead/&quot;&gt;Copy-on-write or merge-on-read&lt;/a&gt; for how row-level statements are executed&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergStreamingIngestion/&quot;&gt;Streaming into Iceberg&lt;/a&gt; for the continuous case&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergCdcPipelines/&quot;&gt;CDC into Iceberg&lt;/a&gt; for MERGE applied repeatedly&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Spark, Apache and the Apache feather logo are either
registered trademarks or trademarks of The Apache Software Foundation in the
United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Iceberg" /><category term="Iceberg" /><category term="Ingestion" /><category term="Merge" /><category term="Overwrite" /><summary type="html">The four ways in Overwrite, and the one that surprises people Row-level SQL MERGE INTO Two ways to get MERGE wrong Batch loading patterns Controlling the files a write produces Idempotent batch loads Writing from outside Spark What to check after a load Writing well Common misconceptions A model worth keeping References Trademarks TL;DR Four ways in: append, overwritePartitions, overwrite with a filter, and row-level SQL (UPDATE, DELETE, MERGE INTO). A single MERGE INTO applied inserts, updates and deletes together: a 1,000-row table plus a 1,000-row changelog produced init=500, upd=300, ins=500 — 1,300 rows from one statement. Each write records an operation in the snapshot log: append for added files, overwrite when files were replaced, delete when a delete file was written. Prefer writeTo(...).append() over write.format(&quot;iceberg&quot;).save(path) — the v2 API resolves the table through the catalog rather than a path. MERGE fails on duplicate keys in the source rather than picking one. Deduplicate before merging. The four ways in # 1. append: add data, touch nothing existing df.writeTo(&quot;local.db.events&quot;).append() # 2. dynamic partition overwrite: replace only the partitions present in df df.writeTo(&quot;local.db.events&quot;).overwritePartitions() # 3. filtered overwrite: replace exactly what the filter matches df.writeTo(&quot;local.db.events&quot;).overwrite(F.col(&quot;event_ts&quot;) &amp;gt;= &quot;2026-01-06&quot;) # 4. row-level SQL spark.sql(&quot;UPDATE local.db.events SET region = &apos;r9&apos; WHERE id &amp;lt; 100&quot;) spark.sql(&quot;DELETE FROM local.db.events WHERE region = &apos;test&apos;&quot;) spark.sql(&quot;MERGE INTO local.db.events t USING changes s ON t.id = s.id ...&quot;) Use the writeTo API, not write.format(&quot;iceberg&quot;).save(path). The former resolves the table through the catalog, which is what makes the commit atomic and the identity stable. Writing by path bypasses the thing that makes an Iceberg table a table. Each of these records what it did: Statement Snapshot operation append() append overwritePartitions() overwrite DELETE under copy-on-write overwrite DELETE under merge-on-read delete MERGE INTO under copy-on-write overwrite That column is the audit trail. When a table changed unexpectedly, the sequence of operations says what kind of change happened before you go looking at data. Overwrite, and the one that surprises people overwritePartitions() is dynamic: it replaces the partitions present in the incoming DataFrame and leaves every other partition alone. Reloading one day touches that day only. The surprise is what happens when the DataFrame is empty. No partitions are present, so nothing is replaced — the statement succeeds and changes nothing. A pipeline whose upstream produced no rows silently keeps yesterday’s data, and the job is green. overwrite(filter) is explicit: it replaces exactly what the filter matches, whether or not the incoming data covers it. An empty DataFrame with a filter for yesterday deletes yesterday, which is at least unambiguous. Use the filtered form when correctness depends on the old data being gone, and the dynamic form when you are topping up partitions you know you have. Row-level SQL The statements that make a lakehouse table behave like a database table. UPDATE local.db.events SET region = &apos;r9&apos; WHERE id &amp;lt; 100; DELETE FROM local.db.events WHERE region = &apos;test&apos;; Both work on format v1 by rewriting files, and on v2 either by rewriting (copy-on-write) or by writing delete files (merge-on-read), governed by write.update.mode and write.delete.mode. The comparison of the two has both measured. MERGE INTO The statement that applies a changelog, doing all three operations in one pass: MERGE INTO local.db.tgt t USING changes s ON t.id = s.id WHEN MATCHED AND s.chg = &apos;D&apos; THEN DELETE WHEN MATCHED THEN UPDATE SET t.v = s.v, t.op = &apos;upd&apos; WHEN NOT MATCHED THEN INSERT (id, v, op) VALUES (s.id, s.v, &apos;ins&apos;) Against a 1,000-row table with a 1,000-row changelog overlapping half of it: op=init rows=500 op=ins rows=500 op=upd rows=300 total rows = 1300 snapshot ops: [&apos;append&apos;, &apos;overwrite&apos;] Five hundred untouched rows, 500 inserted, 300 updated, 200 deleted — and one snapshot. The whole changelog applied atomically: no reader saw the inserts without the deletes. Two ways to get MERGE wrong Duplicate keys in the source. If changes contains two rows for the same id, the statement fails rather than picking one, because the outcome would be arbitrary. Collapse to one row per key first, using the source’s sequence number or commit timestamp: w = Window.partitionBy(&quot;id&quot;).orderBy(F.col(&quot;log_seq&quot;).desc()) latest = changes.withColumn(&quot;rn&quot;, F.row_number().over(w)).filter(&quot;rn = 1&quot;).drop(&quot;rn&quot;) Ordering by arrival time instead of the source’s own sequence is a common bug: out-of-order delivery then applies an older version over a newer one, and the table is quietly wrong. A match condition that is not the real key. Matching on a column that is not unique in the target updates more rows than intended, or fails ambiguously. It usually passes testing, because the test data happens to be unique. Batch loading patterns Full reload: overwrite with a filter covering everything, or CREATE OR REPLACE TABLE ... AS SELECT. Simple, and rewrites everything every time. Incremental append: the cheapest path, and the one that accumulates small files fastest. Compaction is the other half of this pattern. Partition reload: overwritePartitions() with the partitions you regenerated. The normal shape for a daily pipeline that reprocesses late-arriving data. Upsert: MERGE INTO, with the deduplication above. Whichever you use, the file-count consequence is the same: every commit writes files, and nothing consolidates them until compaction runs. A pipeline appending hourly produces 24 files per partition per day. Controlling the files a write produces The most common ingestion problem is not correctness, it is file count, and it is decided before the write by how the DataFrame is partitioned. A DataFrame with 200 Spark partitions writing into 30 table partitions can produce up to 6,000 files in one commit. write.distribution-mode makes Iceberg shuffle for you instead: ALTER TABLE local.db.events SET TBLPROPERTIES ( &apos;write.distribution-mode&apos; = &apos;hash&apos;, -- shuffle by partition key &apos;write.target-file-size-bytes&apos; = &apos;268435456&apos;); -- 256 MiB Mode Behaviour Use when none write whatever partitioning the DataFrame has data is already partitioned correctly hash shuffle by partition key before writing the usual choice for partitioned tables range range-partition, respecting sort order writing with a declared sort order hash costs a shuffle and saves you from the small-file problem that shuffle was avoiding. On a partitioned table it is almost always the right default, and the exception is a pipeline that already repartitioned deliberately: (df.repartition(24, &quot;event_ts&quot;) .sortWithinPartitions(&quot;region&quot;) .writeTo(&quot;local.db.events&quot;).append()) Check what a write actually produced rather than assuming: SELECT partition, count(*) AS files, round(avg(file_size_in_bytes) / 1048576, 1) AS avg_mb, sum(record_count) AS rows FROM local.db.events.files GROUP BY partition ORDER BY files DESC LIMIT 10; Average file sizes in the low single-digit megabytes mean the next query against that partition pays for it. Idempotent batch loads A reload that runs twice should not double the data, and append gives you no help with that. Three patterns, in order of preference. Partition overwrite, when the load is naturally partition-scoped: (daily_df.writeTo(&quot;local.db.events&quot;).overwritePartitions()) Re-running replaces the same partitions. Safe to repeat — with the empty-DataFrame caveat from earlier, which is worth guarding explicitly: if daily_df.head(1): daily_df.writeTo(&quot;local.db.events&quot;).overwritePartitions() else: raise ValueError(&quot;refusing to run a load that produced no rows&quot;) Filtered overwrite, when correctness requires the old rows gone whether or not new ones arrived: (daily_df.writeTo(&quot;local.db.events&quot;) .overwrite(F.col(&quot;event_ts&quot;) == F.lit(&quot;2026-01-06&quot;).cast(&quot;date&quot;))) MERGE on a key, when the load overlaps arbitrary existing rows. Naturally idempotent because it reconciles rather than adds. Writing from outside Spark Two ingestion routes that are not a Spark DataFrame, and both come up. Adopting files that already exist — data written by another tool into the right layout, added to the table without copying: CALL local.system.add_files( table =&amp;gt; &apos;db.events&apos;, source_table =&amp;gt; &apos;`parquet`.`s3://landing/events/dt=2026-01-06`&apos;); This reads the Parquet footers for statistics and writes manifests pointing at the files where they are. It does not validate that the schema matches, so check before and count after. CREATE TABLE AS SELECT for the initial load of a derived table: CREATE TABLE local.db.events_summary USING iceberg PARTITIONED BY (days(event_ts)) TBLPROPERTIES (&apos;write.target-file-size-bytes&apos; = &apos;268435456&apos;) AS SELECT event_ts, region, count(*) AS n, sum(amount_usd) AS total FROM local.db.events GROUP BY event_ts, region; Set the table properties in the CREATE, because properties applied afterwards only affect subsequent writes — the initial load will already have produced its files. What to check after a load -- did the commit do what you expected? SELECT operation, summary[&apos;added-data-files&apos;] AS files, summary[&apos;added-records&apos;] AS records, summary[&apos;deleted-records&apos;] AS deleted FROM local.db.events.snapshots ORDER BY committed_at DESC LIMIT 1; deleted-records on a load you believed was an append is the signal that overwritePartitions replaced more than intended — the fastest way to catch a partition-scoping mistake, and it costs one query. Writing well Control the number of output files. A DataFrame with 200 partitions writes up to 200 files per target partition. repartition to something proportional to the data before writing, or set write.distribution-mode to hash so Iceberg shuffles by partition key for you. Set the target file size at create time rather than discovering it later, and know that it shapes compaction more reliably than ingestion. Do not commit more often than you need to. Commit frequency drives metadata growth — three metadata files per commit — and is the main lever on long-term table health. Common misconceptions “overwritePartitions replaces the table.” It replaces only the partitions present in the incoming data. With no rows, it replaces nothing. “MERGE is several statements.” It is one commit. The measured merge produced one snapshot for inserts, updates and deletes together. “save(path) is equivalent to writeTo.” Writing by path bypasses the catalog, which is where atomicity and identity live. “Appending is free.” Each append is a commit: three metadata files plus data files, and small files until compacted. “UPDATE needs merge-on-read.” It works on any version; the mode decides whether files are rewritten or delete files written. A model worth keeping Every write is a commit, and the snapshot log records which kind. Choose the statement by what should be true afterwards — add rows, replace these partitions, replace what matches this filter, or reconcile against a changelog — and let the mode properties decide how it is physically done. Then remember that ingestion is half the job: the files you write are the files someone has to compact. References Iceberg Spark writes for the write API and row-level SQL Iceberg configuration for distribution mode and file sizing Copy-on-write or merge-on-read for how row-level statements are executed Streaming into Iceberg for the continuous case CDC into Iceberg for MERGE applied repeatedly Trademarks Apache Iceberg, Apache Spark, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Hidden partitioning and partition evolution: changing layout without rewriting history</title><link href="https://rangareddy.github.io/IcebergPartitioning/" rel="alternate" type="text/html" title="Hidden partitioning and partition evolution: changing layout without rewriting history" /><published>2026-08-30T10:00:00+05:30</published><updated>2026-08-30T10:00:00+05:30</updated><id>https://rangareddy.github.io/IcebergPartitioning</id><content type="html" xml:base="https://rangareddy.github.io/IcebergPartitioning/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#the-hive-problem&quot; id=&quot;markdown-toc-the-hive-problem&quot;&gt;The Hive problem it fixes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#how-iceberg-derives&quot; id=&quot;markdown-toc-how-iceberg-derives&quot;&gt;How Iceberg derives partitions&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#the-transforms&quot; id=&quot;markdown-toc-the-transforms&quot;&gt;The transforms&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#partition-evolution-measured&quot; id=&quot;markdown-toc-partition-evolution-measured&quot;&gt;Partition evolution, measured&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing-the-transform&quot; id=&quot;markdown-toc-choosing-the-transform&quot;&gt;Choosing the transform, with arithmetic&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#evolving-in-practice&quot; id=&quot;markdown-toc-evolving-in-practice&quot;&gt;Evolving in practice&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#partitioning-versus-sorting&quot; id=&quot;markdown-toc-partitioning-versus-sorting&quot;&gt;Where partitioning stops and sorting starts&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-it-does-not-do&quot; id=&quot;markdown-toc-what-it-does-not-do&quot;&gt;What evolution does not do&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing-a-layout&quot; id=&quot;markdown-toc-choosing-a-layout&quot;&gt;Choosing a layout&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-model-worth-keeping&quot; id=&quot;markdown-toc-a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Partition values are &lt;strong&gt;derived&lt;/strong&gt; from a column by a transform, so a query filters on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;event_ts&lt;/code&gt; and Iceberg prunes &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;event_ts_day&lt;/code&gt; for it. Measured: a plain timestamp predicate matched 3,000 rows in one day’s partition.&lt;/li&gt;
    &lt;li&gt;Changing the layout does &lt;strong&gt;not&lt;/strong&gt; rewrite data. After &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;REPLACE PARTITION FIELD months(ts) WITH days(ts)&lt;/code&gt;, files carried &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spec_id&lt;/code&gt; values of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;[1, 0, 0]&lt;/code&gt; — the old ones untouched.&lt;/li&gt;
    &lt;li&gt;Both specs stay in metadata (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spec-id&lt;/code&gt; 0 and 1) and both reference the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;source-id&lt;/code&gt;, so one predicate prunes across both layouts.&lt;/li&gt;
    &lt;li&gt;Directory names like &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ts_month=2026-01&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ts_day=2026-03-09&lt;/code&gt; coexist in the same table, and all 900 rows stayed queryable.&lt;/li&gt;
    &lt;li&gt;Hidden partitioning removes the Hive failure where forgetting the partition filter scans everything silently.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&quot;the-hive-problem&quot;&gt;The Hive problem it fixes&lt;/h2&gt;

&lt;p&gt;On a Hive table, a partition is a physical column in the path:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;/trips/dt=2026-01-06/part-0000.parquet
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;To get pruning, a query must filter on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dt&lt;/code&gt;. Filtering on the actual event
timestamp does not prune, because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dt&lt;/code&gt; is a separate column the user is
responsible for keeping consistent.&lt;/p&gt;

&lt;p&gt;Two failures follow, both routine. Someone writes &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE event_ts &amp;gt;= &apos;2026-01-06&apos;&lt;/code&gt;
and scans the whole table, because the query is correct and simply did not
mention &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dt&lt;/code&gt;. And someone writes a row whose &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dt&lt;/code&gt; disagrees with its &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;event_ts&lt;/code&gt;,
producing data that is invisible to correct queries.&lt;/p&gt;

&lt;h2 id=&quot;how-iceberg-derives&quot;&gt;How Iceberg derives partitions&lt;/h2&gt;

&lt;p&gt;You declare a transform on a real column:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;id&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;BIGINT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;TIMESTAMP&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;STRING&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;DOUBLE&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;PARTITIONED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;days&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;));&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;There is no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dt&lt;/code&gt; column. Iceberg computes the day from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;event_ts&lt;/code&gt; at write time
and records it in the manifests. The spec, read from the metadata file:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;partition-specs = [{&quot;spec-id&quot;: 0, &quot;fields&quot;: [
  {&quot;name&quot;: &quot;event_ts_day&quot;, &quot;transform&quot;: &quot;day&quot;, &quot;source-id&quot;: 2, &quot;field-id&quot;: 1000}]}]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;source-id: 2&lt;/code&gt; is the field id of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;event_ts&lt;/code&gt;. The partition is defined as &lt;em&gt;a
function of that column&lt;/em&gt;, which is the indirection everything else depends on.&lt;/p&gt;

&lt;p&gt;At read time the query names the real column:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;=&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;timestamp&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;2026-01-06 00:00:00&apos;&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;AND&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;  &lt;span class=&quot;nb&quot;&gt;timestamp&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;2026-01-07 00:00:00&apos;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;partition directories on disk: [&apos;event_ts_day=2026-01-05&apos;,
                                &apos;event_ts_day=2026-01-06&apos;,
                                &apos;event_ts_day=2026-01-07&apos;]
rows matched = 3000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Iceberg applies &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;day()&lt;/code&gt; to the predicate and matches it against partition values.
&lt;strong&gt;Nobody has to know the layout, and nobody can forget to filter on it.&lt;/strong&gt;&lt;/p&gt;

&lt;h3 id=&quot;the-transforms&quot;&gt;The transforms&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Transform&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Use&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;years&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;months&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;days&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hours&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;time columns, at the granularity queries filter on&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucket(N, col)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;high-cardinality keys; spreads evenly into N buckets&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;truncate(N, col)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;prefixes of strings, ranges of numbers&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;identity(col)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;the value itself — the Hive-style behaviour&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucket&lt;/code&gt; is the one people under-use. Partitioning by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;identity(customer_id)&lt;/code&gt; on a
million customers produces a million partitions and a small-file disaster;
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucket(64, customer_id)&lt;/code&gt; gives 64 balanced partitions that still prune equality
filters.&lt;/p&gt;

&lt;h2 id=&quot;partition-evolution-measured&quot;&gt;Partition evolution, measured&lt;/h2&gt;

&lt;p&gt;Monthly partitioning stops working when data grows. On Hive that means rewriting
the table. On Iceberg:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;pe&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;REPLACE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;PARTITION&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;FIELD&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;months&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WITH&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;days&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Then one more append. The result:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;dirs after months(): [&apos;ts_month=2026-01&apos;, &apos;ts_month=2026-02&apos;]
dirs after days()  : [&apos;ts_day=2026-03-09&apos;, &apos;ts_month=2026-01&apos;, &apos;ts_month=2026-02&apos;]

partition-specs: [{&quot;spec-id&quot;: 0, ... &quot;transform&quot;: &quot;month&quot;, &quot;source-id&quot;: 2, &quot;field-id&quot;: 1000},
                  {&quot;spec-id&quot;: 1, ... &quot;transform&quot;: &quot;day&quot;,   &quot;source-id&quot;: 2, &quot;field-id&quot;: 1001}]
default-spec-id: 1
spec_id per data file: [1, 0, 0]
rows total: 900
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;img src=&quot;/assets/images/iceberg/partition-evolution.png&quot; alt=&quot;After replacing a monthly partition transform with a daily one, the table records two partition specs and each data file is tagged with the spec that wrote it: two files keep spec_id 0 with month-based paths, the new file uses spec_id 1 with a day-based path, and nothing was rewritten&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Two of the three data files still carry &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spec_id = 0&lt;/code&gt;.&lt;/strong&gt; They were not rewritten,
moved or touched. The new file uses spec 1. Both directory layouts sit in the same
table, and all 900 rows are queryable through one name.&lt;/p&gt;

&lt;p&gt;This works because a manifest records which spec each file was written under. A
reader consults the right spec per file rather than assuming one layout for the
table.&lt;/p&gt;

&lt;p&gt;Note both specs reference &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;source-id: 2&lt;/code&gt;. Because both derive from the same
column, &lt;strong&gt;a single predicate on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ts&lt;/code&gt; prunes files under either spec&lt;/strong&gt; — the reader
applies &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;month()&lt;/code&gt; to old files and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;day()&lt;/code&gt; to new ones.&lt;/p&gt;

&lt;p&gt;The new field also got a new &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;field-id&lt;/code&gt; (1001, not 1000), which keeps old
partition values unambiguous.&lt;/p&gt;

&lt;h2 id=&quot;choosing-the-transform&quot;&gt;Choosing the transform, with arithmetic&lt;/h2&gt;

&lt;p&gt;The transform decides how many partitions exist, and partition count is the
number that goes wrong.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- how many partitions does the current layout actually produce?&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;partitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;k&quot;&gt;avg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;file_count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;avg_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
       &lt;span class=&quot;k&quot;&gt;avg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;avg_rows&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Aim for partitions holding &lt;strong&gt;hundreds of megabytes to a few gigabytes&lt;/strong&gt;. Work
backwards from volume: a table taking 50 GB a month partitioned by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;days&lt;/code&gt; gives
~1.6 GB per partition, which is comfortable. The same table partitioned by
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hours&lt;/code&gt; gives ~70 MB per partition and 720 partitions a month, which is the
beginning of a small-file problem.&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucket&lt;/code&gt; is the transform for high-cardinality keys, and the bucket count is a
capacity decision:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CREATE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orders&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;order_id&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;BIGINT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;customer_id&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;BIGINT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;DOUBLE&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;USING&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;iceberg&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;PARTITIONED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;bucket&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;64&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;customer_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;));&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Sixty-four balanced partitions, regardless of how many customers exist. An
equality filter on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;customer_id&lt;/code&gt; prunes to one bucket; a range filter prunes
nothing, because hashing destroys ordering. &lt;strong&gt;Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucket&lt;/code&gt; for keys you look up
and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;truncate&lt;/code&gt; or a time transform for keys you scan ranges of.&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;Multiple fields multiply:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;PARTITIONED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;days&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;bucket&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;16&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;customer_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;-- 30 x 16 = 480 per month&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is usually one field too many. Each additional field divides the data
further, and the small-file cost arrives faster than the pruning benefit.&lt;/p&gt;

&lt;h2 id=&quot;evolving-in-practice&quot;&gt;Evolving in practice&lt;/h2&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- add a field: new data gains the finer layout, old data keeps its own&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ADD&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;PARTITION&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;FIELD&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;hours&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- replace one: the usual month-to-day move&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;REPLACE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;PARTITION&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;FIELD&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;months&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WITH&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;days&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- drop one entirely&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DROP&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;PARTITION&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;FIELD&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;bucket&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;16&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;customer_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;All three are metadata-only, and all three leave existing files alone. To check
what you have afterwards:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spec_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;record_count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;records&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;files&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;GROUP&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spec_id&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spec_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A table with files spread across three or four specs is not broken, but it is
harder to reason about, and planning must consult each spec. When old data is
queried often enough to matter, rewrite it into the current layout:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CALL&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;system&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rewrite_data_files&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;table&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;db.events&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;where&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;event_ts &amp;lt; date&apos;&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2026&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;-&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;03&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;-&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;01&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&apos;&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is a data-moving job, unlike the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ALTER&lt;/code&gt;, and it is the step that actually
gives historical data the new pruning.&lt;/p&gt;

&lt;h2 id=&quot;partitioning-versus-sorting&quot;&gt;Where partitioning stops and sorting starts&lt;/h2&gt;

&lt;p&gt;Partitioning eliminates whole directories of files. Sorting narrows the column
statistics inside them so individual files can be skipped. They solve adjacent
problems and people reach for the wrong one regularly.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Filter shape&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What helps&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;event_ts&lt;/code&gt; in a narrow range&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;a time partition transform&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;customer_id = ?&lt;/code&gt; on a huge key space&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucket&lt;/code&gt; partitioning&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;region = ?&lt;/code&gt; with few regions&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;sort order, not partitioning&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;amount &amp;gt; ?&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;sort order&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;several unrelated columns&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;sort by the most selective, accept the rest&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;strong&gt;Partitioning on a low-cardinality column like &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;region&lt;/code&gt; is usually a mistake&lt;/strong&gt;:
four regions means four partitions, which does nothing for parallelism and
produces skew when one region dominates. A sort order handles it without the
file-count cost:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ALTER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;local&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;db&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WRITE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;ORDERED&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;region&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;event_ts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;what-it-does-not-do&quot;&gt;What evolution does not do&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;It does not reorganise history.&lt;/strong&gt; Old data keeps the old granularity. If queries
need day-level pruning over historical data, that is a rewrite —
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; with the new spec — and it is a data-movement job, distinct
from the metadata-only &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ALTER&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;It does not fix bad partitioning retroactively.&lt;/strong&gt; Over-partitioned history stays
over-partitioned until rewritten.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;It does not reduce the need to choose well.&lt;/strong&gt; Evolution is an escape hatch, not
a reason to skip the decision.&lt;/p&gt;

&lt;h2 id=&quot;choosing-a-layout&quot;&gt;Choosing a layout&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Partition on what you filter on&lt;/strong&gt;, at the granularity you filter at. Hourly
partitions for queries that always span weeks produce thousands of tiny files and
prune nothing useful.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Target partitions that hold real data.&lt;/strong&gt; Hundreds of megabytes to a few
gigabytes is a reasonable band. A partition per minute holding ten rows is
overhead with a directory name.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucket&lt;/code&gt; for high-cardinality keys&lt;/strong&gt; rather than &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;identity&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Prefer fewer partition fields.&lt;/strong&gt; Each one multiplies the partition count, and
two fields with a hundred values each is ten thousand partitions.&lt;/p&gt;

&lt;p&gt;Remember that pruning has a second stage: column bounds inside manifests skip
files within a partition, but only if data is sorted. Partitioning and sort order
work together, and the read path post covers where each applies.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Hidden partitioning means no partitions.”&lt;/strong&gt; The partitions are real and visible
on disk. What is hidden is the need to name them in queries.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Partition evolution rewrites the table.”&lt;/strong&gt; Measured: two of three files kept
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spec_id = 0&lt;/code&gt; and nothing moved.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“I must query the partition column.”&lt;/strong&gt; There is no partition column to query.
Filter the real column.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Old data becomes unqueryable after evolution.”&lt;/strong&gt; All 900 rows stayed readable
across both specs.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“More partitions prune better.”&lt;/strong&gt; More partitions mean more metadata and smaller
files. Pruning comes from matching the transform to the filter.&lt;/p&gt;

&lt;h2 id=&quot;a-model-worth-keeping&quot;&gt;A model worth keeping&lt;/h2&gt;

&lt;p&gt;A partition is a function of a column, recorded per file. Queries filter the
column; Iceberg applies the function. Change the function and old files keep
their old one, because each file remembers which spec produced it.&lt;/p&gt;

&lt;p&gt;That is hidden partitioning and partition evolution — one indirection, used twice.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/partitioning/&quot;&gt;Iceberg partitioning&lt;/a&gt; for the transforms and hidden partitioning&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/evolution/#partition-evolution&quot;&gt;Iceberg evolution&lt;/a&gt; for how specs coexist&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergReadPath/&quot;&gt;Life of a read query in Iceberg&lt;/a&gt; for where partition pruning sits among the four prunes&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/IcebergSchemaEvolution/&quot;&gt;Schema evolution in Iceberg&lt;/a&gt; for the field ids partition specs point at&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheIcebergArchitecture/&quot;&gt;Apache Iceberg architecture&lt;/a&gt; for manifests and spec ids&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Iceberg, Apache Spark, Apache Hive, Apache Parquet, Apache and the Apache
feather logo are either registered trademarks or trademarks of The Apache
Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Iceberg" /><category term="Iceberg" /><category term="Partitioning" /><category term="Pruning" /><category term="Transforms" /><summary type="html">The Hive problem it fixes How Iceberg derives partitions The transforms Partition evolution, measured Choosing the transform, with arithmetic Evolving in practice Where partitioning stops and sorting starts What evolution does not do Choosing a layout Common misconceptions A model worth keeping References Trademarks TL;DR Partition values are derived from a column by a transform, so a query filters on event_ts and Iceberg prunes event_ts_day for it. Measured: a plain timestamp predicate matched 3,000 rows in one day’s partition. Changing the layout does not rewrite data. After REPLACE PARTITION FIELD months(ts) WITH days(ts), files carried spec_id values of [1, 0, 0] — the old ones untouched. Both specs stay in metadata (spec-id 0 and 1) and both reference the same source-id, so one predicate prunes across both layouts. Directory names like ts_month=2026-01 and ts_day=2026-03-09 coexist in the same table, and all 900 rows stayed queryable. Hidden partitioning removes the Hive failure where forgetting the partition filter scans everything silently. The Hive problem it fixes On a Hive table, a partition is a physical column in the path: /trips/dt=2026-01-06/part-0000.parquet To get pruning, a query must filter on dt. Filtering on the actual event timestamp does not prune, because dt is a separate column the user is responsible for keeping consistent. Two failures follow, both routine. Someone writes WHERE event_ts &amp;gt;= &apos;2026-01-06&apos; and scans the whole table, because the query is correct and simply did not mention dt. And someone writes a row whose dt disagrees with its event_ts, producing data that is invisible to correct queries. How Iceberg derives partitions You declare a transform on a real column: CREATE TABLE local.db.events ( id BIGINT, event_ts TIMESTAMP, region STRING, amount DOUBLE) USING iceberg PARTITIONED BY (days(event_ts)); There is no dt column. Iceberg computes the day from event_ts at write time and records it in the manifests. The spec, read from the metadata file: partition-specs = [{&quot;spec-id&quot;: 0, &quot;fields&quot;: [ {&quot;name&quot;: &quot;event_ts_day&quot;, &quot;transform&quot;: &quot;day&quot;, &quot;source-id&quot;: 2, &quot;field-id&quot;: 1000}]}] source-id: 2 is the field id of event_ts. The partition is defined as a function of that column, which is the indirection everything else depends on. At read time the query names the real column: SELECT count(*) FROM local.db.events WHERE event_ts &amp;gt;= timestamp&apos;2026-01-06 00:00:00&apos; AND event_ts &amp;lt; timestamp&apos;2026-01-07 00:00:00&apos; partition directories on disk: [&apos;event_ts_day=2026-01-05&apos;, &apos;event_ts_day=2026-01-06&apos;, &apos;event_ts_day=2026-01-07&apos;] rows matched = 3000 Iceberg applies day() to the predicate and matches it against partition values. Nobody has to know the layout, and nobody can forget to filter on it. The transforms Transform Use years, months, days, hours time columns, at the granularity queries filter on bucket(N, col) high-cardinality keys; spreads evenly into N buckets truncate(N, col) prefixes of strings, ranges of numbers identity(col) the value itself — the Hive-style behaviour bucket is the one people under-use. Partitioning by identity(customer_id) on a million customers produces a million partitions and a small-file disaster; bucket(64, customer_id) gives 64 balanced partitions that still prune equality filters. Partition evolution, measured Monthly partitioning stops working when data grows. On Hive that means rewriting the table. On Iceberg: ALTER TABLE local.db.pe REPLACE PARTITION FIELD months(ts) WITH days(ts); Then one more append. The result: dirs after months(): [&apos;ts_month=2026-01&apos;, &apos;ts_month=2026-02&apos;] dirs after days() : [&apos;ts_day=2026-03-09&apos;, &apos;ts_month=2026-01&apos;, &apos;ts_month=2026-02&apos;] partition-specs: [{&quot;spec-id&quot;: 0, ... &quot;transform&quot;: &quot;month&quot;, &quot;source-id&quot;: 2, &quot;field-id&quot;: 1000}, {&quot;spec-id&quot;: 1, ... &quot;transform&quot;: &quot;day&quot;, &quot;source-id&quot;: 2, &quot;field-id&quot;: 1001}] default-spec-id: 1 spec_id per data file: [1, 0, 0] rows total: 900 Two of the three data files still carry spec_id = 0. They were not rewritten, moved or touched. The new file uses spec 1. Both directory layouts sit in the same table, and all 900 rows are queryable through one name. This works because a manifest records which spec each file was written under. A reader consults the right spec per file rather than assuming one layout for the table. Note both specs reference source-id: 2. Because both derive from the same column, a single predicate on ts prunes files under either spec — the reader applies month() to old files and day() to new ones. The new field also got a new field-id (1001, not 1000), which keeps old partition values unambiguous. Choosing the transform, with arithmetic The transform decides how many partitions exist, and partition count is the number that goes wrong. -- how many partitions does the current layout actually produce? SELECT count(*) AS partitions, avg(file_count) AS avg_files, avg(record_count) AS avg_rows FROM local.db.events.partitions; Aim for partitions holding hundreds of megabytes to a few gigabytes. Work backwards from volume: a table taking 50 GB a month partitioned by days gives ~1.6 GB per partition, which is comfortable. The same table partitioned by hours gives ~70 MB per partition and 720 partitions a month, which is the beginning of a small-file problem. bucket is the transform for high-cardinality keys, and the bucket count is a capacity decision: CREATE TABLE local.db.orders (order_id BIGINT, customer_id BIGINT, amount DOUBLE) USING iceberg PARTITIONED BY (bucket(64, customer_id)); Sixty-four balanced partitions, regardless of how many customers exist. An equality filter on customer_id prunes to one bucket; a range filter prunes nothing, because hashing destroys ordering. Use bucket for keys you look up and truncate or a time transform for keys you scan ranges of. Multiple fields multiply: PARTITIONED BY (days(event_ts), bucket(16, customer_id)) -- 30 x 16 = 480 per month That is usually one field too many. Each additional field divides the data further, and the small-file cost arrives faster than the pruning benefit. Evolving in practice -- add a field: new data gains the finer layout, old data keeps its own ALTER TABLE local.db.events ADD PARTITION FIELD hours(event_ts); -- replace one: the usual month-to-day move ALTER TABLE local.db.events REPLACE PARTITION FIELD months(event_ts) WITH days(event_ts); -- drop one entirely ALTER TABLE local.db.events DROP PARTITION FIELD bucket(16, customer_id); All three are metadata-only, and all three leave existing files alone. To check what you have afterwards: SELECT spec_id, count(*) AS files, sum(record_count) AS records FROM local.db.events.files GROUP BY spec_id ORDER BY spec_id; A table with files spread across three or four specs is not broken, but it is harder to reason about, and planning must consult each spec. When old data is queried often enough to matter, rewrite it into the current layout: CALL local.system.rewrite_data_files( table =&amp;gt; &apos;db.events&apos;, where =&amp;gt; &apos;event_ts &amp;lt; date&apos;2026-03-01&apos;&apos;); That is a data-moving job, unlike the ALTER, and it is the step that actually gives historical data the new pruning. Where partitioning stops and sorting starts Partitioning eliminates whole directories of files. Sorting narrows the column statistics inside them so individual files can be skipped. They solve adjacent problems and people reach for the wrong one regularly. Filter shape What helps event_ts in a narrow range a time partition transform customer_id = ? on a huge key space bucket partitioning region = ? with few regions sort order, not partitioning amount &amp;gt; ? sort order several unrelated columns sort by the most selective, accept the rest Partitioning on a low-cardinality column like region is usually a mistake: four regions means four partitions, which does nothing for parallelism and produces skew when one region dominates. A sort order handles it without the file-count cost: ALTER TABLE local.db.events WRITE ORDERED BY region, event_ts; What evolution does not do It does not reorganise history. Old data keeps the old granularity. If queries need day-level pruning over historical data, that is a rewrite — rewrite_data_files with the new spec — and it is a data-movement job, distinct from the metadata-only ALTER. It does not fix bad partitioning retroactively. Over-partitioned history stays over-partitioned until rewritten. It does not reduce the need to choose well. Evolution is an escape hatch, not a reason to skip the decision. Choosing a layout Partition on what you filter on, at the granularity you filter at. Hourly partitions for queries that always span weeks produce thousands of tiny files and prune nothing useful. Target partitions that hold real data. Hundreds of megabytes to a few gigabytes is a reasonable band. A partition per minute holding ten rows is overhead with a directory name. Use bucket for high-cardinality keys rather than identity. Prefer fewer partition fields. Each one multiplies the partition count, and two fields with a hundred values each is ten thousand partitions. Remember that pruning has a second stage: column bounds inside manifests skip files within a partition, but only if data is sorted. Partitioning and sort order work together, and the read path post covers where each applies. Common misconceptions “Hidden partitioning means no partitions.” The partitions are real and visible on disk. What is hidden is the need to name them in queries. “Partition evolution rewrites the table.” Measured: two of three files kept spec_id = 0 and nothing moved. “I must query the partition column.” There is no partition column to query. Filter the real column. “Old data becomes unqueryable after evolution.” All 900 rows stayed readable across both specs. “More partitions prune better.” More partitions mean more metadata and smaller files. Pruning comes from matching the transform to the filter. A model worth keeping A partition is a function of a column, recorded per file. Queries filter the column; Iceberg applies the function. Change the function and old files keep their old one, because each file remembers which spec produced it. That is hidden partitioning and partition evolution — one indirection, used twice. References Iceberg partitioning for the transforms and hidden partitioning Iceberg evolution for how specs coexist Life of a read query in Iceberg for where partition pruning sits among the four prunes Schema evolution in Iceberg for the field ids partition specs point at Apache Iceberg architecture for manifests and spec ids Trademarks Apache Iceberg, Apache Spark, Apache Hive, Apache Parquet, Apache and the Apache feather logo are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry></feed>