<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="3.9.2">Jekyll</generator><link href="https://rangareddy.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://rangareddy.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-09-19T09:01:06+05:30</updated><id>https://rangareddy.github.io/feed.xml</id><title type="html">Ranga Reddy</title><subtitle>Hands-on notes, production troubleshooting and interactive tools for Apache Hudi, Iceberg, Spark, Kafka and the lakehouse stack underneath them.</subtitle><author><name>Ranga Reddy</name></author><entry><title type="html">Every Apache Spark release, and the problem each one was built to solve</title><link href="https://rangareddy.github.io/SparkReleaseHistory/" rel="alternate" type="text/html" title="Every Apache Spark release, and the problem each one was built to solve" /><published>2026-09-19T08:00:00+05:30</published><updated>2026-09-19T08:00:00+05:30</updated><id>https://rangareddy.github.io/SparkReleaseHistory</id><content type="html" xml:base="https://rangareddy.github.io/SparkReleaseHistory/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-the-four-eras-and-what-moved-between-them&quot; id=&quot;markdown-toc-architecture-the-four-eras-and-what-moved-between-them&quot;&gt;Architecture: the four eras, and what moved between them&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-1x-line-structure-arrives&quot; id=&quot;markdown-toc-the-1x-line-structure-arrives&quot;&gt;The 1.x line: structure arrives&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-10-may-2014&quot; id=&quot;markdown-toc-spark-10-may-2014&quot;&gt;Spark 1.0, May 2014&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-11-september-2014&quot; id=&quot;markdown-toc-spark-11-september-2014&quot;&gt;Spark 1.1, September 2014&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-12-december-2014&quot; id=&quot;markdown-toc-spark-12-december-2014&quot;&gt;Spark 1.2, December 2014&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-13-march-2015&quot; id=&quot;markdown-toc-spark-13-march-2015&quot;&gt;Spark 1.3, March 2015&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-14-june-2015&quot; id=&quot;markdown-toc-spark-14-june-2015&quot;&gt;Spark 1.4, June 2015&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-15-september-2015&quot; id=&quot;markdown-toc-spark-15-september-2015&quot;&gt;Spark 1.5, September 2015&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-16-january-2016&quot; id=&quot;markdown-toc-spark-16-january-2016&quot;&gt;Spark 1.6, January 2016&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-2x-line-unification-and-codegen&quot; id=&quot;markdown-toc-the-2x-line-unification-and-codegen&quot;&gt;The 2.x line: unification and codegen&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-20-july-2016&quot; id=&quot;markdown-toc-spark-20-july-2016&quot;&gt;Spark 2.0, July 2016&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-21-december-2016&quot; id=&quot;markdown-toc-spark-21-december-2016&quot;&gt;Spark 2.1, December 2016&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-22-july-2017&quot; id=&quot;markdown-toc-spark-22-july-2017&quot;&gt;Spark 2.2, July 2017&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-23-february-2018&quot; id=&quot;markdown-toc-spark-23-february-2018&quot;&gt;Spark 2.3, February 2018&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-24-november-2018&quot; id=&quot;markdown-toc-spark-24-november-2018&quot;&gt;Spark 2.4, November 2018&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-3x-line-the-planner-stops-guessing&quot; id=&quot;markdown-toc-the-3x-line-the-planner-stops-guessing&quot;&gt;The 3.x line: the planner stops guessing&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-30-june-2020&quot; id=&quot;markdown-toc-spark-30-june-2020&quot;&gt;Spark 3.0, June 2020&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-31-march-2021&quot; id=&quot;markdown-toc-spark-31-march-2021&quot;&gt;Spark 3.1, March 2021&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-32-october-2021&quot; id=&quot;markdown-toc-spark-32-october-2021&quot;&gt;Spark 3.2, October 2021&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-33-june-2022&quot; id=&quot;markdown-toc-spark-33-june-2022&quot;&gt;Spark 3.3, June 2022&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-34-april-2023&quot; id=&quot;markdown-toc-spark-34-april-2023&quot;&gt;Spark 3.4, April 2023&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-35-september-2023&quot; id=&quot;markdown-toc-spark-35-september-2023&quot;&gt;Spark 3.5, September 2023&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-4x-line-sql-becomes-the-surface-again&quot; id=&quot;markdown-toc-the-4x-line-sql-becomes-the-surface-again&quot;&gt;The 4.x line: SQL becomes the surface again&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-40-may-2025&quot; id=&quot;markdown-toc-spark-40-may-2025&quot;&gt;Spark 4.0, May 2025&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-41-december-2025&quot; id=&quot;markdown-toc-spark-41-december-2025&quot;&gt;Spark 4.1, December 2025&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#spark-42-july-2026&quot; id=&quot;markdown-toc-spark-42-july-2026&quot;&gt;Spark 4.2, July 2026&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#defaults-that-changed-underneath-you&quot; id=&quot;markdown-toc-defaults-that-changed-underneath-you&quot;&gt;Defaults that changed underneath you&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-was-removed-and-when&quot; id=&quot;markdown-toc-what-was-removed-and-when&quot;&gt;What was removed, and when&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing-where-to-sit&quot; id=&quot;markdown-toc-choosing-where-to-sit&quot;&gt;Choosing where to sit&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-mental-model&quot; id=&quot;markdown-toc-the-mental-model&quot;&gt;The mental model&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Spark’s centre of gravity has moved four times: from the &lt;strong&gt;RDD&lt;/strong&gt;, to the &lt;strong&gt;DataFrame&lt;/strong&gt;, to the &lt;strong&gt;query plan&lt;/strong&gt;, to a &lt;strong&gt;protocol&lt;/strong&gt; between client and driver. Every release makes more sense once you know which of those four eras it belongs to.&lt;/li&gt;
    &lt;li&gt;The biggest upgrades are rarely the headline features. They are the &lt;strong&gt;default changes&lt;/strong&gt;: sort shuffle, a 10 MB broadcast threshold, adaptive execution, bloom-filter joins, and ANSI mode all flipped on in different releases and silently changed what your existing jobs do.&lt;/li&gt;
    &lt;li&gt;Some 1.x defaults have never moved. The broadcast-join threshold set in 1.2 is still &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10485760b&lt;/code&gt;, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; is still &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt;, both read from a 4.1.3 session.&lt;/li&gt;
    &lt;li&gt;A single &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain&lt;/code&gt; output on a current cluster shows three eras at once: whole-stage codegen markers from 2.0, the adaptive wrapper from 3.0, and coalescing from 3.2. Adaptive execution itself is older than any of them, first shipping in 1.6.&lt;/li&gt;
    &lt;li&gt;The newest release ships a SQL surface ahead of its engine in places. The change-data-capture clause parses and analyses, then stops at the catalog, because the built-in one does not implement it.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;You have inherited a Spark job. It is pinned to some version because that is
what the platform team installed in 2021, and someone is now asking whether
moving it forward is worth a quarter of engineering time. The release notes are
no help: each one is a list of between 1,100 and 5,100 resolved tickets, sorted
by component, with no indication of which three entries will change your job’s
behaviour and which 3,000 you can ignore.&lt;/p&gt;

&lt;p&gt;That is the question this post answers. Not “what is in each release”, which the
project already documents, but which changes altered the execution model, which
changed a default under you, and which deleted something you might still be
using.&lt;/p&gt;

&lt;p&gt;Everything below was checked against a running cluster rather than recalled. The
version under test is 4.1.3 unless the text says otherwise, with a second
cluster on 4.2.0 for the newest features. Five claims I was confident about
turned out to be wrong when I ran them, and I have kept those corrections in the
text where they are useful.&lt;/p&gt;

&lt;h2 id=&quot;architecture-the-four-eras-and-what-moved-between-them&quot;&gt;Architecture: the four eras, and what moved between them&lt;/h2&gt;

&lt;p&gt;Spark has had one durable architectural habit: each era moves the thing you
program against one level further from the machine, and one level closer to a
description of intent.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  E1[&quot;&amp;lt;b&amp;gt;1.x&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the RDD era&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;you describe&amp;lt;br/&amp;gt;the computation&amp;lt;/i&amp;gt;&quot;]
  E2[&quot;&amp;lt;b&amp;gt;2.x&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the DataFrame era&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;you describe&amp;lt;br/&amp;gt;the data&amp;lt;/i&amp;gt;&quot;]
  E3[&quot;&amp;lt;b&amp;gt;3.x&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the plan era&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;the engine revises&amp;lt;br/&amp;gt;its own decisions&amp;lt;/i&amp;gt;&quot;]
  E4[&quot;&amp;lt;b&amp;gt;4.x&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the protocol era&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;the client is&amp;lt;br/&amp;gt;detached from the driver&amp;lt;/i&amp;gt;&quot;]
  E1 --&amp;gt; E2 --&amp;gt; E3 --&amp;gt; E4
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Each move buys something and costs something:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Era&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What you program against&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What it buys&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What it costs&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1.x&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;RDD of JVM objects&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Total control, any type&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The engine cannot see inside your closure, so it cannot optimise it&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2.x&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;DataFrame over a schema&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Codegen, columnar scans, one API for batch and stream&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Your lambda becomes an expression tree, and UDFs become the slow path&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3.x&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The same DataFrame, re-planned at runtime&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Decisions made on measurements, not estimates&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Plans you read before execution are no longer the plans that ran&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.x&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;A plan sent over a wire&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Thin clients, many languages, isolation from the driver JVM&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Two API surfaces to support, and features arrive on them at different times&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The version-by-version sections follow that arc. If you only read one section,
read &lt;a href=&quot;#defaults-that-changed-underneath-you&quot;&gt;Defaults that changed underneath you&lt;/a&gt;,
because default changes cause more upgrade surprises than features do.&lt;/p&gt;

&lt;h2 id=&quot;the-1x-line-structure-arrives&quot;&gt;The 1.x line: structure arrives&lt;/h2&gt;

&lt;p&gt;The first line’s job was to turn a fast research engine into something an
operations team would accept, and then to discover that the RDD was the wrong
abstraction for most of the work people were doing with it.&lt;/p&gt;

&lt;h3 id=&quot;spark-10-may-2014&quot;&gt;Spark 1.0, May 2014&lt;/h3&gt;

&lt;p&gt;The release that made Spark deployable rather than merely usable.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Why it mattered&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Packaging&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-submit&lt;/code&gt;, one submission path for local, Mesos and YARN&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Before this, each cluster manager had its own launch story&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Operations&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;History server for the web UI&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Post-mortem debugging of finished applications&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Security&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Hadoop and YARN security model, credential transfer&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The blocker for regulated Hadoop shops&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark SQL as an alpha component, with Catalyst behind it&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The first appearance of the optimizer that everything later depends on&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;API&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Stability guarantee for the whole 1.x line&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Applications could be written against a moving project&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Spark SQL arrived with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SchemaRDD&lt;/code&gt;, not &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataFrame&lt;/code&gt;. The name survived exactly
three releases. Catalyst, introduced in the same release to choose execution
plans and push predicates into Parquet, is still the optimizer in 4.2 and is
covered in depth in
&lt;a href=&quot;/CatalystOptimizer/&quot;&gt;Inside Spark’s Catalyst optimizer&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;spark-11-september-2014&quot;&gt;Spark 1.1, September 2014&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-based shuffle, available but not yet default&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;JDBC/ODBC server, JSON source with schema inference, dynamic bytecode generation for expressions&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;UDF registration from Python, Scala and Java&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Disk spilling for skewed blocks during cache operations&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Two defaults changed here. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.io.compression.codec&lt;/code&gt; became &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snappy&lt;/code&gt;, though
it has moved on since: the default in the 4.1.3 build I ran is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;lz4&lt;/code&gt;. And
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.broadcast.factory&lt;/code&gt; became &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TorrentBroadcastFactory&lt;/code&gt;, which is why
broadcasting a large variable no longer saturates the driver’s network link.&lt;/p&gt;

&lt;p&gt;The dynamic bytecode generation in this release is the seed of what becomes
whole-stage codegen in 2.0. It compiled expressions; 2.0 compiled whole
operator chains.&lt;/p&gt;

&lt;h3 id=&quot;spark-12-december-2014&quot;&gt;Spark 1.2, December 2014&lt;/h3&gt;

&lt;p&gt;The release that changed the most defaults in the shortest note.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.manager&lt;/code&gt; default changed from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hash&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sort&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Network&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.blockTransferService&lt;/code&gt; default changed from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;nio&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;netty&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scaling&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Elastic scaling (dynamic allocation) on YARN&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;External data source API, the ancestor of every connector you use&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Write-ahead log for driver high availability, and a Python API&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;GraphX&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Graduated from alpha&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The hash-to-sort shuffle change is the single most consequential default flip in
Spark’s history. Hash shuffle opened one file per reduce partition per map task;
at a few thousand partitions on each side that is millions of open files. Sort
shuffle writes one file plus an index per map task. Everything about shuffle
tuning after 1.2 assumes the sort writer, which is described in
&lt;a href=&quot;/ApacheSparkArchitecture/&quot;&gt;Apache Spark architecture&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;This release also set &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10485760&lt;/code&gt;,
raising it from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10000&lt;/code&gt;. That value has not changed since. On the 4.1.3 cluster
I ran for this post:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;defaults&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# 10485760b
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# 200
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Eleven years and three major versions later, the broadcast threshold a 2014
release chose is still deciding whether your join shuffles. That is worth
knowing before you conclude that a join strategy was picked by something
sophisticated.&lt;/p&gt;

&lt;h3 id=&quot;spark-13-march-2015&quot;&gt;Spark 1.3, March 2015&lt;/h3&gt;

&lt;p&gt;The release where Spark stopped being an RDD engine.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;API&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataFrame&lt;/code&gt; API, in Python, Scala and Java&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SchemaRDD&lt;/code&gt; renamed to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataFrame&lt;/code&gt;, Spark SQL graduated from alpha&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;JDBC data source for MySQL, Postgres and other relational databases&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Parquet schema merging for compatible schemas&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Direct Kafka API, exactly-once without a write-ahead log&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The rename looks cosmetic and was not. A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SchemaRDD&lt;/code&gt; was an RDD that happened to
carry a schema. A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataFrame&lt;/code&gt; is a description of a computation over named,
typed columns, which the engine is free to execute however it likes. That
freedom is what every later optimisation spends.&lt;/p&gt;

&lt;p&gt;The direct Kafka API deserves its own note. The receiver-based approach ran a
long-lived receiver task that wrote to a write-ahead log for durability. The
direct approach treats Kafka offsets as the checkpoint, so the log becomes
unnecessary and delivery becomes exactly-once instead of at-least-once. This is
the same reasoning Structured Streaming later generalises to every source.&lt;/p&gt;

&lt;p&gt;This is the oldest API in the post that still runs unchanged today:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;df13&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;orders&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;pune&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1200&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;hyderabad&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;800&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;pune&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;450&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;order_id INT, city STRING, amount INT&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;orders&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orderBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +---------+-----------+
# |     city|sum(amount)|
# +---------+-----------+
# |hyderabad|        800|
# |     pune|       1650|
# +---------+-----------+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h3 id=&quot;spark-14-june-2015&quot;&gt;Spark 1.4, June 2015&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;R&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SparkR, an R binding built on the DataFrame API&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Window functions in SQL and DataFrames&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-1442&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge joins for very large joins&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-2213&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;ORCFile support&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-2883&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;First performance work under the name Project Tungsten&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-7081&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;DAG visualisation in the UI&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-6942&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python 3 support&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-4897&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;REST API for application information&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-3644&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Sort-merge join is the important entry. Until it existed, a shuffle join that
did not fit in memory failed. After it, join size is bounded by disk instead of
memory. The trade-off is explicit: sort-merge join pays a sort on both sides to
remove the memory ceiling. Spark still makes exactly this choice today, and the
decision procedure is walked through in
&lt;a href=&quot;/SparkJoinsInDepth/&quot;&gt;Spark joins in depth&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;spark-15-september-2015&quot;&gt;Spark 1.5, September 2015&lt;/h3&gt;

&lt;p&gt;Tungsten’s main release. The theme is that the JVM’s object model is the
bottleneck, so Spark should stop using it for data.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Code generation on by default for almost all DataFrame and SQL functions&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Cache-friendly in-memory hash map layout for aggregation&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Fallback to external sort-based aggregation when memory is exhausted&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Compact binary in-memory representation, execution memory accounted for explicitly rather than left to the garbage collector&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge join preferred over hash join for shuffle joins&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Around 100 new built-in functions, a UDAF interface (SPARK-3947), and a broadcast hint (SPARK-8300)&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;“Execution memory is explicitly accounted for, without relying on JVM GC” is the
sentence that matters. Before Tungsten, a Spark executor’s memory behaviour was
the JVM’s behaviour, and tuning meant tuning a garbage collector. After it,
Spark tracks its own execution memory and spills deliberately. The memory model
that grows out of this is covered in
&lt;a href=&quot;/SparkMemoryManagement/&quot;&gt;Spark memory management&lt;/a&gt;.&lt;/p&gt;

&lt;h3 id=&quot;spark-16-january-2016&quot;&gt;Spark 1.6, January 2016&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;API&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Dataset&lt;/code&gt; API, typed objects with the SQL engine underneath&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-9999&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Memory&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Unified memory management, one pool shared by execution and storage&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-10000&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Memory&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Query execution using off-heap memory&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-11389&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;First adaptive query execution, choosing reducer counts automatically&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-9858&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapWithState&lt;/code&gt;, replacing &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;updateStateByKey&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-2629&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Per-operator metrics for SQL execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-10412&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Two things are worth flagging.&lt;/p&gt;

&lt;p&gt;Unified memory management (SPARK-10000) replaced a fixed split between execution
and cache memory with one pool and a borrowing rule. The eviction is
deliberately asymmetric: execution can evict cached blocks, storage can never
evict execution. A query that needs memory to finish beats a cache that is only
an optimisation.&lt;/p&gt;

&lt;p&gt;Adaptive query execution (SPARK-9858) is dated 1.6, not 3.0. The 1.6 version
only picked the number of reducers for joins and aggregations. It was rebuilt
from scratch for 3.0 and enabled by default in 3.2. Anyone who says AQE is new
in 3.0 is describing the rewrite, not the idea.&lt;/p&gt;

&lt;h2 id=&quot;the-2x-line-unification-and-codegen&quot;&gt;The 2.x line: unification and codegen&lt;/h2&gt;

&lt;p&gt;If 1.x discovered that the DataFrame was the right abstraction, 2.x committed to
it: one entry point, one type, one execution strategy, and streaming redefined
as a query over an unbounded table.&lt;/p&gt;

&lt;h3 id=&quot;spark-20-july-2016&quot;&gt;Spark 2.0, July 2016&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;API&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt; replaces &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SQLContext&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HiveContext&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;API&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataFrame&lt;/code&gt; becomes a type alias for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Dataset[Row]&lt;/code&gt; in Scala and Java&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL 2003 support, all 99 TPC-DS queries runnable&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Native SQL parser, native DDL, correlated and uncorrelated subqueries&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Whole-stage code generation&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Vectorized Parquet reads, off-heap memory for cache and execution&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Native CSV source, Hive-style bucketing&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Structured Streaming, as an experimental API&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Whole-stage codegen is the defining change. Before it, each operator was a
separate object pulling rows from the next through an iterator, which costs a
virtual call per row per operator. After it, Spark compiles an entire chain of
operators into one Java method with the loop fused, which the JIT can then treat
like hand-written code. The release notes claim speedups of two to ten times for
common operators.&lt;/p&gt;

&lt;p&gt;You can see it in any plan today. The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*&lt;/code&gt; markers and the number after them are
codegen stage identifiers:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;codegen&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1_000_000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;selectExpr&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;id % 7 AS bucket&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;bucket&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;On 4.1.3 that prints:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AdaptiveSparkPlan isFinalPlan=true
+- == Final Plan ==
   ResultQueryStage 1
   +- *(2) HashAggregate(keys=[bucket#1L], functions=[count(1)], output=[bucket#1L, count#2L])
      +- AQEShuffleRead coalesced
         +- ShuffleQueryStage 0
            +- Exchange hashpartitioning(bucket#1L, 200), ENSURE_REQUIREMENTS, [plan_id=25]
               +- *(1) HashAggregate(keys=[bucket#1L], functions=[partial_count(1)], output=[bucket#1L, count#7L])
                  +- *(1) Project [(id#0L % 7) AS bucket#1L]
                     +- *(1) Range (0, 1000000, step=1, splits=11)
+- == Initial Plan ==
   HashAggregate(keys=[bucket#1L], functions=[count(1)], output=[bucket#1L, count#2L])
   +- Exchange hashpartitioning(bucket#1L, 200), ENSURE_REQUIREMENTS, [plan_id=15]
      +- HashAggregate(keys=[bucket#1L], functions=[partial_count(1)], output=[bucket#1L, count#7L])
         +- Project [(id#0L % 7) AS bucket#1L]
            +- Range (0, 1000000, step=1, splits=11)
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Three eras are visible in that one output. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*(1)&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*(2)&lt;/code&gt; are 2.0’s
whole-stage codegen: the range scan, the projection and the partial aggregate
were compiled into a single generated method, and the final aggregate into
another. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AdaptiveSparkPlan&lt;/code&gt; with its &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Initial Plan&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Final Plan&lt;/code&gt; sections
is 3.0. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AQEShuffleRead coalesced&lt;/code&gt; is 3.2 deciding at runtime that 200 shuffle
partitions were too many for seven groups and reading them as fewer.&lt;/p&gt;

&lt;p&gt;Note also what did not happen: the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt; still says &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt;, because
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; is still the 1.x default. AQE coalesced after the
fact rather than planning a better number in the first place.&lt;/p&gt;

&lt;h3 id=&quot;spark-21-december-2016&quot;&gt;Spark 2.1, December 2016&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Event-time watermarks&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-18124&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Kafka 0.10 support in Structured Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-17346&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Stable format for the offset log&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-17829&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;from_json&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;to_json&lt;/code&gt; for string columns&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-18351&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scalable partition handling, partition metadata in the metastore&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-17861&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Watermarks are the release’s real content. A streaming aggregation over event
time has to keep state for every open window, and without a rule for closing
them that state grows without bound. A watermark is that rule: a declaration
that data more than a stated interval behind the maximum seen event time will
not be waited for. It buys bounded state at the cost of dropping genuinely late
records, and you choose where on that trade-off to sit.&lt;/p&gt;

&lt;h3 id=&quot;spark-22-july-2017&quot;&gt;Spark 2.2, July 2017&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Structured Streaming APIs declared generally available&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-20844&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Cost-based optimizer: cardinality estimation for filter, join, aggregate, project and limit&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-17075 and related&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Cost-based join reordering&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-17080&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast hints in SQL: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCASTJOIN&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MAPJOIN&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-16475&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Session-local time zone&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-18350&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Blacklist mechanism for task scheduling&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-8425&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Build&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Java 7 support removed&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-19493&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Packaging&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pip install pyspark&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The cost-based optimizer is the one to be realistic about. It requires
statistics you have to compute yourself with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ANALYZE TABLE&lt;/code&gt;, and without them
the rule-based path does the work. Nine years later that is still true, and the
&lt;a href=&quot;/CatalystOptimizer/&quot;&gt;Catalyst post&lt;/a&gt;
shows a join reordering where the rule-based &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReorderJoin&lt;/code&gt; fired and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CostBasedJoinReorder&lt;/code&gt; ran and changed nothing.&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pip install pyspark&lt;/code&gt; belongs in the same list as the engine features. It is the
moment PySpark stopped requiring a cluster distribution to try.&lt;/p&gt;

&lt;h3 id=&quot;spark-23-february-2018&quot;&gt;Spark 2.3, February 2018&lt;/h3&gt;

&lt;p&gt;A dense release.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Deployment&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Kubernetes scheduler backend, experimental&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-18278&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Vectorized ORC reader, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.orc.impl=native&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-16060&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Data Source API V2, experimental&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-15689&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Continuous processing, sub-millisecond end-to-end latency&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Stream-to-stream joins&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Pandas UDFs, vectorized execution over Arrow&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-22216&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Histogram support in the cost-based optimizer&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-21975&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Fixed a long-standing correctness bug where shuffle plus repartition could produce wrong answers&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-23207&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Build&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala 2.10 support removed&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-19810&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Two entries deserve emphasis for opposite reasons.&lt;/p&gt;

&lt;p&gt;Pandas UDFs (SPARK-22216) changed the economics of Python on Spark. A plain
Python UDF serialises one row at a time to a Python worker and back. A pandas
UDF ships an Arrow batch, runs vectorized pandas code over it, and ships a batch
back. Same language, different order of magnitude of overhead. Everything Spark
has done for Python since, through to the Arrow UDFs of 4.1, follows this line.&lt;/p&gt;

&lt;p&gt;SPARK-23207 is a correctness fix, not a feature: certain combinations of shuffle
and repartition could return wrong results. The DataFrame case was fixed here in
2.3; the RDD case followed in 2.4 as SPARK-23243. If you are on anything older
and you repartition after a shuffle, that is your reason to move, and it
outranks every feature in this post.&lt;/p&gt;

&lt;h3 id=&quot;spark-24-november-2018&quot;&gt;Spark 2.4, November 2018&lt;/h3&gt;

&lt;p&gt;The last 2.x feature release, and the long-term home of an enormous amount of
production Spark.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scheduling&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Barrier execution mode, for deep learning frameworks&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-24374&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Higher-order functions over arrays and maps&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-23899&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Built-in Avro data source&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-24768&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Native ORC reader on by default&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-23456&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;EXCEPT ALL&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INTERSECT ALL&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-21274&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PIVOT&lt;/code&gt; syntax&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-24035&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Nested schema pruning for Parquet&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-4502&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Bucket pruning&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-23803&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Blocks larger than 2 GB can be replicated and sent&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-24296, SPARK-24307&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Build&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala 2.12 support, experimental&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-14220&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Higher-order functions are the feature most 2.4 users never adopted and should
have. Before them, transforming an array column meant either exploding and
regrouping, which is a shuffle, or writing a UDF, which leaves the engine.
After them it is an expression the optimizer understands:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hof&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT transform(array(1, 2, 3), x -&amp;gt; x * 2) AS doubled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +---------+
# |  doubled|
# +---------+
# |[2, 4, 6]|
# +---------+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The 2 GB block limit (SPARK-24296) is the other entry worth knowing, because it
explains a class of failure that simply stops happening after 2.4. Spark’s
network layer used a single &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ByteBuffer&lt;/code&gt; per block, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ByteBuffer&lt;/code&gt; is indexed
by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;int&lt;/code&gt;. Any shuffle block or cached partition over 2 GB failed with an
overflow. The fix is unglamorous and removes a hard ceiling that used to dictate
partition counts.&lt;/p&gt;

&lt;h2 id=&quot;the-3x-line-the-planner-stops-guessing&quot;&gt;The 3.x line: the planner stops guessing&lt;/h2&gt;

&lt;p&gt;The 2.x engine made one plan from estimates and executed it. The estimates were
frequently wrong, because nobody knows the selectivity of a filter over data
nobody has read yet. The 3.x line’s theme is letting the engine revise itself,
and separately, admitting that Python is the primary language.&lt;/p&gt;

&lt;p&gt;Note the gap: 2.4.0 shipped in November 2018 and 3.0.0 in June 2020, nineteen
months later. Major versions are where the project spends its compatibility
budget, and it does not spend it often.&lt;/p&gt;

&lt;h3 id=&quot;spark-30-june-2020&quot;&gt;Spark 3.0, June 2020&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Adaptive query execution, rebuilt&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-31412&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Dynamic partition pruning&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-11150&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scheduling&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Accelerator-aware scheduling, for GPUs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-24615&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Pandas UDF API redesigned around Python type hints&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-28264&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Catalog plugin API, the basis of every modern table format connector&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-31121&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Proleptic Gregorian calendar&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-26651&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;A dedicated Structured Streaming UI&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-29543&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Build&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Java 11 and Hadoop 3 support&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-24417, SPARK-23534&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Adaptive query execution re-plans after each shuffle stage completes, using the
measured sizes of what was just written. It coalesces partitions that turned out
small, splits ones that turned out skewed, converts a sort-merge join to a
broadcast when the build side turns out to fit, and reads shuffle data locally
when the join no longer needs it distributed. It is off by default in this
release. What it does and does not revisit is the subject of
&lt;a href=&quot;/SparkAQE/&quot;&gt;Adaptive query execution&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;Dynamic partition pruning solves a different problem. A star-schema query
filtering a small dimension table cannot prune fact-table partitions at plan
time, because the filter is on the other side of a join. DPP plans a subquery
that computes the surviving dimension keys first, then uses them to prune the
fact scan. A common query shape goes from reading every partition to reading a
handful.&lt;/p&gt;

&lt;p&gt;The calendar change (SPARK-26651) is the sleeper. Spark moved from a hybrid
Julian-Gregorian calendar to the proleptic Gregorian one. Dates before 1582 can
shift. If you store historical dates, test before upgrading rather than after.&lt;/p&gt;

&lt;p&gt;The release notes state that Spark 3.0 is roughly twice as fast as 2.4 on a
30 TB TPC-DS benchmark. That is the project’s own measurement on its own
hardware, quoted here as a claim rather than asserted as a result you will see.
Your workload is not TPC-DS.&lt;/p&gt;

&lt;h3 id=&quot;spark-31-march-2021&quot;&gt;Spark 3.1, March 2021&lt;/h3&gt;

&lt;p&gt;There is no Spark 3.1.0. The line starts at 3.1.1; the release notes for 3.1.1
describe it as the second release of the 3.x line.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Deployment&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Kubernetes support declared generally available&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-33005&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Project Zen: Python type annotations and dependency management&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CHAR&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VARCHAR&lt;/code&gt; types&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-33480&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;ANSI mode raises runtime errors instead of returning null&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-33275&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;New explicit cast rules under ANSI mode&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-33354&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffled hash join improvements, including full outer join support&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-32461&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Node decommissioning for Kubernetes and Standalone, experimental&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-20624&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Kubernetes going GA three years after its experimental introduction in 2.3 is
the clearest example of the project’s pace on deployment features.&lt;/p&gt;

&lt;p&gt;SPARK-33275 opens an argument that runs across the rest of the 3.x line and is settled in 4.0. Silently
returning null on overflow or a bad cast is convenient and hides data
corruption. ANSI mode raises instead. In 3.1 it is opt-in.&lt;/p&gt;

&lt;h3 id=&quot;spark-32-october-2021&quot;&gt;Spark 3.2, October 2021&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;pandas API on Spark, the former Koalas project, merged in&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-34849&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Adaptive query execution enabled by default&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-33679&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Push-based shuffle&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-30602&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;RocksDB state store&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-34198&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Session windows&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-10816&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;ANSI mode declared generally available&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-35030&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;ANSI &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INTERVAL&lt;/code&gt; types&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-27790&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Build&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala 2.13 support&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-34218&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;AQE on by default (SPARK-33679) is the most important line in the 3.x table. If
you upgrade from 3.1 to 3.2 with no config changes, your query plans change.
They usually improve. They do change.&lt;/p&gt;

&lt;p&gt;The RocksDB state store is the fix for the other half of the streaming state
problem. The default state store keeps state in the JVM heap, so a large
stateful stream becomes a garbage collection problem. RocksDB moves it off-heap
and onto local disk, trading some read latency for state that scales past what
the heap can hold.&lt;/p&gt;

&lt;p&gt;pandas API on Spark deserves an honest note on packaging: it needs pandas
installed on the cluster, and the error if it is missing is explicit about the
minimum version.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.pandas&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;ps&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;pandas-api&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;psdf&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;ps&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;DataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;({&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;pune&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;pune&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;hyderabad&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;20&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;30&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]})&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;psdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupby&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sort_index&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;to_dict&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# {&apos;hyderabad&apos;: 30, &apos;pune&apos;: 30}
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;On a clean &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;apache/spark&lt;/code&gt; image that fails with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;[PACKAGE_NOT_INSTALLED] Pandas &amp;gt;= 2.2.0 must be installed&lt;/code&gt;, which is the
correct error and still a surprise the first time.&lt;/p&gt;

&lt;h3 id=&quot;spark-33-june-2022&quot;&gt;Spark 3.3, June 2022&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Row-level runtime filtering, including bloom filters&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-32268&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Errors&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Error classes and structured error messages&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-38781&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Complex types in the vectorized Parquet reader&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-34863&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Hidden file metadata, the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_metadata&lt;/code&gt; column&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-37273&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Profiler for Python and pandas UDFs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-37443&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Row-level runtime filtering extends the dynamic pruning idea from partitions to
rows. Spark builds a bloom filter from the small side of a join and pushes it
into the large-side scan, so rows that cannot possibly match are discarded
before they are shuffled. It buys a smaller shuffle at the cost of building and
broadcasting the filter, which is why it is gated on size thresholds.&lt;/p&gt;

&lt;p&gt;The error class work (SPARK-38781) is the unglamorous change that improved daily
life most. Spark errors acquired stable identifiers such as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DIVIDE_BY_ZERO&lt;/code&gt;
instead of free-text messages, which means you can match on them in code and
search for them without pasting a stack trace into a search box.&lt;/p&gt;

&lt;h3 id=&quot;spark-34-april-2023&quot;&gt;Spark 3.4, April 2023&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Architecture&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark Connect, Python client&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-39375&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DEFAULT&lt;/code&gt; values for table columns&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-38334&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TIMESTAMP WITHOUT TIME ZONE&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-35662&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Lateral column alias references&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-27561&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Parameterized SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-41271&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UNPIVOT&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;melt&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-38864&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Bloom filter joins enabled by default&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-38841&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Errors&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQLSTATE codes attached to error classes&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-41994&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Memory profiler for UDFs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-40281&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Spark Connect is the architectural change of the 3.x line, even though it
arrives near the end of it. Until 3.4, a Spark application meant a driver JVM in
your process: PySpark launched one and talked to it over a local socket, and
your application’s lifetime was the driver’s lifetime. Connect replaces that
with a gRPC protocol carrying unresolved logical plans. The client becomes thin
and the driver becomes a server that many clients can share.&lt;/p&gt;

&lt;p&gt;What it costs is a second API surface. For several releases, some things worked
in a classic session and not in a Connect one, which produces confusing
symptoms; two posts here work through that in practice, for
&lt;a href=&quot;/SparkConnectIceberg/&quot;&gt;Iceberg&lt;/a&gt;
and for &lt;a href=&quot;/SparkConnectHudi/&quot;&gt;Hudi&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;Lateral column aliases are the small feature people notice immediately, because
every other SQL engine already had it:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;lca&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT 1 AS a, a + 1 AS b&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +---+---+
# |  a|  b|
# +---+---+
# |  1|  2|
# +---+---+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Before 3.4, referring to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;a&lt;/code&gt; in the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SELECT&lt;/code&gt; list that defined it was an
error, and you repeated the expression or nested a subquery.&lt;/p&gt;

&lt;h3 id=&quot;spark-35-september-2023&quot;&gt;Spark 3.5, September 2023&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Connect&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala and Go clients&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-42554, SPARK-43351&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Connect&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Structured Streaming support in Python and Scala&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-42938&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Connect&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;pandas API support on the Python client&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-42497&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Arrow-optimized Python UDFs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-40307&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python user-defined table functions&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-43798&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;ML&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PyTorch-based distributed training on Connect&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-42471&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Errors&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark errors migrated onto error classes&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-42986&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;3.5 is the long-term support release of the 3.x line in practice, and it is
where most organisations that have not moved to 4.x are sitting. Its maintenance
line is still active: 3.5.9 shipped in July 2026, after 4.2.0.&lt;/p&gt;

&lt;h2 id=&quot;the-4x-line-sql-becomes-the-surface-again&quot;&gt;The 4.x line: SQL becomes the surface again&lt;/h2&gt;

&lt;p&gt;The 4.x theme is that after a decade of enriching the DataFrame API, the project
turned back to SQL and started adding things that SQL engines have and Spark did
not: variant data, collations, user-defined functions written in SQL, procedural
scripting, and a pipeline syntax.&lt;/p&gt;

&lt;h3 id=&quot;spark-40-may-2025&quot;&gt;Spark 4.0, May 2025&lt;/h3&gt;

&lt;p&gt;The break. Read the removals first.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Removed or dropped&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala 2.12, Scala 2.13 becomes the only supported version&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-45314&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;JDK 8 and JDK 11, JDK 17 becomes the minimum&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-45315&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Mesos support&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-44442&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python 3.8&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-47993&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SparkR deprecated, not yet removed&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-49347&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;And the additions:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;ANSI SQL mode on by default&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-44444&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VARIANT&lt;/code&gt; data type for semi-structured data&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-45827&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;String collation support&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-46830&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL user-defined functions&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-46057&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Session variables&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-42849&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL pipe syntax&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-49555&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Built-in XML data source&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-44265&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python Data Source API&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Native plotting API&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Arbitrary State API v2, and a state data source for debugging&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Deployment&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark Kubernetes Operator&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-45923&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;ANSI mode by default is the change that will break jobs, and it is the right
change. On 4.1.3:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;ansi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.ansi.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# true
&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;try&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT 1/0&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;except&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;Exception&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;e&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;type&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;e&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;__name__&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;str&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;e&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;split&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\n&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# ArithmeticException [DIVIDE_BY_ZERO] Division by zero. Use `try_divide` to tolerate ...
&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT try_divide(1, 0) AS v&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +----+
# |   v|
# +----+
# |NULL|
# +----+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The same applies to casts: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CAST(&apos;abc&apos; AS INT)&lt;/code&gt; now raises &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CAST_INVALID_INPUT&lt;/code&gt;
rather than returning null, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;try_cast&lt;/code&gt; gives you the old behaviour where you
actually want it. The migration is mechanical and the benefit is that silent
null-poisoning of a column stops being possible by accident.&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VARIANT&lt;/code&gt; is the answer to a question every lakehouse team has asked: how do you
store JSON whose schema you do not control, without either flattening it or
storing a string and paying to parse it on every read? &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VARIANT&lt;/code&gt; stores a parsed
binary encoding with its own metadata dictionary.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;variant&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  SELECT variant_get(parse_json(&apos;{&quot;user&quot;:{&quot;id&quot;:7,&quot;city&quot;:&quot;pune&quot;}}&apos;), &apos;$.user.id&apos;, &apos;int&apos;) AS user_id
&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +-------+
# |user_id|
# +-------+
# |      7|
# +-------+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Collations let a string column declare its own comparison rules, so
case-insensitive matching stops requiring &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;lower()&lt;/code&gt; on both sides and the
optimizer keeps its ability to use the column:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;collation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT &apos;Spark&apos; COLLATE UTF8_LCASE = &apos;spark&apos; AS eq&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +----+
# |  eq|
# +----+
# |true|
# +----+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;SQL pipe syntax rewrites a query as a sequence of steps in execution order
instead of a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SELECT&lt;/code&gt; whose clauses run in an order that does not match how they
are written. This is where my first guess was wrong. I wrote this:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VALUES&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;|&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;|&amp;gt;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;and got &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;[PIPE_OPERATOR_CONTAINS_AGGREGATE_FUNCTION]&lt;/code&gt;. Aggregation has its own
pipe operator, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;|&amp;gt; SELECT&lt;/code&gt; refuses to take an aggregate function. The
correct form:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;pipe&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;FROM VALUES (1), (2), (3) AS t(x) |&amp;gt; WHERE x &amp;gt; 1 |&amp;gt; AGGREGATE sum(x) AS s&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +---+
# |  s|
# +---+
# |  5|
# +---+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;SQL user-defined functions and session variables close a long-standing gap for
teams whose logic lives in SQL rather than in Scala or Python:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sqludf&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;DECLARE OR REPLACE cutoff INT DEFAULT 100&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SET VAR cutoff = 250&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  CREATE OR REPLACE TEMPORARY FUNCTION over_cutoff(amount INT)
  RETURNS BOOLEAN RETURN amount &amp;gt; cutoff
&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT over_cutoff(300) AS big, over_cutoff(100) AS small&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +----+-----+
# | big|small|
# +----+-----+
# |true|false|
# +----+-----+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h3 id=&quot;spark-41-december-2025&quot;&gt;Spark 4.1, December 2025&lt;/h3&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Pipelines&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark Declarative Pipelines&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-51727&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Streaming&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Real-time mode, sub-second continuous processing&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-53736&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL scripting enabled by default, declared GA&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-54499&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VARIANT&lt;/code&gt; enabled by default, declared GA, with shredding&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-54454&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Recursive common table expressions&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Stored procedures API for catalogs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-44167&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;KLL and Theta approximate sketches&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Connect&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;JDBC driver for Spark Connect&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-53484&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Arrow-native UDF and UDTF decorators&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-52214, SPARK-52979&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Core&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Checksum-based full shuffle stage retry, to avoid incorrect results&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-51756&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;ML&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark ML on Connect generally available for the Python client&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-51236&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Declarative Pipelines is the largest addition. You declare datasets and the
queries that produce them; Spark derives the execution graph, the dependency
order, the parallelism, the checkpoints and the retries. It ships with a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-pipelines&lt;/code&gt; CLI.&lt;/p&gt;

&lt;p&gt;One practical note that cost me a few minutes, and is not in the release notes:
the CLI is a Spark Connect client. On a stock &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;apache/spark:4.1.3-python3&lt;/code&gt; image
it fails twice before it runs, first on PyYAML and then on grpcio:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;ModuleNotFoundError: No module named &apos;yaml&apos;
...
PySparkImportError: [PACKAGE_NOT_INSTALLED] grpcio &amp;gt;= 1.48.1 must be installed; however, it was not found.
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pip install pyyaml grpcio&lt;/code&gt; fixes both. It is worth knowing that adopting
Declarative Pipelines means adopting Spark Connect’s client dependencies,
because that is an architectural commitment, not a packaging detail.&lt;/p&gt;

&lt;p&gt;SQL scripting being on by default turns Spark SQL into a procedural language.
Local variables, control flow and multi-statement blocks run inside the engine
rather than in a Python wrapper around it:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;scripting&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.scripting.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# true
&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
BEGIN
  DECLARE total INT DEFAULT 0;
  SET total = (SELECT sum(x) FROM VALUES (1), (2), (3) AS t(x));
  SELECT total AS grand_total;
END
&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +-----------+
# |grand_total|
# +-----------+
# |          6|
# +-----------+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Recursive CTEs arrived in the same release, which matters for anyone who has
been faking a hierarchy traversal with a loop in Python:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;cte&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  WITH RECURSIVE nums(n) AS (
    SELECT 1
    UNION ALL
    SELECT n + 1 FROM nums WHERE n &amp;lt; 5
  )
  SELECT collect_list(n) AS ns FROM nums
&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +---------------+
# |             ns|
# +---------------+
# |[1, 2, 3, 4, 5]|
# +---------------+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The Arrow UDF decorator is the endpoint of the line that started with pandas
UDFs in 2.3. It takes and returns PyArrow arrays directly, with no pandas
conversion in the middle:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyarrow&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pa&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql.functions&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;arrow_udf&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;arrowudf&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;o&quot;&gt;@&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;arrow_udf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;long&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;plus_one&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pa&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;Array&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;-&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pa&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;Array&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pa&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;compute&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;add&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;plus_one&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;p&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +---+
# |  p|
# +---+
# |  1|
# |  2|
# |  3|
# +---+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h3 id=&quot;spark-42-july-2026&quot;&gt;Spark 4.2, July 2026&lt;/h3&gt;

&lt;p&gt;The current release.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Area&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What landed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Ticket&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Types&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Geospatial &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;GEOMETRY&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;GEOGRAPHY&lt;/code&gt; types, on by default&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-51658&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Change data capture: a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CHANGES&lt;/code&gt; clause plus DataFrame and Connect APIs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-55668&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Pipelines&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Auto CDC in Declarative Pipelines, declarative SCD Type 1&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-56249&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Arrow-optimized Python UDFs and Arrow IPC on by default&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-54555&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NEAREST BY&lt;/code&gt; top-k ranking join&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-56395&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sources&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Data Source V2 transaction management&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-55855&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Path-based name resolution: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SET PATH&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CURRENT_PATH()&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-54806&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Metric views&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-54119&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;UI&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Rebuilt web UI with dark mode and side-by-side plan comparison&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-55760&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Build&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Builds and runs on Java 25&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SPARK-51167&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The Arrow default (SPARK-54555) is the change that affects everyone without
being asked for. On 4.1.3,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.execution.arrow.pyspark.enabled&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;; on 4.2.0 it is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;,
and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.execution.pythonUDF.arrow.enabled&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; as well. Ordinary
Python UDFs get the vectorized path without being rewritten.&lt;/p&gt;

&lt;p&gt;Geospatial support is where I have to be precise, because the release note
summary and the shipped function registry do not quite agree. The note says
“ST_* functions, WKB/WKT and Parquet read/write”. What is actually registered as
a built-in in 4.2.0 is five functions:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;geo&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;fns&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SHOW FUNCTIONS&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()]&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sorted&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;f&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;f&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fns&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;startswith&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;st_&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# [&apos;st_asbinary&apos;, &apos;st_geogfromwkb&apos;, &apos;st_geomfromwkb&apos;, &apos;st_setsrid&apos;, &apos;st_srid&apos;]
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;There is no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ST_Point&lt;/code&gt;, no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ST_AsText&lt;/code&gt;, and no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ST_Distance&lt;/code&gt; in the built-in
registry, and there is no cast from a WKT string either:
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CAST(&apos;POINT(1 2)&apos; AS GEOMETRY(4326))&lt;/code&gt; fails with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DATATYPE_MISMATCH.CAST_WITHOUT_SUGGESTION&lt;/code&gt;. The entry point in 4.2.0 is
well-known binary. What does work, and is genuinely new, is that the spatial
reference identifier is part of the type rather than a column beside it:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;geo2&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# WKB for POINT(73.8567 18.5204), little-endian
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;wkb&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;0101000000ed9e3c2cd4765240a1d634ef38853240&quot;&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  SELECT typeof(st_geomfromwkb(unhex(&apos;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;wkb&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;)))                  AS geom_type,
         typeof(st_geogfromwkb(unhex(&apos;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;wkb&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;)))                  AS geog_type,
         st_srid(st_setsrid(st_geomfromwkb(unhex(&apos;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;wkb&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&apos;)), 4326)) AS srid
&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;truncate&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;False&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# +-----------+---------------+----+
# |geom_type  |geog_type      |srid|
# +-----------+---------------+----+
# |geometry(0)|geography(4326)|4326|
# +-----------+---------------+----+
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;geometry(0)&lt;/code&gt; means SRID 0, an unspecified reference system. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;GEOGRAPHY&lt;/code&gt; defaults
to 4326, which is WGS 84. Note also that the bare type name does not parse:
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CAST(NULL AS GEOMETRY)&lt;/code&gt; is a syntax error while &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CAST(NULL AS GEOMETRY(4326))&lt;/code&gt;
is fine, because the SRID is part of the type’s grammar.&lt;/p&gt;

&lt;p&gt;Change data capture is the other feature where running it is more informative
than reading about it. The syntax is not the one I guessed; the grammar takes a
version or timestamp range:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;CHANGES&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VERSION&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TO&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;VERSION&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;On a built-in Parquet table that parses and analyses, then stops:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;[UNSUPPORTED_FEATURE.CHANGE_DATA_CAPTURE] The feature is not supported:
Catalog spark_catalog does not support Change Data Capture (CDC). SQLSTATE: 0A000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is the correct behaviour and it tells you exactly what the feature is.
Spark 4.2 ships the SQL surface, the parser and the analyzer rules for CDC; the
capability itself is delegated to the Data Source V2 catalog. You get change
feeds when your table format implements them, which is the same division of
labour Spark used for time travel.&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NEAREST BY&lt;/code&gt; does work on built-in tables, and its grammar is worth stating
because it is easy to guess wrong:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;JOIN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;APPROX&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;NEAREST&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;DISTANCE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;-&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The shape is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(APPROX | EXACT) NEAREST &amp;lt;k&amp;gt; BY (DISTANCE | SIMILARITY) &amp;lt;expr&amp;gt;&lt;/code&gt;,
attached where a join condition would go. It gives nearest-neighbour search a
planner-visible operator instead of a cross join with a sort, which is how
people have been doing vector search on Spark.&lt;/p&gt;

&lt;h2 id=&quot;defaults-that-changed-underneath-you&quot;&gt;Defaults that changed underneath you&lt;/h2&gt;

&lt;p&gt;Features are opt-in. Defaults are not. This table is the one to check before an
upgrade, and every current value in it was read from a live 4.1.3 or 4.2.0
session rather than from documentation.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Setting&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Changed in&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;From&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;To&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Current value&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.manager&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hash&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sort&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sort&lt;/code&gt;, now internal and absent from the 4.1.3 configuration reference&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.blockTransferService&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;nio&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;netty&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;netty only; the key is gone from the 4.1.3 configuration reference&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.io.compression.codec&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1.1&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;lzf&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snappy&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;lz4&lt;/code&gt;, changed again in a release the notes do not record&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;1.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10000&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10485760&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10485760b&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;no change found in any release note&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.orc.impl&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2.4&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hive&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;native&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;native&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.optimizer.dynamicPartitionPruning.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3.0&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt; &lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.optimizer.runtime.bloomFilter.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3.4&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.ansi.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.0&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.scripting.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.1&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.execution.arrow.pyspark.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt; on 4.1.3, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; on 4.2.0&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.execution.pythonUDF.arrow.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; on 4.2.0&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Three of these change results rather than performance. ANSI mode turns silent
nulls into errors. The proleptic Gregorian calendar in 3.0 shifts pre-1582
dates. And the 2.3 fix for shuffle plus repartition (SPARK-23207) changed wrong
answers into right ones, which is also a behaviour change if something
downstream was tuned to the wrong ones.&lt;/p&gt;

&lt;h2 id=&quot;what-was-removed-and-when&quot;&gt;What was removed, and when&lt;/h2&gt;

&lt;p&gt;Upgrades fail on removals more often than they fail on features.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Removed&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Version&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Replacement&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Java 7&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Java 8&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala 2.10&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2.3&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala 2.11&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Hadoop 2.5 and earlier&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;2.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Hadoop 2.6 or later&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python 3.7&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;3.5&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python 3.8 or later&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python 3.8&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.0&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python 3.9 or later&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala 2.12&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.0&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Scala 2.13&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;JDK 8 and 11&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.0&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;JDK 17 or later&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Mesos&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.0&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Standalone, YARN or Kubernetes&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;R 3.x&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;4.2&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;R 4.x&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;SparkR was deprecated in 4.0 (SPARK-49347) and has not been removed: 4.2 still
lists SparkR changes, including Java 25 support. Deprecated is not gone, but it
is a clear signal about where to put new R work.&lt;/p&gt;

&lt;p&gt;The Scala 2.12 removal in 4.0 is the one that breaks builds hardest, because it
is not a Spark-only change. Every JVM dependency you bring, including every
connector, needs a 2.13 artifact.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Adaptive query execution is a Spark 3 feature.”&lt;/strong&gt; The idea shipped in 1.6
(SPARK-9858) and only chose reducer counts. The implementation you know was
written for 3.0 (SPARK-31412) and switched on by default in 3.2 (SPARK-33679).
The distinction matters when reading old tuning advice, which may be about the
1.6 version.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Structured Streaming arrived in 2.0.”&lt;/strong&gt; It arrived as an experimental API in
2.0 and was declared generally available in 2.2 (SPARK-20844), two releases and
a year later. Production advice written between those points is about a moving
target.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Kubernetes has been supported since 2.3.”&lt;/strong&gt; Experimentally. The release note
for 2.3 says configurations, container images and entrypoints were expected to
change. It went GA in 3.1 (SPARK-33005).&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Spark 4 enables ANSI mode, so my job will fail loudly on bad data.”&lt;/strong&gt; Only
for the operations ANSI mode governs, which are arithmetic overflow, division by
zero and invalid casts. A string that parses as a number but means something
else still passes silently, as it should.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; does not matter any more, because AQE
coalesces.”&lt;/strong&gt; AQE coalesces partitions down after the shuffle has been written.
The value still decides how many partitions get written in the first place, and
it is still &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt;. The plan output earlier in this post shows exactly that:
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange hashpartitioning(bucket#1L, 200)&lt;/code&gt; followed by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AQEShuffleRead
coalesced&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Spark 4.2 ships geospatial functions, so I can port my PostGIS queries.”&lt;/strong&gt;
Five &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;st_*&lt;/code&gt; functions are registered as built-ins in 4.2.0, and WKT is not among
the entry points. Check &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SHOW FUNCTIONS&lt;/code&gt; against the queries you intend to port
before planning the work.&lt;/p&gt;

&lt;h2 id=&quot;choosing-where-to-sit&quot;&gt;Choosing where to sit&lt;/h2&gt;

&lt;p&gt;A short version of the advice, for the three positions most teams are actually
in.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;On 2.4.&lt;/strong&gt; The correctness fix in 2.3 (SPARK-23207) is behind you, which is the
good news. Everything else argues for moving: no adaptive execution, no dynamic
partition pruning, no Kubernetes GA, and a Scala version no current connector
ships for. The jump to 3.5 is large but well trodden.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;On 3.1 to 3.3.&lt;/strong&gt; The single biggest gain available to you is AQE by default in
3.2, and you can have most of it today by setting
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.enabled=true&lt;/code&gt; and measuring. Do that first; it tells you
what the upgrade is worth before you spend it.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;On 3.5.&lt;/strong&gt; You are on the practical long-term-support release, and it is still
being maintained: 3.5.9 shipped on 16 July 2026, two days after 4.2.0. Moving to 4.x
costs you a Scala 2.13 rebuild, a JDK 17 or later runtime, and an ANSI-mode
audit. Budget the ANSI audit seriously and treat it as a data-quality exercise
rather than a compatibility chore, because every error it raises is a place your
pipeline was silently producing nulls.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;On 4.0 or 4.1.&lt;/strong&gt; Moving within the 4.x line is ordinary. Watch for the Arrow
defaults in 4.2 changing Python UDF behaviour, which is usually an improvement
and is still a change worth a test run.&lt;/p&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Which Spark version should I move to from 2.4?&lt;/strong&gt;
3.5 if you need a staged migration, because it keeps Scala 2.12 as an option and
gets you adaptive execution, dynamic partition pruning and Kubernetes GA. 4.2 if
you are willing to do the Scala 2.13 and JDK 17 work once rather than twice.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Is Spark 3.5 still maintained?&lt;/strong&gt;
Yes. 3.5.9 was released on 16 July 2026, two days after 4.2.0. The 3.5 line is
the practical long-term-support release for organisations that have not yet
moved to 4.x.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;What actually breaks when I move to Spark 4?&lt;/strong&gt;
Four things, in descending order of how often they bite: Scala 2.12 artifacts
that have no 2.13 build, a JDK older than 17, ANSI mode turning silent nulls
into raised errors, and Mesos deployments having nowhere to go. The first two
are build failures you find immediately. The third is a runtime failure you find
in production unless you audit for it.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Do I have to use Spark Connect on Spark 4?&lt;/strong&gt;
No. The classic in-process driver is still the default, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-submit&lt;/code&gt; works
as it always has. Connect is opt-in, with one caveat: some newer components are
Connect clients themselves, including the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-pipelines&lt;/code&gt; CLI for Declarative
Pipelines, which needs grpcio installed even when your queries do not.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;When did adaptive query execution actually become the default?&lt;/strong&gt;
3.2, via SPARK-33679. It existed but was off by default in 3.0 and 3.1, and a
much smaller version of the idea shipped in 1.6 as SPARK-9858. Tuning advice
that predates 3.2 usually assumes it is off.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Has SparkR been removed?&lt;/strong&gt;
Not yet. It was deprecated in 4.0 (SPARK-49347) and is still present in 4.2,
which added Java 25 support to it. Support for R 3.x was dropped in 4.2
(SPARK-57767), so an old R runtime will stop you before the deprecation does.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why does the SQL pipe operator reject my aggregate function?&lt;/strong&gt;
Because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;|&amp;gt; SELECT&lt;/code&gt; does not accept aggregates. Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;|&amp;gt; AGGREGATE&lt;/code&gt; instead. The
error class is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PIPE_OPERATOR_CONTAINS_AGGREGATE_FUNCTION&lt;/code&gt; and it names the fix.&lt;/p&gt;

&lt;h2 id=&quot;the-mental-model&quot;&gt;The mental model&lt;/h2&gt;

&lt;p&gt;If you remember one thing, make it the shape rather than the list.&lt;/p&gt;

&lt;p&gt;Spark spent its first line learning that the RDD hid too much from the engine,
its second line replacing it with a schema the engine could compile, its third
line admitting that plans made from estimates are wrong and building a mechanism
to revise them, and its fourth line detaching the client from the driver and
pouring the accumulated capability back into SQL.&lt;/p&gt;

&lt;p&gt;Every feature in this post is an instance of one of those four moves. When the
next release lands, that is the question to ask of each headline: which move is
this, and does it change what my plans do or only what I am able to write?&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/releases/&quot;&gt;Spark release archive&lt;/a&gt;, the index every release note in this post was read from&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/releases/spark-release-3-0-0.html&quot;&gt;Spark 3.0.0 release notes&lt;/a&gt; for adaptive query execution, dynamic partition pruning and the TPC-DS claim quoted above&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/releases/spark-release-4-0-0.html&quot;&gt;Spark 4.0.0 release notes&lt;/a&gt; for ANSI by default, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VARIANT&lt;/code&gt;, collations and the Scala, JDK and Mesos removals&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/releases/spark-release-4.1.0.html&quot;&gt;Spark 4.1.0 release notes&lt;/a&gt; for Declarative Pipelines, real-time mode and SQL scripting&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/releases/spark-release-4-2-0.html&quot;&gt;Spark 4.2.0 release notes&lt;/a&gt; for geospatial types, change data capture and the Arrow defaults&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/api/src/main/antlr4/org/apache/spark/sql/catalyst/parser/SqlBaseParser.g4&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SqlBaseParser.g4&lt;/code&gt;&lt;/a&gt;, the grammar that settled the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CHANGES&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NEAREST BY&lt;/code&gt; syntax when my first guesses failed to parse&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/sql-migration-guide.html&quot;&gt;Spark SQL migration guide&lt;/a&gt; for the behaviour changes each version introduced&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheSparkArchitecture/&quot;&gt;Apache Spark architecture&lt;/a&gt; for the runtime the shuffle and scheduling changes act on&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/SparkAQE/&quot;&gt;Adaptive query execution&lt;/a&gt; for what the 3.x re-planning does and does not revisit&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Hudi, Apache Iceberg, Apache Parquet, Apache Avro, Apache ORC, Apache Arrow, Apache Kafka, Apache Hadoop, Apache Mesos, Apache YuniKorn and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="Releases" /><category term="Migration" /><category term="Defaults" /><category term="Internals" /><summary type="html">Architecture: the four eras, and what moved between them The 1.x line: structure arrives Spark 1.0, May 2014 Spark 1.1, September 2014 Spark 1.2, December 2014 Spark 1.3, March 2015 Spark 1.4, June 2015 Spark 1.5, September 2015 Spark 1.6, January 2016 The 2.x line: unification and codegen Spark 2.0, July 2016 Spark 2.1, December 2016 Spark 2.2, July 2017 Spark 2.3, February 2018 Spark 2.4, November 2018 The 3.x line: the planner stops guessing Spark 3.0, June 2020 Spark 3.1, March 2021 Spark 3.2, October 2021 Spark 3.3, June 2022 Spark 3.4, April 2023 Spark 3.5, September 2023 The 4.x line: SQL becomes the surface again Spark 4.0, May 2025 Spark 4.1, December 2025 Spark 4.2, July 2026 Defaults that changed underneath you What was removed, and when Common misconceptions Choosing where to sit Frequently asked questions The mental model References Trademarks TL;DR Spark’s centre of gravity has moved four times: from the RDD, to the DataFrame, to the query plan, to a protocol between client and driver. Every release makes more sense once you know which of those four eras it belongs to. The biggest upgrades are rarely the headline features. They are the default changes: sort shuffle, a 10 MB broadcast threshold, adaptive execution, bloom-filter joins, and ANSI mode all flipped on in different releases and silently changed what your existing jobs do. Some 1.x defaults have never moved. The broadcast-join threshold set in 1.2 is still 10485760b, and spark.sql.shuffle.partitions is still 200, both read from a 4.1.3 session. A single explain output on a current cluster shows three eras at once: whole-stage codegen markers from 2.0, the adaptive wrapper from 3.0, and coalescing from 3.2. Adaptive execution itself is older than any of them, first shipping in 1.6. The newest release ships a SQL surface ahead of its engine in places. The change-data-capture clause parses and analyses, then stops at the catalog, because the built-in one does not implement it. You have inherited a Spark job. It is pinned to some version because that is what the platform team installed in 2021, and someone is now asking whether moving it forward is worth a quarter of engineering time. The release notes are no help: each one is a list of between 1,100 and 5,100 resolved tickets, sorted by component, with no indication of which three entries will change your job’s behaviour and which 3,000 you can ignore. That is the question this post answers. Not “what is in each release”, which the project already documents, but which changes altered the execution model, which changed a default under you, and which deleted something you might still be using. Everything below was checked against a running cluster rather than recalled. The version under test is 4.1.3 unless the text says otherwise, with a second cluster on 4.2.0 for the newest features. Five claims I was confident about turned out to be wrong when I ran them, and I have kept those corrections in the text where they are useful. Architecture: the four eras, and what moved between them Spark has had one durable architectural habit: each era moves the thing you program against one level further from the machine, and one level closer to a description of intent. flowchart LR E1[&quot;&amp;lt;b&amp;gt;1.x&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the RDD era&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;you describe&amp;lt;br/&amp;gt;the computation&amp;lt;/i&amp;gt;&quot;] E2[&quot;&amp;lt;b&amp;gt;2.x&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the DataFrame era&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;you describe&amp;lt;br/&amp;gt;the data&amp;lt;/i&amp;gt;&quot;] E3[&quot;&amp;lt;b&amp;gt;3.x&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the plan era&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;the engine revises&amp;lt;br/&amp;gt;its own decisions&amp;lt;/i&amp;gt;&quot;] E4[&quot;&amp;lt;b&amp;gt;4.x&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the protocol era&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;the client is&amp;lt;br/&amp;gt;detached from the driver&amp;lt;/i&amp;gt;&quot;] E1 --&amp;gt; E2 --&amp;gt; E3 --&amp;gt; E4 Each move buys something and costs something: Era What you program against What it buys What it costs 1.x RDD of JVM objects Total control, any type The engine cannot see inside your closure, so it cannot optimise it 2.x DataFrame over a schema Codegen, columnar scans, one API for batch and stream Your lambda becomes an expression tree, and UDFs become the slow path 3.x The same DataFrame, re-planned at runtime Decisions made on measurements, not estimates Plans you read before execution are no longer the plans that ran 4.x A plan sent over a wire Thin clients, many languages, isolation from the driver JVM Two API surfaces to support, and features arrive on them at different times The version-by-version sections follow that arc. If you only read one section, read Defaults that changed underneath you, because default changes cause more upgrade surprises than features do. The 1.x line: structure arrives The first line’s job was to turn a fast research engine into something an operations team would accept, and then to discover that the RDD was the wrong abstraction for most of the work people were doing with it. Spark 1.0, May 2014 The release that made Spark deployable rather than merely usable. Area What landed Why it mattered Packaging spark-submit, one submission path for local, Mesos and YARN Before this, each cluster manager had its own launch story Operations History server for the web UI Post-mortem debugging of finished applications Security Hadoop and YARN security model, credential transfer The blocker for regulated Hadoop shops SQL Spark SQL as an alpha component, with Catalyst behind it The first appearance of the optimizer that everything later depends on API Stability guarantee for the whole 1.x line Applications could be written against a moving project Spark SQL arrived with SchemaRDD, not DataFrame. The name survived exactly three releases. Catalyst, introduced in the same release to choose execution plans and push predicates into Parquet, is still the optimizer in 4.2 and is covered in depth in Inside Spark’s Catalyst optimizer. Spark 1.1, September 2014 Area What landed Shuffle Sort-based shuffle, available but not yet default SQL JDBC/ODBC server, JSON source with schema inference, dynamic bytecode generation for expressions SQL UDF registration from Python, Scala and Java Core Disk spilling for skewed blocks during cache operations Two defaults changed here. spark.io.compression.codec became snappy, though it has moved on since: the default in the 4.1.3 build I ran is lz4. And spark.broadcast.factory became TorrentBroadcastFactory, which is why broadcasting a large variable no longer saturates the driver’s network link. The dynamic bytecode generation in this release is the seed of what becomes whole-stage codegen in 2.0. It compiled expressions; 2.0 compiled whole operator chains. Spark 1.2, December 2014 The release that changed the most defaults in the shortest note. Area What landed Shuffle spark.shuffle.manager default changed from hash to sort Network spark.shuffle.blockTransferService default changed from nio to netty Scaling Elastic scaling (dynamic allocation) on YARN SQL External data source API, the ancestor of every connector you use Streaming Write-ahead log for driver high availability, and a Python API GraphX Graduated from alpha The hash-to-sort shuffle change is the single most consequential default flip in Spark’s history. Hash shuffle opened one file per reduce partition per map task; at a few thousand partitions on each side that is millions of open files. Sort shuffle writes one file plus an index per map task. Everything about shuffle tuning after 1.2 assumes the sort writer, which is described in Apache Spark architecture. This release also set spark.sql.autoBroadcastJoinThreshold to 10485760, raising it from 10000. That value has not changed since. On the 4.1.3 cluster I ran for this post: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;defaults&quot;).getOrCreate() print(spark.conf.get(&quot;spark.sql.autoBroadcastJoinThreshold&quot;)) # 10485760b print(spark.conf.get(&quot;spark.sql.shuffle.partitions&quot;)) # 200 spark.stop() Eleven years and three major versions later, the broadcast threshold a 2014 release chose is still deciding whether your join shuffles. That is worth knowing before you conclude that a join strategy was picked by something sophisticated. Spark 1.3, March 2015 The release where Spark stopped being an RDD engine. Area What landed API The DataFrame API, in Python, Scala and Java SQL SchemaRDD renamed to DataFrame, Spark SQL graduated from alpha Sources JDBC data source for MySQL, Postgres and other relational databases Sources Parquet schema merging for compatible schemas Streaming Direct Kafka API, exactly-once without a write-ahead log The rename looks cosmetic and was not. A SchemaRDD was an RDD that happened to carry a schema. A DataFrame is a description of a computation over named, typed columns, which the engine is free to execute however it likes. That freedom is what every later optimisation spends. The direct Kafka API deserves its own note. The receiver-based approach ran a long-lived receiver task that wrote to a write-ahead log for durability. The direct approach treats Kafka offsets as the checkpoint, so the log becomes unnecessary and delivery becomes exactly-once instead of at-least-once. This is the same reasoning Structured Streaming later generalises to every source. This is the oldest API in the post that still runs unchanged today: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;df13&quot;).getOrCreate() orders = spark.createDataFrame( [(1, &quot;pune&quot;, 1200), (2, &quot;hyderabad&quot;, 800), (3, &quot;pune&quot;, 450)], &quot;order_id INT, city STRING, amount INT&quot;, ) orders.groupBy(&quot;city&quot;).sum(&quot;amount&quot;).orderBy(&quot;city&quot;).show() # +---------+-----------+ # | city|sum(amount)| # +---------+-----------+ # |hyderabad| 800| # | pune| 1650| # +---------+-----------+ spark.stop() Spark 1.4, June 2015 Area What landed Ticket R SparkR, an R binding built on the DataFrame API   SQL Window functions in SQL and DataFrames SPARK-1442 SQL Sort-merge joins for very large joins SPARK-2213 SQL ORCFile support SPARK-2883 Core First performance work under the name Project Tungsten SPARK-7081 Core DAG visualisation in the UI SPARK-6942 Core Python 3 support SPARK-4897 Core REST API for application information SPARK-3644 Sort-merge join is the important entry. Until it existed, a shuffle join that did not fit in memory failed. After it, join size is bounded by disk instead of memory. The trade-off is explicit: sort-merge join pays a sort on both sides to remove the memory ceiling. Spark still makes exactly this choice today, and the decision procedure is walked through in Spark joins in depth. Spark 1.5, September 2015 Tungsten’s main release. The theme is that the JVM’s object model is the bottleneck, so Spark should stop using it for data. Area What landed Execution Code generation on by default for almost all DataFrame and SQL functions Execution Cache-friendly in-memory hash map layout for aggregation Execution Fallback to external sort-based aggregation when memory is exhausted Execution Compact binary in-memory representation, execution memory accounted for explicitly rather than left to the garbage collector Execution Sort-merge join preferred over hash join for shuffle joins SQL Around 100 new built-in functions, a UDAF interface (SPARK-3947), and a broadcast hint (SPARK-8300) “Execution memory is explicitly accounted for, without relying on JVM GC” is the sentence that matters. Before Tungsten, a Spark executor’s memory behaviour was the JVM’s behaviour, and tuning meant tuning a garbage collector. After it, Spark tracks its own execution memory and spills deliberately. The memory model that grows out of this is covered in Spark memory management. Spark 1.6, January 2016 Area What landed Ticket API The Dataset API, typed objects with the SQL engine underneath SPARK-9999 Memory Unified memory management, one pool shared by execution and storage SPARK-10000 Memory Query execution using off-heap memory SPARK-11389 Execution First adaptive query execution, choosing reducer counts automatically SPARK-9858 Streaming mapWithState, replacing updateStateByKey SPARK-2629 SQL Per-operator metrics for SQL execution SPARK-10412 Two things are worth flagging. Unified memory management (SPARK-10000) replaced a fixed split between execution and cache memory with one pool and a borrowing rule. The eviction is deliberately asymmetric: execution can evict cached blocks, storage can never evict execution. A query that needs memory to finish beats a cache that is only an optimisation. Adaptive query execution (SPARK-9858) is dated 1.6, not 3.0. The 1.6 version only picked the number of reducers for joins and aggregations. It was rebuilt from scratch for 3.0 and enabled by default in 3.2. Anyone who says AQE is new in 3.0 is describing the rewrite, not the idea. The 2.x line: unification and codegen If 1.x discovered that the DataFrame was the right abstraction, 2.x committed to it: one entry point, one type, one execution strategy, and streaming redefined as a query over an unbounded table. Spark 2.0, July 2016 Area What landed API SparkSession replaces SQLContext and HiveContext API DataFrame becomes a type alias for Dataset[Row] in Scala and Java SQL SQL 2003 support, all 99 TPC-DS queries runnable SQL Native SQL parser, native DDL, correlated and uncorrelated subqueries Execution Whole-stage code generation Execution Vectorized Parquet reads, off-heap memory for cache and execution Sources Native CSV source, Hive-style bucketing Streaming Structured Streaming, as an experimental API Whole-stage codegen is the defining change. Before it, each operator was a separate object pulling rows from the next through an iterator, which costs a virtual call per row per operator. After it, Spark compiles an entire chain of operators into one Java method with the loop fused, which the JIT can then treat like hand-written code. The release notes claim speedups of two to ten times for common operators. You can see it in any plan today. The * markers and the number after them are codegen stage identifiers: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;codegen&quot;).getOrCreate() df = (spark.range(0, 1_000_000) .selectExpr(&quot;id&quot;, &quot;id % 7 AS bucket&quot;) .groupBy(&quot;bucket&quot;).count()) df.collect() print(df._jdf.queryExecution().executedPlan().toString()) spark.stop() On 4.1.3 that prints: AdaptiveSparkPlan isFinalPlan=true +- == Final Plan == ResultQueryStage 1 +- *(2) HashAggregate(keys=[bucket#1L], functions=[count(1)], output=[bucket#1L, count#2L]) +- AQEShuffleRead coalesced +- ShuffleQueryStage 0 +- Exchange hashpartitioning(bucket#1L, 200), ENSURE_REQUIREMENTS, [plan_id=25] +- *(1) HashAggregate(keys=[bucket#1L], functions=[partial_count(1)], output=[bucket#1L, count#7L]) +- *(1) Project [(id#0L % 7) AS bucket#1L] +- *(1) Range (0, 1000000, step=1, splits=11) +- == Initial Plan == HashAggregate(keys=[bucket#1L], functions=[count(1)], output=[bucket#1L, count#2L]) +- Exchange hashpartitioning(bucket#1L, 200), ENSURE_REQUIREMENTS, [plan_id=15] +- HashAggregate(keys=[bucket#1L], functions=[partial_count(1)], output=[bucket#1L, count#7L]) +- Project [(id#0L % 7) AS bucket#1L] +- Range (0, 1000000, step=1, splits=11) Three eras are visible in that one output. *(1) and *(2) are 2.0’s whole-stage codegen: the range scan, the projection and the partial aggregate were compiled into a single generated method, and the final aggregate into another. AdaptiveSparkPlan with its Initial Plan and Final Plan sections is 3.0. AQEShuffleRead coalesced is 3.2 deciding at runtime that 200 shuffle partitions were too many for seven groups and reading them as fewer. Note also what did not happen: the Exchange still says 200, because spark.sql.shuffle.partitions is still the 1.x default. AQE coalesced after the fact rather than planning a better number in the first place. Spark 2.1, December 2016 Area What landed Ticket Streaming Event-time watermarks SPARK-18124 Streaming Kafka 0.10 support in Structured Streaming SPARK-17346 Streaming Stable format for the offset log SPARK-17829 SQL from_json and to_json for string columns SPARK-18351 SQL Scalable partition handling, partition metadata in the metastore SPARK-17861 Watermarks are the release’s real content. A streaming aggregation over event time has to keep state for every open window, and without a rule for closing them that state grows without bound. A watermark is that rule: a declaration that data more than a stated interval behind the maximum seen event time will not be waited for. It buys bounded state at the cost of dropping genuinely late records, and you choose where on that trade-off to sit. Spark 2.2, July 2017 Area What landed Ticket Streaming Structured Streaming APIs declared generally available SPARK-20844 SQL Cost-based optimizer: cardinality estimation for filter, join, aggregate, project and limit SPARK-17075 and related SQL Cost-based join reordering SPARK-17080 SQL Broadcast hints in SQL: BROADCAST, BROADCASTJOIN, MAPJOIN SPARK-16475 SQL Session-local time zone SPARK-18350 Core Blacklist mechanism for task scheduling SPARK-8425 Build Java 7 support removed SPARK-19493 Packaging pip install pyspark   The cost-based optimizer is the one to be realistic about. It requires statistics you have to compute yourself with ANALYZE TABLE, and without them the rule-based path does the work. Nine years later that is still true, and the Catalyst post shows a join reordering where the rule-based ReorderJoin fired and CostBasedJoinReorder ran and changed nothing. pip install pyspark belongs in the same list as the engine features. It is the moment PySpark stopped requiring a cluster distribution to try. Spark 2.3, February 2018 A dense release. Area What landed Ticket Deployment Kubernetes scheduler backend, experimental SPARK-18278 Sources Vectorized ORC reader, spark.sql.orc.impl=native SPARK-16060 Sources Data Source API V2, experimental SPARK-15689 Streaming Continuous processing, sub-millisecond end-to-end latency   Streaming Stream-to-stream joins   PySpark Pandas UDFs, vectorized execution over Arrow SPARK-22216 SQL Histogram support in the cost-based optimizer SPARK-21975 Core Fixed a long-standing correctness bug where shuffle plus repartition could produce wrong answers SPARK-23207 Build Scala 2.10 support removed SPARK-19810 Two entries deserve emphasis for opposite reasons. Pandas UDFs (SPARK-22216) changed the economics of Python on Spark. A plain Python UDF serialises one row at a time to a Python worker and back. A pandas UDF ships an Arrow batch, runs vectorized pandas code over it, and ships a batch back. Same language, different order of magnitude of overhead. Everything Spark has done for Python since, through to the Arrow UDFs of 4.1, follows this line. SPARK-23207 is a correctness fix, not a feature: certain combinations of shuffle and repartition could return wrong results. The DataFrame case was fixed here in 2.3; the RDD case followed in 2.4 as SPARK-23243. If you are on anything older and you repartition after a shuffle, that is your reason to move, and it outranks every feature in this post. Spark 2.4, November 2018 The last 2.x feature release, and the long-term home of an enormous amount of production Spark. Area What landed Ticket Scheduling Barrier execution mode, for deep learning frameworks SPARK-24374 SQL Higher-order functions over arrays and maps SPARK-23899 Sources Built-in Avro data source SPARK-24768 Sources Native ORC reader on by default SPARK-23456 SQL EXCEPT ALL and INTERSECT ALL SPARK-21274 SQL PIVOT syntax SPARK-24035 SQL Nested schema pruning for Parquet SPARK-4502 SQL Bucket pruning SPARK-23803 Core Blocks larger than 2 GB can be replicated and sent SPARK-24296, SPARK-24307 Build Scala 2.12 support, experimental SPARK-14220 Higher-order functions are the feature most 2.4 users never adopted and should have. Before them, transforming an array column meant either exploding and regrouping, which is a shuffle, or writing a UDF, which leaves the engine. After them it is an expression the optimizer understands: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;hof&quot;).getOrCreate() spark.sql(&quot;SELECT transform(array(1, 2, 3), x -&amp;gt; x * 2) AS doubled&quot;).show() # +---------+ # | doubled| # +---------+ # |[2, 4, 6]| # +---------+ spark.stop() The 2 GB block limit (SPARK-24296) is the other entry worth knowing, because it explains a class of failure that simply stops happening after 2.4. Spark’s network layer used a single ByteBuffer per block, and ByteBuffer is indexed by int. Any shuffle block or cached partition over 2 GB failed with an overflow. The fix is unglamorous and removes a hard ceiling that used to dictate partition counts. The 3.x line: the planner stops guessing The 2.x engine made one plan from estimates and executed it. The estimates were frequently wrong, because nobody knows the selectivity of a filter over data nobody has read yet. The 3.x line’s theme is letting the engine revise itself, and separately, admitting that Python is the primary language. Note the gap: 2.4.0 shipped in November 2018 and 3.0.0 in June 2020, nineteen months later. Major versions are where the project spends its compatibility budget, and it does not spend it often. Spark 3.0, June 2020 Area What landed Ticket Execution Adaptive query execution, rebuilt SPARK-31412 Execution Dynamic partition pruning SPARK-11150 Scheduling Accelerator-aware scheduling, for GPUs SPARK-24615 PySpark Pandas UDF API redesigned around Python type hints SPARK-28264 SQL Catalog plugin API, the basis of every modern table format connector SPARK-31121 SQL Proleptic Gregorian calendar SPARK-26651 Streaming A dedicated Structured Streaming UI SPARK-29543 Build Java 11 and Hadoop 3 support SPARK-24417, SPARK-23534 Adaptive query execution re-plans after each shuffle stage completes, using the measured sizes of what was just written. It coalesces partitions that turned out small, splits ones that turned out skewed, converts a sort-merge join to a broadcast when the build side turns out to fit, and reads shuffle data locally when the join no longer needs it distributed. It is off by default in this release. What it does and does not revisit is the subject of Adaptive query execution. Dynamic partition pruning solves a different problem. A star-schema query filtering a small dimension table cannot prune fact-table partitions at plan time, because the filter is on the other side of a join. DPP plans a subquery that computes the surviving dimension keys first, then uses them to prune the fact scan. A common query shape goes from reading every partition to reading a handful. The calendar change (SPARK-26651) is the sleeper. Spark moved from a hybrid Julian-Gregorian calendar to the proleptic Gregorian one. Dates before 1582 can shift. If you store historical dates, test before upgrading rather than after. The release notes state that Spark 3.0 is roughly twice as fast as 2.4 on a 30 TB TPC-DS benchmark. That is the project’s own measurement on its own hardware, quoted here as a claim rather than asserted as a result you will see. Your workload is not TPC-DS. Spark 3.1, March 2021 There is no Spark 3.1.0. The line starts at 3.1.1; the release notes for 3.1.1 describe it as the second release of the 3.x line. Area What landed Ticket Deployment Kubernetes support declared generally available SPARK-33005 PySpark Project Zen: Python type annotations and dependency management   SQL CHAR and VARCHAR types SPARK-33480 SQL ANSI mode raises runtime errors instead of returning null SPARK-33275 SQL New explicit cast rules under ANSI mode SPARK-33354 Execution Shuffled hash join improvements, including full outer join support SPARK-32461 Core Node decommissioning for Kubernetes and Standalone, experimental SPARK-20624 Kubernetes going GA three years after its experimental introduction in 2.3 is the clearest example of the project’s pace on deployment features. SPARK-33275 opens an argument that runs across the rest of the 3.x line and is settled in 4.0. Silently returning null on overflow or a bad cast is convenient and hides data corruption. ANSI mode raises instead. In 3.1 it is opt-in. Spark 3.2, October 2021 Area What landed Ticket PySpark pandas API on Spark, the former Koalas project, merged in SPARK-34849 Execution Adaptive query execution enabled by default SPARK-33679 Shuffle Push-based shuffle SPARK-30602 Streaming RocksDB state store SPARK-34198 Streaming Session windows SPARK-10816 SQL ANSI mode declared generally available SPARK-35030 SQL ANSI INTERVAL types SPARK-27790 Build Scala 2.13 support SPARK-34218 AQE on by default (SPARK-33679) is the most important line in the 3.x table. If you upgrade from 3.1 to 3.2 with no config changes, your query plans change. They usually improve. They do change. The RocksDB state store is the fix for the other half of the streaming state problem. The default state store keeps state in the JVM heap, so a large stateful stream becomes a garbage collection problem. RocksDB moves it off-heap and onto local disk, trading some read latency for state that scales past what the heap can hold. pandas API on Spark deserves an honest note on packaging: it needs pandas installed on the cluster, and the error if it is missing is explicit about the minimum version. from pyspark.sql import SparkSession import pyspark.pandas as ps spark = SparkSession.builder.appName(&quot;pandas-api&quot;).getOrCreate() psdf = ps.DataFrame({&quot;city&quot;: [&quot;pune&quot;, &quot;pune&quot;, &quot;hyderabad&quot;], &quot;amount&quot;: [10, 20, 30]}) print(psdf.groupby(&quot;city&quot;).amount.sum().sort_index().to_dict()) # {&apos;hyderabad&apos;: 30, &apos;pune&apos;: 30} spark.stop() On a clean apache/spark image that fails with [PACKAGE_NOT_INSTALLED] Pandas &amp;gt;= 2.2.0 must be installed, which is the correct error and still a surprise the first time. Spark 3.3, June 2022 Area What landed Ticket Execution Row-level runtime filtering, including bloom filters SPARK-32268 Errors Error classes and structured error messages SPARK-38781 Sources Complex types in the vectorized Parquet reader SPARK-34863 SQL Hidden file metadata, the _metadata column SPARK-37273 PySpark Profiler for Python and pandas UDFs SPARK-37443 Row-level runtime filtering extends the dynamic pruning idea from partitions to rows. Spark builds a bloom filter from the small side of a join and pushes it into the large-side scan, so rows that cannot possibly match are discarded before they are shuffled. It buys a smaller shuffle at the cost of building and broadcasting the filter, which is why it is gated on size thresholds. The error class work (SPARK-38781) is the unglamorous change that improved daily life most. Spark errors acquired stable identifiers such as DIVIDE_BY_ZERO instead of free-text messages, which means you can match on them in code and search for them without pasting a stack trace into a search box. Spark 3.4, April 2023 Area What landed Ticket Architecture Spark Connect, Python client SPARK-39375 SQL DEFAULT values for table columns SPARK-38334 SQL TIMESTAMP WITHOUT TIME ZONE SPARK-35662 SQL Lateral column alias references SPARK-27561 SQL Parameterized SQL SPARK-41271 SQL UNPIVOT and melt SPARK-38864 Execution Bloom filter joins enabled by default SPARK-38841 Errors SQLSTATE codes attached to error classes SPARK-41994 PySpark Memory profiler for UDFs SPARK-40281 Spark Connect is the architectural change of the 3.x line, even though it arrives near the end of it. Until 3.4, a Spark application meant a driver JVM in your process: PySpark launched one and talked to it over a local socket, and your application’s lifetime was the driver’s lifetime. Connect replaces that with a gRPC protocol carrying unresolved logical plans. The client becomes thin and the driver becomes a server that many clients can share. What it costs is a second API surface. For several releases, some things worked in a classic session and not in a Connect one, which produces confusing symptoms; two posts here work through that in practice, for Iceberg and for Hudi. Lateral column aliases are the small feature people notice immediately, because every other SQL engine already had it: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;lca&quot;).getOrCreate() spark.sql(&quot;SELECT 1 AS a, a + 1 AS b&quot;).show() # +---+---+ # | a| b| # +---+---+ # | 1| 2| # +---+---+ spark.stop() Before 3.4, referring to a in the same SELECT list that defined it was an error, and you repeated the expression or nested a subquery. Spark 3.5, September 2023 Area What landed Ticket Connect Scala and Go clients SPARK-42554, SPARK-43351 Connect Structured Streaming support in Python and Scala SPARK-42938 Connect pandas API support on the Python client SPARK-42497 PySpark Arrow-optimized Python UDFs SPARK-40307 PySpark Python user-defined table functions SPARK-43798 ML PyTorch-based distributed training on Connect SPARK-42471 Errors PySpark errors migrated onto error classes SPARK-42986 3.5 is the long-term support release of the 3.x line in practice, and it is where most organisations that have not moved to 4.x are sitting. Its maintenance line is still active: 3.5.9 shipped in July 2026, after 4.2.0. The 4.x line: SQL becomes the surface again The 4.x theme is that after a decade of enriching the DataFrame API, the project turned back to SQL and started adding things that SQL engines have and Spark did not: variant data, collations, user-defined functions written in SQL, procedural scripting, and a pipeline syntax. Spark 4.0, May 2025 The break. Read the removals first. Removed or dropped Ticket Scala 2.12, Scala 2.13 becomes the only supported version SPARK-45314 JDK 8 and JDK 11, JDK 17 becomes the minimum SPARK-45315 Mesos support SPARK-44442 Python 3.8 SPARK-47993 SparkR deprecated, not yet removed SPARK-49347 And the additions: Area What landed Ticket SQL ANSI SQL mode on by default SPARK-44444 SQL VARIANT data type for semi-structured data SPARK-45827 SQL String collation support SPARK-46830 SQL SQL user-defined functions SPARK-46057 SQL Session variables SPARK-42849 SQL SQL pipe syntax SPARK-49555 Sources Built-in XML data source SPARK-44265 PySpark Python Data Source API   PySpark Native plotting API   Streaming Arbitrary State API v2, and a state data source for debugging   Deployment Spark Kubernetes Operator SPARK-45923 ANSI mode by default is the change that will break jobs, and it is the right change. On 4.1.3: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;ansi&quot;).getOrCreate() print(spark.conf.get(&quot;spark.sql.ansi.enabled&quot;)) # true try: spark.sql(&quot;SELECT 1/0&quot;).collect() except Exception as e: print(type(e).__name__, str(e).split(&quot;\n&quot;)[0]) # ArithmeticException [DIVIDE_BY_ZERO] Division by zero. Use `try_divide` to tolerate ... spark.sql(&quot;SELECT try_divide(1, 0) AS v&quot;).show() # +----+ # | v| # +----+ # |NULL| # +----+ spark.stop() The same applies to casts: CAST(&apos;abc&apos; AS INT) now raises CAST_INVALID_INPUT rather than returning null, and try_cast gives you the old behaviour where you actually want it. The migration is mechanical and the benefit is that silent null-poisoning of a column stops being possible by accident. VARIANT is the answer to a question every lakehouse team has asked: how do you store JSON whose schema you do not control, without either flattening it or storing a string and paying to parse it on every read? VARIANT stores a parsed binary encoding with its own metadata dictionary. from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;variant&quot;).getOrCreate() spark.sql(&quot;&quot;&quot; SELECT variant_get(parse_json(&apos;{&quot;user&quot;:{&quot;id&quot;:7,&quot;city&quot;:&quot;pune&quot;}}&apos;), &apos;$.user.id&apos;, &apos;int&apos;) AS user_id &quot;&quot;&quot;).show() # +-------+ # |user_id| # +-------+ # | 7| # +-------+ spark.stop() Collations let a string column declare its own comparison rules, so case-insensitive matching stops requiring lower() on both sides and the optimizer keeps its ability to use the column: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;collation&quot;).getOrCreate() spark.sql(&quot;SELECT &apos;Spark&apos; COLLATE UTF8_LCASE = &apos;spark&apos; AS eq&quot;).show() # +----+ # | eq| # +----+ # |true| # +----+ spark.stop() SQL pipe syntax rewrites a query as a sequence of steps in execution order instead of a SELECT whose clauses run in an order that does not match how they are written. This is where my first guess was wrong. I wrote this: FROM VALUES (1), (2), (3) AS t(x) |&amp;gt; WHERE x &amp;gt; 1 |&amp;gt; SELECT sum(x) AS s and got [PIPE_OPERATOR_CONTAINS_AGGREGATE_FUNCTION]. Aggregation has its own pipe operator, and |&amp;gt; SELECT refuses to take an aggregate function. The correct form: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;pipe&quot;).getOrCreate() spark.sql(&quot;FROM VALUES (1), (2), (3) AS t(x) |&amp;gt; WHERE x &amp;gt; 1 |&amp;gt; AGGREGATE sum(x) AS s&quot;).show() # +---+ # | s| # +---+ # | 5| # +---+ spark.stop() SQL user-defined functions and session variables close a long-standing gap for teams whose logic lives in SQL rather than in Scala or Python: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;sqludf&quot;).getOrCreate() spark.sql(&quot;DECLARE OR REPLACE cutoff INT DEFAULT 100&quot;) spark.sql(&quot;SET VAR cutoff = 250&quot;) spark.sql(&quot;&quot;&quot; CREATE OR REPLACE TEMPORARY FUNCTION over_cutoff(amount INT) RETURNS BOOLEAN RETURN amount &amp;gt; cutoff &quot;&quot;&quot;) spark.sql(&quot;SELECT over_cutoff(300) AS big, over_cutoff(100) AS small&quot;).show() # +----+-----+ # | big|small| # +----+-----+ # |true|false| # +----+-----+ spark.stop() Spark 4.1, December 2025 Area What landed Ticket Pipelines Spark Declarative Pipelines SPARK-51727 Streaming Real-time mode, sub-second continuous processing SPARK-53736 SQL SQL scripting enabled by default, declared GA SPARK-54499 SQL VARIANT enabled by default, declared GA, with shredding SPARK-54454 SQL Recursive common table expressions   SQL Stored procedures API for catalogs SPARK-44167 SQL KLL and Theta approximate sketches   Connect JDBC driver for Spark Connect SPARK-53484 PySpark Arrow-native UDF and UDTF decorators SPARK-52214, SPARK-52979 Core Checksum-based full shuffle stage retry, to avoid incorrect results SPARK-51756 ML Spark ML on Connect generally available for the Python client SPARK-51236 Declarative Pipelines is the largest addition. You declare datasets and the queries that produce them; Spark derives the execution graph, the dependency order, the parallelism, the checkpoints and the retries. It ships with a spark-pipelines CLI. One practical note that cost me a few minutes, and is not in the release notes: the CLI is a Spark Connect client. On a stock apache/spark:4.1.3-python3 image it fails twice before it runs, first on PyYAML and then on grpcio: ModuleNotFoundError: No module named &apos;yaml&apos; ... PySparkImportError: [PACKAGE_NOT_INSTALLED] grpcio &amp;gt;= 1.48.1 must be installed; however, it was not found. pip install pyyaml grpcio fixes both. It is worth knowing that adopting Declarative Pipelines means adopting Spark Connect’s client dependencies, because that is an architectural commitment, not a packaging detail. SQL scripting being on by default turns Spark SQL into a procedural language. Local variables, control flow and multi-statement blocks run inside the engine rather than in a Python wrapper around it: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;scripting&quot;).getOrCreate() print(spark.conf.get(&quot;spark.sql.scripting.enabled&quot;)) # true spark.sql(&quot;&quot;&quot; BEGIN DECLARE total INT DEFAULT 0; SET total = (SELECT sum(x) FROM VALUES (1), (2), (3) AS t(x)); SELECT total AS grand_total; END &quot;&quot;&quot;).show() # +-----------+ # |grand_total| # +-----------+ # | 6| # +-----------+ spark.stop() Recursive CTEs arrived in the same release, which matters for anyone who has been faking a hierarchy traversal with a loop in Python: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;cte&quot;).getOrCreate() spark.sql(&quot;&quot;&quot; WITH RECURSIVE nums(n) AS ( SELECT 1 UNION ALL SELECT n + 1 FROM nums WHERE n &amp;lt; 5 ) SELECT collect_list(n) AS ns FROM nums &quot;&quot;&quot;).show() # +---------------+ # | ns| # +---------------+ # |[1, 2, 3, 4, 5]| # +---------------+ spark.stop() The Arrow UDF decorator is the endpoint of the line that started with pandas UDFs in 2.3. It takes and returns PyArrow arrays directly, with no pandas conversion in the middle: import pyarrow as pa from pyspark.sql import SparkSession from pyspark.sql.functions import arrow_udf spark = SparkSession.builder.appName(&quot;arrowudf&quot;).getOrCreate() @arrow_udf(&quot;long&quot;) def plus_one(v: pa.Array) -&amp;gt; pa.Array: return pa.compute.add(v, 1) spark.range(3).select(plus_one(&quot;id&quot;).alias(&quot;p&quot;)).show() # +---+ # | p| # +---+ # | 1| # | 2| # | 3| # +---+ spark.stop() Spark 4.2, July 2026 The current release. Area What landed Ticket Types Geospatial GEOMETRY and GEOGRAPHY types, on by default SPARK-51658 SQL Change data capture: a CHANGES clause plus DataFrame and Connect APIs SPARK-55668 Pipelines Auto CDC in Declarative Pipelines, declarative SCD Type 1 SPARK-56249 PySpark Arrow-optimized Python UDFs and Arrow IPC on by default SPARK-54555 SQL NEAREST BY top-k ranking join SPARK-56395 Sources Data Source V2 transaction management SPARK-55855 SQL Path-based name resolution: SET PATH, CURRENT_PATH() SPARK-54806 SQL Metric views SPARK-54119 UI Rebuilt web UI with dark mode and side-by-side plan comparison SPARK-55760 Build Builds and runs on Java 25 SPARK-51167 The Arrow default (SPARK-54555) is the change that affects everyone without being asked for. On 4.1.3, spark.sql.execution.arrow.pyspark.enabled is false; on 4.2.0 it is true, and spark.sql.execution.pythonUDF.arrow.enabled is true as well. Ordinary Python UDFs get the vectorized path without being rewritten. Geospatial support is where I have to be precise, because the release note summary and the shipped function registry do not quite agree. The note says “ST_* functions, WKB/WKT and Parquet read/write”. What is actually registered as a built-in in 4.2.0 is five functions: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;geo&quot;).getOrCreate() fns = [r[0] for r in spark.sql(&quot;SHOW FUNCTIONS&quot;).collect()] print(sorted(f for f in fns if f.startswith(&quot;st_&quot;))) # [&apos;st_asbinary&apos;, &apos;st_geogfromwkb&apos;, &apos;st_geomfromwkb&apos;, &apos;st_setsrid&apos;, &apos;st_srid&apos;] spark.stop() There is no ST_Point, no ST_AsText, and no ST_Distance in the built-in registry, and there is no cast from a WKT string either: CAST(&apos;POINT(1 2)&apos; AS GEOMETRY(4326)) fails with DATATYPE_MISMATCH.CAST_WITHOUT_SUGGESTION. The entry point in 4.2.0 is well-known binary. What does work, and is genuinely new, is that the spatial reference identifier is part of the type rather than a column beside it: from pyspark.sql import SparkSession spark = SparkSession.builder.appName(&quot;geo2&quot;).getOrCreate() # WKB for POINT(73.8567 18.5204), little-endian wkb = &quot;0101000000ed9e3c2cd4765240a1d634ef38853240&quot; spark.sql(f&quot;&quot;&quot; SELECT typeof(st_geomfromwkb(unhex(&apos;{wkb}&apos;))) AS geom_type, typeof(st_geogfromwkb(unhex(&apos;{wkb}&apos;))) AS geog_type, st_srid(st_setsrid(st_geomfromwkb(unhex(&apos;{wkb}&apos;)), 4326)) AS srid &quot;&quot;&quot;).show(truncate=False) # +-----------+---------------+----+ # |geom_type |geog_type |srid| # +-----------+---------------+----+ # |geometry(0)|geography(4326)|4326| # +-----------+---------------+----+ spark.stop() geometry(0) means SRID 0, an unspecified reference system. GEOGRAPHY defaults to 4326, which is WGS 84. Note also that the bare type name does not parse: CAST(NULL AS GEOMETRY) is a syntax error while CAST(NULL AS GEOMETRY(4326)) is fine, because the SRID is part of the type’s grammar. Change data capture is the other feature where running it is more informative than reading about it. The syntax is not the one I guessed; the grammar takes a version or timestamp range: SELECT * FROM sales CHANGES FROM VERSION 0 TO VERSION 1 On a built-in Parquet table that parses and analyses, then stops: [UNSUPPORTED_FEATURE.CHANGE_DATA_CAPTURE] The feature is not supported: Catalog spark_catalog does not support Change Data Capture (CDC). SQLSTATE: 0A000 That is the correct behaviour and it tells you exactly what the feature is. Spark 4.2 ships the SQL surface, the parser and the analyzer rules for CDC; the capability itself is delegated to the Data Source V2 catalog. You get change feeds when your table format implements them, which is the same division of labour Spark used for time travel. NEAREST BY does work on built-in tables, and its grammar is worth stating because it is easy to guess wrong: SELECT * FROM sales a JOIN sales b APPROX NEAREST 5 BY DISTANCE a.amount - b.amount The shape is (APPROX | EXACT) NEAREST &amp;lt;k&amp;gt; BY (DISTANCE | SIMILARITY) &amp;lt;expr&amp;gt;, attached where a join condition would go. It gives nearest-neighbour search a planner-visible operator instead of a cross join with a sort, which is how people have been doing vector search on Spark. Defaults that changed underneath you Features are opt-in. Defaults are not. This table is the one to check before an upgrade, and every current value in it was read from a live 4.1.3 or 4.2.0 session rather than from documentation. Setting Changed in From To Current value spark.shuffle.manager 1.2 hash sort sort, now internal and absent from the 4.1.3 configuration reference spark.shuffle.blockTransferService 1.2 nio netty netty only; the key is gone from the 4.1.3 configuration reference spark.io.compression.codec 1.1 lzf snappy lz4, changed again in a release the notes do not record spark.sql.autoBroadcastJoinThreshold 1.2 10000 10485760 10485760b spark.sql.shuffle.partitions no change found in any release note     200 spark.sql.orc.impl 2.4 hive native native spark.sql.adaptive.enabled 3.2 false true true spark.sql.optimizer.dynamicPartitionPruning.enabled 3.0   true true spark.sql.optimizer.runtime.bloomFilter.enabled 3.4 false true true spark.sql.ansi.enabled 4.0 false true true spark.sql.scripting.enabled 4.1 false true true spark.sql.execution.arrow.pyspark.enabled 4.2 false true false on 4.1.3, true on 4.2.0 spark.sql.execution.pythonUDF.arrow.enabled 4.2 false true true on 4.2.0 Three of these change results rather than performance. ANSI mode turns silent nulls into errors. The proleptic Gregorian calendar in 3.0 shifts pre-1582 dates. And the 2.3 fix for shuffle plus repartition (SPARK-23207) changed wrong answers into right ones, which is also a behaviour change if something downstream was tuned to the wrong ones. What was removed, and when Upgrades fail on removals more often than they fail on features. Removed Version Replacement Java 7 2.2 Java 8 Scala 2.10 2.3 Scala 2.11 Hadoop 2.5 and earlier 2.2 Hadoop 2.6 or later Python 3.7 3.5 Python 3.8 or later Python 3.8 4.0 Python 3.9 or later Scala 2.12 4.0 Scala 2.13 JDK 8 and 11 4.0 JDK 17 or later Mesos 4.0 Standalone, YARN or Kubernetes R 3.x 4.2 R 4.x SparkR was deprecated in 4.0 (SPARK-49347) and has not been removed: 4.2 still lists SparkR changes, including Java 25 support. Deprecated is not gone, but it is a clear signal about where to put new R work. The Scala 2.12 removal in 4.0 is the one that breaks builds hardest, because it is not a Spark-only change. Every JVM dependency you bring, including every connector, needs a 2.13 artifact. Common misconceptions “Adaptive query execution is a Spark 3 feature.” The idea shipped in 1.6 (SPARK-9858) and only chose reducer counts. The implementation you know was written for 3.0 (SPARK-31412) and switched on by default in 3.2 (SPARK-33679). The distinction matters when reading old tuning advice, which may be about the 1.6 version. “Structured Streaming arrived in 2.0.” It arrived as an experimental API in 2.0 and was declared generally available in 2.2 (SPARK-20844), two releases and a year later. Production advice written between those points is about a moving target. “Kubernetes has been supported since 2.3.” Experimentally. The release note for 2.3 says configurations, container images and entrypoints were expected to change. It went GA in 3.1 (SPARK-33005). “Spark 4 enables ANSI mode, so my job will fail loudly on bad data.” Only for the operations ANSI mode governs, which are arithmetic overflow, division by zero and invalid casts. A string that parses as a number but means something else still passes silently, as it should. “spark.sql.shuffle.partitions does not matter any more, because AQE coalesces.” AQE coalesces partitions down after the shuffle has been written. The value still decides how many partitions get written in the first place, and it is still 200. The plan output earlier in this post shows exactly that: Exchange hashpartitioning(bucket#1L, 200) followed by AQEShuffleRead coalesced. “Spark 4.2 ships geospatial functions, so I can port my PostGIS queries.” Five st_* functions are registered as built-ins in 4.2.0, and WKT is not among the entry points. Check SHOW FUNCTIONS against the queries you intend to port before planning the work. Choosing where to sit A short version of the advice, for the three positions most teams are actually in. On 2.4. The correctness fix in 2.3 (SPARK-23207) is behind you, which is the good news. Everything else argues for moving: no adaptive execution, no dynamic partition pruning, no Kubernetes GA, and a Scala version no current connector ships for. The jump to 3.5 is large but well trodden. On 3.1 to 3.3. The single biggest gain available to you is AQE by default in 3.2, and you can have most of it today by setting spark.sql.adaptive.enabled=true and measuring. Do that first; it tells you what the upgrade is worth before you spend it. On 3.5. You are on the practical long-term-support release, and it is still being maintained: 3.5.9 shipped on 16 July 2026, two days after 4.2.0. Moving to 4.x costs you a Scala 2.13 rebuild, a JDK 17 or later runtime, and an ANSI-mode audit. Budget the ANSI audit seriously and treat it as a data-quality exercise rather than a compatibility chore, because every error it raises is a place your pipeline was silently producing nulls. On 4.0 or 4.1. Moving within the 4.x line is ordinary. Watch for the Arrow defaults in 4.2 changing Python UDF behaviour, which is usually an improvement and is still a change worth a test run. Frequently asked questions Which Spark version should I move to from 2.4? 3.5 if you need a staged migration, because it keeps Scala 2.12 as an option and gets you adaptive execution, dynamic partition pruning and Kubernetes GA. 4.2 if you are willing to do the Scala 2.13 and JDK 17 work once rather than twice. Is Spark 3.5 still maintained? Yes. 3.5.9 was released on 16 July 2026, two days after 4.2.0. The 3.5 line is the practical long-term-support release for organisations that have not yet moved to 4.x. What actually breaks when I move to Spark 4? Four things, in descending order of how often they bite: Scala 2.12 artifacts that have no 2.13 build, a JDK older than 17, ANSI mode turning silent nulls into raised errors, and Mesos deployments having nowhere to go. The first two are build failures you find immediately. The third is a runtime failure you find in production unless you audit for it. Do I have to use Spark Connect on Spark 4? No. The classic in-process driver is still the default, and spark-submit works as it always has. Connect is opt-in, with one caveat: some newer components are Connect clients themselves, including the spark-pipelines CLI for Declarative Pipelines, which needs grpcio installed even when your queries do not. When did adaptive query execution actually become the default? 3.2, via SPARK-33679. It existed but was off by default in 3.0 and 3.1, and a much smaller version of the idea shipped in 1.6 as SPARK-9858. Tuning advice that predates 3.2 usually assumes it is off. Has SparkR been removed? Not yet. It was deprecated in 4.0 (SPARK-49347) and is still present in 4.2, which added Java 25 support to it. Support for R 3.x was dropped in 4.2 (SPARK-57767), so an old R runtime will stop you before the deprecation does. Why does the SQL pipe operator reject my aggregate function? Because |&amp;gt; SELECT does not accept aggregates. Use |&amp;gt; AGGREGATE instead. The error class is PIPE_OPERATOR_CONTAINS_AGGREGATE_FUNCTION and it names the fix. The mental model If you remember one thing, make it the shape rather than the list. Spark spent its first line learning that the RDD hid too much from the engine, its second line replacing it with a schema the engine could compile, its third line admitting that plans made from estimates are wrong and building a mechanism to revise them, and its fourth line detaching the client from the driver and pouring the accumulated capability back into SQL. Every feature in this post is an instance of one of those four moves. When the next release lands, that is the question to ask of each headline: which move is this, and does it change what my plans do or only what I am able to write? References Spark release archive, the index every release note in this post was read from Spark 3.0.0 release notes for adaptive query execution, dynamic partition pruning and the TPC-DS claim quoted above Spark 4.0.0 release notes for ANSI by default, VARIANT, collations and the Scala, JDK and Mesos removals Spark 4.1.0 release notes for Declarative Pipelines, real-time mode and SQL scripting Spark 4.2.0 release notes for geospatial types, change data capture and the Arrow defaults SqlBaseParser.g4, the grammar that settled the CHANGES and NEAREST BY syntax when my first guesses failed to parse Spark SQL migration guide for the behaviour changes each version introduced Apache Spark architecture for the runtime the shuffle and scheduling changes act on Adaptive query execution for what the 3.x re-planning does and does not revisit Trademarks Apache Spark, Apache Hudi, Apache Iceberg, Apache Parquet, Apache Avro, Apache ORC, Apache Arrow, Apache Kafka, Apache Hadoop, Apache Mesos, Apache YuniKorn and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Spark memory management: every region, and the arithmetic that sizes it</title><link href="https://rangareddy.github.io/SparkMemoryManagement/" rel="alternate" type="text/html" title="Spark memory management: every region, and the arithmetic that sizes it" /><published>2026-09-18T19:00:00+05:30</published><updated>2026-09-18T19:00:00+05:30</updated><id>https://rangareddy.github.io/SparkMemoryManagement</id><content type="html" xml:base="https://rangareddy.github.io/SparkMemoryManagement/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-what-the-cluster-manager-allocates&quot; id=&quot;markdown-toc-architecture-what-the-cluster-manager-allocates&quot;&gt;Architecture: what the cluster manager allocates&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-four-regions-inside-the-heap&quot; id=&quot;markdown-toc-the-four-regions-inside-the-heap&quot;&gt;The four regions inside the heap&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#checking-the-arithmetic-against-a-running-executor&quot; id=&quot;markdown-toc-checking-the-arithmetic-against-a-running-executor&quot;&gt;Checking the arithmetic against a running executor&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-elastic-boundary&quot; id=&quot;markdown-toc-the-elastic-boundary&quot;&gt;The elastic boundary&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-i-could-and-could-not-demonstrate&quot; id=&quot;markdown-toc-what-i-could-and-could-not-demonstrate&quot;&gt;What I could and could not demonstrate&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#off-heap-memory&quot; id=&quot;markdown-toc-off-heap-memory&quot;&gt;Off-heap memory&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#how-tasks-share-execution-memory&quot; id=&quot;markdown-toc-how-tasks-share-execution-memory&quot;&gt;How tasks share execution memory&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-one-hard-floor-450-mib&quot; id=&quot;markdown-toc-the-one-hard-floor-450-mib&quot;&gt;The one hard floor: 450 MiB&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#tuning-in-the-order-worth-trying&quot; id=&quot;markdown-toc-tuning-in-the-order-worth-trying&quot;&gt;Tuning, in the order worth trying&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-notes&quot; id=&quot;markdown-toc-production-notes&quot;&gt;Production notes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;An executor’s heap is split four ways: a fixed &lt;strong&gt;300 MB reserved&lt;/strong&gt;, a &lt;strong&gt;user&lt;/strong&gt; region Spark does not manage, and one &lt;strong&gt;unified pool&lt;/strong&gt; shared by execution and storage.&lt;/li&gt;
    &lt;li&gt;The sizes come from two fractions applied in order: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(heap - 300MB) * spark.memory.fraction&lt;/code&gt;, then &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;* spark.memory.storageFraction&lt;/code&gt; for storage’s protected share. On a 4 GB heap that is 2277.6 MiB unified, of which 1138.8 MiB is storage’s to keep.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Execution wins.&lt;/strong&gt; The boundary between execution and storage is elastic and only storage gets evicted, so cached data is never guaranteed to still be there.&lt;/li&gt;
    &lt;li&gt;Off-heap memory is &lt;strong&gt;additional&lt;/strong&gt;, not carved out of the heap, and the container needs a third allocation on top of both: &lt;strong&gt;memory overhead&lt;/strong&gt;, which is where JVM internals and Python live.&lt;/li&gt;
    &lt;li&gt;The container is killed for the total, not for the heap. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;heap + off-heap + overhead + PySpark&lt;/code&gt; is the number your cluster manager enforces.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;Memory is where Spark tuning stops being folklore and becomes arithmetic. Given
three configuration values you can compute every region size exactly, and given
the region sizes most memory failures become predictable rather than mysterious.&lt;/p&gt;

&lt;p&gt;The trouble is that the regions have confusing names, two of them overlap, one is
invisible to Spark, and the number the cluster manager kills you for is none of
the ones you configured. This post lays out each region, derives its size, checks
the derivation against a running executor, and then goes through what happens
when each one fills up.&lt;/p&gt;

&lt;p&gt;Every number below was read from a live session or from the Spark source. Where I
tried to demonstrate something and failed, the post says so.&lt;/p&gt;

&lt;h2 id=&quot;architecture-what-the-cluster-manager-allocates&quot;&gt;Architecture: what the cluster manager allocates&lt;/h2&gt;

&lt;p&gt;Start outside the JVM, because this is the level at which your job gets killed.
A container is three or four allocations, only one of which is the heap:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph C[&quot;executor container, what YARN or Kubernetes reserves&quot;]
    H[&quot;&amp;lt;b&amp;gt;JVM heap&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;spark.executor.memory&quot;]
    O[&quot;&amp;lt;b&amp;gt;off-heap&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;spark.memory.offHeap.size&amp;lt;br/&amp;gt;(0 unless enabled)&quot;]
    V[&quot;&amp;lt;b&amp;gt;memory overhead&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;spark.executor.memoryOverhead&amp;lt;br/&amp;gt;JVM internals, native buffers&quot;]
    P[&quot;&amp;lt;b&amp;gt;PySpark memory&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;spark.executor.pyspark.memory&amp;lt;br/&amp;gt;(only for Python)&quot;]
  end
  C --&amp;gt; K{&quot;total &amp;gt; container limit?&quot;}
  K --&amp;gt;|&quot;yes&quot;| X[&quot;container killed&amp;lt;br/&amp;gt;by the cluster manager&quot;]
  K --&amp;gt;|&quot;no&quot;| R[&quot;runs&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The point of that diagram is the arrow at the bottom. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt;
sizes the heap, and nothing else. The cluster manager enforces the sum, so a job
that raises &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;executor.memory&lt;/code&gt; to exactly the container limit is asking to be
killed, because overhead still has to fit somewhere.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Config&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Default&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Covers&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The JVM heap, and only the heap&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverhead&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;unset&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;JVM metaspace, thread stacks, native libraries, Python&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverheadFactor&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0.10&lt;/code&gt;, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0.40&lt;/code&gt; for non-JVM Kubernetes jobs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The overhead as a fraction of heap, when overhead is not set directly&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.minMemoryOverhead&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;384m&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The floor under the factor calculation&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.offHeap.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Whether Tungsten allocates outside the heap&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.offHeap.size&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;How much, when enabled&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;So overhead is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;max(executor.memory * 0.10, 384m)&lt;/code&gt; unless you set it directly, in
which case the factor and the minimum are both ignored. That is worth knowing
because the two derived knobs look like they still apply and they do not.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;A naming change worth pinning down.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.minMemoryOverhead&lt;/code&gt;
carries &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.version(&quot;4.0.0&quot;)&lt;/code&gt; in the config definitions, and it is absent from the
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;3.5.9&lt;/code&gt; source, where the same 384 MiB floor was the hardcoded constant
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ResourceProfile.MEMORY_OVERHEAD_MIN_MIB = 384L&lt;/code&gt;. The number has not changed; it
became configurable.&lt;/p&gt;

&lt;h2 id=&quot;the-four-regions-inside-the-heap&quot;&gt;The four regions inside the heap&lt;/h2&gt;

&lt;p&gt;Now inside the JVM. The heap is divided in a fixed order, and each division is a
simple multiplication:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;&amp;lt;b&amp;gt;JVM heap&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;for example 4096 MiB&quot;] --&amp;gt; B[&quot;&amp;lt;b&amp;gt;Reserved: 300 MB&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;fixed, not configurable&quot;]
  A --&amp;gt; C[&quot;&amp;lt;b&amp;gt;usable = heap - 300MB&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;3796 MiB&quot;]
  C --&amp;gt; D[&quot;&amp;lt;b&amp;gt;User memory&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;usable x (1 - memory.fraction)&amp;lt;br/&amp;gt;1518 MiB, unmanaged&quot;]
  C --&amp;gt; E[&quot;&amp;lt;b&amp;gt;Unified pool&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;usable x memory.fraction&amp;lt;br/&amp;gt;2277.6 MiB&quot;]
  E --&amp;gt; F[&quot;&amp;lt;b&amp;gt;Storage&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;protected share:&amp;lt;br/&amp;gt;pool x storageFraction&amp;lt;br/&amp;gt;1138.8 MiB&quot;]
  E --&amp;gt; G[&quot;&amp;lt;b&amp;gt;Execution&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the rest, and it can&amp;lt;br/&amp;gt;take storage&apos;s free space&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Each region has a different job and a different failure mode:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Region&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Size&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Holds&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;When it fills&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Reserved&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;300 MB, fixed&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Nothing. It is a safety margin for Spark’s own internals&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Not applicable&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;User&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;usable * (1 - memory.fraction)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Your objects: UDF state, data structures in closures, anything Spark does not track&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;, with no spill and no warning&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Storage&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pool * storageFraction&lt;/code&gt; protected&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Cached blocks, broadcast variables&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Blocks are evicted, then recomputed on next use&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Execution&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The rest of the pool&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle buffers, sorts, hash tables for joins and aggregations&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spills to disk&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;strong&gt;The user region is the one that surprises people.&lt;/strong&gt; Spark does not track it, so
it has no spill path and no eviction. If your UDF builds a large dictionary per
task, it competes for the 40% of usable heap that Spark is deliberately not
managing, and the failure is a plain &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;.&lt;/p&gt;

&lt;h2 id=&quot;checking-the-arithmetic-against-a-running-executor&quot;&gt;Checking the arithmetic against a running executor&lt;/h2&gt;

&lt;p&gt;The derivation above is easy to get wrong, so here it is checked against the
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MemoryManager&lt;/code&gt; in a live session with a 4 GB heap:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;mm&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;org&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;apache&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;SparkEnv&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;memoryManager&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;mb&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Long&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;${b / 1024.0 / 1024.0}%.1f MiB&quot;&lt;/span&gt;

&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;runtime maxMemory     = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;mb&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;Runtime&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getRuntime&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;maxMemory&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;maxOnHeapStorage      = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;mb&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;mm&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;maxOnHeapStorageMemory&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;memory manager class  = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;mm&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getClass&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getSimpleName&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;// the documented arithmetic, recomputed by hand&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;heap&lt;/span&gt;     &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;Runtime&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getRuntime&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;maxMemory&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;usable&lt;/span&gt;   &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;heap&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;-&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;300L&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1024&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1024&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;unified&lt;/span&gt;  &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;usable&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;0.6&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;toLong&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;usable  (heap - 300MB) = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;mb&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;usable&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;unified (x 0.6)        = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;mb&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unified&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;storage region (x 0.5) = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;mb&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unified&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;0.5&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;toLong&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;runtime maxMemory     = 4096.0 MiB
maxOnHeapStorage      = 2277.6 MiB
memory manager class  = UnifiedMemoryManager

usable  (heap - 300MB) = 3796.0 MiB
unified (x 0.6)        = 2277.6 MiB
storage region (x 0.5) = 1138.8 MiB
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The hand calculation matches &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxOnHeapStorageMemory&lt;/code&gt; exactly. And that raises
the point most diagrams get wrong.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxOnHeapStorageMemory&lt;/code&gt; is 2277.6 MiB, the whole unified pool, not the
1138.8 MiB storage region.&lt;/strong&gt; Storage is not capped at its fraction. The fraction
sets how much storage may &lt;em&gt;keep&lt;/em&gt; under pressure; with execution idle, cached data
can occupy the entire pool. The two numbers answer different questions:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.storageFraction&lt;/code&gt; is the share of the pool that execution &lt;strong&gt;cannot take back&lt;/strong&gt;.&lt;/li&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxOnHeapStorageMemory&lt;/code&gt; is the share that storage &lt;strong&gt;may grow into&lt;/strong&gt; when nothing else wants it.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Read &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;storageFraction&lt;/code&gt; as an eviction floor, not a size limit. That single
reframing makes the next section obvious.&lt;/p&gt;

&lt;h2 id=&quot;the-elastic-boundary&quot;&gt;The elastic boundary&lt;/h2&gt;

&lt;p&gt;Execution and storage share one pool, and the divide between them moves. The
rules are asymmetric and they are short enough to read directly.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  S1[&quot;storage using less&amp;lt;br/&amp;gt;than its share&quot;] --&amp;gt;|&quot;execution needs memory&quot;| S2[&quot;execution takes&amp;lt;br/&amp;gt;the free space&quot;]
  S3[&quot;storage borrowed past&amp;lt;br/&amp;gt;its protected share&quot;] --&amp;gt;|&quot;execution needs memory&quot;| S4[&quot;blocks are &amp;lt;b&amp;gt;evicted&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;until storage is back&amp;lt;br/&amp;gt;to its share&quot;]
  E1[&quot;execution using more&amp;lt;br/&amp;gt;than half the pool&quot;] --&amp;gt;|&quot;storage needs memory&quot;| E2[&quot;storage &amp;lt;b&amp;gt;waits&amp;lt;/b&amp;gt;.&amp;lt;br/&amp;gt;Execution is never evicted,&amp;lt;br/&amp;gt;it only spills on its own terms&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;From &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UnifiedMemoryManager&lt;/code&gt;, this is what execution is allowed to reclaim:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;        &lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;memoryReclaimableFromStorage&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;math&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;
          &lt;span class=&quot;nv&quot;&gt;storagePool&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;memoryFree&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt;
          &lt;span class=&quot;nv&quot;&gt;storagePool&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;poolSize&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;-&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;storageRegionSize&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;max&lt;/code&gt; of two things: storage’s free space, and however much storage has
borrowed beyond its region. So execution can always take back what it lent, plus
anything storage is not currently using.&lt;/p&gt;

&lt;p&gt;And this is the cap on how large the execution pool may become:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;    &lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;computeMaxExecutionPoolSize&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;()&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Long&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
      &lt;span class=&quot;n&quot;&gt;maxMemory&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;-&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;math&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;min&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;storagePool&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;memoryUsed&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;storageRegionSize&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;min(storage used, storage region)&lt;/code&gt; is exactly the protected part. Execution may
grow into everything except that.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;There is no symmetric rule for storage.&lt;/strong&gt; Nothing in the manager evicts
execution memory, because a half-built hash table cannot be dropped and
recomputed cheaply the way a cached block can. Execution releases memory by
spilling to disk, and it decides when.&lt;/p&gt;

&lt;p&gt;The practical consequence: &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache()&lt;/code&gt; is a hint, not a guarantee.&lt;/strong&gt; A cached
DataFrame that fitted yesterday can be partly evicted today because a different
stage in the same application wanted execution memory.&lt;/p&gt;

&lt;h2 id=&quot;what-i-could-and-could-not-demonstrate&quot;&gt;What I could and could not demonstrate&lt;/h2&gt;

&lt;p&gt;Two claims from the section above are easy to state and harder to produce on
demand, so here is what actually happened when I tried.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Storage borrowing past its region: confirmed.&lt;/strong&gt; With a 1 GB driver heap, so a
434 MiB unified pool and a 217 MiB storage region, caching 7,000,000 rows put
306 MiB into storage across 8 blocks, comfortably past the region:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;pool=434 MiB  region=217 MiB
after caching: storage=306 MiB blocks=8  beyond region=true
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Eviction under execution pressure: not reproduced.&lt;/strong&gt; I then ran sorts designed
to demand execution memory, up to 30,000,000 rows over 8 concurrent tasks, and
nothing was evicted:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;after sort  : storage=306 MiB blocks=8  evicted=0
peakExecutionMemory (max task) = 0 MiB
memoryBytesSpilled  (total)    = 0 MiB
diskBytesSpilled    (total)    = 0 MiB
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Zero spill is the explanation: there was never real pressure, so nothing needed
reclaiming. The mechanism is in the source above and I am not going to claim I
watched it fire when I did not. If you want to see eviction on your own cluster,
the observable signals are the storage memory dropping in the executors tab and
cached partitions falling below 100% in the storage tab.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;A different way cache fails, and this one did reproduce.&lt;/strong&gt; Caching more than
the pool can hold with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_ONLY&lt;/code&gt; does not raise anything. It silently caches
nothing:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;# 20,000,000 rows into a 434 MiB pool
after caching: storage=0 MiB  blocks=0
cached count = 20000000     # still correct, recomputed from source
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Zero blocks cached, and the count is still right because Spark recomputed the
whole thing. A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache()&lt;/code&gt; call that achieves nothing while your job still produces
correct answers is the worst kind of performance bug, because nothing fails. The
storage tab showing a cached RDD at 0% is the tell, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK&lt;/code&gt; is the
usual fix.&lt;/p&gt;

&lt;h2 id=&quot;off-heap-memory&quot;&gt;Off-heap memory&lt;/h2&gt;

&lt;p&gt;Off-heap is Tungsten allocating with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sun.misc.Unsafe&lt;/code&gt; outside the Java heap, so
the data is not subject to garbage collection and is stored in Spark’s own binary
format rather than as Java objects.&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;// started with --conf spark.memory.offHeap.enabled=true --conf spark.memory.offHeap.size=1g&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;maxOnHeapStorage   = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;mb&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;mm&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;maxOnHeapStorageMemory&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;maxOffHeapStorage  = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;mb&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;mm&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;maxOffHeapStorageMemory&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;tungstenMemoryMode = &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;mm&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;tungstenMemoryMode&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;maxOnHeapStorage   = 2277.6 MiB
maxOffHeapStorage  = 1024.0 MiB
tungstenMemoryMode = OFF_HEAP
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Three things to take from that output:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Off-heap is additional.&lt;/strong&gt; The on-heap pool is unchanged at 2277.6 MiB. Enabling 1 GB off-heap did not shrink the heap; it added a second pool, and the container now needs 1 GB more than before.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;It has its own execution and storage split&lt;/strong&gt;, governed by the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;storageFraction&lt;/code&gt;, so the same elastic rules apply within it.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;tungstenMemoryMode&lt;/code&gt; flips to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OFF_HEAP&lt;/code&gt;&lt;/strong&gt;, which is what actually changes where sorts and hash tables allocate.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The trade is GC pressure against manual accounting. Off-heap memory is invisible
to the JVM, so a leak or an underestimate shows up as the cluster manager killing
the container rather than as an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; you can read a stack trace
from.&lt;/p&gt;

&lt;h2 id=&quot;how-tasks-share-execution-memory&quot;&gt;How tasks share execution memory&lt;/h2&gt;

&lt;p&gt;One more division, inside the execution pool. Several tasks run per executor and
they compete, with a fairness rule in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ExecutionMemoryPool&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;      &lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;maxPoolSize&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;computeMaxPoolSize&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;()&lt;/span&gt;
      &lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;maxMemoryPerTask&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;maxPoolSize&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;numActiveTasks&lt;/span&gt;
      &lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;minMemoryPerTask&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;poolSize&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;numActiveTasks&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  P[&quot;execution pool, N active tasks&quot;] --&amp;gt; A[&quot;each task capped at &amp;lt;b&amp;gt;1/N&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;of the pool&quot;]
  P --&amp;gt; B[&quot;each task guaranteed at least &amp;lt;b&amp;gt;1/2N&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;before it is made to wait&quot;]
  B --&amp;gt; C[&quot;below 1/2N, the task blocks&amp;lt;br/&amp;gt;until another releases memory&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So a task can never take more than &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1/N&lt;/code&gt; of the pool, and it will not be blocked
until it has at least &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1/2N&lt;/code&gt;. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;N&lt;/code&gt; is the number of &lt;em&gt;active&lt;/em&gt; tasks, not the core
count, so the guarantee moves as tasks start and finish.&lt;/p&gt;

&lt;p&gt;This is why &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.cores&lt;/code&gt; is a memory setting as much as a parallelism
setting. Doubling the cores per executor halves each task’s share of the same
pool, which is the usual reason a job that worked with 4 cores per executor
spills constantly with 8.&lt;/p&gt;

&lt;h2 id=&quot;the-one-hard-floor-450-mib&quot;&gt;The one hard floor: 450 MiB&lt;/h2&gt;

&lt;p&gt;Spark refuses to start if the heap cannot cover the reserved region with room to
spare. From &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UnifiedMemoryManager&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;  &lt;span class=&quot;k&quot;&gt;private&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;RESERVED_SYSTEM_MEMORY_BYTES&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;300&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1024&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1024&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;...&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;minSystemMemory&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;reservedMemory&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;1.5&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;ceil&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;toLong&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;300 MB * 1.5&lt;/code&gt; is 450 MiB, and there are two separate checks. A 400 MB driver:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;[INVALID_DRIVER_MEMORY] System memory 419430400 must be at least 471859200.
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;and a 400 MB executor:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;[INVALID_EXECUTOR_MEMORY] Executor memory 419430400 must be at least 471859200.
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;471859200&lt;/code&gt; is exactly 450 MiB. This is a fail-fast check added deliberately, and
it is a good thing: below that size the unified pool would be a few tens of
megabytes and every job would spill pathologically instead of failing clearly.&lt;/p&gt;

&lt;h2 id=&quot;tuning-in-the-order-worth-trying&quot;&gt;Tuning, in the order worth trying&lt;/h2&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Symptom&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;First move&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Container killed, exit 137, “memory overhead exceeded”&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverhead&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The heap was never the problem; the sum exceeded the container&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Heavy spill during joins and aggregations&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Fewer cores per executor, or raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.fraction&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Each task gets &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1/N&lt;/code&gt; of the pool, so N is the lever&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Cached data keeps being recomputed&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.storageFraction&lt;/code&gt;, or use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The fraction is the eviction floor&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; with no spill in the logs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Look at user memory, not the pool&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark does not manage or spill your own objects&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;GC pauses dominating&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Enable off-heap, or shrink the heap and add executors&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Off-heap data is not scanned by the collector&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;PySpark workers killed&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.pyspark.memory&lt;/code&gt;, and raise overhead&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Python lives outside the JVM entirely&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The ordering matters more than any individual row. Overhead problems masquerade
as heap problems, and the first question for any kill is always whether the JVM
died or the container was terminated from outside.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.storageFraction&lt;/code&gt; caps how much I can cache.”&lt;/strong&gt; It does not. It
sets the share of the unified pool that execution is not allowed to evict.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxOnHeapStorageMemory&lt;/code&gt; on the 4 GB heap above reports 2277.6 MiB, the whole
pool, not the 1138.8 MiB storage region, because storage can grow into the
entire pool when execution is idle. Read the fraction as an eviction floor.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Enabling off-heap memory takes it out of the heap.”&lt;/strong&gt; It is additional.
Turning on 1 GB of off-heap left the on-heap pool unchanged at 2277.6 MiB and
added a second pool of 1024.0 MiB. The container now needs a gigabyte more than
before, which is how enabling off-heap gets a job killed by the cluster manager
without a single change to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“The container was killed, so the heap was too small.”&lt;/strong&gt; The cluster manager
enforces &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;heap + off-heap + overhead + PySpark&lt;/code&gt;, and only the first of those is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt;. A heap sitting at 60% with Python and native buffers
pushing the container over its limit is killed with no JVM &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; at
all, usually as exit code 137.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“An &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; means I should raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.fraction&lt;/code&gt;.”&lt;/strong&gt; Not if
the memory went to your own objects. The user region, the part Spark does not
manage, has no spill path and no eviction, so a UDF building a large structure
per task fails outright. Raising the managed fraction makes that region
&lt;em&gt;smaller&lt;/em&gt;.&lt;/p&gt;

&lt;h2 id=&quot;production-notes&quot;&gt;Production notes&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Size the container, not the heap.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;heap + off-heap + overhead + PySpark&lt;/code&gt; is the number enforced, so leave headroom rather than setting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;executor.memory&lt;/code&gt; to the limit.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Setting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverhead&lt;/code&gt; directly disables both derived knobs.&lt;/strong&gt; The factor and the minimum are ignored once it is explicit.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Treat &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;storageFraction&lt;/code&gt; as an eviction floor.&lt;/strong&gt; It does not cap how much cache can grow, only how much survives pressure.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Never assume a cached DataFrame is resident.&lt;/strong&gt; Check the storage tab for the cached fraction before attributing a slow stage to something else.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK&lt;/code&gt; unless you have measured that the data fits.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_ONLY&lt;/code&gt; silently caches nothing when it does not, and the job stays correct while getting slower.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Treat &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.cores&lt;/code&gt; as a memory knob.&lt;/strong&gt; It divides the execution pool &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1/N&lt;/code&gt;, so raising it can cause spill with no other change.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Account for off-heap twice.&lt;/strong&gt; Once in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;offHeap.size&lt;/code&gt; and once in the container total. It is additional to the heap, not carved from it.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Do not tune &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.fraction&lt;/code&gt; first.&lt;/strong&gt; It trades user memory against managed memory, and the common problems are overhead and core count.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Why is my container killed when the heap looks fine?&lt;/strong&gt;
Because the cluster manager enforces the total, and overhead is not part of
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt;. A heap sitting at 60% with off-heap and Python pushing
the container over its limit gets killed with no JVM &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; at all,
usually as exit code 137.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;What is actually in the 300 MB reserved region?&lt;/strong&gt;
Nothing of yours. It is a margin so that Spark’s own internal structures cannot
be squeezed out by execution and storage. It is not configurable outside tests,
and the 450 MiB startup floor exists to keep it meaningful.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Does &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.storageFraction&lt;/code&gt; limit how much I can cache?&lt;/strong&gt;
No, and this is the most common misreading. It sets the portion of the unified
pool that execution cannot evict. With execution idle, cache can occupy the whole
pool, which is why &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxOnHeapStorageMemory&lt;/code&gt; reports the full pool size.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Can storage evict execution?&lt;/strong&gt;
No. The asymmetry is deliberate: a cached block can be dropped and recomputed,
while a partially built hash table cannot. Execution gives memory back by
spilling, on its own schedule.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Is off-heap memory faster?&lt;/strong&gt;
It is not automatically faster. It removes garbage-collection pressure and stores
data compactly, which helps on large heaps, and it costs you the JVM’s memory
accounting. A leak becomes a killed container instead of a stack trace.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Where does PySpark memory come from?&lt;/strong&gt;
Outside the JVM, from the overhead allocation, unless you set
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.pyspark.memory&lt;/code&gt; to reserve it explicitly. A Python UDF holding a
large object is not visible in any Spark memory metric.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why did my job start spilling after I gave executors more cores?&lt;/strong&gt;
Because each task is capped at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1/N&lt;/code&gt; of the execution pool where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;N&lt;/code&gt; is the
active task count. More cores means more concurrent tasks and a smaller share
each, from the same pool.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;The useful way to hold all of this is as one sum and one asymmetry.&lt;/p&gt;

&lt;p&gt;The sum is the container: heap, off-heap, overhead and Python, of which
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt; is only the first. Most memory incidents that look
mysterious are the cluster manager enforcing that sum while you were watching the
heap.&lt;/p&gt;

&lt;p&gt;The asymmetry is inside the unified pool: execution can take memory from storage,
and storage can never take it from execution. Every consequence follows from
that. Cached data is evictable, so caching is a hint. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;storageFraction&lt;/code&gt; is a
floor rather than a ceiling, so it does not limit cache growth. Execution handles
its own shortfall by spilling, which is why spill is a tuning signal rather than
an error. Once those two ideas are in place the configuration stops being a list
of fractions to try and becomes arithmetic you can do before changing anything:
compute the pool from the heap, divide by the concurrent task count, and compare
it with what one task actually needs. That prediction beats trial and error.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/configuration.html&quot;&gt;Spark configuration reference&lt;/a&gt; for the memory and overhead settings and their defaults&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/core/src/main/scala/org/apache/spark/memory/UnifiedMemoryManager.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UnifiedMemoryManager.scala&lt;/code&gt;&lt;/a&gt;, which holds the 300 MB constant, the 450 MiB check and the borrowing rules quoted above&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/core/src/main/scala/org/apache/spark/memory/ExecutionMemoryPool.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ExecutionMemoryPool.scala&lt;/code&gt;&lt;/a&gt; for the per-task &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1/N&lt;/code&gt; cap and the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1/2N&lt;/code&gt; guarantee&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://luminousmen.com/post/dive-into-spark-memory/&quot;&gt;Dive into Spark memory&lt;/a&gt;, whose region-by-region breakdown is the shape this post follows&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheSparkArchitecture/&quot;&gt;Apache Spark architecture&lt;/a&gt; for the executor and task machinery these pools belong to&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Hadoop and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="Memory" /><category term="Tuning" /><category term="Internals" /><category term="Performance" /><summary type="html">Architecture: what the cluster manager allocates The four regions inside the heap Checking the arithmetic against a running executor The elastic boundary What I could and could not demonstrate Off-heap memory How tasks share execution memory The one hard floor: 450 MiB Tuning, in the order worth trying Common misconceptions Production notes Frequently asked questions Conclusion References Trademarks TL;DR An executor’s heap is split four ways: a fixed 300 MB reserved, a user region Spark does not manage, and one unified pool shared by execution and storage. The sizes come from two fractions applied in order: (heap - 300MB) * spark.memory.fraction, then * spark.memory.storageFraction for storage’s protected share. On a 4 GB heap that is 2277.6 MiB unified, of which 1138.8 MiB is storage’s to keep. Execution wins. The boundary between execution and storage is elastic and only storage gets evicted, so cached data is never guaranteed to still be there. Off-heap memory is additional, not carved out of the heap, and the container needs a third allocation on top of both: memory overhead, which is where JVM internals and Python live. The container is killed for the total, not for the heap. heap + off-heap + overhead + PySpark is the number your cluster manager enforces. Memory is where Spark tuning stops being folklore and becomes arithmetic. Given three configuration values you can compute every region size exactly, and given the region sizes most memory failures become predictable rather than mysterious. The trouble is that the regions have confusing names, two of them overlap, one is invisible to Spark, and the number the cluster manager kills you for is none of the ones you configured. This post lays out each region, derives its size, checks the derivation against a running executor, and then goes through what happens when each one fills up. Every number below was read from a live session or from the Spark source. Where I tried to demonstrate something and failed, the post says so. Architecture: what the cluster manager allocates Start outside the JVM, because this is the level at which your job gets killed. A container is three or four allocations, only one of which is the heap: flowchart TB subgraph C[&quot;executor container, what YARN or Kubernetes reserves&quot;] H[&quot;&amp;lt;b&amp;gt;JVM heap&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;spark.executor.memory&quot;] O[&quot;&amp;lt;b&amp;gt;off-heap&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;spark.memory.offHeap.size&amp;lt;br/&amp;gt;(0 unless enabled)&quot;] V[&quot;&amp;lt;b&amp;gt;memory overhead&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;spark.executor.memoryOverhead&amp;lt;br/&amp;gt;JVM internals, native buffers&quot;] P[&quot;&amp;lt;b&amp;gt;PySpark memory&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;spark.executor.pyspark.memory&amp;lt;br/&amp;gt;(only for Python)&quot;] end C --&amp;gt; K{&quot;total &amp;gt; container limit?&quot;} K --&amp;gt;|&quot;yes&quot;| X[&quot;container killed&amp;lt;br/&amp;gt;by the cluster manager&quot;] K --&amp;gt;|&quot;no&quot;| R[&quot;runs&quot;] The point of that diagram is the arrow at the bottom. spark.executor.memory sizes the heap, and nothing else. The cluster manager enforces the sum, so a job that raises executor.memory to exactly the container limit is asking to be killed, because overhead still has to fit somewhere. Config Default Covers spark.executor.memory 1g The JVM heap, and only the heap spark.executor.memoryOverhead unset JVM metaspace, thread stacks, native libraries, Python spark.executor.memoryOverheadFactor 0.10, and 0.40 for non-JVM Kubernetes jobs The overhead as a fraction of heap, when overhead is not set directly spark.executor.minMemoryOverhead 384m The floor under the factor calculation spark.memory.offHeap.enabled false Whether Tungsten allocates outside the heap spark.memory.offHeap.size 0 How much, when enabled So overhead is max(executor.memory * 0.10, 384m) unless you set it directly, in which case the factor and the minimum are both ignored. That is worth knowing because the two derived knobs look like they still apply and they do not. A naming change worth pinning down. spark.executor.minMemoryOverhead carries .version(&quot;4.0.0&quot;) in the config definitions, and it is absent from the 3.5.9 source, where the same 384 MiB floor was the hardcoded constant ResourceProfile.MEMORY_OVERHEAD_MIN_MIB = 384L. The number has not changed; it became configurable. The four regions inside the heap Now inside the JVM. The heap is divided in a fixed order, and each division is a simple multiplication: flowchart TB A[&quot;&amp;lt;b&amp;gt;JVM heap&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;for example 4096 MiB&quot;] --&amp;gt; B[&quot;&amp;lt;b&amp;gt;Reserved: 300 MB&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;fixed, not configurable&quot;] A --&amp;gt; C[&quot;&amp;lt;b&amp;gt;usable = heap - 300MB&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;3796 MiB&quot;] C --&amp;gt; D[&quot;&amp;lt;b&amp;gt;User memory&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;usable x (1 - memory.fraction)&amp;lt;br/&amp;gt;1518 MiB, unmanaged&quot;] C --&amp;gt; E[&quot;&amp;lt;b&amp;gt;Unified pool&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;usable x memory.fraction&amp;lt;br/&amp;gt;2277.6 MiB&quot;] E --&amp;gt; F[&quot;&amp;lt;b&amp;gt;Storage&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;protected share:&amp;lt;br/&amp;gt;pool x storageFraction&amp;lt;br/&amp;gt;1138.8 MiB&quot;] E --&amp;gt; G[&quot;&amp;lt;b&amp;gt;Execution&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;the rest, and it can&amp;lt;br/&amp;gt;take storage&apos;s free space&quot;] Each region has a different job and a different failure mode: Region Size Holds When it fills Reserved 300 MB, fixed Nothing. It is a safety margin for Spark’s own internals Not applicable User usable * (1 - memory.fraction) Your objects: UDF state, data structures in closures, anything Spark does not track OutOfMemoryError, with no spill and no warning Storage pool * storageFraction protected Cached blocks, broadcast variables Blocks are evicted, then recomputed on next use Execution The rest of the pool Shuffle buffers, sorts, hash tables for joins and aggregations Spills to disk The user region is the one that surprises people. Spark does not track it, so it has no spill path and no eviction. If your UDF builds a large dictionary per task, it competes for the 40% of usable heap that Spark is deliberately not managing, and the failure is a plain OutOfMemoryError. Checking the arithmetic against a running executor The derivation above is easy to get wrong, so here it is checked against the MemoryManager in a live session with a 4 GB heap: val mm = org.apache.spark.SparkEnv.get.memoryManager def mb(b: Long) = f&quot;${b / 1024.0 / 1024.0}%.1f MiB&quot; println(&quot;runtime maxMemory = &quot; + mb(Runtime.getRuntime.maxMemory)) println(&quot;maxOnHeapStorage = &quot; + mb(mm.maxOnHeapStorageMemory)) println(&quot;memory manager class = &quot; + mm.getClass.getSimpleName) // the documented arithmetic, recomputed by hand val heap = Runtime.getRuntime.maxMemory val usable = heap - 300L * 1024 * 1024 val unified = (usable * 0.6).toLong println(&quot;usable (heap - 300MB) = &quot; + mb(usable)) println(&quot;unified (x 0.6) = &quot; + mb(unified)) println(&quot;storage region (x 0.5) = &quot; + mb((unified * 0.5).toLong)) runtime maxMemory = 4096.0 MiB maxOnHeapStorage = 2277.6 MiB memory manager class = UnifiedMemoryManager usable (heap - 300MB) = 3796.0 MiB unified (x 0.6) = 2277.6 MiB storage region (x 0.5) = 1138.8 MiB The hand calculation matches maxOnHeapStorageMemory exactly. And that raises the point most diagrams get wrong. maxOnHeapStorageMemory is 2277.6 MiB, the whole unified pool, not the 1138.8 MiB storage region. Storage is not capped at its fraction. The fraction sets how much storage may keep under pressure; with execution idle, cached data can occupy the entire pool. The two numbers answer different questions: spark.memory.storageFraction is the share of the pool that execution cannot take back. maxOnHeapStorageMemory is the share that storage may grow into when nothing else wants it. Read storageFraction as an eviction floor, not a size limit. That single reframing makes the next section obvious. The elastic boundary Execution and storage share one pool, and the divide between them moves. The rules are asymmetric and they are short enough to read directly. flowchart TB S1[&quot;storage using less&amp;lt;br/&amp;gt;than its share&quot;] --&amp;gt;|&quot;execution needs memory&quot;| S2[&quot;execution takes&amp;lt;br/&amp;gt;the free space&quot;] S3[&quot;storage borrowed past&amp;lt;br/&amp;gt;its protected share&quot;] --&amp;gt;|&quot;execution needs memory&quot;| S4[&quot;blocks are &amp;lt;b&amp;gt;evicted&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;until storage is back&amp;lt;br/&amp;gt;to its share&quot;] E1[&quot;execution using more&amp;lt;br/&amp;gt;than half the pool&quot;] --&amp;gt;|&quot;storage needs memory&quot;| E2[&quot;storage &amp;lt;b&amp;gt;waits&amp;lt;/b&amp;gt;.&amp;lt;br/&amp;gt;Execution is never evicted,&amp;lt;br/&amp;gt;it only spills on its own terms&quot;] From UnifiedMemoryManager, this is what execution is allowed to reclaim: val memoryReclaimableFromStorage = math.max( storagePool.memoryFree, storagePool.poolSize - storageRegionSize) The max of two things: storage’s free space, and however much storage has borrowed beyond its region. So execution can always take back what it lent, plus anything storage is not currently using. And this is the cap on how large the execution pool may become: def computeMaxExecutionPoolSize(): Long = { maxMemory - math.min(storagePool.memoryUsed, storageRegionSize) } min(storage used, storage region) is exactly the protected part. Execution may grow into everything except that. There is no symmetric rule for storage. Nothing in the manager evicts execution memory, because a half-built hash table cannot be dropped and recomputed cheaply the way a cached block can. Execution releases memory by spilling to disk, and it decides when. The practical consequence: cache() is a hint, not a guarantee. A cached DataFrame that fitted yesterday can be partly evicted today because a different stage in the same application wanted execution memory. What I could and could not demonstrate Two claims from the section above are easy to state and harder to produce on demand, so here is what actually happened when I tried. Storage borrowing past its region: confirmed. With a 1 GB driver heap, so a 434 MiB unified pool and a 217 MiB storage region, caching 7,000,000 rows put 306 MiB into storage across 8 blocks, comfortably past the region: pool=434 MiB region=217 MiB after caching: storage=306 MiB blocks=8 beyond region=true Eviction under execution pressure: not reproduced. I then ran sorts designed to demand execution memory, up to 30,000,000 rows over 8 concurrent tasks, and nothing was evicted: after sort : storage=306 MiB blocks=8 evicted=0 peakExecutionMemory (max task) = 0 MiB memoryBytesSpilled (total) = 0 MiB diskBytesSpilled (total) = 0 MiB Zero spill is the explanation: there was never real pressure, so nothing needed reclaiming. The mechanism is in the source above and I am not going to claim I watched it fire when I did not. If you want to see eviction on your own cluster, the observable signals are the storage memory dropping in the executors tab and cached partitions falling below 100% in the storage tab. A different way cache fails, and this one did reproduce. Caching more than the pool can hold with MEMORY_ONLY does not raise anything. It silently caches nothing: # 20,000,000 rows into a 434 MiB pool after caching: storage=0 MiB blocks=0 cached count = 20000000 # still correct, recomputed from source Zero blocks cached, and the count is still right because Spark recomputed the whole thing. A cache() call that achieves nothing while your job still produces correct answers is the worst kind of performance bug, because nothing fails. The storage tab showing a cached RDD at 0% is the tell, and MEMORY_AND_DISK is the usual fix. Off-heap memory Off-heap is Tungsten allocating with sun.misc.Unsafe outside the Java heap, so the data is not subject to garbage collection and is stored in Spark’s own binary format rather than as Java objects. // started with --conf spark.memory.offHeap.enabled=true --conf spark.memory.offHeap.size=1g println(&quot;maxOnHeapStorage = &quot; + mb(mm.maxOnHeapStorageMemory)) println(&quot;maxOffHeapStorage = &quot; + mb(mm.maxOffHeapStorageMemory)) println(&quot;tungstenMemoryMode = &quot; + mm.tungstenMemoryMode) maxOnHeapStorage = 2277.6 MiB maxOffHeapStorage = 1024.0 MiB tungstenMemoryMode = OFF_HEAP Three things to take from that output: Off-heap is additional. The on-heap pool is unchanged at 2277.6 MiB. Enabling 1 GB off-heap did not shrink the heap; it added a second pool, and the container now needs 1 GB more than before. It has its own execution and storage split, governed by the same storageFraction, so the same elastic rules apply within it. tungstenMemoryMode flips to OFF_HEAP, which is what actually changes where sorts and hash tables allocate. The trade is GC pressure against manual accounting. Off-heap memory is invisible to the JVM, so a leak or an underestimate shows up as the cluster manager killing the container rather than as an OutOfMemoryError you can read a stack trace from. How tasks share execution memory One more division, inside the execution pool. Several tasks run per executor and they compete, with a fairness rule in ExecutionMemoryPool: val maxPoolSize = computeMaxPoolSize() val maxMemoryPerTask = maxPoolSize / numActiveTasks val minMemoryPerTask = poolSize / (2 * numActiveTasks) flowchart TB P[&quot;execution pool, N active tasks&quot;] --&amp;gt; A[&quot;each task capped at &amp;lt;b&amp;gt;1/N&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;of the pool&quot;] P --&amp;gt; B[&quot;each task guaranteed at least &amp;lt;b&amp;gt;1/2N&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;before it is made to wait&quot;] B --&amp;gt; C[&quot;below 1/2N, the task blocks&amp;lt;br/&amp;gt;until another releases memory&quot;] So a task can never take more than 1/N of the pool, and it will not be blocked until it has at least 1/2N. N is the number of active tasks, not the core count, so the guarantee moves as tasks start and finish. This is why spark.executor.cores is a memory setting as much as a parallelism setting. Doubling the cores per executor halves each task’s share of the same pool, which is the usual reason a job that worked with 4 cores per executor spills constantly with 8. The one hard floor: 450 MiB Spark refuses to start if the heap cannot cover the reserved region with room to spare. From UnifiedMemoryManager: private val RESERVED_SYSTEM_MEMORY_BYTES = 300 * 1024 * 1024 ... val minSystemMemory = (reservedMemory * 1.5).ceil.toLong 300 MB * 1.5 is 450 MiB, and there are two separate checks. A 400 MB driver: [INVALID_DRIVER_MEMORY] System memory 419430400 must be at least 471859200. and a 400 MB executor: [INVALID_EXECUTOR_MEMORY] Executor memory 419430400 must be at least 471859200. 471859200 is exactly 450 MiB. This is a fail-fast check added deliberately, and it is a good thing: below that size the unified pool would be a few tens of megabytes and every job would spill pathologically instead of failing clearly. Tuning, in the order worth trying Symptom First move Why Container killed, exit 137, “memory overhead exceeded” Raise spark.executor.memoryOverhead The heap was never the problem; the sum exceeded the container Heavy spill during joins and aggregations Fewer cores per executor, or raise spark.memory.fraction Each task gets 1/N of the pool, so N is the lever Cached data keeps being recomputed Raise spark.memory.storageFraction, or use MEMORY_AND_DISK The fraction is the eviction floor OutOfMemoryError with no spill in the logs Look at user memory, not the pool Spark does not manage or spill your own objects GC pauses dominating Enable off-heap, or shrink the heap and add executors Off-heap data is not scanned by the collector PySpark workers killed spark.executor.pyspark.memory, and raise overhead Python lives outside the JVM entirely The ordering matters more than any individual row. Overhead problems masquerade as heap problems, and the first question for any kill is always whether the JVM died or the container was terminated from outside. Common misconceptions “spark.memory.storageFraction caps how much I can cache.” It does not. It sets the share of the unified pool that execution is not allowed to evict. maxOnHeapStorageMemory on the 4 GB heap above reports 2277.6 MiB, the whole pool, not the 1138.8 MiB storage region, because storage can grow into the entire pool when execution is idle. Read the fraction as an eviction floor. “Enabling off-heap memory takes it out of the heap.” It is additional. Turning on 1 GB of off-heap left the on-heap pool unchanged at 2277.6 MiB and added a second pool of 1024.0 MiB. The container now needs a gigabyte more than before, which is how enabling off-heap gets a job killed by the cluster manager without a single change to spark.executor.memory. “The container was killed, so the heap was too small.” The cluster manager enforces heap + off-heap + overhead + PySpark, and only the first of those is spark.executor.memory. A heap sitting at 60% with Python and native buffers pushing the container over its limit is killed with no JVM OutOfMemoryError at all, usually as exit code 137. “An OutOfMemoryError means I should raise spark.memory.fraction.” Not if the memory went to your own objects. The user region, the part Spark does not manage, has no spill path and no eviction, so a UDF building a large structure per task fails outright. Raising the managed fraction makes that region smaller. Production notes Size the container, not the heap. heap + off-heap + overhead + PySpark is the number enforced, so leave headroom rather than setting executor.memory to the limit. Setting spark.executor.memoryOverhead directly disables both derived knobs. The factor and the minimum are ignored once it is explicit. Treat storageFraction as an eviction floor. It does not cap how much cache can grow, only how much survives pressure. Never assume a cached DataFrame is resident. Check the storage tab for the cached fraction before attributing a slow stage to something else. Use MEMORY_AND_DISK unless you have measured that the data fits. MEMORY_ONLY silently caches nothing when it does not, and the job stays correct while getting slower. Treat spark.executor.cores as a memory knob. It divides the execution pool 1/N, so raising it can cause spill with no other change. Account for off-heap twice. Once in offHeap.size and once in the container total. It is additional to the heap, not carved from it. Do not tune spark.memory.fraction first. It trades user memory against managed memory, and the common problems are overhead and core count. Frequently asked questions Why is my container killed when the heap looks fine? Because the cluster manager enforces the total, and overhead is not part of spark.executor.memory. A heap sitting at 60% with off-heap and Python pushing the container over its limit gets killed with no JVM OutOfMemoryError at all, usually as exit code 137. What is actually in the 300 MB reserved region? Nothing of yours. It is a margin so that Spark’s own internal structures cannot be squeezed out by execution and storage. It is not configurable outside tests, and the 450 MiB startup floor exists to keep it meaningful. Does spark.memory.storageFraction limit how much I can cache? No, and this is the most common misreading. It sets the portion of the unified pool that execution cannot evict. With execution idle, cache can occupy the whole pool, which is why maxOnHeapStorageMemory reports the full pool size. Can storage evict execution? No. The asymmetry is deliberate: a cached block can be dropped and recomputed, while a partially built hash table cannot. Execution gives memory back by spilling, on its own schedule. Is off-heap memory faster? It is not automatically faster. It removes garbage-collection pressure and stores data compactly, which helps on large heaps, and it costs you the JVM’s memory accounting. A leak becomes a killed container instead of a stack trace. Where does PySpark memory come from? Outside the JVM, from the overhead allocation, unless you set spark.executor.pyspark.memory to reserve it explicitly. A Python UDF holding a large object is not visible in any Spark memory metric. Why did my job start spilling after I gave executors more cores? Because each task is capped at 1/N of the execution pool where N is the active task count. More cores means more concurrent tasks and a smaller share each, from the same pool. Conclusion The useful way to hold all of this is as one sum and one asymmetry. The sum is the container: heap, off-heap, overhead and Python, of which spark.executor.memory is only the first. Most memory incidents that look mysterious are the cluster manager enforcing that sum while you were watching the heap. The asymmetry is inside the unified pool: execution can take memory from storage, and storage can never take it from execution. Every consequence follows from that. Cached data is evictable, so caching is a hint. storageFraction is a floor rather than a ceiling, so it does not limit cache growth. Execution handles its own shortfall by spilling, which is why spill is a tuning signal rather than an error. Once those two ideas are in place the configuration stops being a list of fractions to try and becomes arithmetic you can do before changing anything: compute the pool from the heap, divide by the concurrent task count, and compare it with what one task actually needs. That prediction beats trial and error. References Spark configuration reference for the memory and overhead settings and their defaults UnifiedMemoryManager.scala, which holds the 300 MB constant, the 450 MiB check and the borrowing rules quoted above ExecutionMemoryPool.scala for the per-task 1/N cap and the 1/2N guarantee Dive into Spark memory, whose region-by-region breakdown is the shape this post follows Apache Spark architecture for the executor and task machinery these pools belong to Trademarks Apache Spark, Apache Hadoop and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Adaptive query execution in Apache Spark: what it re-plans, and what it leaves alone</title><link href="https://rangareddy.github.io/SparkAQE/" rel="alternate" type="text/html" title="Adaptive query execution in Apache Spark: what it re-plans, and what it leaves alone" /><published>2026-09-18T17:00:00+05:30</published><updated>2026-09-18T17:00:00+05:30</updated><id>https://rangareddy.github.io/SparkAQE</id><content type="html" xml:base="https://rangareddy.github.io/SparkAQE/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-where-the-re-planning-happens&quot; id=&quot;markdown-toc-architecture-where-the-re-planning-happens&quot;&gt;Architecture: where the re-planning happens&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#how-to-inspect-it-and-the-mistake-to-avoid&quot; id=&quot;markdown-toc-how-to-inspect-it-and-the-mistake-to-avoid&quot;&gt;How to inspect it, and the mistake to avoid&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#change-1-coalescing-partitions&quot; id=&quot;markdown-toc-change-1-coalescing-partitions&quot;&gt;Change 1: coalescing partitions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#change-2-converting-a-join-to-a-broadcast&quot; id=&quot;markdown-toc-change-2-converting-a-join-to-a-broadcast&quot;&gt;Change 2: converting a join to a broadcast&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#change-3-reading-the-shuffle-locally&quot; id=&quot;markdown-toc-change-3-reading-the-shuffle-locally&quot;&gt;Change 3: reading the shuffle locally&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#change-4-splitting-a-skewed-partition&quot; id=&quot;markdown-toc-change-4-splitting-a-skewed-partition&quot;&gt;Change 4: splitting a skewed partition&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-aqe-does-not-do&quot; id=&quot;markdown-toc-what-aqe-does-not-do&quot;&gt;What AQE does not do&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-notes&quot; id=&quot;markdown-toc-production-notes&quot;&gt;Production notes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Everything Catalyst decides before a query starts comes from &lt;strong&gt;estimates&lt;/strong&gt;. Adaptive query execution re-plans after each shuffle stage finishes, using &lt;strong&gt;measurements&lt;/strong&gt;, and it is on by default.&lt;/li&gt;
    &lt;li&gt;It does four things: coalesces small partitions, splits skewed ones, converts a sort-merge join to a broadcast join, and reads a shuffle locally when the join no longer needs it distributed.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; shows the plan &lt;strong&gt;before&lt;/strong&gt; any of it. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=false&lt;/code&gt; means nothing has been re-planned yet, which is why plans read from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; and plans that actually ran often disagree.&lt;/li&gt;
    &lt;li&gt;To inspect it you must execute the plan you are holding and re-read the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt;. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.count()&lt;/code&gt; builds a different query, so checking that way inspects a plan that never ran.&lt;/li&gt;
    &lt;li&gt;It re-plans &lt;strong&gt;physical&lt;/strong&gt; choices only. The logical rewrites, pushdown, pruning, constant folding, are finished before execution begins and are never revisited.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;Every planning decision Spark makes before a query starts is made from
estimates, and the estimates are frequently poor. A filter whose selectivity is
unknown, a join whose output size nobody can predict, a shuffle whose partitions
turn out to be 4 KB each. Adaptive query execution is the acknowledgement that
after the first shuffle completes, Spark knows things it could only guess at
before, and should be allowed to change its mind.&lt;/p&gt;

&lt;p&gt;This post takes each thing it changes and shows the plan before and after, the
threshold that gates it, and how to tell from the plan that it fired. It also
covers the part most descriptions leave out: what AQE does not touch, which
matters because it explains why some bad plans survive it.&lt;/p&gt;

&lt;h2 id=&quot;architecture-where-the-re-planning-happens&quot;&gt;Architecture: where the re-planning happens&lt;/h2&gt;

&lt;p&gt;A shuffle is a natural barrier. The stage above it cannot start until the stage
below it has written its output, and at that moment the sizes of every shuffle
partition are known exactly. AQE inserts itself at that barrier.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;optimized logical plan&quot;] --&amp;gt; B[&quot;physical plan, from estimates&quot;]
  B --&amp;gt; C[&quot;AdaptiveSparkPlan&amp;lt;br/&amp;gt;isFinalPlan=false&quot;]
  C --&amp;gt; D[&quot;run the next stage&quot;]
  D --&amp;gt; M[&quot;&amp;lt;b&amp;gt;measured&amp;lt;/b&amp;gt; shuffle&amp;lt;br/&amp;gt;partition sizes&quot;]
  M --&amp;gt; R{&quot;re-optimize&amp;lt;br/&amp;gt;what remains&quot;}
  R --&amp;gt;|&quot;many tiny partitions&quot;| R1[&quot;coalesce&quot;]
  R --&amp;gt;|&quot;one huge partition&quot;| R2[&quot;split the skew&quot;]
  R --&amp;gt;|&quot;a side is small after all&quot;| R3[&quot;convert to broadcast&quot;]
  R --&amp;gt;|&quot;no shuffle needed now&quot;| R4[&quot;read the shuffle locally&quot;]
  R1 --&amp;gt; D
  R2 --&amp;gt; D
  R3 --&amp;gt; D
  R4 --&amp;gt; D
  R --&amp;gt;|&quot;nothing left&quot;| Z[&quot;isFinalPlan=true&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The loop matters. AQE is not one re-plan at the start; it runs again at every
shuffle boundary, so a query with three shuffles gets three opportunities to
change the remaining plan.&lt;/p&gt;

&lt;p&gt;The defaults, read from a session:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Config&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Default&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What it controls&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The whole feature&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.coalescePartitions.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Merging small partitions&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.advisoryPartitionSizeInBytes&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;67108864b&lt;/code&gt; (64 MB)&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The partition size it aims for&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.coalescePartitions.minPartitionSize&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1048576b&lt;/code&gt; (1 MB)&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The floor for a coalesced partition&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.coalescePartitions.parallelismFirst&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Prefer using all cores over hitting the advisory size&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Splitting skewed partitions&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.localShuffleReader.enabled&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Local reads after a broadcast conversion&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;parallelismFirst&lt;/code&gt; being &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; is worth knowing, because it means the 64 MB
advisory size is not really the target on a small cluster: Spark would rather
produce at least as many partitions as you have cores than produce fewer, larger
ones.&lt;/p&gt;

&lt;h2 id=&quot;how-to-inspect-it-and-the-mistake-to-avoid&quot;&gt;How to inspect it, and the mistake to avoid&lt;/h2&gt;

&lt;p&gt;This tripped me up, so it goes before the examples. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; and the plan
object both show the pre-execution plan. To see what AQE did, you must run the
plan you are holding and then look at the &lt;strong&gt;same&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt; again:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT city_id, count(*) c FROM fact GROUP BY city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;splitlines&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# before
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                                          &lt;span class=&quot;c1&quot;&gt;# execute THIS plan
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;splitlines&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# after
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AdaptiveSparkPlan isFinalPlan=false
AdaptiveSparkPlan isFinalPlan=true
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Do not use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.count()&lt;/code&gt; for this.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count()&lt;/code&gt; builds a different query, an
aggregate over your plan, so it executes and finalises something else while the
plan you are inspecting stays at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=false&lt;/code&gt;. I spent a while concluding
AQE was not firing before noticing that.&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=true&lt;/code&gt; is the signal that the plan you are now reading is the one
that ran. Everything below is read that way.&lt;/p&gt;

&lt;h2 id=&quot;change-1-coalescing-partitions&quot;&gt;Change 1: coalescing partitions&lt;/h2&gt;

&lt;p&gt;The most common win, and the one that quietly fixes the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;
problem everyone used to tune by hand.&lt;/p&gt;

&lt;p&gt;With the default 200 shuffle partitions and only 400 groups to produce, the
planned exchange asks for 200 partitions:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Exchange hashpartitioning(city_id#3L, 200), ENSURE_REQUIREMENTS, [plan_id=25]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;and the final plan reads them back merged:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AdaptiveSparkPlan isFinalPlan=true
+- ...
   +- AQEShuffleRead coalesced
      +- ShuffleQueryStage 0
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AQEShuffleRead coalesced&lt;/code&gt; is the marker. The number of reduce tasks is now
derived from the measured data rather than from a config you guessed at, which
is why the old advice to tune &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;shuffle.partitions&lt;/code&gt; per job has largely
evaporated: set it high enough and let AQE bring it down.&lt;/p&gt;

&lt;p&gt;What it cannot do is go the other way on a single partition. Coalescing merges
neighbours; it never splits one, except through the skew path below.&lt;/p&gt;

&lt;h2 id=&quot;change-2-converting-a-join-to-a-broadcast&quot;&gt;Change 2: converting a join to a broadcast&lt;/h2&gt;

&lt;p&gt;The most dramatic change, because the operator itself is different after
execution. Plan-time broadcasting is disabled here and runtime broadcasting
allowed, which is the arrangement that isolates AQE’s contribution:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;-1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;        &lt;span class=&quot;c1&quot;&gt;# plan time: never
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10MB&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;  &lt;span class=&quot;c1&quot;&gt;# run time: allowed
&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) FROM fact f JOIN dim_city c ON f.city_id = c.city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;q&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;planned&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;final&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;planned  : SortMergeJoin [city_id#3L], [city_id#10L], Inner
executed : *(3) BroadcastHashJoin [city_id#3L], [city_id#10L], Inner, BuildRight, false
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A sort-merge join became a broadcast hash join once the build side’s real size
was known. Note that these are &lt;strong&gt;two separate thresholds&lt;/strong&gt;: the plan-time
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; and the runtime
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;adaptive.autoBroadcastJoinThreshold&lt;/code&gt;. When the latter is unset it falls back to
the former, so the usual trick of setting the plan-time threshold to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; to stop
broadcasting also stops the runtime conversion unless you set the adaptive one
too.&lt;/p&gt;

&lt;h2 id=&quot;change-3-reading-the-shuffle-locally&quot;&gt;Change 3: reading the shuffle locally&lt;/h2&gt;

&lt;p&gt;This one follows from the last and is easy to miss. Once a join has become a
broadcast join, its inputs no longer need to be partitioned by the join key, so
fetching shuffle blocks across the network is pointless. AQE switches those reads
to local:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;+- *(3) BroadcastHashJoin [city_id#3L], [city_id#9L], Inner, BuildRight, false
   :- AQEShuffleRead local
   +- AQEShuffleRead local
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AQEShuffleRead local&lt;/code&gt; means each task reads the shuffle blocks already on its own
executor rather than fetching from peers. It is controlled by
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.localShuffleReader.enabled&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;, and it is a
consequence of change 2 rather than something you trigger directly.&lt;/p&gt;

&lt;h2 id=&quot;change-4-splitting-a-skewed-partition&quot;&gt;Change 4: splitting a skewed partition&lt;/h2&gt;

&lt;p&gt;When one shuffle partition is far larger than the others, AQE divides it into
several and replicates the matching rows from the other side so each piece can be
joined independently.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph B[&quot;before: one task does most of the work&quot;]
    P1[&quot;partition 3&amp;lt;br/&amp;gt;329,000 rows&quot;] --&amp;gt; T1[&quot;one long task&quot;]
  end
  subgraph A[&quot;after: the partition is split&quot;]
    S1[&quot;3a&quot;] --&amp;gt; U1[&quot;task&quot;]
    S2[&quot;3b&quot;] --&amp;gt; U2[&quot;task&quot;]
    S3[&quot;3c&quot;] --&amp;gt; U3[&quot;task&quot;]
  end
  B --&amp;gt; A
&lt;/code&gt;&lt;/pre&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;# skewJoin.enabled = true
*(5) SortMergeJoin(skew=true) [city_id#1L], [city_id#6L], Inner
  +- AQEShuffleRead coalesced and skewed

# skewJoin.enabled = false
*(5) SortMergeJoin [city_id#1L], [city_id#6L], Inner
  +- AQEShuffleRead coalesced
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(skew=true)&lt;/code&gt; on the join and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;and skewed&lt;/code&gt; on the shuffle read are the markers.&lt;/p&gt;

&lt;p&gt;The threshold is the thing to understand, because it is the usual reason nothing
happens. The rule takes the &lt;strong&gt;larger&lt;/strong&gt; of two numbers as its skew threshold:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;  &lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;getSkewThreshold&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;medianSize&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Long&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Long&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;nv&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getConf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;SQLConf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;SKEW_JOIN_SKEWED_PARTITION_THRESHOLD&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;
      &lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;medianSize&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getConf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;SQLConf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;SKEW_JOIN_SKEWED_PARTITION_FACTOR&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;toLong&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;so a partition must beat both &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;5.0&lt;/code&gt; times the median and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;256MB&lt;/code&gt;. Taking the
maximum makes the byte floor absolute: on data where no partition reaches 256 MB
this never fires, however lopsided the key distribution is. That limitation, and
the fixes that do not depend on it, are the subject of
&lt;a href=&quot;/SparkDataSkew/&quot;&gt;data skew in Apache Spark&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;what-aqe-does-not-do&quot;&gt;What AQE does not do&lt;/h2&gt;

&lt;p&gt;This is the part that matters for expectations, and it is short.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;It does not revisit the logical plan.&lt;/strong&gt; Predicate pushdown, column pruning,
constant folding and the rest run once, before execution, and their results are
fixed. If a predicate failed to push into your scan, AQE will not rescue it after
the first stage. The logical phase is the subject of
&lt;a href=&quot;/CatalystOptimizer/&quot;&gt;inside Spark’s Catalyst optimizer&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;It does not re-plan a stage that is already running.&lt;/strong&gt; Re-planning happens at
shuffle boundaries, so a query with no shuffle gets no adaptive treatment at all,
and the first stage is always executed as planned.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;It does not fix a bad scan.&lt;/strong&gt; If the plan reads five columns because pruning
did not apply, or scans every partition because a partition filter was not
recognised, that work happens in the first stage on the estimated plan.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;It does not choose a different join order.&lt;/strong&gt; Join reordering is a logical
rewrite. AQE changes the join &lt;em&gt;algorithm&lt;/em&gt;, not which tables are joined first.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;It does not make statistics unnecessary.&lt;/strong&gt; It removes the need for accurate
estimates &lt;em&gt;after the first shuffle&lt;/em&gt;. Everything before that, including the first
join’s strategy and every scan decision, still runs on whatever the catalog
knows.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Decision&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Made from&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Revisited by AQE&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Predicate pushdown, column pruning&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Logical rules&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Join order&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Logical rules, optionally CBO&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;First stage’s scan and filters&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Estimates&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Number of reduce partitions&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Config, then measurement&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes, coalesced&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Join algorithm after a shuffle&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Estimates, then measurement&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes, can become broadcast&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Skewed partition handling&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Measurement only&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes, above the threshold&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle fetch locality&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Plan shape&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes, after a broadcast conversion&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Adaptive execution handles skew, so I do not need to.”&lt;/strong&gt; Only above its
threshold. A partition must beat both five times the median and 256 MB, because
the rule takes the larger of the two as its skew threshold. On data where no
partition reaches 256 MB the split never fires however lopsided the key is, and
nothing warns you.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=true&lt;/code&gt; means AQE improved something.”&lt;/strong&gt; It means re-planning
finished. The final plan is often identical to the planned one, which is normal.
Look for the specific markers instead: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AQEShuffleRead coalesced&lt;/code&gt;,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AQEShuffleRead local&lt;/code&gt;, or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(skew=true)&lt;/code&gt; on the join operator.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“I set &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt;, so nothing will broadcast.”&lt;/strong&gt; The
runtime conversion is governed by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.autoBroadcastJoinThreshold&lt;/code&gt;,
which falls back to the plan-time value when unset. Setting only the plan-time
one to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; therefore disables the runtime conversion too, which is usually the
opposite of what was intended.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.count()&lt;/code&gt; is a fine way to check what AQE did.”&lt;/strong&gt; It is not. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count()&lt;/code&gt;
builds a different query, so it executes and finalises something else while the
plan you are inspecting stays at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=false&lt;/code&gt;. Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; and re-read
the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt;.&lt;/p&gt;

&lt;h2 id=&quot;production-notes&quot;&gt;Production notes&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Leave it on.&lt;/strong&gt; It is on by default, and the failure mode of turning it off is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;shuffle.partitions&lt;/code&gt; value that is wrong for every query but one.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Set &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; high rather than precisely.&lt;/strong&gt; Coalescing brings it down from too many; nothing brings it up from too few.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Set both broadcast thresholds deliberately.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;adaptive.autoBroadcastJoinThreshold&lt;/code&gt; falls back to the plan-time one, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; to disable plan-time broadcasting also disables the runtime conversion.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Trust the SQL tab over &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt;.&lt;/strong&gt; For anything adaptive, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; is a statement of intent. The UI shows the final plan with real row counts per operator.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Check the markers before believing a claim.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=true&lt;/code&gt; for “this is what ran”, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AQEShuffleRead coalesced&lt;/code&gt; for coalescing, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AQEShuffleRead local&lt;/code&gt; for local reads, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(skew=true)&lt;/code&gt; for a skew split.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Do not expect it to cover skew.&lt;/strong&gt; The 256 MB floor means it often does not, and it never helps a skewed aggregation, only a skewed join.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Fix the logical plan separately.&lt;/strong&gt; Pushdown and pruning are decided before AQE exists, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReadSchema&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushedFilters&lt;/code&gt; on the scan are still yours to get right.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Why does my &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; output not match what the UI shows?&lt;/strong&gt;
Because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; prints the plan before execution, where
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AdaptiveSparkPlan isFinalPlan=false&lt;/code&gt;. The UI shows the plan after AQE has
finished re-planning. They are both correct about different moments.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;I set &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; to 2000 and nothing got slower. Why?&lt;/strong&gt;
Coalescing. The exchange still asks for 2000 partitions, and AQE merges them back
into a number derived from the measured data, so the config now sets an upper
bound rather than the actual parallelism.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why did my join not convert to a broadcast at runtime?&lt;/strong&gt;
Most often because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;adaptive.autoBroadcastJoinThreshold&lt;/code&gt; was never set, so the runtime threshold
inherited the disabled value. Otherwise, the join type may forbid broadcasting
that side, which no amount of measurement changes.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Does AQE help a query with no joins and no aggregations?&lt;/strong&gt;
No. With no shuffle there is no boundary at which to re-plan, so a scan-and-filter
query executes exactly as planned.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=true&lt;/code&gt; a guarantee that AQE changed something?&lt;/strong&gt;
No. It only means re-planning has finished. The plan may be identical to the
planned one, which is common and fine. Look for the specific markers to see
whether anything was altered.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Should I still compute statistics if AQE is on?&lt;/strong&gt;
Yes. AQE uses measurements only after a shuffle. The first stage’s scan
decisions, the first join’s strategy and any logical rewrite that depends on size
all happen beforehand, on catalog statistics.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;Adaptive query execution is best understood as a correction layer with a narrow
and well-defined scope. It fixes the decisions that depend on sizes Spark could
not have known, at the only moment it could know them, which is when a shuffle
has just finished writing.&lt;/p&gt;

&lt;p&gt;That scope is the useful thing to hold onto. Four changes, all physical:
partition counts, skew splits, join algorithm, fetch locality. Everything logical
is settled before the query starts and stays settled. So AQE removes an entire
category of tuning, the one that used to be about guessing a good
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;shuffle.partitions&lt;/code&gt;, and removes none of the work of getting the logical plan
right.&lt;/p&gt;

&lt;p&gt;The practical habit that follows is small. Read plans after execution rather than
before, from the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt; or from the SQL tab, and look for the four
markers. A plan printed from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; is a plan of what Spark intended, and on
any query with a shuffle that is not the same document as what it did.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/sql-performance-tuning.html&quot;&gt;SQL performance tuning&lt;/a&gt; for the adaptive-execution configuration reference and defaults&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/core/src/main/scala/org/apache/spark/sql/execution/adaptive/AdaptiveSparkPlanExec.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AdaptiveSparkPlanExec.scala&lt;/code&gt;&lt;/a&gt;, the operator that runs the re-planning loop at each shuffle boundary&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/core/src/main/scala/org/apache/spark/sql/execution/adaptive/OptimizeSkewedJoin.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OptimizeSkewedJoin.scala&lt;/code&gt;&lt;/a&gt; for the skew threshold quoted above&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/SparkDataSkew/&quot;&gt;Data skew in Apache Spark&lt;/a&gt; for the fixes that work below AQE’s skew threshold&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/CatalystOptimizer/&quot;&gt;Inside Spark’s Catalyst optimizer&lt;/a&gt; for the logical phase that AQE never revisits&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Hudi, Apache Iceberg, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="AQE" /><category term="Performance" /><category term="Tuning" /><category term="Internals" /><summary type="html">Architecture: where the re-planning happens How to inspect it, and the mistake to avoid Change 1: coalescing partitions Change 2: converting a join to a broadcast Change 3: reading the shuffle locally Change 4: splitting a skewed partition What AQE does not do Common misconceptions Production notes Frequently asked questions Conclusion References Trademarks TL;DR Everything Catalyst decides before a query starts comes from estimates. Adaptive query execution re-plans after each shuffle stage finishes, using measurements, and it is on by default. It does four things: coalesces small partitions, splits skewed ones, converts a sort-merge join to a broadcast join, and reads a shuffle locally when the join no longer needs it distributed. explain() shows the plan before any of it. isFinalPlan=false means nothing has been re-planned yet, which is why plans read from explain() and plans that actually ran often disagree. To inspect it you must execute the plan you are holding and re-read the same QueryExecution. df.count() builds a different query, so checking that way inspects a plan that never ran. It re-plans physical choices only. The logical rewrites, pushdown, pruning, constant folding, are finished before execution begins and are never revisited. Every planning decision Spark makes before a query starts is made from estimates, and the estimates are frequently poor. A filter whose selectivity is unknown, a join whose output size nobody can predict, a shuffle whose partitions turn out to be 4 KB each. Adaptive query execution is the acknowledgement that after the first shuffle completes, Spark knows things it could only guess at before, and should be allowed to change its mind. This post takes each thing it changes and shows the plan before and after, the threshold that gates it, and how to tell from the plan that it fired. It also covers the part most descriptions leave out: what AQE does not touch, which matters because it explains why some bad plans survive it. Architecture: where the re-planning happens A shuffle is a natural barrier. The stage above it cannot start until the stage below it has written its output, and at that moment the sizes of every shuffle partition are known exactly. AQE inserts itself at that barrier. flowchart TB A[&quot;optimized logical plan&quot;] --&amp;gt; B[&quot;physical plan, from estimates&quot;] B --&amp;gt; C[&quot;AdaptiveSparkPlan&amp;lt;br/&amp;gt;isFinalPlan=false&quot;] C --&amp;gt; D[&quot;run the next stage&quot;] D --&amp;gt; M[&quot;&amp;lt;b&amp;gt;measured&amp;lt;/b&amp;gt; shuffle&amp;lt;br/&amp;gt;partition sizes&quot;] M --&amp;gt; R{&quot;re-optimize&amp;lt;br/&amp;gt;what remains&quot;} R --&amp;gt;|&quot;many tiny partitions&quot;| R1[&quot;coalesce&quot;] R --&amp;gt;|&quot;one huge partition&quot;| R2[&quot;split the skew&quot;] R --&amp;gt;|&quot;a side is small after all&quot;| R3[&quot;convert to broadcast&quot;] R --&amp;gt;|&quot;no shuffle needed now&quot;| R4[&quot;read the shuffle locally&quot;] R1 --&amp;gt; D R2 --&amp;gt; D R3 --&amp;gt; D R4 --&amp;gt; D R --&amp;gt;|&quot;nothing left&quot;| Z[&quot;isFinalPlan=true&quot;] The loop matters. AQE is not one re-plan at the start; it runs again at every shuffle boundary, so a query with three shuffles gets three opportunities to change the remaining plan. The defaults, read from a session: Config Default What it controls spark.sql.adaptive.enabled true The whole feature spark.sql.adaptive.coalescePartitions.enabled true Merging small partitions spark.sql.adaptive.advisoryPartitionSizeInBytes 67108864b (64 MB) The partition size it aims for spark.sql.adaptive.coalescePartitions.minPartitionSize 1048576b (1 MB) The floor for a coalesced partition spark.sql.adaptive.coalescePartitions.parallelismFirst true Prefer using all cores over hitting the advisory size spark.sql.adaptive.skewJoin.enabled true Splitting skewed partitions spark.sql.adaptive.localShuffleReader.enabled true Local reads after a broadcast conversion parallelismFirst being true is worth knowing, because it means the 64 MB advisory size is not really the target on a small cluster: Spark would rather produce at least as many partitions as you have cores than produce fewer, larger ones. How to inspect it, and the mistake to avoid This tripped me up, so it goes before the examples. explain() and the plan object both show the pre-execution plan. To see what AQE did, you must run the plan you are holding and then look at the same QueryExecution again: df = spark.sql(&quot;SELECT city_id, count(*) c FROM fact GROUP BY city_id&quot;) qe = df._jdf.queryExecution() print(qe.executedPlan().toString().splitlines()[0]) # before df.collect() # execute THIS plan print(qe.executedPlan().toString().splitlines()[0]) # after AdaptiveSparkPlan isFinalPlan=false AdaptiveSparkPlan isFinalPlan=true Do not use df.count() for this. count() builds a different query, an aggregate over your plan, so it executes and finalises something else while the plan you are inspecting stays at isFinalPlan=false. I spent a while concluding AQE was not firing before noticing that. isFinalPlan=true is the signal that the plan you are now reading is the one that ran. Everything below is read that way. Change 1: coalescing partitions The most common win, and the one that quietly fixes the spark.sql.shuffle.partitions problem everyone used to tune by hand. With the default 200 shuffle partitions and only 400 groups to produce, the planned exchange asks for 200 partitions: Exchange hashpartitioning(city_id#3L, 200), ENSURE_REQUIREMENTS, [plan_id=25] and the final plan reads them back merged: AdaptiveSparkPlan isFinalPlan=true +- ... +- AQEShuffleRead coalesced +- ShuffleQueryStage 0 AQEShuffleRead coalesced is the marker. The number of reduce tasks is now derived from the measured data rather than from a config you guessed at, which is why the old advice to tune shuffle.partitions per job has largely evaporated: set it high enough and let AQE bring it down. What it cannot do is go the other way on a single partition. Coalescing merges neighbours; it never splits one, except through the skew path below. Change 2: converting a join to a broadcast The most dramatic change, because the operator itself is different after execution. Plan-time broadcasting is disabled here and runtime broadcasting allowed, which is the arrangement that isolates AQE’s contribution: spark.conf.set(&quot;spark.sql.autoBroadcastJoinThreshold&quot;, &quot;-1&quot;) # plan time: never spark.conf.set(&quot;spark.sql.adaptive.autoBroadcastJoinThreshold&quot;, &quot;10MB&quot;) # run time: allowed q = spark.sql(&quot;SELECT count(*) FROM fact f JOIN dim_city c ON f.city_id = c.city_id&quot;) qe = q._jdf.queryExecution() planned = qe.executedPlan().toString() q.collect() final = qe.executedPlan().toString() planned : SortMergeJoin [city_id#3L], [city_id#10L], Inner executed : *(3) BroadcastHashJoin [city_id#3L], [city_id#10L], Inner, BuildRight, false A sort-merge join became a broadcast hash join once the build side’s real size was known. Note that these are two separate thresholds: the plan-time autoBroadcastJoinThreshold and the runtime adaptive.autoBroadcastJoinThreshold. When the latter is unset it falls back to the former, so the usual trick of setting the plan-time threshold to -1 to stop broadcasting also stops the runtime conversion unless you set the adaptive one too. Change 3: reading the shuffle locally This one follows from the last and is easy to miss. Once a join has become a broadcast join, its inputs no longer need to be partitioned by the join key, so fetching shuffle blocks across the network is pointless. AQE switches those reads to local: +- *(3) BroadcastHashJoin [city_id#3L], [city_id#9L], Inner, BuildRight, false :- AQEShuffleRead local +- AQEShuffleRead local AQEShuffleRead local means each task reads the shuffle blocks already on its own executor rather than fetching from peers. It is controlled by spark.sql.adaptive.localShuffleReader.enabled, default true, and it is a consequence of change 2 rather than something you trigger directly. Change 4: splitting a skewed partition When one shuffle partition is far larger than the others, AQE divides it into several and replicates the matching rows from the other side so each piece can be joined independently. flowchart TB subgraph B[&quot;before: one task does most of the work&quot;] P1[&quot;partition 3&amp;lt;br/&amp;gt;329,000 rows&quot;] --&amp;gt; T1[&quot;one long task&quot;] end subgraph A[&quot;after: the partition is split&quot;] S1[&quot;3a&quot;] --&amp;gt; U1[&quot;task&quot;] S2[&quot;3b&quot;] --&amp;gt; U2[&quot;task&quot;] S3[&quot;3c&quot;] --&amp;gt; U3[&quot;task&quot;] end B --&amp;gt; A # skewJoin.enabled = true *(5) SortMergeJoin(skew=true) [city_id#1L], [city_id#6L], Inner +- AQEShuffleRead coalesced and skewed # skewJoin.enabled = false *(5) SortMergeJoin [city_id#1L], [city_id#6L], Inner +- AQEShuffleRead coalesced (skew=true) on the join and and skewed on the shuffle read are the markers. The threshold is the thing to understand, because it is the usual reason nothing happens. The rule takes the larger of two numbers as its skew threshold: def getSkewThreshold(medianSize: Long): Long = { conf.getConf(SQLConf.SKEW_JOIN_SKEWED_PARTITION_THRESHOLD).max( (medianSize * conf.getConf(SQLConf.SKEW_JOIN_SKEWED_PARTITION_FACTOR)).toLong) } so a partition must beat both 5.0 times the median and 256MB. Taking the maximum makes the byte floor absolute: on data where no partition reaches 256 MB this never fires, however lopsided the key distribution is. That limitation, and the fixes that do not depend on it, are the subject of data skew in Apache Spark. What AQE does not do This is the part that matters for expectations, and it is short. It does not revisit the logical plan. Predicate pushdown, column pruning, constant folding and the rest run once, before execution, and their results are fixed. If a predicate failed to push into your scan, AQE will not rescue it after the first stage. The logical phase is the subject of inside Spark’s Catalyst optimizer. It does not re-plan a stage that is already running. Re-planning happens at shuffle boundaries, so a query with no shuffle gets no adaptive treatment at all, and the first stage is always executed as planned. It does not fix a bad scan. If the plan reads five columns because pruning did not apply, or scans every partition because a partition filter was not recognised, that work happens in the first stage on the estimated plan. It does not choose a different join order. Join reordering is a logical rewrite. AQE changes the join algorithm, not which tables are joined first. It does not make statistics unnecessary. It removes the need for accurate estimates after the first shuffle. Everything before that, including the first join’s strategy and every scan decision, still runs on whatever the catalog knows. Decision Made from Revisited by AQE Predicate pushdown, column pruning Logical rules No Join order Logical rules, optionally CBO No First stage’s scan and filters Estimates No Number of reduce partitions Config, then measurement Yes, coalesced Join algorithm after a shuffle Estimates, then measurement Yes, can become broadcast Skewed partition handling Measurement only Yes, above the threshold Shuffle fetch locality Plan shape Yes, after a broadcast conversion Common misconceptions “Adaptive execution handles skew, so I do not need to.” Only above its threshold. A partition must beat both five times the median and 256 MB, because the rule takes the larger of the two as its skew threshold. On data where no partition reaches 256 MB the split never fires however lopsided the key is, and nothing warns you. “isFinalPlan=true means AQE improved something.” It means re-planning finished. The final plan is often identical to the planned one, which is normal. Look for the specific markers instead: AQEShuffleRead coalesced, AQEShuffleRead local, or (skew=true) on the join operator. “I set autoBroadcastJoinThreshold to -1, so nothing will broadcast.” The runtime conversion is governed by spark.sql.adaptive.autoBroadcastJoinThreshold, which falls back to the plan-time value when unset. Setting only the plan-time one to -1 therefore disables the runtime conversion too, which is usually the opposite of what was intended. “df.count() is a fine way to check what AQE did.” It is not. count() builds a different query, so it executes and finalises something else while the plan you are inspecting stays at isFinalPlan=false. Use collect() and re-read the same QueryExecution. Production notes Leave it on. It is on by default, and the failure mode of turning it off is a shuffle.partitions value that is wrong for every query but one. Set spark.sql.shuffle.partitions high rather than precisely. Coalescing brings it down from too many; nothing brings it up from too few. Set both broadcast thresholds deliberately. adaptive.autoBroadcastJoinThreshold falls back to the plan-time one, so -1 to disable plan-time broadcasting also disables the runtime conversion. Trust the SQL tab over explain(). For anything adaptive, explain() is a statement of intent. The UI shows the final plan with real row counts per operator. Check the markers before believing a claim. isFinalPlan=true for “this is what ran”, AQEShuffleRead coalesced for coalescing, AQEShuffleRead local for local reads, (skew=true) for a skew split. Do not expect it to cover skew. The 256 MB floor means it often does not, and it never helps a skewed aggregation, only a skewed join. Fix the logical plan separately. Pushdown and pruning are decided before AQE exists, so ReadSchema and PushedFilters on the scan are still yours to get right. Frequently asked questions Why does my explain() output not match what the UI shows? Because explain() prints the plan before execution, where AdaptiveSparkPlan isFinalPlan=false. The UI shows the plan after AQE has finished re-planning. They are both correct about different moments. I set spark.sql.shuffle.partitions to 2000 and nothing got slower. Why? Coalescing. The exchange still asks for 2000 partitions, and AQE merges them back into a number derived from the measured data, so the config now sets an upper bound rather than the actual parallelism. Why did my join not convert to a broadcast at runtime? Most often because autoBroadcastJoinThreshold is -1 and adaptive.autoBroadcastJoinThreshold was never set, so the runtime threshold inherited the disabled value. Otherwise, the join type may forbid broadcasting that side, which no amount of measurement changes. Does AQE help a query with no joins and no aggregations? No. With no shuffle there is no boundary at which to re-plan, so a scan-and-filter query executes exactly as planned. Is isFinalPlan=true a guarantee that AQE changed something? No. It only means re-planning has finished. The plan may be identical to the planned one, which is common and fine. Look for the specific markers to see whether anything was altered. Should I still compute statistics if AQE is on? Yes. AQE uses measurements only after a shuffle. The first stage’s scan decisions, the first join’s strategy and any logical rewrite that depends on size all happen beforehand, on catalog statistics. Conclusion Adaptive query execution is best understood as a correction layer with a narrow and well-defined scope. It fixes the decisions that depend on sizes Spark could not have known, at the only moment it could know them, which is when a shuffle has just finished writing. That scope is the useful thing to hold onto. Four changes, all physical: partition counts, skew splits, join algorithm, fetch locality. Everything logical is settled before the query starts and stays settled. So AQE removes an entire category of tuning, the one that used to be about guessing a good shuffle.partitions, and removes none of the work of getting the logical plan right. The practical habit that follows is small. Read plans after execution rather than before, from the same QueryExecution or from the SQL tab, and look for the four markers. A plan printed from explain() is a plan of what Spark intended, and on any query with a shuffle that is not the same document as what it did. References SQL performance tuning for the adaptive-execution configuration reference and defaults AdaptiveSparkPlanExec.scala, the operator that runs the re-planning loop at each shuffle boundary OptimizeSkewedJoin.scala for the skew threshold quoted above Data skew in Apache Spark for the fixes that work below AQE’s skew threshold Inside Spark’s Catalyst optimizer for the logical phase that AQE never revisits Trademarks Apache Spark, Apache Hudi, Apache Iceberg, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Data skew in Apache Spark: how to see it, and six ways to fix it</title><link href="https://rangareddy.github.io/SparkDataSkew/" rel="alternate" type="text/html" title="Data skew in Apache Spark: how to see it, and six ways to fix it" /><published>2026-09-18T15:00:00+05:30</published><updated>2026-09-18T15:00:00+05:30</updated><id>https://rangareddy.github.io/SparkDataSkew</id><content type="html" xml:base="https://rangareddy.github.io/SparkDataSkew/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-how-a-shuffle-creates-skew&quot; id=&quot;markdown-toc-architecture-how-a-shuffle-creates-skew&quot;&gt;Architecture: how a shuffle creates skew&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-dataset&quot; id=&quot;markdown-toc-the-dataset&quot;&gt;The dataset&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#step-1-measure-it&quot; id=&quot;markdown-toc-step-1-measure-it&quot;&gt;Step 1: measure it&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#fix-1-broadcast-the-small-side&quot; id=&quot;markdown-toc-fix-1-broadcast-the-small-side&quot;&gt;Fix 1: broadcast the small side&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#fix-2-let-adaptive-execution-split-the-partition&quot; id=&quot;markdown-toc-fix-2-let-adaptive-execution-split-the-partition&quot;&gt;Fix 2: let adaptive execution split the partition&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#fix-3-salting&quot; id=&quot;markdown-toc-fix-3-salting&quot;&gt;Fix 3: salting&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#fix-4-isolate-the-hot-key&quot; id=&quot;markdown-toc-fix-4-isolate-the-hot-key&quot;&gt;Fix 4: isolate the hot key&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#fix-5-pre-aggregate-before-the-join&quot; id=&quot;markdown-toc-fix-5-pre-aggregate-before-the-join&quot;&gt;Fix 5: pre-aggregate before the join&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#fix-6-change-the-key-or-the-layout&quot; id=&quot;markdown-toc-fix-6-change-the-key-or-the-layout&quot;&gt;Fix 6: change the key or the layout&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#choosing-between-them&quot; id=&quot;markdown-toc-choosing-between-them&quot;&gt;Choosing between them&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-failure-looks-like&quot; id=&quot;markdown-toc-what-failure-looks-like&quot;&gt;What failure looks like&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-notes&quot; id=&quot;markdown-toc-production-notes&quot;&gt;Production notes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Skew is not “slow”. It is &lt;strong&gt;one task doing most of the work&lt;/strong&gt; while the rest of the cluster waits, so adding executors changes nothing.&lt;/li&gt;
    &lt;li&gt;Measure it before fixing it. The number that matters is the ratio of the largest partition to the median: on the dataset below it is &lt;strong&gt;27x&lt;/strong&gt;, and salting brings it to &lt;strong&gt;1.9x&lt;/strong&gt;.&lt;/li&gt;
    &lt;li&gt;Adaptive execution can split a skewed partition for you, but only when a partition exceeds &lt;strong&gt;256 MB&lt;/strong&gt; by default, so on smaller data it never fires however lopsided the key is.&lt;/li&gt;
    &lt;li&gt;Broadcasting the small side removes the shuffle entirely, which is the only fix that makes skew irrelevant rather than survivable. Reach for it first.&lt;/li&gt;
    &lt;li&gt;Every fix here changes the physical layout and must not change the answer. Each one below is checked against the unskewed result, because a salted join with a wrong replication step silently drops or duplicates rows.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;A skewed job looks wrong in a specific way. The stage sits at 199 of 200 tasks
complete for a long time, one executor’s GC climbs, and nothing you do to the
cluster helps. That shape is diagnostic: the work is not too large, it is too
unevenly divided, and a single task has become the critical path.&lt;/p&gt;

&lt;p&gt;This post uses one deliberately skewed dataset throughout, measures the
distribution, and then applies six fixes, reporting the partition distribution
and a correctness check for each. The dataset is small so it runs on a laptop,
which means the interesting numbers are structural, row counts and partition
ratios, rather than timings. Timings on a laptop would tell you about page cache
and JVM warm-up, not about skew.&lt;/p&gt;

&lt;h2 id=&quot;architecture-how-a-shuffle-creates-skew&quot;&gt;Architecture: how a shuffle creates skew&lt;/h2&gt;

&lt;p&gt;Spark parallelises by partition, and a shuffle assigns rows to partitions by
hashing the key. If one key holds most of the rows, one partition holds most of
the rows, and one task processes them.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph S[&quot;shuffle by city_id&quot;]
    A[&quot;hash(city_id) % 8&quot;]
  end
  A --&amp;gt; P0[&quot;partition 0&amp;lt;br/&amp;gt;5,000 rows&quot;]
  A --&amp;gt; P3[&quot;&amp;lt;b&amp;gt;partition 3&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;329,000 rows&amp;lt;/b&amp;gt;&quot;]
  A --&amp;gt; P5[&quot;partition 5&amp;lt;br/&amp;gt;8,000 rows&quot;]
  A --&amp;gt; P7[&quot;partition 7&amp;lt;br/&amp;gt;12,000 rows&quot;]
  P0 --&amp;gt; T0[&quot;task: done&quot;]
  P3 --&amp;gt; T3[&quot;&amp;lt;b&amp;gt;task: still running&amp;lt;/b&amp;gt;&quot;]
  P5 --&amp;gt; T5[&quot;task: done&quot;]
  P7 --&amp;gt; T7[&quot;task: done&quot;]
  T3 --&amp;gt; R[&quot;the stage finishes&amp;lt;br/&amp;gt;when this one does&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Two consequences follow, and both are counter-intuitive until you see the shape:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;More executors do not help.&lt;/strong&gt; The stage cannot finish before its longest task, and that task is one thread on one executor.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;More partitions often do not help either.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; divides the &lt;em&gt;key space&lt;/em&gt;, not a single key. Raising it from 200 to 2000 splits the small keys further and leaves the hot key exactly where it was.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;That second point is why “increase shuffle partitions” is such common and such
useless advice for skew. It is the right fix for partitions that are uniformly
too big, and the wrong fix for one partition that is too big.&lt;/p&gt;

&lt;h2 id=&quot;the-dataset&quot;&gt;The dataset&lt;/h2&gt;

&lt;p&gt;One city holds 80% of the trips, which is the shape of most real skew: a default
value, a null-substitute, a single huge tenant, or one popular product.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;functions&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;skew&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;master&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local[4]&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;config&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;8&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;config&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;false&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# off, to see the raw problem
&lt;/span&gt;         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;400000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
            &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;when&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;80&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;7&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# 80% land on city 7
&lt;/span&gt;             &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;otherwise&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;400&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
            &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4000&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;100.0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;double&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/skew/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;400&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;selectExpr&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id AS city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;concat(&apos;city-&apos;, id) AS city_name&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/skew/cities&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;t&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/skew/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/skew/cities&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;step-1-measure-it&quot;&gt;Step 1: measure it&lt;/h2&gt;

&lt;p&gt;Two measurements are worth taking, and they answer different questions.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The key distribution&lt;/strong&gt; tells you whether you have skew and which key is hot:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;total&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;top&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orderBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;desc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;count&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;limit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;top&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]:&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; rows=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]:&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;8&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; &lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;mf&quot;&gt;100.0&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;total&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;mf&quot;&gt;5.1&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;% of &lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;total&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;city_id=7     rows=320000    80.0% of 400000
city_id=80    rows=1000       0.2% of 400000
city_id=82    rows=1000       0.2% of 400000
city_id=180   rows=1000       0.2% of 400000
city_id=96    rows=1000       0.2% of 400000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Every city other than 7 holds exactly 1,000 rows here, so which four appear
after the first is an arbitrary tie-break and will differ on your run. The first
line is the finding: one key out of 81 holds four fifths of the table.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The partition distribution&lt;/strong&gt; tells you how bad it will be after the shuffle,
which is the number that predicts the stall:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;partition_stats&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;label&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;pid&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark_partition_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
              &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;pid&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orderBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;pid&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;counts&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;median&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;sorted&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;counts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)[&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;len&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;counts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;//&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;label&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;34&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; partitions=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;len&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;counts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; max=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;counts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; &quot;&lt;/span&gt;
          &lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;median=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;median&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; ratio=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;counts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;median&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:.&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;counts&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;partition_stats&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;8&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;repartitioned by city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;repartitioned by city_id           partitions=8 max=329000 median=12000 ratio=27.4
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;A ratio near 1 is healthy; this is 27.&lt;/strong&gt; That single number is the one to track,
because it is what changes when a fix works. It also survives being run on a
sample, so you can measure it on a slice of production data without a full job.&lt;/p&gt;

&lt;p&gt;In the Spark UI the same thing appears in the stage’s task table: sort by
duration or by shuffle read size and look at max against the 75th percentile. The
summary metrics row gives you min, 25th, median, 75th and max directly, and a max
an order of magnitude above the median is the signature.&lt;/p&gt;

&lt;h2 id=&quot;fix-1-broadcast-the-small-side&quot;&gt;Fix 1: broadcast the small side&lt;/h2&gt;

&lt;p&gt;If one side fits in memory, broadcast it. There is then no shuffle of the large
side, so there is no partition for the hot key to overfill and the skew stops
mattering at all.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;ep&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;strategy = BroadcastHashJoin [city_id#1L], [city_id#3L], Inner, BuildRight
Exchange nodes in plan = 1
join rows = 400000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt; is the broadcast being built from the 400-row dimension, not a
shuffle of the 400,000-row fact. Nothing about the key distribution matters any
more.&lt;/p&gt;

&lt;p&gt;This is the first thing to try and the most commonly missed, because the
threshold is 10 MB and dimensions are often just over it &lt;em&gt;as stored&lt;/em&gt; while being
well under it after a filter and a projection. Reduce the small side first, then
look at whether it broadcasts.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;When it does not apply:&lt;/strong&gt; both sides large, or a join type that forbids
broadcasting that side. A full outer join can never be a broadcast hash join, and
for a right outer only the left may be broadcast. Those rules are the subject of
&lt;a href=&quot;/SparkJoinsInDepth/&quot;&gt;Apache Spark joins in depth&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;fix-2-let-adaptive-execution-split-the-partition&quot;&gt;Fix 2: let adaptive execution split the partition&lt;/h2&gt;

&lt;p&gt;Adaptive query execution can detect an oversized partition after the shuffle has
run and split it into several, joining each piece against a copy of the matching
rows from the other side. It is on by default.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;true&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.skewJoin.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;true&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;j&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_name&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;s&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;j&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;j&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                       &lt;span class=&quot;c1&quot;&gt;# execute this plan, then read it back
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;# skewJoin.enabled = true
*(5) SortMergeJoin(skew=true) [city_id#1L], [city_id#6L], Inner
  +- AQEShuffleRead coalesced and skewed

# skewJoin.enabled = false
*(5) SortMergeJoin [city_id#1L], [city_id#6L], Inner
  +- AQEShuffleRead coalesced
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(skew=true)&lt;/code&gt; on the join operator and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;and skewed&lt;/code&gt; on the shuffle read are the
two markers that it fired.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The catch, and it is a big one.&lt;/strong&gt; The rule computes one threshold as the
&lt;strong&gt;larger&lt;/strong&gt; of two numbers:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;  &lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;getSkewThreshold&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;medianSize&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Long&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Long&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;nv&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getConf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;SQLConf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;SKEW_JOIN_SKEWED_PARTITION_THRESHOLD&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;
      &lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;medianSize&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getConf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;SQLConf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;SKEW_JOIN_SKEWED_PARTITION_FACTOR&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;toLong&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;so a partition must exceed both &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;skewJoin.skewedPartitionFactor&lt;/code&gt; times the median
(default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;5.0&lt;/code&gt;) and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;skewJoin.skewedPartitionThresholdInBytes&lt;/code&gt; (default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;256MB&lt;/code&gt;).
Taking the maximum is what makes the byte floor absolute: on a dataset where no
partition reaches 256 MB the rule can never fire, however lopsided the
distribution is. The example above only fires because I lowered that floor to
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1MB&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;So AQE skew handling is real and it is not a general answer. It helps on genuinely
large partitions and does nothing on a 27x ratio between partitions of 300 MB and
12 MB. Check the markers rather than assuming.&lt;/p&gt;

&lt;h2 id=&quot;fix-3-salting&quot;&gt;Fix 3: salting&lt;/h2&gt;

&lt;p&gt;Salting is the general fix and the one to understand properly, because a
half-implemented version silently changes results.&lt;/p&gt;

&lt;p&gt;The idea: add a random bucket number to the key on the large side so the hot key
becomes N keys, and replicate the small side once per bucket so every piece still
finds its match.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph L[&quot;large side&quot;]
    A[&quot;city_id=7&quot;] --&amp;gt; A1[&quot;(7, salt=0)&quot;]
    A --&amp;gt; A2[&quot;(7, salt=1)&quot;]
    A --&amp;gt; A3[&quot;(7, salt=...)&quot;]
    A --&amp;gt; A4[&quot;(7, salt=7)&quot;]
  end
  subgraph R[&quot;small side, replicated&quot;]
    B[&quot;city_id=7&quot;] --&amp;gt; B1[&quot;(7, 0)&quot;]
    B --&amp;gt; B2[&quot;(7, 1)&quot;]
    B --&amp;gt; B3[&quot;(7, ...)&quot;]
    B --&amp;gt; B4[&quot;(7, 7)&quot;]
  end
  A1 --&amp;gt; J[&quot;join on (city_id, salt)&quot;]
  B1 --&amp;gt; J
  A4 --&amp;gt; J
  B4 --&amp;gt; J
  J --&amp;gt; O[&quot;same rows as the plain join,&amp;lt;br/&amp;gt;spread over 8 partitions&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;SALT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;8&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# large side: one random bucket per row
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tf&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rand&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;seed&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;7&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SALT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;int&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# small side: one copy per bucket, so no match is lost
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cf&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;array&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;SALT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)])))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;salted&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;tf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;small side rows: 400 -&amp;gt; 3200 after explode by 8
fact repartitioned by (key, salt)   partitions=8 max=92841 median=50039 ratio=1.9
join rows = 400000   same as plain join: True
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;The ratio drops from 27.4 to 1.9, and the row count is unchanged.&lt;/strong&gt; That second
half is not a formality. The replication step is where salting goes wrong: forget
the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explode&lt;/code&gt; and rows of the hot key whose salt has no match disappear, and
replicate the wrong side and you get duplicates. Always compare the row count,
and ideally the full result, against the unsalted join.&lt;/p&gt;

&lt;p&gt;Three practical notes:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;The cost is the replication.&lt;/strong&gt; The small side grows by a factor of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SALT&lt;/code&gt;, so salting is cheap when it is small and pointless when it is not.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Salt only the hot keys&lt;/strong&gt; if you can. Applying the salt conditionally, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;when(col(&quot;city_id&quot;) == 7, rand * SALT).otherwise(0)&lt;/code&gt;, keeps the replication to one key and needs the dimension exploded only for that key.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Pick &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SALT&lt;/code&gt; from the ratio, not from taste.&lt;/strong&gt; A 27x imbalance needs roughly 27 buckets to level; 8 got it to 1.9 here because the other partitions grew too.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;fix-4-isolate-the-hot-key&quot;&gt;Fix 4: isolate the hot key&lt;/h2&gt;

&lt;p&gt;When one key is hot and you know which, split the query: broadcast-join the hot
key on its own, shuffle-join everything else, and union the two.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;HOT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;7&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;hot&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;HOT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;HOT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;cold&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;!=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;HOT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;!=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;HOT&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;result&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;hot&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unionByName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cold&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;hot rows=320000 cold rows=80000 total=400000
matches plain join: True
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The hot side is a broadcast join, so it never shuffles. The cold side is a normal
shuffle join over a now-uniform key distribution. It is more code than salting
and it is easier to reason about, since nothing is replicated and nothing is
random. It needs you to know the hot key, which the measurement step gives you.&lt;/p&gt;

&lt;h2 id=&quot;fix-5-pre-aggregate-before-the-join&quot;&gt;Fix 5: pre-aggregate before the join&lt;/h2&gt;

&lt;p&gt;If the query aggregates after joining, do the aggregation first. Skew in an
aggregation is far cheaper than skew in a join, because the hot key collapses to
one row rather than producing a large intermediate.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# 400,000 rows into the join
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join_then_agg&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
                  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_name&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;s&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# 81 rows into the join
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg_then_join&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;s&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
                  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;rows into the join: pre-aggregated=81 vs raw=400000
same answer: True
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;This is the largest structural win available when it applies, because it changes
the size of the problem rather than its distribution. It applies only when the
aggregate does not need columns from the dimension in its grouping, which is why
the version above groups by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_id&lt;/code&gt; and joins for the name afterwards.&lt;/p&gt;

&lt;h2 id=&quot;fix-6-change-the-key-or-the-layout&quot;&gt;Fix 6: change the key or the layout&lt;/h2&gt;

&lt;p&gt;Two slower fixes, for skew you have to live with.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Bucketing.&lt;/strong&gt; Writing both tables bucketed on the join key with the same bucket
count removes the shuffle from every future join on that key. It does not fix the
imbalance inside a bucket, so it pairs with salting rather than replacing it, but
it takes the shuffle out of the repeated read path.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;A composite key.&lt;/strong&gt; If the hot key is hot because it is a placeholder, a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt;, an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UNKNOWN&lt;/code&gt;, or a null substitute, the real fix is upstream. Those rows
often should not be joined at all. Filtering them out before the join, or
replacing the placeholder with something genuinely distinct, is a data-modelling
change that makes the skew disappear rather than spreading it.&lt;/p&gt;

&lt;h2 id=&quot;choosing-between-them&quot;&gt;Choosing between them&lt;/h2&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Situation&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Small side fits in memory after filtering&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No shuffle, so skew is irrelevant&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Query aggregates after the join&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Pre-aggregate&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shrinks the input rather than spreading it&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;One or two known hot keys&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Isolate them&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No replication, no randomness, easy to verify&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Many hot keys, or unknown ones&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Salting&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The general answer, at the cost of replicating the small side&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Partitions genuinely over 256 MB&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;AQE skew join&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Automatic, no query change&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The same large join runs repeatedly&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Bucketing, plus one of the above&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Removes the shuffle permanently&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The hot key is a placeholder&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Fix it upstream&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The rows usually should not be there&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The order in that table is roughly the order to try things in. Broadcast and
pre-aggregation change the shape of the work; salting and isolation only
redistribute it; AQE only helps above its threshold.&lt;/p&gt;

&lt;h2 id=&quot;what-failure-looks-like&quot;&gt;What failure looks like&lt;/h2&gt;

&lt;p&gt;Skew rarely announces itself as skew. The symptoms, in the order they usually
appear:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;A stage stuck at “199/200 tasks complete”, sometimes for longer than the rest of the job took.&lt;/li&gt;
  &lt;li&gt;In the stage’s summary metrics, a max shuffle-read size an order of magnitude above the 75th percentile.&lt;/li&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ExecutorLostFailure&lt;/code&gt; or a container killed by the resource manager, because the one big task exhausted its memory.&lt;/li&gt;
  &lt;li&gt;Repeated spill on one task only, visible as a large “Spill (disk)” for the max task and nothing for the median.&lt;/li&gt;
  &lt;li&gt;A job that got slower after you added executors, since the extra parallelism went to tasks that were already fast.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The last one is the clearest tell. If doubling the cluster changed nothing, the
critical path is a single task, and no amount of hardware divides one partition.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Adding executors will help.”&lt;/strong&gt; It cannot. The stage ends when its slowest
task ends, and that task is one thread processing one partition. Extra
executors give more parallelism to the tasks that were already finishing early.
A job that got no faster after you doubled the cluster is the clearest signal
that the critical path is a single partition.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Raising &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; will spread the hot key.”&lt;/strong&gt; It divides
the key space, not a single key. Going from 200 to 2000 splits the small keys
further and leaves the hot key exactly where it was. That setting is the right
fix for partitions that are uniformly too large, and the wrong one here.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition&lt;/code&gt; fixes skew.”&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt; with no column does spread rows
evenly, and then destroys the partitioning the join needs, so the join
re-shuffles and the skew returns. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n, &quot;key&quot;)&lt;/code&gt; reproduces precisely
the distribution causing the problem.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Salting is a safe, mechanical transformation.”&lt;/strong&gt; Only if the replication
matches the salting. The large side gets one random bucket per row and the small
side needs a copy for every bucket. Get that wrong and rows vanish or duplicate
with no error at all, which is why every salted join in this post is checked
against the unsalted row count.&lt;/p&gt;

&lt;h2 id=&quot;production-notes&quot;&gt;Production notes&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Measure before and after with the max-to-median partition ratio.&lt;/strong&gt; It is one number, it is cheap to compute on a sample, and it is what tells you a fix worked.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Check the row count after any salting change.&lt;/strong&gt; The replication step is easy to get subtly wrong, and the failure is silent: wrong results rather than an error.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Do not reach for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; for skew.&lt;/strong&gt; It divides the key space, not a single key. It is the right knob for uniformly oversized partitions.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Do not assume AQE has it covered.&lt;/strong&gt; Confirm &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(skew=true)&lt;/code&gt; in the final plan. The 256 MB default threshold means it often has not.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Salt only the hot keys&lt;/strong&gt; where you can, so the small side is replicated for one key rather than all of them.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Look upstream when the hot key is a placeholder.&lt;/strong&gt; A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NULL&lt;/code&gt;-substitute key that joins to nothing useful is a modelling bug wearing a performance costume.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Re-measure after data growth.&lt;/strong&gt; Skew is a property of the data, not the query, so a job that was balanced last quarter can stall on the same code.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;How much skew is too much?&lt;/strong&gt;
Watch the ratio of the largest partition to the median. Below about 2 it is not
worth acting on. Around 10 the stage is noticeably waiting on one task. At 27, as
here, the job is effectively serial in that stage.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why did adding executors make no difference?&lt;/strong&gt;
Because the stage ends when its slowest task ends, and that task is one thread
processing one partition. Extra executors give more parallelism to work that was
already finishing early.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Does &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition&lt;/code&gt; fix skew?&lt;/strong&gt;
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt; without a column fixes it, by shuffling rows round-robin, but it
destroys the partitioning by key that the join needs, so the join then re-shuffles
and the skew comes back. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n, &quot;key&quot;)&lt;/code&gt; reproduces exactly the
distribution that is causing the problem.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Is salting always safe?&lt;/strong&gt;
Only if the replication matches the salting. The large side gets one random bucket
per row and the small side gets a copy for every bucket. Get that wrong and rows
vanish or duplicate with no error, which is why the row-count check belongs in the
job and not just in the investigation.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Can I salt an aggregation instead of a join?&lt;/strong&gt;
Yes, and it is simpler: aggregate by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(key, salt)&lt;/code&gt; first, then aggregate the
partial results by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;key&lt;/code&gt;. No replication is needed because there is no second
side, which is why skewed aggregations are much less trouble than skewed joins.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Does this apply to Hudi, Iceberg and Delta tables?&lt;/strong&gt;
The skew is in the shuffle, not the format, so all of it applies unchanged. The
formats do help upstream: partitioning and clustering on the join key change what
lands in each file, which changes the distribution before Spark ever shuffles it.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;Skew is the one performance problem where the usual instincts are actively
misleading. It does not respond to more executors, it does not respond to more
shuffle partitions, and it does not look like a data problem until you measure
the distribution.&lt;/p&gt;

&lt;p&gt;The useful reframing is that every fix here does one of three things. It removes
the shuffle, which is broadcasting. It shrinks what goes into the shuffle, which
is pre-aggregation and filtering. Or it redistributes what the shuffle produces,
which is salting, hot-key isolation and AQE’s skew split. The first two change
the problem; the third divides it. Try them in that order, because a problem
removed needs no tuning.&lt;/p&gt;

&lt;p&gt;And whichever you pick, check the answer. Of the six fixes above, three replicate
or split data, and all three can change results if the replication is wrong. The
row-count comparison that took one line in every example here is the cheapest
insurance in this entire area.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/sql-performance-tuning.html&quot;&gt;SQL performance tuning&lt;/a&gt; for the adaptive-execution and skew-join configuration defaults&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/core/src/main/scala/org/apache/spark/sql/execution/adaptive/OptimizeSkewedJoin.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OptimizeSkewedJoin.scala&lt;/code&gt;&lt;/a&gt;, the rule that performs the runtime split and the two thresholds it tests&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/core/src/main/scala/org/apache/spark/sql/execution/adaptive/ShufflePartitionsUtil.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShufflePartitionsUtil.scala&lt;/code&gt;&lt;/a&gt; for how partitions are coalesced and split&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/SparkJoinsInDepth/&quot;&gt;Apache Spark joins in depth&lt;/a&gt; for the strategy and build-side rules that decide whether broadcasting is even available&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/CatalystOptimizer/&quot;&gt;Inside Spark’s Catalyst optimizer&lt;/a&gt; for where adaptive execution sits relative to the logical rewrites&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Hudi, Apache Iceberg, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="Skew" /><category term="Performance" /><category term="Joins" /><category term="Tuning" /><summary type="html">Architecture: how a shuffle creates skew The dataset Step 1: measure it Fix 1: broadcast the small side Fix 2: let adaptive execution split the partition Fix 3: salting Fix 4: isolate the hot key Fix 5: pre-aggregate before the join Fix 6: change the key or the layout Choosing between them What failure looks like Common misconceptions Production notes Frequently asked questions Conclusion References Trademarks TL;DR Skew is not “slow”. It is one task doing most of the work while the rest of the cluster waits, so adding executors changes nothing. Measure it before fixing it. The number that matters is the ratio of the largest partition to the median: on the dataset below it is 27x, and salting brings it to 1.9x. Adaptive execution can split a skewed partition for you, but only when a partition exceeds 256 MB by default, so on smaller data it never fires however lopsided the key is. Broadcasting the small side removes the shuffle entirely, which is the only fix that makes skew irrelevant rather than survivable. Reach for it first. Every fix here changes the physical layout and must not change the answer. Each one below is checked against the unskewed result, because a salted join with a wrong replication step silently drops or duplicates rows. A skewed job looks wrong in a specific way. The stage sits at 199 of 200 tasks complete for a long time, one executor’s GC climbs, and nothing you do to the cluster helps. That shape is diagnostic: the work is not too large, it is too unevenly divided, and a single task has become the critical path. This post uses one deliberately skewed dataset throughout, measures the distribution, and then applies six fixes, reporting the partition distribution and a correctness check for each. The dataset is small so it runs on a laptop, which means the interesting numbers are structural, row counts and partition ratios, rather than timings. Timings on a laptop would tell you about page cache and JVM warm-up, not about skew. Architecture: how a shuffle creates skew Spark parallelises by partition, and a shuffle assigns rows to partitions by hashing the key. If one key holds most of the rows, one partition holds most of the rows, and one task processes them. flowchart TB subgraph S[&quot;shuffle by city_id&quot;] A[&quot;hash(city_id) % 8&quot;] end A --&amp;gt; P0[&quot;partition 0&amp;lt;br/&amp;gt;5,000 rows&quot;] A --&amp;gt; P3[&quot;&amp;lt;b&amp;gt;partition 3&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;329,000 rows&amp;lt;/b&amp;gt;&quot;] A --&amp;gt; P5[&quot;partition 5&amp;lt;br/&amp;gt;8,000 rows&quot;] A --&amp;gt; P7[&quot;partition 7&amp;lt;br/&amp;gt;12,000 rows&quot;] P0 --&amp;gt; T0[&quot;task: done&quot;] P3 --&amp;gt; T3[&quot;&amp;lt;b&amp;gt;task: still running&amp;lt;/b&amp;gt;&quot;] P5 --&amp;gt; T5[&quot;task: done&quot;] P7 --&amp;gt; T7[&quot;task: done&quot;] T3 --&amp;gt; R[&quot;the stage finishes&amp;lt;br/&amp;gt;when this one does&quot;] Two consequences follow, and both are counter-intuitive until you see the shape: More executors do not help. The stage cannot finish before its longest task, and that task is one thread on one executor. More partitions often do not help either. spark.sql.shuffle.partitions divides the key space, not a single key. Raising it from 200 to 2000 splits the small keys further and leaves the hot key exactly where it was. That second point is why “increase shuffle partitions” is such common and such useless advice for skew. It is the right fix for partitions that are uniformly too big, and the wrong fix for one partition that is too big. The dataset One city holds 80% of the trips, which is the shape of most real skew: a default value, a null-substitute, a single huge tenant, or one popular product. from pyspark.sql import SparkSession, functions as F spark = (SparkSession.builder.appName(&quot;skew&quot;).master(&quot;local[4]&quot;) .config(&quot;spark.sql.shuffle.partitions&quot;, &quot;8&quot;) .config(&quot;spark.sql.adaptive.enabled&quot;, &quot;false&quot;) # off, to see the raw problem .getOrCreate()) trips = (spark.range(0, 400000) .select(F.col(&quot;id&quot;).alias(&quot;trip_id&quot;), F.when(F.col(&quot;id&quot;) % 100 &amp;lt; 80, F.lit(7)) # 80% land on city 7 .otherwise(F.col(&quot;id&quot;) % 400).alias(&quot;city_id&quot;), (F.col(&quot;id&quot;) % 4000 / 100.0).cast(&quot;double&quot;).alias(&quot;fare&quot;))) trips.write.mode(&quot;overwrite&quot;).parquet(&quot;/tmp/skew/trips&quot;) cities = spark.range(0, 400).selectExpr(&quot;id AS city_id&quot;, &quot;concat(&apos;city-&apos;, id) AS city_name&quot;) cities.write.mode(&quot;overwrite&quot;).parquet(&quot;/tmp/skew/cities&quot;) t = spark.read.parquet(&quot;/tmp/skew/trips&quot;) c = spark.read.parquet(&quot;/tmp/skew/cities&quot;) Step 1: measure it Two measurements are worth taking, and they answer different questions. The key distribution tells you whether you have skew and which key is hot: total = t.count() top = t.groupBy(&quot;city_id&quot;).count().orderBy(F.desc(&quot;count&quot;)).limit(5).collect() for r in top: print(f&quot;city_id={r[0]:&amp;lt;5} rows={r[1]:&amp;lt;8} {100.0 * r[1] / total:5.1f}% of {total}&quot;) city_id=7 rows=320000 80.0% of 400000 city_id=80 rows=1000 0.2% of 400000 city_id=82 rows=1000 0.2% of 400000 city_id=180 rows=1000 0.2% of 400000 city_id=96 rows=1000 0.2% of 400000 Every city other than 7 holds exactly 1,000 rows here, so which four appear after the first is an arbitrary tie-break and will differ on your run. The first line is the finding: one key out of 81 holds four fifths of the table. The partition distribution tells you how bad it will be after the shuffle, which is the number that predicts the stall: def partition_stats(df, label): rows = (df.withColumn(&quot;pid&quot;, F.spark_partition_id()) .groupBy(&quot;pid&quot;).count().orderBy(&quot;pid&quot;).collect()) counts = [r[1] for r in rows] median = sorted(counts)[len(counts) // 2] print(f&quot;{label:&amp;lt;34} partitions={len(counts)} max={max(counts)} &quot; f&quot;median={median} ratio={max(counts) / median:.1f}&quot;) return counts partition_stats(t.repartition(8, &quot;city_id&quot;), &quot;repartitioned by city_id&quot;) repartitioned by city_id partitions=8 max=329000 median=12000 ratio=27.4 A ratio near 1 is healthy; this is 27. That single number is the one to track, because it is what changes when a fix works. It also survives being run on a sample, so you can measure it on a slice of production data without a full job. In the Spark UI the same thing appears in the stage’s task table: sort by duration or by shuffle read size and look at max against the 75th percentile. The summary metrics row gives you min, 25th, median, 75th and max directly, and a max an order of magnitude above the median is the signature. Fix 1: broadcast the small side If one side fits in memory, broadcast it. There is then no shuffle of the large side, so there is no partition for the hot key to overfill and the skew stops mattering at all. b = t.join(F.broadcast(c), &quot;city_id&quot;) ep = b._jdf.queryExecution().executedPlan().toString() strategy = BroadcastHashJoin [city_id#1L], [city_id#3L], Inner, BuildRight Exchange nodes in plan = 1 join rows = 400000 The one Exchange is the broadcast being built from the 400-row dimension, not a shuffle of the 400,000-row fact. Nothing about the key distribution matters any more. This is the first thing to try and the most commonly missed, because the threshold is 10 MB and dimensions are often just over it as stored while being well under it after a filter and a projection. Reduce the small side first, then look at whether it broadcasts. When it does not apply: both sides large, or a join type that forbids broadcasting that side. A full outer join can never be a broadcast hash join, and for a right outer only the left may be broadcast. Those rules are the subject of Apache Spark joins in depth. Fix 2: let adaptive execution split the partition Adaptive query execution can detect an oversized partition after the shuffle has run and split it into several, joining each piece against a copy of the matching rows from the other side. It is on by default. spark.conf.set(&quot;spark.sql.adaptive.enabled&quot;, &quot;true&quot;) spark.conf.set(&quot;spark.sql.adaptive.skewJoin.enabled&quot;, &quot;true&quot;) j = t.join(c, &quot;city_id&quot;).groupBy(&quot;city_name&quot;).agg(F.sum(&quot;fare&quot;).alias(&quot;s&quot;)) qe = j._jdf.queryExecution() j.collect() # execute this plan, then read it back print(qe.executedPlan().toString()) # skewJoin.enabled = true *(5) SortMergeJoin(skew=true) [city_id#1L], [city_id#6L], Inner +- AQEShuffleRead coalesced and skewed # skewJoin.enabled = false *(5) SortMergeJoin [city_id#1L], [city_id#6L], Inner +- AQEShuffleRead coalesced (skew=true) on the join operator and and skewed on the shuffle read are the two markers that it fired. The catch, and it is a big one. The rule computes one threshold as the larger of two numbers: def getSkewThreshold(medianSize: Long): Long = { conf.getConf(SQLConf.SKEW_JOIN_SKEWED_PARTITION_THRESHOLD).max( (medianSize * conf.getConf(SQLConf.SKEW_JOIN_SKEWED_PARTITION_FACTOR)).toLong) } so a partition must exceed both skewJoin.skewedPartitionFactor times the median (default 5.0) and skewJoin.skewedPartitionThresholdInBytes (default 256MB). Taking the maximum is what makes the byte floor absolute: on a dataset where no partition reaches 256 MB the rule can never fire, however lopsided the distribution is. The example above only fires because I lowered that floor to 1MB. So AQE skew handling is real and it is not a general answer. It helps on genuinely large partitions and does nothing on a 27x ratio between partitions of 300 MB and 12 MB. Check the markers rather than assuming. Fix 3: salting Salting is the general fix and the one to understand properly, because a half-implemented version silently changes results. The idea: add a random bucket number to the key on the large side so the hot key becomes N keys, and replicate the small side once per bucket so every piece still finds its match. flowchart TB subgraph L[&quot;large side&quot;] A[&quot;city_id=7&quot;] --&amp;gt; A1[&quot;(7, salt=0)&quot;] A --&amp;gt; A2[&quot;(7, salt=1)&quot;] A --&amp;gt; A3[&quot;(7, salt=...)&quot;] A --&amp;gt; A4[&quot;(7, salt=7)&quot;] end subgraph R[&quot;small side, replicated&quot;] B[&quot;city_id=7&quot;] --&amp;gt; B1[&quot;(7, 0)&quot;] B --&amp;gt; B2[&quot;(7, 1)&quot;] B --&amp;gt; B3[&quot;(7, ...)&quot;] B --&amp;gt; B4[&quot;(7, 7)&quot;] end A1 --&amp;gt; J[&quot;join on (city_id, salt)&quot;] B1 --&amp;gt; J A4 --&amp;gt; J B4 --&amp;gt; J J --&amp;gt; O[&quot;same rows as the plain join,&amp;lt;br/&amp;gt;spread over 8 partitions&quot;] SALT = 8 # large side: one random bucket per row tf = t.withColumn(&quot;salt&quot;, (F.rand(seed=7) * SALT).cast(&quot;int&quot;)) # small side: one copy per bucket, so no match is lost cf = c.withColumn(&quot;salt&quot;, F.explode(F.array([F.lit(i) for i in range(SALT)]))) salted = tf.join(cf, [&quot;city_id&quot;, &quot;salt&quot;]) small side rows: 400 -&amp;gt; 3200 after explode by 8 fact repartitioned by (key, salt) partitions=8 max=92841 median=50039 ratio=1.9 join rows = 400000 same as plain join: True The ratio drops from 27.4 to 1.9, and the row count is unchanged. That second half is not a formality. The replication step is where salting goes wrong: forget the explode and rows of the hot key whose salt has no match disappear, and replicate the wrong side and you get duplicates. Always compare the row count, and ideally the full result, against the unsalted join. Three practical notes: The cost is the replication. The small side grows by a factor of SALT, so salting is cheap when it is small and pointless when it is not. Salt only the hot keys if you can. Applying the salt conditionally, when(col(&quot;city_id&quot;) == 7, rand * SALT).otherwise(0), keeps the replication to one key and needs the dimension exploded only for that key. Pick SALT from the ratio, not from taste. A 27x imbalance needs roughly 27 buckets to level; 8 got it to 1.9 here because the other partitions grew too. Fix 4: isolate the hot key When one key is hot and you know which, split the query: broadcast-join the hot key on its own, shuffle-join everything else, and union the two. HOT = 7 hot = (t.filter(F.col(&quot;city_id&quot;) == HOT) .join(F.broadcast(c.filter(F.col(&quot;city_id&quot;) == HOT)), &quot;city_id&quot;)) cold = (t.filter(F.col(&quot;city_id&quot;) != HOT) .join(c.filter(F.col(&quot;city_id&quot;) != HOT), &quot;city_id&quot;)) result = hot.unionByName(cold) hot rows=320000 cold rows=80000 total=400000 matches plain join: True The hot side is a broadcast join, so it never shuffles. The cold side is a normal shuffle join over a now-uniform key distribution. It is more code than salting and it is easier to reason about, since nothing is replicated and nothing is random. It needs you to know the hot key, which the measurement step gives you. Fix 5: pre-aggregate before the join If the query aggregates after joining, do the aggregation first. Skew in an aggregation is far cheaper than skew in a join, because the hot key collapses to one row rather than producing a large intermediate. # 400,000 rows into the join join_then_agg = (t.join(F.broadcast(c), &quot;city_id&quot;) .groupBy(&quot;city_name&quot;).agg(F.sum(&quot;fare&quot;).alias(&quot;s&quot;))) # 81 rows into the join agg_then_join = (t.groupBy(&quot;city_id&quot;).agg(F.sum(&quot;fare&quot;).alias(&quot;s&quot;)) .join(F.broadcast(c), &quot;city_id&quot;)) rows into the join: pre-aggregated=81 vs raw=400000 same answer: True This is the largest structural win available when it applies, because it changes the size of the problem rather than its distribution. It applies only when the aggregate does not need columns from the dimension in its grouping, which is why the version above groups by city_id and joins for the name afterwards. Fix 6: change the key or the layout Two slower fixes, for skew you have to live with. Bucketing. Writing both tables bucketed on the join key with the same bucket count removes the shuffle from every future join on that key. It does not fix the imbalance inside a bucket, so it pairs with salting rather than replacing it, but it takes the shuffle out of the repeated read path. A composite key. If the hot key is hot because it is a placeholder, a -1, an UNKNOWN, or a null substitute, the real fix is upstream. Those rows often should not be joined at all. Filtering them out before the join, or replacing the placeholder with something genuinely distinct, is a data-modelling change that makes the skew disappear rather than spreading it. Choosing between them Situation Fix Why Small side fits in memory after filtering Broadcast No shuffle, so skew is irrelevant Query aggregates after the join Pre-aggregate Shrinks the input rather than spreading it One or two known hot keys Isolate them No replication, no randomness, easy to verify Many hot keys, or unknown ones Salting The general answer, at the cost of replicating the small side Partitions genuinely over 256 MB AQE skew join Automatic, no query change The same large join runs repeatedly Bucketing, plus one of the above Removes the shuffle permanently The hot key is a placeholder Fix it upstream The rows usually should not be there The order in that table is roughly the order to try things in. Broadcast and pre-aggregation change the shape of the work; salting and isolation only redistribute it; AQE only helps above its threshold. What failure looks like Skew rarely announces itself as skew. The symptoms, in the order they usually appear: A stage stuck at “199/200 tasks complete”, sometimes for longer than the rest of the job took. In the stage’s summary metrics, a max shuffle-read size an order of magnitude above the 75th percentile. ExecutorLostFailure or a container killed by the resource manager, because the one big task exhausted its memory. Repeated spill on one task only, visible as a large “Spill (disk)” for the max task and nothing for the median. A job that got slower after you added executors, since the extra parallelism went to tasks that were already fast. The last one is the clearest tell. If doubling the cluster changed nothing, the critical path is a single task, and no amount of hardware divides one partition. Common misconceptions “Adding executors will help.” It cannot. The stage ends when its slowest task ends, and that task is one thread processing one partition. Extra executors give more parallelism to the tasks that were already finishing early. A job that got no faster after you doubled the cluster is the clearest signal that the critical path is a single partition. “Raising spark.sql.shuffle.partitions will spread the hot key.” It divides the key space, not a single key. Going from 200 to 2000 splits the small keys further and leaves the hot key exactly where it was. That setting is the right fix for partitions that are uniformly too large, and the wrong one here. “repartition fixes skew.” repartition(n) with no column does spread rows evenly, and then destroys the partitioning the join needs, so the join re-shuffles and the skew returns. repartition(n, &quot;key&quot;) reproduces precisely the distribution causing the problem. “Salting is a safe, mechanical transformation.” Only if the replication matches the salting. The large side gets one random bucket per row and the small side needs a copy for every bucket. Get that wrong and rows vanish or duplicate with no error at all, which is why every salted join in this post is checked against the unsalted row count. Production notes Measure before and after with the max-to-median partition ratio. It is one number, it is cheap to compute on a sample, and it is what tells you a fix worked. Check the row count after any salting change. The replication step is easy to get subtly wrong, and the failure is silent: wrong results rather than an error. Do not reach for spark.sql.shuffle.partitions for skew. It divides the key space, not a single key. It is the right knob for uniformly oversized partitions. Do not assume AQE has it covered. Confirm (skew=true) in the final plan. The 256 MB default threshold means it often has not. Salt only the hot keys where you can, so the small side is replicated for one key rather than all of them. Look upstream when the hot key is a placeholder. A NULL-substitute key that joins to nothing useful is a modelling bug wearing a performance costume. Re-measure after data growth. Skew is a property of the data, not the query, so a job that was balanced last quarter can stall on the same code. Frequently asked questions How much skew is too much? Watch the ratio of the largest partition to the median. Below about 2 it is not worth acting on. Around 10 the stage is noticeably waiting on one task. At 27, as here, the job is effectively serial in that stage. Why did adding executors make no difference? Because the stage ends when its slowest task ends, and that task is one thread processing one partition. Extra executors give more parallelism to work that was already finishing early. Does repartition fix skew? repartition(n) without a column fixes it, by shuffling rows round-robin, but it destroys the partitioning by key that the join needs, so the join then re-shuffles and the skew comes back. repartition(n, &quot;key&quot;) reproduces exactly the distribution that is causing the problem. Is salting always safe? Only if the replication matches the salting. The large side gets one random bucket per row and the small side gets a copy for every bucket. Get that wrong and rows vanish or duplicate with no error, which is why the row-count check belongs in the job and not just in the investigation. Can I salt an aggregation instead of a join? Yes, and it is simpler: aggregate by (key, salt) first, then aggregate the partial results by key. No replication is needed because there is no second side, which is why skewed aggregations are much less trouble than skewed joins. Does this apply to Hudi, Iceberg and Delta tables? The skew is in the shuffle, not the format, so all of it applies unchanged. The formats do help upstream: partitioning and clustering on the join key change what lands in each file, which changes the distribution before Spark ever shuffles it. Conclusion Skew is the one performance problem where the usual instincts are actively misleading. It does not respond to more executors, it does not respond to more shuffle partitions, and it does not look like a data problem until you measure the distribution. The useful reframing is that every fix here does one of three things. It removes the shuffle, which is broadcasting. It shrinks what goes into the shuffle, which is pre-aggregation and filtering. Or it redistributes what the shuffle produces, which is salting, hot-key isolation and AQE’s skew split. The first two change the problem; the third divides it. Try them in that order, because a problem removed needs no tuning. And whichever you pick, check the answer. Of the six fixes above, three replicate or split data, and all three can change results if the replication is wrong. The row-count comparison that took one line in every example here is the cheapest insurance in this entire area. References SQL performance tuning for the adaptive-execution and skew-join configuration defaults OptimizeSkewedJoin.scala, the rule that performs the runtime split and the two thresholds it tests ShufflePartitionsUtil.scala for how partitions are coalesced and split Apache Spark joins in depth for the strategy and build-side rules that decide whether broadcasting is even available Inside Spark’s Catalyst optimizer for where adaptive execution sits relative to the logical rewrites Trademarks Apache Spark, Apache Hudi, Apache Iceberg, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Inside Spark’s Catalyst optimizer: how a query gets rewritten, rule by rule</title><link href="https://rangareddy.github.io/CatalystOptimizer/" rel="alternate" type="text/html" title="Inside Spark’s Catalyst optimizer: how a query gets rewritten, rule by rule" /><published>2026-09-18T13:00:00+05:30</published><updated>2026-09-18T13:00:00+05:30</updated><id>https://rangareddy.github.io/CatalystOptimizer</id><content type="html" xml:base="https://rangareddy.github.io/CatalystOptimizer/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#why-there-is-an-optimizer-at-all-rdds-versus-dataframes&quot; id=&quot;markdown-toc-why-there-is-an-optimizer-at-all-rdds-versus-dataframes&quot;&gt;Why there is an optimizer at all: RDDs versus DataFrames&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-four-trees-one-pipeline&quot; id=&quot;markdown-toc-architecture-four-trees-one-pipeline&quot;&gt;Architecture: four trees, one pipeline&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-a-rule-actually-is&quot; id=&quot;markdown-toc-what-a-rule-actually-is&quot;&gt;What a rule actually is&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-engine-batches-strategies-and-the-fixed-point&quot; id=&quot;markdown-toc-the-engine-batches-strategies-and-the-fixed-point&quot;&gt;The engine: batches, strategies, and the fixed point&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#one-query-four-trees&quot; id=&quot;markdown-toc-one-query-four-trees&quot;&gt;One query, four trees&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#which-rules-actually-fired&quot; id=&quot;markdown-toc-which-rules-actually-fired&quot;&gt;Which rules actually fired&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#a-tour-of-rules-you-can-see-working&quot; id=&quot;markdown-toc-a-tour-of-rules-you-can-see-working&quot;&gt;A tour of rules you can see working&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#turning-a-rule-off&quot; id=&quot;markdown-toc-turning-a-rule-off&quot;&gt;Turning a rule off&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#writing-your-own-rule&quot; id=&quot;markdown-toc-writing-your-own-rule&quot;&gt;Writing your own rule&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#testing-a-rule&quot; id=&quot;markdown-toc-testing-a-rule&quot;&gt;Testing a rule&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#physical-planning-from-what-to-how&quot; id=&quot;markdown-toc-physical-planning-from-what-to-how&quot;&gt;Physical planning: from what to how&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#rule-based-versus-cost-based-optimization&quot; id=&quot;markdown-toc-rule-based-versus-cost-based-optimization&quot;&gt;Rule-based versus cost-based optimization&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#what-statistics-spark-actually-holds&quot; id=&quot;markdown-toc-what-statistics-spark-actually-holds&quot;&gt;What statistics Spark actually holds&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#the-same-query-with-and-without-cbo&quot; id=&quot;markdown-toc-the-same-query-with-and-without-cbo&quot;&gt;The same query, with and without CBO&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#join-reordering-and-the-honest-result&quot; id=&quot;markdown-toc-join-reordering-and-the-honest-result&quot;&gt;Join reordering, and the honest result&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#codegen-where-the-tree-stops-being-a-tree&quot; id=&quot;markdown-toc-codegen-where-the-tree-stops-being-a-tree&quot;&gt;Codegen, where the tree stops being a tree&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#dynamic-partition-pruning&quot; id=&quot;markdown-toc-dynamic-partition-pruning&quot;&gt;Dynamic partition pruning&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#adaptive-query-execution&quot; id=&quot;markdown-toc-adaptive-query-execution&quot;&gt;Adaptive query execution&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#debugging-and-inspection&quot; id=&quot;markdown-toc-debugging-and-inspection&quot;&gt;Debugging and inspection&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-notes&quot; id=&quot;markdown-toc-production-notes&quot;&gt;Production notes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Catalyst is a &lt;strong&gt;tree rewriter&lt;/strong&gt;. Every phase takes a tree and returns a tree, and almost every optimization is one small function matching a pattern and returning a replacement.&lt;/li&gt;
    &lt;li&gt;A query passes through four trees: parsed, analyzed, optimized, physical. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; shows them, and the difference between analyzed and optimized is the entire optimizer’s work made visible.&lt;/li&gt;
    &lt;li&gt;Rules run in &lt;strong&gt;batches&lt;/strong&gt;, each either &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Once&lt;/code&gt; or to a &lt;strong&gt;fixed point&lt;/strong&gt;, meaning repeatedly until the tree stops changing, which is how one rule enables another with nobody ordering them by hand. On a single join-and-aggregate query Spark invoked over two hundred rules and eleven changed the plan.&lt;/li&gt;
    &lt;li&gt;You can switch a rule off with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.optimizer.excludedRules&lt;/code&gt; and add your own in about ten lines, which is the fastest way to understand what any of them actually does.&lt;/li&gt;
    &lt;li&gt;Almost all of it is &lt;strong&gt;rule-based&lt;/strong&gt;. The &lt;strong&gt;cost-based&lt;/strong&gt; part is a thin layer that needs statistics you have to compute yourself, and in the join-reordering test below the rule-based &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReorderJoin&lt;/code&gt; did the work while &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CostBasedJoinReorder&lt;/code&gt; ran and changed nothing.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;Most explanations of Catalyst stop at a diagram with four boxes in it. That is a
fair summary and it is not much use when a plan does something surprising,
because the interesting questions are one level down. Which rule removed my
filter? Why did a cast appear? Why did the optimizer not push this predicate?
What runs before what?&lt;/p&gt;

&lt;p&gt;Those questions have exact answers, and they are answerable from your own shell.
Catalyst keeps its rules in named batches you can list, records which ones fired
on your query, and lets you disable any of them and watch the plan change. This
post works through that: the pipeline, what a rule is in the source, the engine
that runs them, and then a query taken apart rule by rule.&lt;/p&gt;

&lt;p&gt;Every plan and number below came from running the query on Spark &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.0.2&lt;/code&gt; with a
plain in-memory catalog session. The counts move between releases, so treat them
as a snapshot of shape rather than constants.&lt;/p&gt;

&lt;h2 id=&quot;why-there-is-an-optimizer-at-all-rdds-versus-dataframes&quot;&gt;Why there is an optimizer at all: RDDs versus DataFrames&lt;/h2&gt;

&lt;p&gt;Catalyst exists because of a change in what you hand Spark. An RDD stage is a
chain of closures, and a closure is opaque: Spark knows it must call your
function on every row and nothing else about it. A DataFrame operation is a
description of intent, which is inspectable.&lt;/p&gt;

&lt;p&gt;The difference is visible rather than philosophical. Both of these do the same
work, filtering on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fare&lt;/code&gt; and keeping one column:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# declarative: Catalyst sees columns and a predicate
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/catalyst/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;fare&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare &amp;gt; 10.0&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# imperative: Catalyst sees three Python functions
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/catalyst/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trip_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;10.0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;p&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]))&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;hasattr&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;queryExecution&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toDebugString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;decode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;splitlines&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Project [trip_id#0L]
+- Filter (isnotnull(fare#3) AND (fare#3 &amp;gt; 10.0))
   +- Relation [trip_id#0L,city_id#1L,rider_id#2L,fare#3,trip_date#4] parquet

False
(2) PythonRDD[8] at RDD at PythonRDD.scala:56 []
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The DataFrame has a plan, so it can be rewritten: the executed plan reads
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;struct&amp;lt;trip_id:bigint,fare:double&amp;gt;&lt;/code&gt;, two columns out of five, and hands the
predicate to Parquet. The RDD has no plan at all, only a lineage of
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MapPartitionsRDD&lt;/code&gt; stages, so there is nothing to prune and nothing to push. It
reads every column and evaluates your comparison in Python, row by row.&lt;/p&gt;

&lt;p&gt;That is the whole trade. You give up control over how the work happens, and in
return a rewriting engine gets to see what the work &lt;em&gt;is&lt;/em&gt;.&lt;/p&gt;

&lt;h2 id=&quot;architecture-four-trees-one-pipeline&quot;&gt;Architecture: four trees, one pipeline&lt;/h2&gt;

&lt;p&gt;Catalyst never mutates anything. Each phase consumes a tree and produces a new
one, which is why you can print any intermediate stage without disturbing the
rest.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;SQL string or&amp;lt;br/&amp;gt;DataFrame calls&quot;] --&amp;gt; B[&quot;&amp;lt;b&amp;gt;Unresolved&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;logical plan&quot;]
  B --&amp;gt;|&quot;&amp;lt;b&amp;gt;Analysis&amp;lt;/b&amp;gt;&quot;| C[&quot;&amp;lt;b&amp;gt;Analyzed&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;logical plan&quot;]
  CAT[(&quot;Catalog /&amp;lt;br/&amp;gt;metastore&quot;)] -.-&amp;gt;|&quot;relations, columns, types&quot;| C
  C --&amp;gt;|&quot;&amp;lt;b&amp;gt;Logical optimization&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;rule batches, to a fixed point&quot;| D[&quot;&amp;lt;b&amp;gt;Optimized&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;logical plan&quot;]
  D --&amp;gt;|&quot;&amp;lt;b&amp;gt;Physical planning&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;strategies&quot;| P1[&quot;physical plan 1&quot;]
  D --&amp;gt; P2[&quot;physical plan 2&quot;]
  D --&amp;gt; P3[&quot;physical plan n&quot;]
  P1 --&amp;gt; CM{&quot;&amp;lt;b&amp;gt;Cost model&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;statistics&quot;}
  P2 --&amp;gt; CM
  P3 --&amp;gt; CM
  CM --&amp;gt;|&quot;selected&quot;| SP[&quot;&amp;lt;b&amp;gt;Selected&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;physical plan&quot;]
  SP --&amp;gt;|&quot;&amp;lt;b&amp;gt;Code generation&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;Tungsten&quot;| RDD[&quot;RDDs of&amp;lt;br/&amp;gt;InternalRow&quot;]
  STATS[(&quot;Table and column&amp;lt;br/&amp;gt;statistics&quot;)] -.-&amp;gt; CM
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;That fan-out in the middle is the part worth being precise about, because the
familiar version of this diagram promises more than Spark delivers. Catalyst does
generate alternative physical plans and does consult a cost model, but for most
queries the alternatives are per-operator choices, above all which join
algorithm to use, and the “cost” consulted is a size estimate rather than a
search over whole plan shapes. The section on rule-based versus cost-based
optimization below measures exactly how thin that layer is.&lt;/p&gt;

&lt;p&gt;The names are worth keeping straight, because the phases fail in different ways
and the error messages differ:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Phase&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Input&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What it does&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Typical failure&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Parse&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Text&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Builds a tree of unresolved nodes&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PARSE_SYNTAX_ERROR&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Analyze&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Unresolved tree&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Resolves names against the catalog, coerces types&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UNRESOLVED_COLUMN&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TABLE_OR_VIEW_NOT_FOUND&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Optimize&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Resolved tree&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Rewrites the tree to an equivalent, cheaper one&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Rarely fails; produces a worse plan instead&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Plan&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Optimized tree&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Chooses physical operators and exchanges&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INTERNAL_ERROR&lt;/code&gt; on an unresolved node&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Codegen&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Physical tree&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Emits and compiles Java per stage&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Falls back to interpreted execution&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;strong&gt;Analysis is not optimization.&lt;/strong&gt; Analysis makes the query &lt;em&gt;meaningful&lt;/em&gt;, so it
adds things: catalog relations, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SubqueryAlias&lt;/code&gt; wrappers, casts. Optimization
makes it &lt;em&gt;cheap&lt;/em&gt;, so it mostly removes things. Reading the two trees side by side
is the single most useful habit in this whole area.&lt;/p&gt;

&lt;h2 id=&quot;what-a-rule-actually-is&quot;&gt;What a rule actually is&lt;/h2&gt;

&lt;p&gt;A rule is an object with one method, from tree to tree. The mechanism is easiest
to see in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ConstantFolding&lt;/code&gt;, whose core is a pattern match over expressions.
These are the two clauses that do the actual folding, quoted from the source:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;    &lt;span class=&quot;c1&quot;&gt;// Skip redundant folding of literals. This rule is technically not necessary. Placing this&lt;/span&gt;
    &lt;span class=&quot;c1&quot;&gt;// here avoids running the next rule for Literal values, which would create a new Literal&lt;/span&gt;
    &lt;span class=&quot;c1&quot;&gt;// object and running eval unnecessarily.&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;l&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Literal&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;l&lt;/span&gt;

    &lt;span class=&quot;c1&quot;&gt;// Fold expressions that are foldable.&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;e&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;e&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;foldable&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;tryFold&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;e&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;isConditionalBranch&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;tryFold&lt;/code&gt; is where the expression is evaluated at planning time:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;  &lt;span class=&quot;k&quot;&gt;private&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;tryFold&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;expr&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Expression&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;isConditionalBranch&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Boolean&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Expression&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;try&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
      &lt;span class=&quot;nv&quot;&gt;Literal&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;create&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;expr&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;freshCopyIfContainsStatefulExpression&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;eval&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nc&quot;&gt;EmptyRow&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;expr&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;dataType&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;catch&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
      &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;NonFatal&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;isConditionalBranch&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&amp;gt;&lt;/span&gt;
        &lt;span class=&quot;c1&quot;&gt;// When doing constant folding inside conditional expressions, we should not fail&lt;/span&gt;
        &lt;span class=&quot;c1&quot;&gt;// during expression evaluation, as the branch we are evaluating may not be reached at&lt;/span&gt;
        &lt;span class=&quot;c1&quot;&gt;// runtime, and we shouldn&apos;t fail the query, to match the original behavior.&lt;/span&gt;
        &lt;span class=&quot;nv&quot;&gt;expr&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;setTagValue&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nc&quot;&gt;FAILED_TO_EVALUATE&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;())&lt;/span&gt;
        &lt;span class=&quot;n&quot;&gt;expr&lt;/span&gt;
    &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is the whole idea: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;e.foldable&lt;/code&gt; is the test for “this expression depends on
no column”, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;eval(EmptyRow)&lt;/code&gt; computes it with no input row, and the result
replaces the subtree as a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Literal&lt;/code&gt;. The surrounding match has other clauses for
cases where folding would be wrong, and the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;catch&lt;/code&gt; exists because an expression
in an untaken &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CASE&lt;/code&gt; branch must not fail the query at planning time. That
pattern repeats across the rule set: a small core, plus the exceptions found the
hard way.&lt;/p&gt;

&lt;p&gt;Two tree-walking helpers appear constantly, and the difference matters when you
write your own:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Helper&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Walks&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Use it for&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;transform&lt;/code&gt; / &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;transformDown&lt;/code&gt; / &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;transformUp&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Plan nodes&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Restructuring operators, for example pushing a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Filter&lt;/code&gt; under a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Join&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;transformAllExpressions&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Expressions inside every node&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Rewriting expressions regardless of which operator holds them&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;transformWithPruning&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Same, but skips subtrees&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Performance: the tree carries bit flags saying which patterns it contains&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;transformWithPruning&lt;/code&gt; variant is a detail worth noticing, because it
explains a line you see in nearly every modern rule. Each tree node carries a set
of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TreePattern&lt;/code&gt; flags, so a rule that only cares about literals can skip whole
subtrees that contain none, rather than visiting every node to discover it has
nothing to do.&lt;/p&gt;

&lt;h2 id=&quot;the-engine-batches-strategies-and-the-fixed-point&quot;&gt;The engine: batches, strategies, and the fixed point&lt;/h2&gt;

&lt;p&gt;Rules do not run in a flat list. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RuleExecutor&lt;/code&gt; runs &lt;strong&gt;batches&lt;/strong&gt;, and each batch
has a strategy that decides how many times its rules run.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  S[&quot;a batch: name, strategy, rules&quot;] --&amp;gt; L{&quot;for each rule&amp;lt;br/&amp;gt;in order&quot;}
  L --&amp;gt; R[&quot;apply rule to the tree&quot;]
  R --&amp;gt; M[&quot;record invocation,&amp;lt;br/&amp;gt;and whether it changed anything&quot;]
  M --&amp;gt; L
  L --&amp;gt;|&quot;end of the rule list&quot;| C{&quot;strategy&quot;}
  C --&amp;gt;|&quot;Once&quot;| D[&quot;done after one pass&quot;]
  C --&amp;gt;|&quot;FixedPoint(n)&quot;| E{&quot;did the tree change?&quot;}
  E --&amp;gt;|&quot;yes, and passes &amp;lt; n&quot;| L
  E --&amp;gt;|&quot;no, tree is stable&quot;| D
  E --&amp;gt;|&quot;passes = n&quot;| W[&quot;log a max-iterations warning,&amp;lt;br/&amp;gt;stop anyway&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The termination test is plan equality, not a change counter:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nf&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;curPlan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;fastEquals&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lastPlan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;nf&quot;&gt;logTrace&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;Fixed point reached for batch ${batch.name} after ${iteration - 1} iterations.&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;continue&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;kc&quot;&gt;false&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That design is what makes the rule set composable. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ConstantFolding&lt;/code&gt; can create a
literal that lets &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PruneFilters&lt;/code&gt; delete an entire subtree on the next pass, and
nobody had to encode that dependency: the batch simply runs again because the
tree changed.&lt;/p&gt;

&lt;p&gt;Reading the real structure out of a session is more informative than any
description of it:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;catalyst&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;master&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local[2]&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;config&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.catalogImplementation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;in-memory&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;batches&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jsparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sessionState&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizer&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;batches&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;batches&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;size&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;batches&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;apply&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;batches&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;apply&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;size&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;39 Finish Analysis 1
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Scala is friendlier for walking the whole list, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-shell&lt;/code&gt; has it to hand:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;bs&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;sessionState&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;optimizer&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;batches&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;batches=${bs.size}&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;distinct rules=${bs.flatMap(_.rules.map(_.ruleName)).distinct.size}&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;once batches=${bs.count(_.strategy.toString.startsWith(&quot;&lt;/span&gt;&lt;span class=&quot;nc&quot;&gt;Once&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;))}&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;nv&quot;&gt;bs&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;take&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;12&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;foreach&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;${b.name}%-46s ${b.strategy}%-46s ${b.rules.size}&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;On this build:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;batches=39   distinct rules=117   once batches=21   fixed-point batches=18

Finish Analysis                                FixedPoint(1,false,null)         1
Rewrite With expression                        FixedPoint(100, ...maxIterations) 1
Eliminate Distinct                             Once                              1
Inline CTE                                     Once                              1
Union                                          FixedPoint(100, ...maxIterations) 3
LocalRelation early                            FixedPoint(100, ...maxIterations) 3
Pullup Correlated Expressions                  Once                              3
Subquery                                       FixedPoint(1,false,null)          2
Replace Operators                              FixedPoint(100, ...maxIterations) 7
Aggregate                                      FixedPoint(100, ...maxIterations) 2
Operator Optimization before Inferring Filters FixedPoint(100, ...maxIterations) 53
Infer Filters                                  Once                              2
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Three things in that listing are worth pausing on.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The big batch is the one people mean by “the optimizer”.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Operator
Optimization before Inferring Filters&lt;/code&gt; holds 53 rules and runs to a fixed point.
It contains the whole familiar cast: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushDownPredicates&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ColumnPruning&lt;/code&gt;,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ConstantFolding&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BooleanSimplification&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CollapseProject&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PruneFilters&lt;/code&gt; and
the rest.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Infer Filters&lt;/code&gt; sits between two copies of it.&lt;/strong&gt; The same 53-rule set runs
again afterwards, as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Operator Optimization after Inferring Filters&lt;/code&gt;, because
inferring a new predicate creates new work for the push-down rules. This is the
fixed-point idea applied at the batch level.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FixedPoint(1, ...)&lt;/code&gt; is not the same as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Once&lt;/code&gt;.&lt;/strong&gt; Both run the rules a single
time, but &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Once&lt;/code&gt; batches are additionally checked for idempotence in Spark’s own
test builds, so a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Once&lt;/code&gt; rule that changes the tree twice is a bug that gets
caught. The iteration cap comes from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.optimizer.maxIterations&lt;/code&gt;, default
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;100&lt;/code&gt;; exceeding it logs a warning in production and throws in testing.&lt;/p&gt;

&lt;h2 id=&quot;one-query-four-trees&quot;&gt;One query, four trees&lt;/h2&gt;

&lt;p&gt;Here is the whole pipeline on a query with a join, an aggregate, a real predicate
and a deliberately silly one.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;catalyst&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;master&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local[2]&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;config&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;4&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;500&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;90000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;float&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;100.0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-0%d&quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;9&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
     &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;20000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;trip_id bigint, city_id bigint, rider_id bigint, fare double, trip_date string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city-%d&quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;IN&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;US&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;DE&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;][&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;500&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;city_id bigint, city_name string, country_code string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/catalyst/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/catalyst/cities&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/catalyst/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createOrReplaceTempView&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/catalyst/cities&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createOrReplaceTempView&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;cities&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  SELECT c.city_name, count(*) AS n
  FROM trips t JOIN cities c ON t.city_id = c.city_id
  WHERE t.fare &amp;gt; 10.0 AND 1 = 1
  GROUP BY c.city_name&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;logical&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;        &lt;span class=&quot;c1&quot;&gt;# parsed
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;analyzed&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Parsed.&lt;/strong&gt; Nothing is resolved. The leading ticks mark unresolved nodes and
attributes, and the tables are &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UnresolvedRelation&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&apos;Aggregate [&apos;c.city_name], [&apos;c.city_name, &apos;count(1) AS n#16]
+- &apos;Filter ((&apos;t.fare &amp;gt; 10.0) AND (1 = 1))
   +- &apos;Join Inner, (&apos;t.city_id = &apos;c.city_id)
      :- &apos;SubqueryAlias t
      :  +- &apos;UnresolvedRelation [trips], [], false
      +- &apos;SubqueryAlias c
         +- &apos;UnresolvedRelation [cities], [], false
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Analyzed.&lt;/strong&gt; The ticks are gone. Every attribute now has an expression id
(&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_id#9L&lt;/code&gt;), the views and aliases are explicit, and note what analysis
&lt;em&gt;added&lt;/em&gt;: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cast(10.0 as double)&lt;/code&gt; around the literal, because the comparison needs
matching types.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Aggregate [city_name#14], [city_name#14, count(1) AS n#16L]
+- Filter ((fare#11 &amp;gt; cast(10.0 as double)) AND (1 = 1))
   +- Join Inner, (city_id#9L = city_id#13L)
      :- SubqueryAlias t
      :  +- SubqueryAlias trips
      :     +- View (`trips`, [trip_id#8L, city_id#9L, rider_id#10L, fare#11, trip_date#12])
      :        +- Relation [trip_id#8L,city_id#9L,rider_id#10L,fare#11,trip_date#12] parquet
      +- SubqueryAlias c
         +- SubqueryAlias cities
            +- View (`cities`, [city_id#13L, city_name#14, country_code#15])
               +- Relation [city_id#13L,city_name#14,country_code#15] parquet
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Optimized.&lt;/strong&gt; This is the interesting one, and five separate rewrites are
visible in it at once:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Aggregate [city_name#14], [city_name#14, count(1) AS n#16L]
+- Project [city_name#14]
   +- Join Inner, (city_id#9L = city_id#13L)
      :- Project [city_id#9L]
      :  +- Filter ((isnotnull(fare#11) AND (fare#11 &amp;gt; 10.0)) AND isnotnull(city_id#9L))
      :     +- Relation [trip_id#8L,city_id#9L,rider_id#10L,fare#11,trip_date#12] parquet
      +- Project [city_id#13L, city_name#14]
         +- Filter isnotnull(city_id#13L)
            +- Relation [city_id#13L,city_name#14,country_code#15] parquet
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1 = 1&lt;/code&gt; has &lt;strong&gt;disappeared&lt;/strong&gt;, folded to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; and then dropped from the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AND&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cast(10.0 as double)&lt;/code&gt; is &lt;strong&gt;gone&lt;/strong&gt;, folded into the literal &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10.0&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SubqueryAlias&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;View&lt;/code&gt; wrappers are &lt;strong&gt;removed&lt;/strong&gt;: they carried naming information analysis needed and execution does not.&lt;/li&gt;
  &lt;li&gt;A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Filter&lt;/code&gt; now sits &lt;strong&gt;under&lt;/strong&gt; the join on each side, pushed down from above it.&lt;/li&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isnotnull(...)&lt;/code&gt; predicates were &lt;strong&gt;added&lt;/strong&gt; on both join keys, which is the optimizer &lt;em&gt;inserting&lt;/em&gt; work because an inner join already discards nulls, and a cheap null check at the scan is worth more than the rows it removes later.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;strong&gt;Physical.&lt;/strong&gt; The scan is where the logical rewrites turn into less IO:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AdaptiveSparkPlan isFinalPlan=false
+- HashAggregate(keys=[city_name#14], functions=[count(1)], ...)
   +- Exchange hashpartitioning(city_name#14, 4), ENSURE_REQUIREMENTS, [plan_id=76]
      +- HashAggregate(keys=[city_name#14], functions=[partial_count(1)], ...)
         +- Project [city_name#14]
            +- BroadcastHashJoin [city_id#9L], [city_id#13L], Inner, BuildRight, false
               :- Project [city_id#9L]
               :  +- Filter ((isnotnull(fare#11) AND (fare#11 &amp;gt; 10.0)) AND isnotnull(city_id#9L))
               :     +- FileScan parquet [city_id#9L,fare#11] ...
                        PushedFilters: [IsNotNull(fare), GreaterThan(fare,10.0), IsNotNull(city_id)],
                        ReadSchema: struct&amp;lt;city_id:bigint,fare:double&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReadSchema: struct&amp;lt;city_id:bigint,fare:double&amp;gt;&lt;/code&gt; is the payoff. The table has
five columns and the scan reads two, because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ColumnPruning&lt;/code&gt; worked out that
nothing else is needed. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushedFilters&lt;/code&gt; is the predicate handed to Parquet
itself, so row groups whose statistics cannot satisfy &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fare &amp;gt; 10.0&lt;/code&gt; are never
decompressed. And the aggregate is split into &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partial_count&lt;/code&gt; before the exchange
and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count&lt;/code&gt; after it, which is map-side pre-aggregation.&lt;/p&gt;

&lt;h2 id=&quot;which-rules-actually-fired&quot;&gt;Which rules actually fired&lt;/h2&gt;

&lt;p&gt;You do not have to infer the responsible rules. Every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt; carries a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryPlanningTracker&lt;/code&gt; that records each rule’s invocations and how many of those
changed the tree:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                     &lt;span class=&quot;c1&quot;&gt;# force analysis and optimization
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tracker&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;it&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;keysIterator&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[]&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;while&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;it&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;hasNext&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;():&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;k&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;it&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;next&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;apply&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;k&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;append&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;k&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;split&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;.&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)[&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;-&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;numInvocations&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;numEffectiveInvocations&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()))&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;n&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;eff&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;sorted&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;key&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;-&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]):&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;eff&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
        &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;46&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; invocations=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;n&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; effective=&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;eff&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;invoked but never effective:&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;e&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;e&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;For the query above:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;PushDownPredicates                             invocations=6    effective=2
Analyzer$ResolveReferences                     invocations=3    effective=2
ColumnPruning                                  invocations=6    effective=1
ConstantFolding                                invocations=5    effective=1
BooleanSimplification                          invocations=5    effective=1
ResolveTimeZone                                invocations=3    effective=1
Analyzer$ResolveRelations                      invocations=3    effective=1
AnsiTypeCoercion$AnsiCombinedTypeCoercionRule  invocations=3    effective=1
Analyzer$ResolveFunctions                      invocations=3    effective=1
InferFiltersFromConstraints                    invocations=1    effective=1
Optimizer$FinishAnalysis                       invocations=1    effective=1

total distinct rules invoked: 217
invoked but never effective: 206
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That table answers the questions from the introduction directly. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1 = 1&lt;/code&gt; went to
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BooleanSimplification&lt;/code&gt;, the casts to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ConstantFolding&lt;/code&gt;, the two-column read to
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ColumnPruning&lt;/code&gt;, and the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isnotnull&lt;/code&gt; predicates to
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;InferFiltersFromConstraints&lt;/code&gt;. Six invocations of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushDownPredicates&lt;/code&gt; with two
effective is the fixed point at work: it kept being retried until it had nothing
left to move.&lt;/p&gt;

&lt;p&gt;The headline number is the last one. &lt;strong&gt;217 rules ran, 11 did something, 206 found
nothing to do.&lt;/strong&gt; Catalyst’s cost is dominated by rules cheaply declining, which
is exactly why the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TreePattern&lt;/code&gt; pruning mentioned earlier exists.&lt;/p&gt;

&lt;h2 id=&quot;a-tour-of-rules-you-can-see-working&quot;&gt;A tour of rules you can see working&lt;/h2&gt;

&lt;p&gt;Each of these is one small query and its optimized plan, so you can watch a
single rule in isolation. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql(q)._jdf.queryExecution().optimizedPlan()&lt;/code&gt; is
the only tool needed.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Rule&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Query fragment&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What the optimized plan shows&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ConstantFolding&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fare * (2 + 3)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(fare#3 * 5.0)&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BooleanSimplification&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fare &amp;gt; 10.0 AND 1 = 1&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;the tautology is dropped&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PruneFilters&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE 1 = 2&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LocalRelation &amp;lt;empty&amp;gt;&lt;/code&gt;, the scan is gone&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NullPropagation&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count(NULL)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0 AS count(NULL)&lt;/code&gt;, no data read&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LikeSimplification&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_name LIKE &apos;city-1%&apos;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;StartsWith(city_name#6, city-1)&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CollapseProject&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;nested &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SELECT&lt;/code&gt; of a computed column&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Project&lt;/code&gt; with the composed expression&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;EliminateLimits&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LIMIT 100&lt;/code&gt; inside &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LIMIT 10&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;a single &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LocalLimit 10&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReplaceDistinctWithAggregate&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SELECT DISTINCT city_id&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Aggregate [city_id#1L], [city_id#1L]&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RewriteNonCorrelatedExists&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE EXISTS (SELECT 1 FROM cities)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;a scalar subquery with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LocalLimit 1&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Two are worth showing in full because the rewrite is drastic.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PruneFilters&lt;/code&gt; deletes the query.&lt;/strong&gt; An always-false predicate does not become a
filter that matches nothing; it becomes an empty relation, and the scan is never
planned:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT trip_id FROM trips WHERE 1 = 2&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; \
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;LocalRelation &amp;lt;empty&amp;gt;, [trip_id#0L]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NullPropagation&lt;/code&gt; answers without reading anything.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count(NULL)&lt;/code&gt; is zero by
definition, so the aggregate is replaced by a literal and the projection below it
is emptied:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Aggregate [0 AS count(NULL)#10L]
+- Project
   +- Relation [trip_id#0L,city_id#1L,rider_id#2L,fare#3,trip_date#4] parquet
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LikeSimplification&lt;/code&gt; is the one with the most practical value, because
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;StartsWith&lt;/code&gt; is a predicate Parquet can evaluate against column statistics and a
general &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LIKE&lt;/code&gt; is not. A leading wildcard (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LIKE &apos;%city-1&apos;&lt;/code&gt;) cannot be rewritten
that way, which is the real reason the advice about leading wildcards exists.&lt;/p&gt;

&lt;h2 id=&quot;turning-a-rule-off&quot;&gt;Turning a rule off&lt;/h2&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.optimizer.excludedRules&lt;/code&gt; takes fully qualified rule names. This is the
fastest way to find out what a rule does, and it is genuinely useful when a rule
misbehaves on your workload.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;Q&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;SELECT trip_id, fare * (2 + 3) AS f5 FROM trips&quot;&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unset&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.optimizer.excludedRules&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;Q&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.optimizer.excludedRules&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
               &lt;span class=&quot;s&quot;&gt;&quot;org.apache.spark.sql.catalyst.optimizer.ConstantFolding&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;Q&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;# default
Project [trip_id#0L, (fare#3 * 5.0) AS f5#8]

# ConstantFolding excluded
Project [trip_id#0L, (fare#3 * cast((2 + 3) as double)) AS f5#9]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The unfolded version computes &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;2 + 3&lt;/code&gt; and a cast for every row. The same trick on
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushDownPredicates&lt;/code&gt; leaves the filter stranded above the scan in a nested shape
rather than combined at the leaf.&lt;/p&gt;

&lt;p&gt;Two limits to know. Some rules cannot be excluded: the optimizer keeps a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;nonExcludableRules&lt;/code&gt; list, 25 entries on this build, covering rewrites that
produce an &lt;em&gt;invalid&lt;/em&gt; plan if skipped rather than merely a slow one. Naming one of
those in the config is silently ignored rather than rejected, so an exclusion
that appears to do nothing may be one of them.&lt;/p&gt;

&lt;h2 id=&quot;writing-your-own-rule&quot;&gt;Writing your own rule&lt;/h2&gt;

&lt;p&gt;A rule is an object with one method, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.experimental.extraOptimizations&lt;/code&gt;
injects it without building a session extension. This runs in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-shell&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;org.apache.spark.sql.catalyst.expressions._&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;org.apache.spark.sql.catalyst.plans.logical._&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;org.apache.spark.sql.catalyst.rules.Rule&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;// Rewrite x * 1 to x, and x + 0 to x.&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;object&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;RemoveIdentityArithmetic&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;extends&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Rule&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;kt&quot;&gt;LogicalPlan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;apply&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;plan&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;LogicalPlan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;LogicalPlan&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;plan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;transformAllExpressions&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Multiply&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;left&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Literal&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;k&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;left&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Multiply&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;left&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Literal&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mf&quot;&gt;1.0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;left&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Add&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;left&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Literal&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;        &lt;span class=&quot;k&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;left&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Add&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;left&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Literal&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mf&quot;&gt;0.0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;      &lt;span class=&quot;k&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;left&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;

&lt;span class=&quot;nv&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/catalyst/trips&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;createOrReplaceTempView&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trips&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;q&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;SELECT trip_id, fare * 1 AS f, rider_id + 0 AS r FROM trips&quot;&lt;/span&gt;

&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;nv&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;experimental&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;extraOptimizations&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Seq&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nc&quot;&gt;RemoveIdentityArithmetic&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;# before
Project [trip_id#0L, (fare#3 * 1.0) AS f#5, rider_id#2L AS r#6L]

# after
Project [trip_id#0L, fare#3 AS f#7, rider_id#2L AS r#8L]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Look closely at the “before” line, because it contains a free lesson. `rider_id&lt;/p&gt;
&lt;ul&gt;
  &lt;li&gt;0&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt; was **already** simplified to &lt;/code&gt;rider_id&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt; by Spark&apos;s existing rules, while
&lt;/code&gt;fare * 1&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt; survived as &lt;/code&gt;(fare#3 * 1.0)&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;. The gap is not an oversight to be smug
about: &lt;/code&gt;x * 1&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt; is only safe to remove for some type and nullability combinations,
and the multiplication here is on a &lt;/code&gt;double` where the literal had to be cast.
The example is useful precisely because half of it was redundant.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;An injected rule lands in its own batch at the end of the pipeline:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Batch(User Provided Optimizers, FixedPoint(100, ...maxIterations),
      List(RemoveIdentityArithmetic))
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FixedPoint&lt;/code&gt; means your rule is retried until the tree stops changing, so it must
be idempotent. A rule that keeps rewriting its own output will spin to
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxIterations&lt;/code&gt; and log a warning, and in a test build it throws.&lt;/p&gt;

&lt;p&gt;For production, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.experimental.extraOptimizations&lt;/code&gt; is a session-local hook.
The supported path is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSessionExtensions&lt;/code&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;injectOptimizerRule&lt;/code&gt;, set
through &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt;, which is also how Iceberg and Hudi install their
rules.&lt;/p&gt;

&lt;h3 id=&quot;testing-a-rule&quot;&gt;Testing a rule&lt;/h3&gt;

&lt;p&gt;A rule is a pure function on a tree, so it needs no cluster to test. Apply it to
a plan and compare:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;optimized&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;assert_rule_effective&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rule_name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;Fail unless the named rule changed the plan for this query.&quot;&quot;&quot;&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tracker&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;it&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;keysIterator&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;while&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;it&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;hasNext&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;():&lt;/span&gt;
        &lt;span class=&quot;n&quot;&gt;key&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;it&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;next&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
        &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;key&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;endswith&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rule_name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
            &lt;span class=&quot;n&quot;&gt;eff&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rules&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;apply&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;key&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;numEffectiveInvocations&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
            &lt;span class=&quot;k&quot;&gt;assert&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;eff&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rule_name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; ran but changed nothing&quot;&lt;/span&gt;
            &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;eff&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;raise&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;AssertionError&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rule_name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt; was never invoked&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;assert&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;fare#&quot;&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;optimized&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT fare * 1 AS f FROM trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# before injection
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;assert_rule_effective&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT trip_id FROM trips WHERE 1 = 2&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;PruneFilters&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Three properties are worth asserting on any rule you write. That it &lt;strong&gt;fires&lt;/strong&gt; at
all, which the tracker answers. That it is &lt;strong&gt;idempotent&lt;/strong&gt;, since it runs in a
fixed-point batch: applying it to its own output must change nothing. And that it
&lt;strong&gt;preserves results&lt;/strong&gt;, which means running the query with and without the rule
and comparing the rows, not just the plans. Spark’s own suites do exactly this,
with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RuleExecutor&lt;/code&gt;’s idempotence check on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Once&lt;/code&gt; batches as the automated
version of the second one.&lt;/p&gt;

&lt;h2 id=&quot;physical-planning-from-what-to-how&quot;&gt;Physical planning: from what to how&lt;/h2&gt;

&lt;p&gt;The optimized logical plan still says nothing about execution. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Join Inner&lt;/code&gt; is a
requirement, not an algorithm. Turning each logical operator into a physical one
is the job of the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkPlanner&lt;/code&gt;, which applies a list of &lt;strong&gt;strategies&lt;/strong&gt; rather
than rules:&lt;/p&gt;

&lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;st&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;sessionState&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;planner&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;strategies&lt;/span&gt;
&lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;strategies=${st.size}&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;nv&quot;&gt;st&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;foreach&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;  &quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getClass&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;getSimpleName&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;replace&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;$&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;strategies=16
  HiveTableScans          LogicalQueryStageStrategy   PythonEvals
  HiveScripts             DataSourceV2Strategy        V2CommandStrategy
  FileSourceStrategy      DataSourceStrategy          SpecialLimits
  Aggregation             Window                      WindowGroupLimit
  JoinSelection           InMemoryScans               SparkScripts
  BasicOperators
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A strategy differs from a rule in what it returns. A rule returns one tree; a
strategy returns a &lt;em&gt;list&lt;/em&gt; of candidate physical plans for a logical operator, and
the planner takes the first that applies. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;JoinSelection&lt;/code&gt; is where
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastHashJoinExec&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoinExec&lt;/code&gt; are chosen between, and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FileSourceStrategy&lt;/code&gt; is where a scan acquires its &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushedFilters&lt;/code&gt; and its pruned
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReadSchema&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;This is also where the first genuinely cost-sensitive decision happens, because
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;JoinSelection&lt;/code&gt; compares a size estimate against
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt;. The strategy ordering, the build-side
rules per join type and the full decision procedure are their own subject, taken
apart in &lt;a href=&quot;/SparkJoinsInDepth/&quot;&gt;Apache Spark joins in depth&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;rule-based-versus-cost-based-optimization&quot;&gt;Rule-based versus cost-based optimization&lt;/h2&gt;

&lt;p&gt;This is the distinction that most descriptions of Catalyst blur, and it is
measurable.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Rule-based optimization (RBO)&lt;/strong&gt; applies rewrites that are always improvements.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ConstantFolding&lt;/code&gt; never makes a plan worse, so no cost model is consulted and no
statistics are needed. Every rule in the earlier tour is RBO.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Cost-based optimization (CBO)&lt;/strong&gt; chooses between alternatives using estimated
cardinalities, which means it needs statistics about your data, which means
somebody has to compute them.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Rule-based&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Cost-based&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Decides by&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;A pattern in the tree&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Estimated rows and sizes&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Needs statistics&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes, computed by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ANALYZE TABLE&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;On by default&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.cbo.enabled&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Can make a plan worse&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Not by design&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes, on a bad estimate&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Examples&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Constant folding, predicate pushdown, column pruning&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Join reordering, and the size estimates behind broadcast selection&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h3 id=&quot;what-statistics-spark-actually-holds&quot;&gt;What statistics Spark actually holds&lt;/h3&gt;

&lt;p&gt;A fresh table has none, which is worth seeing before anything else:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;DESCRIBE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;EXTENDED&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fact&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;-- Statistics row: absent&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ANALYZE TABLE&lt;/code&gt; fills in two different things, and the distinction matters:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;-- table level: a row count and a size&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ANALYZE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fact&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;COMPUTE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;STATISTICS&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;-- column level: per-column cardinality, nulls, bounds, and a histogram&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ANALYZE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fact&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;COMPUTE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;STATISTICS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FOR&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;COLUMNS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rider_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;fact       1779205 bytes, 200000 rows
dim_city   4818 bytes, 400 rows
dim_rider  449573 bytes, 50000 rows
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Column statistics are the interesting ones, because selectivity estimation is
impossible without them:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;DESCRIBE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;EXTENDED&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fact&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;min                    0
max                    399
num_nulls              0
distinct_count         397
avg_col_len            8
max_col_len            8
histogram              height: 787.4015748031496, num_of_bins: 254
bin_0                  lower_bound: 0.0, upper_bound: 1.0, distinct_count: 2
bin_1                  lower_bound: 1.0, upper_bound: 3.0, distinct_count: 2
bin_2                  lower_bound: 3.0, upper_bound: 4.0, distinct_count: 1
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is an equi-height histogram: 254 bins each holding about 787 rows, so the
bin &lt;em&gt;widths&lt;/em&gt; vary and dense ranges get more bins. It is built only when
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.statistics.histogram.enabled&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;, which is not the default, and
it is what lets Catalyst estimate a range predicate rather than guessing.&lt;/p&gt;

&lt;h3 id=&quot;the-same-query-with-and-without-cbo&quot;&gt;The same query, with and without CBO&lt;/h3&gt;

&lt;p&gt;Now the measurement. One filter, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain(&quot;cost&quot;)&lt;/code&gt; twice:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.cbo.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;true&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT city_id FROM fact WHERE fare &amp;gt; 10.0&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;cost&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.cbo.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;false&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT city_id FROM fact WHERE fare &amp;gt; 10.0&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;cost&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;# cbo = true, with column statistics
Project [city_id#1L], Statistics(sizeInBytes=2.3 MiB, rowCount=1.50E+5)
+- Filter (isnotnull(fare#3) AND (fare#3 &amp;gt; 10.0)), Statistics(sizeInBytes=5.7 MiB, rowCount=1.50E+5)
   +- Relation ... fact[...] parquet, Statistics(sizeInBytes=7.6 MiB, rowCount=2.00E+5)

# cbo = false
Project [city_id#1L], Statistics(sizeInBytes=695.0 KiB)
+- Filter (isnotnull(fare#3) AND (fare#3 &amp;gt; 10.0)), Statistics(sizeInBytes=1737.5 KiB)
   +- Relation ... fact[...] parquet, Statistics(sizeInBytes=1737.5 KiB)
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Two differences, both important:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;With CBO there is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rowCount&lt;/code&gt;, and the filter &lt;strong&gt;reduces&lt;/strong&gt; it, from 200,000 to 150,000. That is a selectivity estimate computed from the histogram.&lt;/li&gt;
  &lt;li&gt;Without CBO there is no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rowCount&lt;/code&gt; at all, and the filter does &lt;strong&gt;not&lt;/strong&gt; change &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sizeInBytes&lt;/code&gt;: 1737.5 KiB in, 1737.5 KiB out.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The second line is the one to remember. Off the default path, a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE&lt;/code&gt; clause
does not make Spark expect fewer rows, so any later decision that depends on size
is made as though the filter were not there. That is the mechanism behind a great
many “why did it not broadcast” questions.&lt;/p&gt;

&lt;h3 id=&quot;join-reordering-and-the-honest-result&quot;&gt;Join reordering, and the honest result&lt;/h3&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CostBasedJoinReorder&lt;/code&gt; is the rule people mean by “Spark has a CBO”. It sits in a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Join Reorder&lt;/code&gt; batch with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FixedPoint(1)&lt;/code&gt; and needs both
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.cbo.enabled&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.cbo.joinReorder.enabled&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;So I wrote the joins in a deliberately bad order, big table first, computed
table and column statistics for all three tables, and compared:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;Q&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;SELECT count(*) FROM dim_rider r
       JOIN fact f     ON f.rider_id = r.rider_id
       JOIN dim_city c ON f.city_id  = c.city_id
       WHERE c.city_name = &apos;city-7&apos;&quot;&quot;&quot;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The resulting join order was &lt;strong&gt;identical&lt;/strong&gt; with CBO on and off, and the tracker
says why:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;CostBasedJoinReorder   invocations=1  effective=0
ReorderJoin            invocations=5  effective=1
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReorderJoin&lt;/code&gt;, which is &lt;strong&gt;rule-based&lt;/strong&gt; and always on, had already moved the joins
into a sensible order. By the time the cost-based rule ran there was nothing left
for it to improve, so it declined.&lt;/p&gt;

&lt;p&gt;That is not a claim that &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CostBasedJoinReorder&lt;/code&gt; never helps. It is a claim about
where the value usually comes from: the rule-based rewrites do the heavy lifting,
and CBO is a narrow addition that needs statistics you must maintain. Treat
“enable CBO and joins get reordered” as something to verify on your own workload
with the tracker, rather than as a given.&lt;/p&gt;

&lt;h2 id=&quot;codegen-where-the-tree-stops-being-a-tree&quot;&gt;Codegen, where the tree stops being a tree&lt;/h2&gt;

&lt;p&gt;The physical plan is the last tree. Tungsten then collapses runs of operators
into one generated Java class, so there is no per-row virtual call between a
filter and a projection.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT trip_id + 1 AS x FROM trips WHERE fare &amp;gt; 10.0&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;codegen&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Found 1 WholeStageCodegen subtrees.
== Subtree 1 / 1 (maxMethodCodeSize:359; maxConstantPoolSize:173(0.26% used); numInnerClasses:0) ==
*(1) Project [(trip_id#0L + 1) AS x#9L]
+- *(1) Filter (isnotnull(fare#3) AND (fare#3 &amp;gt; 10.0))
   +- *(1) ColumnarToRow
      +- FileScan parquet [trip_id#0L,fare#3] ...

Generated code:
/* 001 */ public Object generate(Object[] references) {
/* 002 */   return new GeneratedIteratorForCodegenStage1(references);
/* 003 */ }
/* 005 */ // codegenStageId=1
/* 006 */ final class GeneratedIteratorForCodegenStage1 extends
/* 006 */     org.apache.spark.sql.execution.BufferedRowIterator {
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*(1)&lt;/code&gt; prefix marks operators fused into codegen stage 1. An operator without
the star is not generating code, which is the fastest way to spot a fallback. The
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxMethodCodeSize&lt;/code&gt; figure matters because the JVM refuses to JIT-compile very
large methods, so Spark gives up on codegen for a stage past
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.codegen.hugeMethodLimit&lt;/code&gt; and runs it interpreted instead.&lt;/p&gt;

&lt;h2 id=&quot;dynamic-partition-pruning&quot;&gt;Dynamic partition pruning&lt;/h2&gt;

&lt;p&gt;Dynamic partition pruning (DPP) is the runtime trick for the classic star-schema
shape: a partitioned fact table joined to a filtered dimension. The partition
values that survive the dimension filter are not known at planning time, so
Catalyst plants a subquery that supplies them once the dimension side has been
broadcast, and the fact scan then lists only those partitions.&lt;/p&gt;

&lt;p&gt;It is on by default:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.optimizer.dynamicPartitionPruning.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# true
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The shape it looks for:&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt;   &lt;span class=&quot;n&quot;&gt;sales&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;JOIN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;dates&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;d&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ON&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;day&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;d&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;day&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;-- s.day is a partition column&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt;  &lt;span class=&quot;n&quot;&gt;d&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;is_promo&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;true&lt;/span&gt;                          &lt;span class=&quot;c1&quot;&gt;-- a filter on the dimension&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Worth reporting honestly: &lt;strong&gt;I could not get it to fire on this local setup.&lt;/strong&gt;
With a 12-partition, 400,000-row partitioned &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sales&lt;/code&gt; table, a 12-row dimension
that does get broadcast, statistics computed on both, and adaptive execution
disabled, the plan came back with a plain partition filter every time:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;join operator    : BroadcastHashJoin [day#2], [day#3], Inner, BuildRight
PartitionFilters : [isnotnull(day#2)]
dynamicpruning in optimized plan: False
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The tracker shows the rule ran and declined rather than being absent:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;PartitionPruning                inv=1 eff=0
CleanupDynamicPruningFilters    inv=1 eff=0
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;I tried &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dynamicPartitionPruning.useStats=false&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fallbackFilterRatio=0.01&lt;/code&gt;, which are the two knobs on its cost check, and the
result did not change. The most likely explanation is that on a table this small
the estimated saving never clears the cost of the extra subquery, but I did not
establish which condition failed, so I am reporting the observation rather than a
cause.&lt;/p&gt;

&lt;p&gt;The useful takeaway is the diagnostic, not the disappointment. DPP is a rule like
any other, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PartitionPruning&lt;/code&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;eff=0&lt;/code&gt; in the tracker tells you it
considered your query and said no, which is a different problem from it being
switched off. The marker to look for when it does apply is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dynamicpruning#...&lt;/code&gt; inside &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PartitionFilters&lt;/code&gt;, or a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SubqueryBroadcast&lt;/code&gt; node in
the physical plan.&lt;/p&gt;

&lt;h2 id=&quot;adaptive-query-execution&quot;&gt;Adaptive query execution&lt;/h2&gt;

&lt;p&gt;Everything up to here happens before any data moves, from estimates. Adaptive
query execution re-plans after each shuffle stage completes, using measured
sizes, and it is on by default.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;optimized logical plan&quot;] --&amp;gt; B[&quot;physical plan&quot;]
  B --&amp;gt; C[&quot;AdaptiveSparkPlan&amp;lt;br/&amp;gt;isFinalPlan=false&quot;]
  C --&amp;gt; D[&quot;run the next stage&quot;]
  D --&amp;gt; E[&quot;real statistics from&amp;lt;br/&amp;gt;the completed stage&quot;]
  E --&amp;gt; F{&quot;re-optimize the rest&quot;}
  F --&amp;gt;|&quot;coalesce small partitions&quot;| D
  F --&amp;gt;|&quot;split a skewed partition&quot;| D
  F --&amp;gt;|&quot;switch the join strategy&quot;| D
  F --&amp;gt;|&quot;nothing left to run&quot;| G[&quot;isFinalPlan=true&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The thing to know before testing any of this: you have to execute &lt;strong&gt;the plan you
are holding&lt;/strong&gt; and then read the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt; again. Calling
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.count()&lt;/code&gt; builds a &lt;em&gt;different&lt;/em&gt; query, so the plan you inspect afterwards was
never the one that ran. Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt;, or keep a handle on the
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT city_id, count(*) c FROM fact GROUP BY city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;splitlines&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# before
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                                          &lt;span class=&quot;c1&quot;&gt;# run THIS plan
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;splitlines&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# after
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AdaptiveSparkPlan isFinalPlan=false
AdaptiveSparkPlan isFinalPlan=true
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Coalescing partitions.&lt;/strong&gt; With &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; left at 200 and
400 groups to produce, the final plan shows the shuffle being read back as fewer,
larger partitions:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;+- AQEShuffleRead coalesced
   +- ShuffleQueryStage 0
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Switching the join strategy.&lt;/strong&gt; This is the most dramatic one, because the
operator changes after planning. Broadcasting is disabled at plan time and
allowed at runtime:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;-1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10MB&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) FROM fact f JOIN dim_city c ON f.city_id = c.city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;q&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;planned&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;q&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;final&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;qe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;planned  : SortMergeJoin [city_id#3L], [city_id#10L], Inner
executed : *(3) BroadcastHashJoin [city_id#3L], [city_id#10L], Inner, BuildRight, false
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A sort-merge join at plan time became a broadcast hash join at run time, once the
measured size of the build side was known. Note that the two thresholds are
separate configs, so plan-time and runtime broadcasting can be governed by
different numbers.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Splitting a skewed partition.&lt;/strong&gt; With a key holding most of the rows, AQE
divides the oversized partition into several and the join operator says so:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;# skewJoin.enabled = true
*(5) SortMergeJoin(skew=true) [city_id#1L], [city_id#6L], Inner
  +- AQEShuffleRead coalesced and skewed

# skewJoin.enabled = false
*(5) SortMergeJoin [city_id#1L], [city_id#6L], Inner
  +- AQEShuffleRead coalesced
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;(skew=true)&lt;/code&gt; on the operator and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;and skewed&lt;/code&gt; on the shuffle read are the two
markers. The thresholds are strict and are the usual reason nothing happens: a
partition counts as skewed only when it is larger than
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;skewJoin.skewedPartitionFactor&lt;/code&gt; (default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;5.0&lt;/code&gt;) times the median &lt;strong&gt;and&lt;/strong&gt; larger
than &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;skewJoin.skewedPartitionThresholdInBytes&lt;/code&gt; (default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;256MB&lt;/code&gt;). On data where
no partition reaches 256 MB the rule can never fire, however lopsided the
distribution is. Skew, and the six fixes that do not depend on AQE, are taken
apart in &lt;a href=&quot;/SparkDataSkew/&quot;&gt;data skew in Apache Spark&lt;/a&gt;,
and adaptive execution itself in
&lt;a href=&quot;/SparkAQE/&quot;&gt;adaptive query execution in Apache Spark&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&quot;debugging-and-inspection&quot;&gt;Debugging and inspection&lt;/h2&gt;

&lt;p&gt;Five tools, in the order they are usually worth reaching for.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain(mode)&lt;/code&gt;.&lt;/strong&gt; The modes are not interchangeable:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Mode&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Shows&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;&quot;simple&quot;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The physical plan only, the default&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;&quot;extended&quot;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;All four trees: parsed, analyzed, optimized, physical&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;&quot;cost&quot;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Optimized plan annotated with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Statistics(...)&lt;/code&gt; per node&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;&quot;codegen&quot;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The generated Java for each whole-stage subtree&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;&quot;formatted&quot;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Physical plan as a numbered tree plus per-operator detail&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain(&quot;extended&quot;)&lt;/code&gt; is the one to start with, because seeing analyzed and
optimized next to each other is what makes a missing rewrite obvious.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The rule tracker&lt;/strong&gt;, shown earlier, which names the rule that changed the plan.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The scan line.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReadSchema&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushedFilters&lt;/code&gt; on a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FileScan&lt;/code&gt; are the only
proof that pruning and pushdown reached the file format rather than stopping at a
Spark-side &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Filter&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Rule-level logs.&lt;/strong&gt; Setting the logger for
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.sql.catalyst.rules.RuleExecutor&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TRACE&lt;/code&gt; makes Spark print
the plan change per rule and the fixed point being reached per batch. It is
verbose and definitive, and it is the tool for a rule that is firing when you did
not expect it.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The SQL tab in the UI.&lt;/strong&gt; For anything adaptive this beats &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt;, because
the tab shows the final plan with the actual row counts and spill per operator,
where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; shows the intent.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Catalyst is a cost-based optimizer.”&lt;/strong&gt; Barely. On the join-and-aggregate
query above, 217 rules were invoked and 11 changed the plan, and all of the
useful ones were unconditional rewrites. With CBO explicitly enabled and full
column statistics computed, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CostBasedJoinReorder&lt;/code&gt; ran and changed nothing
(&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;inv=1 eff=0&lt;/code&gt;) while the rule-based &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReorderJoin&lt;/code&gt; did the work (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;inv=5 eff=1&lt;/code&gt;).&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE&lt;/code&gt; clause makes Spark expect fewer rows.”&lt;/strong&gt; Only with column
statistics. Without them the estimate passes straight through the filter:
1737.5 KiB in, 1737.5 KiB out, and no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rowCount&lt;/code&gt; at all. Every later decision
that depends on size, broadcast selection above all, is then made as though the
filter were not there.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“The plan &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; prints is the plan that ran.”&lt;/strong&gt; It is the plan before
adaptive execution has re-planned anything, which the output says as
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=false&lt;/code&gt;. Read the final plan from the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt; after
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt;, or from the SQL tab.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Turning a rule off with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;excludedRules&lt;/code&gt; is a supported tuning knob.”&lt;/strong&gt; It is a
diagnostic. Some rules cannot be excluded at all, and the optimizer silently
ignores a request to exclude one of the 25 in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;nonExcludableRules&lt;/code&gt;, so an
exclusion that appears to do nothing may simply have been dropped.&lt;/p&gt;

&lt;h2 id=&quot;production-notes&quot;&gt;Production notes&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Read the analyzed and optimized plans together.&lt;/strong&gt; The diff between them is the optimizer’s entire contribution, and it is where a missing pushdown or a surprise cast becomes obvious.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Use the tracker rather than guessing.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;queryExecution.tracker().rules()&lt;/code&gt; names the rule that changed your plan, which turns an argument into a lookup.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Check &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReadSchema&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushedFilters&lt;/code&gt; on every scan you care about.&lt;/strong&gt; They are the only proof that pruning and pushdown actually reached the file format.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Do not expect a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Filter&lt;/code&gt; to reduce the size estimate.&lt;/strong&gt; Without column statistics there is no selectivity, so the estimate passes through unchanged and broadcast decisions are made on the unfiltered size.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Reach for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;excludedRules&lt;/code&gt; to diagnose, not to fix.&lt;/strong&gt; It is excellent for understanding a rule and a poor permanent setting, and some rules cannot be excluded at all.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Keep a custom rule idempotent.&lt;/strong&gt; It runs in a fixed-point batch, so a rule that rewrites its own output spins until the iteration cap.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Prefer &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSessionExtensions&lt;/code&gt; over &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;extraOptimizations&lt;/code&gt; in production.&lt;/strong&gt; The experimental hook is session-local and Scala-only; the extension point is the one other projects use.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Compute column statistics, not just table statistics.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;COMPUTE STATISTICS&lt;/code&gt; alone gives a row count; only &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FOR COLUMNS&lt;/code&gt; gives the cardinality and histogram that let a filter reduce an estimate.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Verify that CBO earned its keep before leaving it on.&lt;/strong&gt; Enable it, run your query, and check whether &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CostBasedJoinReorder&lt;/code&gt; reports a non-zero effective count. In the test here the rule-based &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReorderJoin&lt;/code&gt; had already done the work.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Execute the plan you are holding before reading it back.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.count()&lt;/code&gt; builds a different query, so an AQE check done that way inspects a plan that never ran. Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; and re-read the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Why did a cast appear in my plan that I did not write?&lt;/strong&gt;
Analysis added it, before the optimizer ran. Type coercion is part of making the
query meaningful, so comparing a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;double&lt;/code&gt; column to a decimal literal inserts a
cast. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ConstantFolding&lt;/code&gt; then usually removes it again by folding the literal, but
only if the cast is on the literal side.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Which rule removed my filter?&lt;/strong&gt;
Ask the tracker. If the filter vanished entirely rather than moving, the usual
answers are &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PruneFilters&lt;/code&gt; for an always-true or always-false predicate, and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BooleanSimplification&lt;/code&gt; for a redundant clause inside an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AND&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OR&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Is Catalyst cost-based?&lt;/strong&gt;
Barely, and the post measures it. The large majority of rules are unconditional
rewrites that are always wins, which is rule-based optimization. Statistics are
used for broadcast-join selection and, if you explicitly enable CBO and compute
column statistics, for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CostBasedJoinReorder&lt;/code&gt;. In the join-reordering test above
that cost-based rule ran and changed nothing, because the rule-based
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReorderJoin&lt;/code&gt; had already produced the same order. Treat “Catalyst is a
cost-based optimizer” as the wrong mental model.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why does &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE&lt;/code&gt; not reduce the size estimate?&lt;/strong&gt;
Because without column statistics there is no selectivity to apply, so the
estimate passes through the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Filter&lt;/code&gt; unchanged. Compute
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ANALYZE TABLE ... COMPUTE STATISTICS FOR COLUMNS&lt;/code&gt; and turn on
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.cbo.enabled&lt;/code&gt;, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain(&quot;cost&quot;)&lt;/code&gt; starts showing a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rowCount&lt;/code&gt; that
the filter actually reduces.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why is dynamic partition pruning not happening?&lt;/strong&gt;
Check the tracker before the config. If &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PartitionPruning&lt;/code&gt; shows
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;inv=1 eff=0&lt;/code&gt; the rule considered your query and declined, which is a cost
decision rather than a switched-off feature. It also needs the join key to be an
actual partition column of the fact table and, by default, a reusable broadcast
of the dimension side.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why is my predicate not pushed into the scan?&lt;/strong&gt;
Push-down happens in stages: the optimizer moves the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Filter&lt;/code&gt; down the logical
tree, and then the data source decides what it can accept. A predicate on a
non-deterministic expression, a UDF, or a column the format cannot filter on
stops at the scan boundary and appears in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataFilters&lt;/code&gt; but not &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushedFilters&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Does the optimizer ever make a plan worse?&lt;/strong&gt;
It can, and the usual mechanism is a bad size estimate rather than a bad rule.
The rewrites themselves are equivalence-preserving; the choices that depend on
statistics are the ones that go wrong.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;How do I see rules running as they fire?&lt;/strong&gt;
Set the log level for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.sql.catalyst.rules.RuleExecutor&lt;/code&gt; to
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TRACE&lt;/code&gt;. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RuleExecutor&lt;/code&gt; logs the fixed point being reached per batch and the plan
changes per rule, which is verbose but definitive.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;Catalyst is easier to reason about once you stop thinking of it as an optimizer
and start thinking of it as a tree rewriter with an execution schedule. There are
four trees, each produced from the last. There are batches of small rules, each
either run once or run until the tree stops changing. Nearly every rule is a
pattern match and a replacement, and nearly every rule declines to apply.&lt;/p&gt;

&lt;p&gt;That framing makes the debugging loop concrete. A surprising plan is not a
mystery about a black box; it is a question about which rule fired, and the
tracker answers it by name. A rule you distrust can be switched off and the
difference read straight out of the plan. A rewrite you want that Spark does not
do is about ten lines in a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Rule[LogicalPlan]&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;The number worth remembering from all of this is 11 of 217. On an ordinary join
and aggregate, eleven rules changed the plan and two hundred and six had nothing
to say. The optimizer’s power is not in any single clever rewrite but in having
enough small, safe, composable ones that the useful few always get their turn.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/catalyst/src/main/scala/org/apache/spark/sql/catalyst/optimizer/Optimizer.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Optimizer.scala&lt;/code&gt;&lt;/a&gt;, whose &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;defaultBatches&lt;/code&gt; is the batch list and order quoted above&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/catalyst/src/main/scala/org/apache/spark/sql/catalyst/rules/RuleExecutor.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RuleExecutor.scala&lt;/code&gt;&lt;/a&gt; for the fixed-point loop, the iteration cap and the idempotence check&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/core/src/main/scala/org/apache/spark/sql/execution/QueryExecution.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;QueryExecution.scala&lt;/code&gt;&lt;/a&gt;, where the four phases are the lazy members you printed&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/sql-performance-tuning.html&quot;&gt;SQL performance tuning&lt;/a&gt; for the CBO and adaptive-execution configs named here&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheSparkArchitecture/&quot;&gt;Apache Spark architecture&lt;/a&gt; for the scheduler and executor machinery the physical plan is handed to&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Hudi, Apache Iceberg, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="Catalyst" /><category term="Optimizer" /><category term="Internals" /><category term="Codegen" /><summary type="html">Why there is an optimizer at all: RDDs versus DataFrames Architecture: four trees, one pipeline What a rule actually is The engine: batches, strategies, and the fixed point One query, four trees Which rules actually fired A tour of rules you can see working Turning a rule off Writing your own rule Testing a rule Physical planning: from what to how Rule-based versus cost-based optimization What statistics Spark actually holds The same query, with and without CBO Join reordering, and the honest result Codegen, where the tree stops being a tree Dynamic partition pruning Adaptive query execution Debugging and inspection Common misconceptions Production notes Frequently asked questions Conclusion References Trademarks TL;DR Catalyst is a tree rewriter. Every phase takes a tree and returns a tree, and almost every optimization is one small function matching a pattern and returning a replacement. A query passes through four trees: parsed, analyzed, optimized, physical. explain() shows them, and the difference between analyzed and optimized is the entire optimizer’s work made visible. Rules run in batches, each either Once or to a fixed point, meaning repeatedly until the tree stops changing, which is how one rule enables another with nobody ordering them by hand. On a single join-and-aggregate query Spark invoked over two hundred rules and eleven changed the plan. You can switch a rule off with spark.sql.optimizer.excludedRules and add your own in about ten lines, which is the fastest way to understand what any of them actually does. Almost all of it is rule-based. The cost-based part is a thin layer that needs statistics you have to compute yourself, and in the join-reordering test below the rule-based ReorderJoin did the work while CostBasedJoinReorder ran and changed nothing. Most explanations of Catalyst stop at a diagram with four boxes in it. That is a fair summary and it is not much use when a plan does something surprising, because the interesting questions are one level down. Which rule removed my filter? Why did a cast appear? Why did the optimizer not push this predicate? What runs before what? Those questions have exact answers, and they are answerable from your own shell. Catalyst keeps its rules in named batches you can list, records which ones fired on your query, and lets you disable any of them and watch the plan change. This post works through that: the pipeline, what a rule is in the source, the engine that runs them, and then a query taken apart rule by rule. Every plan and number below came from running the query on Spark 4.0.2 with a plain in-memory catalog session. The counts move between releases, so treat them as a snapshot of shape rather than constants. Why there is an optimizer at all: RDDs versus DataFrames Catalyst exists because of a change in what you hand Spark. An RDD stage is a chain of closures, and a closure is opaque: Spark knows it must call your function on every row and nothing else about it. A DataFrame operation is a description of intent, which is inspectable. The difference is visible rather than philosophical. Both of these do the same work, filtering on fare and keeping one column: # declarative: Catalyst sees columns and a predicate df = (spark.read.parquet(&quot;/tmp/catalyst/trips&quot;) .select(&quot;trip_id&quot;, &quot;city_id&quot;, &quot;fare&quot;) .filter(&quot;fare &amp;gt; 10.0&quot;) .select(&quot;trip_id&quot;)) print(df._jdf.queryExecution().optimizedPlan().toString()) # imperative: Catalyst sees three Python functions rdd = (spark.read.parquet(&quot;/tmp/catalyst/trips&quot;).rdd .map(lambda r: (r.trip_id, r.fare)) .filter(lambda p: p[1] &amp;gt; 10.0) .map(lambda p: p[0])) print(hasattr(rdd, &quot;queryExecution&quot;)) print(rdd.toDebugString().decode().splitlines()[0]) Project [trip_id#0L] +- Filter (isnotnull(fare#3) AND (fare#3 &amp;gt; 10.0)) +- Relation [trip_id#0L,city_id#1L,rider_id#2L,fare#3,trip_date#4] parquet False (2) PythonRDD[8] at RDD at PythonRDD.scala:56 [] The DataFrame has a plan, so it can be rewritten: the executed plan reads struct&amp;lt;trip_id:bigint,fare:double&amp;gt;, two columns out of five, and hands the predicate to Parquet. The RDD has no plan at all, only a lineage of MapPartitionsRDD stages, so there is nothing to prune and nothing to push. It reads every column and evaluates your comparison in Python, row by row. That is the whole trade. You give up control over how the work happens, and in return a rewriting engine gets to see what the work is. Architecture: four trees, one pipeline Catalyst never mutates anything. Each phase consumes a tree and produces a new one, which is why you can print any intermediate stage without disturbing the rest. flowchart TB A[&quot;SQL string or&amp;lt;br/&amp;gt;DataFrame calls&quot;] --&amp;gt; B[&quot;&amp;lt;b&amp;gt;Unresolved&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;logical plan&quot;] B --&amp;gt;|&quot;&amp;lt;b&amp;gt;Analysis&amp;lt;/b&amp;gt;&quot;| C[&quot;&amp;lt;b&amp;gt;Analyzed&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;logical plan&quot;] CAT[(&quot;Catalog /&amp;lt;br/&amp;gt;metastore&quot;)] -.-&amp;gt;|&quot;relations, columns, types&quot;| C C --&amp;gt;|&quot;&amp;lt;b&amp;gt;Logical optimization&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;rule batches, to a fixed point&quot;| D[&quot;&amp;lt;b&amp;gt;Optimized&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;logical plan&quot;] D --&amp;gt;|&quot;&amp;lt;b&amp;gt;Physical planning&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;strategies&quot;| P1[&quot;physical plan 1&quot;] D --&amp;gt; P2[&quot;physical plan 2&quot;] D --&amp;gt; P3[&quot;physical plan n&quot;] P1 --&amp;gt; CM{&quot;&amp;lt;b&amp;gt;Cost model&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;statistics&quot;} P2 --&amp;gt; CM P3 --&amp;gt; CM CM --&amp;gt;|&quot;selected&quot;| SP[&quot;&amp;lt;b&amp;gt;Selected&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;physical plan&quot;] SP --&amp;gt;|&quot;&amp;lt;b&amp;gt;Code generation&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;Tungsten&quot;| RDD[&quot;RDDs of&amp;lt;br/&amp;gt;InternalRow&quot;] STATS[(&quot;Table and column&amp;lt;br/&amp;gt;statistics&quot;)] -.-&amp;gt; CM That fan-out in the middle is the part worth being precise about, because the familiar version of this diagram promises more than Spark delivers. Catalyst does generate alternative physical plans and does consult a cost model, but for most queries the alternatives are per-operator choices, above all which join algorithm to use, and the “cost” consulted is a size estimate rather than a search over whole plan shapes. The section on rule-based versus cost-based optimization below measures exactly how thin that layer is. The names are worth keeping straight, because the phases fail in different ways and the error messages differ: Phase Input What it does Typical failure Parse Text Builds a tree of unresolved nodes PARSE_SYNTAX_ERROR Analyze Unresolved tree Resolves names against the catalog, coerces types UNRESOLVED_COLUMN, TABLE_OR_VIEW_NOT_FOUND Optimize Resolved tree Rewrites the tree to an equivalent, cheaper one Rarely fails; produces a worse plan instead Plan Optimized tree Chooses physical operators and exchanges INTERNAL_ERROR on an unresolved node Codegen Physical tree Emits and compiles Java per stage Falls back to interpreted execution Analysis is not optimization. Analysis makes the query meaningful, so it adds things: catalog relations, SubqueryAlias wrappers, casts. Optimization makes it cheap, so it mostly removes things. Reading the two trees side by side is the single most useful habit in this whole area. What a rule actually is A rule is an object with one method, from tree to tree. The mechanism is easiest to see in ConstantFolding, whose core is a pattern match over expressions. These are the two clauses that do the actual folding, quoted from the source: // Skip redundant folding of literals. This rule is technically not necessary. Placing this // here avoids running the next rule for Literal values, which would create a new Literal // object and running eval unnecessarily. case l: Literal =&amp;gt; l // Fold expressions that are foldable. case e if e.foldable =&amp;gt; tryFold(e, isConditionalBranch) and tryFold is where the expression is evaluated at planning time: private def tryFold(expr: Expression, isConditionalBranch: Boolean): Expression = { try { Literal.create(expr.freshCopyIfContainsStatefulExpression().eval(EmptyRow), expr.dataType) } catch { case NonFatal(_) if isConditionalBranch =&amp;gt; // When doing constant folding inside conditional expressions, we should not fail // during expression evaluation, as the branch we are evaluating may not be reached at // runtime, and we shouldn&apos;t fail the query, to match the original behavior. expr.setTagValue(FAILED_TO_EVALUATE, ()) expr } } That is the whole idea: e.foldable is the test for “this expression depends on no column”, eval(EmptyRow) computes it with no input row, and the result replaces the subtree as a Literal. The surrounding match has other clauses for cases where folding would be wrong, and the catch exists because an expression in an untaken CASE branch must not fail the query at planning time. That pattern repeats across the rule set: a small core, plus the exceptions found the hard way. Two tree-walking helpers appear constantly, and the difference matters when you write your own: Helper Walks Use it for transform / transformDown / transformUp Plan nodes Restructuring operators, for example pushing a Filter under a Join transformAllExpressions Expressions inside every node Rewriting expressions regardless of which operator holds them transformWithPruning Same, but skips subtrees Performance: the tree carries bit flags saying which patterns it contains The transformWithPruning variant is a detail worth noticing, because it explains a line you see in nearly every modern rule. Each tree node carries a set of TreePattern flags, so a rule that only cares about literals can skip whole subtrees that contain none, rather than visiting every node to discover it has nothing to do. The engine: batches, strategies, and the fixed point Rules do not run in a flat list. RuleExecutor runs batches, and each batch has a strategy that decides how many times its rules run. flowchart TB S[&quot;a batch: name, strategy, rules&quot;] --&amp;gt; L{&quot;for each rule&amp;lt;br/&amp;gt;in order&quot;} L --&amp;gt; R[&quot;apply rule to the tree&quot;] R --&amp;gt; M[&quot;record invocation,&amp;lt;br/&amp;gt;and whether it changed anything&quot;] M --&amp;gt; L L --&amp;gt;|&quot;end of the rule list&quot;| C{&quot;strategy&quot;} C --&amp;gt;|&quot;Once&quot;| D[&quot;done after one pass&quot;] C --&amp;gt;|&quot;FixedPoint(n)&quot;| E{&quot;did the tree change?&quot;} E --&amp;gt;|&quot;yes, and passes &amp;lt; n&quot;| L E --&amp;gt;|&quot;no, tree is stable&quot;| D E --&amp;gt;|&quot;passes = n&quot;| W[&quot;log a max-iterations warning,&amp;lt;br/&amp;gt;stop anyway&quot;] The termination test is plan equality, not a change counter: if (curPlan.fastEquals(lastPlan)) { logTrace(s&quot;Fixed point reached for batch ${batch.name} after ${iteration - 1} iterations.&quot;) continue = false } That design is what makes the rule set composable. ConstantFolding can create a literal that lets PruneFilters delete an entire subtree on the next pass, and nobody had to encode that dependency: the batch simply runs again because the tree changed. Reading the real structure out of a session is more informative than any description of it: from pyspark.sql import SparkSession spark = (SparkSession.builder.appName(&quot;catalyst&quot;).master(&quot;local[2]&quot;) .config(&quot;spark.sql.catalogImplementation&quot;, &quot;in-memory&quot;) .getOrCreate()) batches = spark._jsparkSession.sessionState().optimizer().batches() print(batches.size(), batches.apply(0).name(), batches.apply(0).rules().size()) 39 Finish Analysis 1 Scala is friendlier for walking the whole list, and spark-shell has it to hand: val bs = spark.sessionState.optimizer.batches println(s&quot;batches=${bs.size}&quot;) println(s&quot;distinct rules=${bs.flatMap(_.rules.map(_.ruleName)).distinct.size}&quot;) println(s&quot;once batches=${bs.count(_.strategy.toString.startsWith(&quot;Once&quot;))}&quot;) bs.take(12).foreach(b =&amp;gt; println(f&quot;${b.name}%-46s ${b.strategy}%-46s ${b.rules.size}&quot;)) On this build: batches=39 distinct rules=117 once batches=21 fixed-point batches=18 Finish Analysis FixedPoint(1,false,null) 1 Rewrite With expression FixedPoint(100, ...maxIterations) 1 Eliminate Distinct Once 1 Inline CTE Once 1 Union FixedPoint(100, ...maxIterations) 3 LocalRelation early FixedPoint(100, ...maxIterations) 3 Pullup Correlated Expressions Once 3 Subquery FixedPoint(1,false,null) 2 Replace Operators FixedPoint(100, ...maxIterations) 7 Aggregate FixedPoint(100, ...maxIterations) 2 Operator Optimization before Inferring Filters FixedPoint(100, ...maxIterations) 53 Infer Filters Once 2 Three things in that listing are worth pausing on. The big batch is the one people mean by “the optimizer”. Operator Optimization before Inferring Filters holds 53 rules and runs to a fixed point. It contains the whole familiar cast: PushDownPredicates, ColumnPruning, ConstantFolding, BooleanSimplification, CollapseProject, PruneFilters and the rest. Infer Filters sits between two copies of it. The same 53-rule set runs again afterwards, as Operator Optimization after Inferring Filters, because inferring a new predicate creates new work for the push-down rules. This is the fixed-point idea applied at the batch level. FixedPoint(1, ...) is not the same as Once. Both run the rules a single time, but Once batches are additionally checked for idempotence in Spark’s own test builds, so a Once rule that changes the tree twice is a bug that gets caught. The iteration cap comes from spark.sql.optimizer.maxIterations, default 100; exceeding it logs a warning in production and throws in testing. One query, four trees Here is the whole pipeline on a query with a join, an aggregate, a real predicate and a deliberately silly one. from pyspark.sql import SparkSession spark = (SparkSession.builder.appName(&quot;catalyst&quot;).master(&quot;local[2]&quot;) .config(&quot;spark.sql.shuffle.partitions&quot;, &quot;4&quot;) .getOrCreate()) trips = spark.createDataFrame( [(i, i % 500, i % 90000, float(i % 4000) / 100.0, &quot;2026-09-0%d&quot; % (i % 9 + 1)) for i in range(20000)], &quot;trip_id bigint, city_id bigint, rider_id bigint, fare double, trip_date string&quot;) cities = spark.createDataFrame( [(i, &quot;city-%d&quot; % i, [&quot;IN&quot;, &quot;US&quot;, &quot;DE&quot;][i % 3]) for i in range(500)], &quot;city_id bigint, city_name string, country_code string&quot;) trips.write.mode(&quot;overwrite&quot;).parquet(&quot;/tmp/catalyst/trips&quot;) cities.write.mode(&quot;overwrite&quot;).parquet(&quot;/tmp/catalyst/cities&quot;) spark.read.parquet(&quot;/tmp/catalyst/trips&quot;).createOrReplaceTempView(&quot;trips&quot;) spark.read.parquet(&quot;/tmp/catalyst/cities&quot;).createOrReplaceTempView(&quot;cities&quot;) df = spark.sql(&quot;&quot;&quot; SELECT c.city_name, count(*) AS n FROM trips t JOIN cities c ON t.city_id = c.city_id WHERE t.fare &amp;gt; 10.0 AND 1 = 1 GROUP BY c.city_name&quot;&quot;&quot;) qe = df._jdf.queryExecution() print(qe.logical().toString()) # parsed print(qe.analyzed().toString()) print(qe.optimizedPlan().toString()) print(qe.executedPlan().toString()) Parsed. Nothing is resolved. The leading ticks mark unresolved nodes and attributes, and the tables are UnresolvedRelation: &apos;Aggregate [&apos;c.city_name], [&apos;c.city_name, &apos;count(1) AS n#16] +- &apos;Filter ((&apos;t.fare &amp;gt; 10.0) AND (1 = 1)) +- &apos;Join Inner, (&apos;t.city_id = &apos;c.city_id) :- &apos;SubqueryAlias t : +- &apos;UnresolvedRelation [trips], [], false +- &apos;SubqueryAlias c +- &apos;UnresolvedRelation [cities], [], false Analyzed. The ticks are gone. Every attribute now has an expression id (city_id#9L), the views and aliases are explicit, and note what analysis added: cast(10.0 as double) around the literal, because the comparison needs matching types. Aggregate [city_name#14], [city_name#14, count(1) AS n#16L] +- Filter ((fare#11 &amp;gt; cast(10.0 as double)) AND (1 = 1)) +- Join Inner, (city_id#9L = city_id#13L) :- SubqueryAlias t : +- SubqueryAlias trips : +- View (`trips`, [trip_id#8L, city_id#9L, rider_id#10L, fare#11, trip_date#12]) : +- Relation [trip_id#8L,city_id#9L,rider_id#10L,fare#11,trip_date#12] parquet +- SubqueryAlias c +- SubqueryAlias cities +- View (`cities`, [city_id#13L, city_name#14, country_code#15]) +- Relation [city_id#13L,city_name#14,country_code#15] parquet Optimized. This is the interesting one, and five separate rewrites are visible in it at once: Aggregate [city_name#14], [city_name#14, count(1) AS n#16L] +- Project [city_name#14] +- Join Inner, (city_id#9L = city_id#13L) :- Project [city_id#9L] : +- Filter ((isnotnull(fare#11) AND (fare#11 &amp;gt; 10.0)) AND isnotnull(city_id#9L)) : +- Relation [trip_id#8L,city_id#9L,rider_id#10L,fare#11,trip_date#12] parquet +- Project [city_id#13L, city_name#14] +- Filter isnotnull(city_id#13L) +- Relation [city_id#13L,city_name#14,country_code#15] parquet 1 = 1 has disappeared, folded to true and then dropped from the AND. The cast(10.0 as double) is gone, folded into the literal 10.0. SubqueryAlias and View wrappers are removed: they carried naming information analysis needed and execution does not. A Filter now sits under the join on each side, pushed down from above it. isnotnull(...) predicates were added on both join keys, which is the optimizer inserting work because an inner join already discards nulls, and a cheap null check at the scan is worth more than the rows it removes later. Physical. The scan is where the logical rewrites turn into less IO: AdaptiveSparkPlan isFinalPlan=false +- HashAggregate(keys=[city_name#14], functions=[count(1)], ...) +- Exchange hashpartitioning(city_name#14, 4), ENSURE_REQUIREMENTS, [plan_id=76] +- HashAggregate(keys=[city_name#14], functions=[partial_count(1)], ...) +- Project [city_name#14] +- BroadcastHashJoin [city_id#9L], [city_id#13L], Inner, BuildRight, false :- Project [city_id#9L] : +- Filter ((isnotnull(fare#11) AND (fare#11 &amp;gt; 10.0)) AND isnotnull(city_id#9L)) : +- FileScan parquet [city_id#9L,fare#11] ... PushedFilters: [IsNotNull(fare), GreaterThan(fare,10.0), IsNotNull(city_id)], ReadSchema: struct&amp;lt;city_id:bigint,fare:double&amp;gt; ReadSchema: struct&amp;lt;city_id:bigint,fare:double&amp;gt; is the payoff. The table has five columns and the scan reads two, because ColumnPruning worked out that nothing else is needed. PushedFilters is the predicate handed to Parquet itself, so row groups whose statistics cannot satisfy fare &amp;gt; 10.0 are never decompressed. And the aggregate is split into partial_count before the exchange and count after it, which is map-side pre-aggregation. Which rules actually fired You do not have to infer the responsible rules. Every QueryExecution carries a QueryPlanningTracker that records each rule’s invocations and how many of those changed the tree: qe = df._jdf.queryExecution() qe.optimizedPlan() # force analysis and optimization rules = qe.tracker().rules() it, rows = rules.keysIterator(), [] while it.hasNext(): k = it.next() v = rules.apply(k) rows.append((k.split(&quot;.&quot;)[-1], v.numInvocations(), v.numEffectiveInvocations())) for name, n, eff in sorted(rows, key=lambda r: -r[2]): if eff: print(f&quot;{name:&amp;lt;46} invocations={n:&amp;lt;4} effective={eff}&quot;) print(&quot;invoked but never effective:&quot;, sum(1 for _, _, e in rows if e == 0)) For the query above: PushDownPredicates invocations=6 effective=2 Analyzer$ResolveReferences invocations=3 effective=2 ColumnPruning invocations=6 effective=1 ConstantFolding invocations=5 effective=1 BooleanSimplification invocations=5 effective=1 ResolveTimeZone invocations=3 effective=1 Analyzer$ResolveRelations invocations=3 effective=1 AnsiTypeCoercion$AnsiCombinedTypeCoercionRule invocations=3 effective=1 Analyzer$ResolveFunctions invocations=3 effective=1 InferFiltersFromConstraints invocations=1 effective=1 Optimizer$FinishAnalysis invocations=1 effective=1 total distinct rules invoked: 217 invoked but never effective: 206 That table answers the questions from the introduction directly. 1 = 1 went to BooleanSimplification, the casts to ConstantFolding, the two-column read to ColumnPruning, and the isnotnull predicates to InferFiltersFromConstraints. Six invocations of PushDownPredicates with two effective is the fixed point at work: it kept being retried until it had nothing left to move. The headline number is the last one. 217 rules ran, 11 did something, 206 found nothing to do. Catalyst’s cost is dominated by rules cheaply declining, which is exactly why the TreePattern pruning mentioned earlier exists. A tour of rules you can see working Each of these is one small query and its optimized plan, so you can watch a single rule in isolation. spark.sql(q)._jdf.queryExecution().optimizedPlan() is the only tool needed. Rule Query fragment What the optimized plan shows ConstantFolding fare * (2 + 3) (fare#3 * 5.0) BooleanSimplification fare &amp;gt; 10.0 AND 1 = 1 the tautology is dropped PruneFilters WHERE 1 = 2 LocalRelation &amp;lt;empty&amp;gt;, the scan is gone NullPropagation count(NULL) 0 AS count(NULL), no data read LikeSimplification city_name LIKE &apos;city-1%&apos; StartsWith(city_name#6, city-1) CollapseProject nested SELECT of a computed column one Project with the composed expression EliminateLimits LIMIT 100 inside LIMIT 10 a single LocalLimit 10 ReplaceDistinctWithAggregate SELECT DISTINCT city_id Aggregate [city_id#1L], [city_id#1L] RewriteNonCorrelatedExists WHERE EXISTS (SELECT 1 FROM cities) a scalar subquery with LocalLimit 1 Two are worth showing in full because the rewrite is drastic. PruneFilters deletes the query. An always-false predicate does not become a filter that matches nothing; it becomes an empty relation, and the scan is never planned: spark.sql(&quot;SELECT trip_id FROM trips WHERE 1 = 2&quot;) \ ._jdf.queryExecution().optimizedPlan().toString() LocalRelation &amp;lt;empty&amp;gt;, [trip_id#0L] NullPropagation answers without reading anything. count(NULL) is zero by definition, so the aggregate is replaced by a literal and the projection below it is emptied: Aggregate [0 AS count(NULL)#10L] +- Project +- Relation [trip_id#0L,city_id#1L,rider_id#2L,fare#3,trip_date#4] parquet LikeSimplification is the one with the most practical value, because StartsWith is a predicate Parquet can evaluate against column statistics and a general LIKE is not. A leading wildcard (LIKE &apos;%city-1&apos;) cannot be rewritten that way, which is the real reason the advice about leading wildcards exists. Turning a rule off spark.sql.optimizer.excludedRules takes fully qualified rule names. This is the fastest way to find out what a rule does, and it is genuinely useful when a rule misbehaves on your workload. Q = &quot;SELECT trip_id, fare * (2 + 3) AS f5 FROM trips&quot; spark.conf.unset(&quot;spark.sql.optimizer.excludedRules&quot;) print(spark.sql(Q)._jdf.queryExecution().optimizedPlan().toString()) spark.conf.set(&quot;spark.sql.optimizer.excludedRules&quot;, &quot;org.apache.spark.sql.catalyst.optimizer.ConstantFolding&quot;) print(spark.sql(Q)._jdf.queryExecution().optimizedPlan().toString()) # default Project [trip_id#0L, (fare#3 * 5.0) AS f5#8] # ConstantFolding excluded Project [trip_id#0L, (fare#3 * cast((2 + 3) as double)) AS f5#9] The unfolded version computes 2 + 3 and a cast for every row. The same trick on PushDownPredicates leaves the filter stranded above the scan in a nested shape rather than combined at the leaf. Two limits to know. Some rules cannot be excluded: the optimizer keeps a nonExcludableRules list, 25 entries on this build, covering rewrites that produce an invalid plan if skipped rather than merely a slow one. Naming one of those in the config is silently ignored rather than rejected, so an exclusion that appears to do nothing may be one of them. Writing your own rule A rule is an object with one method, and spark.experimental.extraOptimizations injects it without building a session extension. This runs in spark-shell: import org.apache.spark.sql.catalyst.expressions._ import org.apache.spark.sql.catalyst.plans.logical._ import org.apache.spark.sql.catalyst.rules.Rule // Rewrite x * 1 to x, and x + 0 to x. object RemoveIdentityArithmetic extends Rule[LogicalPlan] { def apply(plan: LogicalPlan): LogicalPlan = plan.transformAllExpressions { case Multiply(left, Literal(1, _), _) =&amp;gt; left case Multiply(left, Literal(1.0, _), _) =&amp;gt; left case Add(left, Literal(0, _), _) =&amp;gt; left case Add(left, Literal(0.0, _), _) =&amp;gt; left } } spark.read.parquet(&quot;/tmp/catalyst/trips&quot;).createOrReplaceTempView(&quot;trips&quot;) val q = &quot;SELECT trip_id, fare * 1 AS f, rider_id + 0 AS r FROM trips&quot; println(spark.sql(q).queryExecution.optimizedPlan) spark.experimental.extraOptimizations = Seq(RemoveIdentityArithmetic) println(spark.sql(q).queryExecution.optimizedPlan) # before Project [trip_id#0L, (fare#3 * 1.0) AS f#5, rider_id#2L AS r#6L] # after Project [trip_id#0L, fare#3 AS f#7, rider_id#2L AS r#8L] Look closely at the “before” line, because it contains a free lesson. `rider_id 0 was **already** simplified to rider_id by Spark&apos;s existing rules, while fare * 1 survived as (fare#3 * 1.0). The gap is not an oversight to be smug about: x * 1 is only safe to remove for some type and nullability combinations, and the multiplication here is on a double` where the literal had to be cast. The example is useful precisely because half of it was redundant. An injected rule lands in its own batch at the end of the pipeline: Batch(User Provided Optimizers, FixedPoint(100, ...maxIterations), List(RemoveIdentityArithmetic)) FixedPoint means your rule is retried until the tree stops changing, so it must be idempotent. A rule that keeps rewriting its own output will spin to maxIterations and log a warning, and in a test build it throws. For production, spark.experimental.extraOptimizations is a session-local hook. The supported path is SparkSessionExtensions with injectOptimizerRule, set through spark.sql.extensions, which is also how Iceberg and Hudi install their rules. Testing a rule A rule is a pure function on a tree, so it needs no cluster to test. Apply it to a plan and compare: def optimized(sql): return spark.sql(sql)._jdf.queryExecution().optimizedPlan().toString() def assert_rule_effective(sql, rule_name): &quot;&quot;&quot;Fail unless the named rule changed the plan for this query.&quot;&quot;&quot; qe = spark.sql(sql)._jdf.queryExecution() qe.optimizedPlan() rules = qe.tracker().rules() it = rules.keysIterator() while it.hasNext(): key = it.next() if key.endswith(rule_name): eff = rules.apply(key).numEffectiveInvocations() assert eff &amp;gt; 0, f&quot;{rule_name} ran but changed nothing&quot; return eff raise AssertionError(f&quot;{rule_name} was never invoked&quot;) assert &quot;fare#&quot; in optimized(&quot;SELECT fare * 1 AS f FROM trips&quot;) # before injection print(assert_rule_effective(&quot;SELECT trip_id FROM trips WHERE 1 = 2&quot;, &quot;PruneFilters&quot;)) Three properties are worth asserting on any rule you write. That it fires at all, which the tracker answers. That it is idempotent, since it runs in a fixed-point batch: applying it to its own output must change nothing. And that it preserves results, which means running the query with and without the rule and comparing the rows, not just the plans. Spark’s own suites do exactly this, with RuleExecutor’s idempotence check on Once batches as the automated version of the second one. Physical planning: from what to how The optimized logical plan still says nothing about execution. Join Inner is a requirement, not an algorithm. Turning each logical operator into a physical one is the job of the SparkPlanner, which applies a list of strategies rather than rules: val st = spark.sessionState.planner.strategies println(s&quot;strategies=${st.size}&quot;) st.foreach(s =&amp;gt; println(&quot; &quot; + s.getClass.getSimpleName.replace(&quot;$&quot;, &quot;&quot;))) strategies=16 HiveTableScans LogicalQueryStageStrategy PythonEvals HiveScripts DataSourceV2Strategy V2CommandStrategy FileSourceStrategy DataSourceStrategy SpecialLimits Aggregation Window WindowGroupLimit JoinSelection InMemoryScans SparkScripts BasicOperators A strategy differs from a rule in what it returns. A rule returns one tree; a strategy returns a list of candidate physical plans for a logical operator, and the planner takes the first that applies. JoinSelection is where BroadcastHashJoinExec and SortMergeJoinExec are chosen between, and FileSourceStrategy is where a scan acquires its PushedFilters and its pruned ReadSchema. This is also where the first genuinely cost-sensitive decision happens, because JoinSelection compares a size estimate against spark.sql.autoBroadcastJoinThreshold. The strategy ordering, the build-side rules per join type and the full decision procedure are their own subject, taken apart in Apache Spark joins in depth. Rule-based versus cost-based optimization This is the distinction that most descriptions of Catalyst blur, and it is measurable. Rule-based optimization (RBO) applies rewrites that are always improvements. ConstantFolding never makes a plan worse, so no cost model is consulted and no statistics are needed. Every rule in the earlier tour is RBO. Cost-based optimization (CBO) chooses between alternatives using estimated cardinalities, which means it needs statistics about your data, which means somebody has to compute them.   Rule-based Cost-based Decides by A pattern in the tree Estimated rows and sizes Needs statistics No Yes, computed by ANALYZE TABLE On by default Yes No, spark.sql.cbo.enabled is false Can make a plan worse Not by design Yes, on a bad estimate Examples Constant folding, predicate pushdown, column pruning Join reordering, and the size estimates behind broadcast selection What statistics Spark actually holds A fresh table has none, which is worth seeing before anything else: DESCRIBE EXTENDED fact; -- Statistics row: absent ANALYZE TABLE fills in two different things, and the distinction matters: -- table level: a row count and a size ANALYZE TABLE fact COMPUTE STATISTICS; -- column level: per-column cardinality, nulls, bounds, and a histogram ANALYZE TABLE fact COMPUTE STATISTICS FOR COLUMNS city_id, rider_id, fare; fact 1779205 bytes, 200000 rows dim_city 4818 bytes, 400 rows dim_rider 449573 bytes, 50000 rows Column statistics are the interesting ones, because selectivity estimation is impossible without them: DESCRIBE EXTENDED fact city_id; min 0 max 399 num_nulls 0 distinct_count 397 avg_col_len 8 max_col_len 8 histogram height: 787.4015748031496, num_of_bins: 254 bin_0 lower_bound: 0.0, upper_bound: 1.0, distinct_count: 2 bin_1 lower_bound: 1.0, upper_bound: 3.0, distinct_count: 2 bin_2 lower_bound: 3.0, upper_bound: 4.0, distinct_count: 1 That is an equi-height histogram: 254 bins each holding about 787 rows, so the bin widths vary and dense ranges get more bins. It is built only when spark.sql.statistics.histogram.enabled is true, which is not the default, and it is what lets Catalyst estimate a range predicate rather than guessing. The same query, with and without CBO Now the measurement. One filter, explain(&quot;cost&quot;) twice: spark.conf.set(&quot;spark.sql.cbo.enabled&quot;, &quot;true&quot;) spark.sql(&quot;SELECT city_id FROM fact WHERE fare &amp;gt; 10.0&quot;).explain(&quot;cost&quot;) spark.conf.set(&quot;spark.sql.cbo.enabled&quot;, &quot;false&quot;) spark.sql(&quot;SELECT city_id FROM fact WHERE fare &amp;gt; 10.0&quot;).explain(&quot;cost&quot;) # cbo = true, with column statistics Project [city_id#1L], Statistics(sizeInBytes=2.3 MiB, rowCount=1.50E+5) +- Filter (isnotnull(fare#3) AND (fare#3 &amp;gt; 10.0)), Statistics(sizeInBytes=5.7 MiB, rowCount=1.50E+5) +- Relation ... fact[...] parquet, Statistics(sizeInBytes=7.6 MiB, rowCount=2.00E+5) # cbo = false Project [city_id#1L], Statistics(sizeInBytes=695.0 KiB) +- Filter (isnotnull(fare#3) AND (fare#3 &amp;gt; 10.0)), Statistics(sizeInBytes=1737.5 KiB) +- Relation ... fact[...] parquet, Statistics(sizeInBytes=1737.5 KiB) Two differences, both important: With CBO there is a rowCount, and the filter reduces it, from 200,000 to 150,000. That is a selectivity estimate computed from the histogram. Without CBO there is no rowCount at all, and the filter does not change sizeInBytes: 1737.5 KiB in, 1737.5 KiB out. The second line is the one to remember. Off the default path, a WHERE clause does not make Spark expect fewer rows, so any later decision that depends on size is made as though the filter were not there. That is the mechanism behind a great many “why did it not broadcast” questions. Join reordering, and the honest result CostBasedJoinReorder is the rule people mean by “Spark has a CBO”. It sits in a Join Reorder batch with FixedPoint(1) and needs both spark.sql.cbo.enabled and spark.sql.cbo.joinReorder.enabled. So I wrote the joins in a deliberately bad order, big table first, computed table and column statistics for all three tables, and compared: Q = &quot;&quot;&quot;SELECT count(*) FROM dim_rider r JOIN fact f ON f.rider_id = r.rider_id JOIN dim_city c ON f.city_id = c.city_id WHERE c.city_name = &apos;city-7&apos;&quot;&quot;&quot; The resulting join order was identical with CBO on and off, and the tracker says why: CostBasedJoinReorder invocations=1 effective=0 ReorderJoin invocations=5 effective=1 ReorderJoin, which is rule-based and always on, had already moved the joins into a sensible order. By the time the cost-based rule ran there was nothing left for it to improve, so it declined. That is not a claim that CostBasedJoinReorder never helps. It is a claim about where the value usually comes from: the rule-based rewrites do the heavy lifting, and CBO is a narrow addition that needs statistics you must maintain. Treat “enable CBO and joins get reordered” as something to verify on your own workload with the tracker, rather than as a given. Codegen, where the tree stops being a tree The physical plan is the last tree. Tungsten then collapses runs of operators into one generated Java class, so there is no per-row virtual call between a filter and a projection. spark.sql(&quot;SELECT trip_id + 1 AS x FROM trips WHERE fare &amp;gt; 10.0&quot;).explain(&quot;codegen&quot;) Found 1 WholeStageCodegen subtrees. == Subtree 1 / 1 (maxMethodCodeSize:359; maxConstantPoolSize:173(0.26% used); numInnerClasses:0) == *(1) Project [(trip_id#0L + 1) AS x#9L] +- *(1) Filter (isnotnull(fare#3) AND (fare#3 &amp;gt; 10.0)) +- *(1) ColumnarToRow +- FileScan parquet [trip_id#0L,fare#3] ... Generated code: /* 001 */ public Object generate(Object[] references) { /* 002 */ return new GeneratedIteratorForCodegenStage1(references); /* 003 */ } /* 005 */ // codegenStageId=1 /* 006 */ final class GeneratedIteratorForCodegenStage1 extends /* 006 */ org.apache.spark.sql.execution.BufferedRowIterator { The *(1) prefix marks operators fused into codegen stage 1. An operator without the star is not generating code, which is the fastest way to spot a fallback. The maxMethodCodeSize figure matters because the JVM refuses to JIT-compile very large methods, so Spark gives up on codegen for a stage past spark.sql.codegen.hugeMethodLimit and runs it interpreted instead. Dynamic partition pruning Dynamic partition pruning (DPP) is the runtime trick for the classic star-schema shape: a partitioned fact table joined to a filtered dimension. The partition values that survive the dimension filter are not known at planning time, so Catalyst plants a subquery that supplies them once the dimension side has been broadcast, and the fact scan then lists only those partitions. It is on by default: spark.conf.get(&quot;spark.sql.optimizer.dynamicPartitionPruning.enabled&quot;) # true The shape it looks for: SELECT sum(s.amount) FROM sales s JOIN dates d ON s.day = d.day -- s.day is a partition column WHERE d.is_promo = true -- a filter on the dimension Worth reporting honestly: I could not get it to fire on this local setup. With a 12-partition, 400,000-row partitioned sales table, a 12-row dimension that does get broadcast, statistics computed on both, and adaptive execution disabled, the plan came back with a plain partition filter every time: join operator : BroadcastHashJoin [day#2], [day#3], Inner, BuildRight PartitionFilters : [isnotnull(day#2)] dynamicpruning in optimized plan: False The tracker shows the rule ran and declined rather than being absent: PartitionPruning inv=1 eff=0 CleanupDynamicPruningFilters inv=1 eff=0 I tried dynamicPartitionPruning.useStats=false and fallbackFilterRatio=0.01, which are the two knobs on its cost check, and the result did not change. The most likely explanation is that on a table this small the estimated saving never clears the cost of the extra subquery, but I did not establish which condition failed, so I am reporting the observation rather than a cause. The useful takeaway is the diagnostic, not the disappointment. DPP is a rule like any other, so PartitionPruning with eff=0 in the tracker tells you it considered your query and said no, which is a different problem from it being switched off. The marker to look for when it does apply is dynamicpruning#... inside PartitionFilters, or a SubqueryBroadcast node in the physical plan. Adaptive query execution Everything up to here happens before any data moves, from estimates. Adaptive query execution re-plans after each shuffle stage completes, using measured sizes, and it is on by default. flowchart TB A[&quot;optimized logical plan&quot;] --&amp;gt; B[&quot;physical plan&quot;] B --&amp;gt; C[&quot;AdaptiveSparkPlan&amp;lt;br/&amp;gt;isFinalPlan=false&quot;] C --&amp;gt; D[&quot;run the next stage&quot;] D --&amp;gt; E[&quot;real statistics from&amp;lt;br/&amp;gt;the completed stage&quot;] E --&amp;gt; F{&quot;re-optimize the rest&quot;} F --&amp;gt;|&quot;coalesce small partitions&quot;| D F --&amp;gt;|&quot;split a skewed partition&quot;| D F --&amp;gt;|&quot;switch the join strategy&quot;| D F --&amp;gt;|&quot;nothing left to run&quot;| G[&quot;isFinalPlan=true&quot;] The thing to know before testing any of this: you have to execute the plan you are holding and then read the same QueryExecution again. Calling df.count() builds a different query, so the plan you inspect afterwards was never the one that ran. Use collect(), or keep a handle on the QueryExecution: df = spark.sql(&quot;SELECT city_id, count(*) c FROM fact GROUP BY city_id&quot;) qe = df._jdf.queryExecution() print(qe.executedPlan().toString().splitlines()[0]) # before df.collect() # run THIS plan print(qe.executedPlan().toString().splitlines()[0]) # after AdaptiveSparkPlan isFinalPlan=false AdaptiveSparkPlan isFinalPlan=true Coalescing partitions. With spark.sql.shuffle.partitions left at 200 and 400 groups to produce, the final plan shows the shuffle being read back as fewer, larger partitions: +- AQEShuffleRead coalesced +- ShuffleQueryStage 0 Switching the join strategy. This is the most dramatic one, because the operator changes after planning. Broadcasting is disabled at plan time and allowed at runtime: spark.conf.set(&quot;spark.sql.autoBroadcastJoinThreshold&quot;, &quot;-1&quot;) spark.conf.set(&quot;spark.sql.adaptive.autoBroadcastJoinThreshold&quot;, &quot;10MB&quot;) q = spark.sql(&quot;SELECT count(*) FROM fact f JOIN dim_city c ON f.city_id = c.city_id&quot;) qe = q._jdf.queryExecution() planned = qe.executedPlan().toString() q.collect() final = qe.executedPlan().toString() planned : SortMergeJoin [city_id#3L], [city_id#10L], Inner executed : *(3) BroadcastHashJoin [city_id#3L], [city_id#10L], Inner, BuildRight, false A sort-merge join at plan time became a broadcast hash join at run time, once the measured size of the build side was known. Note that the two thresholds are separate configs, so plan-time and runtime broadcasting can be governed by different numbers. Splitting a skewed partition. With a key holding most of the rows, AQE divides the oversized partition into several and the join operator says so: # skewJoin.enabled = true *(5) SortMergeJoin(skew=true) [city_id#1L], [city_id#6L], Inner +- AQEShuffleRead coalesced and skewed # skewJoin.enabled = false *(5) SortMergeJoin [city_id#1L], [city_id#6L], Inner +- AQEShuffleRead coalesced (skew=true) on the operator and and skewed on the shuffle read are the two markers. The thresholds are strict and are the usual reason nothing happens: a partition counts as skewed only when it is larger than skewJoin.skewedPartitionFactor (default 5.0) times the median and larger than skewJoin.skewedPartitionThresholdInBytes (default 256MB). On data where no partition reaches 256 MB the rule can never fire, however lopsided the distribution is. Skew, and the six fixes that do not depend on AQE, are taken apart in data skew in Apache Spark, and adaptive execution itself in adaptive query execution in Apache Spark. Debugging and inspection Five tools, in the order they are usually worth reaching for. explain(mode). The modes are not interchangeable: Mode Shows &quot;simple&quot; The physical plan only, the default &quot;extended&quot; All four trees: parsed, analyzed, optimized, physical &quot;cost&quot; Optimized plan annotated with Statistics(...) per node &quot;codegen&quot; The generated Java for each whole-stage subtree &quot;formatted&quot; Physical plan as a numbered tree plus per-operator detail explain(&quot;extended&quot;) is the one to start with, because seeing analyzed and optimized next to each other is what makes a missing rewrite obvious. The rule tracker, shown earlier, which names the rule that changed the plan. The scan line. ReadSchema and PushedFilters on a FileScan are the only proof that pruning and pushdown reached the file format rather than stopping at a Spark-side Filter. Rule-level logs. Setting the logger for org.apache.spark.sql.catalyst.rules.RuleExecutor to TRACE makes Spark print the plan change per rule and the fixed point being reached per batch. It is verbose and definitive, and it is the tool for a rule that is firing when you did not expect it. The SQL tab in the UI. For anything adaptive this beats explain(), because the tab shows the final plan with the actual row counts and spill per operator, where explain() shows the intent. Common misconceptions “Catalyst is a cost-based optimizer.” Barely. On the join-and-aggregate query above, 217 rules were invoked and 11 changed the plan, and all of the useful ones were unconditional rewrites. With CBO explicitly enabled and full column statistics computed, CostBasedJoinReorder ran and changed nothing (inv=1 eff=0) while the rule-based ReorderJoin did the work (inv=5 eff=1). “A WHERE clause makes Spark expect fewer rows.” Only with column statistics. Without them the estimate passes straight through the filter: 1737.5 KiB in, 1737.5 KiB out, and no rowCount at all. Every later decision that depends on size, broadcast selection above all, is then made as though the filter were not there. “The plan explain() prints is the plan that ran.” It is the plan before adaptive execution has re-planned anything, which the output says as isFinalPlan=false. Read the final plan from the same QueryExecution after collect(), or from the SQL tab. “Turning a rule off with excludedRules is a supported tuning knob.” It is a diagnostic. Some rules cannot be excluded at all, and the optimizer silently ignores a request to exclude one of the 25 in nonExcludableRules, so an exclusion that appears to do nothing may simply have been dropped. Production notes Read the analyzed and optimized plans together. The diff between them is the optimizer’s entire contribution, and it is where a missing pushdown or a surprise cast becomes obvious. Use the tracker rather than guessing. queryExecution.tracker().rules() names the rule that changed your plan, which turns an argument into a lookup. Check ReadSchema and PushedFilters on every scan you care about. They are the only proof that pruning and pushdown actually reached the file format. Do not expect a Filter to reduce the size estimate. Without column statistics there is no selectivity, so the estimate passes through unchanged and broadcast decisions are made on the unfiltered size. Reach for excludedRules to diagnose, not to fix. It is excellent for understanding a rule and a poor permanent setting, and some rules cannot be excluded at all. Keep a custom rule idempotent. It runs in a fixed-point batch, so a rule that rewrites its own output spins until the iteration cap. Prefer SparkSessionExtensions over extraOptimizations in production. The experimental hook is session-local and Scala-only; the extension point is the one other projects use. Compute column statistics, not just table statistics. COMPUTE STATISTICS alone gives a row count; only FOR COLUMNS gives the cardinality and histogram that let a filter reduce an estimate. Verify that CBO earned its keep before leaving it on. Enable it, run your query, and check whether CostBasedJoinReorder reports a non-zero effective count. In the test here the rule-based ReorderJoin had already done the work. Execute the plan you are holding before reading it back. df.count() builds a different query, so an AQE check done that way inspects a plan that never ran. Use collect() and re-read the same QueryExecution. Frequently asked questions Why did a cast appear in my plan that I did not write? Analysis added it, before the optimizer ran. Type coercion is part of making the query meaningful, so comparing a double column to a decimal literal inserts a cast. ConstantFolding then usually removes it again by folding the literal, but only if the cast is on the literal side. Which rule removed my filter? Ask the tracker. If the filter vanished entirely rather than moving, the usual answers are PruneFilters for an always-true or always-false predicate, and BooleanSimplification for a redundant clause inside an AND or OR. Is Catalyst cost-based? Barely, and the post measures it. The large majority of rules are unconditional rewrites that are always wins, which is rule-based optimization. Statistics are used for broadcast-join selection and, if you explicitly enable CBO and compute column statistics, for CostBasedJoinReorder. In the join-reordering test above that cost-based rule ran and changed nothing, because the rule-based ReorderJoin had already produced the same order. Treat “Catalyst is a cost-based optimizer” as the wrong mental model. Why does WHERE not reduce the size estimate? Because without column statistics there is no selectivity to apply, so the estimate passes through the Filter unchanged. Compute ANALYZE TABLE ... COMPUTE STATISTICS FOR COLUMNS and turn on spark.sql.cbo.enabled, and explain(&quot;cost&quot;) starts showing a rowCount that the filter actually reduces. Why is dynamic partition pruning not happening? Check the tracker before the config. If PartitionPruning shows inv=1 eff=0 the rule considered your query and declined, which is a cost decision rather than a switched-off feature. It also needs the join key to be an actual partition column of the fact table and, by default, a reusable broadcast of the dimension side. Why is my predicate not pushed into the scan? Push-down happens in stages: the optimizer moves the Filter down the logical tree, and then the data source decides what it can accept. A predicate on a non-deterministic expression, a UDF, or a column the format cannot filter on stops at the scan boundary and appears in DataFilters but not PushedFilters. Does the optimizer ever make a plan worse? It can, and the usual mechanism is a bad size estimate rather than a bad rule. The rewrites themselves are equivalence-preserving; the choices that depend on statistics are the ones that go wrong. How do I see rules running as they fire? Set the log level for org.apache.spark.sql.catalyst.rules.RuleExecutor to TRACE. RuleExecutor logs the fixed point being reached per batch and the plan changes per rule, which is verbose but definitive. Conclusion Catalyst is easier to reason about once you stop thinking of it as an optimizer and start thinking of it as a tree rewriter with an execution schedule. There are four trees, each produced from the last. There are batches of small rules, each either run once or run until the tree stops changing. Nearly every rule is a pattern match and a replacement, and nearly every rule declines to apply. That framing makes the debugging loop concrete. A surprising plan is not a mystery about a black box; it is a question about which rule fired, and the tracker answers it by name. A rule you distrust can be switched off and the difference read straight out of the plan. A rewrite you want that Spark does not do is about ten lines in a Rule[LogicalPlan]. The number worth remembering from all of this is 11 of 217. On an ordinary join and aggregate, eleven rules changed the plan and two hundred and six had nothing to say. The optimizer’s power is not in any single clever rewrite but in having enough small, safe, composable ones that the useful few always get their turn. References Optimizer.scala, whose defaultBatches is the batch list and order quoted above RuleExecutor.scala for the fixed-point loop, the iteration cap and the idempotence check QueryExecution.scala, where the four phases are the lazy members you printed SQL performance tuning for the CBO and adaptive-execution configs named here Apache Spark architecture for the scheduler and executor machinery the physical plan is handed to Trademarks Apache Spark, Apache Hudi, Apache Iceberg, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Apache Iceberg through Spark Connect: the full surface, and the extension that breaks time travel</title><link href="https://rangareddy.github.io/SparkConnectIceberg/" rel="alternate" type="text/html" title="Apache Iceberg through Spark Connect: the full surface, and the extension that breaks time travel" /><published>2026-09-18T11:00:00+05:30</published><updated>2026-09-18T11:00:00+05:30</updated><id>https://rangareddy.github.io/SparkConnectIceberg</id><content type="html" xml:base="https://rangareddy.github.io/SparkConnectIceberg/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-where-iceberg-has-to-live&quot; id=&quot;markdown-toc-architecture-where-iceberg-has-to-live&quot;&gt;Architecture: where Iceberg has to live&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#starting-the-server&quot; id=&quot;markdown-toc-starting-the-server&quot;&gt;Starting the server&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#connecting-the-client&quot; id=&quot;markdown-toc-connecting-the-client&quot;&gt;Connecting the client&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-write-and-read-surface&quot; id=&quot;markdown-toc-the-write-and-read-surface&quot;&gt;The write and read surface&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#time-travel-and-the-extension-that-breaks-it&quot; id=&quot;markdown-toc-time-travel-and-the-extension-that-breaks-it&quot;&gt;Time travel, and the extension that breaks it&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#the-failure-and-the-diagnosis-that-found-it&quot; id=&quot;markdown-toc-the-failure-and-the-diagnosis-that-found-it&quot;&gt;The failure, and the diagnosis that found it&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-the-thin-client-cannot-do&quot; id=&quot;markdown-toc-what-the-thin-client-cannot-do&quot;&gt;What the thin client cannot do&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-notes&quot; id=&quot;markdown-toc-production-notes&quot;&gt;Production notes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;The Iceberg runtime jar, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt; and the catalog configs belong to the &lt;strong&gt;Spark Connect server&lt;/strong&gt;, set at startup. A client cannot add them, which makes the server config a contract rather than a per-job choice.&lt;/li&gt;
    &lt;li&gt;The client is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pip install&lt;/code&gt; with no Spark, no Java and no Iceberg jar. Everything here was driven from one.&lt;/li&gt;
    &lt;li&gt;DDL with hidden partitioning, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INSERT&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;writeTo&lt;/code&gt;, the metadata tables and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CALL&lt;/code&gt; procedures all work unchanged over gRPC.&lt;/li&gt;
    &lt;li&gt;SQL time travel works over Connect too, in all three syntaxes, &lt;strong&gt;as long as Iceberg’s extension is the only one loaded&lt;/strong&gt;.&lt;/li&gt;
    &lt;li&gt;Add Hudi’s session extension to the same server and every SQL time-travel syntax starts failing with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;[INTERNAL_ERROR] Found the unresolved operator: &apos;TimeTravelRelation&lt;/code&gt;. It is one-directional: Hudi keeps working, Iceberg loses time travel, and the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshot-id&lt;/code&gt; reader option keeps working either way.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;Iceberg is not part of Spark. It arrives as a runtime jar, installs a Catalyst
extension and registers a catalog. Those are session-level concerns, and Spark
Connect moves the session behind a gRPC connection, so the first question is
which half of the setup goes where.&lt;/p&gt;

&lt;p&gt;The second question is more interesting, and it is the reason this post exists.
When something fails through Spark Connect, the transport is the most attractive
suspect and usually the wrong one. This post runs Iceberg’s full surface from a
thin client, and the single thing that breaks turns out to be caused neither by
Spark Connect nor by Iceberg, but by another format’s session extension sharing
the server. Finding that took two control experiments, and the first one, run on
its own, produced a confident wrong answer that this post had published.&lt;/p&gt;

&lt;h2 id=&quot;architecture-where-iceberg-has-to-live&quot;&gt;Architecture: where Iceberg has to live&lt;/h2&gt;

&lt;p&gt;A classic session keeps the SparkSession, the analyzer and the catalog in your
process, which is why your own &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-submit&lt;/code&gt; line can install Iceberg. Spark
Connect splits that:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph C[&quot;client, your laptop&quot;]
    A[&quot;DataFrame and SQL calls&quot;] --&amp;gt; B[&quot;unresolved plan&amp;lt;br/&amp;gt;as protobuf&quot;]
  end
  B --&amp;gt;|&quot;gRPC, port 15002&quot;| S
  subgraph S[&quot;Spark Connect server, the driver&quot;]
    D[&quot;analyzer plus SparkCatalog&quot;] --&amp;gt; E[&quot;Catalyst optimiser plus&amp;lt;br/&amp;gt;IcebergSparkSessionExtensions&quot;]
    E --&amp;gt; F[&quot;physical plan&quot;]
  end
  F --&amp;gt; X[&quot;executors, Iceberg runtime on the classpath&quot;]
  X --&amp;gt;|&quot;Arrow batches&quot;| A
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The client cannot resolve &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;iceberg.lake.trips&lt;/code&gt;: it has no catalog and no notion
of an Iceberg table. It ships the operation and the server works out what it
means. Three things therefore have to be on the server, and all three are fixed
when it starts:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What Iceberg needs&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Config&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The runtime jar&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Contains the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataSource&lt;/code&gt;, the catalog and the procedures&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;A Catalyst extension&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Provides &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CALL&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;A catalog&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.catalog.&amp;lt;name&amp;gt;&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Resolves &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;&amp;lt;name&amp;gt;.db.table&lt;/code&gt; and owns commit atomicity&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.conf.set&lt;/code&gt; from a client reaches the server for runtime SQL configs, but
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt; is read once while the session is built, before any client
connects. A client can read it, which is the quickest way to tell a
misconfigured server from a bad query.&lt;/p&gt;

&lt;h2 id=&quot;starting-the-server&quot;&gt;Starting the server&lt;/h2&gt;

&lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nv&quot;&gt;$SPARK_HOME&lt;/span&gt;/sbin/start-connect-server.sh &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--packages&lt;/span&gt; org.apache.iceberg:iceberg-spark-runtime-4.0_2.13:1.11.0 &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.extensions&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.catalog.iceberg&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;org.apache.iceberg.spark.SparkCatalog &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.catalog.iceberg.type&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;hadoop &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.catalog.iceberg.warehouse&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;/tmp/warehouse/iceberg &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--master&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;local[4]&quot;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Swap &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt; with an absolute path when the host cannot reach
Maven Central; both forms were run for this post. The coordinate carries two
compatibilities that fail hard rather than warn:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Coordinate part&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Must match&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;iceberg-spark-runtime-4.0&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Your Spark minor version, 4.0 here&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_2.13&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark’s Scala binary version&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Spark 4 is Scala 2.13 only, so a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_2.12&lt;/code&gt; runtime never loads.&lt;/p&gt;

&lt;p&gt;Two notes on the catalog. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;type=hadoop&lt;/code&gt; keeps the example self-contained by
putting the pointer in the warehouse directory, and it gives no atomic commits
across engines, so use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hive&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rest&lt;/code&gt; for anything shared. And the catalog name
you choose becomes the first part of every table name, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;iceberg&lt;/code&gt; here means
tables are &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;iceberg.lake.trips&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;The extension class is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions&lt;/code&gt;, which is
where you would expect it, under the project’s own package. It can still be
confirmed from the jar rather than from memory:&lt;/p&gt;

&lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;unzip &lt;span class=&quot;nt&quot;&gt;-l&lt;/span&gt; iceberg-spark-runtime-4.0_2.13-1.11.0.jar | &lt;span class=&quot;nb&quot;&gt;grep &lt;/span&gt;IcebergSparkSessionExtensions
&lt;span class=&quot;c&quot;&gt;#    9663  org/apache/iceberg/spark/extensions/IcebergSparkSessionExtensions.class&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A wrong class name does not stop the server. It logs a warning and starts, and
then every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; fails later for reasons that look unrelated, so grep the
server log for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Cannot use&lt;/code&gt; before debugging your SQL.&lt;/p&gt;

&lt;h2 id=&quot;connecting-the-client&quot;&gt;Connecting the client&lt;/h2&gt;

&lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c&quot;&gt;# pin the client to the version your server runs&lt;/span&gt;
pip &lt;span class=&quot;nb&quot;&gt;install&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;pyspark[connect]==4.0.2&quot;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;remote&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sc://localhost:15002&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.extensions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;No Java, no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SPARK_HOME&lt;/code&gt;, no Iceberg jar. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.version&lt;/code&gt; reports the server’s
version, not the client’s, so it is not evidence about your local install; use
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pyspark.__version__&lt;/code&gt; for that.&lt;/p&gt;

&lt;p&gt;The pin is deliberate. An unpinned install takes whatever is newest on PyPI, and
a client newer than the server runs SQL happily and then fails on the first
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SQL_CONF_NOT_FOUND&lt;/code&gt; for
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.session.localRelationSizeLimit&lt;/code&gt;, which it expects the server to have.
An older client is tolerated; a newer one is not.&lt;/p&gt;

&lt;h2 id=&quot;the-write-and-read-surface&quot;&gt;The write and read surface&lt;/h2&gt;

&lt;p&gt;Hidden partitioning in a plain &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CREATE TABLE&lt;/code&gt; is itself proof the extension
loaded, since &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;days(started_at)&lt;/code&gt; is Iceberg syntax rather than Spark syntax:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CREATE NAMESPACE IF NOT EXISTS iceberg.lake&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  CREATE TABLE iceberg.lake.trips (
    trip_id    BIGINT,
    rider_id   BIGINT,
    city       STRING,
    fare       DECIMAL(10,2),
    started_at TIMESTAMP)
  USING iceberg
  PARTITIONED BY (days(started_at))&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  INSERT INTO iceberg.lake.trips VALUES
    (1, 101, &apos;Hyderabad&apos;, 245.50, TIMESTAMP &apos;2026-09-01 08:15:00&apos;),
    (2, 102, &apos;Bengaluru&apos;, 180.00, TIMESTAMP &apos;2026-09-01 09:30:00&apos;),
    (3, 103, &apos;Hyderabad&apos;, 320.75, TIMESTAMP &apos;2026-09-02 07:45:00&apos;)&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Note that the positional &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INSERT&lt;/code&gt; is fine here. Iceberg keeps the declared column
order because its partitioning is derived from a column rather than stored as
one, which is a real difference from &lt;a href=&quot;/SparkConnectHudi/&quot;&gt;Hudi&lt;/a&gt;,
where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PARTITIONED BY&lt;/code&gt; moves the partition column to the end of the schema and
breaks positional inserts.&lt;/p&gt;

&lt;p&gt;Row-level DML, all of it extension-provided:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;UPDATE iceberg.lake.trips SET fare = 250.00 WHERE trip_id = 1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  MERGE INTO iceberg.lake.trips t
  USING (SELECT 4L AS trip_id, 104L AS rider_id, &apos;Pune&apos; AS city,
                CAST(99.00 AS DECIMAL(10,2)) AS fare,
                TIMESTAMP &apos;2026-09-03 10:00:00&apos; AS started_at) s
    ON t.trip_id = s.trip_id
  WHEN MATCHED     THEN UPDATE SET t.fare = s.fare
  WHEN NOT MATCHED THEN INSERT *&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;DELETE FROM iceberg.lake.trips WHERE trip_id = 2&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The v2 DataFrame writer works too:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;decimal&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Decimal&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;functions&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;new_trip&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;105&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;Chennai&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Decimal&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;75.25&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-04 11:00:00&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt;
        &lt;span class=&quot;s&quot;&gt;&quot;trip_id bigint, rider_id bigint, city string, &quot;&lt;/span&gt;
        &lt;span class=&quot;s&quot;&gt;&quot;fare decimal(10,2), started_at string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;to_timestamp&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;new_trip&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeTo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg.lake.trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;append&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Decimal(&quot;75.25&quot;)&lt;/code&gt; rather than &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;75.25&lt;/code&gt; is deliberate, and the reason is in the
limitations section below.&lt;/p&gt;

&lt;p&gt;Metadata tables and procedures, which are the parts people expect to break over
gRPC:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;table&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg.lake.trips.snapshots&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;table&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg.lake.trips.files&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;table&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg.lake.trips.history&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CALL iceberg.system.rewrite_data_files(table =&amp;gt; &apos;lake.trips&apos;)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;From a clean warehouse the whole sequence gives:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;count after INSERT                      3
fare for trip_id 1 after UPDATE         250.00
count after MERGE and DELETE            3
count after writeTo append              4
read via format(&quot;iceberg&quot;)              4
iceberg.lake.trips.snapshots            5 rows
iceberg.lake.trips.files                4 rows
iceberg.lake.trips.history              5 rows
CALL rewrite_data_files                 returns a row, 0 files rewritten
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Five snapshots for five write operations, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; rewriting
nothing is correct on a four-row table whose files are all under the target size.
A procedure that returns a row without doing work is not a failure.&lt;/p&gt;

&lt;h2 id=&quot;time-travel-and-the-extension-that-breaks-it&quot;&gt;Time travel, and the extension that breaks it&lt;/h2&gt;

&lt;p&gt;Start with the good news, on a server whose only extension is Iceberg’s. All
three time-travel syntaxes work through the thin client. Derive the snapshot id
rather than hardcoding one, and derive it by &lt;strong&gt;commit time&lt;/strong&gt;, because Iceberg
snapshot ids are random 64-bit values and are not ordered chronologically:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;oldest&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
    SELECT snapshot_id FROM iceberg.lake.trips.snapshots
    ORDER BY committed_at LIMIT 1&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;][&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) FROM iceberg.lake.trips VERSION AS OF %d&quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;oldest&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) FROM iceberg.lake.trips FOR SYSTEM_VERSION AS OF %d&quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;oldest&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;snapshot-id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;oldest&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg.lake.trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;VERSION AS OF oldest             3
FOR SYSTEM_VERSION AS OF oldest  3
snapshot-id reader option        3
current table                    4
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;min(snapshot_id)&lt;/code&gt; is the wrong way to find the oldest snapshot and it is an easy
mistake to make, because it looks like an id sequence. On one test table the
minimum id was the &lt;strong&gt;third&lt;/strong&gt; of five snapshots, so the query silently returned
the wrong point in history rather than failing. Order by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;committed_at&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;Passing an id that does not exist gives a clear error rather than a wrong answer,
which is worth knowing when you are debugging:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;IllegalArgumentException: Cannot find snapshot with ID 2136647905244113308
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h3 id=&quot;the-failure-and-the-diagnosis-that-found-it&quot;&gt;The failure, and the diagnosis that found it&lt;/h3&gt;

&lt;p&gt;Now the part that cost me a published mistake. On a server that loads &lt;strong&gt;both&lt;/strong&gt;
Hudi’s and Iceberg’s session extensions, every SQL time-travel syntax fails:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) FROM iceberg.lake.trips VERSION AS OF %d&quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;oldest&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;SparkException: [INTERNAL_ERROR] Found the unresolved operator: &apos;TimeTravelRelation
SQLSTATE: XX000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;An &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INTERNAL_ERROR&lt;/code&gt; naming an unresolved operator looks exactly like a transport
that cannot serialise a plan node, and Spark Connect is the newest thing in the
stack, so it is the natural suspect. It is not the culprit, but proving that
takes two controls rather than one, and I originally ran only the first.&lt;/p&gt;

&lt;p&gt;Running it in a classic local session rules out Connect. That much I did, and the
query failed there too, so I concluded the fault was an Iceberg and Spark 4.0
analyzer gap. That conclusion was wrong, because my classic session was
configured from the same runbook as the server: it loaded both extensions. The
control isolated the transport and left the real variable untouched.&lt;/p&gt;

&lt;p&gt;Varying the extension list as well gives a 2x2 that settles it:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Session&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Iceberg extension only&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Hudi and Iceberg extensions&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark Connect&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VERSION AS OF&lt;/code&gt; returns the historical count&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INTERNAL_ERROR&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Classic local&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VERSION AS OF&lt;/code&gt; returns the historical count&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INTERNAL_ERROR&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The transport is irrelevant. &lt;strong&gt;Loading Hudi’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieSparkSessionExtension&lt;/code&gt;
alongside Iceberg’s breaks Iceberg’s SQL time travel&lt;/strong&gt;, in a classic session just
as much as through Connect.&lt;/p&gt;

&lt;p&gt;Two further facts make it easier to live with. The interference is
one-directional: on that same both-extension server, Hudi’s own DML, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;,
procedures, incremental reads and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;as.of.instant&lt;/code&gt; time travel all worked. And the
Iceberg &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshot-id&lt;/code&gt; reader option keeps working with both extensions loaded,
because it never builds a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TimeTravelRelation&lt;/code&gt; in the first place.&lt;/p&gt;

&lt;p&gt;So the practical advice is concrete rather than “wait for a fix”:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;If you need Iceberg SQL time travel, give Iceberg a server whose &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt; contains only &lt;a href=&quot;https://github.com/apache/iceberg/blob/main/spark/v4.0/spark-extensions/src/main/scala/org/apache/iceberg/spark/extensions/IcebergSparkSessionExtensions.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;IcebergSparkSessionExtensions&lt;/code&gt;&lt;/a&gt;.&lt;/li&gt;
  &lt;li&gt;If you must share one server with Hudi, use the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshot-id&lt;/code&gt; reader option, which is unaffected.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;The wider lesson is about the shape of the control experiment, not about Iceberg.
Reproducing a failure without Spark Connect proved only that Connect was not the
cause; it said nothing about which of the remaining variables was. A control that
holds the suspect constant is not a control at all, and copying the full config
into the repro is exactly how that happens.&lt;/p&gt;

&lt;h2 id=&quot;what-the-thin-client-cannot-do&quot;&gt;What the thin client cannot do&lt;/h2&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;PySparkAttributeError: [JVM_ATTRIBUTE_NOT_SUPPORTED] Attribute `sparkContext` is not
supported in Spark Connect as it depends on the JVM.
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;PySparkNotImplementedError: [NOT_IMPLEMENTED] rdd is not implemented.
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Neither matters for Iceberg work, which goes through SQL and DataFrames. The one
that actually costs time is a Python &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;float&lt;/code&gt; in a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;decimal&lt;/code&gt; column, which a
classic session coerces and the Connect client rejects:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;9&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;109&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;Kochi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;75.25&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-05 12:00:00&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;trip_id bigint, rider_id bigint, city string, &quot;&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;fare decimal(10,2), started_at string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AssertionError
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is the whole message. The traceback lands in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;convert_decimal&lt;/code&gt; in
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pyspark/sql/conversion.py&lt;/code&gt;, which does
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;assert isinstance(value, decimal.Decimal)&lt;/code&gt;. Any table with a money column hits
it on the first &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt;, and a bare &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AssertionError&lt;/code&gt; reads like a
client bug rather than a type error.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Classic session&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Spark Connect client&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sparkContext&lt;/code&gt; available&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Raises &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;JVM_ATTRIBUTE_NOT_SUPPORTED&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.rdd&lt;/code&gt; available&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Raises &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NOT_IMPLEMENTED&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;float&lt;/code&gt; coerced into a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;decimal&lt;/code&gt; column&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Bare &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AssertionError&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Iceberg jar and extension per submit&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Server-side only, fixed at startup&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;DDL, DML, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;writeTo&lt;/code&gt;, metadata tables, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CALL&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;All work unchanged&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;VERSION AS OF&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Works, unless Hudi’s extension shares the session&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Spark Connect cannot carry a time-travel query.”&lt;/strong&gt; It can. All three
syntaxes return the historical count over gRPC when Iceberg’s extension is the
only one loaded. The failure appears when Hudi’s session extension shares the
server, and it appears in a classic local session just as readily, so the
variable is the extension list and not the transport.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;min(snapshot_id)&lt;/code&gt; finds the oldest snapshot.”&lt;/strong&gt; Snapshot ids are random
64-bit values, not a sequence. On the test table the minimum id was the &lt;strong&gt;third&lt;/strong&gt;
of five snapshots, so the query returned the wrong point in history and did not
error while doing it. Order by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;committed_at&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“A client library and server must be the same version.”&lt;/strong&gt; An older client is
tolerated: a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;3.5.9&lt;/code&gt; client drove the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.0.2&lt;/code&gt; server through every example here.
A newer one is not, and it fails specifically rather than generally, running all
the SQL and then breaking on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“An &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INTERNAL_ERROR&lt;/code&gt; from a new transport means the transport is at fault.”&lt;/strong&gt;
It is the natural suspect and it was wrong here. Reproducing a failure without
Spark Connect only clears Spark Connect; it says nothing about which of the
remaining variables is responsible, and copying the whole server config into the
repro carries the real cause along with it.&lt;/p&gt;

&lt;h2 id=&quot;production-notes&quot;&gt;Production notes&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Treat the server config as a contract.&lt;/strong&gt; The jar, the extension and the catalogs are fixed at startup, so adding a catalog means a restart. Plan for more than one server if teams need different catalog sets.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hive&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rest&lt;/code&gt; catalog types for shared tables.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hadoop&lt;/code&gt; is fine for a self-contained example and gives no cross-engine commit atomicity.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Confirm the extension from the client&lt;/strong&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.conf.get(&quot;spark.sql.extensions&quot;)&lt;/code&gt; before debugging a rejected &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Give Iceberg a server whose only extension is Iceberg’s&lt;/strong&gt; if you need SQL time travel. Adding &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieSparkSessionExtension&lt;/code&gt; to the same session breaks it.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Vary one thing at a time when reproducing a failure.&lt;/strong&gt; A classic-session repro rules out Spark Connect and nothing else; if you copy the whole config into the repro, the actual cause comes along with it.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Find the oldest snapshot by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;committed_at&lt;/code&gt;, never by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;min(snapshot_id)&lt;/code&gt;.&lt;/strong&gt; Snapshot ids are random 64-bit values, so the minimum is not the earliest and the query returns the wrong history without erroring.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Pass &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Decimal&lt;/code&gt; for decimal columns&lt;/strong&gt; in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt;, and prefer &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CAST(... AS DECIMAL(10,2))&lt;/code&gt; inside SQL.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Does the client need the Iceberg jar?&lt;/strong&gt;
No. The client sends a plan naming a table and the server resolves it, so a
virtualenv with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pyspark&lt;/code&gt; is the entire client-side dependency. That also removes
the jar-version skew that usually makes onboarding painful.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Can one server serve Iceberg and Hudi together?&lt;/strong&gt;
Mostly, with one measured exception. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt; takes a
comma-separated list, Hudi takes over &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark_catalog&lt;/code&gt; while Iceberg gets a named
catalog, and both formats’ DDL and DML work. But loading Hudi’s extension breaks
Iceberg’s SQL time travel, as shown above, so a shared server costs you that one
feature and you fall back to the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshot-id&lt;/code&gt; reader option. Hudi loses nothing
in the arrangement. The Hudi side is covered in
&lt;a href=&quot;/SparkConnectHudi/&quot;&gt;Apache Hudi through Spark Connect&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Are metadata tables really available over gRPC?&lt;/strong&gt;
Yes, and they are just tables as far as the client is concerned.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.table(&quot;iceberg.lake.trips.snapshots&quot;)&lt;/code&gt; is resolved entirely on the server,
so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshots&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;files&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;history&lt;/code&gt; and the rest all work.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why does &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt; report zero files?&lt;/strong&gt;
Because there was nothing worth rewriting. On a small table all files are already
under the target size, so the procedure returns a row with zeroes in it. Judge
maintenance procedures by the table’s file layout afterwards, not by whether the
call returned.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Is SQL time travel broken because of Spark Connect?&lt;/strong&gt;
No, and it is not broken in general either. It works through Connect when
Iceberg’s extension is the only one loaded. It breaks when Hudi’s extension
shares the session, in a classic session just as much as through Connect, so the
variable is the extension list rather than the transport.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;Iceberg through Spark Connect is close to uneventful, which is the interesting
result. Hidden partitioning, row-level &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;, the metadata tables, the stored
procedures and time travel all cross a gRPC boundary without special handling,
because they are resolved server-side where the Iceberg runtime already lives,
and the client stays a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pip install&lt;/code&gt; and a URL. What Connect takes away is the
per-job escape hatch: the jar, the extension and the catalog set are decided once
when the server starts, so a new catalog is an operational change rather than a
config line in someone’s notebook. For a shared lakehouse that is usually the
behaviour you wanted anyway.&lt;/p&gt;

&lt;p&gt;The part worth keeping is how the one failure was diagnosed, because the first
attempt got it wrong. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;[INTERNAL_ERROR] Found the unresolved operator&lt;/code&gt; is about
as suggestive of a broken transport as an error message gets, so the first
control was a classic local session, and it reproduced the failure. That was
enough to clear Spark Connect and it felt like enough to name a cause, so this
post originally blamed an Iceberg and Spark 4.0 analyzer gap.&lt;/p&gt;

&lt;p&gt;It was not that. The classic session had been configured by copying the server’s
own config, which loaded Hudi’s session extension alongside Iceberg’s, and that
is the actual cause. Varying the extension list rather than the transport gives a
2x2 where the two Iceberg-only cells pass and the two both-extension cells fail,
whether Connect is involved or not. A control that changes one variable while
silently carrying the real one along is not a control, and copying config into a
repro is the ordinary way that happens. The advice changes with the diagnosis:
not “wait for a fix” but “do not put Hudi’s extension on a server that owes
anyone Iceberg SQL time travel”.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/spark-connect-overview.html&quot;&gt;Spark Connect overview&lt;/a&gt; for the architecture and the client APIs&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-configuration/&quot;&gt;Iceberg Spark configuration&lt;/a&gt; for catalog types and the extension class&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://iceberg.apache.org/docs/latest/spark-queries/&quot;&gt;Iceberg Spark queries&lt;/a&gt; for the metadata tables and the time-travel read options&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/python/pyspark/sql/conversion.py&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;conversion.py&lt;/code&gt;&lt;/a&gt;, whose &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;convert_decimal&lt;/code&gt; is the assertion behind the empty &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AssertionError&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheIcebergArchitecture/&quot;&gt;Apache Iceberg architecture&lt;/a&gt; for the snapshot, manifest and delete-file machinery underneath all of this&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Iceberg, Apache Hudi, Apache Hive, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="SparkConnect" /><category term="Iceberg" /><category term="Lakehouse" /><summary type="html">Architecture: where Iceberg has to live Starting the server Connecting the client The write and read surface Time travel, and the extension that breaks it The failure, and the diagnosis that found it What the thin client cannot do Common misconceptions Production notes Frequently asked questions Conclusion References Trademarks TL;DR The Iceberg runtime jar, spark.sql.extensions and the catalog configs belong to the Spark Connect server, set at startup. A client cannot add them, which makes the server config a contract rather than a per-job choice. The client is a pip install with no Spark, no Java and no Iceberg jar. Everything here was driven from one. DDL with hidden partitioning, INSERT, UPDATE, MERGE, DELETE, writeTo, the metadata tables and CALL procedures all work unchanged over gRPC. SQL time travel works over Connect too, in all three syntaxes, as long as Iceberg’s extension is the only one loaded. Add Hudi’s session extension to the same server and every SQL time-travel syntax starts failing with [INTERNAL_ERROR] Found the unresolved operator: &apos;TimeTravelRelation. It is one-directional: Hudi keeps working, Iceberg loses time travel, and the snapshot-id reader option keeps working either way. Iceberg is not part of Spark. It arrives as a runtime jar, installs a Catalyst extension and registers a catalog. Those are session-level concerns, and Spark Connect moves the session behind a gRPC connection, so the first question is which half of the setup goes where. The second question is more interesting, and it is the reason this post exists. When something fails through Spark Connect, the transport is the most attractive suspect and usually the wrong one. This post runs Iceberg’s full surface from a thin client, and the single thing that breaks turns out to be caused neither by Spark Connect nor by Iceberg, but by another format’s session extension sharing the server. Finding that took two control experiments, and the first one, run on its own, produced a confident wrong answer that this post had published. Architecture: where Iceberg has to live A classic session keeps the SparkSession, the analyzer and the catalog in your process, which is why your own spark-submit line can install Iceberg. Spark Connect splits that: flowchart TB subgraph C[&quot;client, your laptop&quot;] A[&quot;DataFrame and SQL calls&quot;] --&amp;gt; B[&quot;unresolved plan&amp;lt;br/&amp;gt;as protobuf&quot;] end B --&amp;gt;|&quot;gRPC, port 15002&quot;| S subgraph S[&quot;Spark Connect server, the driver&quot;] D[&quot;analyzer plus SparkCatalog&quot;] --&amp;gt; E[&quot;Catalyst optimiser plus&amp;lt;br/&amp;gt;IcebergSparkSessionExtensions&quot;] E --&amp;gt; F[&quot;physical plan&quot;] end F --&amp;gt; X[&quot;executors, Iceberg runtime on the classpath&quot;] X --&amp;gt;|&quot;Arrow batches&quot;| A The client cannot resolve iceberg.lake.trips: it has no catalog and no notion of an Iceberg table. It ships the operation and the server works out what it means. Three things therefore have to be on the server, and all three are fixed when it starts: What Iceberg needs Config Why The runtime jar --jars or --packages Contains the DataSource, the catalog and the procedures A Catalyst extension spark.sql.extensions Provides MERGE, UPDATE, DELETE and CALL A catalog spark.sql.catalog.&amp;lt;name&amp;gt; Resolves &amp;lt;name&amp;gt;.db.table and owns commit atomicity spark.conf.set from a client reaches the server for runtime SQL configs, but spark.sql.extensions is read once while the session is built, before any client connects. A client can read it, which is the quickest way to tell a misconfigured server from a bad query. Starting the server $SPARK_HOME/sbin/start-connect-server.sh \ --packages org.apache.iceberg:iceberg-spark-runtime-4.0_2.13:1.11.0 \ --conf spark.sql.extensions=org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions \ --conf spark.sql.catalog.iceberg=org.apache.iceberg.spark.SparkCatalog \ --conf spark.sql.catalog.iceberg.type=hadoop \ --conf spark.sql.catalog.iceberg.warehouse=/tmp/warehouse/iceberg \ --master &quot;local[4]&quot; Swap --packages for --jars with an absolute path when the host cannot reach Maven Central; both forms were run for this post. The coordinate carries two compatibilities that fail hard rather than warn: Coordinate part Must match iceberg-spark-runtime-4.0 Your Spark minor version, 4.0 here _2.13 Spark’s Scala binary version Spark 4 is Scala 2.13 only, so a _2.12 runtime never loads. Two notes on the catalog. type=hadoop keeps the example self-contained by putting the pointer in the warehouse directory, and it gives no atomic commits across engines, so use hive or rest for anything shared. And the catalog name you choose becomes the first part of every table name, so iceberg here means tables are iceberg.lake.trips. The extension class is org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions, which is where you would expect it, under the project’s own package. It can still be confirmed from the jar rather than from memory: unzip -l iceberg-spark-runtime-4.0_2.13-1.11.0.jar | grep IcebergSparkSessionExtensions # 9663 org/apache/iceberg/spark/extensions/IcebergSparkSessionExtensions.class A wrong class name does not stop the server. It logs a warning and starts, and then every MERGE fails later for reasons that look unrelated, so grep the server log for Cannot use before debugging your SQL. Connecting the client # pin the client to the version your server runs pip install &quot;pyspark[connect]==4.0.2&quot; from pyspark.sql import SparkSession spark = SparkSession.builder.remote(&quot;sc://localhost:15002&quot;).getOrCreate() print(spark.conf.get(&quot;spark.sql.extensions&quot;)) # org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions No Java, no SPARK_HOME, no Iceberg jar. spark.version reports the server’s version, not the client’s, so it is not evidence about your local install; use pyspark.__version__ for that. The pin is deliberate. An unpinned install takes whatever is newest on PyPI, and a client newer than the server runs SQL happily and then fails on the first createDataFrame with SQL_CONF_NOT_FOUND for spark.sql.session.localRelationSizeLimit, which it expects the server to have. An older client is tolerated; a newer one is not. The write and read surface Hidden partitioning in a plain CREATE TABLE is itself proof the extension loaded, since days(started_at) is Iceberg syntax rather than Spark syntax: spark.sql(&quot;CREATE NAMESPACE IF NOT EXISTS iceberg.lake&quot;) spark.sql(&quot;&quot;&quot; CREATE TABLE iceberg.lake.trips ( trip_id BIGINT, rider_id BIGINT, city STRING, fare DECIMAL(10,2), started_at TIMESTAMP) USING iceberg PARTITIONED BY (days(started_at))&quot;&quot;&quot;) spark.sql(&quot;&quot;&quot; INSERT INTO iceberg.lake.trips VALUES (1, 101, &apos;Hyderabad&apos;, 245.50, TIMESTAMP &apos;2026-09-01 08:15:00&apos;), (2, 102, &apos;Bengaluru&apos;, 180.00, TIMESTAMP &apos;2026-09-01 09:30:00&apos;), (3, 103, &apos;Hyderabad&apos;, 320.75, TIMESTAMP &apos;2026-09-02 07:45:00&apos;)&quot;&quot;&quot;) Note that the positional INSERT is fine here. Iceberg keeps the declared column order because its partitioning is derived from a column rather than stored as one, which is a real difference from Hudi, where PARTITIONED BY moves the partition column to the end of the schema and breaks positional inserts. Row-level DML, all of it extension-provided: spark.sql(&quot;UPDATE iceberg.lake.trips SET fare = 250.00 WHERE trip_id = 1&quot;) spark.sql(&quot;&quot;&quot; MERGE INTO iceberg.lake.trips t USING (SELECT 4L AS trip_id, 104L AS rider_id, &apos;Pune&apos; AS city, CAST(99.00 AS DECIMAL(10,2)) AS fare, TIMESTAMP &apos;2026-09-03 10:00:00&apos; AS started_at) s ON t.trip_id = s.trip_id WHEN MATCHED THEN UPDATE SET t.fare = s.fare WHEN NOT MATCHED THEN INSERT *&quot;&quot;&quot;) spark.sql(&quot;DELETE FROM iceberg.lake.trips WHERE trip_id = 2&quot;) The v2 DataFrame writer works too: from decimal import Decimal from pyspark.sql import functions as F new_trip = (spark.createDataFrame( [(5, 105, &quot;Chennai&quot;, Decimal(&quot;75.25&quot;), &quot;2026-09-04 11:00:00&quot;)], &quot;trip_id bigint, rider_id bigint, city string, &quot; &quot;fare decimal(10,2), started_at string&quot;) .withColumn(&quot;started_at&quot;, F.to_timestamp(&quot;started_at&quot;))) new_trip.writeTo(&quot;iceberg.lake.trips&quot;).append() Decimal(&quot;75.25&quot;) rather than 75.25 is deliberate, and the reason is in the limitations section below. Metadata tables and procedures, which are the parts people expect to break over gRPC: spark.table(&quot;iceberg.lake.trips.snapshots&quot;).count() spark.table(&quot;iceberg.lake.trips.files&quot;).count() spark.table(&quot;iceberg.lake.trips.history&quot;).count() spark.sql(&quot;CALL iceberg.system.rewrite_data_files(table =&amp;gt; &apos;lake.trips&apos;)&quot;).collect() From a clean warehouse the whole sequence gives: count after INSERT 3 fare for trip_id 1 after UPDATE 250.00 count after MERGE and DELETE 3 count after writeTo append 4 read via format(&quot;iceberg&quot;) 4 iceberg.lake.trips.snapshots 5 rows iceberg.lake.trips.files 4 rows iceberg.lake.trips.history 5 rows CALL rewrite_data_files returns a row, 0 files rewritten Five snapshots for five write operations, and rewrite_data_files rewriting nothing is correct on a four-row table whose files are all under the target size. A procedure that returns a row without doing work is not a failure. Time travel, and the extension that breaks it Start with the good news, on a server whose only extension is Iceberg’s. All three time-travel syntaxes work through the thin client. Derive the snapshot id rather than hardcoding one, and derive it by commit time, because Iceberg snapshot ids are random 64-bit values and are not ordered chronologically: oldest = spark.sql(&quot;&quot;&quot; SELECT snapshot_id FROM iceberg.lake.trips.snapshots ORDER BY committed_at LIMIT 1&quot;&quot;&quot;).collect()[0][0] spark.sql(&quot;SELECT count(*) FROM iceberg.lake.trips VERSION AS OF %d&quot; % oldest) spark.sql(&quot;SELECT count(*) FROM iceberg.lake.trips FOR SYSTEM_VERSION AS OF %d&quot; % oldest) spark.read.option(&quot;snapshot-id&quot;, oldest).format(&quot;iceberg&quot;).load(&quot;iceberg.lake.trips&quot;).count() VERSION AS OF oldest 3 FOR SYSTEM_VERSION AS OF oldest 3 snapshot-id reader option 3 current table 4 min(snapshot_id) is the wrong way to find the oldest snapshot and it is an easy mistake to make, because it looks like an id sequence. On one test table the minimum id was the third of five snapshots, so the query silently returned the wrong point in history rather than failing. Order by committed_at. Passing an id that does not exist gives a clear error rather than a wrong answer, which is worth knowing when you are debugging: IllegalArgumentException: Cannot find snapshot with ID 2136647905244113308 The failure, and the diagnosis that found it Now the part that cost me a published mistake. On a server that loads both Hudi’s and Iceberg’s session extensions, every SQL time-travel syntax fails: spark.sql(&quot;SELECT count(*) FROM iceberg.lake.trips VERSION AS OF %d&quot; % oldest) SparkException: [INTERNAL_ERROR] Found the unresolved operator: &apos;TimeTravelRelation SQLSTATE: XX000 An INTERNAL_ERROR naming an unresolved operator looks exactly like a transport that cannot serialise a plan node, and Spark Connect is the newest thing in the stack, so it is the natural suspect. It is not the culprit, but proving that takes two controls rather than one, and I originally ran only the first. Running it in a classic local session rules out Connect. That much I did, and the query failed there too, so I concluded the fault was an Iceberg and Spark 4.0 analyzer gap. That conclusion was wrong, because my classic session was configured from the same runbook as the server: it loaded both extensions. The control isolated the transport and left the real variable untouched. Varying the extension list as well gives a 2x2 that settles it: Session Iceberg extension only Hudi and Iceberg extensions Spark Connect VERSION AS OF returns the historical count INTERNAL_ERROR Classic local VERSION AS OF returns the historical count INTERNAL_ERROR The transport is irrelevant. Loading Hudi’s HoodieSparkSessionExtension alongside Iceberg’s breaks Iceberg’s SQL time travel, in a classic session just as much as through Connect. Two further facts make it easier to live with. The interference is one-directional: on that same both-extension server, Hudi’s own DML, MERGE, procedures, incremental reads and as.of.instant time travel all worked. And the Iceberg snapshot-id reader option keeps working with both extensions loaded, because it never builds a TimeTravelRelation in the first place. So the practical advice is concrete rather than “wait for a fix”: If you need Iceberg SQL time travel, give Iceberg a server whose spark.sql.extensions contains only IcebergSparkSessionExtensions. If you must share one server with Hudi, use the snapshot-id reader option, which is unaffected. The wider lesson is about the shape of the control experiment, not about Iceberg. Reproducing a failure without Spark Connect proved only that Connect was not the cause; it said nothing about which of the remaining variables was. A control that holds the suspect constant is not a control at all, and copying the full config into the repro is exactly how that happens. What the thin client cannot do spark.sparkContext PySparkAttributeError: [JVM_ATTRIBUTE_NOT_SUPPORTED] Attribute `sparkContext` is not supported in Spark Connect as it depends on the JVM. spark.range(10).rdd.map(lambda r: r.id).collect() PySparkNotImplementedError: [NOT_IMPLEMENTED] rdd is not implemented. Neither matters for Iceberg work, which goes through SQL and DataFrames. The one that actually costs time is a Python float in a decimal column, which a classic session coerces and the Connect client rejects: spark.createDataFrame( [(9, 109, &quot;Kochi&quot;, 75.25, &quot;2026-09-05 12:00:00&quot;)], &quot;trip_id bigint, rider_id bigint, city string, &quot; &quot;fare decimal(10,2), started_at string&quot;) AssertionError That is the whole message. The traceback lands in convert_decimal in pyspark/sql/conversion.py, which does assert isinstance(value, decimal.Decimal). Any table with a money column hits it on the first createDataFrame, and a bare AssertionError reads like a client bug rather than a type error. Classic session Spark Connect client spark.sparkContext available Raises JVM_ATTRIBUTE_NOT_SUPPORTED df.rdd available Raises NOT_IMPLEMENTED float coerced into a decimal column Bare AssertionError Iceberg jar and extension per submit Server-side only, fixed at startup DDL, DML, writeTo, metadata tables, CALL All work unchanged SQL VERSION AS OF Works, unless Hudi’s extension shares the session Common misconceptions “Spark Connect cannot carry a time-travel query.” It can. All three syntaxes return the historical count over gRPC when Iceberg’s extension is the only one loaded. The failure appears when Hudi’s session extension shares the server, and it appears in a classic local session just as readily, so the variable is the extension list and not the transport. “min(snapshot_id) finds the oldest snapshot.” Snapshot ids are random 64-bit values, not a sequence. On the test table the minimum id was the third of five snapshots, so the query returned the wrong point in history and did not error while doing it. Order by committed_at. “A client library and server must be the same version.” An older client is tolerated: a 3.5.9 client drove the 4.0.2 server through every example here. A newer one is not, and it fails specifically rather than generally, running all the SQL and then breaking on createDataFrame. “An INTERNAL_ERROR from a new transport means the transport is at fault.” It is the natural suspect and it was wrong here. Reproducing a failure without Spark Connect only clears Spark Connect; it says nothing about which of the remaining variables is responsible, and copying the whole server config into the repro carries the real cause along with it. Production notes Treat the server config as a contract. The jar, the extension and the catalogs are fixed at startup, so adding a catalog means a restart. Plan for more than one server if teams need different catalog sets. Use hive or rest catalog types for shared tables. hadoop is fine for a self-contained example and gives no cross-engine commit atomicity. Confirm the extension from the client with spark.conf.get(&quot;spark.sql.extensions&quot;) before debugging a rejected MERGE. Give Iceberg a server whose only extension is Iceberg’s if you need SQL time travel. Adding HoodieSparkSessionExtension to the same session breaks it. Vary one thing at a time when reproducing a failure. A classic-session repro rules out Spark Connect and nothing else; if you copy the whole config into the repro, the actual cause comes along with it. Find the oldest snapshot by committed_at, never by min(snapshot_id). Snapshot ids are random 64-bit values, so the minimum is not the earliest and the query returns the wrong history without erroring. Pass Decimal for decimal columns in createDataFrame, and prefer CAST(... AS DECIMAL(10,2)) inside SQL. Frequently asked questions Does the client need the Iceberg jar? No. The client sends a plan naming a table and the server resolves it, so a virtualenv with pyspark is the entire client-side dependency. That also removes the jar-version skew that usually makes onboarding painful. Can one server serve Iceberg and Hudi together? Mostly, with one measured exception. spark.sql.extensions takes a comma-separated list, Hudi takes over spark_catalog while Iceberg gets a named catalog, and both formats’ DDL and DML work. But loading Hudi’s extension breaks Iceberg’s SQL time travel, as shown above, so a shared server costs you that one feature and you fall back to the snapshot-id reader option. Hudi loses nothing in the arrangement. The Hudi side is covered in Apache Hudi through Spark Connect. Are metadata tables really available over gRPC? Yes, and they are just tables as far as the client is concerned. spark.table(&quot;iceberg.lake.trips.snapshots&quot;) is resolved entirely on the server, so snapshots, files, history and the rest all work. Why does rewrite_data_files report zero files? Because there was nothing worth rewriting. On a small table all files are already under the target size, so the procedure returns a row with zeroes in it. Judge maintenance procedures by the table’s file layout afterwards, not by whether the call returned. Is SQL time travel broken because of Spark Connect? No, and it is not broken in general either. It works through Connect when Iceberg’s extension is the only one loaded. It breaks when Hudi’s extension shares the session, in a classic session just as much as through Connect, so the variable is the extension list rather than the transport. Conclusion Iceberg through Spark Connect is close to uneventful, which is the interesting result. Hidden partitioning, row-level MERGE, the metadata tables, the stored procedures and time travel all cross a gRPC boundary without special handling, because they are resolved server-side where the Iceberg runtime already lives, and the client stays a pip install and a URL. What Connect takes away is the per-job escape hatch: the jar, the extension and the catalog set are decided once when the server starts, so a new catalog is an operational change rather than a config line in someone’s notebook. For a shared lakehouse that is usually the behaviour you wanted anyway. The part worth keeping is how the one failure was diagnosed, because the first attempt got it wrong. [INTERNAL_ERROR] Found the unresolved operator is about as suggestive of a broken transport as an error message gets, so the first control was a classic local session, and it reproduced the failure. That was enough to clear Spark Connect and it felt like enough to name a cause, so this post originally blamed an Iceberg and Spark 4.0 analyzer gap. It was not that. The classic session had been configured by copying the server’s own config, which loaded Hudi’s session extension alongside Iceberg’s, and that is the actual cause. Varying the extension list rather than the transport gives a 2x2 where the two Iceberg-only cells pass and the two both-extension cells fail, whether Connect is involved or not. A control that changes one variable while silently carrying the real one along is not a control, and copying config into a repro is the ordinary way that happens. The advice changes with the diagnosis: not “wait for a fix” but “do not put Hudi’s extension on a server that owes anyone Iceberg SQL time travel”. References Spark Connect overview for the architecture and the client APIs Iceberg Spark configuration for catalog types and the extension class Iceberg Spark queries for the metadata tables and the time-travel read options conversion.py, whose convert_decimal is the assertion behind the empty AssertionError Apache Iceberg architecture for the snapshot, manifest and delete-file machinery underneath all of this Trademarks Apache Spark, Apache Iceberg, Apache Hudi, Apache Hive, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Apache Hudi through Spark Connect: running the full table surface from a thin client</title><link href="https://rangareddy.github.io/SparkConnectHudi/" rel="alternate" type="text/html" title="Apache Hudi through Spark Connect: running the full table surface from a thin client" /><published>2026-09-18T09:00:00+05:30</published><updated>2026-09-18T09:00:00+05:30</updated><id>https://rangareddy.github.io/SparkConnectHudi</id><content type="html" xml:base="https://rangareddy.github.io/SparkConnectHudi/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-what-spark-connect-changes-for-hudi&quot; id=&quot;markdown-toc-architecture-what-spark-connect-changes-for-hudi&quot;&gt;Architecture: what Spark Connect changes for Hudi&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#starting-a-server-that-speaks-hudi&quot; id=&quot;markdown-toc-starting-a-server-that-speaks-hudi&quot;&gt;Starting a server that speaks Hudi&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#the-extension-class-name-which-is-the-usual-mistake&quot; id=&quot;markdown-toc-the-extension-class-name-which-is-the-usual-mistake&quot;&gt;The extension class name, which is the usual mistake&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#connecting-the-client&quot; id=&quot;markdown-toc-connecting-the-client&quot;&gt;Connecting the client&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#copy-on-write-tables-the-full-dml-surface&quot; id=&quot;markdown-toc-copy-on-write-tables-the-full-dml-surface&quot;&gt;Copy-on-write tables, the full DML surface&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#the-partitioning-trap&quot; id=&quot;markdown-toc-the-partitioning-trap&quot;&gt;The partitioning trap&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#update-merge-and-delete&quot; id=&quot;markdown-toc-update-merge-and-delete&quot;&gt;Update, merge and delete&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#schema-evolution-two-of-four-operations&quot; id=&quot;markdown-toc-schema-evolution-two-of-four-operations&quot;&gt;Schema evolution: two of four operations&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#merge-on-read-and-what-the-two-query-types-show&quot; id=&quot;markdown-toc-merge-on-read-and-what-the-two-query-types-show&quot;&gt;Merge-on-read, and what the two query types show&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#incremental-queries-and-time-travel&quot; id=&quot;markdown-toc-incremental-queries-and-time-travel&quot;&gt;Incremental queries and time travel&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#procedures-and-metadata-over-grpc&quot; id=&quot;markdown-toc-procedures-and-metadata-over-grpc&quot;&gt;Procedures and metadata, over gRPC&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#rollback_to_instant-only-unwinds-the-newest-instant&quot; id=&quot;markdown-toc-rollback_to_instant-only-unwinds-the-newest-instant&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_instant&lt;/code&gt; only unwinds the newest instant&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-the-thin-client-cannot-do&quot; id=&quot;markdown-toc-what-the-thin-client-cannot-do&quot;&gt;What the thin client cannot do&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-notes&quot; id=&quot;markdown-toc-production-notes&quot;&gt;Production notes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Spark Connect moves planning to a server, so the Hudi bundle, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt;, the Kryo serializer and &lt;a href=&quot;https://github.com/apache/hudi/blob/master/hudi-spark-datasource/hudi-spark-common/src/main/scala/org/apache/spark/sql/hudi/catalog/HoodieCatalog.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieCatalog&lt;/code&gt;&lt;/a&gt; all belong to the &lt;strong&gt;server&lt;/strong&gt; at startup. A client cannot add them later.&lt;/li&gt;
    &lt;li&gt;The client becomes a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pip install&lt;/code&gt; with no Spark, no Java and no Hudi jar. Everything below was driven from one.&lt;/li&gt;
    &lt;li&gt;Almost the whole surface works: COW and MOR, partitioned tables, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INSERT&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DELETE&lt;/code&gt;, incremental queries, time travel, 12 stored procedures and the metadata table functions.&lt;/li&gt;
    &lt;li&gt;Four things do not. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RENAME COLUMN&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DROP COLUMN&lt;/code&gt; are rejected outright, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_instant&lt;/code&gt; only unwinds the newest instant, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi_table_changes&lt;/code&gt; in CDC mode needs a CDC-enabled table.&lt;/li&gt;
    &lt;li&gt;The trap that costs the most time is not a Connect issue at all: &lt;strong&gt;partitioning moves the partition column to the end of the schema&lt;/strong&gt;, so a positional &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INSERT&lt;/code&gt; fails with an error that blames the wrong column.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;Hudi is not part of Spark. It arrives as a bundle jar, installs a Catalyst
extension, registers a catalog and needs a specific serializer. All four are
session-level concerns, and Spark Connect moves the session to the far side of a
gRPC connection.&lt;/p&gt;

&lt;p&gt;So the practical question is not whether Hudi works through Spark Connect. It is
which half of your setup goes where, and what the failures look like when a piece
lands on the wrong side. This post answers that by running the whole thing: one
Connect server, one client with nothing installed, and every Hudi operation worth
using, including the ones that fail.&lt;/p&gt;

&lt;p&gt;Every result quoted here came out of that run. Where something failed I say so,
and where a failure turned out to be my own mistake rather than a limitation I
say that too, because the error messages in two of those cases point somewhere
misleading.&lt;/p&gt;

&lt;h2 id=&quot;architecture-what-spark-connect-changes-for-hudi&quot;&gt;Architecture: what Spark Connect changes for Hudi&lt;/h2&gt;

&lt;p&gt;In a classic session your process owns the SparkSession, the analyzer, the
catalog and the scheduler. Hudi’s extension and catalog are installed into
&lt;em&gt;your&lt;/em&gt; session, by your own &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-submit&lt;/code&gt; line.&lt;/p&gt;

&lt;p&gt;Spark Connect cuts that in half:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph C[&quot;client, your laptop&quot;]
    A[&quot;DataFrame and SQL calls&quot;] --&amp;gt; B[&quot;unresolved plan&amp;lt;br/&amp;gt;as protobuf&quot;]
  end
  B --&amp;gt;|&quot;gRPC, port 15002&quot;| S
  subgraph S[&quot;Spark Connect server, the driver&quot;]
    D[&quot;analyzer plus HoodieCatalog&quot;] --&amp;gt; E[&quot;Catalyst optimiser plus&amp;lt;br/&amp;gt;HoodieSparkSessionExtension&quot;]
    E --&amp;gt; F[&quot;physical plan&quot;]
  end
  F --&amp;gt; X[&quot;executors, Hudi bundle on the classpath&quot;]
  X --&amp;gt;|&quot;Arrow batches&quot;| A
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The client cannot resolve &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;default.trips_cow&lt;/code&gt; because it has no catalog and no
idea what a Hudi table is. It sends the operation and lets the server work it
out. Everything below follows from that single split.&lt;/p&gt;

&lt;p&gt;Hudi plugs into Spark at four points, and Connect puts all four on the server:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What Hudi needs&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Config&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The bundle jar&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Contains the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataSource&lt;/code&gt;, the catalog and the procedures&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;A Catalyst extension&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Provides &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CALL&lt;/code&gt; and the table-valued functions&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;A catalog&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.catalog.spark_catalog&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Lets &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;USING hudi&lt;/code&gt; tables live in the default catalog&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Kryo serialization&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.serializer&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Hudi’s internal types do not survive the Java serializer&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph CL[&quot;client&quot;]
    P[&quot;pip install pyspark&amp;lt;br/&amp;gt;no Java, no Hudi jar&quot;]
  end
  subgraph SV[&quot;server, fixed at startup&quot;]
    J[&quot;hudi-spark4.0-bundle&quot;]
    K[&quot;HoodieSparkSessionExtension&quot;]
    L[&quot;HoodieCatalog over spark_catalog&quot;]
    M[&quot;KryoSerializer&quot;]
  end
  P --&amp;gt;|&quot;SQL and DataFrame&quot;| SV
  J --- K --- L --- M
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;None of the four can be set by a client. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.conf.set&lt;/code&gt; reaches the server for
runtime SQL configs, but &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt; is read once while the session is
built, before any client connects. A client can &lt;strong&gt;read&lt;/strong&gt; it, which is the fastest
way to tell a broken server from a broken query.&lt;/p&gt;

&lt;h2 id=&quot;starting-a-server-that-speaks-hudi&quot;&gt;Starting a server that speaks Hudi&lt;/h2&gt;

&lt;p&gt;Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt; when the host can reach Maven Central, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt; with
pre-downloaded files when it cannot. Both were run for this post.&lt;/p&gt;

&lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nv&quot;&gt;$SPARK_HOME&lt;/span&gt;/sbin/start-connect-server.sh &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--packages&lt;/span&gt; org.apache.hudi:hudi-spark4.0-bundle_2.13:1.2.0 &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.extensions&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;org.apache.spark.sql.hudi.HoodieSparkSessionExtension &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.serializer&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;org.apache.spark.serializer.KryoSerializer &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.catalog.spark_catalog&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;org.apache.spark.sql.hudi.catalog.HoodieCatalog &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.warehouse.dir&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;/tmp/warehouse/spark &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--master&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;local[4]&quot;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The coordinate encodes two compatibilities at once, and both are hard failures
rather than warnings:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Coordinate part&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Must match&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi-spark4.0-bundle&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Your Spark minor version, 4.0 here&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_2.13&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Spark’s Scala binary version&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Spark 4 is Scala 2.13 only, so a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_2.12&lt;/code&gt; bundle never loads.&lt;/p&gt;

&lt;h3 id=&quot;the-extension-class-name-which-is-the-usual-mistake&quot;&gt;The extension class name, which is the usual mistake&lt;/h3&gt;

&lt;p&gt;Hudi’s session extension is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.sql.hudi.HoodieSparkSessionExtension&lt;/code&gt;. It lives under
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.sql&lt;/code&gt;, &lt;strong&gt;not&lt;/strong&gt; under &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi&lt;/code&gt;, which is where most
people reach for it first, and where I reached for it first.&lt;/p&gt;

&lt;p&gt;Getting it wrong does not stop the server. That is what makes it expensive: the
server starts, reports healthy, and then every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CALL&lt;/code&gt; fails later
with an error about syntax or a missing table. The only evidence is a warning in
the server log at startup:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;WARN SparkSession: Cannot use org.apache.hudi.HoodieSparkSessionExtension to configure session extensions.
java.lang.ClassNotFoundException: org.apache.hudi.HoodieSparkSessionExtension
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Two habits remove this whole class of problem. Read the name out of the jar
rather than from memory:&lt;/p&gt;

&lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;unzip &lt;span class=&quot;nt&quot;&gt;-l&lt;/span&gt; hudi-spark4.0-bundle_2.13-1.2.0.jar | &lt;span class=&quot;nb&quot;&gt;grep &lt;/span&gt;SessionExtension
&lt;span class=&quot;c&quot;&gt;#   11028  org/apache/spark/sql/hudi/HoodieSparkSessionExtension.class&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;And confirm from the client that the server loaded it:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.extensions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# org.apache.spark.sql.hudi.HoodieSparkSessionExtension
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That one line separates “my SQL is wrong” from “the server is misconfigured”, and
it is the first thing to run when a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; is rejected.&lt;/p&gt;

&lt;h2 id=&quot;connecting-the-client&quot;&gt;Connecting the client&lt;/h2&gt;

&lt;p&gt;The client needs &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pyspark&lt;/code&gt; and gRPC. No Java, no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SPARK_HOME&lt;/code&gt;, no Hudi jar. Pin
it to the version your server runs:&lt;/p&gt;

&lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;pip &lt;span class=&quot;nb&quot;&gt;install&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;pyspark[connect]==4.0.2&quot;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;remote&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sc://localhost:15002&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;version&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.version&lt;/code&gt; reports the &lt;strong&gt;server’s&lt;/strong&gt; version, so it tells you nothing about
your client. Check the client separately with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pyspark.__version__&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;The pin matters more than it looks, and a bare &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pip install &quot;pyspark[connect]&quot;&lt;/code&gt;
is the wrong instruction. It installs whatever is newest on PyPI, and a client
newer than the server fails on the first &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;SparkNoSuchElementException: [SQL_CONF_NOT_FOUND] The SQL config
&quot;spark.sql.session.localRelationSizeLimit&quot; cannot be found. SQLSTATE: 42K0I
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The newer client asks the older server for a SQL config that does not exist
there. Every SQL statement in this post still works in that configuration, so
the failure looks arbitrary: DDL, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;, procedures and metadata queries all
succeed, and only the paths that build a local relation break.&lt;/p&gt;

&lt;p&gt;The gap is not symmetric. An &lt;strong&gt;older&lt;/strong&gt; client against a newer server was fine: a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;3.5.9&lt;/code&gt; client drove the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.0.2&lt;/code&gt; server through every example here,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt; included. A &lt;strong&gt;newer&lt;/strong&gt; client against an older server was not.
Match the versions, and when you cannot, be the older side.&lt;/p&gt;

&lt;h2 id=&quot;copy-on-write-tables-the-full-dml-surface&quot;&gt;Copy-on-write tables, the full DML surface&lt;/h2&gt;

&lt;p&gt;Hudi installs over the default catalog rather than adding a named one, so tables
are ordinary two-part names. The Hudi specifics go in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TBLPROPERTIES&lt;/code&gt;.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  CREATE TABLE default.trips_cow (
    trip_id    BIGINT,
    rider_id   BIGINT,
    city       STRING,
    fare       DECIMAL(10,2),
    started_at TIMESTAMP)
  USING hudi
  PARTITIONED BY (city)
  TBLPROPERTIES (
    primaryKey      = &apos;trip_id&apos;,
    preCombineField = &apos;started_at&apos;,
    type            = &apos;cow&apos;)
  LOCATION &apos;/tmp/warehouse/trips_cow&apos;&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;primaryKey&lt;/code&gt; is what makes an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; possible at all, since Hudi
needs a record key to find the row it is replacing. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;preCombineField&lt;/code&gt; decides
which version wins when two records share a key. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;type = &apos;cow&apos;&lt;/code&gt; selects
&lt;strong&gt;copy-on-write&lt;/strong&gt;, where an update rewrites the whole base file, as opposed to
merge-on-read, which appends a delta log and merges at query time.&lt;/p&gt;

&lt;h3 id=&quot;the-partitioning-trap&quot;&gt;The partitioning trap&lt;/h3&gt;

&lt;p&gt;Now the thing that will cost you an afternoon. This insert looks obviously
correct and fails:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  INSERT INTO default.trips_cow VALUES
    (1, 101, &apos;Hyderabad&apos;, 245.50, TIMESTAMP &apos;2026-09-01 08:15:00&apos;)&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AnalysisException: [INCOMPATIBLE_DATA_FOR_TABLE.CANNOT_SAFELY_CAST]
Cannot write incompatible data for the table `spark_catalog`.`default`.`trips_cow`:
Cannot safely cast `fare` &quot;STRING&quot; to &quot;DECIMAL(10,2)&quot;. SQLSTATE: KD000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The error names &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fare&lt;/code&gt;, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fare&lt;/code&gt; is fine. Look at the schema Hudi actually
created:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;f&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;table&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;default.trips_cow&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;schema&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fields&lt;/span&gt;
 &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;not&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;startswith&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;_hoodie&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)]&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;[&apos;trip_id&apos;, &apos;rider_id&apos;, &apos;fare&apos;, &apos;started_at&apos;, &apos;city&apos;]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;Partitioning moves the partition column to the end of the schema.&lt;/strong&gt; The table
was declared with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city&lt;/code&gt; third, and it is stored fifth. A positional
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INSERT ... VALUES&lt;/code&gt; maps by position, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;&apos;Hyderabad&apos;&lt;/code&gt; lands on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fare&lt;/code&gt;, and the
cast error is a truthful report of a problem one column to the left of where you
are looking.&lt;/p&gt;

&lt;p&gt;Two fixes. Name the columns, which is worth doing on any Hudi table whether it is
partitioned or not:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  INSERT INTO default.trips_cow (trip_id, rider_id, city, fare, started_at) VALUES
    (1, 101, &apos;Hyderabad&apos;, 245.50, TIMESTAMP &apos;2026-09-01 08:15:00&apos;),
    (2, 102, &apos;Bengaluru&apos;, 180.00, TIMESTAMP &apos;2026-09-01 09:30:00&apos;),
    (3, 103, &apos;Hyderabad&apos;, 320.75, TIMESTAMP &apos;2026-09-02 07:45:00&apos;)&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Or supply the values in Hudi’s stored order, with the partition column last.
Both were verified; the explicit column list is the one to use, because it
survives a later &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ADD COLUMNS&lt;/code&gt;.&lt;/p&gt;

&lt;h3 id=&quot;update-merge-and-delete&quot;&gt;Update, merge and delete&lt;/h3&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;UPDATE default.trips_cow SET fare = 250.00 WHERE trip_id = 1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  MERGE INTO default.trips_cow t
  USING (SELECT 4L AS trip_id, 104L AS rider_id, &apos;Pune&apos; AS city,
                CAST(99.00 AS DECIMAL(10,2)) AS fare,
                TIMESTAMP &apos;2026-09-03 10:00:00&apos; AS started_at) s
    ON t.trip_id = s.trip_id
  WHEN MATCHED     THEN UPDATE SET *
  WHEN NOT MATCHED THEN INSERT *&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;DELETE FROM default.trips_cow WHERE trip_id = 2&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;All three are extension-provided syntax, so they are also a working test that the
server loaded &lt;a href=&quot;https://github.com/apache/hudi/blob/master/hudi-spark-datasource/hudi-spark/src/main/scala/org/apache/spark/sql/hudi/HoodieSparkSessionExtension.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieSparkSessionExtension&lt;/code&gt;&lt;/a&gt;. Results from the sequence above:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;count after INSERT                      3
fare for trip_id 1 after UPDATE         250.00
count after MERGE (Pune inserted)       4
count after DELETE                      3
count WHERE city = &apos;Hyderabad&apos;          2
SHOW PARTITIONS   city=Bengaluru, city=Hyderabad, city=Pune
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SHOW PARTITIONS&lt;/code&gt; still listing &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city=Bengaluru&lt;/code&gt; after its only row was deleted
is correct: the partition directory and its metadata remain until cleaning
removes them.&lt;/p&gt;

&lt;p&gt;The five Hudi bookkeeping columns come through the client untouched, which is a
quick way to confirm you are reading a real Hudi table and not a directory of
Parquet:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;table&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;default.trips_cow&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;columns&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;startswith&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;_hoodie&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)]&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;[&apos;_hoodie_commit_time&apos;, &apos;_hoodie_commit_seqno&apos;, &apos;_hoodie_record_key&apos;,
 &apos;_hoodie_partition_path&apos;, &apos;_hoodie_file_name&apos;]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;h2 id=&quot;schema-evolution-two-of-four-operations&quot;&gt;Schema evolution: two of four operations&lt;/h2&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ADD COLUMNS&lt;/code&gt; works, and old rows read back &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NULL&lt;/code&gt; for the new column as they
should:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;ALTER TABLE default.trips_cow ADD COLUMNS (pay_method STRING)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  INSERT INTO default.trips_cow (trip_id, rider_id, city, fare, started_at, pay_method)
  VALUES (5, 105, &apos;Chennai&apos;, 75.25, TIMESTAMP &apos;2026-09-04 11:00:00&apos;, &apos;upi&apos;)&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;pay_method for trip_id 5           upi
rows WHERE pay_method IS NULL      3
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Widening a column type with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ALTER COLUMN ... TYPE&lt;/code&gt; is accepted too. The other
two are not:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;ALTER TABLE default.trips_cow RENAME COLUMN pay_method TO pm&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;ALTER TABLE default.trips_cow DROP COLUMN pay_method&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AnalysisException: [UNSUPPORTED_FEATURE.TABLE_OPERATION] The feature is not supported:
Table `spark_catalog`.`default`.`trips_cow` does not support RENAME COLUMN.

AnalysisException: [UNSUPPORTED_FEATURE.TABLE_OPERATION] The feature is not supported:
Table `spark_catalog`.`default`.`trips_cow` does not support DROP COLUMN
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Operation&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Through Spark Connect&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ADD COLUMNS&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Works&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ALTER COLUMN ... TYPE&lt;/code&gt; (widening)&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Works&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RENAME COLUMN&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UNSUPPORTED_FEATURE.TABLE_OPERATION&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DROP COLUMN&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UNSUPPORTED_FEATURE.TABLE_OPERATION&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Plan schema changes as additive. A rename means writing a new column and
backfilling it, so decide column names before the table has data you care about.&lt;/p&gt;

&lt;h2 id=&quot;merge-on-read-and-what-the-two-query-types-show&quot;&gt;Merge-on-read, and what the two query types show&lt;/h2&gt;

&lt;p&gt;Merge-on-read is where Hudi’s read path becomes visible, and the divergence is
easy to demonstrate through a thin client:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  CREATE TABLE default.trips_mor (
    trip_id BIGINT, rider_id BIGINT, city STRING,
    fare DECIMAL(10,2), started_at TIMESTAMP)
  USING hudi
  TBLPROPERTIES (primaryKey = &apos;trip_id&apos;, preCombineField = &apos;started_at&apos;, type = &apos;mor&apos;)
  LOCATION &apos;/tmp/warehouse/trips_mor&apos;&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&quot;&quot;
  INSERT INTO default.trips_mor VALUES
    (1, 101, &apos;Hyderabad&apos;, 245.50, TIMESTAMP &apos;2026-09-01 08:15:00&apos;),
    (2, 102, &apos;Bengaluru&apos;, 180.00, TIMESTAMP &apos;2026-09-01 09:30:00&apos;)&quot;&quot;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;UPDATE default.trips_mor SET fare = 999.99 WHERE trip_id = 1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Now read the same row two ways:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# snapshot, the default: base files merged with the delta logs
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT fare FROM default.trips_mor WHERE trip_id = 1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;][&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# read-optimised: base files only, ignoring the logs
&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hudi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hoodie.datasource.query.type&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;read_optimized&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/warehouse/trips_mor&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id = 1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;][&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;snapshot         999.99
read_optimized   245.50
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That gap is the whole merge-on-read trade, measured: the snapshot read pays a
merge to be correct, the read-optimised read is cheaper and stale until
compaction. Both paths work identically through Connect.&lt;/p&gt;

&lt;p&gt;Compaction is where expectations need managing. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;run_compaction&lt;/code&gt; is not a
command that compacts on demand:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CALL run_compaction(op =&amp;gt; &apos;scheduleandexecute&apos;, table =&amp;gt; &apos;default.trips_mor&apos;)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;0 rows returned, and CALL show_compaction(...) reports 0 pending compactions
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Nothing was scheduled because the delta-commit threshold was not met, which on
these defaults takes more delta commits than two updates produce. Setting
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.compact.inline.max.delta.commits&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1&lt;/code&gt; at table creation went the other
way: compaction then ran inline on the write itself, and the read-optimised query
returned &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;999.99&lt;/code&gt; immediately, with still nothing left for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;run_compaction&lt;/code&gt; to
do. Either way the procedure returning zero rows is not a failure, and the way to
verify compaction is to watch the read-optimised query catch up, not to read the
procedure’s return value.&lt;/p&gt;

&lt;h2 id=&quot;incremental-queries-and-time-travel&quot;&gt;Incremental queries and time travel&lt;/h2&gt;

&lt;p&gt;This is the Hudi capability with no direct equivalent in the other formats, and
it survives the trip through gRPC intact. The DataFrame writer path, where the
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.*&lt;/code&gt; options are explicit:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;decimal&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Decimal&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;functions&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;7&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;107&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;Kochi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Decimal&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;55.00&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-06 10:00:00&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;8&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;108&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;Kochi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Decimal&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;65.00&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-06 11:00:00&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt;
        &lt;span class=&quot;s&quot;&gt;&quot;trip_id bigint, rider_id bigint, city string, &quot;&lt;/span&gt;
        &lt;span class=&quot;s&quot;&gt;&quot;fare decimal(10,2), started_at string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;to_timestamp&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;options&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;hoodie.table.name&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;trips_df&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;hoodie.datasource.write.recordkey.field&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;hoodie.datasource.write.precombine.field&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;

&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hudi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;options&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;options&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hoodie.datasource.write.operation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;bulk_insert&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;})&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;save&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/warehouse/trips_df&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The three write operations behave as their names promise, which is worth checking
once rather than assuming. First an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;upsert&lt;/code&gt; of a row that already exists:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;changed&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;7&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;107&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;Kochi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Decimal&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;77.77&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-06 12:00:00&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt;
        &lt;span class=&quot;s&quot;&gt;&quot;trip_id bigint, rider_id bigint, city string, &quot;&lt;/span&gt;
        &lt;span class=&quot;s&quot;&gt;&quot;fare decimal(10,2), started_at string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;to_timestamp&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# upsert matches on the record key, so the row count does not grow
&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;changed&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hudi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;options&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;options&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hoodie.datasource.write.operation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;upsert&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;})&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;save&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/warehouse/trips_df&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Then the same two original rows again, this time with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;insert&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# insert does NOT match on the record key
&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hudi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;options&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;options&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hoodie.datasource.write.operation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;insert&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;})&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;save&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/warehouse/trips_df&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;after bulk_insert of 2 rows              2 rows
after upsert of trip_id 7                2 rows, fare now 77.77
after insert of the same 2 rows again    4 rows
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The last line is the one to notice. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;insert&lt;/code&gt; does not deduplicate on the record
key, so re-sending rows you already have doubles them. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;upsert&lt;/code&gt; is what most
pipelines want, and it is not what every path defaults to.&lt;/p&gt;

&lt;p&gt;Time travel reads a table as of an instant:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;commits&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hudi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
           &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/warehouse/trips_df&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
           &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;_hoodie_commit_time&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;distinct&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
           &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orderBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;_hoodie_commit_time&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()]&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hudi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;as.of.instant&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;commits&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt; \
     &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/warehouse/trips_df&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;commit times            [&apos;20260918093803909&apos;, &apos;20260918093804944&apos;, &apos;20260918093805997&apos;]
as.of.instant, first    2
current                 4
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Two rows against four is time travel working over a gRPC connection. And the
incremental read, which returns only what changed after an instant:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hudi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hoodie.datasource.query.type&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;incremental&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hoodie.datasource.read.begin.instanttime&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;0&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;/tmp/warehouse/trips_df&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Passing &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0&lt;/code&gt; means “everything”, which is how you check the plumbing before
wiring a real watermark. In production you store the last processed
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_hoodie_commit_time&lt;/code&gt; and pass that instead. The commit times are timestamps, so
yours will differ from the ones above; every other number here is reproducible
from a clean warehouse.&lt;/p&gt;

&lt;h2 id=&quot;procedures-and-metadata-over-grpc&quot;&gt;Procedures and metadata, over gRPC&lt;/h2&gt;

&lt;p&gt;People assume the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CALL&lt;/code&gt; interface will not survive Spark Connect. It does. These
all ran from the thin client:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Procedure&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What it returned&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show_commits&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The instant timeline, newest first&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show_table_properties&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.*&lt;/code&gt; properties as stored&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show_commit_files&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Files touched by one instant&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show_fsview_latest&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The latest file slices for a partition&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;stats_file_sizes&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;File-size distribution&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;stats_wa&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Write-amplification statistics&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;run_clean&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Cleans older file versions&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;run_clustering&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Reorganises data files&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;run_compaction&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Schedules or runs MOR compaction&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;create_savepoint&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show_savepoints&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Savepoint management&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_instant&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Unwinds a commit, with the limit below&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CALL show_commits(table =&amp;gt; &apos;default.trips_cow&apos;, limit =&amp;gt; 10)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CALL stats_wa(table =&amp;gt; &apos;default.trips_cow&apos;)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CALL run_clean(table =&amp;gt; &apos;default.trips_cow&apos;)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The table-valued functions work as well, with one argument requirement that is
easy to miss:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) FROM hudi_metadata(&apos;default.trips_cow&apos;)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) FROM hudi_table_changes(&apos;default.trips_cow&apos;,&apos;latest_state&apos;,&apos;earliest&apos;)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT count(*) FROM hudi_filesystem_view(&apos;default.trips_cow&apos;,&apos;city=Hyderabad&apos;)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi_filesystem_view&lt;/code&gt; takes a &lt;strong&gt;partition&lt;/strong&gt; as its second argument. Called with
only a table name it returns zero rows rather than an error, which reads like an
empty table:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;hudi_filesystem_view(&apos;default.trips_cow&apos;)                    0
hudi_filesystem_view(&apos;default.trips_cow&apos;,&apos;city=Hyderabad&apos;)   3
hudi_filesystem_view(&apos;default.trips_mor&apos;,&apos;&apos;)                 1
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;For a non-partitioned table pass the empty string. And &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi_table_changes&lt;/code&gt; in
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cdc&lt;/code&gt; mode needs a table created with CDC enabled:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;IllegalArgumentException: It isn&apos;t a CDC hudi table on file:/tmp/warehouse/trips_cow
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is a table-property problem, not a Connect one. Enable
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.table.cdc.enabled&lt;/code&gt; at creation if you want the CDC feed.&lt;/p&gt;

&lt;h3 id=&quot;rollback_to_instant-only-unwinds-the-newest-instant&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_instant&lt;/code&gt; only unwinds the newest instant&lt;/h3&gt;

&lt;p&gt;This one deserves its own note because the error is opaque and my first
explanation for it was wrong. Rolling back the newest instant works:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;instants&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;sorted&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CALL show_commits(table =&amp;gt; &apos;default.trips_cow&apos;, limit =&amp;gt; 30)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CALL rollback_to_instant(table =&amp;gt; &apos;default.trips_cow&apos;, &quot;&lt;/span&gt;
          &lt;span class=&quot;s&quot;&gt;&quot;instant_time =&amp;gt; &apos;%s&apos;)&quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;instants&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;-&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# [Row(rollback_result=True)], and the row count drops by that commit
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Rolling back an older instant fails:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;UnknownException: (org.apache.hudi.exception.HoodieRollbackException)
Failed to rollback file:///tmp/warehouse/trips commits 20260918094116830
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;I first assumed a savepoint on the table was blocking it, and tested that:
creating a savepoint and then rolling back the newest instant still succeeded, so
the savepoint was not the cause. The actual rule is simpler. Rollback unwinds the
tip of the timeline, one commit at a time. It is not “restore to this point”, and
the way to go further back is repeated rollbacks or a savepoint plus restore.&lt;/p&gt;

&lt;p&gt;The practical mistake this invites is an ordering one. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show_commits&lt;/code&gt; returns
instants newest first, so the first row it hands you is the newest, while sorting
those instants ascending puts the oldest first. Sorting a list of Hudi instants
and passing the front of it gives &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_instant&lt;/code&gt; exactly the argument it
cannot process, which is how I produced the failure above in the first place.&lt;/p&gt;

&lt;h2 id=&quot;what-the-thin-client-cannot-do&quot;&gt;What the thin client cannot do&lt;/h2&gt;

&lt;p&gt;The limitations are narrow and all follow from the client not having a JVM.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;PySparkAttributeError: [JVM_ATTRIBUTE_NOT_SUPPORTED] Attribute `sparkContext` is not
supported in Spark Connect as it depends on the JVM.
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;PySparkNotImplementedError: [NOT_IMPLEMENTED] rdd is not implemented.
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Neither matters much for table work, since Hudi is driven through SQL and the
DataFrame API anyway. What does bite is subtler. &lt;strong&gt;A Python &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;float&lt;/code&gt; for a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;decimal&lt;/code&gt; column fails with an empty error&lt;/strong&gt;, where a classic session coerces it:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;9&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;109&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;Kochi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;75.25&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-05 12:00:00&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;trip_id bigint, rider_id bigint, city string, &quot;&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;fare decimal(10,2), started_at string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AssertionError
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That is the entire message. The traceback points at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;convert_decimal&lt;/code&gt; in
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pyspark/sql/conversion.py&lt;/code&gt;, which does
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;assert isinstance(value, decimal.Decimal)&lt;/code&gt;. Pass &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Decimal(&quot;75.25&quot;)&lt;/code&gt; and it
works. Any Hudi table with a money column hits this on the first
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt;.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Classic session&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Spark Connect client&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sparkContext&lt;/code&gt; available&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Raises &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;JVM_ATTRIBUTE_NOT_SUPPORTED&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.rdd&lt;/code&gt; available&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Raises &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NOT_IMPLEMENTED&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;float&lt;/code&gt; coerced into a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;decimal&lt;/code&gt; column&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Bare &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AssertionError&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Hudi jar and extension per submit&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Server-side only, fixed at startup&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;SQL, DML, procedures, TVFs, incremental reads&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;All work unchanged&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“One server can serve Hudi and Iceberg with no interference.”&lt;/strong&gt; Nearly, and
the exception is measurable. Loading Hudi’s session extension alongside
Iceberg’s breaks &lt;strong&gt;Iceberg’s&lt;/strong&gt; SQL time travel, which then fails with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;[INTERNAL_ERROR] Found the unresolved operator: &apos;TimeTravelRelation&lt;/code&gt;. It is
one-directional: Hudi’s own DML, procedures, incremental reads and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;as.of.instant&lt;/code&gt; all keep working.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“The extension class lives under the project’s own package.”&lt;/strong&gt; Hudi’s does
not. It is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.sql.hudi.HoodieSparkSessionExtension&lt;/code&gt;, under
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.sql&lt;/code&gt;. Getting it wrong does not stop the server: it starts,
logs one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Cannot use&lt;/code&gt; warning, and then every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; fails later for reasons
that look unrelated.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;”&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt; is not enough for a session extension, it needs the driver
classpath.”&lt;/strong&gt; It is enough. That conclusion came from a wrong class name, not a
classpath problem, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt; alone loads both extensions correctly once the
name is right.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“A bare &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AssertionError&lt;/code&gt; from the client is a client bug.”&lt;/strong&gt; It is usually a
type error. The Connect client asserts the exact type during Arrow conversion,
so a Python &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;float&lt;/code&gt; in a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;decimal&lt;/code&gt; column fails with an empty message where a
classic session would have coerced it.&lt;/p&gt;

&lt;h2 id=&quot;production-notes&quot;&gt;Production notes&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Treat the server config as a contract.&lt;/strong&gt; The jar, the extension, the catalog and Kryo are fixed at startup, so a change means a restart. Nobody’s client can work around a server that was started wrong.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Check the extension before debugging SQL.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.conf.get(&quot;spark.sql.extensions&quot;)&lt;/code&gt; from the client, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;grep &quot;Cannot use&quot;&lt;/code&gt; on the server log, resolve most “MERGE is not supported” reports.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Always name columns in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INSERT&lt;/code&gt;.&lt;/strong&gt; It costs one line and removes the partition-reordering trap permanently, including after a later &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ADD COLUMNS&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Design schema changes as additive.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RENAME COLUMN&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DROP COLUMN&lt;/code&gt; are rejected, so a rename is a new column plus a backfill.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;upsert&lt;/code&gt;, not &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;insert&lt;/code&gt;, unless you mean to duplicate.&lt;/strong&gt; The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;insert&lt;/code&gt; operation does not deduplicate on the record key.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Do not judge compaction by the procedure’s return value.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;run_compaction&lt;/code&gt; returning zero rows usually means the delta-commit threshold was not met. Watch the read-optimised query instead.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Pass &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Decimal&lt;/code&gt; for decimal columns&lt;/strong&gt; in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt;, and prefer &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CAST(... AS DECIMAL(10,2))&lt;/code&gt; inside SQL.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Keep Kryo set even on a server you think is Iceberg-only&lt;/strong&gt;, because one shared server usually ends up serving both.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Think twice before adding Hudi’s extension to a server that serves Iceberg.&lt;/strong&gt; It breaks Iceberg’s SQL time travel, one-directionally, and nothing warns you at startup.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Does the client need the Hudi jar?&lt;/strong&gt;
No, and that is the main reason to use Connect for Hudi work. The client sends a
plan naming a table and the server resolves it. A virtualenv with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;pyspark&lt;/code&gt; is
the whole client-side dependency, which also removes the jar-version skew that
usually makes onboarding painful.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Can one server serve Hudi and Iceberg together?&lt;/strong&gt;
Mostly, and the cost falls entirely on Iceberg. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.extensions&lt;/code&gt; takes a
comma-separated list, Hudi takes over &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark_catalog&lt;/code&gt; while Iceberg gets a named
catalog, and both formats’ DDL and DML work. But loading Hudi’s extension
alongside Iceberg’s breaks &lt;strong&gt;Iceberg’s&lt;/strong&gt; SQL time travel, which then fails with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;[INTERNAL_ERROR] Found the unresolved operator: &apos;TimeTravelRelation&lt;/code&gt;. Hudi is
unaffected in that arrangement: its own DML, procedures, incremental reads and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;as.of.instant&lt;/code&gt; time travel all keep working. The measurement and the workaround
are in &lt;a href=&quot;/SparkConnectIceberg/&quot;&gt;Apache Iceberg through Spark Connect&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why does my &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; fail with a parse error?&lt;/strong&gt;
Because the session extension is not loaded on the server, from either a wrong
class name or a jar missing from the server classpath. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UPDATE&lt;/code&gt; on
Hudi are extension-provided syntax, so without it the statement is not merely
unsupported, it is unparseable.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Can I set &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.*&lt;/code&gt; options per job from the client?&lt;/strong&gt;
Write-time &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.*&lt;/code&gt; options travel fine, because they are passed with the write
rather than baked into the session. What you cannot change from a client is the
session-level set: the extension, the catalog and the serializer.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Is the partition-column reordering a Spark Connect bug?&lt;/strong&gt;
No. It is how Hudi stores a partitioned table, and a classic session behaves the
same way. Connect only makes it more visible, because the error arrives without
a local stack trace to poke at.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Do client and server versions have to match?&lt;/strong&gt;
Match them. An older client is tolerated: a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;3.5.9&lt;/code&gt; client drove the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.0.2&lt;/code&gt;
server through every example here. A newer one is not, and the failure is
specific rather than general. A pip-installed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.2.0&lt;/code&gt; client ran every SQL
statement against the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.0.2&lt;/code&gt; server and then failed on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;createDataFrame&lt;/code&gt; with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SQL_CONF_NOT_FOUND&lt;/code&gt; for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.session.localRelationSizeLimit&lt;/code&gt;, because it
expects a config the older server does not have. That is why the install line
above is pinned.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;The useful reframing is that Spark Connect forces a separation which was always
there. Hudi is infrastructure: a jar, a Catalyst extension, a catalog and a
serializer. Your query is not. Classic Spark let you blur the two by shipping
jars with every submit, and most &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt; lines in most runbooks are that
blur made permanent.&lt;/p&gt;

&lt;p&gt;Connect makes you decide once, on the server, and then hands every client a clean
API with nothing to install. The cost is exactly the flexibility you were abusing:
no per-job jars, no per-job extensions, no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sparkContext&lt;/code&gt; escape hatch. For a
team sharing a Hudi lakehouse, that is a good trade.&lt;/p&gt;

&lt;p&gt;What is worth carrying away is how few of the failures were about Spark Connect.
An extension class under a package that looks wrong but is not, a partition
column silently moved to the end of the schema, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;insert&lt;/code&gt; where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;upsert&lt;/code&gt; was
meant, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rollback_to_instant&lt;/code&gt; that only unwinds the tip, and a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;float&lt;/code&gt; where a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Decimal&lt;/code&gt; belongs. Every one of those behaves identically in a classic session.
Reproduce a failure locally before blaming the transport, which is the habit that
turned two of my own confident wrong answers here into the right ones.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/spark-connect-overview.html&quot;&gt;Spark Connect overview&lt;/a&gt; for the architecture and the client APIs&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://hudi.apache.org/docs/sql_ddl/&quot;&gt;Hudi Spark SQL DDL&lt;/a&gt; and &lt;a href=&quot;https://hudi.apache.org/docs/procedures/&quot;&gt;SQL procedures&lt;/a&gt; for table properties and the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CALL&lt;/code&gt; surface&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://hudi.apache.org/docs/configurations/&quot;&gt;Hudi configuration reference&lt;/a&gt; for the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.*&lt;/code&gt; write and read options used above&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/python/pyspark/sql/conversion.py&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;conversion.py&lt;/code&gt;&lt;/a&gt;, whose &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;convert_decimal&lt;/code&gt; is the assertion behind the empty &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AssertionError&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheHudiArchitecture/&quot;&gt;Apache Hudi architecture&lt;/a&gt; for the timeline, file slices and compaction machinery underneath all of this&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Hudi, Apache Iceberg, Apache Hive, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="SparkConnect" /><category term="Hudi" /><category term="Lakehouse" /><summary type="html">Architecture: what Spark Connect changes for Hudi Starting a server that speaks Hudi The extension class name, which is the usual mistake Connecting the client Copy-on-write tables, the full DML surface The partitioning trap Update, merge and delete Schema evolution: two of four operations Merge-on-read, and what the two query types show Incremental queries and time travel Procedures and metadata, over gRPC rollback_to_instant only unwinds the newest instant What the thin client cannot do Common misconceptions Production notes Frequently asked questions Conclusion References Trademarks TL;DR Spark Connect moves planning to a server, so the Hudi bundle, spark.sql.extensions, the Kryo serializer and HoodieCatalog all belong to the server at startup. A client cannot add them later. The client becomes a pip install with no Spark, no Java and no Hudi jar. Everything below was driven from one. Almost the whole surface works: COW and MOR, partitioned tables, INSERT, UPDATE, MERGE, DELETE, incremental queries, time travel, 12 stored procedures and the metadata table functions. Four things do not. RENAME COLUMN and DROP COLUMN are rejected outright, rollback_to_instant only unwinds the newest instant, and hudi_table_changes in CDC mode needs a CDC-enabled table. The trap that costs the most time is not a Connect issue at all: partitioning moves the partition column to the end of the schema, so a positional INSERT fails with an error that blames the wrong column. Hudi is not part of Spark. It arrives as a bundle jar, installs a Catalyst extension, registers a catalog and needs a specific serializer. All four are session-level concerns, and Spark Connect moves the session to the far side of a gRPC connection. So the practical question is not whether Hudi works through Spark Connect. It is which half of your setup goes where, and what the failures look like when a piece lands on the wrong side. This post answers that by running the whole thing: one Connect server, one client with nothing installed, and every Hudi operation worth using, including the ones that fail. Every result quoted here came out of that run. Where something failed I say so, and where a failure turned out to be my own mistake rather than a limitation I say that too, because the error messages in two of those cases point somewhere misleading. Architecture: what Spark Connect changes for Hudi In a classic session your process owns the SparkSession, the analyzer, the catalog and the scheduler. Hudi’s extension and catalog are installed into your session, by your own spark-submit line. Spark Connect cuts that in half: flowchart TB subgraph C[&quot;client, your laptop&quot;] A[&quot;DataFrame and SQL calls&quot;] --&amp;gt; B[&quot;unresolved plan&amp;lt;br/&amp;gt;as protobuf&quot;] end B --&amp;gt;|&quot;gRPC, port 15002&quot;| S subgraph S[&quot;Spark Connect server, the driver&quot;] D[&quot;analyzer plus HoodieCatalog&quot;] --&amp;gt; E[&quot;Catalyst optimiser plus&amp;lt;br/&amp;gt;HoodieSparkSessionExtension&quot;] E --&amp;gt; F[&quot;physical plan&quot;] end F --&amp;gt; X[&quot;executors, Hudi bundle on the classpath&quot;] X --&amp;gt;|&quot;Arrow batches&quot;| A The client cannot resolve default.trips_cow because it has no catalog and no idea what a Hudi table is. It sends the operation and lets the server work it out. Everything below follows from that single split. Hudi plugs into Spark at four points, and Connect puts all four on the server: What Hudi needs Config Why The bundle jar --jars or --packages Contains the DataSource, the catalog and the procedures A Catalyst extension spark.sql.extensions Provides MERGE, UPDATE, CALL and the table-valued functions A catalog spark.sql.catalog.spark_catalog Lets USING hudi tables live in the default catalog Kryo serialization spark.serializer Hudi’s internal types do not survive the Java serializer flowchart TB subgraph CL[&quot;client&quot;] P[&quot;pip install pyspark&amp;lt;br/&amp;gt;no Java, no Hudi jar&quot;] end subgraph SV[&quot;server, fixed at startup&quot;] J[&quot;hudi-spark4.0-bundle&quot;] K[&quot;HoodieSparkSessionExtension&quot;] L[&quot;HoodieCatalog over spark_catalog&quot;] M[&quot;KryoSerializer&quot;] end P --&amp;gt;|&quot;SQL and DataFrame&quot;| SV J --- K --- L --- M None of the four can be set by a client. spark.conf.set reaches the server for runtime SQL configs, but spark.sql.extensions is read once while the session is built, before any client connects. A client can read it, which is the fastest way to tell a broken server from a broken query. Starting a server that speaks Hudi Use --packages when the host can reach Maven Central, --jars with pre-downloaded files when it cannot. Both were run for this post. $SPARK_HOME/sbin/start-connect-server.sh \ --packages org.apache.hudi:hudi-spark4.0-bundle_2.13:1.2.0 \ --conf spark.sql.extensions=org.apache.spark.sql.hudi.HoodieSparkSessionExtension \ --conf spark.serializer=org.apache.spark.serializer.KryoSerializer \ --conf spark.sql.catalog.spark_catalog=org.apache.spark.sql.hudi.catalog.HoodieCatalog \ --conf spark.sql.warehouse.dir=/tmp/warehouse/spark \ --master &quot;local[4]&quot; The coordinate encodes two compatibilities at once, and both are hard failures rather than warnings: Coordinate part Must match hudi-spark4.0-bundle Your Spark minor version, 4.0 here _2.13 Spark’s Scala binary version Spark 4 is Scala 2.13 only, so a _2.12 bundle never loads. The extension class name, which is the usual mistake Hudi’s session extension is org.apache.spark.sql.hudi.HoodieSparkSessionExtension. It lives under org.apache.spark.sql, not under org.apache.hudi, which is where most people reach for it first, and where I reached for it first. Getting it wrong does not stop the server. That is what makes it expensive: the server starts, reports healthy, and then every MERGE and CALL fails later with an error about syntax or a missing table. The only evidence is a warning in the server log at startup: WARN SparkSession: Cannot use org.apache.hudi.HoodieSparkSessionExtension to configure session extensions. java.lang.ClassNotFoundException: org.apache.hudi.HoodieSparkSessionExtension Two habits remove this whole class of problem. Read the name out of the jar rather than from memory: unzip -l hudi-spark4.0-bundle_2.13-1.2.0.jar | grep SessionExtension # 11028 org/apache/spark/sql/hudi/HoodieSparkSessionExtension.class And confirm from the client that the server loaded it: print(spark.conf.get(&quot;spark.sql.extensions&quot;)) # org.apache.spark.sql.hudi.HoodieSparkSessionExtension That one line separates “my SQL is wrong” from “the server is misconfigured”, and it is the first thing to run when a MERGE is rejected. Connecting the client The client needs pyspark and gRPC. No Java, no SPARK_HOME, no Hudi jar. Pin it to the version your server runs: pip install &quot;pyspark[connect]==4.0.2&quot; from pyspark.sql import SparkSession spark = SparkSession.builder.remote(&quot;sc://localhost:15002&quot;).getOrCreate() print(spark.version) spark.version reports the server’s version, so it tells you nothing about your client. Check the client separately with pyspark.__version__. The pin matters more than it looks, and a bare pip install &quot;pyspark[connect]&quot; is the wrong instruction. It installs whatever is newest on PyPI, and a client newer than the server fails on the first createDataFrame: SparkNoSuchElementException: [SQL_CONF_NOT_FOUND] The SQL config &quot;spark.sql.session.localRelationSizeLimit&quot; cannot be found. SQLSTATE: 42K0I The newer client asks the older server for a SQL config that does not exist there. Every SQL statement in this post still works in that configuration, so the failure looks arbitrary: DDL, MERGE, procedures and metadata queries all succeed, and only the paths that build a local relation break. The gap is not symmetric. An older client against a newer server was fine: a 3.5.9 client drove the same 4.0.2 server through every example here, createDataFrame included. A newer client against an older server was not. Match the versions, and when you cannot, be the older side. Copy-on-write tables, the full DML surface Hudi installs over the default catalog rather than adding a named one, so tables are ordinary two-part names. The Hudi specifics go in TBLPROPERTIES. spark.sql(&quot;&quot;&quot; CREATE TABLE default.trips_cow ( trip_id BIGINT, rider_id BIGINT, city STRING, fare DECIMAL(10,2), started_at TIMESTAMP) USING hudi PARTITIONED BY (city) TBLPROPERTIES ( primaryKey = &apos;trip_id&apos;, preCombineField = &apos;started_at&apos;, type = &apos;cow&apos;) LOCATION &apos;/tmp/warehouse/trips_cow&apos;&quot;&quot;&quot;) primaryKey is what makes an UPDATE or MERGE possible at all, since Hudi needs a record key to find the row it is replacing. preCombineField decides which version wins when two records share a key. type = &apos;cow&apos; selects copy-on-write, where an update rewrites the whole base file, as opposed to merge-on-read, which appends a delta log and merges at query time. The partitioning trap Now the thing that will cost you an afternoon. This insert looks obviously correct and fails: spark.sql(&quot;&quot;&quot; INSERT INTO default.trips_cow VALUES (1, 101, &apos;Hyderabad&apos;, 245.50, TIMESTAMP &apos;2026-09-01 08:15:00&apos;)&quot;&quot;&quot;) AnalysisException: [INCOMPATIBLE_DATA_FOR_TABLE.CANNOT_SAFELY_CAST] Cannot write incompatible data for the table `spark_catalog`.`default`.`trips_cow`: Cannot safely cast `fare` &quot;STRING&quot; to &quot;DECIMAL(10,2)&quot;. SQLSTATE: KD000 The error names fare, and fare is fine. Look at the schema Hudi actually created: [f.name for f in spark.table(&quot;default.trips_cow&quot;).schema.fields if not f.name.startswith(&quot;_hoodie&quot;)] [&apos;trip_id&apos;, &apos;rider_id&apos;, &apos;fare&apos;, &apos;started_at&apos;, &apos;city&apos;] Partitioning moves the partition column to the end of the schema. The table was declared with city third, and it is stored fifth. A positional INSERT ... VALUES maps by position, so &apos;Hyderabad&apos; lands on fare, and the cast error is a truthful report of a problem one column to the left of where you are looking. Two fixes. Name the columns, which is worth doing on any Hudi table whether it is partitioned or not: spark.sql(&quot;&quot;&quot; INSERT INTO default.trips_cow (trip_id, rider_id, city, fare, started_at) VALUES (1, 101, &apos;Hyderabad&apos;, 245.50, TIMESTAMP &apos;2026-09-01 08:15:00&apos;), (2, 102, &apos;Bengaluru&apos;, 180.00, TIMESTAMP &apos;2026-09-01 09:30:00&apos;), (3, 103, &apos;Hyderabad&apos;, 320.75, TIMESTAMP &apos;2026-09-02 07:45:00&apos;)&quot;&quot;&quot;) Or supply the values in Hudi’s stored order, with the partition column last. Both were verified; the explicit column list is the one to use, because it survives a later ADD COLUMNS. Update, merge and delete spark.sql(&quot;UPDATE default.trips_cow SET fare = 250.00 WHERE trip_id = 1&quot;) spark.sql(&quot;&quot;&quot; MERGE INTO default.trips_cow t USING (SELECT 4L AS trip_id, 104L AS rider_id, &apos;Pune&apos; AS city, CAST(99.00 AS DECIMAL(10,2)) AS fare, TIMESTAMP &apos;2026-09-03 10:00:00&apos; AS started_at) s ON t.trip_id = s.trip_id WHEN MATCHED THEN UPDATE SET * WHEN NOT MATCHED THEN INSERT *&quot;&quot;&quot;) spark.sql(&quot;DELETE FROM default.trips_cow WHERE trip_id = 2&quot;) All three are extension-provided syntax, so they are also a working test that the server loaded HoodieSparkSessionExtension. Results from the sequence above: count after INSERT 3 fare for trip_id 1 after UPDATE 250.00 count after MERGE (Pune inserted) 4 count after DELETE 3 count WHERE city = &apos;Hyderabad&apos; 2 SHOW PARTITIONS city=Bengaluru, city=Hyderabad, city=Pune SHOW PARTITIONS still listing city=Bengaluru after its only row was deleted is correct: the partition directory and its metadata remain until cleaning removes them. The five Hudi bookkeeping columns come through the client untouched, which is a quick way to confirm you are reading a real Hudi table and not a directory of Parquet: [c for c in spark.table(&quot;default.trips_cow&quot;).columns if c.startswith(&quot;_hoodie&quot;)] [&apos;_hoodie_commit_time&apos;, &apos;_hoodie_commit_seqno&apos;, &apos;_hoodie_record_key&apos;, &apos;_hoodie_partition_path&apos;, &apos;_hoodie_file_name&apos;] Schema evolution: two of four operations ADD COLUMNS works, and old rows read back NULL for the new column as they should: spark.sql(&quot;ALTER TABLE default.trips_cow ADD COLUMNS (pay_method STRING)&quot;) spark.sql(&quot;&quot;&quot; INSERT INTO default.trips_cow (trip_id, rider_id, city, fare, started_at, pay_method) VALUES (5, 105, &apos;Chennai&apos;, 75.25, TIMESTAMP &apos;2026-09-04 11:00:00&apos;, &apos;upi&apos;)&quot;&quot;&quot;) pay_method for trip_id 5 upi rows WHERE pay_method IS NULL 3 Widening a column type with ALTER COLUMN ... TYPE is accepted too. The other two are not: spark.sql(&quot;ALTER TABLE default.trips_cow RENAME COLUMN pay_method TO pm&quot;) spark.sql(&quot;ALTER TABLE default.trips_cow DROP COLUMN pay_method&quot;) AnalysisException: [UNSUPPORTED_FEATURE.TABLE_OPERATION] The feature is not supported: Table `spark_catalog`.`default`.`trips_cow` does not support RENAME COLUMN. AnalysisException: [UNSUPPORTED_FEATURE.TABLE_OPERATION] The feature is not supported: Table `spark_catalog`.`default`.`trips_cow` does not support DROP COLUMN Operation Through Spark Connect ADD COLUMNS Works ALTER COLUMN ... TYPE (widening) Works RENAME COLUMN UNSUPPORTED_FEATURE.TABLE_OPERATION DROP COLUMN UNSUPPORTED_FEATURE.TABLE_OPERATION Plan schema changes as additive. A rename means writing a new column and backfilling it, so decide column names before the table has data you care about. Merge-on-read, and what the two query types show Merge-on-read is where Hudi’s read path becomes visible, and the divergence is easy to demonstrate through a thin client: spark.sql(&quot;&quot;&quot; CREATE TABLE default.trips_mor ( trip_id BIGINT, rider_id BIGINT, city STRING, fare DECIMAL(10,2), started_at TIMESTAMP) USING hudi TBLPROPERTIES (primaryKey = &apos;trip_id&apos;, preCombineField = &apos;started_at&apos;, type = &apos;mor&apos;) LOCATION &apos;/tmp/warehouse/trips_mor&apos;&quot;&quot;&quot;) spark.sql(&quot;&quot;&quot; INSERT INTO default.trips_mor VALUES (1, 101, &apos;Hyderabad&apos;, 245.50, TIMESTAMP &apos;2026-09-01 08:15:00&apos;), (2, 102, &apos;Bengaluru&apos;, 180.00, TIMESTAMP &apos;2026-09-01 09:30:00&apos;)&quot;&quot;&quot;) spark.sql(&quot;UPDATE default.trips_mor SET fare = 999.99 WHERE trip_id = 1&quot;) Now read the same row two ways: # snapshot, the default: base files merged with the delta logs spark.sql(&quot;SELECT fare FROM default.trips_mor WHERE trip_id = 1&quot;).collect()[0][0] # read-optimised: base files only, ignoring the logs (spark.read.format(&quot;hudi&quot;) .option(&quot;hoodie.datasource.query.type&quot;, &quot;read_optimized&quot;) .load(&quot;/tmp/warehouse/trips_mor&quot;) .filter(&quot;trip_id = 1&quot;).select(&quot;fare&quot;).collect()[0][0]) snapshot 999.99 read_optimized 245.50 That gap is the whole merge-on-read trade, measured: the snapshot read pays a merge to be correct, the read-optimised read is cheaper and stale until compaction. Both paths work identically through Connect. Compaction is where expectations need managing. run_compaction is not a command that compacts on demand: spark.sql(&quot;CALL run_compaction(op =&amp;gt; &apos;scheduleandexecute&apos;, table =&amp;gt; &apos;default.trips_mor&apos;)&quot;) 0 rows returned, and CALL show_compaction(...) reports 0 pending compactions Nothing was scheduled because the delta-commit threshold was not met, which on these defaults takes more delta commits than two updates produce. Setting hoodie.compact.inline.max.delta.commits to 1 at table creation went the other way: compaction then ran inline on the write itself, and the read-optimised query returned 999.99 immediately, with still nothing left for run_compaction to do. Either way the procedure returning zero rows is not a failure, and the way to verify compaction is to watch the read-optimised query catch up, not to read the procedure’s return value. Incremental queries and time travel This is the Hudi capability with no direct equivalent in the other formats, and it survives the trip through gRPC intact. The DataFrame writer path, where the hoodie.* options are explicit: from decimal import Decimal from pyspark.sql import functions as F trips = (spark.createDataFrame( [(7, 107, &quot;Kochi&quot;, Decimal(&quot;55.00&quot;), &quot;2026-09-06 10:00:00&quot;), (8, 108, &quot;Kochi&quot;, Decimal(&quot;65.00&quot;), &quot;2026-09-06 11:00:00&quot;)], &quot;trip_id bigint, rider_id bigint, city string, &quot; &quot;fare decimal(10,2), started_at string&quot;) .withColumn(&quot;started_at&quot;, F.to_timestamp(&quot;started_at&quot;))) options = { &quot;hoodie.table.name&quot;: &quot;trips_df&quot;, &quot;hoodie.datasource.write.recordkey.field&quot;: &quot;trip_id&quot;, &quot;hoodie.datasource.write.precombine.field&quot;: &quot;started_at&quot;, } (trips.write.format(&quot;hudi&quot;) .options(**options, **{&quot;hoodie.datasource.write.operation&quot;: &quot;bulk_insert&quot;}) .mode(&quot;overwrite&quot;).save(&quot;/tmp/warehouse/trips_df&quot;)) The three write operations behave as their names promise, which is worth checking once rather than assuming. First an upsert of a row that already exists: changed = (spark.createDataFrame( [(7, 107, &quot;Kochi&quot;, Decimal(&quot;77.77&quot;), &quot;2026-09-06 12:00:00&quot;)], &quot;trip_id bigint, rider_id bigint, city string, &quot; &quot;fare decimal(10,2), started_at string&quot;) .withColumn(&quot;started_at&quot;, F.to_timestamp(&quot;started_at&quot;))) # upsert matches on the record key, so the row count does not grow (changed.write.format(&quot;hudi&quot;) .options(**options, **{&quot;hoodie.datasource.write.operation&quot;: &quot;upsert&quot;}) .mode(&quot;append&quot;).save(&quot;/tmp/warehouse/trips_df&quot;)) Then the same two original rows again, this time with insert: # insert does NOT match on the record key (trips.write.format(&quot;hudi&quot;) .options(**options, **{&quot;hoodie.datasource.write.operation&quot;: &quot;insert&quot;}) .mode(&quot;append&quot;).save(&quot;/tmp/warehouse/trips_df&quot;)) after bulk_insert of 2 rows 2 rows after upsert of trip_id 7 2 rows, fare now 77.77 after insert of the same 2 rows again 4 rows The last line is the one to notice. insert does not deduplicate on the record key, so re-sending rows you already have doubles them. upsert is what most pipelines want, and it is not what every path defaults to. Time travel reads a table as of an instant: commits = [r[0] for r in spark.read.format(&quot;hudi&quot;) .load(&quot;/tmp/warehouse/trips_df&quot;) .select(&quot;_hoodie_commit_time&quot;).distinct() .orderBy(&quot;_hoodie_commit_time&quot;).collect()] spark.read.format(&quot;hudi&quot;).option(&quot;as.of.instant&quot;, commits[0]) \ .load(&quot;/tmp/warehouse/trips_df&quot;).count() commit times [&apos;20260918093803909&apos;, &apos;20260918093804944&apos;, &apos;20260918093805997&apos;] as.of.instant, first 2 current 4 Two rows against four is time travel working over a gRPC connection. And the incremental read, which returns only what changed after an instant: (spark.read.format(&quot;hudi&quot;) .option(&quot;hoodie.datasource.query.type&quot;, &quot;incremental&quot;) .option(&quot;hoodie.datasource.read.begin.instanttime&quot;, &quot;0&quot;) .load(&quot;/tmp/warehouse/trips_df&quot;) .count()) Passing 0 means “everything”, which is how you check the plumbing before wiring a real watermark. In production you store the last processed _hoodie_commit_time and pass that instead. The commit times are timestamps, so yours will differ from the ones above; every other number here is reproducible from a clean warehouse. Procedures and metadata, over gRPC People assume the CALL interface will not survive Spark Connect. It does. These all ran from the thin client: Procedure What it returned show_commits The instant timeline, newest first show_table_properties The hoodie.* properties as stored show_commit_files Files touched by one instant show_fsview_latest The latest file slices for a partition stats_file_sizes File-size distribution stats_wa Write-amplification statistics run_clean Cleans older file versions run_clustering Reorganises data files run_compaction Schedules or runs MOR compaction create_savepoint, show_savepoints Savepoint management rollback_to_instant Unwinds a commit, with the limit below spark.sql(&quot;CALL show_commits(table =&amp;gt; &apos;default.trips_cow&apos;, limit =&amp;gt; 10)&quot;).count() spark.sql(&quot;CALL stats_wa(table =&amp;gt; &apos;default.trips_cow&apos;)&quot;).count() spark.sql(&quot;CALL run_clean(table =&amp;gt; &apos;default.trips_cow&apos;)&quot;).count() The table-valued functions work as well, with one argument requirement that is easy to miss: spark.sql(&quot;SELECT count(*) FROM hudi_metadata(&apos;default.trips_cow&apos;)&quot;).collect() spark.sql(&quot;SELECT count(*) FROM hudi_table_changes(&apos;default.trips_cow&apos;,&apos;latest_state&apos;,&apos;earliest&apos;)&quot;).collect() spark.sql(&quot;SELECT count(*) FROM hudi_filesystem_view(&apos;default.trips_cow&apos;,&apos;city=Hyderabad&apos;)&quot;).collect() hudi_filesystem_view takes a partition as its second argument. Called with only a table name it returns zero rows rather than an error, which reads like an empty table: hudi_filesystem_view(&apos;default.trips_cow&apos;) 0 hudi_filesystem_view(&apos;default.trips_cow&apos;,&apos;city=Hyderabad&apos;) 3 hudi_filesystem_view(&apos;default.trips_mor&apos;,&apos;&apos;) 1 For a non-partitioned table pass the empty string. And hudi_table_changes in cdc mode needs a table created with CDC enabled: IllegalArgumentException: It isn&apos;t a CDC hudi table on file:/tmp/warehouse/trips_cow That is a table-property problem, not a Connect one. Enable hoodie.table.cdc.enabled at creation if you want the CDC feed. rollback_to_instant only unwinds the newest instant This one deserves its own note because the error is opaque and my first explanation for it was wrong. Rolling back the newest instant works: instants = sorted(r[0] for r in spark.sql(&quot;CALL show_commits(table =&amp;gt; &apos;default.trips_cow&apos;, limit =&amp;gt; 30)&quot;).collect()) spark.sql(&quot;CALL rollback_to_instant(table =&amp;gt; &apos;default.trips_cow&apos;, &quot; &quot;instant_time =&amp;gt; &apos;%s&apos;)&quot; % instants[-1]).collect() # [Row(rollback_result=True)], and the row count drops by that commit Rolling back an older instant fails: UnknownException: (org.apache.hudi.exception.HoodieRollbackException) Failed to rollback file:///tmp/warehouse/trips commits 20260918094116830 I first assumed a savepoint on the table was blocking it, and tested that: creating a savepoint and then rolling back the newest instant still succeeded, so the savepoint was not the cause. The actual rule is simpler. Rollback unwinds the tip of the timeline, one commit at a time. It is not “restore to this point”, and the way to go further back is repeated rollbacks or a savepoint plus restore. The practical mistake this invites is an ordering one. show_commits returns instants newest first, so the first row it hands you is the newest, while sorting those instants ascending puts the oldest first. Sorting a list of Hudi instants and passing the front of it gives rollback_to_instant exactly the argument it cannot process, which is how I produced the failure above in the first place. What the thin client cannot do The limitations are narrow and all follow from the client not having a JVM. spark.sparkContext PySparkAttributeError: [JVM_ATTRIBUTE_NOT_SUPPORTED] Attribute `sparkContext` is not supported in Spark Connect as it depends on the JVM. spark.range(10).rdd.map(lambda r: r.id).collect() PySparkNotImplementedError: [NOT_IMPLEMENTED] rdd is not implemented. Neither matters much for table work, since Hudi is driven through SQL and the DataFrame API anyway. What does bite is subtler. A Python float for a decimal column fails with an empty error, where a classic session coerces it: spark.createDataFrame( [(9, 109, &quot;Kochi&quot;, 75.25, &quot;2026-09-05 12:00:00&quot;)], &quot;trip_id bigint, rider_id bigint, city string, &quot; &quot;fare decimal(10,2), started_at string&quot;) AssertionError That is the entire message. The traceback points at convert_decimal in pyspark/sql/conversion.py, which does assert isinstance(value, decimal.Decimal). Pass Decimal(&quot;75.25&quot;) and it works. Any Hudi table with a money column hits this on the first createDataFrame. Classic session Spark Connect client spark.sparkContext available Raises JVM_ATTRIBUTE_NOT_SUPPORTED df.rdd available Raises NOT_IMPLEMENTED float coerced into a decimal column Bare AssertionError Hudi jar and extension per submit Server-side only, fixed at startup SQL, DML, procedures, TVFs, incremental reads All work unchanged Common misconceptions “One server can serve Hudi and Iceberg with no interference.” Nearly, and the exception is measurable. Loading Hudi’s session extension alongside Iceberg’s breaks Iceberg’s SQL time travel, which then fails with [INTERNAL_ERROR] Found the unresolved operator: &apos;TimeTravelRelation. It is one-directional: Hudi’s own DML, procedures, incremental reads and as.of.instant all keep working. “The extension class lives under the project’s own package.” Hudi’s does not. It is org.apache.spark.sql.hudi.HoodieSparkSessionExtension, under org.apache.spark.sql. Getting it wrong does not stop the server: it starts, logs one Cannot use warning, and then every MERGE fails later for reasons that look unrelated. ”--jars is not enough for a session extension, it needs the driver classpath.” It is enough. That conclusion came from a wrong class name, not a classpath problem, and --jars alone loads both extensions correctly once the name is right. “A bare AssertionError from the client is a client bug.” It is usually a type error. The Connect client asserts the exact type during Arrow conversion, so a Python float in a decimal column fails with an empty message where a classic session would have coerced it. Production notes Treat the server config as a contract. The jar, the extension, the catalog and Kryo are fixed at startup, so a change means a restart. Nobody’s client can work around a server that was started wrong. Check the extension before debugging SQL. spark.conf.get(&quot;spark.sql.extensions&quot;) from the client, and grep &quot;Cannot use&quot; on the server log, resolve most “MERGE is not supported” reports. Always name columns in INSERT. It costs one line and removes the partition-reordering trap permanently, including after a later ADD COLUMNS. Design schema changes as additive. RENAME COLUMN and DROP COLUMN are rejected, so a rename is a new column plus a backfill. Use upsert, not insert, unless you mean to duplicate. The insert operation does not deduplicate on the record key. Do not judge compaction by the procedure’s return value. run_compaction returning zero rows usually means the delta-commit threshold was not met. Watch the read-optimised query instead. Pass Decimal for decimal columns in createDataFrame, and prefer CAST(... AS DECIMAL(10,2)) inside SQL. Keep Kryo set even on a server you think is Iceberg-only, because one shared server usually ends up serving both. Think twice before adding Hudi’s extension to a server that serves Iceberg. It breaks Iceberg’s SQL time travel, one-directionally, and nothing warns you at startup. Frequently asked questions Does the client need the Hudi jar? No, and that is the main reason to use Connect for Hudi work. The client sends a plan naming a table and the server resolves it. A virtualenv with pyspark is the whole client-side dependency, which also removes the jar-version skew that usually makes onboarding painful. Can one server serve Hudi and Iceberg together? Mostly, and the cost falls entirely on Iceberg. spark.sql.extensions takes a comma-separated list, Hudi takes over spark_catalog while Iceberg gets a named catalog, and both formats’ DDL and DML work. But loading Hudi’s extension alongside Iceberg’s breaks Iceberg’s SQL time travel, which then fails with [INTERNAL_ERROR] Found the unresolved operator: &apos;TimeTravelRelation. Hudi is unaffected in that arrangement: its own DML, procedures, incremental reads and as.of.instant time travel all keep working. The measurement and the workaround are in Apache Iceberg through Spark Connect. Why does my MERGE fail with a parse error? Because the session extension is not loaded on the server, from either a wrong class name or a jar missing from the server classpath. MERGE and UPDATE on Hudi are extension-provided syntax, so without it the statement is not merely unsupported, it is unparseable. Can I set hoodie.* options per job from the client? Write-time hoodie.* options travel fine, because they are passed with the write rather than baked into the session. What you cannot change from a client is the session-level set: the extension, the catalog and the serializer. Is the partition-column reordering a Spark Connect bug? No. It is how Hudi stores a partitioned table, and a classic session behaves the same way. Connect only makes it more visible, because the error arrives without a local stack trace to poke at. Do client and server versions have to match? Match them. An older client is tolerated: a 3.5.9 client drove the 4.0.2 server through every example here. A newer one is not, and the failure is specific rather than general. A pip-installed 4.2.0 client ran every SQL statement against the 4.0.2 server and then failed on createDataFrame with SQL_CONF_NOT_FOUND for spark.sql.session.localRelationSizeLimit, because it expects a config the older server does not have. That is why the install line above is pinned. Conclusion The useful reframing is that Spark Connect forces a separation which was always there. Hudi is infrastructure: a jar, a Catalyst extension, a catalog and a serializer. Your query is not. Classic Spark let you blur the two by shipping jars with every submit, and most --packages lines in most runbooks are that blur made permanent. Connect makes you decide once, on the server, and then hands every client a clean API with nothing to install. The cost is exactly the flexibility you were abusing: no per-job jars, no per-job extensions, no sparkContext escape hatch. For a team sharing a Hudi lakehouse, that is a good trade. What is worth carrying away is how few of the failures were about Spark Connect. An extension class under a package that looks wrong but is not, a partition column silently moved to the end of the schema, insert where upsert was meant, rollback_to_instant that only unwinds the tip, and a float where a Decimal belongs. Every one of those behaves identically in a classic session. Reproduce a failure locally before blaming the transport, which is the habit that turned two of my own confident wrong answers here into the right ones. References Spark Connect overview for the architecture and the client APIs Hudi Spark SQL DDL and SQL procedures for table properties and the CALL surface Hudi configuration reference for the hoodie.* write and read options used above conversion.py, whose convert_decimal is the assertion behind the empty AssertionError Apache Hudi architecture for the timeline, file slices and compaction machinery underneath all of this Trademarks Apache Spark, Apache Hudi, Apache Iceberg, Apache Hive, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">Apache Spark joins in depth: the five strategies and how one gets picked</title><link href="https://rangareddy.github.io/SparkJoinsInDepth/" rel="alternate" type="text/html" title="Apache Spark joins in depth: the five strategies and how one gets picked" /><published>2026-09-17T09:00:00+05:30</published><updated>2026-09-17T09:00:00+05:30</updated><id>https://rangareddy.github.io/SparkJoinsInDepth</id><content type="html" xml:base="https://rangareddy.github.io/SparkJoinsInDepth/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#types-and-strategies-are-different-things&quot; id=&quot;markdown-toc-types-and-strategies-are-different-things&quot;&gt;Types and strategies are different things&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-what-every-join-is-made-of&quot; id=&quot;markdown-toc-architecture-what-every-join-is-made-of&quot;&gt;Architecture: what every join is made of&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#why-there-are-five-and-not-six&quot; id=&quot;markdown-toc-why-there-are-five-and-not-six&quot;&gt;Why there are five, and not six&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-five-strategies&quot; id=&quot;markdown-toc-the-five-strategies&quot;&gt;The five strategies&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#broadcast-hash-join&quot; id=&quot;markdown-toc-broadcast-hash-join&quot;&gt;Broadcast hash join&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#shuffle-hash-join&quot; id=&quot;markdown-toc-shuffle-hash-join&quot;&gt;Shuffle hash join&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#sort-merge-join&quot; id=&quot;markdown-toc-sort-merge-join&quot;&gt;Sort-merge join&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#broadcast-nested-loop-join&quot; id=&quot;markdown-toc-broadcast-nested-loop-join&quot;&gt;Broadcast nested loop join&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#cartesian-product&quot; id=&quot;markdown-toc-cartesian-product&quot;&gt;Cartesian product&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#how-does-spark-actually-choose&quot; id=&quot;markdown-toc-how-does-spark-actually-choose&quot;&gt;How does Spark actually choose?&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#the-size-rules-exactly&quot; id=&quot;markdown-toc-the-size-rules-exactly&quot;&gt;The size rules, exactly&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#which-build-side-each-join-type-allows&quot; id=&quot;markdown-toc-which-build-side-each-join-type-allows&quot;&gt;Which build side each join type allows&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#hands-on-making-each-strategy-appear&quot; id=&quot;markdown-toc-hands-on-making-each-strategy-appear&quot;&gt;Hands-on: making each strategy appear&lt;/a&gt;    &lt;ul&gt;
      &lt;li&gt;&lt;a href=&quot;#the-join-type-overrides-the-size&quot; id=&quot;markdown-toc-the-join-type-overrides-the-size&quot;&gt;The join type overrides the size&lt;/a&gt;&lt;/li&gt;
      &lt;li&gt;&lt;a href=&quot;#why-shuffle-hash-join-is-rare-demonstrated&quot; id=&quot;markdown-toc-why-shuffle-hash-join-is-rare-demonstrated&quot;&gt;Why shuffle hash join is rare, demonstrated&lt;/a&gt;&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-adaptive-execution-changes&quot; id=&quot;markdown-toc-what-adaptive-execution-changes&quot;&gt;What adaptive execution changes&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#reading-the-plan&quot; id=&quot;markdown-toc-reading-the-plan&quot;&gt;Reading the plan&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#which-strategy-for-which-scenario&quot; id=&quot;markdown-toc-which-strategy-for-which-scenario&quot;&gt;Which strategy for which scenario&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#common-misconceptions&quot; id=&quot;markdown-toc-common-misconceptions&quot;&gt;Common misconceptions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-tips&quot; id=&quot;markdown-toc-production-tips&quot;&gt;Production tips&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;A join &lt;strong&gt;type&lt;/strong&gt; is semantics (inner, outer, semi, anti). A join &lt;strong&gt;strategy&lt;/strong&gt; is the algorithm (broadcast hash, shuffle hash, sort-merge, broadcast nested loop, cartesian). Mixing the two words up is why join tuning feels arbitrary.&lt;/li&gt;
    &lt;li&gt;Spark’s choice is a documented, ordered procedure in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;JoinSelection&lt;/code&gt;, not a cost model. Hints first, then broadcast, then shuffle hash, then sort-merge, then cartesian, then nested loop “as the final solution”.&lt;/li&gt;
    &lt;li&gt;The join type silently restricts the strategy. &lt;strong&gt;A full outer join can never be broadcast&lt;/strong&gt;, and for a right outer join only the &lt;em&gt;left&lt;/em&gt; side may be. Both are confirmed below by running them.&lt;/li&gt;
    &lt;li&gt;Shuffle hash join needs three conditions at once, one of which is off by default, which is why you almost never see it without a hint.&lt;/li&gt;
    &lt;li&gt;The 10 MB threshold is not the only size rule. Shuffle hash join has its own ceiling of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; multiplied by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;, which is 2 GB on the defaults, and a side effect: setting that threshold to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; disables shuffle hash join along with broadcasting.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;Join tuning has a reputation for being folklore. Broadcast the small side, raise
the threshold, add a hint, and if none of that works, salt the key. It works
often enough to feel like knowledge and fails often enough to feel like luck.&lt;/p&gt;

&lt;p&gt;The reason is that most of what circulates describes the five strategies without
describing the &lt;strong&gt;procedure&lt;/strong&gt; that picks between them. That procedure is not
mysterious: it is written down, in order, in a comment above
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;JoinSelection&lt;/code&gt; in Spark’s own source, and once you have read it the behaviour
stops being surprising. A full outer join that refuses to broadcast is not a
bug, and no amount of raising the threshold will change it.&lt;/p&gt;

&lt;p&gt;This post has three parts. What each strategy actually does, the exact order
Spark tries them in, and which scenario should get which.&lt;/p&gt;

&lt;h2 id=&quot;types-and-strategies-are-different-things&quot;&gt;Types and strategies are different things&lt;/h2&gt;

&lt;p&gt;This is the distinction to get right first, because the two are chosen
independently and the first one constrains the second.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Join type&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Join strategy&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Decides&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Which rows appear in the result&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;How the rows are brought together&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;You write it&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes, it is your query’s meaning&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;No, Catalyst picks it&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Examples&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;inner, left outer, full outer, left semi, left anti, cross&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;broadcast hash, shuffle hash, sort-merge, broadcast nested loop, cartesian&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Changes results&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Never&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;A strategy change can make a query ten times faster or fail outright, and it
&lt;strong&gt;cannot&lt;/strong&gt; change the answer. A type change always changes the answer. So when
someone says “we changed the join and it got faster”, the useful question is
which of the two they changed.&lt;/p&gt;

&lt;p&gt;The types, briefly, since the rest of the post assumes them:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;                      &lt;span class=&quot;c1&quot;&gt;# inner, the default
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;              &lt;span class=&quot;c1&quot;&gt;# left outer
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;full&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;              &lt;span class=&quot;c1&quot;&gt;# full outer
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left_semi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;         &lt;span class=&quot;c1&quot;&gt;# left rows that match
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left_anti&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;         &lt;span class=&quot;c1&quot;&gt;# left rows that do not
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;crossJoin&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;                            &lt;span class=&quot;c1&quot;&gt;# every pair
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Semi and anti deserve a word because they are under-used. Both filter the left
side by whether a match exists and return &lt;strong&gt;no columns from the right&lt;/strong&gt;, so a
left row matching a hundred right rows is emitted once. An inner join in the
same position emits it a hundred times and needs a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt; afterwards, which
is a second shuffle.&lt;/p&gt;

&lt;h2 id=&quot;architecture-what-every-join-is-made-of&quot;&gt;Architecture: what every join is made of&lt;/h2&gt;

&lt;p&gt;All five strategies are assembled from the same three parts, and naming them
makes the differences between the strategies easy to hold.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph P[&quot;every join has these&quot;]
    A[&quot;&amp;lt;b&amp;gt;build side&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;materialised into a lookup&amp;lt;br/&amp;gt;structure, or sorted&quot;]
    B[&quot;&amp;lt;b&amp;gt;stream side&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;read once and probed&amp;lt;br/&amp;gt;against the build side&quot;]
    C[&quot;&amp;lt;b&amp;gt;co-location step&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;how matching keys are made&amp;lt;br/&amp;gt;to meet on one executor&quot;]
  end
  A --&amp;gt; J[&quot;the join operator&quot;]
  B --&amp;gt; J
  C --&amp;gt; J
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;strong&gt;The build side&lt;/strong&gt; is the one Spark materialises: a hash table for the hash
joins, a sorted run for sort-merge. It is the side that costs memory, which is
why “which side is the build side” is the question behind most join failures.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The stream side&lt;/strong&gt; is read once and probed. It costs nothing to hold, which is
why you want the large table here.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The co-location step&lt;/strong&gt; is where the strategies genuinely differ, and it is the
expensive part:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Strategy&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;How keys are co-located&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Network cost&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast hash&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Copy the whole build side everywhere&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;One copy per executor&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle hash&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle both sides by key&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Full shuffle&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle both sides by key, then sort&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Full shuffle plus sort&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast nested loop&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Copy one side everywhere, compare all pairs&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;One copy per executor&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Cartesian&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Replicate partitions across each other&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Every partition pair&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Read down that column and the whole subject compresses to one trade: &lt;strong&gt;either
you copy the small side to where the data already is, or you move both sides to
a common place.&lt;/strong&gt; Broadcasting does the first, the shuffle-based strategies do
the second, and bucketing is how you pay for the second once at write time
instead of on every read.&lt;/p&gt;

&lt;h2 id=&quot;why-there-are-five-and-not-six&quot;&gt;Why there are five, and not six&lt;/h2&gt;

&lt;p&gt;The five strategies are not five unrelated algorithms. They are combinations of
two independent choices, and separating them is the clearest way to hold the
whole subject.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Choice one, the data exchange:&lt;/strong&gt; how matching keys are made to meet on one
executor.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Broadcast.&lt;/strong&gt; Copy one side to every executor. The other side never moves.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Shuffle.&lt;/strong&gt; Repartition both sides by the join key so matching keys land in the same partition.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;strong&gt;Choice two, the join algorithm:&lt;/strong&gt; what happens once the rows are together.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Hash.&lt;/strong&gt; Build a hash table from one side, probe it with the other.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Sort-merge.&lt;/strong&gt; Sort both sides, walk them with a cursor each.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Nested loop.&lt;/strong&gt; Compare every pair, evaluating an arbitrary condition.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Two exchanges times three algorithms is six combinations. Spark ships &lt;strong&gt;five&lt;/strong&gt;
physical operators, and the grid shows exactly which cell is missing:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Hash&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Sort-merge&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Nested loop&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Broadcast&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastHashJoinExec&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;em&gt;does not exist&lt;/em&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastNestedLoopJoinExec&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Shuffle&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffledHashJoinExec&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoinExec&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProductExec&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;strong&gt;Why there is no broadcast sort-merge join&lt;/strong&gt; is worth a moment, because it
explains what sorting is actually for. Sorting buys &lt;strong&gt;bounded memory&lt;/strong&gt;: you
never hold a whole side, and the sort can spill. But if a side has already been
copied to every executor, it is in memory by definition, so a hash table over it
costs nothing extra and gives constant-time probes. Sorting both sides to get
the same answer more slowly has no upside. Sorting only earns its place when you
had to shuffle anyway and cannot afford to hold a side in memory.&lt;/p&gt;

&lt;p&gt;So the decision Spark makes is really two decisions:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;can one side be copied&amp;lt;br/&amp;gt;to every executor?&quot;]
  A --&amp;gt;|&quot;yes&quot;| B{&quot;equi-join?&quot;}
  A --&amp;gt;|&quot;no&quot;| C{&quot;equi-join?&quot;}
  B --&amp;gt;|&quot;yes&quot;| D[&quot;BroadcastHashJoin&quot;]
  B --&amp;gt;|&quot;no&quot;| E[&quot;BroadcastNestedLoopJoin&quot;]
  C --&amp;gt;|&quot;yes&quot;| F{&quot;hold a side in memory&amp;lt;br/&amp;gt;per partition?&quot;}
  C --&amp;gt;|&quot;no&quot;| G[&quot;CartesianProduct&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;inner-like only&amp;lt;/i&amp;gt;&quot;]
  F --&amp;gt;|&quot;yes&quot;| H[&quot;ShuffledHashJoin&quot;]
  F --&amp;gt;|&quot;no&quot;| I[&quot;SortMergeJoin&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Read it as: &lt;strong&gt;the exchange is decided by size, and the algorithm by the
condition and by how much memory you can hold.&lt;/strong&gt; An equi-join can hash or
sort-merge; a non-equi join has only nested loop, which is why it collapses to
two options. That single sentence predicts most of what the planner does.&lt;/p&gt;

&lt;h2 id=&quot;the-five-strategies&quot;&gt;The five strategies&lt;/h2&gt;

&lt;p&gt;Spark’s source documents all five with their limits. The table below is that
comment, condensed:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Strategy&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;Equi-join only&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;Keys must be sortable&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Join types&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast hash join&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;Yes&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;All &lt;strong&gt;except full outer&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Shuffle hash join&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;Yes&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;All&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge join&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;Yes&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;&lt;strong&gt;Yes&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;All&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast nested loop&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;All, but efficient only in some combinations&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Cartesian product&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Inner-like only&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;An &lt;strong&gt;equi-join&lt;/strong&gt; is one whose condition is equality on keys, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;a.id = b.id&lt;/code&gt;.
Anything else, a range or an inequality, is a non-equi join, and only the bottom
two rows can execute it at all. That single fact explains most surprises in the
plan.&lt;/p&gt;

&lt;h3 id=&quot;broadcast-hash-join&quot;&gt;Broadcast hash join&lt;/h3&gt;

&lt;p&gt;Spark collects the small side to the driver, sends a copy to every executor, and
each executor builds a hash table from it. Each partition of the large side then
probes that table locally. &lt;strong&gt;The large side is never shuffled.&lt;/strong&gt;&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  S[&quot;small side: cities&quot;] --&amp;gt; D[&quot;driver collects it&quot;]
  D --&amp;gt; BC[(&quot;broadcast:&amp;lt;br/&amp;gt;one copy per executor&quot;)]
  BC --&amp;gt; E1[&quot;executor 1&amp;lt;br/&amp;gt;hash table&quot;]
  BC --&amp;gt; E2[&quot;executor 2&amp;lt;br/&amp;gt;hash table&quot;]
  BC --&amp;gt; E3[&quot;executor 3&amp;lt;br/&amp;gt;hash table&quot;]
  T1[&quot;trips partition 0&amp;lt;br/&amp;gt;does not move&quot;] --&amp;gt; E1
  T2[&quot;trips partition 1&amp;lt;br/&amp;gt;does not move&quot;] --&amp;gt; E2
  T3[&quot;trips partition 2&amp;lt;br/&amp;gt;does not move&quot;] --&amp;gt; E3
  E1 --&amp;gt; R[&quot;output, no shuffle&amp;lt;br/&amp;gt;of the large side&quot;]
  E2 --&amp;gt; R
  E3 --&amp;gt; R
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Cost: one copy of the small side per executor, held in memory for the duration,
plus the driver having to assemble it first.&lt;/p&gt;

&lt;p&gt;That driver step is the part people forget. A broadcast that is too large fails
on the &lt;strong&gt;driver&lt;/strong&gt;, not the executors, and the error often arrives as a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.broadcastTimeout&lt;/code&gt; after the default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;300&lt;/code&gt; seconds rather than as an
obvious out-of-memory.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Key points&lt;/strong&gt;&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Build side:&lt;/strong&gt; the small one, if &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;canBroadcastBySize&lt;/code&gt; passes and the join type allows that side.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Network:&lt;/strong&gt; one copy of the small side per executor. The large side never moves.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Memory:&lt;/strong&gt; the hash table sits on every executor, and on the driver first.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Wins when:&lt;/strong&gt; one side is genuinely small after filters and projections are applied.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Fails when:&lt;/strong&gt; the small side is not small, or the join type forbids that build side.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;In the plan:&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastHashJoin&lt;/code&gt; with a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastExchange&lt;/code&gt; child.&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;shuffle-hash-join&quot;&gt;Shuffle hash join&lt;/h3&gt;

&lt;p&gt;Both sides are shuffled on the join key. Then, for each partition, Spark builds
a hash table from the smaller side and probes it with the larger. No sorting.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;riders&amp;lt;br/&amp;gt;the smaller side&quot;] --&amp;gt; SH[(&quot;shuffle both sides&amp;lt;br/&amp;gt;by the join key&quot;)]
  B[&quot;trips&amp;lt;br/&amp;gt;the larger side&quot;] --&amp;gt; SH
  SH --&amp;gt; P0[&quot;partition 0&amp;lt;br/&amp;gt;build hash from riders&amp;lt;br/&amp;gt;probe with trips&quot;]
  SH --&amp;gt; P1[&quot;partition 1&amp;lt;br/&amp;gt;build hash from riders&amp;lt;br/&amp;gt;probe with trips&quot;]
  SH --&amp;gt; P2[&quot;partition 2&amp;lt;br/&amp;gt;build hash from riders&amp;lt;br/&amp;gt;probe with trips&quot;]
  P0 --&amp;gt; O[&quot;output, never sorted&quot;]
  P1 --&amp;gt; O
  P2 --&amp;gt; O
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;It sits in a narrow band: one side too big to broadcast, but small enough that a
per-partition hash table fits. It skips the sort that sort-merge pays, and in
exchange it can run out of memory where sort-merge would spill.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Key points&lt;/strong&gt;&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Build side:&lt;/strong&gt; the smaller side, per partition, not the whole table.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Network:&lt;/strong&gt; a full shuffle of both sides, the same as sort-merge.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Memory:&lt;/strong&gt; one hash table per partition, which is what can blow up.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Wins when:&lt;/strong&gt; the build side is much smaller but still too big to broadcast, and the sort is the thing you want to skip.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Fails when:&lt;/strong&gt; a partition’s build side does not fit, since a hash table cannot spill the way a sort can.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;In the plan:&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffledHashJoin&lt;/code&gt; with an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange hashpartitioning&lt;/code&gt; under each side and no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Sort&lt;/code&gt; node.&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;sort-merge-join&quot;&gt;Sort-merge join&lt;/h3&gt;

&lt;p&gt;Both sides are shuffled on the key, each partition is sorted, and the two sorted
streams are walked together with a cursor on each.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;trips&quot;] --&amp;gt; S[(&quot;shuffle both sides&amp;lt;br/&amp;gt;by the join key&quot;)]
  B[&quot;cities&quot;] --&amp;gt; S
  S --&amp;gt; S0[&quot;partition 0&amp;lt;br/&amp;gt;sort both sides&quot;]
  S --&amp;gt; S1[&quot;partition 1&amp;lt;br/&amp;gt;sort both sides&quot;]
  S0 --&amp;gt; M0[&quot;merge with one&amp;lt;br/&amp;gt;cursor per side&quot;]
  S1 --&amp;gt; M1[&quot;merge with one&amp;lt;br/&amp;gt;cursor per side&quot;]
  M0 --&amp;gt; O[&quot;output, sorted by key&quot;]
  M1 --&amp;gt; O
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The merge itself is the cheap part. Each side has a cursor, and whichever cursor
points at the smaller key advances:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;left  (sorted):  a a b c d ...
right (sorted):  a b b d e ...
                 ^ ^
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Its virtue is bounded memory. Only the merge buffers are held, not a whole hash
table, and the sort can spill, so a partition larger than memory still finishes.
That is why it is the default for large-to-large joins, and why
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.join.preferSortMergeJoin&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;The cost is the sort, and the requirement is that the &lt;strong&gt;keys are sortable&lt;/strong&gt;. It
is the only strategy with that constraint.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Key points&lt;/strong&gt;&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Build side:&lt;/strong&gt; neither, in the hash-table sense. Both sides are sorted and streamed.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Network:&lt;/strong&gt; a full shuffle of both sides, plus a sort per partition.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Memory:&lt;/strong&gt; bounded, and the sort spills to disk rather than failing.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Wins when:&lt;/strong&gt; both sides are large, which is why it is the default.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Fails when:&lt;/strong&gt; rarely, and not on memory. Its failure mode is slowness from the shuffle and the sort, or one skewed partition, rather than an out-of-memory.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;In the plan:&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoin&lt;/code&gt; with a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Sort&lt;/code&gt; node above an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt; on each side.&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;broadcast-nested-loop-join&quot;&gt;Broadcast nested loop join&lt;/h3&gt;

&lt;p&gt;One side is broadcast, and then every row of one side is compared against every
row of the other, evaluating an arbitrary condition. It is the fallback that
makes non-equi joins possible at all.&lt;/p&gt;

&lt;p&gt;The source notes it is optimised for three specific shapes, and slow otherwise:&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Broadcasting the &lt;strong&gt;left&lt;/strong&gt; side in a right outer join.&lt;/li&gt;
  &lt;li&gt;Broadcasting the &lt;strong&gt;right&lt;/strong&gt; side in a left outer, left semi, left anti or existence join.&lt;/li&gt;
  &lt;li&gt;Broadcasting &lt;strong&gt;either&lt;/strong&gt; side in an inner-like join.&lt;/li&gt;
&lt;/ol&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  S[&quot;one side, broadcast whole&quot;] --&amp;gt; E1[&quot;executor 1&quot;]
  S --&amp;gt; E2[&quot;executor 2&quot;]
  P1[&quot;partition of the other side&quot;] --&amp;gt; E1
  P2[&quot;partition of the other side&quot;] --&amp;gt; E2
  E1 --&amp;gt; C1[&quot;compare every local row&amp;lt;br/&amp;gt;against every broadcast row&quot;]
  E2 --&amp;gt; C2[&quot;compare every local row&amp;lt;br/&amp;gt;against every broadcast row&quot;]
  C1 --&amp;gt; O[&quot;rows where the&amp;lt;br/&amp;gt;condition holds&quot;]
  C2 --&amp;gt; O
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Outside those, “we need to scan the data multiple times, which can be rather
slow”. Seeing &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastNestedLoopJoin&lt;/code&gt; on two large inputs means the query is
doing something close to a cross product.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Key points&lt;/strong&gt;&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Build side:&lt;/strong&gt; the broadcast one, and only the shapes listed above are efficient.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Network:&lt;/strong&gt; one copy of the broadcast side per executor.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Cost:&lt;/strong&gt; the row comparisons, which grow as the product of the two row counts.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Wins when:&lt;/strong&gt; the condition is not an equality and one side is genuinely tiny.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Fails when:&lt;/strong&gt; both sides are large, where it is the “may OOM” last resort.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;In the plan:&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastNestedLoopJoin&lt;/code&gt; with the condition printed inline.&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&quot;cartesian-product&quot;&gt;Cartesian product&lt;/h3&gt;

&lt;p&gt;Also called shuffle-and-replicate nested loop. Every partition of one side is
paired with every partition of the other, producing the full cross product. It
supports &lt;strong&gt;inner-like joins only&lt;/strong&gt;, and it is what you get from a deliberate
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;crossJoin&lt;/code&gt; once broadcasting is off the table.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A0[&quot;A partition 0&quot;] --&amp;gt; X[&quot;every A partition paired&amp;lt;br/&amp;gt;with every B partition&quot;]
  A1[&quot;A partition 1&quot;] --&amp;gt; X
  B0[&quot;B partition 0&quot;] --&amp;gt; X
  B1[&quot;B partition 1&quot;] --&amp;gt; X
  X --&amp;gt; P00[&quot;A0 x B0&quot;]
  X --&amp;gt; P01[&quot;A0 x B1&quot;]
  X --&amp;gt; P10[&quot;A1 x B0&quot;]
  X --&amp;gt; P11[&quot;A1 x B1&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;With two partitions a side that is four tasks. With 200 a side it is 40,000, and
that multiplication is the whole reason this strategy is a warning sign.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Key points&lt;/strong&gt;&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Build side:&lt;/strong&gt; neither. Partitions are replicated against each other.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Network:&lt;/strong&gt; every partition pair, so the shuffle grows as the product of the partition counts.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Cost:&lt;/strong&gt; the output itself, which is the product of the two row counts.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Wins when:&lt;/strong&gt; you actually want a cross product, on small inputs.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Fails when:&lt;/strong&gt; you did not want one, which is the usual case.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;In the plan:&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct&lt;/code&gt;, almost always worth investigating.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;how-does-spark-actually-choose&quot;&gt;How does Spark actually choose?&lt;/h2&gt;

&lt;p&gt;Here is the part that turns folklore into a procedure. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;JoinSelection&lt;/code&gt; follows a
fixed order, and the first applicable rule wins.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;For an equi-join, hints are consulted first, in this order:&lt;/strong&gt;&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt;: pick broadcast hash join if the join type supports it.&lt;/li&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;: pick sort-merge join if the keys are sortable.&lt;/li&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SHUFFLE_HASH&lt;/code&gt;: pick shuffle hash join if the join type supports it.&lt;/li&gt;
  &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SHUFFLE_REPLICATE_NL&lt;/code&gt;: pick cartesian product if the join type is inner-like.&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;&lt;strong&gt;With no hints, for an equi-join:&lt;/strong&gt;&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Broadcast hash join, if one side is small enough to broadcast &lt;strong&gt;and&lt;/strong&gt; the join type allows that side to be the build side.&lt;/li&gt;
  &lt;li&gt;Shuffle hash join, if one side can build a local hash map, is much smaller than the other, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.join.preferSortMergeJoin&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Sort-merge join, if the join keys are sortable.&lt;/li&gt;
  &lt;li&gt;Cartesian product, if the join type is inner-like.&lt;/li&gt;
  &lt;li&gt;Broadcast nested loop join as the last resort. The source is blunt about it: “It may OOM but we don’t have other choice.”&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;&lt;strong&gt;For a non-equi join&lt;/strong&gt; the list is much shorter, which is why these are so
often slow:&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Broadcast nested loop join, if one side is small enough to broadcast.&lt;/li&gt;
  &lt;li&gt;Cartesian product, if the join type is inner-like.&lt;/li&gt;
  &lt;li&gt;Broadcast nested loop join anyway.&lt;/li&gt;
&lt;/ol&gt;

&lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  H{&quot;a join hint?&quot;} --&amp;gt;|&quot;yes&quot;| HH[&quot;honour it if the type&amp;lt;br/&amp;gt;and keys allow&quot;]
  H --&amp;gt;|&quot;no&quot;| EQ{&quot;equi-join?&quot;}
  EQ --&amp;gt;|&quot;no&quot;| NE[&quot;broadcast nested loop if a side is small,&amp;lt;br/&amp;gt;else cartesian if inner-like,&amp;lt;br/&amp;gt;else nested loop anyway&quot;]
  EQ --&amp;gt;|&quot;yes&quot;| B{&quot;a side small enough,&amp;lt;br/&amp;gt;and allowed as build side?&quot;}
  B --&amp;gt;|&quot;yes&quot;| BHJ[&quot;BroadcastHashJoin&quot;]
  B --&amp;gt;|&quot;no&quot;| SH{&quot;local hash map fits,&amp;lt;br/&amp;gt;much smaller,&amp;lt;br/&amp;gt;preferSortMergeJoin false?&quot;}
  SH --&amp;gt;|&quot;yes&quot;| SHJ[&quot;ShuffledHashJoin&quot;]
  SH --&amp;gt;|&quot;no&quot;| SM{&quot;keys sortable?&quot;}
  SM --&amp;gt;|&quot;yes&quot;| SMJ[&quot;SortMergeJoin&quot;]
  SM --&amp;gt;|&quot;no&quot;| C[&quot;cartesian if inner-like,&amp;lt;br/&amp;gt;else nested loop&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&quot;the-size-rules-exactly&quot;&gt;The size rules, exactly&lt;/h3&gt;

&lt;p&gt;Three predicates decide “small enough”, and only the first is widely known.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Broadcast.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;canBroadcastBySize&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sizeInBytes &amp;gt;= 0 &amp;amp;&amp;amp; sizeInBytes &amp;lt;=
autoBroadcastJoinThreshold&lt;/code&gt;, where the threshold is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10485760&lt;/code&gt;, which is 10 MB.&lt;/p&gt;

&lt;p&gt;There is a detail worth having: when the statistics are &lt;strong&gt;runtime&lt;/strong&gt; statistics,
meaning adaptive execution has measured a completed stage, Spark uses
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.autoBroadcastJoinThreshold&lt;/code&gt; instead, falling back to the
static one when that is unset. So plan-time and runtime broadcasting can be
governed by two different numbers.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Shuffle hash, ceiling.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;canBuildLocalHashMapBySize&lt;/code&gt; is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sizeInBytes &amp;lt; autoBroadcastJoinThreshold * numShufflePartitions&lt;/code&gt;. On the
defaults that is 10 MB multiplied by 200:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;10,485,760 x 200 = 2,097,152,000 bytes = ~2 GB
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;So the shuffle-hash ceiling is roughly 2 GB by default, two hundred times the
broadcast threshold, and it moves when you change the shuffle partition count.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Shuffle hash, ratio.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;muchSmaller&lt;/code&gt; is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;a.size * spark.sql.shuffledHashJoinFactor &amp;lt;= b.size&lt;/code&gt;, and that factor defaults
to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;3&lt;/code&gt;. The source explains why: “The cost to build hash map is higher than
sorting, we should only build hash map on a table that is much smaller than
other one.”&lt;/p&gt;

&lt;p&gt;So shuffle hash join requires &lt;strong&gt;three&lt;/strong&gt; things simultaneously: under the 2 GB
ceiling, at least three times smaller than the other side, and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;preferSortMergeJoin&lt;/code&gt; flipped to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;. That conjunction, with the last
condition off by default, is why it is rare in practice.&lt;/p&gt;

&lt;h3 id=&quot;which-build-side-each-join-type-allows&quot;&gt;Which build side each join type allows&lt;/h3&gt;

&lt;p&gt;This is the rule that makes broadcast joins look unpredictable, and it is pure
lookup, not heuristics.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Join type&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;Broadcast the left?&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;Broadcast the right?&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Inner, cross&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;Yes&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;Yes&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Left outer&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;Yes&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Right outer&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;&lt;strong&gt;Yes&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;&lt;strong&gt;No&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Full outer&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;&lt;strong&gt;No&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;&lt;strong&gt;No&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Left semi, left anti&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;No&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;Yes&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The logic follows from correctness. A left outer join must emit every left row,
so the left side has to be streamed and the right one built. A right outer is
the mirror image. A full outer must emit unmatched rows from &lt;strong&gt;both&lt;/strong&gt; sides, so
neither can be the build side, and &lt;strong&gt;no full outer join is ever a broadcast hash
join&lt;/strong&gt; regardless of how small either side is.&lt;/p&gt;

&lt;p&gt;Shuffle hash join is less restricted, which is why the strategy table says it
supports all join types: its build side may be the left for inner, left outer,
full outer and right outer, and the right for those plus left semi, left anti
and existence joins.&lt;/p&gt;

&lt;h2 id=&quot;hands-on-making-each-strategy-appear&quot;&gt;Hands-on: making each strategy appear&lt;/h2&gt;

&lt;p&gt;Every rule above is testable. This section builds two tables, forces all five
strategies, and reads back the one that actually executed. The transcripts below
are from Spark &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.0.2&lt;/code&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; set to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;8&lt;/code&gt;; the five
selection predicates quoted earlier are byte-identical in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4.2.0&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;A fact table and a dimension:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;functions&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;join-strategies&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;master&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;local[4]&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;config&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;8&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;600&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
                  &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;concat&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city-&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_name&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
                  &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;when&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;IN&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
                   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;when&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;US&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
                   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;otherwise&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;DE&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;country_code&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3_000_000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
                 &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;500&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
                 &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;90000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
                 &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4000&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;100.0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;decimal(10,2)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Two helpers. The first reports the strategy that ran, the second reports the size
Catalyst believes a side to be, which is the input to every size rule:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;plan&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;executedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;BroadcastHashJoin&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;ShuffledHashJoin&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;SortMergeJoin&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
                 &lt;span class=&quot;s&quot;&gt;&quot;BroadcastNestedLoopJoin&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;CartesianProduct&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]:&lt;/span&gt;
        &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;plan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
            &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;unknown&quot;&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;size_in_bytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;int&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;_jdf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;queryExecution&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;optimizedPlan&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;stats&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sizeInBytes&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;For these two tables the estimates are:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;size_in_bytes(cities) =     16,800 bytes
size_in_bytes(trips)  = 60,000,000 bytes
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trips&lt;/code&gt; references only the first 500 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_id&lt;/code&gt; values, so 100 of the 600 cities
have no trips. That matters for the outer joins later. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cities&lt;/code&gt; is far under the
10 MB broadcast threshold and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trips&lt;/code&gt; is far over it, so the defaults should
broadcast. Now force each strategy in turn:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# 1 broadcast hash join, the default here because cities is under the threshold
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# 2 sort-merge, by taking broadcasting away
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;-1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unset&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# 3 shuffle hash, by asking for it
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;hint&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SHUFFLE_HASH&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# 4 broadcast nested loop, by using an inequality instead of an equality
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# 5 cartesian, a deliberate cross product with broadcasting off
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;-1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;limit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;crossJoin&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;limit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;50&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unset&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The operator line each one produced:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;1  BroadcastHashJoin [city_id#6L], [city_id#1L], Inner, BuildRight, false
2  SortMergeJoin [city_id#6L], [city_id#1L], Inner
3  ShuffledHashJoin [city_id#6L], [city_id#1L], Inner, BuildRight
4  BroadcastNestedLoopJoin BuildRight, Inner, (city_id#6L &amp;gt; city_id#1L)
5  CartesianProduct
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;All five, from one dataset, by changing one thing at a time. Note that case 4
returned 748,500,000 rows from a 3,000,000-row table: an inequality against 600
cities is a near-cross-product, which is what the strategy name is telling you.&lt;/p&gt;

&lt;p&gt;Each of the four hints behaves as the procedure says, overriding the size rules
but not the join-type rules:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Hint&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Written as&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Strategy it produced&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cities.hint(&quot;BROADCAST&quot;)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastHashJoin&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cities.hint(&quot;MERGE&quot;)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoin&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SHUFFLE_HASH&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cities.hint(&quot;SHUFFLE_HASH&quot;)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffledHashJoin&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SHUFFLE_REPLICATE_NL&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cities.hint(&quot;SHUFFLE_REPLICATE_NL&quot;)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;A hint is the one lever that skips the size checks entirely, which is why
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt; on a large table moves the failure to the driver rather than
preventing it. The last row is the most destructive of the four: it printed
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct (city_id#5L = city_id#1L)&lt;/code&gt;, meaning the equality was demoted
from a join key to a filter applied after the cross product was built.&lt;/p&gt;

&lt;h3 id=&quot;the-join-type-overrides-the-size&quot;&gt;The join type overrides the size&lt;/h3&gt;

&lt;p&gt;The size rules are only consulted after the join type has had its say, and this
is where the surprises live. One rule sits underneath all of them: &lt;strong&gt;an outer
join must emit every row of its preserved side, so that side has to be streamed,
and the build side is always the other one.&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;A left outer preserves the left and therefore builds the right. A right outer
preserves the right and builds the left. A full outer preserves both, so it can
build neither. That is the entire build-side table, derived rather than
memorised.&lt;/p&gt;

&lt;p&gt;Four joins over the same two tables make the consequence concrete:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;right&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# preserves cities, the small side
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;# preserves cities as well
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;right&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# preserves trips, the large side
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;# preserves trips as well
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;trips  RIGHT JOIN cities  3,000,100 rows  SortMergeJoin     [city_id#6L], [city_id#1L], RightOuter
cities LEFT  JOIN trips   3,000,100 rows  SortMergeJoin     [city_id#1L], [city_id#6L], LeftOuter
cities RIGHT JOIN trips   3,000,000 rows  BroadcastHashJoin [city_id#1L], [city_id#6L], RightOuter, BuildLeft
trips  LEFT  JOIN cities  3,000,000 rows  BroadcastHashJoin [city_id#6L], [city_id#1L], LeftOuter,  BuildRight
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;The first two lines are one query written two ways. Both preserve &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cities&lt;/code&gt;, both
return 3,000,100 rows, and comparing them row by row finds no difference in
either direction. Both sort-merge. The last two are also one query written two
ways, both preserve &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trips&lt;/code&gt;, and both broadcast.&lt;/p&gt;

&lt;p&gt;The two pairs, though, are not the same query. They differ by exactly the 100
cities that have no trips. That is the trap in the advice usually given here:
&lt;strong&gt;swapping the argument order of an outer join changes which side is preserved,
so it changes the answer.&lt;/strong&gt; It is a join-type change wearing the costume of a
refactor, and it is the one rewrite that can quietly drop rows.&lt;/p&gt;

&lt;p&gt;So the honest rule is narrower than “swap the sides”:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;If you must preserve the &lt;strong&gt;large&lt;/strong&gt; side, the build side is the small one, and the broadcast comes for free.&lt;/li&gt;
  &lt;li&gt;If you must preserve the &lt;strong&gt;small&lt;/strong&gt; side, the build side is the large one, and &lt;strong&gt;no threshold, hint or reordering produces a broadcast hash join&lt;/strong&gt;. Sort-merge is the correct plan, and the lever you have is reducing the large side, not persuading the optimiser.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Two more results from the same tables close the picture:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;trips FULL JOIN cities           3,000,100 rows  SortMergeJoin     ... FullOuter
trips LEFT ANTI JOIN cities              0 rows  BroadcastHashJoin ... LeftAnti, BuildRight
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;A full outer preserves both sides, so it sort-merges however small &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cities&lt;/code&gt; is.
A left anti preserves the left and returns no columns from the right at all, so
the right is always available as the build side and it broadcasts. It returns
zero rows here because every trip does have a matching city, which is the
correct answer to “trips whose city is missing”.&lt;/p&gt;

&lt;h3 id=&quot;why-shuffle-hash-join-is-rare-demonstrated&quot;&gt;Why shuffle hash join is rare, demonstrated&lt;/h3&gt;

&lt;p&gt;The three conditions are easier to believe when you watch them fail one at a
time. Take a 400,000-row &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;riders&lt;/code&gt; table against &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trips&lt;/code&gt; on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rider_id&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;riders&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;400_000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
                  &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;concat&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider-&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_name&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;attempt&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;threshold&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;prefer_smj&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;str&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;threshold&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.join.preferSortMergeJoin&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;prefer_smj&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;strategy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;riders&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;attempt&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;-&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;          &lt;span class=&quot;s&quot;&gt;&quot;false&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# the usual &quot;disable broadcast&quot; trick
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;attempt&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1024&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;false&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# broadcast still off, but a positive threshold
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;attempt&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1024&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;true&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;# preferSortMergeJoin back at its default
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;With &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;riders&lt;/code&gt; at 7,200,000 bytes and the projected &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trips&lt;/code&gt; at 36,000,000:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Attempt&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;canBroadcastBySize&lt;/code&gt;&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Hash-map ceiling&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;canBuildLocalHashMapBySize&lt;/code&gt;&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;muchSmaller&lt;/code&gt;&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;preferSortMergeJoin&lt;/code&gt;&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Strategy&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;threshold = -1&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;false&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1 x 8 = -8&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;&lt;strong&gt;false&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;true&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;false&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoin&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;threshold = 2 MB&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;false&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;2 MB x 8 = 16 MB&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;true&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;true&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;false&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffledHashJoin&lt;/code&gt;&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;threshold = 2 MB&lt;/code&gt;, prefer on&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;false&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;2 MB x 8 = 16 MB&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;true&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;true&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;&lt;strong&gt;true&lt;/strong&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoin&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The middle row is a shuffle hash join with &lt;strong&gt;no hint at all&lt;/strong&gt;, which is the thing
most people have never seen. The other two rows each break one condition.&lt;/p&gt;

&lt;p&gt;The first row deserves its own warning, because it is a trap in the most common
tuning gesture there is. &lt;strong&gt;Setting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; does not
just disable broadcasting, it also makes shuffle hash join unreachable&lt;/strong&gt;, since
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;canBuildLocalHashMapBySize&lt;/code&gt; multiplies that same threshold by the shuffle
partition count and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1 x 8&lt;/code&gt; is negative. Every size below zero fails the test.
If you want to stop broadcasting but keep shuffle hash join available, set the
threshold to a small positive number instead of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt;.&lt;/p&gt;

&lt;h2 id=&quot;what-adaptive-execution-changes&quot;&gt;What adaptive execution changes&lt;/h2&gt;

&lt;p&gt;Everything above is plan-time. Adaptive query execution re-plans after a shuffle
stage completes, when the sizes are measurements rather than estimates, and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.enabled&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;For joins it does two things:&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;strong&gt;Demotes a sort-merge join to a broadcast hash join&lt;/strong&gt; when a side turns out small enough, using &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.autoBroadcastJoinThreshold&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Splits skewed partitions&lt;/strong&gt;, with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.enabled&lt;/code&gt; default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;.&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;The skew thresholds are strict and worth knowing, because they explain “skew
handling is on but nothing happened”. A partition is treated as skewed only when
it is larger than &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.skewedPartitionFactor&lt;/code&gt;, default
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;5.0&lt;/code&gt;, times the median &lt;strong&gt;and&lt;/strong&gt; larger than
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;256MB&lt;/code&gt;.
On a job whose partitions are all under 256 MB, it never fires no matter how
uneven they are.&lt;/p&gt;

&lt;p&gt;The practical consequence for reading plans: &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; shows the planned
shape, not the final one.&lt;/strong&gt; Before execution the tree is wrapped in
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AdaptiveSparkPlan&lt;/code&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=false&lt;/code&gt;. The SQL tab of the UI after the
query finishes shows what actually ran.&lt;/p&gt;

&lt;h2 id=&quot;reading-the-plan&quot;&gt;Reading the plan&lt;/h2&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;formatted&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Here is the real output for the broadcast join above, projected down to three
columns. The tree comes first, then a numbered detail block per operator:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;== Physical Plan ==
AdaptiveSparkPlan (9)
+- Project (8)
   +- BroadcastHashJoin Inner BuildRight (7)
      :- Project (3)
      :  +- Filter (2)
      :     +- Range (1)
      +- BroadcastExchange (6)
         +- Project (5)
            +- Range (4)

(6) BroadcastExchange
Input [2]: [city_id#1L, city_name#2]
Arguments: HashedRelationBroadcastMode(List(input[0, bigint, false]),false), [plan_id=28]

(7) BroadcastHashJoin
Left keys [1]: [city_id#6L]
Right keys [1]: [city_id#1L]
Join type: Inner
Join condition: None

(9) AdaptiveSparkPlan
Output [3]: [trip_id#5L, city_name#2, fare_amount#8]
Arguments: isFinalPlan=false
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Four things in that output are worth reading deliberately. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BuildRight&lt;/code&gt; on node
7 names the build side. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastExchange&lt;/code&gt; on node 6 appears under the right
branch only, so the left branch has no exchange and the large side is not
shuffled. Node 6 lists &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_id&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_name&lt;/code&gt; but not &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;country_code&lt;/code&gt;, because
the projection pushed down and shrank what gets broadcast. And node 9 says
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=false&lt;/code&gt;, so this is the plan before adaptive execution has had a say.&lt;/p&gt;

&lt;p&gt;What to look for, in order of how much it tells you:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;In the plan&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Meaning&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastHashJoin&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The small side is being broadcast. Usually what you want&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoin&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Both sides shuffled and sorted. Correct for large-to-large&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffledHashJoin&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Rare without a hint&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastNestedLoopJoin&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Non-equi, or a missing condition&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Almost always a mistake&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange hashpartitioning(k, 200)&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The shuffle, with its key and partition count&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastExchange&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The broadcast being built&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReusedExchange&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;One shuffle serving two branches. Good news&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=false&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;AQE has not re-planned yet&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;A useful habit: count the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt; nodes. That is the number of shuffles, and
if two consecutive operations key on the same column, one exchange can serve
both.&lt;/p&gt;

&lt;h2 id=&quot;which-strategy-for-which-scenario&quot;&gt;Which strategy for which scenario&lt;/h2&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Scenario&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Aim for&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;How&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Large fact, small dimension&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast hash&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Nothing, if the dimension is under 10 MB. Filter and project it first if not&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Large fact, medium dimension (tens of MB)&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast hash&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Reduce it first, then raise the threshold deliberately if it is genuinely small in memory&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Large to large, recurring&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge, or no shuffle at all&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Bucket both tables on the key with the same bucket count&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Large to large, one-off&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Filter and project both sides, size the shuffle, handle skew&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Full outer with a small side&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge, unavoidable&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Accept it, or restructure as two outer joins and a union&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Outer join preserving the large side&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast hash&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Nothing. The build side is the small one already&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Outer join preserving the small side&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge, unavoidable&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The preserved side cannot be built. Reduce the other side instead&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Range or inequality condition&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Broadcast nested loop&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Add an equality that buckets both sides, so the nested loop runs within buckets&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Existence check&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Semi or anti join&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;left_semi&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;left_anti&lt;/code&gt;, never inner plus &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Skewed join key&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Sort-merge plus skew handling&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;AQE first, then salt the hot key&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Two of those need expanding.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;The range join.&lt;/strong&gt; A condition like &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ON t.ts BETWEEN d.start AND d.end&lt;/code&gt; has no
equality, so Spark can only nested-loop it. The standard fix is to manufacture a
key: bucket both sides by a coarser value, join on that equality &lt;strong&gt;and&lt;/strong&gt; the
range, so the comparison happens within each bucket rather than across the whole
product.&lt;/p&gt;

&lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;day&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;to_date&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;ts&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;windows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;day&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;to_date&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;start&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)),&lt;/span&gt;
         &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;day&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;                                    &lt;span class=&quot;c1&quot;&gt;# an equality to partition on
&lt;/span&gt;   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;ts&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;start&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;amp;&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;ts&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;lt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;end&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;The full outer with a small side.&lt;/strong&gt; If broadcasting matters more than the
single scan, a full outer can be rewritten as a left outer union an anti join
the other way, both of which &lt;em&gt;can&lt;/em&gt; broadcast. It is more code and two passes, so
only worth it when the sort-merge is genuinely the bottleneck.&lt;/p&gt;

&lt;h2 id=&quot;common-misconceptions&quot;&gt;Common misconceptions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;“Raising &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; will make this broadcast.”&lt;/strong&gt; Not if the
join type forbids it. A full outer join can never be a broadcast hash join,
because both sides must emit unmatched rows and neither may be the build side.
No threshold changes that. Check the join type before the size.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Swapping the sides of an outer join is a free optimisation.”&lt;/strong&gt; It changes the
answer. An outer join preserves one side, and swapping the argument order swaps
which side that is. Measured on 600 cities where 100 have no trips, the two
orderings differ by exactly those 100 rows, while returning the same count on
data where everything matches, which is how the bug hides.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“Setting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; only turns off broadcasting.”&lt;/strong&gt;
It also removes shuffle hash join from consideration. The shuffle-hash ceiling
is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold * spark.sql.shuffle.partitions&lt;/code&gt;, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; makes it
negative and every candidate fails the size test. Use a small positive value if
you want to stop broadcasting and keep the option.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;“A different join strategy can change my results.”&lt;/strong&gt; It cannot. Strategies are
algorithms for the same semantics. If the rows changed, the join &lt;em&gt;type&lt;/em&gt; or the
condition changed, or nulls in the key are behaving as SQL says they should.&lt;/p&gt;

&lt;h2 id=&quot;production-tips&quot;&gt;Production tips&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Read the plan before tuning.&lt;/strong&gt; The strategy Spark chose tells you which rule fired, and therefore which lever exists.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Reduce before joining.&lt;/strong&gt; Filtering and projecting a side is the highest-value change available, because it can move the join into the broadcast band and change the strategy entirely.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Do not hint &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt; on something large.&lt;/strong&gt; A hint overrides the size check, so you move the failure to the driver, which must collect it first.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Prefer a small positive &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; over &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt;.&lt;/strong&gt; Setting it to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; also drives the shuffle-hash ceiling negative, removing that strategy from consideration along with broadcasting.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Treat a full outer join as unbroadcastable.&lt;/strong&gt; Raising the threshold will never help; the join type is what forbids it.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;For an outer join, ask which side is preserved, not which side is small.&lt;/strong&gt; The build side is always the non-preserved one, so an outer join that must preserve the small side cannot broadcast at any threshold. Reordering the arguments changes the result, not just the plan.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Fix the estimate rather than forcing the plan&lt;/strong&gt; where you can, with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ANALYZE TABLE ... COMPUTE STATISTICS&lt;/code&gt;, so Catalyst chooses correctly on its own.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Bucket the keys you join on repeatedly.&lt;/strong&gt; It is the only way to remove the shuffle from a large-to-large join permanently.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Trust the SQL tab over &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt;&lt;/strong&gt; for what actually ran, since AQE re-plans after the shuffle.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Why is my small table not being broadcast?&lt;/strong&gt;
Four common reasons: the estimate is of the unfiltered table because the filter
did not push down; the table is compressed on disk and larger in memory than you
think; there are no statistics so Spark fell back to file size; or the join type
forbids that side as the build side. Check the last one first, because no config
change fixes it.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Does a bigger &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold&lt;/code&gt; make joins faster?&lt;/strong&gt;
Sometimes, and it moves the failure mode from slow to fragile. Every executor
holds a full copy and the driver assembles it first, so a 500 MB broadcast across
50 executors is 25 GB of cluster memory plus a driver that must hold it. Raise it
deliberately, with a number you can justify.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why do I almost never see &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffledHashJoin&lt;/code&gt;?&lt;/strong&gt;
Because it needs three conditions at once and one of them,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.join.preferSortMergeJoin&lt;/code&gt; being &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;, is not the default. Spark
prefers sort-merge because it cannot run out of memory, while a hash join can.
There is also a trap: if you disabled broadcasting with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;autoBroadcastJoinThreshold = -1&lt;/code&gt;, the shuffle-hash ceiling became negative too,
so the strategy cannot be chosen without a hint no matter what else you set. The
table above shows the same join picking it once the threshold is a small
positive number.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Can a join strategy change my results?&lt;/strong&gt;
No. Strategies are algorithms for the same semantics. If results changed, the
join &lt;strong&gt;type&lt;/strong&gt; or the condition changed, or there are nulls in the key behaving
as SQL says they should.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why is my join a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct&lt;/code&gt; when I wrote a condition?&lt;/strong&gt;
Because Catalyst could not use the condition as an equality: it is an
inequality, it is wrapped in an expression it cannot match on, or it sits in a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;where&lt;/code&gt; after a cross join in a way that could not be pushed into the join.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;What about joining on a collated string column?&lt;/strong&gt;
In Spark 4 this matters. Hash joins require keys that are binary-stable, and a
case- or accent-insensitive collation is not, because two rows can be equal
without having equal bytes. Joining on such a column disables hash joins and
leaves sort-merge.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;The thing worth taking away is that join selection is a &lt;strong&gt;lookup, not a
judgement&lt;/strong&gt;. Spark asks, in a fixed order: is there a hint, is this an
equi-join, is one side small enough, does the join type permit that side as the
build side, are the keys sortable. The first rule that fits wins. Nothing about
it is adaptive except the parts AQE re-plans after a shuffle.&lt;/p&gt;

&lt;p&gt;That reframes tuning. You are not persuading an optimiser, you are changing the
inputs to a decision procedure. Filtering a dimension until it fits under 10 MB
changes the answer to “is one side small enough”. Deciding which side an outer
join preserves settles which side may be built, though it changes the result
too, so it is a modelling decision rather than a tuning knob. Bucketing changes
whether a shuffle is needed at all. Each of those is a specific rule with a
specific lever.&lt;/p&gt;

&lt;p&gt;And three answers are simply no. A full outer join will not broadcast, a
non-equi join will not hash, and an outer join that must preserve its small side
will not broadcast either. When you hit those, the fix is to change the query
shape, or to accept the plan and shrink the other side, rather than to keep
raising thresholds.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/core/src/main/scala/org/apache/spark/sql/execution/SparkStrategies.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkStrategies.scala&lt;/code&gt;&lt;/a&gt;, whose &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;JoinSelection&lt;/code&gt; comment documents the five strategies and the order they are tried in&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/spark/blob/master/sql/catalyst/src/main/scala/org/apache/spark/sql/catalyst/optimizer/joins.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;joins.scala&lt;/code&gt;&lt;/a&gt; for the size predicates and the build-side rules per join type&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/sql-performance-tuning.html&quot;&gt;SQL performance tuning&lt;/a&gt; for join hints and adaptive query execution&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheSparkArchitecture/&quot;&gt;Apache Spark architecture&lt;/a&gt; for the shuffle and stage machinery these strategies sit on&lt;/li&gt;
  &lt;li&gt;Two community treatments this post builds on: &lt;a href=&quot;https://medium.com/@ongchengjie/different-types-of-spark-join-strategies-997671fbf6b0&quot;&gt;Different types of Spark join strategies&lt;/a&gt; for separating data exchange from join algorithm, and &lt;a href=&quot;https://medium.com/@amarkrgupta96/join-strategies-in-apache-spark-a-hands-on-approach-d0696fc0a6c9&quot;&gt;Join strategies in Apache Spark, a hands-on approach&lt;/a&gt; for forcing each strategy on one dataset&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Hive, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="Joins" /><category term="Catalyst" /><category term="Performance" /><category term="Internals" /><summary type="html">Types and strategies are different things Architecture: what every join is made of Why there are five, and not six The five strategies Broadcast hash join Shuffle hash join Sort-merge join Broadcast nested loop join Cartesian product How does Spark actually choose? The size rules, exactly Which build side each join type allows Hands-on: making each strategy appear The join type overrides the size Why shuffle hash join is rare, demonstrated What adaptive execution changes Reading the plan Which strategy for which scenario Common misconceptions Production tips Frequently asked questions Conclusion References Trademarks TL;DR A join type is semantics (inner, outer, semi, anti). A join strategy is the algorithm (broadcast hash, shuffle hash, sort-merge, broadcast nested loop, cartesian). Mixing the two words up is why join tuning feels arbitrary. Spark’s choice is a documented, ordered procedure in JoinSelection, not a cost model. Hints first, then broadcast, then shuffle hash, then sort-merge, then cartesian, then nested loop “as the final solution”. The join type silently restricts the strategy. A full outer join can never be broadcast, and for a right outer join only the left side may be. Both are confirmed below by running them. Shuffle hash join needs three conditions at once, one of which is off by default, which is why you almost never see it without a hint. The 10 MB threshold is not the only size rule. Shuffle hash join has its own ceiling of autoBroadcastJoinThreshold multiplied by spark.sql.shuffle.partitions, which is 2 GB on the defaults, and a side effect: setting that threshold to -1 disables shuffle hash join along with broadcasting. Join tuning has a reputation for being folklore. Broadcast the small side, raise the threshold, add a hint, and if none of that works, salt the key. It works often enough to feel like knowledge and fails often enough to feel like luck. The reason is that most of what circulates describes the five strategies without describing the procedure that picks between them. That procedure is not mysterious: it is written down, in order, in a comment above JoinSelection in Spark’s own source, and once you have read it the behaviour stops being surprising. A full outer join that refuses to broadcast is not a bug, and no amount of raising the threshold will change it. This post has three parts. What each strategy actually does, the exact order Spark tries them in, and which scenario should get which. Types and strategies are different things This is the distinction to get right first, because the two are chosen independently and the first one constrains the second.   Join type Join strategy Decides Which rows appear in the result How the rows are brought together You write it Yes, it is your query’s meaning No, Catalyst picks it Examples inner, left outer, full outer, left semi, left anti, cross broadcast hash, shuffle hash, sort-merge, broadcast nested loop, cartesian Changes results Yes Never A strategy change can make a query ten times faster or fail outright, and it cannot change the answer. A type change always changes the answer. So when someone says “we changed the join and it got faster”, the useful question is which of the two they changed. The types, briefly, since the rest of the post assumes them: trips.join(cities, &quot;city_id&quot;) # inner, the default trips.join(cities, &quot;city_id&quot;, &quot;left&quot;) # left outer trips.join(cities, &quot;city_id&quot;, &quot;full&quot;) # full outer trips.join(cities, &quot;city_id&quot;, &quot;left_semi&quot;) # left rows that match trips.join(cities, &quot;city_id&quot;, &quot;left_anti&quot;) # left rows that do not trips.crossJoin(cities) # every pair Semi and anti deserve a word because they are under-used. Both filter the left side by whether a match exists and return no columns from the right, so a left row matching a hundred right rows is emitted once. An inner join in the same position emits it a hundred times and needs a distinct afterwards, which is a second shuffle. Architecture: what every join is made of All five strategies are assembled from the same three parts, and naming them makes the differences between the strategies easy to hold. flowchart TB subgraph P[&quot;every join has these&quot;] A[&quot;&amp;lt;b&amp;gt;build side&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;materialised into a lookup&amp;lt;br/&amp;gt;structure, or sorted&quot;] B[&quot;&amp;lt;b&amp;gt;stream side&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;read once and probed&amp;lt;br/&amp;gt;against the build side&quot;] C[&quot;&amp;lt;b&amp;gt;co-location step&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;how matching keys are made&amp;lt;br/&amp;gt;to meet on one executor&quot;] end A --&amp;gt; J[&quot;the join operator&quot;] B --&amp;gt; J C --&amp;gt; J The build side is the one Spark materialises: a hash table for the hash joins, a sorted run for sort-merge. It is the side that costs memory, which is why “which side is the build side” is the question behind most join failures. The stream side is read once and probed. It costs nothing to hold, which is why you want the large table here. The co-location step is where the strategies genuinely differ, and it is the expensive part: Strategy How keys are co-located Network cost Broadcast hash Copy the whole build side everywhere One copy per executor Shuffle hash Shuffle both sides by key Full shuffle Sort-merge Shuffle both sides by key, then sort Full shuffle plus sort Broadcast nested loop Copy one side everywhere, compare all pairs One copy per executor Cartesian Replicate partitions across each other Every partition pair Read down that column and the whole subject compresses to one trade: either you copy the small side to where the data already is, or you move both sides to a common place. Broadcasting does the first, the shuffle-based strategies do the second, and bucketing is how you pay for the second once at write time instead of on every read. Why there are five, and not six The five strategies are not five unrelated algorithms. They are combinations of two independent choices, and separating them is the clearest way to hold the whole subject. Choice one, the data exchange: how matching keys are made to meet on one executor. Broadcast. Copy one side to every executor. The other side never moves. Shuffle. Repartition both sides by the join key so matching keys land in the same partition. Choice two, the join algorithm: what happens once the rows are together. Hash. Build a hash table from one side, probe it with the other. Sort-merge. Sort both sides, walk them with a cursor each. Nested loop. Compare every pair, evaluating an arbitrary condition. Two exchanges times three algorithms is six combinations. Spark ships five physical operators, and the grid shows exactly which cell is missing:   Hash Sort-merge Nested loop Broadcast BroadcastHashJoinExec does not exist BroadcastNestedLoopJoinExec Shuffle ShuffledHashJoinExec SortMergeJoinExec CartesianProductExec Why there is no broadcast sort-merge join is worth a moment, because it explains what sorting is actually for. Sorting buys bounded memory: you never hold a whole side, and the sort can spill. But if a side has already been copied to every executor, it is in memory by definition, so a hash table over it costs nothing extra and gives constant-time probes. Sorting both sides to get the same answer more slowly has no upside. Sorting only earns its place when you had to shuffle anyway and cannot afford to hold a side in memory. So the decision Spark makes is really two decisions: flowchart TB A[&quot;can one side be copied&amp;lt;br/&amp;gt;to every executor?&quot;] A --&amp;gt;|&quot;yes&quot;| B{&quot;equi-join?&quot;} A --&amp;gt;|&quot;no&quot;| C{&quot;equi-join?&quot;} B --&amp;gt;|&quot;yes&quot;| D[&quot;BroadcastHashJoin&quot;] B --&amp;gt;|&quot;no&quot;| E[&quot;BroadcastNestedLoopJoin&quot;] C --&amp;gt;|&quot;yes&quot;| F{&quot;hold a side in memory&amp;lt;br/&amp;gt;per partition?&quot;} C --&amp;gt;|&quot;no&quot;| G[&quot;CartesianProduct&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;inner-like only&amp;lt;/i&amp;gt;&quot;] F --&amp;gt;|&quot;yes&quot;| H[&quot;ShuffledHashJoin&quot;] F --&amp;gt;|&quot;no&quot;| I[&quot;SortMergeJoin&quot;] Read it as: the exchange is decided by size, and the algorithm by the condition and by how much memory you can hold. An equi-join can hash or sort-merge; a non-equi join has only nested loop, which is why it collapses to two options. That single sentence predicts most of what the planner does. The five strategies Spark’s source documents all five with their limits. The table below is that comment, condensed: Strategy Equi-join only Keys must be sortable Join types Broadcast hash join Yes No All except full outer Shuffle hash join Yes No All Sort-merge join Yes Yes All Broadcast nested loop No No All, but efficient only in some combinations Cartesian product No No Inner-like only An equi-join is one whose condition is equality on keys, a.id = b.id. Anything else, a range or an inequality, is a non-equi join, and only the bottom two rows can execute it at all. That single fact explains most surprises in the plan. Broadcast hash join Spark collects the small side to the driver, sends a copy to every executor, and each executor builds a hash table from it. Each partition of the large side then probes that table locally. The large side is never shuffled. flowchart TB S[&quot;small side: cities&quot;] --&amp;gt; D[&quot;driver collects it&quot;] D --&amp;gt; BC[(&quot;broadcast:&amp;lt;br/&amp;gt;one copy per executor&quot;)] BC --&amp;gt; E1[&quot;executor 1&amp;lt;br/&amp;gt;hash table&quot;] BC --&amp;gt; E2[&quot;executor 2&amp;lt;br/&amp;gt;hash table&quot;] BC --&amp;gt; E3[&quot;executor 3&amp;lt;br/&amp;gt;hash table&quot;] T1[&quot;trips partition 0&amp;lt;br/&amp;gt;does not move&quot;] --&amp;gt; E1 T2[&quot;trips partition 1&amp;lt;br/&amp;gt;does not move&quot;] --&amp;gt; E2 T3[&quot;trips partition 2&amp;lt;br/&amp;gt;does not move&quot;] --&amp;gt; E3 E1 --&amp;gt; R[&quot;output, no shuffle&amp;lt;br/&amp;gt;of the large side&quot;] E2 --&amp;gt; R E3 --&amp;gt; R Cost: one copy of the small side per executor, held in memory for the duration, plus the driver having to assemble it first. That driver step is the part people forget. A broadcast that is too large fails on the driver, not the executors, and the error often arrives as a spark.sql.broadcastTimeout after the default 300 seconds rather than as an obvious out-of-memory. Key points Build side: the small one, if canBroadcastBySize passes and the join type allows that side. Network: one copy of the small side per executor. The large side never moves. Memory: the hash table sits on every executor, and on the driver first. Wins when: one side is genuinely small after filters and projections are applied. Fails when: the small side is not small, or the join type forbids that build side. In the plan: BroadcastHashJoin with a BroadcastExchange child. Shuffle hash join Both sides are shuffled on the join key. Then, for each partition, Spark builds a hash table from the smaller side and probes it with the larger. No sorting. flowchart TB A[&quot;riders&amp;lt;br/&amp;gt;the smaller side&quot;] --&amp;gt; SH[(&quot;shuffle both sides&amp;lt;br/&amp;gt;by the join key&quot;)] B[&quot;trips&amp;lt;br/&amp;gt;the larger side&quot;] --&amp;gt; SH SH --&amp;gt; P0[&quot;partition 0&amp;lt;br/&amp;gt;build hash from riders&amp;lt;br/&amp;gt;probe with trips&quot;] SH --&amp;gt; P1[&quot;partition 1&amp;lt;br/&amp;gt;build hash from riders&amp;lt;br/&amp;gt;probe with trips&quot;] SH --&amp;gt; P2[&quot;partition 2&amp;lt;br/&amp;gt;build hash from riders&amp;lt;br/&amp;gt;probe with trips&quot;] P0 --&amp;gt; O[&quot;output, never sorted&quot;] P1 --&amp;gt; O P2 --&amp;gt; O It sits in a narrow band: one side too big to broadcast, but small enough that a per-partition hash table fits. It skips the sort that sort-merge pays, and in exchange it can run out of memory where sort-merge would spill. Key points Build side: the smaller side, per partition, not the whole table. Network: a full shuffle of both sides, the same as sort-merge. Memory: one hash table per partition, which is what can blow up. Wins when: the build side is much smaller but still too big to broadcast, and the sort is the thing you want to skip. Fails when: a partition’s build side does not fit, since a hash table cannot spill the way a sort can. In the plan: ShuffledHashJoin with an Exchange hashpartitioning under each side and no Sort node. Sort-merge join Both sides are shuffled on the key, each partition is sorted, and the two sorted streams are walked together with a cursor on each. flowchart TB A[&quot;trips&quot;] --&amp;gt; S[(&quot;shuffle both sides&amp;lt;br/&amp;gt;by the join key&quot;)] B[&quot;cities&quot;] --&amp;gt; S S --&amp;gt; S0[&quot;partition 0&amp;lt;br/&amp;gt;sort both sides&quot;] S --&amp;gt; S1[&quot;partition 1&amp;lt;br/&amp;gt;sort both sides&quot;] S0 --&amp;gt; M0[&quot;merge with one&amp;lt;br/&amp;gt;cursor per side&quot;] S1 --&amp;gt; M1[&quot;merge with one&amp;lt;br/&amp;gt;cursor per side&quot;] M0 --&amp;gt; O[&quot;output, sorted by key&quot;] M1 --&amp;gt; O The merge itself is the cheap part. Each side has a cursor, and whichever cursor points at the smaller key advances: left (sorted): a a b c d ... right (sorted): a b b d e ... ^ ^ Its virtue is bounded memory. Only the merge buffers are held, not a whole hash table, and the sort can spill, so a partition larger than memory still finishes. That is why it is the default for large-to-large joins, and why spark.sql.join.preferSortMergeJoin defaults to true. The cost is the sort, and the requirement is that the keys are sortable. It is the only strategy with that constraint. Key points Build side: neither, in the hash-table sense. Both sides are sorted and streamed. Network: a full shuffle of both sides, plus a sort per partition. Memory: bounded, and the sort spills to disk rather than failing. Wins when: both sides are large, which is why it is the default. Fails when: rarely, and not on memory. Its failure mode is slowness from the shuffle and the sort, or one skewed partition, rather than an out-of-memory. In the plan: SortMergeJoin with a Sort node above an Exchange on each side. Broadcast nested loop join One side is broadcast, and then every row of one side is compared against every row of the other, evaluating an arbitrary condition. It is the fallback that makes non-equi joins possible at all. The source notes it is optimised for three specific shapes, and slow otherwise: Broadcasting the left side in a right outer join. Broadcasting the right side in a left outer, left semi, left anti or existence join. Broadcasting either side in an inner-like join. flowchart TB S[&quot;one side, broadcast whole&quot;] --&amp;gt; E1[&quot;executor 1&quot;] S --&amp;gt; E2[&quot;executor 2&quot;] P1[&quot;partition of the other side&quot;] --&amp;gt; E1 P2[&quot;partition of the other side&quot;] --&amp;gt; E2 E1 --&amp;gt; C1[&quot;compare every local row&amp;lt;br/&amp;gt;against every broadcast row&quot;] E2 --&amp;gt; C2[&quot;compare every local row&amp;lt;br/&amp;gt;against every broadcast row&quot;] C1 --&amp;gt; O[&quot;rows where the&amp;lt;br/&amp;gt;condition holds&quot;] C2 --&amp;gt; O Outside those, “we need to scan the data multiple times, which can be rather slow”. Seeing BroadcastNestedLoopJoin on two large inputs means the query is doing something close to a cross product. Key points Build side: the broadcast one, and only the shapes listed above are efficient. Network: one copy of the broadcast side per executor. Cost: the row comparisons, which grow as the product of the two row counts. Wins when: the condition is not an equality and one side is genuinely tiny. Fails when: both sides are large, where it is the “may OOM” last resort. In the plan: BroadcastNestedLoopJoin with the condition printed inline. Cartesian product Also called shuffle-and-replicate nested loop. Every partition of one side is paired with every partition of the other, producing the full cross product. It supports inner-like joins only, and it is what you get from a deliberate crossJoin once broadcasting is off the table. flowchart TB A0[&quot;A partition 0&quot;] --&amp;gt; X[&quot;every A partition paired&amp;lt;br/&amp;gt;with every B partition&quot;] A1[&quot;A partition 1&quot;] --&amp;gt; X B0[&quot;B partition 0&quot;] --&amp;gt; X B1[&quot;B partition 1&quot;] --&amp;gt; X X --&amp;gt; P00[&quot;A0 x B0&quot;] X --&amp;gt; P01[&quot;A0 x B1&quot;] X --&amp;gt; P10[&quot;A1 x B0&quot;] X --&amp;gt; P11[&quot;A1 x B1&quot;] With two partitions a side that is four tasks. With 200 a side it is 40,000, and that multiplication is the whole reason this strategy is a warning sign. Key points Build side: neither. Partitions are replicated against each other. Network: every partition pair, so the shuffle grows as the product of the partition counts. Cost: the output itself, which is the product of the two row counts. Wins when: you actually want a cross product, on small inputs. Fails when: you did not want one, which is the usual case. In the plan: CartesianProduct, almost always worth investigating. How does Spark actually choose? Here is the part that turns folklore into a procedure. JoinSelection follows a fixed order, and the first applicable rule wins. For an equi-join, hints are consulted first, in this order: BROADCAST: pick broadcast hash join if the join type supports it. MERGE: pick sort-merge join if the keys are sortable. SHUFFLE_HASH: pick shuffle hash join if the join type supports it. SHUFFLE_REPLICATE_NL: pick cartesian product if the join type is inner-like. With no hints, for an equi-join: Broadcast hash join, if one side is small enough to broadcast and the join type allows that side to be the build side. Shuffle hash join, if one side can build a local hash map, is much smaller than the other, and spark.sql.join.preferSortMergeJoin is false. Sort-merge join, if the join keys are sortable. Cartesian product, if the join type is inner-like. Broadcast nested loop join as the last resort. The source is blunt about it: “It may OOM but we don’t have other choice.” For a non-equi join the list is much shorter, which is why these are so often slow: Broadcast nested loop join, if one side is small enough to broadcast. Cartesian product, if the join type is inner-like. Broadcast nested loop join anyway. flowchart TB H{&quot;a join hint?&quot;} --&amp;gt;|&quot;yes&quot;| HH[&quot;honour it if the type&amp;lt;br/&amp;gt;and keys allow&quot;] H --&amp;gt;|&quot;no&quot;| EQ{&quot;equi-join?&quot;} EQ --&amp;gt;|&quot;no&quot;| NE[&quot;broadcast nested loop if a side is small,&amp;lt;br/&amp;gt;else cartesian if inner-like,&amp;lt;br/&amp;gt;else nested loop anyway&quot;] EQ --&amp;gt;|&quot;yes&quot;| B{&quot;a side small enough,&amp;lt;br/&amp;gt;and allowed as build side?&quot;} B --&amp;gt;|&quot;yes&quot;| BHJ[&quot;BroadcastHashJoin&quot;] B --&amp;gt;|&quot;no&quot;| SH{&quot;local hash map fits,&amp;lt;br/&amp;gt;much smaller,&amp;lt;br/&amp;gt;preferSortMergeJoin false?&quot;} SH --&amp;gt;|&quot;yes&quot;| SHJ[&quot;ShuffledHashJoin&quot;] SH --&amp;gt;|&quot;no&quot;| SM{&quot;keys sortable?&quot;} SM --&amp;gt;|&quot;yes&quot;| SMJ[&quot;SortMergeJoin&quot;] SM --&amp;gt;|&quot;no&quot;| C[&quot;cartesian if inner-like,&amp;lt;br/&amp;gt;else nested loop&quot;] The size rules, exactly Three predicates decide “small enough”, and only the first is widely known. Broadcast. canBroadcastBySize is sizeInBytes &amp;gt;= 0 &amp;amp;&amp;amp; sizeInBytes &amp;lt;= autoBroadcastJoinThreshold, where the threshold is spark.sql.autoBroadcastJoinThreshold, default 10485760, which is 10 MB. There is a detail worth having: when the statistics are runtime statistics, meaning adaptive execution has measured a completed stage, Spark uses spark.sql.adaptive.autoBroadcastJoinThreshold instead, falling back to the static one when that is unset. So plan-time and runtime broadcasting can be governed by two different numbers. Shuffle hash, ceiling. canBuildLocalHashMapBySize is sizeInBytes &amp;lt; autoBroadcastJoinThreshold * numShufflePartitions. On the defaults that is 10 MB multiplied by 200: 10,485,760 x 200 = 2,097,152,000 bytes = ~2 GB So the shuffle-hash ceiling is roughly 2 GB by default, two hundred times the broadcast threshold, and it moves when you change the shuffle partition count. Shuffle hash, ratio. muchSmaller is a.size * spark.sql.shuffledHashJoinFactor &amp;lt;= b.size, and that factor defaults to 3. The source explains why: “The cost to build hash map is higher than sorting, we should only build hash map on a table that is much smaller than other one.” So shuffle hash join requires three things simultaneously: under the 2 GB ceiling, at least three times smaller than the other side, and preferSortMergeJoin flipped to false. That conjunction, with the last condition off by default, is why it is rare in practice. Which build side each join type allows This is the rule that makes broadcast joins look unpredictable, and it is pure lookup, not heuristics. Join type Broadcast the left? Broadcast the right? Inner, cross Yes Yes Left outer No Yes Right outer Yes No Full outer No No Left semi, left anti No Yes The logic follows from correctness. A left outer join must emit every left row, so the left side has to be streamed and the right one built. A right outer is the mirror image. A full outer must emit unmatched rows from both sides, so neither can be the build side, and no full outer join is ever a broadcast hash join regardless of how small either side is. Shuffle hash join is less restricted, which is why the strategy table says it supports all join types: its build side may be the left for inner, left outer, full outer and right outer, and the right for those plus left semi, left anti and existence joins. Hands-on: making each strategy appear Every rule above is testable. This section builds two tables, forces all five strategies, and reads back the one that actually executed. The transcripts below are from Spark 4.0.2 with spark.sql.shuffle.partitions set to 8; the five selection predicates quoted earlier are byte-identical in 4.2.0. A fact table and a dimension: from pyspark.sql import SparkSession, functions as F spark = (SparkSession.builder .appName(&quot;join-strategies&quot;) .master(&quot;local[4]&quot;) .config(&quot;spark.sql.shuffle.partitions&quot;, &quot;8&quot;) .getOrCreate()) cities = (spark.range(0, 600) .select(F.col(&quot;id&quot;).alias(&quot;city_id&quot;), F.concat(F.lit(&quot;city-&quot;), F.col(&quot;id&quot;)).alias(&quot;city_name&quot;), F.when(F.col(&quot;id&quot;) % 3 == 0, &quot;IN&quot;) .when(F.col(&quot;id&quot;) % 3 == 1, &quot;US&quot;) .otherwise(&quot;DE&quot;).alias(&quot;country_code&quot;))) trips = (spark.range(0, 3_000_000) .select(F.col(&quot;id&quot;).alias(&quot;trip_id&quot;), (F.col(&quot;id&quot;) % 500).alias(&quot;city_id&quot;), (F.col(&quot;id&quot;) % 90000).alias(&quot;rider_id&quot;), (F.col(&quot;id&quot;) % 4000 / 100.0).cast(&quot;decimal(10,2)&quot;).alias(&quot;fare_amount&quot;))) Two helpers. The first reports the strategy that ran, the second reports the size Catalyst believes a side to be, which is the input to every size rule: def strategy(df): plan = df._jdf.queryExecution().executedPlan().toString() for name in [&quot;BroadcastHashJoin&quot;, &quot;ShuffledHashJoin&quot;, &quot;SortMergeJoin&quot;, &quot;BroadcastNestedLoopJoin&quot;, &quot;CartesianProduct&quot;]: if name in plan: return name return &quot;unknown&quot; def size_in_bytes(df): return int(df._jdf.queryExecution().optimizedPlan().stats().sizeInBytes()) For these two tables the estimates are: size_in_bytes(cities) = 16,800 bytes size_in_bytes(trips) = 60,000,000 bytes trips references only the first 500 city_id values, so 100 of the 600 cities have no trips. That matters for the outer joins later. cities is far under the 10 MB broadcast threshold and trips is far over it, so the defaults should broadcast. Now force each strategy in turn: # 1 broadcast hash join, the default here because cities is under the threshold strategy(trips.join(cities, &quot;city_id&quot;)) # 2 sort-merge, by taking broadcasting away spark.conf.set(&quot;spark.sql.autoBroadcastJoinThreshold&quot;, &quot;-1&quot;) strategy(trips.join(cities, &quot;city_id&quot;)) spark.conf.unset(&quot;spark.sql.autoBroadcastJoinThreshold&quot;) # 3 shuffle hash, by asking for it strategy(trips.join(cities.hint(&quot;SHUFFLE_HASH&quot;), &quot;city_id&quot;)) # 4 broadcast nested loop, by using an inequality instead of an equality strategy(trips.join(cities, trips.city_id &amp;gt; cities.city_id)) # 5 cartesian, a deliberate cross product with broadcasting off spark.conf.set(&quot;spark.sql.autoBroadcastJoinThreshold&quot;, &quot;-1&quot;) strategy(trips.limit(1000).crossJoin(cities.limit(50))) spark.conf.unset(&quot;spark.sql.autoBroadcastJoinThreshold&quot;) The operator line each one produced: 1 BroadcastHashJoin [city_id#6L], [city_id#1L], Inner, BuildRight, false 2 SortMergeJoin [city_id#6L], [city_id#1L], Inner 3 ShuffledHashJoin [city_id#6L], [city_id#1L], Inner, BuildRight 4 BroadcastNestedLoopJoin BuildRight, Inner, (city_id#6L &amp;gt; city_id#1L) 5 CartesianProduct All five, from one dataset, by changing one thing at a time. Note that case 4 returned 748,500,000 rows from a 3,000,000-row table: an inequality against 600 cities is a near-cross-product, which is what the strategy name is telling you. Each of the four hints behaves as the procedure says, overriding the size rules but not the join-type rules: Hint Written as Strategy it produced BROADCAST cities.hint(&quot;BROADCAST&quot;) BroadcastHashJoin MERGE cities.hint(&quot;MERGE&quot;) SortMergeJoin SHUFFLE_HASH cities.hint(&quot;SHUFFLE_HASH&quot;) ShuffledHashJoin SHUFFLE_REPLICATE_NL cities.hint(&quot;SHUFFLE_REPLICATE_NL&quot;) CartesianProduct A hint is the one lever that skips the size checks entirely, which is why BROADCAST on a large table moves the failure to the driver rather than preventing it. The last row is the most destructive of the four: it printed CartesianProduct (city_id#5L = city_id#1L), meaning the equality was demoted from a join key to a filter applied after the cross product was built. The join type overrides the size The size rules are only consulted after the join type has had its say, and this is where the surprises live. One rule sits underneath all of them: an outer join must emit every row of its preserved side, so that side has to be streamed, and the build side is always the other one. A left outer preserves the left and therefore builds the right. A right outer preserves the right and builds the left. A full outer preserves both, so it can build neither. That is the entire build-side table, derived rather than memorised. Four joins over the same two tables make the consequence concrete: strategy(trips.join(cities, &quot;city_id&quot;, &quot;right&quot;)) # preserves cities, the small side strategy(cities.join(trips, &quot;city_id&quot;, &quot;left&quot;)) # preserves cities as well strategy(cities.join(trips, &quot;city_id&quot;, &quot;right&quot;)) # preserves trips, the large side strategy(trips.join(cities, &quot;city_id&quot;, &quot;left&quot;)) # preserves trips as well trips RIGHT JOIN cities 3,000,100 rows SortMergeJoin [city_id#6L], [city_id#1L], RightOuter cities LEFT JOIN trips 3,000,100 rows SortMergeJoin [city_id#1L], [city_id#6L], LeftOuter cities RIGHT JOIN trips 3,000,000 rows BroadcastHashJoin [city_id#1L], [city_id#6L], RightOuter, BuildLeft trips LEFT JOIN cities 3,000,000 rows BroadcastHashJoin [city_id#6L], [city_id#1L], LeftOuter, BuildRight The first two lines are one query written two ways. Both preserve cities, both return 3,000,100 rows, and comparing them row by row finds no difference in either direction. Both sort-merge. The last two are also one query written two ways, both preserve trips, and both broadcast. The two pairs, though, are not the same query. They differ by exactly the 100 cities that have no trips. That is the trap in the advice usually given here: swapping the argument order of an outer join changes which side is preserved, so it changes the answer. It is a join-type change wearing the costume of a refactor, and it is the one rewrite that can quietly drop rows. So the honest rule is narrower than “swap the sides”: If you must preserve the large side, the build side is the small one, and the broadcast comes for free. If you must preserve the small side, the build side is the large one, and no threshold, hint or reordering produces a broadcast hash join. Sort-merge is the correct plan, and the lever you have is reducing the large side, not persuading the optimiser. Two more results from the same tables close the picture: trips FULL JOIN cities 3,000,100 rows SortMergeJoin ... FullOuter trips LEFT ANTI JOIN cities 0 rows BroadcastHashJoin ... LeftAnti, BuildRight A full outer preserves both sides, so it sort-merges however small cities is. A left anti preserves the left and returns no columns from the right at all, so the right is always available as the build side and it broadcasts. It returns zero rows here because every trip does have a matching city, which is the correct answer to “trips whose city is missing”. Why shuffle hash join is rare, demonstrated The three conditions are easier to believe when you watch them fail one at a time. Take a 400,000-row riders table against trips on rider_id: riders = (spark.range(0, 400_000) .select(F.col(&quot;id&quot;).alias(&quot;rider_id&quot;), F.concat(F.lit(&quot;rider-&quot;), F.col(&quot;id&quot;)).alias(&quot;rider_name&quot;))) def attempt(threshold, prefer_smj): spark.conf.set(&quot;spark.sql.autoBroadcastJoinThreshold&quot;, str(threshold)) spark.conf.set(&quot;spark.sql.join.preferSortMergeJoin&quot;, prefer_smj) return strategy(trips.select(&quot;trip_id&quot;, &quot;rider_id&quot;).join(riders, &quot;rider_id&quot;)) attempt(-1, &quot;false&quot;) # the usual &quot;disable broadcast&quot; trick attempt(2 * 1024**2, &quot;false&quot;) # broadcast still off, but a positive threshold attempt(2 * 1024**2, &quot;true&quot;) # preferSortMergeJoin back at its default With riders at 7,200,000 bytes and the projected trips at 36,000,000: Attempt canBroadcastBySize Hash-map ceiling canBuildLocalHashMapBySize muchSmaller preferSortMergeJoin Strategy threshold = -1 false -1 x 8 = -8 false true false SortMergeJoin threshold = 2 MB false 2 MB x 8 = 16 MB true true false ShuffledHashJoin threshold = 2 MB, prefer on false 2 MB x 8 = 16 MB true true true SortMergeJoin The middle row is a shuffle hash join with no hint at all, which is the thing most people have never seen. The other two rows each break one condition. The first row deserves its own warning, because it is a trap in the most common tuning gesture there is. Setting autoBroadcastJoinThreshold to -1 does not just disable broadcasting, it also makes shuffle hash join unreachable, since canBuildLocalHashMapBySize multiplies that same threshold by the shuffle partition count and -1 x 8 is negative. Every size below zero fails the test. If you want to stop broadcasting but keep shuffle hash join available, set the threshold to a small positive number instead of -1. What adaptive execution changes Everything above is plan-time. Adaptive query execution re-plans after a shuffle stage completes, when the sizes are measurements rather than estimates, and spark.sql.adaptive.enabled defaults to true. For joins it does two things: Demotes a sort-merge join to a broadcast hash join when a side turns out small enough, using spark.sql.adaptive.autoBroadcastJoinThreshold. Splits skewed partitions, with spark.sql.adaptive.skewJoin.enabled default true. The skew thresholds are strict and worth knowing, because they explain “skew handling is on but nothing happened”. A partition is treated as skewed only when it is larger than spark.sql.adaptive.skewJoin.skewedPartitionFactor, default 5.0, times the median and larger than spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes, default 256MB. On a job whose partitions are all under 256 MB, it never fires no matter how uneven they are. The practical consequence for reading plans: explain() shows the planned shape, not the final one. Before execution the tree is wrapped in AdaptiveSparkPlan with isFinalPlan=false. The SQL tab of the UI after the query finishes shows what actually ran. Reading the plan df.explain(&quot;formatted&quot;) Here is the real output for the broadcast join above, projected down to three columns. The tree comes first, then a numbered detail block per operator: == Physical Plan == AdaptiveSparkPlan (9) +- Project (8) +- BroadcastHashJoin Inner BuildRight (7) :- Project (3) : +- Filter (2) : +- Range (1) +- BroadcastExchange (6) +- Project (5) +- Range (4) (6) BroadcastExchange Input [2]: [city_id#1L, city_name#2] Arguments: HashedRelationBroadcastMode(List(input[0, bigint, false]),false), [plan_id=28] (7) BroadcastHashJoin Left keys [1]: [city_id#6L] Right keys [1]: [city_id#1L] Join type: Inner Join condition: None (9) AdaptiveSparkPlan Output [3]: [trip_id#5L, city_name#2, fare_amount#8] Arguments: isFinalPlan=false Four things in that output are worth reading deliberately. BuildRight on node 7 names the build side. BroadcastExchange on node 6 appears under the right branch only, so the left branch has no exchange and the large side is not shuffled. Node 6 lists city_id and city_name but not country_code, because the projection pushed down and shrank what gets broadcast. And node 9 says isFinalPlan=false, so this is the plan before adaptive execution has had a say. What to look for, in order of how much it tells you: In the plan Meaning BroadcastHashJoin The small side is being broadcast. Usually what you want SortMergeJoin Both sides shuffled and sorted. Correct for large-to-large ShuffledHashJoin Rare without a hint BroadcastNestedLoopJoin Non-equi, or a missing condition CartesianProduct Almost always a mistake Exchange hashpartitioning(k, 200) The shuffle, with its key and partition count BroadcastExchange The broadcast being built ReusedExchange One shuffle serving two branches. Good news isFinalPlan=false AQE has not re-planned yet A useful habit: count the Exchange nodes. That is the number of shuffles, and if two consecutive operations key on the same column, one exchange can serve both. Which strategy for which scenario Scenario Aim for How Large fact, small dimension Broadcast hash Nothing, if the dimension is under 10 MB. Filter and project it first if not Large fact, medium dimension (tens of MB) Broadcast hash Reduce it first, then raise the threshold deliberately if it is genuinely small in memory Large to large, recurring Sort-merge, or no shuffle at all Bucket both tables on the key with the same bucket count Large to large, one-off Sort-merge Filter and project both sides, size the shuffle, handle skew Full outer with a small side Sort-merge, unavoidable Accept it, or restructure as two outer joins and a union Outer join preserving the large side Broadcast hash Nothing. The build side is the small one already Outer join preserving the small side Sort-merge, unavoidable The preserved side cannot be built. Reduce the other side instead Range or inequality condition Broadcast nested loop Add an equality that buckets both sides, so the nested loop runs within buckets Existence check Semi or anti join left_semi or left_anti, never inner plus distinct Skewed join key Sort-merge plus skew handling AQE first, then salt the hot key Two of those need expanding. The range join. A condition like ON t.ts BETWEEN d.start AND d.end has no equality, so Spark can only nested-loop it. The standard fix is to manufacture a key: bucket both sides by a coarser value, join on that equality and the range, so the comparison happens within each bucket rather than across the whole product. (events.withColumn(&quot;day&quot;, F.to_date(&quot;ts&quot;)) .join(windows.withColumn(&quot;day&quot;, F.to_date(&quot;start&quot;)), [&quot;day&quot;]) # an equality to partition on .filter((F.col(&quot;ts&quot;) &amp;gt;= F.col(&quot;start&quot;)) &amp;amp; (F.col(&quot;ts&quot;) &amp;lt;= F.col(&quot;end&quot;)))) The full outer with a small side. If broadcasting matters more than the single scan, a full outer can be rewritten as a left outer union an anti join the other way, both of which can broadcast. It is more code and two passes, so only worth it when the sort-merge is genuinely the bottleneck. Common misconceptions “Raising autoBroadcastJoinThreshold will make this broadcast.” Not if the join type forbids it. A full outer join can never be a broadcast hash join, because both sides must emit unmatched rows and neither may be the build side. No threshold changes that. Check the join type before the size. “Swapping the sides of an outer join is a free optimisation.” It changes the answer. An outer join preserves one side, and swapping the argument order swaps which side that is. Measured on 600 cities where 100 have no trips, the two orderings differ by exactly those 100 rows, while returning the same count on data where everything matches, which is how the bug hides. “Setting autoBroadcastJoinThreshold to -1 only turns off broadcasting.” It also removes shuffle hash join from consideration. The shuffle-hash ceiling is autoBroadcastJoinThreshold * spark.sql.shuffle.partitions, so -1 makes it negative and every candidate fails the size test. Use a small positive value if you want to stop broadcasting and keep the option. “A different join strategy can change my results.” It cannot. Strategies are algorithms for the same semantics. If the rows changed, the join type or the condition changed, or nulls in the key are behaving as SQL says they should. Production tips Read the plan before tuning. The strategy Spark chose tells you which rule fired, and therefore which lever exists. Reduce before joining. Filtering and projecting a side is the highest-value change available, because it can move the join into the broadcast band and change the strategy entirely. Do not hint BROADCAST on something large. A hint overrides the size check, so you move the failure to the driver, which must collect it first. Prefer a small positive autoBroadcastJoinThreshold over -1. Setting it to -1 also drives the shuffle-hash ceiling negative, removing that strategy from consideration along with broadcasting. Treat a full outer join as unbroadcastable. Raising the threshold will never help; the join type is what forbids it. For an outer join, ask which side is preserved, not which side is small. The build side is always the non-preserved one, so an outer join that must preserve the small side cannot broadcast at any threshold. Reordering the arguments changes the result, not just the plan. Fix the estimate rather than forcing the plan where you can, with ANALYZE TABLE ... COMPUTE STATISTICS, so Catalyst chooses correctly on its own. Bucket the keys you join on repeatedly. It is the only way to remove the shuffle from a large-to-large join permanently. Trust the SQL tab over explain() for what actually ran, since AQE re-plans after the shuffle. Frequently asked questions Why is my small table not being broadcast? Four common reasons: the estimate is of the unfiltered table because the filter did not push down; the table is compressed on disk and larger in memory than you think; there are no statistics so Spark fell back to file size; or the join type forbids that side as the build side. Check the last one first, because no config change fixes it. Does a bigger autoBroadcastJoinThreshold make joins faster? Sometimes, and it moves the failure mode from slow to fragile. Every executor holds a full copy and the driver assembles it first, so a 500 MB broadcast across 50 executors is 25 GB of cluster memory plus a driver that must hold it. Raise it deliberately, with a number you can justify. Why do I almost never see ShuffledHashJoin? Because it needs three conditions at once and one of them, spark.sql.join.preferSortMergeJoin being false, is not the default. Spark prefers sort-merge because it cannot run out of memory, while a hash join can. There is also a trap: if you disabled broadcasting with autoBroadcastJoinThreshold = -1, the shuffle-hash ceiling became negative too, so the strategy cannot be chosen without a hint no matter what else you set. The table above shows the same join picking it once the threshold is a small positive number. Can a join strategy change my results? No. Strategies are algorithms for the same semantics. If results changed, the join type or the condition changed, or there are nulls in the key behaving as SQL says they should. Why is my join a CartesianProduct when I wrote a condition? Because Catalyst could not use the condition as an equality: it is an inequality, it is wrapped in an expression it cannot match on, or it sits in a where after a cross join in a way that could not be pushed into the join. What about joining on a collated string column? In Spark 4 this matters. Hash joins require keys that are binary-stable, and a case- or accent-insensitive collation is not, because two rows can be equal without having equal bytes. Joining on such a column disables hash joins and leaves sort-merge. Conclusion The thing worth taking away is that join selection is a lookup, not a judgement. Spark asks, in a fixed order: is there a hint, is this an equi-join, is one side small enough, does the join type permit that side as the build side, are the keys sortable. The first rule that fits wins. Nothing about it is adaptive except the parts AQE re-plans after a shuffle. That reframes tuning. You are not persuading an optimiser, you are changing the inputs to a decision procedure. Filtering a dimension until it fits under 10 MB changes the answer to “is one side small enough”. Deciding which side an outer join preserves settles which side may be built, though it changes the result too, so it is a modelling decision rather than a tuning knob. Bucketing changes whether a shuffle is needed at all. Each of those is a specific rule with a specific lever. And three answers are simply no. A full outer join will not broadcast, a non-equi join will not hash, and an outer join that must preserve its small side will not broadcast either. When you hit those, the fix is to change the query shape, or to accept the plan and shrink the other side, rather than to keep raising thresholds. References SparkStrategies.scala, whose JoinSelection comment documents the five strategies and the order they are tried in joins.scala for the size predicates and the build-side rules per join type SQL performance tuning for join hints and adaptive query execution Apache Spark architecture for the shuffle and stage machinery these strategies sit on Two community treatments this post builds on: Different types of Spark join strategies for separating data exchange from join algorithm, and Join strategies in Apache Spark, a hands-on approach for forcing each strategy on one dataset Trademarks Apache Spark, Apache Hive, Apache Parquet and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry><entry><title type="html">200 Apache Spark interview questions, with answers</title><link href="https://rangareddy.github.io/SparkInterviewQuestions/" rel="alternate" type="text/html" title="200 Apache Spark interview questions, with answers" /><published>2026-09-16T09:00:00+05:30</published><updated>2026-09-16T09:00:00+05:30</updated><id>https://rangareddy.github.io/SparkInterviewQuestions</id><content type="html" xml:base="https://rangareddy.github.io/SparkInterviewQuestions/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#how-this-is-organised&quot; id=&quot;markdown-toc-how-this-is-organised&quot;&gt;How this is organised&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#spark-fundamentals&quot; id=&quot;markdown-toc-spark-fundamentals&quot;&gt;Spark fundamentals&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#rdds&quot; id=&quot;markdown-toc-rdds&quot;&gt;RDDs&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#dataframes-datasets-and-spark-sql&quot; id=&quot;markdown-toc-dataframes-datasets-and-spark-sql&quot;&gt;DataFrames, Datasets and Spark SQL&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-and-execution&quot; id=&quot;markdown-toc-architecture-and-execution&quot;&gt;Architecture and execution&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#partitioning-and-shuffle&quot; id=&quot;markdown-toc-partitioning-and-shuffle&quot;&gt;Partitioning and shuffle&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#memory-caching-and-persistence&quot; id=&quot;markdown-toc-memory-caching-and-persistence&quot;&gt;Memory, caching and persistence&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#joins-catalyst-and-adaptive-execution&quot; id=&quot;markdown-toc-joins-catalyst-and-adaptive-execution&quot;&gt;Joins, Catalyst and adaptive execution&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#performance-tuning-and-troubleshooting&quot; id=&quot;markdown-toc-performance-tuning-and-troubleshooting&quot;&gt;Performance tuning and troubleshooting&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#structured-streaming&quot; id=&quot;markdown-toc-structured-streaming&quot;&gt;Structured Streaming&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#deployment-cluster-managers-and-operations&quot; id=&quot;markdown-toc-deployment-cluster-managers-and-operations&quot;&gt;Deployment, cluster managers and operations&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#scenario-based-questions&quot; id=&quot;markdown-toc-scenario-based-questions&quot;&gt;Scenario-based questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#how-to-use-this-in-an-actual-interview&quot; id=&quot;markdown-toc-how-to-use-this-in-an-actual-interview&quot;&gt;How to use this in an actual interview&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;200 questions in 11 sections, each answered properly: the direct answer, the mechanism underneath, a worked example, and the trap. Click any question to open it.&lt;/li&gt;
    &lt;li&gt;The last 15 are &lt;strong&gt;scenario questions&lt;/strong&gt;: a symptom, and how to reason to the cause. Practise those out loud, because they are what separate people who have run Spark in production.&lt;/li&gt;
    &lt;li&gt;Where a default surprises people the number is given, because a confidently wrong default is worse in an interview than an honest “I would look it up”.&lt;/li&gt;
    &lt;li&gt;The answers say where the textbook version is wrong: AQE skew handling needs a partition over 256 MB before it fires, input partitions do not come from file count, accumulators double-count outside actions, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(1)&lt;/code&gt; can make an entire pipeline single-threaded.&lt;/li&gt;
    &lt;li&gt;No speedup ratios anywhere. Where performance is the point, the answer names the mechanism and leaves the measuring to you.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;Interview lists tend to collect questions without checking the answers, so they
repeat things that were true in Spark 1.6 and quote defaults that changed three
releases ago. They also tend to answer in two lines, which is enough to
recognise a term and not enough to defend it when someone asks “why”.&lt;/p&gt;

&lt;p&gt;This one is built the other way round. The answers came first, from source and
documentation at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;v4.2.0&lt;/code&gt;, and each one is written to survive a follow-up
question: what the mechanism is, what the numbers actually are, and where the
common version of the answer is wrong.&lt;/p&gt;

&lt;p&gt;Use it two ways. Read a section end to end to find the gaps in your own model,
or open a single question when you want the defensible version of something you
already half know. The sections run roughly from basics to advanced, and the
scenario section at the end is the one worth rehearsing aloud.&lt;/p&gt;

&lt;p&gt;A note on what is deliberately absent: there are no benchmark numbers or speed
multipliers. Where performance is the point, the answer explains the mechanism
that makes one path cheaper and leaves the measuring to you, on your data and
your cluster.&lt;/p&gt;

&lt;h2 id=&quot;how-this-is-organised&quot;&gt;How this is organised&lt;/h2&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Section&lt;/th&gt;
      &lt;th style=&quot;text-align: right&quot;&gt;Questions&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Focus&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#spark-fundamentals&quot;&gt;Spark fundamentals&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;1-20&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Vocabulary and the execution model&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#rdds&quot;&gt;RDDs&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;21-40&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Lineage, partitioning, shared variables&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#dataframes-datasets-and-spark-sql&quot;&gt;DataFrames, Datasets and Spark SQL&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;41-62&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Catalyst, plans, formats&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#architecture-and-execution&quot;&gt;Architecture and execution&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;63-84&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Driver, executors, schedulers&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#partitioning-and-shuffle&quot;&gt;Partitioning and shuffle&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;85-104&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The main source of slowness&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#memory-caching-and-persistence&quot;&gt;Memory, caching and persistence&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;105-122&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;The unified memory model&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#joins-catalyst-and-adaptive-execution&quot;&gt;Joins, Catalyst and adaptive execution&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;123-142&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Strategy selection and AQE&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#performance-tuning-and-troubleshooting&quot;&gt;Performance tuning and troubleshooting&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;143-160&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Diagnosis before knobs&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#structured-streaming&quot;&gt;Structured Streaming&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;161-175&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Watermarks, state, guarantees&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#deployment-cluster-managers-and-operations&quot;&gt;Deployment, cluster managers and operations&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;176-185&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Running it for real&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;#scenario-based-questions&quot;&gt;Scenario-based questions&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;186-200&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Symptom to cause&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The diagrams render when you open a question, so nothing is drawn until you ask for it.&lt;/p&gt;

&lt;h2 id=&quot;spark-fundamentals&quot;&gt;Spark fundamentals&lt;/h2&gt;

&lt;p&gt;Start here. These come up in the first ten minutes of almost every interview, and the answers below give you the version you can defend rather than the version everyone recites.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;1. What is Apache Spark?&lt;/summary&gt;

  &lt;p&gt;Apache Spark is a distributed engine for large-scale data processing. You
describe a computation over a dataset, and Spark splits both the data and the
work across a cluster of machines, coordinating the whole thing from a single
process called the driver.&lt;/p&gt;

  &lt;p&gt;Three properties define it:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Distributed execution.&lt;/strong&gt; A dataset is divided into &lt;em&gt;partitions&lt;/em&gt;, and each partition is processed independently, in parallel, on whichever machine has capacity.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Lazy evaluation.&lt;/strong&gt; Operations build a plan rather than running immediately, which lets the engine optimise the whole computation before any of it starts.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Fault tolerance through lineage.&lt;/strong&gt; Spark records how each partition was derived, so a lost one is rebuilt rather than the job being restarted.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;It ships APIs for Scala, Java, Python and R, plus libraries for SQL,
structured streaming, machine learning and graph processing, all on the same
execution engine.&lt;/p&gt;

  &lt;p&gt;The framing that lands well in an interview: &lt;strong&gt;Spark is a compute engine, not a
storage system and not a table format.&lt;/strong&gt; It reads from HDFS, S3, JDBC or Kafka,
and writes back out. Confusing Spark with Hudi, Iceberg or Delta is a common
mistake, and being crisp about the boundary is a good signal.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;2. Why use Spark instead of a single machine?&lt;/summary&gt;

  &lt;p&gt;Because one of two things has become true: the data no longer fits the
memory, CPU or disk of one machine, or it fits but takes unacceptably long to
process serially.&lt;/p&gt;

  &lt;p&gt;Spark’s answer to both is partitioning. A 1 TB dataset might become 8,000
partitions of roughly 128 MB, and if the cluster has 200 cores then 200 of them
are processed at a time.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;1 TB input
     |
     v  split into partitions
+------+------+------+     +--------+
|  P1  |  P2  |  P3  | ... |  P8000 |
+------+------+------+     +--------+
     |                          |
     v   processed in parallel  v
        across 200 cores
     |
     v
   result
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The part candidates usually miss is the cost. Distribution is not free: there is
serialization, network transfer during shuffles, scheduling overhead per task,
and the coordination cost of the driver. For a dataset that fits comfortably on
one machine, a single-process tool such as pandas, DuckDB or Polars will
routinely beat a Spark cluster, because Spark’s overhead is not amortised.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;A good answer names the crossover point rather than claiming Spark is always
faster.&lt;/strong&gt; Spark earns its keep when the work genuinely exceeds one machine, or
when you need fault tolerance across a long-running job.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;3. What is the difference between Spark and Hadoop MapReduce?&lt;/summary&gt;

  &lt;p&gt;The structural difference is where intermediate results live and how much of
the computation the engine can see at once.&lt;/p&gt;

  &lt;p&gt;MapReduce runs one map phase and one reduce phase, and writes the intermediate
result to disk, usually replicated, between them. A multi-step algorithm becomes
a chain of MapReduce jobs, each re-reading the previous one’s output from disk.&lt;/p&gt;

  &lt;p&gt;Spark keeps intermediate data in memory where it can, and expresses an entire
chain of operations as one plan before running any of it. That gives it two
advantages MapReduce structurally cannot have:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;MapReduce&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Spark&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Intermediate data&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Written to disk between stages&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Kept in memory where possible&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Unit of work&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One map plus one reduce&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;An arbitrary DAG of stages&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Optimisation scope&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Per job&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The whole plan, via Catalyst&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Iterative workloads&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Re-reads input each pass&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Can cache and reuse&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;APIs&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Map and reduce&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;SQL, DataFrames, streaming, ML, graph&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;The iterative case is where the gap is widest. Ten passes of an algorithm in
MapReduce means ten round trips through disk; in Spark the dataset can be cached
once and reused.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The honest framing for an interview:&lt;/strong&gt; the advantage is in-memory
intermediate data plus whole-plan optimisation, not a fixed speed multiplier.
The often-quoted “100x” comes from a specific iterative benchmark and does not
describe a typical batch job. Quote a number only if you measured it on your own
data, and otherwise describe the mechanism.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;4. What is lazy evaluation?&lt;/summary&gt;

  &lt;p&gt;Transformations do not compute anything when you call them. Each one records
what to do and returns a new dataset describing the result. Nothing executes
until an &lt;strong&gt;action&lt;/strong&gt; asks for a value.&lt;/p&gt;

  &lt;p&gt;The RDD programming guide states it directly:&lt;/p&gt;

  &lt;blockquote&gt;
    &lt;p&gt;All transformations in Spark are &lt;em&gt;lazy&lt;/em&gt;, in that they do not compute their
results right away. Instead, they just remember the transformations applied
to some base dataset.&lt;/p&gt;
  &lt;/blockquote&gt;

  &lt;p&gt;Why it matters is the important half. Because Spark holds the whole chain before
running it, Catalyst can rewrite it: push a filter down into the scan so fewer
rows are read, drop columns nobody selected, fuse adjacent operations into one
generated loop, and choose a join strategy knowing what comes next. None of that
is possible for an engine that executes each call as it arrives.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  T[&quot;transformations called:&amp;lt;br/&amp;gt;read, filter, withColumn, groupBy&quot;]
  T --&amp;gt; P[(&quot;plan recorded&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;0 jobs, 0 tasks&amp;lt;/b&amp;gt;&quot;)]
  P -.-&amp;gt;|&quot;nothing has run&quot;| W[&quot;cluster is idle&quot;]
  A[&quot;an action is called:&amp;lt;br/&amp;gt;count()&quot;] --&amp;gt; R[&quot;&amp;lt;b&amp;gt;1 job&amp;lt;/b&amp;gt;: Catalyst optimises&amp;lt;br/&amp;gt;the whole plan, then runs it&quot;]
  P --&amp;gt; R
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;There are two consequences to be ready for:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;A dataset is recomputed every time an action needs it,&lt;/strong&gt; unless you persist it. The guide is explicit: “each transformed RDD may be recomputed each time you run an action on it.”&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Errors surface at the action, not at the transformation.&lt;/strong&gt; A typo in a column name in a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt; may only raise when you call &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count()&lt;/code&gt;, which is why analysis errors seem to appear far from their cause.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;5. What is the difference between a transformation and an action?&lt;/summary&gt;

  &lt;p&gt;A transformation creates a new dataset from an existing one and runs nothing.
An action returns a value to the driver or writes to storage, and that is what
submits a job.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Transformation&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Action&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Examples&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;map&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupBy&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;join&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;first&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;take&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreach&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Returns&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A new dataset&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A value to the driver, or a write&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Triggers execution&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, one job&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Transformations divide further, and the distinction drives everything about
performance:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Narrow&lt;/strong&gt; transformations, where each input partition feeds at most one output partition, so the work pipelines inside a single task: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;map&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapPartitions&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Wide&lt;/strong&gt; transformations, where an output partition draws from many input partitions, so data must move across the network: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;join&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The count of actions tells you how many jobs you will run, and the count of wide
transformations tells you how many stages each job will have.&lt;/p&gt;

  &lt;p&gt;One trap worth naming: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show()&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;take(n)&lt;/code&gt; are actions, so calling them
during development runs the whole chain up to that point. A notebook with ten
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show()&lt;/code&gt; calls has run ten jobs, and if nothing is cached, much of the work is
repeated each time.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;6. How can you prove that transformations are lazy?&lt;/summary&gt;

  &lt;p&gt;Count the jobs. Chaining transformations submits none; the action submits one.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;doubled&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;len&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;statusTracker&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getJobIdsForGroup&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;or&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[]))&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# 0
&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;doubled&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;len&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;statusTracker&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getJobIdsForGroup&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;or&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[]))&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# 1
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Two transformations, zero jobs. One action, one job.&lt;/p&gt;

  &lt;p&gt;A second demonstration is more visual: put a side effect in a transformation and
watch when it happens.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;accumulator&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;counted&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;add&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# 0, the map has not run
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;counted&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# 100, it ran when the action asked
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The same trick exposes the other half of laziness. Call &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count()&lt;/code&gt; a second time
and the accumulator reads 200, for the same 100 rows, because the uncached
dataset was recomputed from scratch. Add &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.cache()&lt;/code&gt; and it stays at 100.&lt;/p&gt;

  &lt;p&gt;In an interview, the accumulator version is the stronger answer, because it
demonstrates laziness and recomputation in one go.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;7. What is a partition?&lt;/summary&gt;

  &lt;p&gt;A partition is the unit of parallelism: a slice of the dataset that one task
processes on one core. A stage runs exactly one task per partition, so the
partition count is the maximum parallelism available to that stage.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph S[&quot;one stage&quot;]
    P1[&quot;partition 0&quot;] --&amp;gt; T1[&quot;task 0&quot;]
    P2[&quot;partition 1&quot;] --&amp;gt; T2[&quot;task 1&quot;]
    P3[&quot;partition 2&quot;] --&amp;gt; T3[&quot;task 2&quot;]
    P4[&quot;partition 3&quot;] --&amp;gt; T4[&quot;task 3&quot;]
  end
  T1 --&amp;gt; E[&quot;executors run&amp;lt;br/&amp;gt;as many as they have slots&quot;]
  T2 --&amp;gt; E
  T3 --&amp;gt; E
  T4 --&amp;gt; E
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Three numbers follow from this, and almost every tuning question is one of them:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;How many partitions.&lt;/strong&gt; Too few and cores sit idle; too many and per-task scheduling overhead dominates. A useful target is partitions in the low hundreds of megabytes, with several tasks per core so stragglers average out.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;How evenly the data is spread.&lt;/strong&gt; One partition holding most of the rows is &lt;em&gt;skew&lt;/em&gt;, and because a task cannot be split, that one task sets the stage duration.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Where they come from.&lt;/strong&gt; On read, from file splitting; after a shuffle, from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; or adaptive coalescing.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Check the number rather than assume it:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getNumPartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;8. What is `SparkSession` and how does it relate to `SparkContext`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt; is the entry point for the DataFrame and SQL APIs, and it is
what modern code creates. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt; is the older, lower-level entry point
for RDDs and for cluster coordination.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;appName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trips-ingest&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;config&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;400&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# the SparkContext inside it
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt;&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Introduced&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;2.0&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;1.0&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Gives you&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;DataFrames, SQL, catalog, streaming&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;RDDs, broadcasts, accumulators&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Relationship&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Wraps one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One per application&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt; wraps exactly one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;, and one application has one
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;getOrCreate()&lt;/code&gt; returns the existing one rather than making a
second, which is why it is the idiomatic call.&lt;/p&gt;

  &lt;p&gt;Two details interviewers probe:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Creating the session is what starts negotiating for executors.&lt;/strong&gt; Before it, nothing has been requested from the cluster manager.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;newSession()&lt;/code&gt;&lt;/strong&gt; gives an isolated SQL session, with its own temporary views and SQL configs, sharing the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt; and therefore the same executors. That is how a multi-tenant server such as the Thrift server isolates users.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;9. What languages does Spark support, and does the choice affect performance?&lt;/summary&gt;

  &lt;p&gt;Scala, Java, Python and R, with a community-maintained C# binding outside the
project. For DataFrame and SQL work the choice barely affects performance,
because operations in any language compile to the same logical plan and execute
in the JVM.&lt;/p&gt;

  &lt;p&gt;It matters in two places:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;RDD code&lt;/strong&gt;, where the logic itself is a closure in your language. A Python RDD &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;map&lt;/code&gt; ships every row to a Python worker process and back.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Python UDFs&lt;/strong&gt;, which sit inside a plan that is otherwise all JVM. Rows must be serialized out to Python and results serialized back, and because Catalyst cannot see inside the function, it cannot push filters through it or fuse it into generated code.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# stays in the JVM, fully optimisable
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;50&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# leaves the JVM per batch, breaks codegen across this point
&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;@&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;pandas_udf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;double&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;adjust&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;v&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;Series&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;-&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;Series&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;v&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;1.1&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The ordering to remember, best first: built-in functions and SQL, then pandas
(vectorised) UDFs which amortise the crossing over an Arrow batch, then plain
Python UDFs which pay it per row.&lt;/p&gt;

  &lt;p&gt;Scala and Java additionally get the typed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Dataset&lt;/code&gt; API. Python does not, since
it has no compile-time types to check.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;10. What is a job, a stage and a task?&lt;/summary&gt;

  &lt;p&gt;Three nested units of work, each created by a different component.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Term&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Definition&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Created by&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Job&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;All the work for one action&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The action call&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Stage&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A group of tasks with no shuffle between them&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt;, cut at shuffle boundaries&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Task&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One partition of one stage, on one core&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSchedulerImpl&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;action: write()&quot;] --&amp;gt; J[&quot;1 job&quot;]
  J --&amp;gt; S0[&quot;stage 0&amp;lt;br/&amp;gt;read, filter, partial aggregate&quot;]
  S0 --&amp;gt;|&quot;shuffle&quot;| S1[&quot;stage 1&amp;lt;br/&amp;gt;final aggregate, write&quot;]
  S0 --&amp;gt; T0[&quot;8 tasks&amp;lt;br/&amp;gt;one per input partition&quot;]
  S1 --&amp;gt; T1[&quot;200 tasks&amp;lt;br/&amp;gt;one per shuffle partition&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Read it as a hierarchy: a job has one or more stages, and a stage has one task
per partition. The arithmetic is worth internalising, because it is how you
predict what the UI will show:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Jobs&lt;/strong&gt; equals the number of actions you called.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Stages per job&lt;/strong&gt; equals the number of shuffles, plus one.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Tasks per stage&lt;/strong&gt; equals that stage’s partition count, unrelated to how many executors you have.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;A nuance that catches people: a single &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write&lt;/code&gt; can produce more than one job,
because some writers run extra jobs for committing or for computing statistics.
“One action, one job” is the right model, with that exception noted.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;11. What is a DAG in Spark?&lt;/summary&gt;

  &lt;p&gt;A directed acyclic graph of dependencies between datasets. &lt;strong&gt;Directed&lt;/strong&gt;
because every edge points from an input to an output; &lt;strong&gt;acyclic&lt;/strong&gt; because
nothing can depend on itself, which is what makes the graph safe to walk.&lt;/p&gt;

  &lt;p&gt;Spark builds it as you chain transformations, and &lt;a href=&quot;https://github.com/apache/spark/blob/master/core/src/main/scala/org/apache/spark/scheduler/DAGScheduler.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt;&lt;/a&gt; uses it for two
jobs:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Cutting stages.&lt;/strong&gt; It walks the graph backwards from the action and starts a new stage at every shuffle boundary, because everything narrow can pipeline into the stage it is already in.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Recovery.&lt;/strong&gt; Because each node records how it was derived, a lost partition can be rebuilt by replaying just the path that produced it.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;read trips&quot;] --&amp;gt; B[&quot;filter&quot;]
  C[&quot;read cities&quot;] --&amp;gt; D[&quot;filter&quot;]
  B --&amp;gt; J[&quot;join&quot;]
  D --&amp;gt; J
  J --&amp;gt; K[&quot;groupBy city&quot;]
  J --&amp;gt; L[&quot;groupBy rider&quot;]
  K --&amp;gt; M[&quot;write by city&quot;]
  L --&amp;gt; N[&quot;write by rider&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The DAG is the reason Spark is not restricted to a map phase and a reduce phase:
any shape of dependency graph is expressible, so a job can branch, rejoin and
have many stages.&lt;/p&gt;

  &lt;p&gt;You can print it for an RDD with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;toDebugString()&lt;/code&gt;, and for a DataFrame the
physical plan from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain(&quot;formatted&quot;)&lt;/code&gt; is the equivalent. In the UI, the DAG
visualisation on the job page shows the same thing graphically, with each box a
stage.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;12. What is the driver?&lt;/summary&gt;

  &lt;p&gt;The driver is the process that holds the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt; and makes every
decision. In order, it:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;Creates the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt;, and with it the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkEnv&lt;/code&gt; and the RPC endpoints executors will call back on.&lt;/li&gt;
    &lt;li&gt;Requests executors from the cluster manager through &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SchedulerBackend&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Turns your code into a logical plan, has Catalyst optimise it, and selects a physical plan.&lt;/li&gt;
    &lt;li&gt;Cuts the plan into stages in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt;, at every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleDependency&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Places individual tasks on executors via &lt;a href=&quot;https://github.com/apache/spark/blob/master/core/src/main/scala/org/apache/spark/scheduler/TaskSchedulerImpl.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSchedulerImpl&lt;/code&gt;&lt;/a&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSetManager&lt;/code&gt;, preferring the best available locality.&lt;/li&gt;
    &lt;li&gt;Tracks every attempt, retrying failures up to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.task.maxFailures&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Maintains &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BlockManagerMaster&lt;/code&gt;, the index of every cached and shuffle block in the cluster.&lt;/li&gt;
    &lt;li&gt;Receives results, bounded in total by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.maxResultSize&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Serves the UI on port 4040 and writes the event log the history server replays.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;What it does &lt;strong&gt;not&lt;/strong&gt; do is process your data. It is a coordinator.&lt;/p&gt;

  &lt;p&gt;Two consequences follow, and both are common interview questions:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.memory&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;,&lt;/strong&gt; which is a coordinator’s budget. Anything that pulls data back, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;toPandas()&lt;/code&gt;, needs more, or needs not to do that.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The driver is the one process with no redundancy.&lt;/strong&gt; The plan, the scheduler state and the block index exist nowhere else, so &lt;strong&gt;if the driver dies the application dies&lt;/strong&gt;, whereas losing an executor costs only recomputable work.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;13. What is an executor?&lt;/summary&gt;

  &lt;p&gt;An executor is a JVM launched for one specific application that runs tasks and
stores blocks. The Spark source describes it in a line: a “Spark executor,
backed by a threadpool to run tasks”.&lt;/p&gt;

  &lt;p&gt;Its lifecycle:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Registers.&lt;/strong&gt; &lt;a href=&quot;https://github.com/apache/spark/blob/master/core/src/main/scala/org/apache/spark/executor/CoarseGrainedExecutorBackend.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CoarseGrainedExecutorBackend&lt;/code&gt;&lt;/a&gt; sends &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RegisterExecutor&lt;/code&gt; and waits for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RegisteredExecutor&lt;/code&gt; before accepting work. Until that round trip finishes, a granted container is doing nothing.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Accepts &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LaunchTask&lt;/code&gt;&lt;/strong&gt; messages and runs each in a pool thread. Concurrency is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.cores&lt;/code&gt;, which defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1&lt;/code&gt; on YARN and to every core on the machine in standalone mode.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Reads input, runs the generated code,&lt;/strong&gt; and for a shuffle map stage writes shuffle output to local disk.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Reports back&lt;/strong&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;StatusUpdate&lt;/code&gt; per task and a heartbeat every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.heartbeatInterval&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10s&lt;/code&gt;. Miss enough and the driver declares it lost.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Stores and serves blocks&lt;/strong&gt; through its &lt;a href=&quot;https://github.com/apache/spark/blob/master/core/src/main/scala/org/apache/spark/storage/BlockManager.scala&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BlockManager&lt;/code&gt;&lt;/a&gt;, which is what makes a cached DataFrame reusable and lets the next stage fetch shuffle data from its peers.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The contract is deliberately narrow: an executor never makes a scheduling
decision, never sees the query plan, and never coordinates with another executor
about what to run. It receives tasks, runs them, and serves bytes.&lt;/p&gt;

  &lt;p&gt;That narrowness is what makes executors &lt;strong&gt;disposable&lt;/strong&gt;. Lose one and its tasks
are retried elsewhere and its cached partitions are recomputed from lineage.
Executors are also per-application: two applications never share one, which is
why memory is reserved per application rather than pooled.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;14. What is a cluster manager, and which ones does Spark support?&lt;/summary&gt;

  &lt;p&gt;A cluster manager is a separate service whose only job is to decide who gets
containers. At Spark 4.x the supported options are Standalone, YARN and
Kubernetes.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cluster manager&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What grants the container&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What Spark runs there&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Standalone&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Master&lt;/code&gt;, tracking registered &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Worker&lt;/code&gt; daemons&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The worker launches an executor JVM per grant&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;YARN&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The ResourceManager, asked by Spark’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ApplicationMaster&lt;/code&gt; through &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;YarnAllocator&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;An executor per allocated container&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Kubernetes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The API server. The driver’s own &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ExecutorPodsAllocator&lt;/code&gt; creates the pods&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;An executor per pod&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;Mesos support was removed in the 4.x line.&lt;/strong&gt; At &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;v4.2.0&lt;/code&gt; the
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;resource-managers/&lt;/code&gt; directory in the source tree contains only &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;kubernetes&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;yarn&lt;/code&gt;, so the three rows above are the whole list.&lt;/p&gt;

  &lt;p&gt;The division worth internalising is that &lt;strong&gt;the cluster manager never schedules
your tasks.&lt;/strong&gt; It hands out containers; executors register back with the driver;
and from that point the driver drives everything. That single fact explains a
lot of otherwise puzzling behaviour:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Why an under-provisioned driver throttles a large cluster.&lt;/li&gt;
    &lt;li&gt;Why YARN’s own scheduler settings change what you are &lt;em&gt;given&lt;/em&gt; but never how your tasks are distributed.&lt;/li&gt;
    &lt;li&gt;Why Kubernetes needs no Spark daemon at all: the driver talks to the API server directly.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Kubernetes is the clearest illustration of the split, because the component
granting resources and the component scheduling work are visibly different
things.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;15. Is Spark a master-slave architecture?&lt;/summary&gt;

  &lt;p&gt;In the general sense yes: one process coordinates and many do the work. But
Spark’s own vocabulary is worth getting exactly right, because “master” is
overloaded and people conflate three different things with it.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Term&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What it actually is&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What it is not&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Driver&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The coordinator: holds the plan, schedules every task&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Not called the master anywhere in Spark&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--master&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A URL naming the cluster manager (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;yarn&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;k8s://…&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;local[*]&lt;/code&gt;)&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Not a machine, and not the driver&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Standalone Master&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A daemon that tracks workers and grants resources&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Standalone mode only; YARN and Kubernetes have their own&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Worker&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A machine, or the standalone daemon on it&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Not an executor; a worker hosts executors&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Executor&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A JVM doing the work&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Not a machine&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Spark has also moved away from the older terminology in its own scripts. At
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;v4.2.0&lt;/code&gt; there is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sbin/start-workers.sh&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;conf/workers.template&lt;/code&gt;, while
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;start-slaves.sh&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;slaves.template&lt;/code&gt; are gone, and the standalone
documentation speaks of “a master and workers”.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Where the analogy breaks&lt;/strong&gt; is the interesting part, and it is a strong thing
to volunteer. In a classic master-worker system the master assigns work. In
Spark the cluster manager does &lt;em&gt;not&lt;/em&gt; assign your tasks: it grants containers,
and the driver schedules into them. The coordinator role is split, resources
from one component and work from another.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;16. What is `spark-submit`?&lt;/summary&gt;

  &lt;p&gt;The script that launches an application against any supported cluster manager
through one uniform interface, so the same jar runs on YARN, Kubernetes or
standalone with only &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--master&lt;/code&gt; changing.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--class&lt;/span&gt; org.apache.spark.examples.SparkPi &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--master&lt;/span&gt; yarn &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--deploy-mode&lt;/span&gt; cluster &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--executor-memory&lt;/span&gt; 4g &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--executor-cores&lt;/span&gt; 4 &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.shuffle.partitions&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;400 &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  examples/jars/spark-examples.jar 1000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The options fall into three groups: what to run (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--class&lt;/code&gt;, the jar), where to
run it (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--master&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--deploy-mode&lt;/code&gt;), and how much to ask for
(&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--executor-memory&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--executor-cores&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--num-executors&lt;/code&gt;).&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The mistake to know:&lt;/strong&gt; every Spark-facing option must come &lt;em&gt;before&lt;/em&gt; the
application jar. Anything after the jar is passed to your application’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;main&lt;/code&gt;
as an argument instead, so a misplaced &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--conf&lt;/code&gt; is silently ignored by Spark and
handed to your code.&lt;/p&gt;

  &lt;p&gt;Configuration precedence, highest first:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;Values set on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkConf&lt;/code&gt; in your code.&lt;/li&gt;
    &lt;li&gt;Flags passed to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-submit&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-defaults.conf&lt;/code&gt;.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--properties-file&lt;/code&gt; names an alternative defaults file, and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--load-spark-defaults&lt;/code&gt; makes Spark read &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-defaults.conf&lt;/code&gt; as well as that
file. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--verbose&lt;/code&gt; prints where each value came from, which settles most
“why is my config not applying” questions.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;17. What is the difference between client and cluster deploy mode?&lt;/summary&gt;

  &lt;p&gt;Where the driver runs. That is the whole difference, and everything else
follows from it.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph C[&quot;client mode&quot;]
    C1[&quot;submitting machine&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;driver here&amp;lt;/b&amp;gt;&quot;] --&amp;gt; C2[&quot;cluster manager&quot;]
    C2 --&amp;gt; C3[&quot;executors in the cluster&quot;]
    C1 &amp;lt;--&amp;gt;|&quot;all scheduling traffic&quot;| C3
  end
  subgraph K[&quot;cluster mode&quot;]
    K1[&quot;submitting machine&amp;lt;br/&amp;gt;exits after submit&quot;] --&amp;gt; K2[&quot;cluster manager&quot;]
    K2 --&amp;gt; K3[&quot;&amp;lt;b&amp;gt;driver in the cluster&amp;lt;/b&amp;gt;&quot;]
    K3 &amp;lt;--&amp;gt; K4[&quot;executors in the cluster&quot;]
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Client&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cluster&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Driver runs&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;In the submitting process&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;In a container in the cluster&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Console output&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Attached to your terminal&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;In the driver’s log&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Submitting machine&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Must stay up for the whole job&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Can disconnect&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Network&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Driver-executor traffic crosses to your machine&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Stays inside the cluster&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Suited to&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shells, notebooks, debugging&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Production jobs&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Two practical consequences:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Client mode from a laptop over a VPN is slow and fragile,&lt;/strong&gt; because every scheduling message and every collected result crosses that link. Cluster mode keeps it local.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;In client mode the driver JVM already exists by the time your code runs.&lt;/strong&gt; So &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.extraJavaOptions&lt;/code&gt; set in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkConf&lt;/code&gt; arrives too late to affect the JVM. Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--driver-java-options&lt;/code&gt; or a properties file instead. This catches people trying to add a logging config or a heap dump flag.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;18. What is the default parallelism, and what sets it?&lt;/summary&gt;

  &lt;p&gt;There are two separate settings, and confusing them is a classic slip.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.default.parallelism&lt;/code&gt; governs &lt;strong&gt;RDD&lt;/strong&gt; operations. Its documented default
depends on the operation: for a distributed shuffle such as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt; it is
the largest number of partitions in the parent RDD, and for an operation with no
parent, such as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;parallelize&lt;/code&gt;, it follows the cluster manager, which on local
mode is the number of cores.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; governs &lt;strong&gt;DataFrame and SQL&lt;/strong&gt; shuffles, and it
defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt; regardless of your data or your cluster.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# 200
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.default.parallelism&lt;/code&gt;&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Applies to&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;RDD operations&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;DataFrame and SQL shuffles&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Default&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Depends on parent and cluster manager&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;So if you are tuning a DataFrame job and you change
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.default.parallelism&lt;/code&gt;, nothing happens, which is a frequent source of
confusion. The other thing to know is that input read parallelism is governed by
neither: it comes from file splitting via &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.maxPartitionBytes&lt;/code&gt;.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;19. What is `spark.sql.shuffle.partitions` and why does the default cause trouble?&lt;/summary&gt;

  &lt;p&gt;It is the number of partitions produced by a DataFrame or SQL shuffle, and it
defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;The trouble is that &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt; is a constant, while the right value depends on your
data volume and your core count. Both directions hurt:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Too few for the data.&lt;/strong&gt; 200 partitions over 1 TB means 5 GB per task, which will spill to disk and may fail outright.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Too many for the data.&lt;/strong&gt; 200 partitions over 100 MB means 500 KB per task, so scheduling and task startup dominate and 200 near-empty files get written.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Mismatched to cores.&lt;/strong&gt; With 32 cores, 200 tasks is a sensible several-waves-per-core; with 2,000 cores, most sit idle.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;A reasonable target is partitions in the low hundreds of megabytes, and a total
that is a small multiple of your core count so stragglers average out.&lt;/p&gt;

  &lt;p&gt;Adaptive query execution changes the calculus. With
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.enabled&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.coalescePartitions.enabled&lt;/code&gt;
both &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; by default, Spark writes 200 shuffle partitions and then &lt;strong&gt;merges
the small ones at runtime&lt;/strong&gt; toward
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.advisoryPartitionSizeInBytes&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;64MB&lt;/code&gt;.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;128MB&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;So in modern Spark the better habit is to set the advisory size and leave the
partition count high,&lt;/strong&gt; letting coalescing do the arithmetic, rather than
guessing a single number for every stage in the query.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;20. Which Spark version introduced Log4j 2, and why does it matter?&lt;/summary&gt;

  &lt;p&gt;Spark 3.3.0. The configuration file became &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j2.properties&lt;/code&gt; and the JVM
flag became &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-Dlog4j.configurationFile=&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;It matters because the syntax changed completely, and most logging guides on the
internet predate it. Log4j 1 used a flat &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j.&lt;/code&gt; prefix:&lt;/p&gt;

  &lt;div class=&quot;language-properties highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c&quot;&gt;# Log4j 1, ignored by Spark 3.3.0 and later
&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;log4j.rootLogger&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;INFO, console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;log4j.logger.org.apache.spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;WARN&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Log4j 2 uses named components:&lt;/p&gt;

  &lt;div class=&quot;language-properties highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c&quot;&gt;# Log4j 2
&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;rootLogger.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;info&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;rootLogger.appenderRef.stdout.ref&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;Console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.layout.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;PatternLayout&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.layout.pattern&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;%d{HH:mm:ss} %-5level %logger{1} - %msg%n&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.spark.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.spark&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.spark.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;warn&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;At &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;v4.2.0&lt;/code&gt; the only template Spark ships is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;conf/log4j2.properties.template&lt;/code&gt;; the Log4j 1 template is gone. So a config
written in the old syntax is not an error you will see, it is a file that is
silently ignored, which is the worst kind of failure.&lt;/p&gt;

  &lt;p&gt;Ship it to both sides, since driver and executor answer different questions:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--files&lt;/span&gt; /etc/spark/conf/log4j2.properties &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.driver.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.executor.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  app.jar
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

&lt;/details&gt;

&lt;h2 id=&quot;rdds&quot;&gt;RDDs&lt;/h2&gt;

&lt;p&gt;The original API. Interviewers still ask, because the concepts underneath the DataFrame API are these, and because RDD questions expose whether you understand lineage and partitioning or just the syntax.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;21. What is an RDD?&lt;/summary&gt;

  &lt;p&gt;A Resilient Distributed Dataset: an immutable, partitioned collection of
records that can be operated on in parallel, carrying enough information to
rebuild any lost partition.&lt;/p&gt;

  &lt;p&gt;Unpack the name, because each word is a design decision:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Resilient.&lt;/strong&gt; Spark records &lt;em&gt;how&lt;/em&gt; each partition was derived, not just the data. Lose one and it is recomputed from its parents rather than the job failing.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Distributed.&lt;/strong&gt; The collection is split into partitions spread across executors.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Dataset.&lt;/strong&gt; A collection of records, with no schema and no interpretation of their contents.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;It is also &lt;strong&gt;immutable&lt;/strong&gt;. Every transformation produces a new RDD rather than
modifying the old one, which is what makes lineage a reliable recovery record: a
parent cannot have changed underneath you.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sf&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;24.5&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;nyc&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;61.75&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;totals&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;reduceByKey&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The trade against DataFrames is visibility. Because an RDD holds opaque
objects, Spark cannot see inside your closures, so there is no schema, no
column pruning, no predicate pushdown and no Catalyst. You get control and lose
optimisation, which is why the DataFrame API is the default for structured
data.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;22. What are the five properties of an RDD?&lt;/summary&gt;

  &lt;p&gt;From &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RDD.scala&lt;/code&gt;, every RDD is characterised by five things, three required
and two optional:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;A list of partitions.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;getPartitions&lt;/code&gt;, which defines the parallelism.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A function for computing each partition.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;compute&lt;/code&gt;, which is the actual work.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A list of dependencies on other RDDs.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;getDependencies&lt;/code&gt;, which is the lineage.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Optionally, a partitioner&lt;/strong&gt; for key-value RDDs, such as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HashPartitioner&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Optionally, preferred locations&lt;/strong&gt; for each partition, such as the HDFS block locations.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The first three make the RDD work; the last two make it &lt;em&gt;fast&lt;/em&gt;, and they are the
ones worth talking about:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;The partitioner&lt;/strong&gt; is what lets Spark skip a shuffle. If two RDDs share a partitioner on the join key, matching keys are already co-located, so the join is narrow.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Preferred locations&lt;/strong&gt; are what data locality is built on. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSchedulerImpl&lt;/code&gt; tries to place a task where its input already is, which is why locality levels exist.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getNumPartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;                    &lt;span class=&quot;c1&quot;&gt;# 1
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitioner&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;                           &lt;span class=&quot;c1&quot;&gt;# 4, None for a non-pair RDD
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Naming all five, and then saying which two are optional and why they matter, is
a much stronger answer than listing them.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;23. How does Spark achieve fault tolerance for RDDs?&lt;/summary&gt;

  &lt;p&gt;Through &lt;strong&gt;lineage&lt;/strong&gt;: the chain of transformations from a source to the dataset
in hand. Because Spark recorded how to build every partition, not just the
partition itself, a lost one is rebuilt on a surviving executor.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;lineage&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lineage&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toDebugString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;decode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Two things make this cheaper than it sounds:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Recovery is per partition, not per job.&lt;/strong&gt; Losing one executor costs the partitions it held, which are recomputed while everything else stands.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Only the path matters.&lt;/strong&gt; Spark replays the branch that produced the lost partition, not the whole graph.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  S[&quot;source&quot;] --&amp;gt; A[&quot;map&quot;] --&amp;gt; B[&quot;filter&quot;] --&amp;gt; C[&quot;partition 3 lost&quot;]
  C -.-&amp;gt;|&quot;recompute just this path&quot;| D[&quot;partition 3 rebuilt&amp;lt;br/&amp;gt;on another executor&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The limits are worth volunteering:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Shuffle output is the exception.&lt;/strong&gt; If the shuffle files a completed stage wrote die with their executor, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; resubmits that entire stage, because the next stage has nothing to fetch. That is a stage rerun rather than a task retry, and it is why a job can appear to go backwards.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Long lineages get expensive to replay,&lt;/strong&gt; which is what checkpointing exists to fix: it writes the data to reliable storage and truncates the chain.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A non-deterministic transformation breaks the guarantee,&lt;/strong&gt; because a recomputed partition may not match the lost one.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;24. What is the difference between a narrow and a wide dependency?&lt;/summary&gt;

  &lt;p&gt;In a &lt;strong&gt;narrow&lt;/strong&gt; dependency each parent partition feeds at most one child
partition, so the work can pipeline inside a single task with no data movement.
In a &lt;strong&gt;wide&lt;/strong&gt; dependency a child partition draws from many parents, which
requires a shuffle.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph N[&quot;narrow: map, filter&quot;]
    P0[&quot;P0&quot;] --&amp;gt; C0[&quot;C0&quot;]
    P1[&quot;P1&quot;] --&amp;gt; C1[&quot;C1&quot;]
    P2[&quot;P2&quot;] --&amp;gt; C2[&quot;C2&quot;]
  end
  subgraph W[&quot;wide: groupByKey, join&quot;]
    Q0[&quot;P0&quot;] --&amp;gt; D0[&quot;C0&quot;]
    Q0 --&amp;gt; D1[&quot;C1&quot;]
    Q1[&quot;P1&quot;] --&amp;gt; D0
    Q1 --&amp;gt; D1
    Q2[&quot;P2&quot;] --&amp;gt; D0
    Q2 --&amp;gt; D1
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Narrow&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Wide&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Classes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OneToOneDependency&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RangeDependency&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleDependency&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Examples&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;map&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;flatMap&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapPartitions&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;union&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;join&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Data movement&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;None&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Across the network&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Stage&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Pipelines into the current stage&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Creates a boundary&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;On failure&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Recompute one parent partition&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;May need to recompute many, or rerun the stage&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;The recovery difference is the part people miss. A lost narrow partition needs
one parent partition recomputed. A lost partition after a wide dependency may
need data from &lt;em&gt;every&lt;/em&gt; parent partition, which is far more expensive, and is
another reason shuffles are costly beyond their immediate network cost.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce&lt;/code&gt; is the interesting edge case: it is narrow, because several parent
partitions feed one child without a shuffle, which is exactly why it can only
reduce the count and may leave partitions uneven.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;25. Where exactly does Spark cut a stage?&lt;/summary&gt;

  &lt;p&gt;At every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleDependency&lt;/code&gt; in the lineage, and nowhere else.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; walks the graph backwards from the action. Each time it crosses a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleDependency&lt;/code&gt; it ends the current stage and starts a new one; everything
narrow is absorbed into the stage it is already in.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph S0[&quot;stage 0&quot;]
    R[&quot;read&quot;] --&amp;gt; F[&quot;filter&quot;] --&amp;gt; W[&quot;withColumn&quot;] --&amp;gt; PA[&quot;partial aggregate&quot;]
  end
  PA --&amp;gt;|&quot;ShuffleDependency&quot;| SH[(&quot;shuffle files&amp;lt;br/&amp;gt;on local disk&quot;)]
  subgraph S1[&quot;stage 1&quot;]
    SH --&amp;gt; FA[&quot;final aggregate&quot;] --&amp;gt; O[&quot;write&quot;]
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The practical consequences:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;A long chain of narrow operations costs no extra stages.&lt;/strong&gt; Twenty chained &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; calls still run in one stage, fused by whole-stage codegen into a single loop.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The stage count is the shuffle count plus one,&lt;/strong&gt; per job. Counting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt; nodes in a physical plan gives you the same number.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Every stage but the last is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleMapStage&lt;/code&gt;,&lt;/strong&gt; whose purpose is to produce shuffle output. The last is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ResultStage&lt;/code&gt;, which computes what the action asked for.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Stages also enforce a barrier: stage 1 cannot start until stage 0 has finished
writing &lt;em&gt;all&lt;/em&gt; its output, because a reduce task needs its slice from every map
task. That is why one straggler in stage 0 delays the entire next stage, and why
skew is so expensive.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;26. What is the difference between `reduceByKey` and `groupByKey`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt; combines values on the map side before the shuffle;
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt; shuffles every value and combines afterwards.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# prefer this: combines locally first
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;pairs&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;reduceByKey&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# shuffles every value across the network
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;pairs&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupByKey&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mapValues&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph R[&quot;reduceByKey&quot;]
    A1[&quot;(&apos;sf&apos;,1) (&apos;sf&apos;,1) (&apos;sf&apos;,1)&quot;] --&amp;gt; A2[&quot;local combine&amp;lt;br/&amp;gt;(&apos;sf&apos;,3)&quot;] --&amp;gt; A3[&quot;shuffle&amp;lt;br/&amp;gt;1 record&quot;]
  end
  subgraph G[&quot;groupByKey&quot;]
    B1[&quot;(&apos;sf&apos;,1) (&apos;sf&apos;,1) (&apos;sf&apos;,1)&quot;] --&amp;gt; B3[&quot;shuffle&amp;lt;br/&amp;gt;3 records&quot;] --&amp;gt; B4[&quot;combine after&quot;]
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;With a hot key, the difference is the number of records crossing the network:
one per key per partition for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt;, versus every record for
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt;. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt; also has a correctness-adjacent risk: all values for
one key must fit in memory on the reduce side, so a hot key can cause an
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; that &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt; would not.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The subtlety that makes a strong answer:&lt;/strong&gt; a map-side combine disqualifies
Spark’s two faster shuffle writers, because they cannot combine. So
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt; shuffles less data through a more expensive writer. It is still
almost always the right choice, because network volume dominates, but the
trade-off is real and worth naming.&lt;/p&gt;

  &lt;p&gt;Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt; only when you genuinely need every value, for example to
collect them into a list, and even then consider &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;aggregateByKey&lt;/code&gt; which combines
into a buffer as it goes.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;27. What is the difference between `map` and `mapPartitions`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;map&lt;/code&gt; applies your function once per element. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapPartitions&lt;/code&gt; applies it once
per partition, handing you an iterator over that partition’s elements and
expecting an iterator back.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# once per row: a connection per row, which is fatal
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;lookup&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;open_conn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# once per partition: one connection amortised over thousands of rows
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;enrich&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;conn&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;open_conn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
        &lt;span class=&quot;k&quot;&gt;yield&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;lookup&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;conn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;close&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mapPartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;enrich&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapPartitions&lt;/code&gt; when there is per-partition setup worth amortising: a
database or HTTP connection, loading a model, or compiling a regex.&lt;/p&gt;

  &lt;p&gt;Two cautions:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Do not materialise the partition.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;list(rows)&lt;/code&gt; inside the function loads the whole partition into memory and defeats the streaming design. Use a generator, as above.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Return an iterator,&lt;/strong&gt; not a single value. Returning a list works but holds everything in memory.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapPartitionsWithIndex&lt;/code&gt; additionally gives you the partition number, which is
the standard trick for inspecting how data is distributed:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mapPartitionsWithIndex&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;_&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))]).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;That prints rows per partition, which is how you confirm skew on an RDD.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;28. What is the difference between `map` and `flatMap`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;map&lt;/code&gt; returns exactly one output per input. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;flatMap&lt;/code&gt; returns an iterable per
input and flattens the results, so it can return zero, one or many.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;lines&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;a b c&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;d e&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;lines&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;split&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# [[&apos;a&apos;,&apos;b&apos;,&apos;c&apos;], [&apos;d&apos;,&apos;e&apos;]]        two elements, each a list
&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;lines&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;flatMap&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;split&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# [&apos;a&apos;,&apos;b&apos;,&apos;c&apos;,&apos;d&apos;,&apos;e&apos;]             five elements
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The “zero” case is the one worth knowing, because it makes &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;flatMap&lt;/code&gt; a combined
map and filter:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# parse, and silently drop anything unparseable, in one pass
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;flatMap&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parse&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;valid&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;else&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[])&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Both are narrow transformations, so neither causes a shuffle, and both pipeline
into the surrounding stage. The DataFrame equivalent of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;flatMap&lt;/code&gt; for a column
is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explode&lt;/code&gt;, which turns an array column into one row per element.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;29. What is the difference between `repartition` and `coalesce`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition&lt;/code&gt; does a full shuffle to reach any target count and balances the
result. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce&lt;/code&gt; merges existing partitions without a full shuffle, so it can
only reduce the count and may leave partitions uneven.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt;&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(n)&lt;/code&gt;&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shuffle&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Full&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;None by default&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Direction&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Up or down&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Down only&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Balance&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Even&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Possibly uneven&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Dependency&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Wide, so a new stage&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Narrow&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;coalesce&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# cheap, before a write
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;400&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;k&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;c1&quot;&gt;# balanced, before heavy work
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;                &lt;span class=&quot;c1&quot;&gt;# hash-partitioned by a column
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The trap is what &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce&lt;/code&gt; does upstream,&lt;/strong&gt; and it is a favourite interview
question. Because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce&lt;/code&gt; is narrow, it does not create a stage boundary, so
the reduced parallelism propagates &lt;em&gt;backwards&lt;/em&gt; into the stage feeding it.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(1)&lt;/code&gt; before a heavy transformation can make that entire transformation
run in a single task.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph C[&quot;coalesce(2): merge, no shuffle&quot;]
    A1[&quot;P0 10 MB&quot;] --&amp;gt; M1[&quot;P0 + P1&amp;lt;br/&amp;gt;30 MB&quot;]
    A2[&quot;P1 20 MB&quot;] --&amp;gt; M1
    A3[&quot;P2 5 MB&quot;] --&amp;gt; M2[&quot;P2 + P3&amp;lt;br/&amp;gt;45 MB&quot;]
    A4[&quot;P3 40 MB&quot;] --&amp;gt; M2
  end
  subgraph R[&quot;repartition(2): shuffle, balanced&quot;]
    B1[&quot;P0 10 MB&quot;] --&amp;gt; SH[(&quot;shuffle&quot;)]
    B2[&quot;P1 20 MB&quot;] --&amp;gt; SH
    B3[&quot;P2 5 MB&quot;] --&amp;gt; SH
    B4[&quot;P3 40 MB&quot;] --&amp;gt; SH
    SH --&amp;gt; N1[&quot;37 MB&quot;]
    SH --&amp;gt; N2[&quot;38 MB&quot;]
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;So if you need one output file, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(1)&lt;/code&gt; pays for a shuffle but keeps
the upstream stage parallel. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(1)&lt;/code&gt; is only safe when the work before it
is trivial.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;30. What is the difference between `cache` and `persist`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache()&lt;/code&gt; is exactly &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;persist()&lt;/code&gt; with the default storage level.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;persist(level)&lt;/code&gt; lets you choose the level.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                                    &lt;span class=&quot;c1&quot;&gt;# default level
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;persist&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;StorageLevel&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;MEMORY_AND_DISK_SER&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;  &lt;span class=&quot;c1&quot;&gt;# explicit
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The default differs by API, which is the part that surprises people:&lt;/strong&gt;&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;API&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Default level&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Behaviour when it does not fit&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rdd.cache()&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Memory only&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Partitions are dropped and recomputed&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.cache()&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Disk and memory, deserialized&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Partitions spill to disk&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;So an RDD cache that exceeds memory silently recomputes, which looks like the
cache doing nothing at all. A DataFrame cache spills instead, which is slower
than memory but still avoids recomputation. If an RDD cache seems to have no
effect, that asymmetry is usually why.&lt;/p&gt;

  &lt;p&gt;Both are &lt;strong&gt;lazy&lt;/strong&gt;: they mark the dataset and populate it on the next action. The
exception is SQL’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CACHE TABLE&lt;/code&gt;, which is eager by default.&lt;/p&gt;

  &lt;p&gt;Release it explicitly when you are done, because cached blocks hold storage
memory for the life of the application and squeeze execution memory from every
later stage:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unpersist&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;catalog&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;clearCache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;       &lt;span class=&quot;c1&quot;&gt;# everything
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;31. What are the RDD storage levels?&lt;/summary&gt;

  &lt;p&gt;The levels combine three choices: memory or disk, serialized or not, and
replicated or not.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Level&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Memory&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Disk&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Serialized&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Replicas&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_ONLY&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_ONLY_SER&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Spills&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK_SER&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Spills&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DISK_ONLY&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*_2&lt;/code&gt; variants&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;as above&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;as above&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;as above&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;2&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OFF_HEAP&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Off-heap&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;1&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;How to choose:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Serialized&lt;/strong&gt; forms are far more compact, so more fits in memory, at the cost of CPU to deserialize on every read. Worth it when memory is the constraint.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_2&lt;/code&gt; replicas&lt;/strong&gt; put a copy on a second node, so losing an executor does not force recomputation. Worth it when recomputation is very expensive; otherwise it doubles the memory cost.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DISK_ONLY&lt;/code&gt;&lt;/strong&gt; is right when recomputing is more expensive than reading back, which is the case after a long or costly lineage.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OFF_HEAP&lt;/code&gt;&lt;/strong&gt; avoids garbage collection pressure but needs &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.offHeap.enabled&lt;/code&gt;, which defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;, and a size.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Read the storage tab of the UI to check what actually happened. A dataset
showing well below 100% in memory is being partly recomputed or read from disk
on every use, which usually means the cache is not paying for itself.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;32. What are broadcast variables?&lt;/summary&gt;

  &lt;p&gt;A broadcast variable is a read-only value sent to each executor &lt;strong&gt;once&lt;/strong&gt; and
cached there, rather than serialized into every task.&lt;/p&gt;

  &lt;p&gt;The guide describes them as keeping “a read-only variable cached on each machine
rather than shipping a copy of it with tasks”.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;({&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sf&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;San Francisco&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;nyc&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;New York&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;})&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;resolved&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sf&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;nyc&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;sf&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;resolved&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# [&apos;San Francisco&apos;, &apos;New York&apos;, &apos;San Francisco&apos;]
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Without the broadcast, that dictionary is captured by the closure and serialized
with &lt;strong&gt;every task&lt;/strong&gt;. With 10,000 tasks and a 10 MB lookup table, that is 100 GB
of serialization instead of one copy per executor.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph W[&quot;captured variable&quot;]
    D1[&quot;driver&quot;] --&amp;gt;|&quot;copy per task&amp;lt;br/&amp;gt;x 10,000&quot;| T1[&quot;tasks&quot;]
  end
  subgraph B[&quot;broadcast&quot;]
    D2[&quot;driver&quot;] --&amp;gt;|&quot;one copy per executor&amp;lt;br/&amp;gt;x 20&quot;| E2[&quot;executors&quot;] --&amp;gt; T2[&quot;all tasks read locally&quot;]
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Practical notes:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Read-only.&lt;/strong&gt; Mutating &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.value&lt;/code&gt; on an executor changes only that copy and nothing comes back.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Call &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.unpersist()&lt;/code&gt;&lt;/strong&gt; when finished, or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.destroy()&lt;/code&gt; to release it permanently, if it is large and no longer needed.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;This is the same machinery&lt;/strong&gt; behind a broadcast hash join, which is why &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt; exists and why broadcasting something large pressures the driver, which must assemble it first.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;33. What are accumulators, and what is the catch?&lt;/summary&gt;

  &lt;p&gt;Accumulators are write-only-from-tasks counters that the driver can read. Tasks
add to them; only the driver should read the value.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;errors&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;accumulator&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;parse&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;line&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;try&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
        &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;json&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;loads&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;line&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;except&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;ValueError&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
        &lt;span class=&quot;n&quot;&gt;errors&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;add&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
        &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;bp&quot;&gt;None&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;parsed&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parse&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;is&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;not&lt;/span&gt; &lt;span class=&quot;bp&quot;&gt;None&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;parsed&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;errors&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The catch is the guarantee, and it is the whole question.&lt;/strong&gt; The guide is
explicit:&lt;/p&gt;

  &lt;blockquote&gt;
    &lt;p&gt;For accumulator updates performed inside &lt;strong&gt;actions only&lt;/strong&gt;, Spark guarantees
that each task’s update to the accumulator will only be applied once, i.e.
restarted tasks will not update the value. In transformations, users should be
aware of that each task’s update may be applied more than once if tasks or job
stages are re-executed.&lt;/p&gt;
  &lt;/blockquote&gt;

  &lt;p&gt;This is not theoretical, and it follows directly from lazy evaluation:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;accumulator&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;counted&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;add&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;counted&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;();&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# 100
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;counted&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;();&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# 200, for the same 100 rows
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The uncached dataset is recomputed for the second action, so the accumulator
increments again. Add &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.cache()&lt;/code&gt; and it stays at 100. A task retry has the same
effect.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;So use accumulators for diagnostics you can tolerate being approximate, and
never as the source of a number the output depends on.&lt;/strong&gt; If you need an exact
count, compute it as data with an aggregation.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;34. What is a partitioner, and when does it help?&lt;/summary&gt;

  &lt;p&gt;A partitioner is the function mapping a key to a partition number. Spark ships
two:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HashPartitioner&lt;/code&gt;&lt;/strong&gt;, which uses &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hash(key) % numPartitions&lt;/code&gt;. The default for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt; and friends.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RangePartitioner&lt;/code&gt;&lt;/strong&gt;, which samples the data to find range boundaries so that partitions are roughly equal and ordered. Used by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sortByKey&lt;/code&gt; and by a global &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;orderBy&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;It helps because &lt;strong&gt;a known partitioner lets Spark skip a shuffle&lt;/strong&gt;. If two RDDs
share the same partitioner on the join key, matching keys are already
co-located, so the join becomes a narrow dependency.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;part&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rdd_a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;part&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# note the cache
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rdd_b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;part&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;joined&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;                       &lt;span class=&quot;c1&quot;&gt;# no shuffle
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache&lt;/code&gt; is not optional.&lt;/strong&gt; Without persisting, the partitioning is
recomputed each time the RDD is used, so you pay the shuffle you were trying to
avoid.&lt;/p&gt;

  &lt;p&gt;You can inspect and set it:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;pairs&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitioner&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;                      &lt;span class=&quot;c1&quot;&gt;# None until you set one
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tagged&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pairs&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;HashPartitioner&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The DataFrame equivalents are &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(col)&lt;/code&gt; for the same effect within a
job, and bucketing for the effect to persist across jobs, recorded in the
metastore.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;35. What does `toDebugString` tell you?&lt;/summary&gt;

  &lt;p&gt;It prints the lineage of an RDD as an indented tree: each level is an RDD, with
its type, its id and its partition count.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;lin&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;x&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;%&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;reduceByKey&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lin&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;toDebugString&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;decode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;(4) PythonRDD[3] at RDD at PythonRDD.scala:53 []
 |  MapPartitionsRDD[2] at mapPartitions at ... []
 |  ShuffledRDD[1] at partitionBy at ... []
 +-(4) PairwiseRDD[0] at reduceByKey at ... []
    |  ParallelCollectionRDD ... []
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;How to read it:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;The number in parentheses&lt;/strong&gt; is the partition count at that level.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Indentation changes, marked by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;+-&lt;/code&gt;,&lt;/strong&gt; are shuffle boundaries. Count them and you have the stage count minus one.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Read bottom up:&lt;/strong&gt; the source is at the bottom, the RDD you called it on is at the top.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;It is the fastest way to answer “how many stages will this take” for RDD code,
and to spot an accidental shuffle. For DataFrames the equivalent is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.explain(&quot;formatted&quot;)&lt;/code&gt;, where each &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt; is the same signal.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;36. When should you still use RDDs?&lt;/summary&gt;

  &lt;p&gt;When you need something the structured APIs do not expose. The honest list is
short:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Custom partitioning logic&lt;/strong&gt; beyond hash or range, for example routing by a business rule.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Per-partition resource handling&lt;/strong&gt; via &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapPartitions&lt;/code&gt;, although the DataFrame API has &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapInPandas&lt;/code&gt; for much of this now.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Genuinely unstructured data&lt;/strong&gt; with no schema to impose: raw text, binary records, custom parsing where a row shape only emerges after processing.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Low-level control over lineage and checkpointing&lt;/strong&gt;, in iterative algorithms.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;For everything with a schema, the DataFrame API wins, and the reason is
structural rather than a matter of taste: Catalyst can see a DataFrame plan and
cannot see inside an RDD closure. That means no column pruning, no predicate
pushdown, no join strategy selection and no whole-stage codegen for RDD code.
Tungsten’s compact binary rows also avoid materialising JVM objects, which RDD
code cannot avoid.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;A good answer also notes that RDDs did not go away.&lt;/strong&gt; DataFrames are built on
them: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.rdd&lt;/code&gt; gives you the underlying RDD, and the scheduler works entirely in
RDD terms. So understanding RDDs is still how you understand what a DataFrame
job actually does.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;37. What is the difference between `collect` and `take`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; brings every row to the driver. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;take(n)&lt;/code&gt; returns the first n rows
and tries to read as little as possible to get them.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# everything, bounded only by driver memory
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;head&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;take&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;       &lt;span class=&quot;c1&quot;&gt;# 5 rows, usually from one partition
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;5&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;              &lt;span class=&quot;c1&quot;&gt;# take plus formatting
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;take(n)&lt;/code&gt; starts by scanning one partition. If that yields fewer than n rows it
scans more, increasing the number it tries each round. So on a large dataset it
usually touches a tiny fraction of the data.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; is bounded by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.maxResultSize&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;, and by the
driver heap, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.memory&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;. Exceeding the first raises an
error naming the config; exceeding the second is an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;This is the classic way to kill a driver,&lt;/strong&gt; and the answer an interviewer
wants is what to do instead:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;To inspect, use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show()&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;take()&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;limit()&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;To count, use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count()&lt;/code&gt;, which aggregates rather than transferring rows.&lt;/li&gt;
    &lt;li&gt;To export, use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write&lt;/code&gt; so executors write in parallel and only a status returns.&lt;/li&gt;
    &lt;li&gt;To hand data to Python, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;toPandas()&lt;/code&gt; has the same problem as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt;; prefer writing out or using &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapInPandas&lt;/code&gt; to keep the work distributed.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;38. What is the difference between `reduce` and `fold`?&lt;/summary&gt;

  &lt;p&gt;Both aggregate an RDD to a single value using a binary function. The difference
is the zero value.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;reduce&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# 10
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fold&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# 10
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([]).&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;reduce&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;                &lt;span class=&quot;c1&quot;&gt;# raises
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([]).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fold&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;               &lt;span class=&quot;c1&quot;&gt;# 0
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;So &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fold&lt;/code&gt; is defined on an empty RDD and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduce&lt;/code&gt; is not.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The trap is that the zero value must be a true identity,&lt;/strong&gt; because Spark
applies it once per partition as well as when combining partitions. With four
partitions and a zero of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1&lt;/code&gt; for addition, you would add 1 five times, not once.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;aggregate&lt;/code&gt; generalises both: it takes a zero, a function to fold a partition
into an accumulator, and a function to combine accumulators. That is what you
need when the accumulator type differs from the element type, such as computing
a mean:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;total&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;n&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parallelize&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;aggregate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;x&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;acc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# fold within a partition
&lt;/span&gt;    &lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]))&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# combine partitions
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;total&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;/&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;n&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Note also that all three are &lt;strong&gt;actions&lt;/strong&gt;, returning to the driver, whereas
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt; is a transformation returning a distributed RDD.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;39. Why are RDD operations on key-value pairs special?&lt;/summary&gt;

  &lt;p&gt;Because only pair RDDs can be partitioned &lt;em&gt;by key&lt;/em&gt;, and key-partitioning is
what enables the optimisations that matter.&lt;/p&gt;

  &lt;p&gt;Being a pair RDD unlocks:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionBy&lt;/code&gt;&lt;/strong&gt;, and therefore shuffle-free joins between co-partitioned RDDs.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Map-side combines&lt;/strong&gt;: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;aggregateByKey&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;combineByKey&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Key-wise operations&lt;/strong&gt;: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;join&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cogroup&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;lookup&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sortByKey&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;pairs&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fare_amount&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# now a pair RDD
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;pairs&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;reduceByKey&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;In Scala these arrive by implicit conversion on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RDD[(K, V)]&lt;/code&gt; through
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PairRDDFunctions&lt;/code&gt;, which is why they are not on the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RDD&lt;/code&gt; class itself. In
Python any RDD whose elements are 2-tuples qualifies.&lt;/p&gt;

  &lt;p&gt;The distinction matters in an interview because it explains &lt;em&gt;why&lt;/em&gt; the API is
shaped this way: partitioning is defined on keys, so anything that benefits from
co-location has to be expressed as a pair. The DataFrame equivalent is that
these optimisations attach to columns you group or join on, which is why
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(col)&lt;/code&gt; and bucketing exist.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;40. What happens if a task fails?&lt;/summary&gt;

  &lt;p&gt;It is retried, and if retries are exhausted the stage and then the job fail.&lt;/p&gt;

  &lt;p&gt;The sequence:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;The task fails and reports back to the driver.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSetManager&lt;/code&gt; reschedules it, preferring a different executor if the failure looked executor-specific.&lt;/li&gt;
    &lt;li&gt;Retries continue up to &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.task.maxFailures&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4&lt;/code&gt;&lt;/strong&gt;, counted per task across the stage.&lt;/li&gt;
    &lt;li&gt;On the fourth failure the stage fails, and the job fails with it.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;Two related mechanisms are worth naming:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Blacklisting, now called excludeOnFailure.&lt;/strong&gt; If one executor or node keeps failing tasks, Spark can stop scheduling onto it, which prevents a single bad machine from burning through the retry budget of every task.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Speculation.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.speculation&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;, duplicates tasks that are running much slower than their peers and takes whichever finishes. It helps with a slow &lt;em&gt;node&lt;/em&gt; and hurts with &lt;em&gt;skew&lt;/em&gt;, because the duplicate is equally slow.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The distinct case is losing shuffle output rather than a task:&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  T1[&quot;task attempt 1 fails&quot;] --&amp;gt; T2[&quot;attempt 2, preferably&amp;lt;br/&amp;gt;on another executor&quot;]
  T2 --&amp;gt; T3[&quot;attempt 3&quot;]
  T3 --&amp;gt; T4[&quot;attempt 4&quot;]
  T4 --&amp;gt; F[&quot;spark.task.maxFailures reached:&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;stage fails, job fails&amp;lt;/b&amp;gt;&quot;]
  T2 -.-&amp;gt;|&quot;same executor keeps failing&quot;| X[&quot;excludeOnFailure:&amp;lt;br/&amp;gt;stop scheduling there&quot;]
  T2 -.-&amp;gt;|&quot;slow, not failing&quot;| SP[&quot;speculation may&amp;lt;br/&amp;gt;duplicate the task&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;That is a stage rerun, and it is why a job can appear to go backwards in the UI.
An external shuffle service avoids it by serving shuffle blocks independently of
the executor that wrote them.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;dataframes-datasets-and-spark-sql&quot;&gt;DataFrames, Datasets and Spark SQL&lt;/h2&gt;

&lt;p&gt;The API you should reach for by default. These questions separate people who use it from people who understand what it does underneath.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;41. What is a DataFrame?&lt;/summary&gt;

  &lt;p&gt;A distributed collection of rows organised into named columns, with a schema
Spark knows at plan time. That last part is the whole point.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;s3a://lakehouse-prod/warehouse/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;printSchema&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Because Spark knows the schema and the operations, a DataFrame plan goes through
Catalyst, which can:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Prune columns&lt;/strong&gt; nobody selected, so they are never read from disk.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Push predicates&lt;/strong&gt; into the scan, so rows are filtered at the source.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Choose a join strategy&lt;/strong&gt; based on estimated sizes.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Fuse operators&lt;/strong&gt; into a single generated loop via whole-stage codegen.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Store rows in Tungsten’s binary format&lt;/strong&gt;, avoiding JVM objects entirely.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;An RDD of the same data gets none of that, because its records are opaque
objects and its closures are opaque functions.&lt;/p&gt;

  &lt;p&gt;Two clarifications interviewers like:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;A DataFrame in Scala is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Dataset[Row]&lt;/code&gt;.&lt;/strong&gt; It is not a separate type; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Row&lt;/code&gt; is the untyped record.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It is still immutable and lazy.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; returns a new DataFrame, and nothing runs until an action.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;42. What is a Dataset, and how does it differ from a DataFrame?&lt;/summary&gt;

  &lt;p&gt;A Dataset is a typed distributed collection that still runs through Spark SQL’s
optimised engine. It gives you compile-time type safety and your own case
classes instead of generic rows.&lt;/p&gt;

  &lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Trip&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;tripId&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;String&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fareAmount&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Double&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cityId&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;String&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;val&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;Dataset&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;kt&quot;&gt;Trip&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;as&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;kt&quot;&gt;Trip&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;]&lt;/span&gt;
&lt;span class=&quot;nv&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;fareAmount&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;50&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;map&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;_&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;cityId&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;// typed, checked at compile time&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;In Scala, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataFrame&lt;/code&gt; is simply &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Dataset[Row]&lt;/code&gt;, so they are the same machinery
with different static types.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;DataFrame&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Dataset&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Element type&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Row&lt;/code&gt;, untyped&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Your class, typed&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Errors caught&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;At analysis, runtime&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;At compile time&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Available in&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;All four languages&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Scala and Java only&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Encoder&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Built in&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Generated for your type&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;Python has no typed Dataset API,&lt;/strong&gt; and the reason is worth stating: the API
exists to give compile-time checking, and Python has no compile step to check
in. In PySpark, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DataFrame&lt;/code&gt; is what you get.&lt;/p&gt;

  &lt;p&gt;The cost of typed Datasets is real too: a lambda like &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_.fareAmount &amp;gt; 50&lt;/code&gt; is
opaque to Catalyst in the same way an RDD closure is, so it cannot be pushed
down. Typed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;map&lt;/code&gt; trade optimisation for type safety, which is why
even in Scala, column expressions are often preferred for hot paths.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;43. Compare RDD, DataFrame and Dataset.&lt;/summary&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;RDD&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;DataFrame&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Dataset&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Level&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Low&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Structured&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Typed structured&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Schema&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;None&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, from your type&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Catalyst optimisation&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, for column expressions&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Whole-stage codegen&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Tungsten binary format&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Type safety&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Compile time (Scala)&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Runtime&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Compile time&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Python support&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No typed API&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Serialization&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Java or Kryo&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Tungsten encoders&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Tungsten encoders&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;How to choose, in one line each:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;DataFrame or SQL&lt;/strong&gt; for almost everything. It is the fastest path because Catalyst can see it all.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Dataset&lt;/strong&gt; in Scala or Java when compile-time safety on a domain type is worth losing pushdown through typed lambdas.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;RDD&lt;/strong&gt; only for custom partitioning, per-partition resource handling, or data with no schema.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The evolution is worth a sentence: RDDs came first, DataFrames added a schema
and the optimiser, and Datasets added types back on top of the optimiser. They
are layered, not alternatives, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.rdd&lt;/code&gt; drops down a level whenever you
need it.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The strong closing point:&lt;/strong&gt; hand-written RDD code loses to DataFrame code on
structured data not because the author wrote it badly, but because the engine is
allowed to rewrite the DataFrame version and is not allowed to touch the RDD
one.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;44. What is Catalyst?&lt;/summary&gt;

  &lt;p&gt;Catalyst is Spark SQL’s query optimiser: the component that turns a query you
wrote into a physical plan the engine runs.&lt;/p&gt;

  &lt;p&gt;It is a &lt;strong&gt;rule-based tree transformation framework&lt;/strong&gt;. Plans are trees, rules are
functions from tree to tree, and the optimiser applies batches of rules until
the tree stops changing. That design is why Spark SQL is extensible: a new
optimisation is a new rule, and you can add your own through session extensions.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;SQL or DataFrame&quot;] --&amp;gt; B[&quot;unresolved&amp;lt;br/&amp;gt;logical plan&quot;]
  B --&amp;gt;|&quot;catalog&quot;| C[&quot;analysed&amp;lt;br/&amp;gt;logical plan&quot;]
  C --&amp;gt;|&quot;rules&quot;| D[&quot;optimised&amp;lt;br/&amp;gt;logical plan&quot;]
  D --&amp;gt;|&quot;strategies + cost&quot;| E[&quot;physical plan&quot;]
  E --&amp;gt;|&quot;codegen&quot;| F[&quot;JVM bytecode&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;What Catalyst gives you in practice, in rough order of value:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Column pruning&lt;/strong&gt;, so unread columns are never touched.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Predicate pushdown&lt;/strong&gt;, so filtering happens at the source.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Join strategy selection&lt;/strong&gt;, so a small side gets broadcast.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Constant folding and expression simplification.&lt;/strong&gt;&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Whole-stage code generation&lt;/strong&gt;, producing tight loops rather than operator-by-operator iteration.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The idea underneath all of it is lazy evaluation. Catalyst can only optimise a
plan it can see in full, which is exactly what laziness provides.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;45. What are Catalyst&apos;s phases?&lt;/summary&gt;

  &lt;p&gt;Five, and naming them in order with what each does is a strong answer.&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Parse.&lt;/strong&gt; SQL text or DataFrame calls become an &lt;em&gt;unresolved logical plan&lt;/em&gt;: a tree with relation and column names that have not been checked against anything.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Analyse.&lt;/strong&gt; The analyzer resolves table names, column names, data types and function calls against the catalog. This is where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AnalysisException&lt;/code&gt; comes from, and why a typo in a column name surfaces here rather than at parse time.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Logical optimisation.&lt;/strong&gt; Rule batches rewrite the plan: predicate pushdown, column pruning, constant folding, boolean simplification, filter and projection reordering, subquery flattening. The output is still logical, meaning it says &lt;em&gt;what&lt;/em&gt;, not &lt;em&gt;how&lt;/em&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Physical planning.&lt;/strong&gt; Strategies turn each logical operator into one or more physical ones, for example a logical join into a broadcast hash join, shuffle hash join or sort-merge join. Costs choose between candidates.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Code generation.&lt;/strong&gt; Whole-stage codegen fuses the operators in a stage into one generated Java method, compiled with Janino, so intermediate rows never become objects.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;You can see each stage:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;extended&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# parsed, analysed, optimised, physical, in that order
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The distinction to be crisp about&lt;/strong&gt; is logical versus physical. Logical
optimisation is algebraic and knows nothing about the cluster; physical planning
is where partitioning, shuffles and join implementations are decided.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;46. What is predicate pushdown?&lt;/summary&gt;

  &lt;p&gt;Moving a filter as close to the data as possible, so that fewer rows are ever
read or carried up the plan.&lt;/p&gt;

  &lt;p&gt;There are two levels, and distinguishing them is a good signal:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Within the plan.&lt;/strong&gt; Catalyst moves a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Filter&lt;/code&gt; below a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Project&lt;/code&gt; or through a join, so downstream operators see fewer rows.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Into the data source.&lt;/strong&gt; For Parquet or ORC, the predicate is handed to the reader, which skips entire row groups using footer statistics. For JDBC, it becomes a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE&lt;/code&gt; clause in the SQL sent to the database.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;You can confirm it in the plan:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;50&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;formatted&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;FileScan parquet [fare_amount#19,city_id#21]
  DataFilters: [isnotnull(fare_amount#19), (fare_amount#19 &amp;gt; 50.0)]
  PushedFilters: [IsNotNull(fare_amount), GreaterThan(fare_amount,50.0)]
  ReadSchema: struct&amp;lt;fare_amount:double,city_id:string&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushedFilters&lt;/code&gt; is the line that matters: those predicates reached the file
format. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.parquet.filterPushdown&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;What blocks it&lt;/strong&gt; is the practically useful half:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Wrapping the column in a function.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;to_date(ts) = &apos;2026-09-17&apos;&lt;/code&gt; cannot push down; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ts &amp;gt;= &apos;...&apos; AND ts &amp;lt; &apos;...&apos;&lt;/code&gt; can.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A UDF in the predicate,&lt;/strong&gt; because Catalyst cannot see inside it.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Filtering after a non-deterministic operation,&lt;/strong&gt; which would change semantics.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;47. What is column pruning?&lt;/summary&gt;

  &lt;p&gt;Reading only the columns the query actually needs, and dropping the rest as
early as possible in the plan.&lt;/p&gt;

  &lt;p&gt;On a columnar format such as Parquet or ORC, unread columns are never touched on
disk at all, so a table with 200 columns queried for 3 reads roughly 1.5% of the
bytes. On a row format such as JSON or CSV the whole row must still be read and
parsed, so pruning helps less.&lt;/p&gt;

  &lt;p&gt;Confirm it by reading &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReadSchema&lt;/code&gt; in the plan:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;ReadSchema: struct&amp;lt;fare_amount:double,city_id:string&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Two columns, not the whole table.&lt;/p&gt;

  &lt;p&gt;What defeats it:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select(&quot;*&quot;)&lt;/code&gt;&lt;/strong&gt;, explicitly asking for everything.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.rdd&lt;/code&gt; or a typed Dataset lambda,&lt;/strong&gt; which forces Spark to materialise the whole row because it cannot see which fields you touch.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Caching before selecting.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.cache()&lt;/code&gt; caches the columns the plan needed at that point, so caching a wide DataFrame and then selecting three columns stores all of them.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The practical habit: &lt;strong&gt;select the columns you need immediately after reading&lt;/strong&gt;,
not at the end of the pipeline. It costs nothing and it is the difference between
reading 1.5% and 100% of a wide table.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;48. What is whole-stage code generation?&lt;/summary&gt;

  &lt;p&gt;An optimisation where Catalyst fuses all the operators in a stage into a single
generated Java method, rather than running each operator as a separate iterator
that passes rows to the next.&lt;/p&gt;

  &lt;p&gt;Without it, a scan-filter-project chain is three objects calling &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;next()&lt;/code&gt; on each
other, with a virtual call and an intermediate row per step. With it, Spark emits
roughly the loop you would write by hand:&lt;/p&gt;

  &lt;div class=&quot;language-java highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;// conceptually, for filter + project over a scan&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;while&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;scan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;hasNext&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;())&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;nc&quot;&gt;InternalRow&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;row&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;scan&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;next&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;();&lt;/span&gt;
  &lt;span class=&quot;kt&quot;&gt;double&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;row&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;getDouble&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;(!&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;row&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;isNullAt&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;amp;&amp;amp;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;50.0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;row&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;getUTF8String&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;));&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;You can see it in the plan. Operators marked with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*&lt;/code&gt; and a number share one
generated stage:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;*(1) HashAggregate(keys=[city_id#21], ...)
+- Exchange hashpartitioning(city_id#21, 200)
   +- *(2) HashAggregate(keys=[city_id#21], ...)
      +- *(2) Project [city_id#21]
         +- *(2) Filter (fare_amount#19 &amp;gt; 50.0)
            +- *(2) FileScan parquet
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Everything marked &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*(2)&lt;/code&gt; was fused into one method.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;What breaks the fusion&lt;/strong&gt; is the useful part to know: a Python UDF, because
rows must leave the JVM; some complex expressions; and plans that exceed the
generated-method size limit, at which point Spark falls back to the
interpreted path. Losing codegen is a large part of why Python UDFs cost what
they do.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;49. How do you read a query plan?&lt;/summary&gt;

  &lt;p&gt;Start with the mode that matches the question:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;formatted&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# the readable one: tree plus per-node details
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;extended&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;# parsed, analysed, optimised, physical
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;cost&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;        &lt;span class=&quot;c1&quot;&gt;# with statistics, where available
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;True&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# same as extended
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Then read the &lt;strong&gt;physical&lt;/strong&gt; plan bottom up, because that is execution order:
scans at the bottom, then filters and projections, then exchanges, then the
final operator.&lt;/p&gt;

  &lt;p&gt;What to look for, in the order that usually matters:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;In the plan&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Means&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A shuffle, therefore a stage boundary. Count them&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PushedFilters&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Predicates that reached the data source&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReadSchema&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Which columns are actually read&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastHashJoin&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The small side is being broadcast&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoin&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Both sides shuffled and sorted&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;*(n)&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Whole-stage codegen; same n means fused&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Almost always a mistake&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AdaptiveSparkPlan isFinalPlan=false&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;AQE will re-plan at runtime&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;The last row matters for reading plans in modern Spark.&lt;/strong&gt; Before execution the
plan is wrapped in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AdaptiveSparkPlan&lt;/code&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=false&lt;/code&gt;, meaning what you
see is the &lt;em&gt;planned&lt;/em&gt; shape. To see what actually ran, look at the SQL tab of the
UI after the query finishes, where the final plan shows the strategies AQE
settled on.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;50. What is the difference between `select` and `withColumn`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt; projects a set of columns, replacing the projection entirely.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; adds or replaces one column, keeping everything else.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;1.1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_with_fee&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_with_fee&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;1.1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;For one or two columns the difference is style. For many it is performance, and
this is a real production issue:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# 50 chained calls: a 50-level deep plan, slow to analyse and optimise
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;50&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;c&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;x&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# one projection: a flat plan
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;*&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;x&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;c&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;50&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)])&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Each &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; adds a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Project&lt;/code&gt; node. Catalyst will usually collapse them,
but the analysis and optimisation passes still walk the deep tree first, and with
enough levels you get very slow planning or a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;StackOverflowError&lt;/code&gt; from the
recursive walk.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The rule of thumb:&lt;/strong&gt; a handful of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; calls is fine and more readable;
dozens should become one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt;. If planning time is a noticeable fraction of
your job, count the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; calls first.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;51. What is the difference between a temporary view and a global temporary view?&lt;/summary&gt;

  &lt;p&gt;Scope and lifetime.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createOrReplaceTempView&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;              &lt;span class=&quot;c1&quot;&gt;# this SparkSession only
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createGlobalTempView&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trips_global&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# all sessions in the app
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;SELECT * FROM global_temp.trips_global&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Temporary view&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Global temporary view&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Visible to&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The creating &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Every session in the application&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Database&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The current one&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;global_temp&lt;/code&gt;, always qualified&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Lives until&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The session ends&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The application ends&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Neither persists metadata: both disappear when the process does. For something
that survives, you need a catalog table via &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;saveAsTable&lt;/code&gt;, which writes to the
metastore.&lt;/p&gt;

  &lt;p&gt;The reason global temp views exist is multi-session applications. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;newSession()&lt;/code&gt;
gives an isolated SQL session sharing the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;, which is how the
Thrift server isolates users. A global temp view is how you deliberately share
something across those sessions.&lt;/p&gt;

  &lt;p&gt;Worth noting: &lt;strong&gt;a view stores the query, not the result.&lt;/strong&gt; Reading a view
re-executes its plan. If you want the result stored, cache it or write a
table.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;52. What is a UDF, and why avoid it?&lt;/summary&gt;

  &lt;p&gt;A user-defined function: your own logic registered as a callable expression.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;o&quot;&gt;@&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;udf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;double&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;add_fee&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;1.1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;else&lt;/span&gt; &lt;span class=&quot;bp&quot;&gt;None&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;total&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;add_fee&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Avoid it when a built-in exists, for three compounding reasons:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Catalyst cannot see inside it.&lt;/strong&gt; It is a black box, so no predicate can be pushed through it, no constant folded, no expression simplified.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It breaks whole-stage codegen&lt;/strong&gt; across that point, so the tight generated loop becomes iterator-by-iterator.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;In PySpark it leaves the JVM.&lt;/strong&gt; Rows are serialized to a Python worker and results serialized back, per row for a plain UDF.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;Almost everything people write a UDF for exists already:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# instead of a UDF
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;when&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;high&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;otherwise&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;mid&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;regexp_extract&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;path&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;sa&quot;&gt;r&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id=(\w+)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;coalesce&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;a&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;b&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;date_format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;yyyy-MM-dd&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The ordering to remember:&lt;/strong&gt; built-in functions, then SQL expressions, then a
pandas (vectorised) UDF, then a Scala UDF if you are on the JVM, and a plain
Python UDF last. If you must write one, keep it narrow and apply it after
filtering, so it sees as few rows as possible.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;53. What is a pandas UDF and when does it help?&lt;/summary&gt;

  &lt;p&gt;A vectorised UDF that receives an Arrow batch as a pandas Series and returns
one, rather than being called per row.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pandas&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pd&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql.functions&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pandas_udf&lt;/span&gt;

&lt;span class=&quot;o&quot;&gt;@&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;pandas_udf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;double&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;adjust&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;Series&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;-&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;pd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;Series&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;fare&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;1.1&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The cost of a Python UDF is dominated by crossing the JVM-Python boundary and by
per-row interpreter overhead. A pandas UDF amortises the crossing over a whole
batch and lets the work happen in vectorised NumPy or pandas code, so the
per-row cost falls sharply.&lt;/p&gt;

  &lt;p&gt;It still breaks whole-stage codegen, because the data still leaves the JVM. So
the ordering is: built-ins beat pandas UDFs, and pandas UDFs beat plain Python
UDFs.&lt;/p&gt;

  &lt;p&gt;Variants worth knowing:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Series to Series&lt;/strong&gt;, the scalar case above.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Iterator of Series&lt;/strong&gt;, when you need expensive per-partition setup such as loading a model once.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Grouped map&lt;/strong&gt;, via &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.groupBy(...).applyInPandas(fn, schema)&lt;/code&gt;, which hands you a whole group as a DataFrame. Watch memory: one group must fit.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapInPandas&lt;/code&gt;&lt;/strong&gt;, which streams batches of an entire DataFrame without grouping.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Arrow is what makes this work, and it is also what makes &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;toPandas()&lt;/code&gt; faster
when enabled.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;54. How do you handle nulls correctly?&lt;/summary&gt;

  &lt;p&gt;Use null-aware operators, because in SQL semantics any comparison with null is
null, not false.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;isNull&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;isNotNull&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;a&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;eqNullSafe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;b&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# null == null is true here
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;na&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;fill&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;({&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;mf&quot;&gt;0.0&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;unknown&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;})&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;na&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;drop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;subset&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;coalesce&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;unknown&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The traps that produce silently wrong results:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;col == value&lt;/code&gt; never matches nulls.&lt;/strong&gt; A filter meant to exclude one city also drops every row whose city is null.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;An inner join drops null keys entirely,&lt;/strong&gt; because null never equals null.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Aggregations skip nulls.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count(&quot;col&quot;)&lt;/code&gt; counts non-null values while &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count(&quot;*&quot;)&lt;/code&gt; counts rows, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;avg&lt;/code&gt; ignores nulls rather than treating them as zero.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A large number of null keys is skew,&lt;/strong&gt; because they all hash to one partition.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;And one version-specific change to know:&lt;/strong&gt; from Spark 4.0
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.ansi.enabled&lt;/code&gt; is on by default, so invalid casts and overflows now
raise instead of quietly producing null. That converts a class of silent
null-generation into a visible error, which is usually an improvement even when
it breaks a job on upgrade.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;55. What changed with ANSI mode in Spark 4?&lt;/summary&gt;

  &lt;p&gt;It became the default. The migration guide is explicit:&lt;/p&gt;

  &lt;blockquote&gt;
    &lt;p&gt;Since Spark 4.0, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.ansi.enabled&lt;/code&gt; is on by default.&lt;/p&gt;
  &lt;/blockquote&gt;

  &lt;p&gt;What changes with it on:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Operation&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Legacy behaviour&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;ANSI behaviour&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CAST(&apos;abc&apos; AS INT)&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;null&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Raises an error&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Integer overflow&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Wraps silently&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Raises an error&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Division by zero&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;null&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Raises an error&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Reserved keywords&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Permitted as identifiers&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Enforced&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;To restore the old behaviour:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.ansi.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;false&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;There is also a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SPARK_ANSI_SQL_MODE&lt;/code&gt; environment variable, which the source
reads when computing the default.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The right framing for an interview&lt;/strong&gt; is that this converts silent data
corruption into loud failure. Under the old behaviour a malformed value became
null, flowed through the pipeline, and quietly changed a downstream aggregate.
Under ANSI the job fails and you find out.&lt;/p&gt;

  &lt;p&gt;So when a job breaks on upgrade, treat it as a &lt;strong&gt;finding rather than a
regression&lt;/strong&gt;: something in the data was already not what the schema claimed. Fix
the cast explicitly with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;try_cast&lt;/code&gt; where null genuinely is the right answer,
rather than disabling ANSI globally.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;56. What is the difference between `union` and `unionByName`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;union&lt;/code&gt; matches columns &lt;strong&gt;by position&lt;/strong&gt;. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;unionByName&lt;/code&gt; matches them &lt;strong&gt;by name&lt;/strong&gt;.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;sf&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createDataFrame&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;nyc&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)],&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;union&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# id=nyc, city=2   &amp;lt;- silently wrong
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unionByName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;# correct
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;This is one of the few Spark behaviours that produces &lt;strong&gt;silently wrong data&lt;/strong&gt;
rather than an error, because the types happened to be compatible. If the types
had differed you would have got an analysis error and noticed.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;unionByName&lt;/code&gt; also handles schema differences:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unionByName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;allowMissingColumns&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;True&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# missing columns become null
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Without that flag, a name present on one side and not the other is an error,
which is usually what you want.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The recommendation is to default to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;unionByName&lt;/code&gt;,&lt;/strong&gt; and to reach for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;union&lt;/code&gt;
only when you have deliberately aligned the schemas and want positional
semantics. In a code review, a bare &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;union&lt;/code&gt; on two DataFrames built by different
code paths is worth questioning.&lt;/p&gt;

  &lt;p&gt;Note also that both are narrow transformations: they do not shuffle, they just
concatenate partitions. Neither deduplicates, unlike SQL’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UNION&lt;/code&gt;, which is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;union&lt;/code&gt; followed by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt;.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;57. How do you write partitioned output, and what is the trap?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionBy&lt;/code&gt; writes a directory per distinct value, which lets later readers
prune whole directories.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;s3a://lakehouse-prod/warehouse/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;trips/city_id=sf/part-00000-....parquet
trips/city_id=nyc/part-00000-....parquet
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The trap is file count&lt;/strong&gt;, and it is arithmetic: every task that holds rows for
a partition value writes a file into it. With 200 shuffle partitions and 250
cities you can get up to 50,000 files.&lt;/p&gt;

  &lt;p&gt;The fix is to repartition by the same columns first, so each value is held by
one task:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Other things to get right:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Cardinality.&lt;/strong&gt; Partition on a low-cardinality column. Partitioning by a timestamp or a user id creates a directory per value, which makes listing slower than scanning.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Dynamic overwrite.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.sources.partitionOverwriteMode&lt;/code&gt; set to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dynamic&lt;/code&gt; makes &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; replace only the partitions present in the data, instead of the whole table.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;File size.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.maxRecordsPerFile&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0&lt;/code&gt; meaning unlimited, caps rows per file when one partition is huge.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;58. What is the difference between `partitionBy` and `bucketBy`?&lt;/summary&gt;

  &lt;p&gt;Both pre-organise data on write, but they solve different problems.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionBy&lt;/code&gt;&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucketBy&lt;/code&gt;&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Layout&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A directory per value&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A fixed number of files per partition&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Good for&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Pruning on read&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Avoiding a shuffle on join or aggregate&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Cardinality&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Low&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Any, hashed into n buckets&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Requires a metastore&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;saveAsTable&lt;/code&gt; only&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Read benefit&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Skips directories&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Skips the shuffle&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;bucketBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;64&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sortBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;saveAsTable&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;lakehouse.trips_bucketed&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Bucketing works because the bucket count and key are recorded in the metastore,
so when two tables are bucketed &lt;strong&gt;the same way on the join key&lt;/strong&gt;, Spark knows
matching rows are already co-located and skips the exchange. That is the only
way to avoid a shuffle on a large-to-large join.&lt;/p&gt;

  &lt;p&gt;The conditions are strict, and naming them is a good answer: both sides
bucketed, on the same column, with the &lt;strong&gt;same number of buckets&lt;/strong&gt;. Mismatched
bucket counts means Spark shuffles anyway.&lt;/p&gt;

  &lt;p&gt;The usual pattern is to combine them: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionBy&lt;/code&gt; a date for pruning, and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucketBy&lt;/code&gt; a join key within each partition.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;59. What are the save modes?&lt;/summary&gt;

  &lt;p&gt;Four, set with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.mode()&lt;/code&gt;:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Mode&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;If the target exists&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;error&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;errorifexists&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Raise. &lt;strong&gt;The default&lt;/strong&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Add new files alongside&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Replace&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ignore&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Do nothing, silently&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The two that need care:&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; on a partitioned table&lt;/strong&gt; replaces the &lt;em&gt;whole table&lt;/em&gt; by default,
even if your DataFrame only contains one day. Setting&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.sources.partitionOverwriteMode&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;dynamic&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;makes it replace only the partitions present in the data, which is almost always
what an incremental job wants.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt; has no transactional guarantee on plain Parquet.&lt;/strong&gt; Two concurrent
appends can interleave, and a failure mid-write leaves partial files with no
way to roll back, because a directory of Parquet files has no commit protocol.
That is the structural reason lakehouse formats exist: Hudi, Iceberg and Delta
add atomic commits over the same files.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ignore&lt;/code&gt; is worth flagging in review, because it silently does nothing, which
hides a logic error rather than surfacing it.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;60. Why is Parquet usually the right format?&lt;/summary&gt;

  &lt;p&gt;Three properties that compound:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Columnar.&lt;/strong&gt; Columns are stored separately, so reading 3 of 200 columns touches roughly 1.5% of the bytes. Row formats must read and parse the whole row.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Statistics per row group.&lt;/strong&gt; Each row group carries min, max and null counts per column, so a pushed-down predicate can skip whole groups without decoding them.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Compression that works.&lt;/strong&gt; A column holds like-typed, often similar values, so dictionary and run-length encoding are very effective, which reduces both storage and read time.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;It also carries a schema, including nested structures, so there is no inference
step and no ambiguity about types.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# schema comes from the files
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.parquet.filterPushdown&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;, so the statistics are
used by default.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;When it is not the right choice:&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Write-once-read-never logs&lt;/strong&gt;, where a cheaper row format is fine.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Row-level random access,&lt;/strong&gt; which no scan format does well.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Very small datasets,&lt;/strong&gt; where the per-file overhead dominates.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Interchange with tools that cannot read it,&lt;/strong&gt; where CSV wins on ubiquity alone.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;ORC is broadly comparable and stronger in some Hive-centric stacks; the
columnar argument is the same. Avro is row-oriented and better for streaming
records and schema evolution on the write path, which is why Hudi uses it for
log blocks.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;61. How do you read from JDBC in parallel?&lt;/summary&gt;

  &lt;p&gt;Give Spark a column to split on, plus bounds and a partition count. Without
them the entire table is read by a &lt;strong&gt;single task&lt;/strong&gt;, whatever your cluster size.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;jdbc&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;url&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;jdbc:postgresql://db.internal:5432/rides&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;dbtable&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;public.trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;user&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;etl&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;partitionColumn&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;trip_seq&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;lowerBound&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;upperBound&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;40000000&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;numPartitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;16&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fetchsize&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10000&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Spark turns this into 16 queries with disjoint ranges on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trip_seq&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;The rules:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionColumn&lt;/code&gt; must be numeric, date or timestamp.&lt;/strong&gt; A string primary key will not work, which catches people with UUID keys.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;All four options go together.&lt;/strong&gt; Setting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;numPartitions&lt;/code&gt; alone does nothing.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The bounds are for splitting, not filtering.&lt;/strong&gt; Rows outside them are still read, in the first and last partitions, so badly chosen bounds create skew rather than dropping data.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;fetchsize&lt;/code&gt;&lt;/strong&gt; controls JDBC round trips and often matters more than partitioning for a moderate table.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;And the caution to volunteer:&lt;/strong&gt; 16 parallel connections is 16 concurrent
queries against a production database. Parallelism here is a load decision on the
source system, not just a Spark setting.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;62. What is the difference between `cache` on a DataFrame and a temporary view?&lt;/summary&gt;

  &lt;p&gt;They do unrelated things. Caching stores a computed result; a view stores a
query.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;createOrReplaceTempView&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# names a plan
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                            &lt;span class=&quot;c1&quot;&gt;# stores the data
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CACHE TABLE trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;        &lt;span class=&quot;c1&quot;&gt;# names a plan AND stores the data
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;So reading a temporary view re-executes its plan every time. If the plan is
expensive and you read it repeatedly, you want caching as well, which is what
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CACHE TABLE&lt;/code&gt; does.&lt;/p&gt;

  &lt;p&gt;One behavioural difference worth knowing: &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CACHE TABLE&lt;/code&gt; is eager by default&lt;/strong&gt;,
populating immediately, while &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.cache()&lt;/code&gt; is lazy and populates on the next
action. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CACHE LAZY TABLE&lt;/code&gt; makes the SQL form lazy.&lt;/p&gt;

  &lt;p&gt;Two related notes:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Caching is by plan, not by name.&lt;/strong&gt; Two DataFrames with identical plans share a cache entry, which is why caching then re-deriving the same query can hit the cache unexpectedly.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A cached DataFrame is pinned until released.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;unpersist()&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.catalog.clearCache()&lt;/code&gt;; otherwise it holds storage memory for the life of the application and squeezes execution memory from every later stage.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;h2 id=&quot;architecture-and-execution&quot;&gt;Architecture and execution&lt;/h2&gt;

&lt;p&gt;How a line of code becomes work on a cluster. Depth here is the strongest signal an interviewer gets.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;63. Walk through what happens when you submit a Spark application.&lt;/summary&gt;

  &lt;p&gt;Ten steps, and being able to narrate them in order is the answer.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;&amp;lt;b&amp;gt;Startup&amp;lt;/b&amp;gt; (1-4)&amp;lt;br/&amp;gt;submit, driver starts,&amp;lt;br/&amp;gt;executors requested and registered&quot;]
  B[&quot;&amp;lt;b&amp;gt;Plan&amp;lt;/b&amp;gt; (5-7)&amp;lt;br/&amp;gt;code builds a plan, an action submits a job,&amp;lt;br/&amp;gt;Catalyst optimises, DAGScheduler cuts stages&quot;]
  C[&quot;&amp;lt;b&amp;gt;Execute&amp;lt;/b&amp;gt; (8-9)&amp;lt;br/&amp;gt;TaskSchedulerImpl places tasks,&amp;lt;br/&amp;gt;executors run them and write shuffle output&quot;]
  D{&quot;more stages?&quot;}
  E[&quot;&amp;lt;b&amp;gt;Finish&amp;lt;/b&amp;gt; (10)&amp;lt;br/&amp;gt;results to the driver,&amp;lt;br/&amp;gt;or written by the executors&quot;]
  A --&amp;gt; B --&amp;gt; C --&amp;gt; D
  D --&amp;gt;|&quot;yes&quot;| C
  D --&amp;gt;|&quot;no&quot;| E
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Two details that make the answer stronger:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Step 5 is where laziness lives.&lt;/strong&gt; Everything between the session and the first action is plan construction, so a job that fails at step 6 may have a bug written many lines earlier.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Step 10 forks.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; pulls every row back to the driver; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write&lt;/code&gt; has each executor write its own partition and return only a status. Same job shape, completely different driver cost.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Steps 8 and 9 repeat per stage, and that loop is where adaptive query execution
intervenes, because between two passes it has real statistics from the stage that
just finished.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;64. What does the driver actually do, step by step?&lt;/summary&gt;

  &lt;p&gt;Nine responsibilities, in roughly the order they happen:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Creates the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt;&lt;/strong&gt;, bringing up &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkEnv&lt;/code&gt;, the block manager and the RPC endpoints executors call back on.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Asks for executors.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SchedulerBackend&lt;/code&gt; requests resources from whichever cluster manager &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--master&lt;/code&gt; named.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Turns code into a plan.&lt;/strong&gt; Unresolved, then analysed against the catalog, then optimised by Catalyst, then a physical plan chosen by cost.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Cuts the plan into stages.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; walks the RDD lineage and breaks it at every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleDependency&lt;/code&gt;, producing a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSet&lt;/code&gt; per stage.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Places every task.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSchedulerImpl&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSetManager&lt;/code&gt; decide which executor runs which task, preferring the best available locality.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Tracks every attempt.&lt;/strong&gt; Retries up to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.task.maxFailures&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4&lt;/code&gt;; speculates slow tasks if enabled; resubmits stages whose shuffle output was lost.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Keeps the block index.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BlockManagerMaster&lt;/code&gt; knows where every cached partition and shuffle block lives, so executors can fetch from each other.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Receives results,&lt;/strong&gt; bounded in total by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.maxResultSize&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Serves the UI&lt;/strong&gt; on port 4040 and writes the event log the history server replays.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The two facts that follow: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.memory&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;, which is a
coordinator’s budget and not a data-processing one; and the driver is the one
process with &lt;strong&gt;no redundancy&lt;/strong&gt;, because items 3 to 7 exist nowhere else.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;65. What does an executor do, step by step?&lt;/summary&gt;

  &lt;p&gt;Much less than the driver, deliberately.&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Registers.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CoarseGrainedExecutorBackend&lt;/code&gt; sends &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RegisterExecutor&lt;/code&gt; and waits for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RegisteredExecutor&lt;/code&gt; before accepting work. Until that round trip completes, a granted container is idle.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Accepts &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LaunchTask&lt;/code&gt;&lt;/strong&gt; and runs each task in a thread from its pool. Concurrency is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.cores&lt;/code&gt;, which defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1&lt;/code&gt; on YARN and to all cores in standalone mode.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Reads input, runs the generated code,&lt;/strong&gt; and on a shuffle map stage writes partitioned output to local disk.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Reports back&lt;/strong&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;StatusUpdate&lt;/code&gt; per task and a heartbeat every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.heartbeatInterval&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10s&lt;/code&gt;. Miss enough and the driver declares it lost.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Stores and serves blocks&lt;/strong&gt; through its &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BlockManager&lt;/code&gt;, which makes a cached DataFrame reusable and lets the next stage fetch shuffle data from peers.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The contract is narrow on purpose: &lt;strong&gt;an executor never makes a scheduling
decision, never sees the query plan, and never coordinates with another executor
about what to run.&lt;/strong&gt; It receives tasks, runs them, serves bytes.&lt;/p&gt;

  &lt;p&gt;That is exactly what makes executors disposable. Lose one and its tasks are
retried elsewhere with its cached partitions recomputed from lineage. It is also
why executors are per-application rather than shared: memory and slots are
reserved for one application’s work.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;66. What does the cluster manager do?&lt;/summary&gt;

  &lt;p&gt;One thing: decide who gets containers. It is a resource manager, not a work
scheduler.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cluster manager&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What grants the container&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What Spark runs there&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Standalone&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Master&lt;/code&gt;, tracking registered &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Worker&lt;/code&gt; daemons&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The worker launches an executor JVM per grant&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;YARN&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The ResourceManager, asked by Spark’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ApplicationMaster&lt;/code&gt; through &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;YarnAllocator&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;An executor per allocated container&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Kubernetes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The API server. The driver’s own &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ExecutorPodsAllocator&lt;/code&gt; creates pods&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;An executor per pod&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Kubernetes is the clearest illustration of the split, because &lt;strong&gt;there is no
Spark cluster-manager daemon at all&lt;/strong&gt;: the driver talks to the API server
directly and asks for pods. The component granting resources and the component
scheduling work are simply different things, however they are packaged.&lt;/p&gt;

  &lt;p&gt;Two defaults follow from this being a separate concern:
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.dynamicAllocation.enabled&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;, so absent a decision your
application holds its executors from first request to exit, idle or not. And
&lt;strong&gt;Mesos was removed in the 4.x line&lt;/strong&gt;, so the three rows above are the whole
list.&lt;/p&gt;

  &lt;p&gt;The practical consequence people find surprising: tuning YARN’s scheduler
changes what you are &lt;em&gt;given&lt;/em&gt;, never how your tasks are distributed across what
you were given. That is the driver’s job.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;67. What is `DAGScheduler` responsible for?&lt;/summary&gt;

  &lt;p&gt;Stage-level scheduling. Given a job, it:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;Walks the RDD lineage backwards from the action.&lt;/li&gt;
    &lt;li&gt;Cuts a new stage at every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleDependency&lt;/code&gt;, absorbing all narrow dependencies into the current stage.&lt;/li&gt;
    &lt;li&gt;Determines the order stages must run in, and which can run in parallel.&lt;/li&gt;
    &lt;li&gt;Submits each ready stage to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskScheduler&lt;/code&gt; as a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSet&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Handles &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FetchFailedException&lt;/code&gt; by &lt;strong&gt;resubmitting the upstream stage&lt;/strong&gt;, because lost shuffle output cannot be re-fetched.&lt;/li&gt;
    &lt;li&gt;Computes preferred locations for tasks from the RDD’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;getPreferredLocations&lt;/code&gt;.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The distinction from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskScheduler&lt;/code&gt; is the point of the question:
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; decides &lt;em&gt;what stages exist and in what order&lt;/em&gt;;
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSchedulerImpl&lt;/code&gt; decides &lt;em&gt;which executor runs which individual task&lt;/em&gt;. One is
about the shape of the job, the other about placement.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  J[&quot;job&quot;] --&amp;gt; D[&quot;DAGScheduler&amp;lt;br/&amp;gt;cuts stages at shuffles&quot;]
  D --&amp;gt; TS[&quot;TaskSet per stage&quot;]
  TS --&amp;gt; T[&quot;TaskSchedulerImpl&amp;lt;br/&amp;gt;places each task&quot;]
  T --&amp;gt; B[&quot;SchedulerBackend&quot;] --&amp;gt; E[&quot;executors&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The stage-resubmission behaviour is the one to volunteer, because it explains a
confusing UI: a job that appears to go backwards, re-running a stage that had
already completed, is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; rebuilding shuffle output that died with an
executor.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;68. What is `TaskSchedulerImpl` responsible for?&lt;/summary&gt;

  &lt;p&gt;Task-level scheduling: taking a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSet&lt;/code&gt; and deciding which executor runs
each task in it.&lt;/p&gt;

  &lt;p&gt;Its concerns:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Placement by locality.&lt;/strong&gt; It offers each task the best available locality level, waiting briefly (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.locality.wait&lt;/code&gt;) before settling for a worse one.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Retries.&lt;/strong&gt; Failed tasks are rescheduled up to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.task.maxFailures&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4&lt;/code&gt;, preferring a different executor when the failure looked executor-specific.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Speculation.&lt;/strong&gt; If enabled, duplicating tasks that lag their peers.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Excluding bad executors.&lt;/strong&gt; If one executor or node keeps failing tasks, it can stop scheduling there so a single bad machine does not burn every task’s retry budget.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Fair or FIFO ordering&lt;/strong&gt; between concurrent &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSet&lt;/code&gt;s, per &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.scheduler.mode&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FIFO&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;It works with a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSetManager&lt;/code&gt; per stage, which holds the per-task state, and
talks to the cluster through &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SchedulerBackend&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;The reason to know the split: when tasks are not being placed where you expect,
the question is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSchedulerImpl&lt;/code&gt; and locality question. When stages are not
in the shape you expect, it is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; question. Knowing which
component owns which symptom is most of debugging.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;69. What is `SchedulerBackend`?&lt;/summary&gt;

  &lt;p&gt;The single component that talks to the cluster manager, with an implementation
per manager. It is the boundary between Spark’s scheduling and the outside world.&lt;/p&gt;

  &lt;p&gt;Its job:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Request and release executors.&lt;/strong&gt; Including the dynamic-allocation calls to scale up and down.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Receive executor registration&lt;/strong&gt; and tell the scheduler how many slots exist.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Launch tasks&lt;/strong&gt; onto executors, and relay their status updates back.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Implementations include &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CoarseGrainedSchedulerBackend&lt;/code&gt; as the common base, with
YARN, Kubernetes and standalone specialisations, plus &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LocalSchedulerBackend&lt;/code&gt;
for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;local[*]&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;“Coarse-grained” is worth explaining because the name is opaque: the executor is
acquired once and held for the application’s lifetime, running many tasks. The
alternative, fine-grained acquisition per task, existed under Mesos and is gone.&lt;/p&gt;

  &lt;p&gt;This is the layer that makes Spark portable. The whole scheduler above it is
identical on every cluster manager, and only &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SchedulerBackend&lt;/code&gt; changes. That is
why the same jar runs on YARN and Kubernetes with nothing but &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--master&lt;/code&gt;
different.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;70. What is a `ShuffleMapStage` versus a `ResultStage`?&lt;/summary&gt;

  &lt;p&gt;Every stage but the last in a job is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleMapStage&lt;/code&gt;; the last is a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ResultStage&lt;/code&gt;.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleMapStage&lt;/code&gt;&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ResultStage&lt;/code&gt;&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Output&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shuffle files on local disk&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The action’s result&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Purpose&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Feed the next stage&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Answer the action&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Consumed by&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The next stage’s fetch&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The driver, or a write&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Rerunnable&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, and resubmitted if output is lost&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;The reason the distinction exists in the scheduler is recovery.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleMapStage&lt;/code&gt; output is &lt;em&gt;registered&lt;/em&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MapOutputTracker&lt;/code&gt; on the driver,
so Spark knows where every map output block lives. If an executor dies and takes
blocks with it, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; knows which stage to resubmit and which partitions
of it are missing.&lt;/p&gt;

  &lt;p&gt;A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ResultStage&lt;/code&gt; has nothing registered, because its output goes to the driver or
to storage.&lt;/p&gt;

  &lt;p&gt;This is also why the UI shows the final stage differently, and why a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ShuffleMapStage&lt;/code&gt; can be &lt;strong&gt;skipped&lt;/strong&gt;: if its output is still registered from a
previous job, Spark reuses it rather than recomputing. Those grey “skipped
stages” in the UI are a feature, not a problem, and they are what makes reusing
a shuffle across two actions cheap.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;71. What are the task locality levels?&lt;/summary&gt;

  &lt;p&gt;Five, best first:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Level&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Meaning&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PROCESS_LOCAL&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The data is in this executor’s memory already&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NODE_LOCAL&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;On the same machine, another executor or local disk or HDFS&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NO_PREF&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No preference, for example a JDBC source&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RACK_LOCAL&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;On the same rack&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ANY&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Anywhere&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TaskSchedulerImpl&lt;/code&gt; offers each task the best level available, and if nothing at
that level is free it &lt;strong&gt;waits&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.locality.wait&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;3s&lt;/code&gt;, before
settling for the next level down.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  A[&quot;task ready&quot;] --&amp;gt; B{&quot;PROCESS_LOCAL&amp;lt;br/&amp;gt;slot free?&quot;}
  B --&amp;gt;|&quot;yes&quot;| R[&quot;run&quot;]
  B --&amp;gt;|&quot;no&quot;| C[&quot;wait spark.locality.wait&quot;]
  C --&amp;gt; D{&quot;NODE_LOCAL free?&quot;}
  D --&amp;gt;|&quot;yes&quot;| R
  D --&amp;gt;|&quot;no&quot;| E[&quot;degrade further, to ANY&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;&lt;strong&gt;The modern caveat is the useful part.&lt;/strong&gt; Locality was designed for HDFS, where
compute ran on the machines holding the blocks. Reading from S3 or another object
store means no partition has a meaningful preferred location, so most tasks are
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;NO_PREF&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ANY&lt;/code&gt; and the waiting buys nothing. On such clusters, lowering
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.locality.wait&lt;/code&gt; can measurably reduce scheduling delay.&lt;/p&gt;

  &lt;p&gt;Where locality still matters is cached data: a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PROCESS_LOCAL&lt;/code&gt; hit on a cached
partition avoids a network fetch entirely.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;72. Where does the input partition count come from?&lt;/summary&gt;

  &lt;p&gt;Not from the number of files, which is the assumption that costs people the
most time.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FilePartition.maxSplitBytes&lt;/code&gt; decides the split size first:&lt;/p&gt;

  &lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nv&quot;&gt;Math&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;min&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;defaultMaxSplitBytes&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;Math&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;max&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;openCostInBytes&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;bytesPerCore&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;defaultMaxSplitBytes&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.maxPartitionBytes&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;128MB&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;openCostInBytes&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.openCostInBytes&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4MB&lt;/code&gt;, charged &lt;strong&gt;per file&lt;/strong&gt; so many small files are packed together rather than each getting a task.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bytesPerCore&lt;/code&gt; is the total size divided by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.minPartitionNum&lt;/code&gt;, which falls back to the default parallelism.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The consequence is measurable. Write the same data as 1, 8 and 16 Parquet files,
then read each back:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: right&quot;&gt;Files written&lt;/th&gt;
        &lt;th style=&quot;text-align: right&quot;&gt;Partitions on read&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: right&quot;&gt;1&lt;/td&gt;
        &lt;td style=&quot;text-align: right&quot;&gt;1&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: right&quot;&gt;8&lt;/td&gt;
        &lt;td style=&quot;text-align: right&quot;&gt;2&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: right&quot;&gt;16&lt;/td&gt;
        &lt;td style=&quot;text-align: right&quot;&gt;2&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;File count did not determine partition count in any of them, because the files
were small enough to pack.&lt;/p&gt;

  &lt;p&gt;Two directions follow: a directory of small files collapses into far fewer
partitions than it has files, and a single file larger than the split size is
divided into several. So read the number rather than predicting it:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getNumPartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;73. How many tasks will a stage have?&lt;/summary&gt;

  &lt;p&gt;One per partition of that stage. Nothing else affects it.&lt;/p&gt;

  &lt;p&gt;Where that partition count comes from depends on the stage:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;The first stage&lt;/strong&gt; takes it from input splitting, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.maxPartitionBytes&lt;/code&gt; and file sizes.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A stage after a shuffle&lt;/strong&gt; takes it from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt;, or whatever adaptive coalescing reduced it to at runtime.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;After an explicit &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt;&lt;/strong&gt;, it is n.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;It is not related to the number of executors,&lt;/strong&gt; and that confusion is the
reason the question gets asked. With 200 tasks and 10 executors of 4 cores each,
you have 40 slots, so the stage runs in 5 waves. Tasks queue for slots; they are
not divided among executors up front.&lt;/p&gt;

  &lt;p&gt;The arithmetic worth having ready:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;slots = num_executors x spark.executor.cores
waves = ceil(tasks / slots)
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;A stage with fewer tasks than slots leaves cores idle. A stage with thousands of
very short tasks spends its time in scheduling. The target is several waves of
tasks each doing a meaningful amount of work, which is what “partitions in the
low hundreds of megabytes” is really a proxy for.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;74. What happens when an executor is lost?&lt;/summary&gt;

  &lt;p&gt;The work is redone, and the reason it can be redone is lineage.&lt;/p&gt;

  &lt;p&gt;Recovery is &lt;strong&gt;per partition&lt;/strong&gt;, not per job: losing one executor costs the
partitions it held, which are rebuilt on whichever executors remain while
everything else stands. Its running tasks are retried elsewhere, counting
against &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.task.maxFailures&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;Cached blocks are recomputed from lineage, unless the storage level replicated
them (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_ONLY_2&lt;/code&gt; and friends), in which case the replica is promoted.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Shuffle output is the special case&lt;/strong&gt; worth volunteering:&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  A[&quot;stage 0 completed&quot;] --&amp;gt; B[&quot;executor dies with&amp;lt;br/&amp;gt;its shuffle files&quot;]
  B --&amp;gt; C[&quot;stage 1 fetch fails&amp;lt;br/&amp;gt;FetchFailedException&quot;]
  C --&amp;gt; D[&quot;DAGScheduler resubmits&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;stage 0&amp;lt;/b&amp;gt;&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;That is a stage rerun rather than a task retry, and it is why a job can appear
to go backwards in the UI. An &lt;strong&gt;external shuffle service&lt;/strong&gt; avoids it by serving
shuffle blocks from a node-level daemon that outlives the executor, which is also
why dynamic allocation wants one.&lt;/p&gt;

  &lt;p&gt;The contrast with the driver is the closing point: executors hold only
recomputable state, so they are disposable; the driver holds the plan, the
scheduler and the block index, so it is not.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;75. Why does losing the driver kill the application, but losing an executor not?&lt;/summary&gt;

  &lt;p&gt;Because of what each holds that exists nowhere else.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Driver&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Executor&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Holds&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Plan, scheduler state, block index, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Task threads, cached blocks, shuffle files&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Recoverable from elsewhere&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, from lineage or a peer&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;On failure&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Application ends&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Tasks retried, partitions recomputed&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;An executor’s contents are either derivable (cached partitions, from lineage) or
reproducible (shuffle output, by rerunning the stage). So Spark can always
reconstruct them.&lt;/p&gt;

  &lt;p&gt;The driver’s contents are not derivable from anything. It is the only process
that knows what the job is, which stages remain, which tasks succeeded, and
where every block lives. There is no second copy, so there is nothing to recover
from.&lt;/p&gt;

  &lt;p&gt;What you can do about it:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Cluster deploy mode&lt;/strong&gt; puts the driver inside the cluster, where the manager can restart it. On standalone, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--supervise&lt;/code&gt; restarts a failed driver.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Structured Streaming checkpoints&lt;/strong&gt; let a restarted driver resume from recorded offsets and state, which is the closest thing to driver fault tolerance in practice.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Keep the driver unloaded.&lt;/strong&gt; Most driver failures are self-inflicted: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; on a large result, a broadcast that is not small, or hundreds of thousands of tasks to track.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;76. What is the external shuffle service, and why use it?&lt;/summary&gt;

  &lt;p&gt;A daemon running on each node that serves shuffle blocks on behalf of the
executors that wrote them. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.service.enabled&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;Without it, shuffle output lives inside the executor process, so the executor
must stay alive for as long as anything might fetch from it. With it, the blocks
are on the node’s disk and served by the daemon, so the executor can exit
freely.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph N[&quot;without the service&quot;]
    E1[&quot;executor&amp;lt;br/&amp;gt;holds shuffle files&quot;] --&amp;gt;|&quot;must stay alive&quot;| F1[&quot;reduce tasks fetch&quot;]
  end
  subgraph W[&quot;with the service&quot;]
    E2[&quot;executor&amp;lt;br/&amp;gt;can exit&quot;] --&amp;gt; D2[&quot;node shuffle service&amp;lt;br/&amp;gt;serves the files&quot;] --&amp;gt; F2[&quot;reduce tasks fetch&quot;]
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Why it matters:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Dynamic allocation needs it,&lt;/strong&gt; or an equivalent. Otherwise Spark cannot release an idle executor that still holds shuffle data someone might want. The alternative is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.dynamicAllocation.shuffleTracking.enabled&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;, which tracks which executors hold live shuffle blocks and keeps only those.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It reduces &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FetchFailedException&lt;/code&gt;,&lt;/strong&gt; because an executor dying no longer destroys its shuffle output.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It offloads serving work&lt;/strong&gt; from the executor JVM, which is otherwise doing your computation.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The cost is an extra daemon to deploy and operate per node, which on YARN means
an auxiliary service in the NodeManager.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;77. What is dynamic allocation?&lt;/summary&gt;

  &lt;p&gt;Adding and removing executors according to pending work, rather than holding a
fixed set.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.dynamicAllocation.enabled&lt;/code&gt; defaults to &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/strong&gt;, so absent a decision
your application holds its executors from first request to exit, idle or not.&lt;/p&gt;

  &lt;p&gt;How it behaves:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Scales up&lt;/strong&gt; when tasks are pending, adding executors exponentially up to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxExecutors&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Scales down&lt;/strong&gt; when an executor has been idle for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;executorIdleTimeout&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Respects &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;minExecutors&lt;/code&gt;&lt;/strong&gt; as a floor, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;initialExecutors&lt;/code&gt; at start.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.dynamicAllocation.enabled&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.dynamicAllocation.minExecutors&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;2 &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.dynamicAllocation.maxExecutors&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;100
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The shuffle problem is the part to know.&lt;/strong&gt; An idle executor may still hold
shuffle blocks a later stage needs, so releasing it would cause fetch failures.
Two solutions: an external shuffle service, or
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.dynamicAllocation.shuffleTracking.enabled&lt;/code&gt;, which defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; and
keeps executors that hold live shuffle data.&lt;/p&gt;

  &lt;p&gt;When it helps: interactive and bursty workloads, notebooks, multi-tenant
clusters, and jobs whose parallelism varies a lot between stages.&lt;/p&gt;

  &lt;p&gt;When it hurts: short jobs, where the ramp-up costs more than it saves, and jobs
with strict latency needs, where waiting for executors is worse than holding
them. Note also that setting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--num-executors&lt;/code&gt; alongside it is contradictory:
the static value only acts as an initial size.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;78. What is speculative execution?&lt;/summary&gt;

  &lt;p&gt;Re-running tasks that are running much slower than their peers, and taking
whichever attempt finishes first. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.speculation&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;Spark watches the distribution of task durations within a stage. Once a
configurable fraction of tasks has completed, any task running more than
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.speculation.multiplier&lt;/code&gt; times the median is duplicated on another
executor.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;When it helps:&lt;/strong&gt; a genuinely slow &lt;em&gt;machine&lt;/em&gt;. A failing disk, a noisy
neighbour, a node with thermal throttling. The duplicate runs elsewhere and
finishes normally.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;When it hurts, and this is the answer interviewers want:&lt;/strong&gt; data &lt;strong&gt;skew&lt;/strong&gt;. If
the task is slow because its partition holds ten times the rows, the duplicate
has exactly the same work to do and is equally slow. You have now consumed two
slots to get one result no sooner. Worse, it can mask the skew, because the
symptom looks like flaky infrastructure rather than an uneven distribution.&lt;/p&gt;

  &lt;p&gt;So the diagnostic order is: check whether slow tasks also have larger shuffle
read. If they do, it is skew and speculation is the wrong tool. If durations
vary while input sizes are equal, it is the environment and speculation helps.&lt;/p&gt;

  &lt;p&gt;One more caution: with a non-idempotent sink, two attempts writing the same
output can duplicate data. Speculation assumes writes are safe to repeat.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;79. What is the default scheduler mode within an application?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.scheduler.mode&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FIFO&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;That governs how concurrent &lt;strong&gt;jobs within one application&lt;/strong&gt; share executor
slots, which only matters when you submit jobs from multiple threads or serve
multiple users from one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt;.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Mode&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Behaviour&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FIFO&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Jobs run in submission order; the first takes all the slots it can use&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FAIR&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Slots are shared round-robin between active jobs&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Under FIFO, a large job submitted first can starve a small interactive query
submitted second, because it holds every slot until it is done. That is
precisely the wrong behaviour for a notebook server or a Thrift server, where a
quick query should not wait behind an hour-long batch.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.scheduler.mode&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;FAIR &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.scheduler.allocation.file&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;/etc/spark/fairscheduler.xml
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;FAIR also supports &lt;strong&gt;pools&lt;/strong&gt; with weights and minimum shares, so you can give an
interactive pool priority over a batch pool. A job joins a pool by setting a
thread-local property:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;setLocalProperty&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.scheduler.pool&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;interactive&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Keep the levels distinct: this is scheduling &lt;em&gt;within&lt;/em&gt; an application. Sharing
&lt;em&gt;between&lt;/em&gt; applications is the cluster manager’s job, with YARN queues or
Kubernetes quotas.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;80. How do you set JVM options for the driver and executors?&lt;/summary&gt;

  &lt;p&gt;Two configs, one per side:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.driver.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.executor.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties -verbose:class&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  app.jar
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;They are the delivery mechanism for almost every JVM-level change: logging
configuration, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-verbose:class&lt;/code&gt; for class-loading problems, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-Xss&lt;/code&gt; for stack
size, heap dump flags, proxy properties, and GC settings.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The client-mode trap is the part that catches people.&lt;/strong&gt; In client mode the
driver JVM is already running by the time your &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkConf&lt;/code&gt; executes, so a driver
JVM option set in code arrives too late to have any effect. Use
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--driver-java-options&lt;/code&gt;, or a properties file, or set it before the JVM starts.&lt;/p&gt;

  &lt;p&gt;Two more notes:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Do not set &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-Xmx&lt;/code&gt; here.&lt;/strong&gt; Heap comes from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.memory&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt;; setting it in extra options conflicts with them.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Avoid secrets.&lt;/strong&gt; The Environment tab of the UI displays configs, so a password in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;extraJavaOptions&lt;/code&gt; is visible to anyone who can reach the UI.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;To confirm a flag took effect, the Environment tab is authoritative, and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--verbose&lt;/code&gt; on submit prints where each value came from.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;81. What is `spark.driver.maxResultSize` and why does it exist?&lt;/summary&gt;

  &lt;p&gt;A cap on the total serialized size of results returned to the driver, default
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;. Exceeding it aborts the job with an error naming the config.&lt;/p&gt;

  &lt;p&gt;It exists because without it, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; on a large dataset would simply
exhaust the driver heap and produce an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;, which is harder to
diagnose and can take the application down mid-flight. The cap converts that
into a clear, early, named failure.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.driver.maxResultSize&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# 1g
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Note it counts the &lt;strong&gt;total across all tasks&lt;/strong&gt; in an action, not per task, and it
is checked as results arrive, so a job can fail partway through collecting.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The right response is almost never to raise it.&lt;/strong&gt; If you are hitting a 1 GB
limit, the question is why a gigabyte of rows needs to be in the driver:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Writing out? Use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;write&lt;/code&gt;, so executors write in parallel and only statuses return.&lt;/li&gt;
    &lt;li&gt;Inspecting? &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show()&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;take()&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;limit()&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;Counting or aggregating? Do it as an aggregation, which returns one row.&lt;/li&gt;
    &lt;li&gt;Handing to Python? &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;toPandas()&lt;/code&gt; has the same problem; consider &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapInPandas&lt;/code&gt; to keep the work distributed.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Setting it to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0&lt;/code&gt; disables the check, which trades a clear error for an
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;. Raising it is defensible only when you genuinely need a
large result on the driver and have sized &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.memory&lt;/code&gt; accordingly.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;82. What port does the Spark UI use, and what happens after the application ends?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.ui.port&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4040&lt;/code&gt;. If that port is taken, Spark increments and
tries &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4041&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4042&lt;/code&gt; and so on, which is why a second concurrent application
appears on a different port.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The UI dies with the application.&lt;/strong&gt; It is served by the driver, so when the
driver exits there is nothing left to serve it. That is a problem, because most
investigation happens after a job has failed or finished.&lt;/p&gt;

  &lt;p&gt;The solution is two-part:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.eventLog.enabled&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.eventLog.dir&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;hdfs:///spark-events
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The driver writes an event log, and a separately running &lt;strong&gt;history server&lt;/strong&gt;
reads that directory and replays the logs, giving you the same UI for completed
applications.&lt;/p&gt;

  &lt;p&gt;Without event logging, a job that failed overnight leaves you with only the
driver’s stdout, which is why enabling it is close to mandatory in production.&lt;/p&gt;

  &lt;p&gt;Two practical notes: event logs grow, so they need retention and ideally
compression (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.eventLog.compress&lt;/code&gt;); and on YARN the ResourceManager UI
links to the history server, so the “tracking URL” for a finished application
lands there rather than at a dead port 4040.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;83. How do broadcast variables and accumulators relate to the driver and executors?&lt;/summary&gt;

  &lt;p&gt;They are the two sanctioned ways to move data between driver and executors,
one in each direction.&lt;/p&gt;

  &lt;p&gt;A task runs in another JVM, so an ordinary variable captured by a closure is
serialized, shipped and then discarded. Updates to it never come back, which is
why both mechanisms exist.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  D[&quot;driver&quot;] --&amp;gt;|&quot;broadcast: one copy per executor&quot;| E[&quot;executors&quot;]
  E --&amp;gt;|&quot;accumulator: merged updates&quot;| D
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;&lt;strong&gt;Broadcast, outward.&lt;/strong&gt; A read-only value cached once per executor rather than
serialized per task. The saving is per-task-count: with 10,000 tasks and a 10 MB
table, one copy per executor instead of 10,000 copies.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Accumulators, inward.&lt;/strong&gt; Tasks add, the driver reads. The guarantee is
exactly-once &lt;strong&gt;only for updates inside actions&lt;/strong&gt;; in a transformation, a recompute
or retry applies the update again.&lt;/p&gt;

  &lt;p&gt;What neither does: give you shared mutable state. There is no way for two tasks
to coordinate through a variable, which is deliberate. If tasks need to agree on
something, that is what a shuffle is for.&lt;/p&gt;

  &lt;p&gt;The common mistake is closing over a large object without broadcasting it, which
silently multiplies serialization cost by the task count and often shows up as
inexplicably slow task launch.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;84. What is Spark Connect?&lt;/summary&gt;

  &lt;p&gt;A client-server architecture where the client builds an unresolved logical plan
and sends it to a remote Spark server over gRPC, instead of the client being the
driver.&lt;/p&gt;

  &lt;p&gt;Traditionally your application &lt;em&gt;is&lt;/em&gt; the driver: it holds the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt;, it
must be a JVM (or a JVM plus a Python process), and it must be network-close to
the executors. Spark Connect splits that.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  C[&quot;thin client&amp;lt;br/&amp;gt;Python, Scala, Go, JVM&quot;] --&amp;gt;|&quot;unresolved plan&amp;lt;br/&amp;gt;over gRPC&quot;| S[&quot;Spark Connect server&amp;lt;br/&amp;gt;holds the real driver&quot;]
  S --&amp;gt; E[&quot;executors&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;What it buys:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Thin clients.&lt;/strong&gt; No local JVM, no cluster-side dependencies, so a notebook, an IDE or a web service can drive a cluster.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Isolation.&lt;/strong&gt; User code crashing or leaking memory no longer takes down the driver, because it is not in the driver.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Upgradeability.&lt;/strong&gt; Client and server versions are decoupled, so the cluster can move independently.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Multi-language.&lt;/strong&gt; The protocol, not the JVM, is the interface, which is how non-JVM clients become possible.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The trade is that anything depending on being &lt;em&gt;inside&lt;/em&gt; the driver does not work:
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkContext&lt;/code&gt; internals, RDD APIs in general, and code that assumes JVM
colocation. So it is a DataFrame and SQL interface, which covers most modern
usage.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;partitioning-and-shuffle&quot;&gt;Partitioning and shuffle&lt;/h2&gt;

&lt;p&gt;The single biggest source of Spark performance problems, and therefore of interview questions. Expect to be pushed for detail here.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;85. What is a shuffle?&lt;/summary&gt;

  &lt;p&gt;A redistribution of data across partitions so that rows which must be
processed together end up in the same partition.&lt;/p&gt;

  &lt;p&gt;It has two halves:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;The map side&lt;/strong&gt; writes each record into a bucket for its target partition, sorts or spills as needed, and writes partitioned output to &lt;strong&gt;local disk&lt;/strong&gt;, along with an index of where each partition’s bytes start.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The reduce side&lt;/strong&gt; fetches its own slice from &lt;em&gt;every&lt;/em&gt; map output, over the network, then merges them.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph M[&quot;map side, stage 0&quot;]
    M1[&quot;task 0&quot;] --&amp;gt; F1[(&quot;file + index&quot;)]
    M2[&quot;task 1&quot;] --&amp;gt; F2[(&quot;file + index&quot;)]
    M3[&quot;task 2&quot;] --&amp;gt; F3[(&quot;file + index&quot;)]
  end
  subgraph R[&quot;reduce side, stage 1&quot;]
    F1 --&amp;gt; R1[&quot;task 0 fetches&amp;lt;br/&amp;gt;its slice from all 3&quot;]
    F2 --&amp;gt; R1
    F3 --&amp;gt; R1
    F1 --&amp;gt; R2[&quot;task 1&quot;]
    F2 --&amp;gt; R2
    F3 --&amp;gt; R2
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Three consequences worth stating:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;It always touches disk&lt;/strong&gt;, even when nothing spills, because map output is written to local disk before being served. Shuffle is not an in-memory operation.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It is all-to-all.&lt;/strong&gt; With m map tasks and r reduce tasks there are m x r block fetches, which is why very high parallelism on both sides is its own cost.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It creates a barrier.&lt;/strong&gt; Stage 1 cannot start until stage 0 has finished writing every partition, so one straggler delays everything.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;86. What triggers a shuffle?&lt;/summary&gt;

  &lt;p&gt;Any wide dependency. In practice:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Operation&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupBy&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Rows with the same key must meet&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;join&lt;/code&gt; without a broadcast&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Both sides must be co-partitioned on the key&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dropDuplicates&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Duplicates must meet to be removed&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;By definition&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;orderBy&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sort&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Global ordering needs range partitioning&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;aggregateByKey&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Combines locally first, but still shuffles&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Window functions&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Unless already partitioned by the same key&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;What does &lt;strong&gt;not&lt;/strong&gt; shuffle: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;map&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;union&lt;/code&gt;,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce&lt;/code&gt;, and a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;join&lt;/code&gt; where one side is broadcast.&lt;/p&gt;

  &lt;p&gt;The reliable way to count them is the plan, because every shuffle is an
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt;:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;formatted&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Exchange hashpartitioning(city_id#21, 200), ENSURE_REQUIREMENTS
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The description tells you the key and the target partition count, so you can see
not just &lt;em&gt;that&lt;/em&gt; it shuffles but &lt;em&gt;on what&lt;/em&gt;.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The optimisation this enables:&lt;/strong&gt; if two consecutive operations shuffle on the
same key, Spark needs only one exchange. So aggregating and joining on the same
column is much cheaper than on different columns, and that is often something
you can arrange.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;87. Why is a shuffle expensive?&lt;/summary&gt;

  &lt;p&gt;Five costs, and naming several of them is the difference between a shallow and
a deep answer.&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Serialization and deserialization.&lt;/strong&gt; Every record crossing the boundary must be encoded and decoded.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Disk I/O.&lt;/strong&gt; Map output is always written to local disk, and spills add more.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Network transfer.&lt;/strong&gt; Every reduce task pulls from every map task, so the fetch pattern is all-to-all.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Sorting or merging.&lt;/strong&gt; Depending on the writer path, records are sorted on the map side and merged on the reduce side.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The stage barrier.&lt;/strong&gt; No reduce task can start before every map task has finished, so the slowest map task sets the floor.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The barrier is the one people forget, and it is why skew is so damaging: a
single slow map task holds up the entire downstream stage, wasting every other
slot.&lt;/p&gt;

  &lt;p&gt;There is also a scaling cost: with m map and r reduce tasks there are m x r
blocks to track and fetch. Very high parallelism on both sides turns into
metadata and connection overhead, which is one reason “just raise the partition
count” has a limit.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;What to do about it, in order:&lt;/strong&gt; eliminate the shuffle (broadcast, or bucket
the tables); reduce the data entering it (filter and project first); reduce the
number of shuffles (aggregate and join on the same key); then size its
partitions sensibly.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;88. What are Spark&apos;s shuffle writers?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortShuffleManager&lt;/code&gt; selects between three writer paths, checked cheapest
first:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Writer&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Chosen when&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Behaviour&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Bypass merge sort&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No map-side combine, and partition count below &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.sort.bypassMergeThreshold&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Writes one file per partition then concatenates. No sorting&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Unsafe (serialized) sort&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No map-side combine, and the serializer supports relocating serialized objects&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Sorts serialized records in binary form, so no deserialization&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Sort&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Everything else&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Sorts deserialized records, spilling as needed. Most general, most expensive&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;The condition that disqualifies both fast paths is the same: &lt;strong&gt;a map-side
combine&lt;/strong&gt;. And a map-side combine is exactly what &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt;,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;aggregateByKey&lt;/code&gt; and a partial aggregation do.&lt;/p&gt;

  &lt;p&gt;So there is a real tension worth articulating:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;reduceByKey   -&amp;gt;  less data shuffled, through the slowest writer
groupByKey    -&amp;gt;  more data shuffled, possibly through a faster writer
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt; still usually wins, because network volume dominates writer
efficiency, and because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt; risks an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; on a hot key.
But knowing &lt;em&gt;why&lt;/em&gt; the trade exists, rather than reciting “always use
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;reduceByKey&lt;/code&gt;”, is what a deeper question is probing for.&lt;/p&gt;

  &lt;p&gt;For DataFrames you rarely choose directly: Catalyst inserts partial aggregation
automatically, which is a map-side combine, so aggregations take the sort
path.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;89. What is data skew?&lt;/summary&gt;

  &lt;p&gt;An uneven distribution of rows across partitions, so some tasks have far more
work than others.&lt;/p&gt;

  &lt;p&gt;The cause is almost always the data itself: one key dominates. A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_id&lt;/code&gt;
column where 60% of trips are in one city, a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;customer_id&lt;/code&gt; where a single
enterprise account has millions of rows, or a null key that every unmatched row
shares.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph E[&quot;even&quot;]
    A1[&quot;task 0: 1M rows&quot;] --- A2[&quot;task 1: 1M&quot;] --- A3[&quot;task 2: 1M&quot;] --- A4[&quot;task 3: 1M&quot;]
  end
  subgraph S[&quot;skewed&quot;]
    B1[&quot;task 0: 100K&quot;] --- B2[&quot;task 1: 100K&quot;] --- B3[&quot;task 2: &amp;lt;b&amp;gt;3.7M&amp;lt;/b&amp;gt;&quot;] --- B4[&quot;task 3: 100K&quot;]
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Why it is so costly: &lt;strong&gt;a task is the unit of work and cannot be split.&lt;/strong&gt; One
task with ten times the rows takes roughly ten times as long, and because of the
stage barrier, every other slot sits idle waiting for it. A 200-task stage can
be limited by one task.&lt;/p&gt;

  &lt;p&gt;The signatures to recognise:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;One or two tasks with far longer duration than the median, in the same stage.&lt;/li&gt;
    &lt;li&gt;Those same tasks showing much larger shuffle read.&lt;/li&gt;
    &lt;li&gt;A stage that is 95% complete for a long time.&lt;/li&gt;
    &lt;li&gt;An &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; on one executor while others are fine.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Skew is a data problem that presents as a performance problem, which is why
config tuning rarely fixes it.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;90. How do you detect skew?&lt;/summary&gt;

  &lt;p&gt;Two places: the UI, and the data.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;In the UI&lt;/strong&gt;, open the stage page and read the task metrics summary, which gives
min, 25th percentile, median, 75th and max for duration and shuffle read. A
median of 3 seconds with a max of 40 minutes is skew, and the shuffle read
column confirms it is data volume rather than a slow machine.&lt;/p&gt;

  &lt;p&gt;That distinction matters:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Duration&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Shuffle read&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Diagnosis&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Skew. Fix the distribution&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Roughly even&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Environment. A slow node, GC, or a bad disk&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;In the data&lt;/strong&gt;, count rows per key and look at the head of the distribution:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orderBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;desc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;count&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;show&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;20&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;truncate&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;False&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;For an RDD, count rows per partition directly:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rdd&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mapPartitionsWithIndex&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;_&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rows&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))]).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;collect&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;And for a join, remember to check &lt;strong&gt;both&lt;/strong&gt; sides plus the null count on the key,
since nulls all hash together:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;isNull&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;91. How do you fix skew?&lt;/summary&gt;

  &lt;p&gt;In order of preference, cheapest and least invasive first.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Let AQE handle it.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.enabled&lt;/code&gt; defaults to
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; and splits skewed partitions automatically. Check the thresholds first,
because they are strict: it needs a partition larger than 5x the median &lt;strong&gt;and&lt;/strong&gt;
larger than 256 MB.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. Broadcast the small side,&lt;/strong&gt; if there is one. No shuffle means no skew.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;3. Salt the hot key.&lt;/strong&gt; Spread it across N synthetic sub-keys, join, then
aggregate the salt away. This is the general answer when the hot key is
genuinely large.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;4. Separate the hot keys.&lt;/strong&gt; Process them with a different strategy and union
the results. More code, but optimal when there are only a handful.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;hot&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;sf&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;isin&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;hot&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;~&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;isin&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;hot&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;result&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unionByName&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;5. Filter nulls out of the join key&lt;/strong&gt; if they carry no meaning, since they all
land in one partition.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;What not to do:&lt;/strong&gt; enable speculation. The duplicate task has the same rows and
is equally slow, so it burns a second slot for nothing.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;92. How does salting work in practice?&lt;/summary&gt;

  &lt;p&gt;Add a random suffix to the hot key on the fact side, and replicate the
dimension side once per possible suffix so every match still exists.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;functions&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;as&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;N&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;16&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;facts&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rand&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;N&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;int&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;dims&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;array&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;([&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;N&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)]))))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;joined&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;facts&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;dims&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;drop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;all &apos;sf&apos; rows&amp;lt;br/&amp;gt;one partition&quot;] --&amp;gt; B[&quot;salt 0..15&quot;]
  B --&amp;gt; C[&quot;16 partitions:&amp;lt;br/&amp;gt;sf-0, sf-1, ... sf-15&quot;]
  D[&quot;cities row &apos;sf&apos;&quot;] --&amp;gt; E[&quot;replicated x16&amp;lt;br/&amp;gt;sf-0 ... sf-15&quot;]
  C --&amp;gt; F[&quot;join, now spread&amp;lt;br/&amp;gt;across 16 tasks&quot;]
  E --&amp;gt; F
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The trade is explicit: &lt;strong&gt;the dimension side is N times larger.&lt;/strong&gt; With N=16 and a
1 MB dimension that is 16 MB, which is nothing. With a 3 GB dimension it is 48 GB,
which is not. So N stays small, and salting suits a large-fact-to-small-dimension
shape.&lt;/p&gt;

  &lt;p&gt;For an &lt;strong&gt;aggregation&lt;/strong&gt; rather than a join, salting is two-phase and cheaper,
because nothing is replicated:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rand&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;N&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;int&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;part&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;part&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;total&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Aggregate per salted key, then aggregate the partials. The first pass spreads
the hot key over N tasks; the second is tiny.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;93. What are the adaptive skew join settings and their defaults?&lt;/summary&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Config&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Default&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.enabled&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.enabled&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.skewedPartitionFactor&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;5.0&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;256MB&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;Both conditions must hold.&lt;/strong&gt; A partition is treated as skewed only when it is
larger than 5 times the median &lt;strong&gt;and&lt;/strong&gt; larger than 256 MB.&lt;/p&gt;

  &lt;p&gt;That conjunction is the answer to “AQE skew handling is on but nothing
happened”. On a job whose partitions are all under 256 MB, skew handling never
fires no matter how uneven they are. A stage with a 2 MB median and a 100 MB
outlier is 50x skewed and completely untouched.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# make it fire on a smaller job
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;32MB&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;When it does fire, Spark splits the skewed partition into several smaller
reduce tasks and replicates the matching partition from the other side so each
split still finds its matches. That is salting, done automatically at runtime.&lt;/p&gt;

  &lt;p&gt;Two limits worth knowing: it applies to &lt;strong&gt;joins&lt;/strong&gt;, so a skewed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupBy&lt;/code&gt; is not
covered, and it needs the shuffle to have actually happened, so it cannot help a
skewed &lt;em&gt;read&lt;/em&gt;.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;94. What is adaptive query execution?&lt;/summary&gt;

  &lt;p&gt;Re-planning parts of a query at runtime, using real statistics from stages that
have already finished, instead of relying on planning-time estimates.&lt;/p&gt;

  &lt;p&gt;It is on by default since Spark 3.2: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.enabled&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;Three things it does:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Optimisation&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What it fixes&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Coalescing shuffle partitions&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A static &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt; that is wrong for this data&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Switching join strategy&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;An estimate that said “large” when the side is actually small&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Splitting skewed partitions&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One task with far more rows than its peers&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  S[&quot;a shuffle stage completes:&amp;lt;br/&amp;gt;real sizes are now known&quot;]
  S --&amp;gt; A[&quot;&amp;lt;b&amp;gt;coalesce partitions&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;merge the small ones&quot;]
  S --&amp;gt; B[&quot;&amp;lt;b&amp;gt;switch join strategy&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;sort-merge to broadcast&quot;]
  S --&amp;gt; C[&quot;&amp;lt;b&amp;gt;split skewed partitions&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;one big task into several&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Why it works so well: planning-time estimates on &lt;em&gt;derived&lt;/em&gt; data are usually
wrong. Spark can estimate the size of a table it has statistics for, but after
three filters and a join, the estimate is a guess. After the shuffle, it is a
measurement.&lt;/p&gt;

  &lt;p&gt;Reading it: before execution the plan shows &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AdaptiveSparkPlan
isFinalPlan=false&lt;/code&gt;, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; gives you the &lt;em&gt;planned&lt;/em&gt; shape. To see what
actually ran, look at the SQL tab after the query finishes.&lt;/p&gt;

  &lt;p&gt;The main caveat is that AQE works at &lt;strong&gt;shuffle boundaries&lt;/strong&gt;, so a query with no
shuffle gets nothing from it, and the first stage is always planned blind.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;95. What does AQE partition coalescing do?&lt;/summary&gt;

  &lt;p&gt;After a shuffle, it merges small output partitions into fewer, larger tasks.&lt;/p&gt;

  &lt;p&gt;The relevant settings:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Config&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Default&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.coalescePartitions.enabled&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.advisoryPartitionSizeInBytes&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;64MB&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.coalescePartitions.minPartitionSize&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1MB&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Spark writes the full &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; worth of output, looks at
the actual size of each, and then assigns contiguous runs of them to single
reduce tasks so each task gets roughly the advisory size.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;200 shuffle partitions, 800 MB total
   -&amp;gt; without coalescing: 200 tasks x 4 MB
   -&amp;gt; with coalescing:     13 tasks x ~64 MB
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;This is what makes the static default tolerable.&lt;/strong&gt; Before AQE, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt; had to be
tuned per job or you paid for it; now a high value is mostly harmless, because
coalescing brings it down.&lt;/p&gt;

  &lt;p&gt;The practical consequence for tuning: &lt;strong&gt;set the advisory size, not the partition
count.&lt;/strong&gt;&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;128MB&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;That expresses the intent (“I want partitions about this big”) and lets Spark do
the arithmetic per stage, which is better than one number for a query whose
stages differ in size.&lt;/p&gt;

  &lt;p&gt;Note it only merges; it never splits. A stage whose partitions are too &lt;em&gt;large&lt;/em&gt;
needs a higher &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;, or skew handling.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;96. How do you choose the number of shuffle partitions?&lt;/summary&gt;

  &lt;p&gt;Work from two constraints and take whichever is larger.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Constraint 1, partition size.&lt;/strong&gt; Aim for partitions in the low hundreds of
megabytes of shuffled data. Too large and you spill; too small and scheduling
dominates.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;partitions = total shuffle bytes / target partition size
           = 600 GB / 128 MB
           = ~4,800
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;Constraint 2, core count.&lt;/strong&gt; You want a small multiple of your total cores, so
every core gets several tasks and stragglers average out.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;cores = num_executors x spark.executor.cores = 50 x 4 = 200
partitions = 200 x 3 = 600      (a floor, not a target)
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;With AQE, the better move is to express the first constraint directly and let
coalescing handle the rest:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2000&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;            &lt;span class=&quot;c1&quot;&gt;# generous
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;128MB&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Set the count high enough that no partition is too big, and let AQE merge the
small ones down. That handles a query whose stages differ in volume, which a
single static number cannot.&lt;/p&gt;

  &lt;p&gt;The diagnostic loop: if the stage page shows spill, partitions are too large. If
it shows thousands of tasks lasting milliseconds, they are too small. Those two
symptoms are the feedback signal, not a formula.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;97. What is the small files problem, and how do you avoid it?&lt;/summary&gt;

  &lt;p&gt;Many tiny files make every later read slow, for reasons that are not about
total bytes:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Listing cost.&lt;/strong&gt; Planning must enumerate the files, which on object storage means many API calls.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Per-file open cost.&lt;/strong&gt; Spark charges &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.openCostInBytes&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4MB&lt;/code&gt;, per file when computing splits, precisely because opening one is not free.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Metadata overhead.&lt;/strong&gt; Each Parquet file has a footer to read.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Compression loss.&lt;/strong&gt; Small files compress worse, because dictionaries and encodings have less to work with.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Where they come from, and the fix for each:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cause&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;High write parallelism&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(n)&lt;/code&gt; before the write&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionBy&lt;/code&gt; on many values&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(same cols)&lt;/code&gt; first&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Streaming with a short trigger&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Longer trigger, or periodic compaction&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Over-partitioning&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Partition on a lower-cardinality column&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;You can also cap file size rather than count:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.files.maxRecordsPerFile&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;5000000&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;which defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0&lt;/code&gt;, meaning unlimited.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The structural fix&lt;/strong&gt; for a continuously written table is a lakehouse format,
because Hudi, Iceberg and Delta all provide compaction as a managed table
service rather than something you bolt on.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;98. What is the difference between `repartition(n)` and `repartition(col)`?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt; distributes rows round-robin into n partitions, producing even
sizes and no relationship between a row’s value and its partition.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(col)&lt;/code&gt; hash-partitions by that column, so all rows sharing a value
land together.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;400&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;                 &lt;span class=&quot;c1&quot;&gt;# even, value-agnostic
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;           &lt;span class=&quot;c1&quot;&gt;# by value, into spark.sql.shuffle.partitions
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;400&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# by value, into 400
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt;&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(col)&lt;/code&gt;&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Distribution&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Even by construction&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Even only if the key is uniform&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Co-locates by value&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Use before&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Heavy per-row work needing balance&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A write partitioned by that column, or a join or aggregate on it&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;The catch with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(col)&lt;/code&gt; is skew.&lt;/strong&gt; Hash partitioning a column where
one value dominates puts all those rows in one partition, by design. So it fixes
file layout and can remove a later shuffle, while potentially creating the very
skew you were trying to avoid. Check the distribution before reaching for it.&lt;/p&gt;

  &lt;p&gt;The most common correct use is immediately before a partitioned write, so each
output directory is produced by one task rather than all of them. The second is
before several operations that key on the same column, so one exchange serves
them all instead of each operator shuffling separately.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;99. Does `orderBy` shuffle, and how?&lt;/summary&gt;

  &lt;p&gt;Yes, and it is more expensive than most shuffles because of how it partitions.&lt;/p&gt;

  &lt;p&gt;A global sort needs range partitioning: partition 0 gets the lowest values,
partition 1 the next, and so on, so that concatenating the sorted partitions
yields a globally sorted result. But Spark does not know the value distribution
in advance, so it must &lt;strong&gt;sample the data first&lt;/strong&gt; to pick range boundaries.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  A[&quot;sample the data&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;an extra job&amp;lt;/i&amp;gt;&quot;] --&amp;gt; B[&quot;compute range boundaries&quot;]
  B --&amp;gt; C[&quot;shuffle by range&quot;]
  C --&amp;gt; D[&quot;sort within each partition&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;That sampling job is why &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;orderBy&lt;/code&gt; can show an unexpected extra job in the UI
before the real work starts.&lt;/p&gt;

  &lt;p&gt;If you only need ordering &lt;strong&gt;within&lt;/strong&gt; partitions, which is often enough (for
example to make a file’s contents ordered, or for a window already partitioned
by the same key), use the cheaper form:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sortWithinPartitions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# no range partitioning, no sampling
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Two related notes:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RangePartitioner&lt;/code&gt; handles skew badly&lt;/strong&gt; when many rows share a value, since boundaries cannot split a single value.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;orderBy&lt;/code&gt; before a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;limit&lt;/code&gt;&lt;/strong&gt; is a special case Spark optimises with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TakeOrderedAndProject&lt;/code&gt;, which avoids a full global sort by taking the top n per partition and merging.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;100. How can you join without a shuffle?&lt;/summary&gt;

  &lt;p&gt;Three ways, and each removes the shuffle for a different reason.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Broadcast the small side.&lt;/strong&gt; Every executor gets the whole small table, so
each large-side partition can join locally. No redistribution needed.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Automatic when the estimated size is below
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10485760&lt;/code&gt; (10 MB).&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. Bucket both tables the same way.&lt;/strong&gt; If both were written with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;bucketBy&lt;/code&gt; on
the join key with the &lt;strong&gt;same bucket count&lt;/strong&gt;, matching rows are already
co-located in corresponding files, and the metastore records that fact.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;bucketBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;64&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sortBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;saveAsTable&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;lakehouse.trips_bucketed&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;This is the only shuffle-free option for a genuinely large-to-large join.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;3. Reuse an existing partitioning.&lt;/strong&gt; If a DataFrame is already hash-partitioned
on the join key, from a previous &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(col)&lt;/code&gt; or an earlier shuffle on the
same key, Catalyst can satisfy the join’s requirement without a new exchange.
With RDDs this is explicit co-partitioning plus &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache&lt;/code&gt;.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;shuffle needed?&quot;] --&amp;gt; B{&quot;one side small?&quot;}
  B --&amp;gt;|&quot;yes&quot;| C[&quot;broadcast: no shuffle&quot;]
  B --&amp;gt;|&quot;no&quot;| D{&quot;both bucketed&amp;lt;br/&amp;gt;same key, same count?&quot;}
  D --&amp;gt;|&quot;yes&quot;| E[&quot;bucketed join: no shuffle&quot;]
  D --&amp;gt;|&quot;no&quot;| F{&quot;already partitioned&amp;lt;br/&amp;gt;on the key?&quot;}
  F --&amp;gt;|&quot;yes&quot;| G[&quot;reuse: no shuffle&quot;]
  F --&amp;gt;|&quot;no&quot;| H[&quot;sort-merge join: shuffle&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;101. What is the shuffle fetch side, and which knobs matter?&lt;/summary&gt;

  &lt;p&gt;The reduce side pulling its blocks from every map output. It is the half that
crosses the network, and the knobs govern how aggressively it does so.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Config&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Governs&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.reducer.maxSizeInFlight&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;How much data may be in flight per reduce task&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.reducer.maxReqsInFlight&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Concurrent fetch requests&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.io.maxRetries&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Retries per failed fetch&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.io.retryWait&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Wait between retries&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.network.timeout&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The general network timeout, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;120s&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Raising the in-flight limits increases throughput and memory pressure; lowering
them is gentler on a loaded cluster.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;But the important answer is that fetch-side tuning is rarely the fix.&lt;/strong&gt; A
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FetchFailedException&lt;/code&gt; almost always means the executor holding those blocks is
gone, was killed for exceeding memory, or is too overwhelmed to serve. Raising
retries makes the job take longer to fail rather than succeed.&lt;/p&gt;

  &lt;p&gt;So the diagnostic order is:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;Find the executor that was serving those blocks and why it died. The executors tab shows loss reasons.&lt;/li&gt;
    &lt;li&gt;If it was killed for memory, fix that: overhead, skew, or partition size.&lt;/li&gt;
    &lt;li&gt;Consider an external shuffle service, so shuffle output survives the executor.&lt;/li&gt;
    &lt;li&gt;Only then touch fetch settings, and only with evidence of genuinely slow but healthy transfers.&lt;/li&gt;
  &lt;/ol&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;102. What causes `FetchFailedException`?&lt;/summary&gt;

  &lt;p&gt;The reduce side could not get shuffle blocks it needs. The block was written,
but whatever was serving it is no longer able to.&lt;/p&gt;

  &lt;p&gt;The causes, most common first:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;The executor died.&lt;/strong&gt; Killed by the cluster manager for exceeding memory limits, or crashed with an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;. Its shuffle files went with it.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The executor is alive but overwhelmed&lt;/strong&gt;, so fetch requests time out while it is busy with GC or computation.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The node died&lt;/strong&gt;, taking its local disk with it.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Disk full,&lt;/strong&gt; so the map side could not write or the service cannot read.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Genuine network problems,&lt;/strong&gt; which are the rarest in practice.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  A[&quot;FetchFailedException&quot;] --&amp;gt; B[&quot;look at the EXECUTORS tab,&amp;lt;br/&amp;gt;not the network config&quot;]
  B --&amp;gt; C{&quot;why did it die?&quot;}
  C --&amp;gt;|&quot;killed for memory&quot;| D[&quot;overhead, skew,&amp;lt;br/&amp;gt;or partition size&quot;]
  C --&amp;gt;|&quot;OOM&quot;| E[&quot;partition too large&quot;]
  C --&amp;gt;|&quot;still alive&quot;| F[&quot;GC pressure or overload&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;&lt;strong&gt;What Spark does automatically:&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; treats it as lost map output
and resubmits the upstream stage to regenerate it. So the job often recovers,
just slowly, which is why you may see a stage run twice.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;What to do:&lt;/strong&gt; fix the executor deaths. An external shuffle service also helps
directly, because shuffle blocks are then served by a node-level daemon that
outlives the executor.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;103. What is shuffle spill, and what does it tell you?&lt;/summary&gt;

  &lt;p&gt;Spill is data written to disk because it did not fit in execution memory. The
stage page reports it as two numbers: &lt;strong&gt;spill (memory)&lt;/strong&gt;, the in-memory size of
what was spilled, and &lt;strong&gt;spill (disk)&lt;/strong&gt;, the serialized size actually written.&lt;/p&gt;

  &lt;p&gt;It happens on either side: the map side while sorting and buffering output, or
the reduce side while merging fetched blocks and aggregating.&lt;/p&gt;

  &lt;p&gt;What it means: &lt;strong&gt;the per-task working set exceeds the execution memory each task
gets.&lt;/strong&gt; That memory is roughly the unified pool divided by the number of
concurrently running tasks on that executor.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;executor memory 16g
  - 300 MB reserved
  x spark.memory.fraction 0.6          -&amp;gt; ~9.6 GB unified pool
  / spark.executor.cores 4             -&amp;gt; ~2.4 GB per running task
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;So the fixes, in order:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;More partitions,&lt;/strong&gt; so each task’s share is smaller. This is usually the right answer and costs nothing but a config.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Fewer cores per executor,&lt;/strong&gt; which gives each running task a larger slice of the same pool.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check for skew,&lt;/strong&gt; because one oversized partition spills while its peers do not.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;More memory,&lt;/strong&gt; last, because it treats the symptom.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;A little spill is not an emergency; Spark is designed to spill rather than fail.
Heavy spill relative to shuffle size means you are paying disk I/O for work that
should have been in memory.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;104. Why can `coalesce(1)` be dangerous?&lt;/summary&gt;

  &lt;p&gt;Because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce&lt;/code&gt; is a &lt;strong&gt;narrow&lt;/strong&gt; dependency, so it creates no stage boundary,
and the reduced parallelism propagates backwards into the stage that feeds it.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  subgraph C[&quot;coalesce(1): ONE stage, 1 task total&quot;]
    R1[&quot;read 200 partitions&quot;] --&amp;gt; M1[&quot;expensive transform&quot;] --&amp;gt; Z1[&quot;coalesce(1)&quot;] --&amp;gt; W1[&quot;write 1 file&quot;]
  end
  subgraph R[&quot;repartition(1): TWO stages&quot;]
    R2[&quot;read 200 partitions&quot;] --&amp;gt; M2[&quot;expensive transform&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;200 tasks&amp;lt;/b&amp;gt;&quot;] --&amp;gt; S2[(&quot;shuffle&quot;)] --&amp;gt; W2[&quot;write 1 file&amp;lt;br/&amp;gt;1 task&quot;]
  end
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;With &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(1)&lt;/code&gt;, the read and the expensive transform are in the same stage as
the single-partition write, so the whole pipeline runs in one task on one core.
A job that should take minutes takes hours, and the cluster sits idle.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(1)&lt;/code&gt; inserts a shuffle, which costs a full redistribution but keeps
the upstream stage at full parallelism.&lt;/p&gt;

  &lt;p&gt;So the rule:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Situation&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Use&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Reducing partitions after cheap work&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(n)&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Reducing to a very small number after expensive work&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Needing balance, or more partitions&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;A middle path is often best: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(200)&lt;/code&gt; to trim file count without a
shuffle, rather than collapsing to one. And if you need exactly one file, ask
whether you actually do, because a single-file requirement usually comes from a
downstream tool that could read a directory instead.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;memory-caching-and-persistence&quot;&gt;Memory, caching and persistence&lt;/h2&gt;

&lt;p&gt;Where the errors are most alarming and the fixes least obvious. Interviewers use this area to find out whether you understand the memory model or just raise numbers until it works.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;105. How is executor memory divided?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UnifiedMemoryManager&lt;/code&gt; does the arithmetic in a fixed order:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;Subtract &lt;strong&gt;300 MB&lt;/strong&gt; of reserved memory, a constant for Spark’s own overhead.&lt;/li&gt;
    &lt;li&gt;Take &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.fraction&lt;/code&gt; of what remains, default &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0.6&lt;/code&gt;&lt;/strong&gt;, as the &lt;strong&gt;unified pool&lt;/strong&gt; shared by execution and storage.&lt;/li&gt;
    &lt;li&gt;What is left, 40% of the post-reserved heap, is &lt;strong&gt;user memory&lt;/strong&gt;: your own data structures, UDF state, and Spark internal metadata.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;--executor-memory 16g  (16,384 MB)
  - 300 MB reserved              -&amp;gt; 16,084 MB usable
  x 0.6 memory.fraction          -&amp;gt; 9,650 MB unified pool
      of which storageFraction 0.5 -&amp;gt; 4,825 MB storage floor
  remaining 40%                  -&amp;gt; 6,434 MB user memory
  + memoryOverhead               -&amp;gt; ~18,022 MB requested from the cluster
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  C[&quot;container: heap + overhead&quot;] --&amp;gt; H[&quot;heap 16g&quot;]
  C --&amp;gt; O[&quot;overhead, off-heap and native&quot;]
  H --&amp;gt; R[&quot;300 MB reserved&quot;]
  H --&amp;gt; U[&quot;unified pool, 60%&amp;lt;br/&amp;gt;execution + storage&quot;]
  H --&amp;gt; M[&quot;user memory, 40%&quot;]
  U --&amp;gt; E[&quot;execution: shuffles, joins, sorts&quot;]
  U --&amp;gt; S[&quot;storage: cached blocks, broadcasts&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Two things people miss: the pool is shared between &lt;strong&gt;execution and storage
combined&lt;/strong&gt;, which is usually less than expected; and the container asks the
cluster manager for heap &lt;strong&gt;plus&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverhead&lt;/code&gt;, which is why a
16g executor consumes about 18g of cluster capacity.&lt;/p&gt;

  &lt;p&gt;Off-heap is separate and off by default: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.offHeap.enabled&lt;/code&gt; is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;106. What is the difference between execution and storage memory?&lt;/summary&gt;

  &lt;p&gt;They are two uses of one pool.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Execution memory&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Storage memory&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Used for&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shuffles, joins, sorts, aggregations&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Cached blocks, broadcast data&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Lifetime&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The duration of a task&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Until evicted or unpersisted&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;If it runs short&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Spills to disk&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Evicts blocks, or spills if the level allows&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Floor&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;None&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.storageFraction&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0.5&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;They borrow from each other, which is what “unified” means. If nothing is
cached, execution can use the whole pool. If no shuffle is running, cached blocks
can fill it.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.storageFraction&lt;/code&gt; is not a partition; it is the &lt;strong&gt;floor below which
cached blocks will not be evicted&lt;/strong&gt;. Cached data can grow past it when execution
is idle, but cannot be pushed below it.&lt;/p&gt;

  &lt;p&gt;Before Spark 1.6 these were static pools, and the tuning pain of that is why
unification happened: a job that cached nothing still had memory reserved for
caching it could not use.&lt;/p&gt;

  &lt;p&gt;The consequence for tuning: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.fraction&lt;/code&gt; is rarely worth changing,
because raising it steals from user memory, which UDFs and your own objects
need. If you are tempted to raise it, the real problem is usually partition
size.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;107. Can execution and storage evict each other?&lt;/summary&gt;

  &lt;p&gt;No, and the asymmetry is the whole answer.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Execution can evict cached blocks, down to the storage floor. Storage can
never evict execution.&lt;/strong&gt;&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  E[&quot;execution needs memory&quot;] --&amp;gt;|&quot;evicts cached blocks&amp;lt;br/&amp;gt;down to storageFraction&quot;| S[&quot;storage&quot;]
  S -.-&amp;gt;|&quot;never evicts&quot;| E
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The reason is correctness and progress: a task that cannot get execution memory
can only spill or fail, whereas a cached block that is evicted can simply be
recomputed from lineage. So Spark prioritises the thing that cannot recover.&lt;/p&gt;

  &lt;p&gt;The practical consequences, and these are the interview-worthy part:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Caching a large DataFrame cannot fail a shuffle.&lt;/strong&gt; Execution will take the memory it needs.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;But it can make a shuffle spill,&lt;/strong&gt; because the cache occupied the pool first and execution had to reclaim it, and because whatever stays above the floor is unavailable.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;So adding a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache()&lt;/code&gt; can make a job slower,&lt;/strong&gt; which is a counterintuitive result worth being able to explain.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;And an under-populated cache is the worst outcome:&lt;/strong&gt; you paid to write it, execution evicted most of it, and now every use partly recomputes.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Check the storage tab. A cached dataset showing 40% in memory is costing you
without paying you back, and either needs &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK&lt;/code&gt;, a serialized level,
or not to be cached at all.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;108. What is the difference between heap and off-heap memory?&lt;/summary&gt;

  &lt;p&gt;On-heap memory is allocated inside the JVM heap and managed by the garbage
collector. Off-heap is allocated outside it, through &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sun.misc.Unsafe&lt;/code&gt;, and is
not subject to GC.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;On-heap&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Off-heap&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Managed by&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;JVM garbage collector&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Spark, explicitly&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Config&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.offHeap.size&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Enabled&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Always&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.memory.offHeap.enabled&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;GC pressure&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Risk&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Long GC pauses&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Leaks are not reclaimed automatically&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Why off-heap exists: a large heap full of small objects makes full garbage
collections slow, and GC pauses stall the executor. Storing data off-heap in a
compact binary form removes it from the collector’s view entirely.&lt;/p&gt;

  &lt;p&gt;This is what &lt;strong&gt;Tungsten&lt;/strong&gt; is built on. Spark’s binary row format stores rows as
contiguous bytes with a known layout, so it can be read field by field without
constructing JVM objects, and can live off-heap.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.memory.offHeap.enabled&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.memory.offHeap.size&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;4g
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;When to bother: very large executors where GC time is a visible fraction of task
time, or heavy shuffle and aggregation workloads. Note that off-heap size counts
toward the container total, so it must fit inside what the cluster manager
granted, which usually means accounting for it in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;memoryOverhead&lt;/code&gt;.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;109. What is executor memory overhead?&lt;/summary&gt;

  &lt;p&gt;Memory requested from the cluster manager &lt;strong&gt;in addition to&lt;/strong&gt; the JVM heap, to
cover everything that is not heap:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Off-heap allocations and Tungsten buffers.&lt;/li&gt;
    &lt;li&gt;The Python worker process, for PySpark.&lt;/li&gt;
    &lt;li&gt;Native libraries, and the JVM’s own non-heap regions such as metaspace, thread stacks and code cache.&lt;/li&gt;
    &lt;li&gt;Netty’s direct buffers for shuffle transfer.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverhead&lt;/code&gt; defaults to a fraction of executor memory with a
minimum floor, so a 16g executor requests roughly 18g in total.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.executor.memory&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;16g &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.executor.memoryOverhead&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;3g
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;Why it is the answer to a specific failure.&lt;/strong&gt; When the cluster manager kills a
container for exceeding its memory limit, that is the &lt;em&gt;total&lt;/em&gt; being exceeded, not
the heap. A JVM &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; means the heap; a container kill means the
total. They have different fixes, and telling them apart is the skill:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Symptom&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cause&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;java.lang.OutOfMemoryError: Java heap space&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Heap exhausted&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;More partitions, or more &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;executor.memory&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Container killed by YARN or Kubernetes for exceeding limits&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Total exceeded&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;memoryOverhead&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;PySpark jobs need noticeably more overhead than JVM ones, because every executor
runs Python workers whose memory is entirely outside the heap. A PySpark job
being killed while the heap looks healthy is the classic case.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;110. What causes a driver `OutOfMemoryError`?&lt;/summary&gt;

  &lt;p&gt;Something is pulling data into the driver, or the driver is tracking too much
metadata.&lt;/p&gt;

  &lt;p&gt;The causes, most common first:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;toPandas()&lt;/code&gt;&lt;/strong&gt; on a large result. This is by far the most common.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A broadcast that is not small.&lt;/strong&gt; The driver must assemble the whole table before sending it, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;broadcast()&lt;/code&gt; on a multi-gigabyte side fails on the driver.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A huge number of tasks.&lt;/strong&gt; The driver tracks metrics and state per task, so a job with hundreds of thousands of tasks needs real memory just for bookkeeping.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A deep or wide query plan.&lt;/strong&gt; Analysis and optimisation hold the plan tree in driver memory; thousands of chained &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; calls or very wide schemas add up.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Accumulating results in a loop&lt;/strong&gt;, appending to a driver-side list across iterations.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.memory&lt;/code&gt; defaults to &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;&lt;/strong&gt;, which is a coordinator’s budget.&lt;/p&gt;

  &lt;p&gt;The distinction from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.maxResultSize&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;, is worth making:
that config aborts the job with a clear named error &lt;em&gt;before&lt;/em&gt; the heap is
exhausted. So a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxResultSize&lt;/code&gt; error is Spark protecting you, and an
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; is what happens when the data arrived some other way, such as
through a broadcast.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The fix is usually structural,&lt;/strong&gt; not a larger driver: write instead of
collect, aggregate instead of transfer, and check the plan for an accidental
broadcast.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;111. What causes an executor `OutOfMemoryError`?&lt;/summary&gt;

  &lt;p&gt;A single task needing more heap than it can get. The usual sources:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;A partition too large.&lt;/strong&gt; The task must hold its working set, and if that exceeds its share of the unified pool plus what can spill, it fails.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Skew.&lt;/strong&gt; One key concentrating rows into one task, so that task alone is oversized.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A high-cardinality aggregation&lt;/strong&gt;, where the hash map of groups grows large.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupByKey&lt;/code&gt; on a hot key,&lt;/strong&gt; because all values for one key must fit on the reduce side.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A UDF materialising too much,&lt;/strong&gt; for example &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;list(rows)&lt;/code&gt; inside &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;mapPartitions&lt;/code&gt;, or an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;applyInPandas&lt;/code&gt; group that does not fit.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Too many cores per executor,&lt;/strong&gt; so the pool is divided too many ways.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The arithmetic that explains most cases:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;per-task execution memory ~= unified pool / concurrent tasks on the executor
                          ~= 9.6 GB / 4 cores
                          ~= 2.4 GB
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;So the fixes in order:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;More partitions.&lt;/strong&gt; Halving partition size halves the working set, and this is usually the correct answer.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check and fix skew,&lt;/strong&gt; since one bad partition is invisible in an average.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Fewer cores per executor,&lt;/strong&gt; which gives each task a bigger slice of the same pool.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A serialized storage level&lt;/strong&gt; if caching is competing.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;More executor memory,&lt;/strong&gt; last.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;Raising memory first often just moves the failure to a larger dataset next
month.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;112. When should you cache?&lt;/summary&gt;

  &lt;p&gt;When both conditions hold: the dataset is used &lt;strong&gt;more than once&lt;/strong&gt;, and
producing it was &lt;strong&gt;expensive&lt;/strong&gt;.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;base&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-01&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;dims&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;        &lt;span class=&quot;c1&quot;&gt;# expensive: a scan and a shuffle
&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;base&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;base&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                        &lt;span class=&quot;c1&quot;&gt;# populates
&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;by_city&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;base&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;by_rider&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;base&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# reuses the cache
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Without the cache, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;base&lt;/code&gt; is recomputed for each aggregation, including the scan
and the join.&lt;/p&gt;

  &lt;p&gt;Good candidates:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;A cleaned, filtered base dataset feeding several outputs.&lt;/li&gt;
    &lt;li&gt;An iterative algorithm reading the same data each pass.&lt;/li&gt;
    &lt;li&gt;A dataset used by both branches of a self-join or a union.&lt;/li&gt;
    &lt;li&gt;Anything you will query interactively in a notebook.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;And cache deliberately, then verify.&lt;/strong&gt; Call an action to populate it, and check
the storage tab: if it is not fully in memory, the cache may be costing more
than it saves. Release it when done:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;base&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unpersist&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;One subtlety: caching is by &lt;strong&gt;plan&lt;/strong&gt;, not by variable. Two DataFrames with
identical plans share the cache entry, which is occasionally surprising and
occasionally very useful.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;113. When should you not cache?&lt;/summary&gt;

  &lt;p&gt;Four cases where it costs more than it saves.&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Used once.&lt;/strong&gt; You pay to write the cache and gain nothing. This is the most common mistake, and in a notebook it is everywhere.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Cheap to recompute.&lt;/strong&gt; A plain scan of a small Parquet file is faster to redo than to serialise into memory.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Memory already tight.&lt;/strong&gt; The cache occupies the unified pool, execution evicts it, and you end up partly recomputing anyway while having made every shuffle more likely to spill.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Immediately before one big shuffle.&lt;/strong&gt; The cache and the shuffle want the same pool, and the shuffle is the thing that cannot recover.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;There is also a correctness-adjacent case: &lt;strong&gt;caching a non-deterministic
DataFrame&lt;/strong&gt; pins one particular result, which may be what you want or may hide a
bug. A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rand()&lt;/code&gt; or a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;current_timestamp()&lt;/code&gt; in the plan behaves differently
cached than uncached.&lt;/p&gt;

  &lt;p&gt;The anti-pattern to name explicitly:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# caching everything, which is caching nothing usefully
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;();&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df2&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;();&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df3&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;();&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df4&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Each competes for the same pool, most get evicted, and every stage now spills
more. &lt;strong&gt;Cache the one dataset that is genuinely reused, populate it, verify it in
the storage tab, and unpersist it when the reuse is over.&lt;/strong&gt;&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;114. What are the DataFrame storage levels worth knowing?&lt;/summary&gt;

  &lt;p&gt;Four, and the choice is a memory-versus-CPU trade.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Level&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;When&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The default for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;df.cache()&lt;/code&gt;. Spills rather than losing blocks&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK_SER&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Memory is tight. More fits, at deserialization cost&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DISK_ONLY&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Recomputation is more expensive than reading back&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_ONLY&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Rarely for DataFrames; the RDD default, and it silently drops what does not fit&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;StorageLevel&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;persist&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;StorageLevel&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;MEMORY_AND_DISK_SER&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The serialized variants are the useful lever. A deserialized cache stores rows
close to their in-memory representation, which is fast to read and large. A
serialized cache is far more compact, so more of the dataset stays in memory, at
the cost of CPU to decode on every read. When the storage tab shows a cache only
partly resident, serialized is usually the better trade.&lt;/p&gt;

  &lt;p&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_2&lt;/code&gt; replicated variants put a copy on a second node so an executor loss
does not force recomputation. They double the memory cost, so they are for the
case where recomputation is genuinely very expensive.&lt;/p&gt;

  &lt;p&gt;Note DataFrame caching already stores data in a compressed columnar form
internally, controlled by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.inMemoryColumnarStorage.compressed&lt;/code&gt;, so the
gap between deserialized and serialized is smaller than for RDDs.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;115. How do you uncache, and why does it matter?&lt;/summary&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unpersist&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;                    &lt;span class=&quot;c1&quot;&gt;# one dataset, blocking=False by default
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;catalog&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;clearCache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;        &lt;span class=&quot;c1&quot;&gt;# everything
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sql&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;UNCACHE TABLE trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;  &lt;span class=&quot;c1&quot;&gt;# a named table
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;It matters because &lt;strong&gt;cached blocks hold storage memory for the lifetime of the
application&lt;/strong&gt;, not the lifetime of the variable. Python’s garbage collector
reclaiming a DataFrame object does not release the blocks on the executors:
Spark’s block manager still holds them.&lt;/p&gt;

  &lt;p&gt;So in a long-running application, a notebook, a streaming job, or a
multi-stage pipeline, forgotten caches accumulate. Every later stage then has
less execution memory, spills more, and runs slower, with no obvious cause. The
symptom is a pipeline that gets slower toward the end for no reason visible in
the plan.&lt;/p&gt;

  &lt;p&gt;The habit worth building:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;base&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;base&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;c1&quot;&gt;# ... the reuse that justified the cache ...
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;base&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;unpersist&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Check what is actually held in the &lt;strong&gt;storage tab&lt;/strong&gt; of the UI, which lists every
cached dataset with its size and memory fraction. Anything there that you no
longer read is pure cost.&lt;/p&gt;

  &lt;p&gt;One note on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;unpersist(blocking=True)&lt;/code&gt;: it waits for the blocks to actually be
removed, which is worth using when you need the memory freed before the next
stage rather than eventually.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;116. Is `cache()` an action?&lt;/summary&gt;

  &lt;p&gt;No. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache()&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;persist()&lt;/code&gt; are &lt;strong&gt;lazy&lt;/strong&gt;: they mark the dataset as one to
store, and the blocks are written during the next action that computes it.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cache&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# nothing happens yet
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# computes, and populates the cache on the way
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(...)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# now reads from the cache
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;This matters in two ways:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;The first action pays the cost.&lt;/strong&gt; It computes the dataset and writes the cache, so it is slower than the uncached version. The saving starts with the second use.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;If you never call an action, nothing is cached,&lt;/strong&gt; which is a silent no-op people hit when they cache and then only inspect the schema.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The idiom is to force population deliberately with a cheap action, usually
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;count()&lt;/code&gt;, so the cost lands where you expect rather than inside the first real
query.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The exception is SQL.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CACHE TABLE&lt;/code&gt; is &lt;strong&gt;eager&lt;/strong&gt; by default:&lt;/p&gt;

  &lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;CACHE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;-- populates immediately&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;CACHE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;LAZY&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;-- behaves like df.cache()&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;That asymmetry between the DataFrame API and SQL is a favourite detail to
probe, because it is easy to assume they behave the same.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;117. What is checkpointing, and how does it differ from caching?&lt;/summary&gt;

  &lt;p&gt;Checkpointing writes a dataset to reliable storage and &lt;strong&gt;truncates its
lineage&lt;/strong&gt;. Caching stores a copy while keeping the lineage intact.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;setCheckpointDir&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hdfs:///checkpoints&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;checkpoint&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# eager by default for DataFrames
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cache&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Checkpoint&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Stored where&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Executor memory or local disk&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Reliable storage, HDFS or S3&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Lineage&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Kept&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Truncated&lt;/strong&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Survives executor loss&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Only with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_2&lt;/code&gt; levels&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Survives application end&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The files do&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Cost&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Cheap&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A full write to reliable storage&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;The reason lineage truncation matters: with a long chain of transformations, a
single lost partition means replaying the whole chain, and the plan itself grows
until analysis is slow or overflows the stack. Checkpointing cuts the chain, so
recovery reads a file instead of recomputing a hundred steps.&lt;/p&gt;

  &lt;p&gt;Where you need it: iterative algorithms that build lineage per iteration, and
Structured Streaming, where checkpointing is mandatory and stores offsets and
state.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;localCheckpoint()&lt;/code&gt; truncates lineage using executor storage instead of
reliable storage: much faster, but lost if the executor is lost, so it trades
durability for speed.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;A common pattern is to do both&lt;/strong&gt;, cache so the write is not recomputed, then
checkpoint.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;118. Why would a long lineage be a problem even if the data fits?&lt;/summary&gt;

  &lt;p&gt;Because lineage is a data structure held on the driver, and it grows whether or
not the data does.&lt;/p&gt;

  &lt;p&gt;Three separate problems:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Planning cost.&lt;/strong&gt; Every action re-analyses and re-optimises the plan. Catalyst walks the tree repeatedly, so a tree with thousands of nodes makes planning a measurable fraction of the job. In a loop, planning time grows each iteration.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Stack depth.&lt;/strong&gt; Many Catalyst rules are recursive over the tree. A deep enough plan overflows the thread stack, producing a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;java.lang.StackOverflowError&lt;/code&gt; that looks nothing like a data problem.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Recovery cost.&lt;/strong&gt; One lost partition replays the entire chain that produced it.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The classic trigger is a loop:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;range&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;200&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;sa&quot;&gt;f&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;c&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;x&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# 200 levels deep
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;or an iterative algorithm where each pass depends on the last.&lt;/p&gt;

  &lt;p&gt;The remedies:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Checkpoint periodically&lt;/strong&gt;, every few iterations, which truncates the chain.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Collapse the operations.&lt;/strong&gt; One &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt; with 200 expressions instead of 200 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; calls.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-Xss&lt;/code&gt;&lt;/strong&gt; on the side that threw, as a stopgap for the stack overflow specifically.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;The tell that distinguishes this from other slowness:&lt;/strong&gt; the job spends its
time before any task starts. If the stages are quick but the gap between them is
long, you are looking at planning, not execution.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;119. What is the default serializer, and should you change it?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.serializer&lt;/code&gt; defaults to
&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.serializer.JavaSerializer&lt;/code&gt;&lt;/strong&gt;.&lt;/p&gt;

  &lt;p&gt;Java serialization is general and slow, and produces large output. Kryo is
substantially more compact and faster:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.serializer&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;org.apache.spark.serializer.KryoSerializer &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.kryo.registrationRequired&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-scala highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nv&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;registerKryoClasses&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nc&quot;&gt;Array&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;classOf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;kt&quot;&gt;Trip&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;],&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;classOf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;kt&quot;&gt;City&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;]))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Registering your classes matters: unregistered classes are written with their
full class name per record, which throws away much of the benefit.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;registrationRequired=true&lt;/code&gt; turns a missed registration into an error rather
than silent bloat.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Where it does and does not help&lt;/strong&gt; is the important half:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Workload&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Effect of Kryo&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;RDDs of custom objects&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Large&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shuffle of RDD data&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Large&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Caching with a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;_SER&lt;/code&gt; level&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Large&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Broadcast variables&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Moderate&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;DataFrame and SQL operations&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Small&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;DataFrames mostly use &lt;strong&gt;Tungsten encoders&lt;/strong&gt;, which are generated per schema and
already compact, so they bypass &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.serializer&lt;/code&gt; for row data. That is why
“set Kryo” is standard advice for RDD-era code and much less impactful for
modern DataFrame pipelines.&lt;/p&gt;

  &lt;p&gt;So: set it if you have RDD-heavy code or serialized caching. If your job is
pure DataFrame, expect little and look elsewhere.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;120. What is Tungsten?&lt;/summary&gt;

  &lt;p&gt;Spark’s execution backend, aimed at making the JVM behave more like
hand-written memory-managing code. Three parts:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Binary row format.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UnsafeRow&lt;/code&gt; stores a row as a contiguous block of bytes with a fixed-size region for primitives and offsets into a variable-length region. Fields are read by offset arithmetic, with no objects constructed.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Explicit memory management.&lt;/strong&gt; Spark allocates and tracks memory itself, on or off heap, rather than relying on the garbage collector for data.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Whole-stage code generation.&lt;/strong&gt; Operators in a stage are fused into one generated Java method, so there is no per-row virtual call chain.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;Why it matters, concretely: a JVM object per row costs a header, pointer
indirection, and GC attention. A million-row partition becomes a million
objects. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;UnsafeRow&lt;/code&gt; makes it one byte array plus offsets.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;JVM object row   : header + pointers + boxed fields, GC-visible
UnsafeRow        : contiguous bytes, read by offset, GC-invisible
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;This is the technical answer to “why do DataFrames beat RDD code”.&lt;/strong&gt; It is not
that the DataFrame API is better written; it is that RDDs must materialise your
objects because Spark does not know their shape, while a DataFrame’s schema lets
Tungsten use a compact layout and generate code against it.&lt;/p&gt;

  &lt;p&gt;It also explains why a Python UDF is expensive: it forces rows out of that
binary format, across a process boundary, and back.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;121. How do you diagnose GC pressure?&lt;/summary&gt;

  &lt;p&gt;Read the &lt;strong&gt;GC Time&lt;/strong&gt; column in the executors tab of the UI, and compare it to
task time. A rough guide: consistently above 10% of task time is worth
investigating, and above 20% is a problem.&lt;/p&gt;

  &lt;p&gt;The causes, and the fix for each:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cause&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Many small objects, typically RDDs or Python UDFs&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Move to DataFrames; serialized storage levels&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Too much data per executor&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;More, smaller executors rather than few huge ones&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A very large heap&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Full collections take longer; consider several executors instead&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Deserialized caching&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK_SER&lt;/code&gt;, which stores far more compactly&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Long-lived cached data plus heavy execution&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Unpersist what is not reused&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;To see what the collector is actually doing, log it:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.executor.extraJavaOptions=-verbose:gc -XX:+PrintGCDetails -XX:+PrintGCTimeStamps&quot;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Two structural points worth making:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Off-heap storage removes data from the collector’s view entirely&lt;/strong&gt;, which is the real fix for GC-bound shuffle and aggregation work.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Very large heaps are not free.&lt;/strong&gt; A 64g executor can spend a long time in a full collection, which is one reason the usual advice is several medium executors rather than one enormous one.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;And distinguish GC pressure from spill: GC is CPU spent on memory management,
spill is disk I/O from insufficient memory. They often appear together, but the
remedies differ.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;122. What does the storage tab of the UI tell you?&lt;/summary&gt;

  &lt;p&gt;For every cached dataset: its name, storage level, the number of cached
partitions, the fraction held in memory, and the sizes in memory and on disk.&lt;/p&gt;

  &lt;p&gt;The single most useful number is the &lt;strong&gt;memory fraction&lt;/strong&gt;. What it tells you:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Fraction in memory&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Meaning&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;100%&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The cache is working as intended&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Partial, with a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK&lt;/code&gt; level&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The remainder is being read from disk each time&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Partial, with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_ONLY&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The remainder is being &lt;strong&gt;recomputed&lt;/strong&gt; each time&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Not listed at all&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Nothing was cached, usually because no action ran after &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache()&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;That third row is the trap: an RDD cached &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_ONLY&lt;/code&gt; that only half fits
silently recomputes half the partitions on every use, which looks like the cache
having no effect at all.&lt;/p&gt;

  &lt;p&gt;What to do with the information:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Partial residency&lt;/strong&gt; means either switch to a serialized level so more fits, accept &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MEMORY_AND_DISK&lt;/code&gt;, cache a smaller projection, or do not cache.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Several datasets listed&lt;/strong&gt; when you only meant to cache one is the “cache everything” anti-pattern, and they are competing for the same pool.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;An empty tab on a job you expected to use a cache&lt;/strong&gt; means the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache()&lt;/code&gt; call never populated.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;It is also the place to confirm an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;unpersist()&lt;/code&gt; actually released the blocks
rather than just dropping your reference.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;joins-catalyst-and-adaptive-execution&quot;&gt;Joins, Catalyst and adaptive execution&lt;/h2&gt;

&lt;p&gt;Join strategy is where most real tuning happens, and where an interviewer can tell in one answer whether you have read a physical plan before.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;123. What join strategies does Spark have?&lt;/summary&gt;

  &lt;p&gt;Five, and Catalyst picks one per join based on size estimates, join type and
hints.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Strategy&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;How it works&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Shuffle&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Good when&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Broadcast hash join&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Ship the small side to every executor, build a hash table locally&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;None&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One side is small&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Shuffle hash join&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shuffle both, build a hash table per partition from the smaller side&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Both sides&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One side much smaller, but not broadcastable&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Sort-merge join&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shuffle both, sort each partition, merge&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Both sides&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Both sides large&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Broadcast nested loop&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Broadcast one side, compare every pair&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One side&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Tiny input, or a non-equi condition&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Cartesian product&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Full cross product&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Both sides&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Explicit cross joins only&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;The selection order is roughly: can it broadcast, then can it shuffle-hash, then
sort-merge, and for non-equi conditions only the nested-loop options are
available at all.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;equi-join?&quot;] --&amp;gt;|&quot;no&quot;| N[&quot;BroadcastNestedLoop&amp;lt;br/&amp;gt;or Cartesian&quot;]
  A --&amp;gt;|&quot;yes&quot;| B{&quot;one side &amp;lt;&amp;lt;br/&amp;gt;autoBroadcastJoinThreshold?&quot;}
  B --&amp;gt;|&quot;yes&quot;| BH[&quot;BroadcastHashJoin&quot;]
  B --&amp;gt;|&quot;no&quot;| C{&quot;one side small enough&amp;lt;br/&amp;gt;to hash per partition?&quot;}
  C --&amp;gt;|&quot;yes&quot;| SH[&quot;ShuffleHashJoin&quot;]
  C --&amp;gt;|&quot;no&quot;| SM[&quot;SortMergeJoin&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The last two rows of the table are the ones to notice in a plan: seeing
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastNestedLoopJoin&lt;/code&gt; where you expected an equi-join
almost always means the join condition is not what you think.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;124. What is a broadcast hash join and when does Spark choose it?&lt;/summary&gt;

  &lt;p&gt;Spark sends the entire smaller side to every executor, where each builds a hash
table in memory. Each partition of the large side then joins locally, so the
large side is never shuffled.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  D[&quot;driver collects&amp;lt;br/&amp;gt;the small side&quot;] --&amp;gt; E1[&quot;executor 1&amp;lt;br/&amp;gt;hash table&quot;]
  D --&amp;gt; E2[&quot;executor 2&amp;lt;br/&amp;gt;hash table&quot;]
  D --&amp;gt; E3[&quot;executor 3&amp;lt;br/&amp;gt;hash table&quot;]
  L1[&quot;large partition&quot;] --&amp;gt; E1
  L2[&quot;large partition&quot;] --&amp;gt; E2
  L3[&quot;large partition&quot;] --&amp;gt; E3
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;Spark chooses it automatically when the estimated size of one side is below
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt;, default &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10485760&lt;/code&gt;&lt;/strong&gt;, which is 10 MB.
Set it to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-1&lt;/code&gt; to disable automatic broadcasting.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# 10485760
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Why it is the fastest option: no shuffle at all on the large side, which removes
the network transfer, the disk write and the stage barrier.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The costs, which a good answer names:&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;The driver assembles it first.&lt;/strong&gt; The small side is collected to the driver before being broadcast, so a too-large broadcast fails on the driver, not the executors.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Memory per executor.&lt;/strong&gt; Every executor holds a full copy, so 200 MB broadcast across 50 executors is 10 GB of cluster memory.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It is bounded by join type.&lt;/strong&gt; A full outer join cannot be broadcast, and for a left outer join only the right side can be.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;125. How do you force or prevent a broadcast?&lt;/summary&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql.functions&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;joined&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;cm&quot;&gt;/*+ BROADCAST(c) */&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;JOIN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;c&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ON&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The available hints, and what each forces:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Hint&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Effect&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Broadcast that side, regardless of the estimate&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Force a sort-merge join&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SHUFFLE_HASH&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Force a shuffle hash join&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SHUFFLE_REPLICATE_NL&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Force a replicated nested loop&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;To prevent broadcasting globally:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;-1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;When forcing is legitimate:&lt;/strong&gt; Spark’s estimate is wrong, usually because
statistics are missing or the side is derived through several transformations,
and you know it is small.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;When it backfires:&lt;/strong&gt; hinting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt; on something genuinely large. The
driver must collect it first, so you move the failure from a slow join to a
driver &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;, which is harder to diagnose. A hint overrides the
safety check, so it is a claim you are making about the data.&lt;/p&gt;

  &lt;p&gt;A better habit than hinting is often to fix the estimate: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ANALYZE TABLE ...
COMPUTE STATISTICS&lt;/code&gt;, or filter and project the small side first so it is
genuinely below the threshold. And with AQE enabled, a runtime switch to a
broadcast often happens without a hint at all.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;126. What is `spark.sql.broadcastTimeout`?&lt;/summary&gt;

  &lt;p&gt;How long Spark will wait for a broadcast to be built and distributed before
failing the query. The default is &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;300&lt;/code&gt;&lt;/strong&gt; seconds.&lt;/p&gt;

  &lt;p&gt;The error is explicit about the config, which is a clue people sometimes miss:
it tells you to either raise the timeout or disable broadcast joins.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.broadcastTimeout&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# 300
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;Why raising it is usually the wrong move.&lt;/strong&gt; Hitting a five-minute timeout to
build what should be a small hash table means the side is not small. The
sequence of events is: Spark estimated it as broadcastable, began collecting it
to the driver, and that collection is taking minutes.&lt;/p&gt;

  &lt;p&gt;So the diagnosis order:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Check the actual size&lt;/strong&gt; of the side being broadcast. If it is hundreds of megabytes, the estimate was wrong.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check whether a hint forced it.&lt;/strong&gt; A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt; hint overrides the size check entirely.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check for a missing filter.&lt;/strong&gt; A side that should be small after filtering is being broadcast before the filter, or the filter cannot push down.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Then consider the timeout,&lt;/strong&gt; legitimately, if the table is genuinely small but the cluster is slow or contended.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The alternative to raising it is to remove the broadcast and let the join
shuffle, which is slower per-query but predictable:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.autoBroadcastJoinThreshold&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;-1&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;127. Why is sort-merge join the default for large joins?&lt;/summary&gt;

  &lt;p&gt;Because its memory requirement does not scale with the size of either side.&lt;/p&gt;

  &lt;p&gt;A hash join must hold one side’s hash table in memory per partition. A
sort-merge join sorts both sides, then walks them together with two cursors, so
it only needs buffers for the merge, not the whole partition.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;left  (sorted):  a a b c d ...
right (sorted):  a b b d e ...
                 ^ ^            advance the smaller cursor, emit matches
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;And the sort can spill to disk, so a partition larger than memory still
completes rather than failing.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.join.preferSortMergeJoin&lt;/code&gt; defaults to &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/strong&gt;, which biases the
planner toward it over shuffle hash join.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Sort-merge&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Shuffle hash&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Memory&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Merge buffers, spillable&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A whole hash table per partition&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Extra work&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Sorting both sides&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;None&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Fails on a huge partition&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No, spills&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Possibly&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;So the trade is CPU for robustness: you pay the sort in exchange for bounded
memory. For two large tables that is the right trade, which is why it is the
default.&lt;/p&gt;

  &lt;p&gt;A useful corollary: if both sides are already sorted on the join key, for
example from bucketing with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sortBy&lt;/code&gt;, the sort is skipped and sort-merge becomes
very cheap.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;128. What is a shuffle hash join, and when is it better than sort-merge?&lt;/summary&gt;

  &lt;p&gt;Both sides are shuffled on the join key, then for each partition Spark builds a
hash table from the &lt;strong&gt;smaller&lt;/strong&gt; side and probes it with the larger.&lt;/p&gt;

  &lt;p&gt;It beats sort-merge when one side is small enough to hash per partition but too
large to broadcast, because it skips the sort entirely.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;sort-merge   : shuffle both, sort both, merge
shuffle hash : shuffle both, hash the small side, probe
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The conditions Spark requires before choosing it:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;The join is an equi-join.&lt;/li&gt;
    &lt;li&gt;One side is estimated small enough that a per-partition hash table fits, judged against &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt; multiplied by the number of shuffle partitions.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.join.preferSortMergeJoin&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;, or the cost model prefers it.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;preferSortMergeJoin&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;, Spark picks it &lt;strong&gt;less often
than you might expect&lt;/strong&gt;, and the usual reason is exactly that conservatism:
sort-merge cannot run out of memory, and shuffle hash can.&lt;/p&gt;

  &lt;p&gt;To force it when you know the shape:&lt;/p&gt;

  &lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;cm&quot;&gt;/*+ SHUFFLE_HASH(c) */&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;JOIN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;c&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ON&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The honest summary for an interview: it occupies a narrow band between broadcast
and sort-merge, and in modern Spark, AQE converting a planned sort-merge into a
broadcast at runtime covers much of that band more safely.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;129. What is the difference between a bucketed join and a broadcast join?&lt;/summary&gt;

  &lt;p&gt;Both avoid a shuffle, for entirely different reasons and at different scales.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Broadcast join&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Bucketed join&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Avoids the shuffle by&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Copying the small side everywhere&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Pre-arranging both sides on disk&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Size limit&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One side must be small&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;None&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Set up&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Nothing, automatic&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Write both tables bucketed, in advance&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Needs a metastore&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, so &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;saveAsTable&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Cost&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Memory per executor&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A one-off write, plus rigidity&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;A broadcast join is a &lt;strong&gt;runtime&lt;/strong&gt; decision about a small side. A bucketed join
is a &lt;strong&gt;storage layout&lt;/strong&gt; decision that pays off on every future join.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;bucketBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;64&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sortBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;saveAsTable&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;lakehouse.trips_b&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;riders&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;bucketBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;64&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sortBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;saveAsTable&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;lakehouse.riders_b&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;table&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;lakehouse.trips_b&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;table&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;lakehouse.riders_b&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The conditions are strict, and naming them is the good answer:&lt;/strong&gt; both sides
bucketed, on the &lt;strong&gt;same column&lt;/strong&gt;, with the &lt;strong&gt;same number of buckets&lt;/strong&gt;. A mismatch
in bucket count means Spark shuffles anyway, silently, and you have paid the
write cost for nothing.&lt;/p&gt;

  &lt;p&gt;So bucketing is the only shuffle-free route for large-to-large joins, and its
drawback is inflexibility: the bucket count is baked in, and a join on a
different key gets no benefit.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;130. Why might a broadcast join not happen even though the table is small?&lt;/summary&gt;

  &lt;p&gt;Because Spark broadcasts on an &lt;strong&gt;estimate&lt;/strong&gt;, and the estimate can be wrong or
unavailable.&lt;/p&gt;

  &lt;p&gt;The reasons, most common first:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;No statistics.&lt;/strong&gt; For a file-based table Spark falls back to the total file size, which can be far larger than the in-memory size after filtering, or unavailable for some sources.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The side is derived.&lt;/strong&gt; After a filter and a join, the estimate is a computed guess. Spark multiplies selectivity factors, and those compound badly.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The filter cannot push down,&lt;/strong&gt; so the estimate is of the unfiltered table.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Compression.&lt;/strong&gt; A 50 MB Parquet file may be 500 MB in memory, and Spark’s estimate accounts for that, so a “small” file can exceed the threshold.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The join type forbids it.&lt;/strong&gt; A full outer join cannot be broadcast, and for outer joins only the nullable side can be.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The fixes, in order of preference:&lt;/p&gt;

  &lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ANALYZE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;lakehouse&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;COMPUTE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;STATISTICS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FOR&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ALL&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;COLUMNS&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;cities_small&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;active&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;name&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;broadcast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities_small&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;And the modern answer:&lt;/strong&gt; adaptive query execution largely solves this. Because
AQE re-plans after the shuffle stage completes, it knows the &lt;em&gt;actual&lt;/em&gt; size and
can convert a planned sort-merge join into a broadcast. So on Spark 3.2 and
later, a missing broadcast at plan time often becomes a broadcast at runtime,
which is why the SQL tab shows a different plan than &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; did.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;131. What is cost-based optimisation and is it on?&lt;/summary&gt;

  &lt;p&gt;Cost-based optimisation uses table and column statistics to make planning
decisions that rule-based optimisation cannot: which join order is cheapest,
and which physical strategy to pick.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.cbo.enabled&lt;/code&gt; defaults to &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;false&lt;/code&gt;&lt;/strong&gt;.&lt;/p&gt;

  &lt;p&gt;Even when enabled it needs statistics to exist:&lt;/p&gt;

  &lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;ANALYZE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;TABLE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;lakehouse&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;COMPUTE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;STATISTICS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FOR&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ALL&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;COLUMNS&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.cbo.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;true&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.cbo.joinReorder.enabled&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;true&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;What it can do that rules cannot: reorder a multi-way join so the most selective
join happens first, which changes the size of every intermediate result.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Why it is off by default, and why that is defensible:&lt;/strong&gt; CBO depends on
statistics being present, current and representative. In a lakehouse where
tables are written continuously, statistics go stale quickly, and a confidently
wrong estimate is worse than no estimate. Collecting column statistics on a
large table is also itself an expensive scan.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;And adaptive execution has taken over much of its job&lt;/strong&gt; using real runtime
numbers rather than stored estimates, which do not go stale. So the modern
answer to “should I enable CBO” is usually: make sure AQE is on, and reach for
CBO only for complex multi-way joins over stable tables where join order is the
problem.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;132. What does AQE do about join strategy?&lt;/summary&gt;

  &lt;p&gt;After a shuffle stage completes, AQE knows the &lt;strong&gt;actual&lt;/strong&gt; size of each side, so
it can replace the planned strategy with a better one. The main conversion is
demoting a sort-merge join to a broadcast hash join.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  A[&quot;plan time:&amp;lt;br/&amp;gt;estimate says 800 MB&amp;lt;br/&amp;gt;-&amp;gt; SortMergeJoin&quot;] --&amp;gt; B[&quot;stage runs&quot;]
  B --&amp;gt; C[&quot;actual: 6 MB&quot;]
  C --&amp;gt; D[&quot;re-plan:&amp;lt;br/&amp;gt;BroadcastHashJoin&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;This is the single most valuable thing AQE does, and the reason is structural:
planning-time estimates on derived data are usually wrong. Spark can estimate a
base table it has statistics for, but after two filters and a join the number is
a product of guessed selectivities. After the shuffle, it is a measurement.&lt;/p&gt;

  &lt;p&gt;The relevant setting is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.autoBroadcastJoinThreshold&lt;/code&gt;, which
if unset falls back to the static &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;Two consequences worth knowing:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; lies about the final plan.&lt;/strong&gt; Before execution the tree is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;AdaptiveSparkPlan isFinalPlan=false&lt;/code&gt;, so what you read is the planned shape. The SQL tab after the query shows what ran.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;There must be a shuffle&lt;/strong&gt; for AQE to act at. A query with no exchange gets no adaptive help, and the first stage is always planned blind.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;A related optimisation follows automatically: once the join becomes a
broadcast, the already-written shuffle can be read locally rather than over the
network.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;133. What is a local shuffle reader?&lt;/summary&gt;

  &lt;p&gt;An AQE optimisation that avoids network traffic when a join strategy changes at
runtime.&lt;/p&gt;

  &lt;p&gt;The situation: Spark planned a sort-merge join, so both sides were shuffled.
Then AQE discovered one side is small and converted the join to a broadcast. The
shuffle files already exist, and with a broadcast join each task no longer needs
a &lt;em&gt;specific&lt;/em&gt; partition from every map output; it just needs the rows. So instead
of fetching them over the network in the shuffle pattern, each executor reads
the shuffle blocks it wrote &lt;strong&gt;locally&lt;/strong&gt;.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.localShuffleReader.enabled&lt;/code&gt; defaults to &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;&lt;/strong&gt;.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;without: every reduce task fetches from every map output    (network)
with   : each executor reads its own local shuffle blocks   (local disk)
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;In the plan it appears as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LocalShuffleReader&lt;/code&gt; above the exchange.&lt;/p&gt;

  &lt;p&gt;The reason to know it exists is mainly for reading plans: seeing
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LocalShuffleReader&lt;/code&gt; tells you AQE changed a join strategy after the shuffle had
already been written, which is a useful signal that your plan-time estimates were
wrong. That in turn tells you statistics might be worth collecting, or that a
filter should be pushed earlier so the plan is right the first time.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;134. How do you read whether AQE actually changed the plan?&lt;/summary&gt;

  &lt;p&gt;Compare the planned tree with what actually ran, because they are different
objects.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Before execution&lt;/strong&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; shows the planned shape wrapped in an adaptive
node:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;AdaptiveSparkPlan isFinalPlan=false
+- SortMergeJoin [city_id#21], [city_id#40], Inner
   :- Sort [city_id#21 ASC], false, 0
   :  +- Exchange hashpartitioning(city_id#21, 200)
   ...
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;After execution&lt;/strong&gt;, the same query’s entry in the &lt;strong&gt;SQL tab&lt;/strong&gt; of the UI shows
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=true&lt;/code&gt; and the strategies AQE settled on. You can also re-run
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; on an already-executed DataFrame and get the final plan.&lt;/p&gt;

  &lt;p&gt;What to look for as evidence AQE acted:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Sign&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Meaning&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isFinalPlan=true&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;You are looking at what ran&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastHashJoin&lt;/code&gt; where the plan said &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoin&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A runtime strategy switch&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;LocalShuffleReader&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Follows such a switch&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CustomShuffleReader&lt;/code&gt; or coalesced partition counts&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Partition coalescing happened&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A stage with far fewer tasks than &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Coalescing again&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;The practical habit:&lt;/strong&gt; never conclude anything about a modern Spark query from
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;explain()&lt;/code&gt; alone. It tells you the starting point. The SQL tab tells you the
result, and when the two differ, the difference is exactly what AQE contributed.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;135. What is the difference between a semi join and an anti join?&lt;/summary&gt;

  &lt;p&gt;Both filter the left side by whether a match exists on the right, and neither
returns any right-side columns.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left_semi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;# rows WITH a match
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left_anti&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;# rows WITHOUT a match
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Left semi&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Left anti&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Returns&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Left rows that match&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Left rows that do not match&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Columns&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Left only&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Left only&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Duplicates&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;One left row per left row, regardless of how many right matches&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Same&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;SQL equivalent&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE EXISTS&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;IN&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WHERE NOT EXISTS&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;Why they are better than the alternatives&lt;/strong&gt;, which is the point of the
question:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Against an inner join plus &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt;:&lt;/strong&gt; a semi join stops at the first match, so a left row matching 100 right rows is emitted once. An inner join emits it 100 times, and then you have to deduplicate.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Against a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;left_outer&lt;/code&gt; plus &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;isNull&lt;/code&gt; filter&lt;/strong&gt; for the anti case: the outer join carries all the right-side columns through the shuffle before you discard them.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;So semi and anti joins are both &lt;strong&gt;correct&lt;/strong&gt; where the alternatives need a
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt;, and &lt;strong&gt;cheaper&lt;/strong&gt;, because less data crosses the shuffle.&lt;/p&gt;

  &lt;p&gt;The null behaviour is worth knowing: for a left anti join, a null key on the
left never matches, so those rows are &lt;strong&gt;returned&lt;/strong&gt;, which is usually what you
want but occasionally surprising.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;136. How do you handle a join key with nulls?&lt;/summary&gt;

  &lt;p&gt;Null never equals null in SQL semantics, so a null key matches nothing. Three
consequences and their handling:&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Inner joins silently drop null keys.&lt;/strong&gt; If the null means “not applicable”,
that is correct. If it means “unknown but should match”, you must say so:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;eqNullSafe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# null == null is true here
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;2. Null keys are a skew source.&lt;/strong&gt; Every null hashes to the same partition, so
a column that is 30% null puts 30% of the rows in one task. If they carry no
meaning, drop them before the join:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;isNotNull&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Or replace them with a distributed sentinel, which is salting for nulls:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;join_key&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;coalesce&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;concat&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;__null_&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rand&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;16&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;int&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;3. Outer joins produce nulls too,&lt;/strong&gt; so after a left outer join you cannot
distinguish “matched with a null value” from “did not match” by looking at the
column. Add a marker before joining if you need to tell them apart:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;b_marked&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;_matched&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;True&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;b_marked&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&quot;had_match&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;coalesce&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;_matched&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lit&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;False&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The habit:&lt;/strong&gt; check the null count on every join key before writing the join.
It answers both the correctness and the skew question at once.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;137. What causes a Cartesian product by accident?&lt;/summary&gt;

  &lt;p&gt;A join condition Spark cannot treat as an equality, so it has no key to
partition on and must compare every pair.&lt;/p&gt;

  &lt;p&gt;The usual causes:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;A non-equi condition.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ON a.ts BETWEEN b.start AND b.end&lt;/code&gt; has no equality, so there is nothing to hash.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The condition moved into a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;where&lt;/code&gt; after a cross join.&lt;/strong&gt; Semantically equivalent SQL, but if Spark cannot push it into the join it evaluates the product first.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A join on an expression&lt;/strong&gt; that Catalyst cannot match to a key, for example &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ON cast(a.id AS string) = b.id&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A missing condition entirely,&lt;/strong&gt; such as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;a.join(b)&lt;/code&gt; with no key, or a multi-column join where one predicate was forgotten.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;In the plan it shows as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastNestedLoopJoin&lt;/code&gt;:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;CartesianProduct
:- FileScan parquet [...]
+- FileScan parquet [...]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;How to catch it:&lt;/strong&gt; read the plan whenever a join is unexpectedly slow. A
Cartesian on two 1-million-row tables is a trillion comparisons, so the symptom
is a stage that never finishes rather than one that is merely slow.&lt;/p&gt;

  &lt;p&gt;There is a guard: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.crossJoin.enabled&lt;/code&gt; historically had to be set for
implicit cross joins, forcing you to be explicit with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;crossJoin()&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;For a legitimate range join,&lt;/strong&gt; the usual technique is to add an equality that
buckets both sides, for example joining on a truncated date as well as the range
condition, so Spark has a key to partition on and the nested-loop comparison
happens only within each bucket.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;138. What is dynamic partition pruning?&lt;/summary&gt;

  &lt;p&gt;Using the result of a filter on one side of a join to prune partitions of the
other side &lt;strong&gt;at runtime&lt;/strong&gt;, rather than scanning all of them.&lt;/p&gt;

  &lt;p&gt;The classic star-schema shape: a large partitioned fact table, joined to a small
filtered dimension table.&lt;/p&gt;

  &lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;*&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;JOIN&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;cities&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;c&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;ON&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;city_id&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;region&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;west&apos;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Static partition pruning cannot help, because the filter is on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;c.region&lt;/code&gt; while
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trips&lt;/code&gt; is partitioned by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_id&lt;/code&gt;. Without DPP, Spark scans every partition of
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trips&lt;/code&gt; and discards most of them after the join.&lt;/p&gt;

  &lt;p&gt;With DPP, Spark runs the dimension side first, collects the matching &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;city_id&lt;/code&gt;
values, and injects them as a partition filter on the fact scan.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  A[&quot;filter cities&amp;lt;br/&amp;gt;region = &apos;west&apos;&quot;] --&amp;gt; B[&quot;city_ids: sf, la, sea&quot;]
  B --&amp;gt; C[&quot;inject as a partition filter&quot;]
  C --&amp;gt; D[&quot;scan only those&amp;lt;br/&amp;gt;trips partitions&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The conditions: the fact table must be &lt;strong&gt;partitioned on the join key&lt;/strong&gt;, the join
must be an equi-join, and the dimension side must be filtered and broadcastable.&lt;/p&gt;

  &lt;p&gt;In the plan it appears as a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dynamicpruningexpression&lt;/code&gt; in the fact-side
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PartitionFilters&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;The payoff is often the largest single win available on a star schema, because
it changes how much data is read rather than how efficiently it is processed.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;139. How do you join a large table to a large table efficiently?&lt;/summary&gt;

  &lt;p&gt;Broadcasting is off the table, so the work is in reducing what enters the
shuffle and making the shuffle well-shaped.&lt;/p&gt;

  &lt;p&gt;In order:&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Reduce both sides first.&lt;/strong&gt; Filter and project before joining. This is the
largest lever and the most often skipped:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;t&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-01&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;fare_amount&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;r&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;riders&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;active&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;segment&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Sometimes this alone makes one side broadcastable, which changes the problem
entirely.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. Check whether one side can become small.&lt;/strong&gt; An aggregation before the join
often shrinks it by orders of magnitude.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;3. Bucket both tables&lt;/strong&gt; on the join key with the same bucket count, if this
join is a recurring pattern. That removes the shuffle permanently.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;4. Size the shuffle.&lt;/strong&gt; Aim for partitions in the low hundreds of megabytes,
and set &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.advisoryPartitionSizeInBytes&lt;/code&gt; rather than guessing a
count.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;5. Handle skew&lt;/strong&gt; on the join key, since a large-to-large join is where skew
hurts most. AQE skew handling first, then salting.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;6. Avoid re-shuffling.&lt;/strong&gt; If you then aggregate on the same key, one exchange
serves both. If you aggregate on a different key, you pay a second shuffle, so
consider whether the order can change.&lt;/p&gt;

  &lt;p&gt;The one thing &lt;strong&gt;not&lt;/strong&gt; to do is hint &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt; and hope.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;140. What is the `Exchange` node in a plan?&lt;/summary&gt;

  &lt;p&gt;A shuffle. Every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt; in a physical plan is one redistribution of data,
and therefore one stage boundary.&lt;/p&gt;

  &lt;p&gt;Its description carries the useful detail:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;Exchange hashpartitioning(city_id#21, 200), ENSURE_REQUIREMENTS, [plan_id=141]
Exchange rangepartitioning(started_at#30 ASC NULLS FIRST, 200), ...
Exchange SinglePartition, ...
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Partitioning&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Means&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hashpartitioning(cols, n)&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Hash on those columns into n partitions. The usual case&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rangepartitioning(cols, n)&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Range partitioning, from a global sort&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SinglePartition&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Everything into one partition. A red flag unless intended&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ENSURE_REQUIREMENTS&lt;/code&gt; tag means the exchange was inserted because an
operator above it &lt;em&gt;required&lt;/em&gt; that partitioning, which is the normal reason.&lt;/p&gt;

  &lt;p&gt;How to use it:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Count them&lt;/strong&gt; to count your shuffles, and therefore your stages.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Read the key&lt;/strong&gt; to see whether consecutive operations shuffle on the same column, in which case one exchange can serve both.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Watch for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SinglePartition&lt;/code&gt;&lt;/strong&gt;, which usually comes from a global &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;orderBy&lt;/code&gt;, a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(1)&lt;/code&gt;, or a window with no &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionBy&lt;/code&gt;. That last one is a common accidental performance disaster: a window function without &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionBy&lt;/code&gt; sends the entire dataset through one task.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;141. What is `ReusedExchange`?&lt;/summary&gt;

  &lt;p&gt;A shuffle whose output is read by more than one branch of the plan, rather than
being computed twice.&lt;/p&gt;

  &lt;p&gt;It appears when the same subtree occurs in two places, for example in a
self-join, a union of two aggregations over the same base, or a query
referencing the same CTE twice.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;:- Exchange hashpartitioning(city_id#21, 200)
:  +- HashAggregate(...)
+- ReusedExchange [city_id#40], Exchange hashpartitioning(city_id#21, 200)
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Seeing it is &lt;strong&gt;good news&lt;/strong&gt;: Catalyst recognised the common subtree and is
reusing the shuffle files rather than recomputing the stage. In the UI this is
also what produces “skipped stages”, shown in grey, which are not a problem but
a saving.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The interesting case is its absence.&lt;/strong&gt; If you expected reuse and do not see
it, the two branches differ in some way you did not intend:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;A different filter, even a logically equivalent one written differently.&lt;/li&gt;
    &lt;li&gt;A different column projection, so the plans are not identical.&lt;/li&gt;
    &lt;li&gt;A non-deterministic expression such as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rand()&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;current_timestamp()&lt;/code&gt;, which makes the subtrees non-equivalent by definition.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;When reuse does not happen and you want it, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;cache()&lt;/code&gt; on the shared base is the
explicit way to force one computation, at the cost of storage memory. Checking
for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReusedExchange&lt;/code&gt; first tells you whether the cache is even necessary.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;142. How do you reduce the number of shuffles in a query?&lt;/summary&gt;

  &lt;p&gt;Six techniques, roughly in order of payoff.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Aggregate and join on the same key.&lt;/strong&gt; If both operations need the same
partitioning, one exchange satisfies both. Reordering a pipeline so the keys line
up can halve the shuffles.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. Repartition once, deliberately.&lt;/strong&gt; If several operators key on the same
column, one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(col)&lt;/code&gt; up front means each of them finds the
partitioning already satisfied.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;3. Replace &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt; plus &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;join&lt;/code&gt; with a semi join.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;distinct&lt;/code&gt; is a shuffle;
a semi join achieves the same filtering in the join’s own shuffle.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;trips&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;active_riders&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left_semi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# not distinct + inner join
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;4. Broadcast what can be broadcast.&lt;/strong&gt; A broadcast join has no exchange at all,
so reducing a dimension until it fits under the threshold removes a shuffle
outright.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;5. Avoid unnecessary global ordering.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;orderBy&lt;/code&gt; costs a range-partitioning
shuffle plus a sampling job. If you only need order within files, use
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;sortWithinPartitions&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;6. Bucket recurring join keys,&lt;/strong&gt; so the shuffle is paid once at write time
rather than on every read.&lt;/p&gt;

  &lt;p&gt;Then verify, because this is all measurable:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;formatted&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# count the Exchange nodes before and after
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The mindset worth conveying:&lt;/strong&gt; do not think of shuffles as something to tune,
think of them as something to &lt;em&gt;count&lt;/em&gt;, and then ask whether each one is earning
its place.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;performance-tuning-and-troubleshooting&quot;&gt;Performance tuning and troubleshooting&lt;/h2&gt;

&lt;p&gt;What an interviewer really wants to know: can you find the bottleneck, or do you guess at knobs.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;143. How do you approach a slow Spark job?&lt;/summary&gt;

  &lt;p&gt;Find the bottleneck before changing anything. The order matters more than any
individual technique.&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Open the SQL tab&lt;/strong&gt; and find the slowest operator. The per-node timing tells you where the work is, which is often not where you assumed.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Open the stage page for that stage&lt;/strong&gt; and read the task duration distribution: min, median, max.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Classify it.&lt;/strong&gt; Almost every slow stage is one of four things:&lt;/li&gt;
  &lt;/ol&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Signal&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Diagnosis&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max task time » median, with larger shuffle read&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Skew&lt;/strong&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Large shuffle read and write across all tasks&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Too much shuffle&lt;/strong&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Spill reported&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Partitions too large&lt;/strong&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Thousands of tasks, each milliseconds long&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Too many partitions&lt;/strong&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Check the plan for structural mistakes&lt;/strong&gt; before tuning: a missing broadcast, an accidental Cartesian, a filter that did not push down, columns you did not need.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Change one thing,&lt;/strong&gt; re-run, and compare the same stage.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;The answer that impresses is the negative one:&lt;/strong&gt; say what you would &lt;em&gt;not&lt;/em&gt; do
first. You would not raise executor memory, not enable speculation, and not
change &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; until you knew which of the four it was.
Tuning without a diagnosis is how jobs acquire a page of configs that nobody can
justify.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;144. What does the Spark UI tell you, tab by tab?&lt;/summary&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Tab&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What it answers&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Jobs&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Which action is slow, how many stages, which stages were skipped&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Stages&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Task duration and shuffle distribution, spill, the DAG for that stage&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;SQL&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The final physical plan and per-operator row counts and time&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Storage&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;What is cached, and what fraction is actually in memory&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Executors&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Per-executor task counts, GC time, failures, and loss reasons&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Environment&lt;/strong&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The configs actually in effect&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;How to use them together, which is the real skill:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Start in SQL&lt;/strong&gt; for a DataFrame job, because it maps operators to time. Starting in Stages tells you &lt;em&gt;where&lt;/em&gt; but not &lt;em&gt;what&lt;/em&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Stages gives you the distribution.&lt;/strong&gt; The summary metrics table with percentiles is the single most valuable view in the UI, because it separates skew from uniform slowness.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Executors answers “why did it fail”.&lt;/strong&gt; Loss reasons live here: killed for memory, heartbeat timeout, or exited.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Environment settles arguments.&lt;/strong&gt; If someone insists a config is set, this is the authoritative answer.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Storage is where caches are exposed as a cost&lt;/strong&gt; rather than assumed to be a benefit.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;And remember the UI dies with the application, which is why event logging plus a
history server is close to mandatory: most investigations happen after the
job.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;145. A few tasks take far longer than the median. What is it?&lt;/summary&gt;

  &lt;p&gt;Skew, in almost every case. Confirm it and then fix the distribution.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Confirm:&lt;/strong&gt; on the stage page, compare the max and median for both duration and
&lt;strong&gt;shuffle read size&lt;/strong&gt;. The second column is what distinguishes the two possible
causes:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Duration&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Shuffle read&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cause&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Skew: the task has more data&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Roughly even&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Environment: slow node, GC, bad disk&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;If it is the second, look at the executors tab for GC time and at which host the
slow tasks ran on; a single bad machine shows up as all slow tasks on one
executor.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;If it is skew, fix it in this order:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;Check whether AQE skew handling &lt;em&gt;could&lt;/em&gt; fire. It needs a partition over 5x the median &lt;strong&gt;and&lt;/strong&gt; over 256 MB, so on a smaller job it silently never triggers.&lt;/li&gt;
    &lt;li&gt;Broadcast the small side if there is one.&lt;/li&gt;
    &lt;li&gt;Salt the hot key.&lt;/li&gt;
    &lt;li&gt;Split the hot keys out and union.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;And do not enable speculation.&lt;/strong&gt; The duplicate task has the same rows and is
equally slow, so it consumes a second slot for no benefit and masks the real
cause by making it look like flaky infrastructure.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;146. The stage shows large spill. What do you change?&lt;/summary&gt;

  &lt;p&gt;Spill means the per-task working set exceeded the execution memory that task
could get. So reduce the working set, or increase the share.&lt;/p&gt;

  &lt;p&gt;The arithmetic that frames the fix:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;per-task execution memory ~= unified pool / concurrent tasks per executor
                          ~= (heap - 300MB) x 0.6 / spark.executor.cores
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Changes in order of preference:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;More partitions,&lt;/strong&gt; so each task handles less. This is usually the right answer, costs nothing, and scales.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;64MB&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2000&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Check for skew.&lt;/strong&gt; One oversized partition spills while its peers do not, and no amount of memory fixes a distribution problem.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Fewer cores per executor,&lt;/strong&gt; which gives each running task a larger slice of the same pool. Going from 5 cores to 3 raises per-task memory by two thirds.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;More executor memory,&lt;/strong&gt; last, because it treats the symptom and costs cluster capacity.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;Context worth adding:&lt;/strong&gt; some spill is normal and by design. Spark spills
rather than failing, which is a feature. The question is proportion: spill of a
few percent of shuffle size is fine, spill several times the shuffle size means
you are paying disk I/O for work that should have been in memory.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;147. Planning takes longer than execution. What is wrong?&lt;/summary&gt;

  &lt;p&gt;The job is spending its time on the driver before tasks start. Three usual
causes:&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Too many input files.&lt;/strong&gt; Planning must list them and compute splits, which on
object storage is many API calls. Tens of thousands of small files can take
minutes before a single task runs.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;read&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;inputFiles&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;__len__&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;2. A deep plan.&lt;/strong&gt; Hundreds of chained &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; calls, or a loop that
rebuilds a DataFrame, produce a very deep tree that Catalyst walks repeatedly.
Collapse them into one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt;, or checkpoint to truncate.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;3. Very wide schemas.&lt;/strong&gt; Thousands of columns, or deeply nested structs, make
every analysis and optimisation pass expensive.&lt;/p&gt;

  &lt;p&gt;How to tell it apart from execution slowness: in the &lt;strong&gt;Jobs&lt;/strong&gt; tab, look at the
gap between the job being submitted and its first stage starting. In the SQL
tab, a long total duration with short stage durations points the same way.&lt;/p&gt;

  &lt;p&gt;The extreme version is a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;StackOverflowError&lt;/code&gt; from the recursive tree walk,
which is the same problem at a larger scale.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The related structural fix&lt;/strong&gt; for the file-count case is a lakehouse format,
because Hudi, Iceberg and Delta keep file metadata in their own manifests rather
than requiring a directory listing, which is one of the main reasons they exist.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;148. There are thousands of tasks each lasting milliseconds. What is wrong?&lt;/summary&gt;

  &lt;p&gt;Too many partitions, so per-task overhead dominates the actual work.&lt;/p&gt;

  &lt;p&gt;Each task carries real fixed costs: serialization of the task description,
scheduling on the driver, JVM thread setup, reading a partition’s metadata, and
reporting results. At a few milliseconds of work per task, those costs are the
job.&lt;/p&gt;

  &lt;p&gt;It also loads the driver, which must track every task’s state and metrics. A job
with hundreds of thousands of tasks can exhaust driver memory on bookkeeping
alone.&lt;/p&gt;

  &lt;p&gt;The fixes depend on where the partitions came from:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Source&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A shuffle&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Lower &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;, or let AQE coalesce&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Many small input files&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Compact them, or raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.maxPartitionBytes&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;An explicit &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(n)&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Lower n&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Before a write&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;coalesce(n)&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;With AQE on, coalescing should already be merging small shuffle partitions
toward &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.advisoryPartitionSizeInBytes&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;64MB&lt;/code&gt;. So if
you are seeing thousands of tiny tasks &lt;em&gt;after&lt;/em&gt; a shuffle, check that AQE is
enabled and that coalescing is not disabled.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The target to state:&lt;/strong&gt; partitions in the low hundreds of megabytes, and a
total that is a small multiple of your core count. Tasks lasting well under a
second are a signal you have overshot.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;149. How do you size executors?&lt;/summary&gt;

  &lt;p&gt;Favour several medium executors over a few enormous ones or many tiny ones.&lt;/p&gt;

  &lt;p&gt;A common starting point is &lt;strong&gt;4 to 5 cores per executor&lt;/strong&gt;, and there are two
reasons:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;HDFS client contention.&lt;/strong&gt; Concurrent tasks in one JVM share the same client, and throughput stops improving past roughly that point.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Garbage collection.&lt;/strong&gt; A very large heap makes full collections slow, and a GC pause stalls every task on that executor.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;Then memory follows from partition size:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;per-task execution memory ~= (heap - 300MB) x 0.6 / cores
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Size the heap so that is comfortably larger than your largest partition’s
working set, and add &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverhead&lt;/code&gt; for off-heap, Python
workers and native libraries.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--executor-cores&lt;/span&gt; 4 &lt;span class=&quot;nt&quot;&gt;--executor-memory&lt;/span&gt; 16g &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.executor.memoryOverhead&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;3g
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The failure modes at each extreme:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Shape&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Problem&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Few huge executors (e.g. 32 cores, 200g)&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Slow full GC, HDFS contention, coarse scheduling granularity&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Many tiny executors (1 core, 2g)&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No memory sharing between tasks, more overhead copies, more broadcast copies&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;And state the caveat:&lt;/strong&gt; these are starting points, not laws. The right answer
depends on your partition sizes and workload, and the honest thing to say is
that you would start here, measure spill and GC, and adjust.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;150. How do you decide `--num-executors` versus dynamic allocation?&lt;/summary&gt;

  &lt;p&gt;Pick one. Setting both is contradictory, and Spark resolves it by treating the
static number as merely the &lt;em&gt;initial&lt;/em&gt; size.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Static sizing&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Dynamic allocation&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Config&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--num-executors&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.dynamicAllocation.enabled=true&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Suits&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Predictable batch jobs&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Bursty, interactive, multi-tenant&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Cost model&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Reserves capacity for the whole run&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Releases idle capacity&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Startup&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;All at once&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Ramps up&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Needs&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Nothing&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shuffle tracking or an external shuffle service&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;Use static sizing when&lt;/strong&gt; the job’s parallelism is roughly constant, you know
the shape from experience, and predictable performance matters more than cluster
efficiency. A nightly batch job is the archetype.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Use dynamic allocation when&lt;/strong&gt; parallelism varies a lot between stages, the
application is long-lived and often idle (a notebook, a Thrift server), or the
cluster is shared and holding idle executors is antisocial.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.dynamicAllocation.enabled&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.dynamicAllocation.minExecutors&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;2 &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.dynamicAllocation.maxExecutors&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;200
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The requirement to name:&lt;/strong&gt; an idle executor may still hold shuffle blocks a
later stage needs. So dynamic allocation needs either an external shuffle
service, or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.dynamicAllocation.shuffleTracking.enabled&lt;/code&gt;, which defaults to
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt; and keeps executors holding live shuffle data.&lt;/p&gt;

  &lt;p&gt;The case against dynamic allocation for short jobs: the ramp-up costs more than
the elasticity saves.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;151. What does a container killed for exceeding memory limits mean?&lt;/summary&gt;

  &lt;p&gt;The cluster manager killed the JVM because the &lt;strong&gt;process total&lt;/strong&gt; exceeded what
was requested, which is heap plus overhead. It is not a JVM
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;, and the distinction drives the fix.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Symptom&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Means&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;java.lang.OutOfMemoryError: Java heap space&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The &lt;strong&gt;heap&lt;/strong&gt; filled&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;More partitions, then more &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;executor.memory&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Container killed by YARN or Kubernetes for exceeding limits&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The &lt;strong&gt;total&lt;/strong&gt; exceeded&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;More &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;memoryOverhead&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;What lives in overhead: off-heap buffers, Netty’s direct buffers for shuffle,
the JVM’s metaspace and thread stacks, native libraries, and &lt;strong&gt;the Python worker
processes&lt;/strong&gt; for PySpark.&lt;/p&gt;

  &lt;p&gt;That last one is why PySpark jobs hit this far more often than JVM jobs: Python
memory is entirely outside the heap, so a job with heavy Python UDFs can have a
perfectly healthy heap and still be killed.&lt;/p&gt;

  &lt;p&gt;The sequence:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Confirm which it is&lt;/strong&gt; from the message. This determines everything.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverhead&lt;/code&gt;&lt;/strong&gt;, generously for PySpark.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check for skew,&lt;/strong&gt; because one oversized partition is a common root cause that presents as a memory shortage.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Raise partition count&lt;/strong&gt; so each task’s footprint shrinks.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Raise executor memory,&lt;/strong&gt; last.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;The point to make:&lt;/strong&gt; raising memory first often just moves the failure to a
larger dataset next month, whereas fixing the partition size or the skew fixes
it structurally.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;152. What is `spark.network.timeout` and when do you raise it?&lt;/summary&gt;

  &lt;p&gt;The default timeout for network interactions between Spark components,
&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;120s&lt;/code&gt;&lt;/strong&gt;. It acts as the default for several more specific timeouts, including
RPC lookups and shuffle fetch waits.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.network.timeout&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# 120s
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;When raising it is legitimate:&lt;/strong&gt; genuinely long operations on healthy
infrastructure. Very large shuffle fetches, a heavily contended cluster, or
cross-availability-zone traffic.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;When raising it is the wrong answer, which is most of the time:&lt;/strong&gt; the timeout
is &lt;em&gt;reporting&lt;/em&gt; that something is dead or overwhelmed, not causing the problem.
An executor stuck in a long garbage collection cannot answer heartbeats; an
executor that was killed cannot serve shuffle blocks. Raising the timeout makes
the job take longer to notice, not longer to succeed.&lt;/p&gt;

  &lt;p&gt;So the diagnosis order:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Look at the executors tab.&lt;/strong&gt; Did an executor die, and why?&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check GC time.&lt;/strong&gt; An executor spending 40% of its time in GC will miss heartbeats, and the fix is memory or partition size, not the timeout.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check for container kills&lt;/strong&gt;, which point at overhead.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Only then&lt;/strong&gt; consider the timeout, and pair it with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.heartbeatInterval&lt;/code&gt;, which must stay well below it.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;A related note: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.heartbeatInterval&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;10s&lt;/code&gt;, and it
must be considerably smaller than the network timeout or executors get declared
lost spuriously.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;153. How do you debug a `NoClassDefFoundError` or a version conflict?&lt;/summary&gt;

  &lt;p&gt;Find out which JAR each class is actually loaded from, rather than reasoning
about what should be on the classpath.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.driver.extraJavaOptions=-verbose:class&quot;&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.executor.extraJavaOptions=-verbose:class&quot;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;That prints a line per class load with its source JAR, turning a guess about
shading into a direct answer:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;[Loaded com.fasterxml.jackson.databind.ObjectMapper from file:/opt/spark/jars/jackson-databind-2.15.2.jar]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The usual causes:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Two versions on the classpath&lt;/strong&gt;, one from Spark’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;jars/&lt;/code&gt; and one from your assembly. Spark’s usually wins, which is why your newer version appears absent.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A provided dependency not actually provided&lt;/strong&gt; at runtime.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A transitive dependency&lt;/strong&gt; you did not know you had.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;The fixes, in order:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Shade the conflicting dependency&lt;/strong&gt; in your assembly, which is the robust answer: relocate the package so there is no collision.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Align versions&lt;/strong&gt; with what Spark ships, which is the simplest when possible.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.userClassPathFirst&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.userClassPathFirst&lt;/code&gt;&lt;/strong&gt;, which put your jars ahead of Spark’s. Effective but risky, because it can break Spark’s own dependencies.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt;&lt;/strong&gt; for genuinely missing classes.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;The habit to convey:&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-verbose:class&lt;/code&gt; converts an argument into a fact. It
is noisy, so filter it, but it answers the question definitively.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;154. What causes a `java.lang.StackOverflowError` in Spark?&lt;/summary&gt;

  &lt;p&gt;A recursive walk over a structure deeper than the thread’s stack. In Spark that
structure is almost always the &lt;strong&gt;query plan or the RDD lineage&lt;/strong&gt;, not your
code’s own recursion.&lt;/p&gt;

  &lt;p&gt;The plan gets deep from:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Hundreds of chained &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt; calls.&lt;/li&gt;
    &lt;li&gt;A loop that rebuilds a DataFrame each iteration.&lt;/li&gt;
    &lt;li&gt;Deeply nested structs or very wide schemas.&lt;/li&gt;
    &lt;li&gt;A long iterative lineage with no checkpoint.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The stack trace is the tell: if the frames are Catalyst classes, an Avro or
Parquet schema walker, or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TreeNode&lt;/code&gt; methods, it is the plan.&lt;/p&gt;

  &lt;p&gt;Two fixes, and the right one depends on which side threw:&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Raise the stack&lt;/strong&gt; on the side that threw, as a direct remedy:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.driver.extraJavaOptions=-Xss64m&quot;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.executor.extraJavaOptions=-Xss64m&quot;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Note in client mode the driver JVM already exists, so use
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--driver-java-options&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. Make the plan shallower,&lt;/strong&gt; which is the structural fix:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Collapse many &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; calls into one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;checkpoint()&lt;/code&gt; periodically inside an iterative loop, truncating lineage.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;The judgement to express:&lt;/strong&gt; if you find yourself needing hundreds of megabytes
of stack, the plan itself is the problem and raising &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-Xss&lt;/code&gt; is buying time rather
than fixing it.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;155. How do you use custom logging to see what a job did?&lt;/summary&gt;

  &lt;p&gt;Write a Log4j 2 config, ship it with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--files&lt;/code&gt;, and point both sides at it.&lt;/p&gt;

  &lt;div class=&quot;language-properties highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;py&quot;&gt;rootLogger.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;warn&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;rootLogger.appenderRef.stdout.ref&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;Console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.layout.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;PatternLayout&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.layout.pattern&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;%d{HH:mm:ss} %-5level %logger{1} - %msg%n&lt;/span&gt;

&lt;span class=&quot;py&quot;&gt;logger.sql.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.spark.sql.execution&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.sql.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;debug&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--files&lt;/span&gt; /etc/spark/conf/log4j2.properties &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.driver.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.executor.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  app.jar
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Three details that make it work:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;The value is a bare filename,&lt;/strong&gt; not a path, because &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--files&lt;/code&gt; places the file in each container’s working directory.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Set both sides.&lt;/strong&gt; The driver logs planning and scheduling; executors log task-level work. Setting one and forgetting the other looks like the feature not working.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Spark 3.3.0 and later is Log4j 2.&lt;/strong&gt; A config in the old &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j.rootLogger=&lt;/code&gt; syntax is silently ignored, not rejected.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Useful namespaces to raise selectively:
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.sql.execution&lt;/code&gt; for plan and codegen detail,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.scheduler&lt;/code&gt; for stage and task decisions,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.spark.storage&lt;/code&gt; for block and cache activity.&lt;/p&gt;

  &lt;p&gt;Keep the root at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;warn&lt;/code&gt; and raise one namespace, otherwise the signal is buried.
And never leave &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DEBUG&lt;/code&gt; on in production.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;156. How do you confirm a config actually took effect?&lt;/summary&gt;

  &lt;p&gt;Three ways, in descending authority:&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. The Environment tab of the UI.&lt;/strong&gt; It lists the Spark properties actually in
force for the running application. This is the authoritative answer and settles
most arguments.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. At runtime, for SQL configs:&lt;/strong&gt;&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Note this works for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.*&lt;/code&gt; configs but raises for many core ones, which
are read from &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkConf&lt;/code&gt; at startup:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;sparkContext&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getConf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.executor.memory&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;(default)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;3. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--verbose&lt;/code&gt; on submit,&lt;/strong&gt; which prints the resolved configuration and where
each value came from.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The cases where a config silently does not apply&lt;/strong&gt;, which is the real content
of this question:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Case&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Driver JVM options in client mode&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The JVM already started&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.*&lt;/code&gt; set after the session exists&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Executors are already running&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.default.parallelism&lt;/code&gt; on a DataFrame job&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Wrong config; you want &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--conf&lt;/code&gt; placed after the application jar&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Passed to your app, not to Spark&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;That last one is worth emphasising: it is not an error, it is an argument to
your &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;main&lt;/code&gt;, so Spark never sees it and nothing warns you.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;157. What is the difference between `spark.sql.files.maxPartitionBytes` and `spark.sql.shuffle.partitions`?&lt;/summary&gt;

  &lt;p&gt;They control parallelism at two different points, and reaching for the wrong
one is a common waste of time.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.maxPartitionBytes&lt;/code&gt;&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt;&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Controls&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Input partitions when reading files&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Output partitions of a shuffle&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Default&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;128MB&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Affects&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The first stage&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Every stage after a shuffle&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Units&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Bytes per partition&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A count&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart LR
  A[&quot;files on storage&quot;] --&amp;gt;|&quot;maxPartitionBytes&amp;lt;br/&amp;gt;decides split size&quot;| B[&quot;stage 0 tasks&quot;]
  B --&amp;gt;|&quot;shuffle&quot;| C[&quot;spark.sql.shuffle.partitions&amp;lt;br/&amp;gt;decides task count&quot;]
  C --&amp;gt; D[&quot;stage 1 tasks&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;So if the &lt;strong&gt;read&lt;/strong&gt; stage has too few tasks, lowering &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxPartitionBytes&lt;/code&gt; gives
you more. If the stage &lt;strong&gt;after a shuffle&lt;/strong&gt; has the wrong number,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.shuffle.partitions&lt;/code&gt; or AQE coalescing is the lever.&lt;/p&gt;

  &lt;p&gt;Note the units differ in a way that matters: one is a size and the other a count.
The size one adapts automatically as data grows; the count one does not, which is
exactly why a static &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;200&lt;/code&gt; ages badly and why AQE coalescing exists.&lt;/p&gt;

  &lt;p&gt;And remember &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxPartitionBytes&lt;/code&gt; is only one input to the split calculation.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.openCostInBytes&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4MB&lt;/code&gt;, is charged per file, so with
many small files the packing behaviour dominates and lowering
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxPartitionBytes&lt;/code&gt; changes nothing.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;158. How do you speed up a job that writes many partitions?&lt;/summary&gt;

  &lt;p&gt;Repartition by the same columns you partition by, so each output directory is
written by one task instead of all of them.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;dt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;dt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The arithmetic that explains why.&lt;/strong&gt; Without the repartition, every task may
hold rows for every partition value, so it opens a writer per value:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;files = tasks x partition values      (worst case)
      = 200 x 250
      = 50,000 small files
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;With the repartition, each value lives in one task:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;files = partition values = 250
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;That also fixes the write itself, not just later reads: 50,000 open file handles
and 50,000 small footers is slow to produce, and on object storage it is
thousands of PUT requests.&lt;/p&gt;

  &lt;p&gt;Other levers:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.maxRecordsPerFile&lt;/code&gt;&lt;/strong&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0&lt;/code&gt; (unlimited), caps rows per file so one huge partition splits into several properly sized files rather than one enormous one.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Dynamic partition overwrite&lt;/strong&gt;, so a job writing one day does not rewrite the table:&lt;/li&gt;
  &lt;/ul&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.sources.partitionOverwriteMode&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;dynamic&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Reconsider the partition column.&lt;/strong&gt; If it has thousands of values, directory-per-value is the wrong layout and a lower-cardinality column, or bucketing, is better.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;159. What are the highest-value things to check before tuning configs?&lt;/summary&gt;

  &lt;p&gt;Six checks, each of which typically outweighs any config change.&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Are you reading more than you need?&lt;/strong&gt; Columns you never select, rows you filter after the join instead of before, partitions you could have pruned. Check &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ReadSchema&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PartitionFilters&lt;/code&gt; in the plan.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Is the plan what you think?&lt;/strong&gt; An accidental &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;CartesianProduct&lt;/code&gt;, a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SortMergeJoin&lt;/code&gt; where a broadcast was possible, a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SinglePartition&lt;/code&gt; exchange from a window without &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;partitionBy&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Are there small files?&lt;/strong&gt; Thousands of tiny inputs make planning dominate execution.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Is there skew?&lt;/strong&gt; Max versus median task time on the slowest stage.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Is something cached that should not be?&lt;/strong&gt; The storage tab: a partly resident cache is pure cost.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Is a UDF doing work a built-in could do?&lt;/strong&gt; Every UDF is an optimisation barrier.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;slow job&quot;] --&amp;gt; B[&quot;read less?&quot;] --&amp;gt; C[&quot;plan correct?&quot;] --&amp;gt; D[&quot;file count?&quot;]
  D --&amp;gt; E[&quot;skew?&quot;] --&amp;gt; F[&quot;caches?&quot;] --&amp;gt; G[&quot;UDFs?&quot;] --&amp;gt; H[&quot;only now: configs&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;&lt;strong&gt;The reason to put it this way in an interview:&lt;/strong&gt; it shows you treat tuning as
diagnosis rather than ritual. A job with a page of carefully tuned configs and a
missing broadcast is slower than a job with default configs and a correct plan.
Configs are the last 20%, and the plan is the first 80%.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;160. How do you benchmark a change honestly?&lt;/summary&gt;

  &lt;p&gt;Change one thing, run at representative scale, and compare the stage you
expected to change rather than total wall clock.&lt;/p&gt;

  &lt;p&gt;The rules that keep a benchmark honest:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;One variable at a time.&lt;/strong&gt; Two changes together tell you nothing about either.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Representative data.&lt;/strong&gt; A change that helps on 1 GB may do nothing at 1 TB, and skew often only appears at scale.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Repeat runs.&lt;/strong&gt; A single run on a shared cluster measures your neighbours as much as your change.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Compare the right metric.&lt;/strong&gt; If you changed shuffle partitions, compare that stage’s duration and spill, not the whole job, which includes unrelated stages.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Account for warm caches.&lt;/strong&gt; The second read of the same data is faster for reasons unrelated to your change: page cache, warm JVMs, and Spark’s own caches.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;And the thing not to do, which is worth saying explicitly:&lt;/strong&gt; do not publish or
rely on wall-clock timings from a laptop or a demo dataset. They are dominated by
JVM warm-up and OS page cache, and they do not transfer to a cluster. A timing
harness is fine; presenting its local output as a result is not.&lt;/p&gt;

  &lt;p&gt;What to report instead: the mechanism, and the structural numbers that are
deterministic. “Spill dropped from 40 GB to zero and the stage went from 200
tasks to 2,000” is reproducible and explains itself. “It got 3x faster on my
machine” does not.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;structured-streaming&quot;&gt;Structured Streaming&lt;/h2&gt;

&lt;p&gt;Enough to show you understand the model, not just the API. Watermarks and state are where these questions go.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;161. What is Structured Streaming?&lt;/summary&gt;

  &lt;p&gt;A stream processing model where a stream is treated as an &lt;strong&gt;unbounded table&lt;/strong&gt;
that rows are appended to, and a query over it is re-executed incrementally as
data arrives.&lt;/p&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;input stream&quot;] --&amp;gt; B[&quot;unbounded input table&amp;lt;br/&amp;gt;rows appended&quot;]
  B --&amp;gt; C[&quot;your query,&amp;lt;br/&amp;gt;the same as batch&quot;]
  C --&amp;gt; D[&quot;result table,&amp;lt;br/&amp;gt;updated each trigger&quot;]
  D --&amp;gt; E[&quot;sink, per output mode&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The consequence that makes it valuable: &lt;strong&gt;you write the same DataFrame
operations as for batch.&lt;/strong&gt; The engine works out how to maintain the result
incrementally, keeping whatever state that requires.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;readStream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;kafka&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;kafka.bootstrap.servers&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;kafka-broker1:9092&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;subscribe&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;load&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;

&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;selectExpr&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;CAST(value AS STRING)&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeStream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;parquet&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;checkpointLocation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;s3a://lakehouse-prod/checkpoints/trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;path&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;s3a://lakehouse-prod/warehouse/trips_raw&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;outputMode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;start&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;By default it executes as &lt;strong&gt;micro-batches&lt;/strong&gt;: the engine runs a small batch job
per trigger, which is why the same Catalyst machinery and the same UI apply.&lt;/p&gt;

  &lt;p&gt;The model is what you should lead with in an interview, because it explains
everything else: output modes exist because the result table can change in
different ways, and watermarks exist because maintaining the result incrementally
requires bounded state.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;162. What is the difference between Structured Streaming and the old DStream API?&lt;/summary&gt;

  &lt;p&gt;DStreams are the original RDD-based API; Structured Streaming is the
DataFrame-based replacement.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;DStreams&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Structured Streaming&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Built on&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;RDDs&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;DataFrames and Catalyst&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Optimisation&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;None&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Full Catalyst and Tungsten&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Time semantics&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Processing time only&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Event time&lt;/strong&gt;, with watermarks&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Late data&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Manual handling&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Watermarks and allowed lateness&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;End-to-end guarantee&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;At-least-once in practice&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Exactly-once with a replayable source and idempotent sink&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Batch and stream code&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Different APIs&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The same API&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Status&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Legacy&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Current&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;The two differences that matter most in practice are &lt;strong&gt;event time&lt;/strong&gt; and the
&lt;strong&gt;unified API&lt;/strong&gt;. DStreams only knew when the engine saw a record, so correct
windowing over event time was the application’s problem. And because DStream code
was a separate API, a batch backfill of the same logic had to be written twice.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;New work should use Structured Streaming.&lt;/strong&gt; The right way to say this in an
interview is that DStreams are not merely older but architecturally unable to
offer event-time correctness or plan optimisation, because they operate on opaque
RDDs.&lt;/p&gt;

  &lt;p&gt;The one thing DStreams retained for a while was some lower-level control over
batch composition, which the newer API has largely absorbed through triggers and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreachBatch&lt;/code&gt;.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;163. What are the output modes?&lt;/summary&gt;

  &lt;p&gt;Three, and which are legal depends on your query.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Mode&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Writes each trigger&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Legal for&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Only new rows that will never change&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Queries with no aggregation, or aggregations with a watermark&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;update&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Only rows whose value changed&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Most aggregations&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;complete&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The entire result table&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Aggregations only&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;outputMode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The logic behind the restrictions is worth explaining, because it shows you
understand the model:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt; requires that a row, once emitted, is final.&lt;/strong&gt; For a plain projection that is always true. For a windowed aggregation it is only true once the watermark has passed the window’s end, which is why &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt; on an aggregation &lt;strong&gt;requires a watermark&lt;/strong&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;update&lt;/code&gt; emits changed rows,&lt;/strong&gt; so it works for aggregations without needing finality, but the sink must be able to handle updates to keys it has seen.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;complete&lt;/code&gt; rewrites everything&lt;/strong&gt; every trigger, so it requires keeping the whole result table in state forever. It does not scale, and it is only appropriate for small result sets such as a dashboard aggregate.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The common mistake is choosing &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;complete&lt;/code&gt; for convenience, then finding state
grows without bound. The other is expecting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt; to emit aggregation results
immediately, when in fact nothing appears until the watermark closes the
window.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;164. What is a trigger, and what are the options?&lt;/summary&gt;

  &lt;p&gt;A trigger controls &lt;strong&gt;when&lt;/strong&gt; the engine runs the next micro-batch.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Trigger&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Behaviour&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Default, unspecified&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Start the next batch as soon as the previous finishes&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;processingTime=&quot;1 minute&quot;&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Start a batch on a fixed interval&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow=True&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Process all available data in as many batches as needed, then stop&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;continuous=&quot;1 second&quot;&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Experimental continuous processing, with limited operation support&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trigger&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;processingTime&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;1 minute&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;trigger&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;availableNow&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;bp&quot;&gt;True&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;How to choose:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;The default&lt;/strong&gt; gives lowest latency and continuous resource use. It also produces many small files if the sink is file-based, because it writes per batch.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A fixed interval&lt;/strong&gt; is the usual production choice, because it batches work into sensibly sized writes and makes resource use predictable. If a batch takes longer than the interval, the next starts immediately, so the interval is a floor, not a guarantee.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;availableNow&lt;/code&gt;&lt;/strong&gt; is how you run a streaming query as a scheduled batch job: it consumes everything outstanding using the streaming checkpoint for offsets, then exits. That gives you exactly-once bookkeeping with batch economics, and it replaced the older &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;once=True&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;The practical trade to name:&lt;/strong&gt; shorter triggers mean lower latency and more
small files; longer triggers mean fewer, larger files and higher latency. For a
lakehouse sink, the file-count consequence usually dominates the latency
preference.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;165. What is event time versus processing time?&lt;/summary&gt;

  &lt;p&gt;&lt;strong&gt;Event time&lt;/strong&gt; is when the thing happened, carried in the record.
&lt;strong&gt;Processing time&lt;/strong&gt; is when the engine saw it.&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;event time      10:00:05   (the trip started)
ingest time     10:00:07   (Kafka received it)
processing time 10:04:30   (Spark&apos;s batch picked it up)
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;They diverge for ordinary reasons: a mobile client was offline, a broker
rebalanced, a consumer lagged, or a batch was delayed.&lt;/p&gt;

  &lt;p&gt;Correctness depends on event time. A trip that happened at 09:59 belongs in the
09:55-10:00 window no matter when it arrives, and aggregating by processing time
would attribute it to the wrong window and produce a different answer on every
replay.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The mistake to watch for&lt;/strong&gt;, and interviewers do ask about it: using the
source’s own timestamp as if it were event time. Kafka’s &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;timestamp&lt;/code&gt; column is
broker ingest time, not when the event occurred:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# wrong: this is ingest time
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;timestamp&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10 minutes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# right: event time from the payload
&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;from_json&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;value&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;string&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;schema&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;t&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;t.*&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10 minutes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The practical test is replayability: if you reprocess yesterday’s data today, an
event-time query gives the same answer and a processing-time query does not.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;166. What is a watermark and what does it actually do?&lt;/summary&gt;

  &lt;p&gt;A watermark is a moving threshold on event time that declares how late data may
be. It is computed as the maximum event time seen so far, minus the delay you
allow.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10 minutes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;window&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;5 minutes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;*&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;It does &lt;strong&gt;two&lt;/strong&gt; things, and naming both is the strong answer:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Finalises windows,&lt;/strong&gt; so their results can be emitted in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt; mode. Until the watermark passes a window’s end, that window might still change, so nothing can be appended.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Bounds state.&lt;/strong&gt; Once a window can no longer change, the engine drops the state it was holding for it. Without that, state grows for the life of the query.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;max event time seen&amp;lt;br/&amp;gt;10:30&quot;] --&amp;gt; B[&quot;minus 10 min delay&quot;]
  B --&amp;gt; C[&quot;watermark 10:20&quot;]
  C --&amp;gt; D[&quot;windows ending before 10:20:&amp;lt;br/&amp;gt;emit and drop state&quot;]
  C --&amp;gt; E[&quot;records older than 10:20:&amp;lt;br/&amp;gt;dropped as too late&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;The trade is explicit: &lt;strong&gt;a longer watermark tolerates later data and holds more
state; a shorter one is cheaper and drops more.&lt;/strong&gt; That is a business decision
about how much lateness matters, not a tuning parameter.&lt;/p&gt;

  &lt;p&gt;The signature of getting it wrong is state size climbing without plateau in
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;lastProgress&lt;/code&gt;, which is the number to monitor.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;167. Does a watermark do anything on a non-aggregating stream?&lt;/summary&gt;

  &lt;p&gt;Essentially nothing, and recognising that is a good sign.&lt;/p&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withWatermark&lt;/code&gt; gives the engine two capabilities: finalise results so they can
be appended, and drop state that can no longer change. A pass-through stream, a
projection, a filter, a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;from_json&lt;/code&gt;, keeps no state and has no results to
finalise. So there is nothing for the watermark to do.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# the watermark here is inert
&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10 minutes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeStream&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;parquet&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;outputMode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;start&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Seeing one there usually means it was copied from an aggregation example.&lt;/p&gt;

  &lt;p&gt;Where it &lt;strong&gt;does&lt;/strong&gt; matter is any stateful operation:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Operation&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Watermark needed&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Windowed aggregation&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt; mode and to bound state&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dropDuplicates&lt;/code&gt; on a stream&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, or the dedup set grows forever&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Stream-stream join&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, on both sides, plus a time condition&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;flatMapGroupsWithState&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Yes, to expire state&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Projection, filter, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;from_json&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;No&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;The one subtle exception&lt;/strong&gt; is worth mentioning: a watermark also drops
individual records older than the threshold, so on a non-aggregating stream it
could silently discard late rows without giving you anything in return. So it is
not merely useless there, it can be actively harmful.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;168. What is checkpointing in Structured Streaming?&lt;/summary&gt;

  &lt;p&gt;A required storage location where the engine records what it has processed and
the state it holds, so a restarted query resumes exactly where it stopped.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;checkpointLocation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;s3a://lakehouse-prod/checkpoints/trips_by_window&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;What it contains:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Offsets&lt;/strong&gt;, a write-ahead log of which source offsets each batch covered, written &lt;em&gt;before&lt;/em&gt; the batch runs.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Commits&lt;/strong&gt;, marking which batches completed.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;State&lt;/strong&gt;, the keyed state for stateful operations, versioned per batch.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Metadata&lt;/strong&gt;, including the query id.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The offsets-before-execution ordering is what makes recovery deterministic: on
restart the engine knows which batch was in flight and re-runs it over exactly
the same input range, which is the basis of the exactly-once story.&lt;/p&gt;

  &lt;p&gt;The rules:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Every streaming query needs one.&lt;/strong&gt; Without it the query cannot recover, and file sinks will refuse to run.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;One per query.&lt;/strong&gt; Two queries sharing a location will corrupt each other.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It must be durable and shared.&lt;/strong&gt; Local disk fails when the driver moves, so use HDFS or object storage.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;It is not disposable.&lt;/strong&gt; Deleting it means reprocessing from the source’s earliest offset, or losing data, depending on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;startingOffsets&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;That last point is the operational trap: people delete a checkpoint to “reset”
a query and either reprocess everything or skip data.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;169. How does Structured Streaming achieve exactly-once?&lt;/summary&gt;

  &lt;p&gt;By combining three things, and it is a property of the &lt;strong&gt;whole pipeline&lt;/strong&gt;, not
of the engine alone.&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;A replayable source.&lt;/strong&gt; One that can be re-read from a recorded offset: Kafka, or files. A source you cannot rewind, such as a plain socket, cannot give exactly-once.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Checkpointed offsets.&lt;/strong&gt; The engine writes which offsets a batch covers &lt;em&gt;before&lt;/em&gt; running it, so a restart re-runs the identical input range.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;An idempotent sink.&lt;/strong&gt; One where writing the same batch twice has the same effect as writing it once, usually by keying the write on the batch id.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  A[&quot;replayable source&amp;lt;br/&amp;gt;Kafka offsets&quot;] --&amp;gt; B[&quot;checkpoint offsets&amp;lt;br/&amp;gt;before the batch&quot;]
  B --&amp;gt; C[&quot;run the batch&quot;]
  C --&amp;gt; D[&quot;idempotent sink&amp;lt;br/&amp;gt;dedupes on batch id&quot;]
  D --&amp;gt; E[&quot;exactly-once end to end&quot;]
&lt;/code&gt;&lt;/pre&gt;

  &lt;p&gt;&lt;strong&gt;The honest framing:&lt;/strong&gt; if any of the three is missing, you get at-least-once.
A sink that appends without deduplication will duplicate on retry no matter what
the engine does. Spark’s file sinks handle this with a commit log, which is why
they are exactly-once, and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreachBatch&lt;/code&gt; puts the responsibility on you.&lt;/p&gt;

  &lt;p&gt;The interview-grade version of the answer is exactly that distinction: the engine
guarantees it will re-present the same batch, and the sink must guarantee that
re-presenting is harmless. Saying “Spark gives exactly-once” without the sink
condition is the answer that gets probed.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;170. What is `foreachBatch` and why is it useful?&lt;/summary&gt;

  &lt;p&gt;A sink that hands you each micro-batch as an ordinary DataFrame, letting you do
anything batch code can do.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;def&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;upsert&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;batch_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;batch_id&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;batch_df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;hudi&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
       &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;options&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;**&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;hudi_options&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
       &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
       &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;save&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;table_path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;writeStream&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;foreachBatch&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;upsert&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;option&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;checkpointLocation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;ckpt&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;start&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Why it matters: streaming sinks support a limited set of operations, while a
batch DataFrame supports everything. So &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreachBatch&lt;/code&gt; is how you reach:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; into a lakehouse table&lt;/strong&gt;, which is the standard way to drive upserts from a stream into Hudi, Iceberg or Delta.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Multiple sinks&lt;/strong&gt; from one stream, writing the same batch twice without reading the source twice.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;JDBC writes&lt;/strong&gt;, and any sink with no streaming implementation.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Batch-only operations&lt;/strong&gt; on the micro-batch, such as a join to a static table with a strategy that streaming would not choose.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;Two responsibilities it transfers to you:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Idempotency.&lt;/strong&gt; The same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;batch_id&lt;/code&gt; can be re-presented after a failure, so your write must tolerate that. The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;batch_id&lt;/code&gt; argument exists precisely so you can deduplicate on it.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Caching if you use the batch twice.&lt;/strong&gt; Writing &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;batch_df&lt;/code&gt; to two sinks without caching recomputes it, which for a Kafka source means re-reading.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;So &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;foreachBatch&lt;/code&gt; is the escape hatch that makes streaming practical for
lakehouse ingestion, at the cost of owning the exactly-once contract yourself.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;171. What happens if you change a streaming query&apos;s code and restart it?&lt;/summary&gt;

  &lt;p&gt;It depends on whether the change is compatible with the checkpointed state. The
engine validates on restart and fails rather than producing wrong results.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Usually safe:&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Adding or removing a projection column.&lt;/li&gt;
    &lt;li&gt;Changing a filter predicate.&lt;/li&gt;
    &lt;li&gt;Changing the sink’s output path.&lt;/li&gt;
    &lt;li&gt;Changing the trigger interval.&lt;/li&gt;
    &lt;li&gt;Adding a new stateless operation.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;Usually not safe:&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Changing the aggregation keys, since the state is keyed by them.&lt;/li&gt;
    &lt;li&gt;Changing the output mode.&lt;/li&gt;
    &lt;li&gt;Adding or removing a stateful operation, which changes the state layout.&lt;/li&gt;
    &lt;li&gt;Changing the watermark column.&lt;/li&gt;
    &lt;li&gt;Changing the source in a way that changes offset semantics, such as a different topic.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;incompatible change -&amp;gt; restart fails, or requires a new checkpoint
new checkpoint      -&amp;gt; reprocess from startingOffsets, or skip data
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The operational consequence, which is the real content of this question:&lt;/strong&gt;
for an incompatible change you need a migration plan, not just a deploy. The
usual options are to start a new query with a new checkpoint and backfill the gap
with a batch job, or to run both old and new in parallel and cut over.&lt;/p&gt;

  &lt;p&gt;This is why streaming state should be treated as a schema: changing it is a
migration. Teams that treat a streaming job as stateless code get caught by this
the first time they need to add a grouping key.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;172. How do you handle late data?&lt;/summary&gt;

  &lt;p&gt;Decide how late is tolerable, express that as the watermark, and then decide
what happens to anything later.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;30 minutes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Data arriving within 30 minutes of the maximum event time seen is included in
its correct window. Data arriving later is &lt;strong&gt;dropped&lt;/strong&gt; from the stateful
aggregation, because the state for its window has been released.&lt;/p&gt;

  &lt;p&gt;The trade is direct:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Watermark&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Late data&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;State&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Short, minutes&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;More dropped&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Small, cheap&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Long, hours or days&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;More included&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Large, expensive&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;So the choice is a business question: how much does a late record matter, and is
a slightly wrong aggregate acceptable in exchange for bounded cost?&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;If you cannot afford to drop it,&lt;/strong&gt; the standard pattern is two paths:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;The streaming aggregation with a modest watermark, for timely approximate results.&lt;/li&gt;
    &lt;li&gt;The &lt;strong&gt;raw stream written as-is&lt;/strong&gt;, with no watermark and no aggregation, so nothing is lost.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;Then a periodic batch job recomputes the affected windows from the raw data and
corrects the serving table. That is the lambda-shaped compromise, and in a
lakehouse it is usually a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; that overwrites the recomputed windows.&lt;/p&gt;

  &lt;p&gt;You can also monitor what you are losing: the difference between raw record
counts and aggregated counts over the same period tells you whether the watermark
is set sensibly.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;173. What are stateful operations, and which are they?&lt;/summary&gt;

  &lt;p&gt;Any operation whose result depends on data seen across triggers, so the engine
must carry something between batches.&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Operation&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;State held&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Windowed aggregation&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A partial aggregate per window per key&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupBy&lt;/code&gt; aggregation without windows&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A running aggregate per key, forever&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dropDuplicates&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The set of keys already seen&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Stream-stream join&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Buffered rows from both sides awaiting matches&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;flatMapGroupsWithState&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;transformWithState&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Whatever you choose to keep&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;Stateless by contrast: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;filter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;from_json&lt;/code&gt;, and a
stream-to-static join.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;All of the stateful ones need a watermark to bound state,&lt;/strong&gt; except a
non-windowed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupBy&lt;/code&gt;, which is unbounded by construction: a running count per
key keeps one entry per key forever, and no watermark can help because the key
never expires. That is worth knowing, because it is the one case where the answer
is “restructure the query”, not “add a watermark”.&lt;/p&gt;

  &lt;p&gt;State lives in a state store, checkpointed per batch, and it is the main scaling
dimension of a streaming job. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;lastProgress&lt;/code&gt; reports &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;stateOperators&lt;/code&gt; with row
counts and memory used, which is the number to watch:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;query&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lastProgress&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;stateOperators&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;Plateauing state&lt;/strong&gt; means the watermark is doing its job. &lt;strong&gt;Monotonically
growing state&lt;/strong&gt; is the signature of a missing watermark, an over-generous one, or
an unbounded aggregation.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;174. What is special about a stream-stream join?&lt;/summary&gt;

  &lt;p&gt;Both sides are unbounded, so the engine must buffer rows from each side while
waiting for a match that may not have arrived yet. That makes it the most
state-hungry operation in Structured Streaming.&lt;/p&gt;

  &lt;p&gt;Two requirements follow:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Watermarks on both sides,&lt;/strong&gt; so the engine knows when a row can no longer find a match and its state can be dropped.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A time constraint&lt;/strong&gt; in the join condition, bounding how far apart matching events can be.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;impressions&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;imp_time&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2 hours&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
 &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
   &lt;span class=&quot;n&quot;&gt;clicks&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;click_time&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;3 hours&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt;
   &lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;expr&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
     &lt;span class=&quot;s&quot;&gt;&quot;imp_id = click_imp_id AND &quot;&lt;/span&gt;
     &lt;span class=&quot;s&quot;&gt;&quot;click_time &amp;gt;= imp_time AND &quot;&lt;/span&gt;
     &lt;span class=&quot;s&quot;&gt;&quot;click_time &amp;lt;= imp_time + interval 1 hour&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Without the time condition the engine must keep every row forever, because any
future row might match.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Outer joins add a further subtlety worth volunteering:&lt;/strong&gt; a non-match cannot be
emitted immediately, because a match might still arrive. The engine must wait
until the watermark proves no match is possible, so outer-join results appear
delayed by the watermark, not at the trigger where the row arrived.&lt;/p&gt;

  &lt;p&gt;The practical advice: prefer a &lt;strong&gt;stream-to-static&lt;/strong&gt; join where you can, joining
the stream to a dimension table. It is stateless, needs no watermark, and is
usually what the requirement actually calls for.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;175. How do you monitor a streaming query?&lt;/summary&gt;

  &lt;p&gt;Through the query’s own progress objects, and through the Streaming tab of the
UI.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;query&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;status&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;          &lt;span class=&quot;c1&quot;&gt;# what it is doing right now
&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;query&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lastProgress&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;    &lt;span class=&quot;c1&quot;&gt;# metrics for the last completed batch
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The numbers that matter, in priority order:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Metric&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;What it tells you&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;inputRowsPerSecond&lt;/code&gt; vs &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;processedRowsPerSecond&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Whether you are keeping up&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;stateOperators[].numRowsTotal&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Whether state is bounded&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;batchDuration&lt;/code&gt; against your trigger interval&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Whether batches fit in their window&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;durationMs&lt;/code&gt; breakdown&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Where the time goes: &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;addBatch&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;getBatch&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;walCommit&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Source &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;endOffset&lt;/code&gt; minus committed&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Consumer lag&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;The two alerts worth having:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Processing rate below input rate,&lt;/strong&gt; sustained. That is falling behind, and lag grows without bound until you add resources or reduce work.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;State size growing without plateau.&lt;/strong&gt; That is the watermark signature, and it ends in an executor &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; hours or days later.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;For programmatic alerting, attach a listener rather than polling:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;streams&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;addListener&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;my_listener&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# onQueryProgress, onQueryTerminated
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The thing to say about alerting on duration:&lt;/strong&gt; batch duration alone is a poor
signal, because it moves with input volume for innocent reasons. The ratio of
processing rate to input rate is the structural signal, and state size is the
one that predicts a failure before it happens.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;deployment-cluster-managers-and-operations&quot;&gt;Deployment, cluster managers and operations&lt;/h2&gt;

&lt;p&gt;The practical half that candidates often cannot answer, which makes it a good differentiator.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;176. How do you run Spark on YARN?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--master yarn&lt;/code&gt;, with the deploy mode choosing where the driver lives.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nb&quot;&gt;export &lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;HADOOP_CONF_DIR&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;/etc/hadoop/conf
spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--master&lt;/span&gt; yarn &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--deploy-mode&lt;/span&gt; cluster &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--queue&lt;/span&gt; analytics &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--num-executors&lt;/span&gt; 50 &lt;span class=&quot;nt&quot;&gt;--executor-cores&lt;/span&gt; 4 &lt;span class=&quot;nt&quot;&gt;--executor-memory&lt;/span&gt; 16g &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.executor.memoryOverhead&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;3g &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--class&lt;/span&gt; com.example.TripsIngest trips.jar
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;What happens: Spark starts an &lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ApplicationMaster&lt;/code&gt;&lt;/strong&gt; in the cluster, which
requests containers from the ResourceManager through &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;YarnAllocator&lt;/code&gt;. Each
granted container becomes an executor. In cluster mode the driver runs inside
the ApplicationMaster container; in client mode the AM only handles resource
requests while the driver stays on your machine.&lt;/p&gt;

  &lt;p&gt;Things that are YARN-specific:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HADOOP_CONF_DIR&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;YARN_CONF_DIR&lt;/code&gt; must be set,&lt;/strong&gt; or Spark cannot find the ResourceManager.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--queue&lt;/code&gt;&lt;/strong&gt; places the application in a scheduler queue, which is how YARN arbitrates between applications.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Container kills for exceeding memory&lt;/strong&gt; are reported by the NodeManager, and the fix is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;memoryOverhead&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The external shuffle service&lt;/strong&gt; runs as a NodeManager auxiliary service.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Kerberos&lt;/strong&gt; delegation tokens are obtained by the driver and renewed for long-running jobs, which needs &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--principal&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--keytab&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The tracking URL in the ResourceManager UI links to the Spark UI while running,
and to the history server afterwards.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;177. How do you run Spark on Kubernetes?&lt;/summary&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--master k8s://https://&amp;lt;api-server&amp;gt;:&amp;lt;port&amp;gt;&lt;/code&gt;, with a container image that has
Spark in it.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--master&lt;/span&gt; k8s://https://k8s.internal:6443 &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--deploy-mode&lt;/span&gt; cluster &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--name&lt;/span&gt; trips-ingest &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.kubernetes.container.image&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;registry.internal/spark:4.2.0 &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.kubernetes.authenticate.driver.serviceAccountName&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;spark &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.executor.instances&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;50 &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--class&lt;/span&gt; com.example.TripsIngest &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nb&quot;&gt;local&lt;/span&gt;:///opt/app/trips.jar
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;The architecture is distinctive: the driver runs in a pod, and &lt;strong&gt;the driver’s own
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ExecutorPodsAllocator&lt;/code&gt; creates executor pods&lt;/strong&gt; by talking to the API server.
There is no Spark cluster-manager daemon at all.&lt;/p&gt;

  &lt;p&gt;What that implies:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;A service account with permission to create pods&lt;/strong&gt; is required, which is what the &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;serviceAccountName&lt;/code&gt; config is for.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The image is the dependency mechanism.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;local://&lt;/code&gt; paths refer to files inside the image, which is the repeatable way to ship code.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Pod templates&lt;/strong&gt; let you set anything Kubernetes supports that Spark has no config for: node selectors, tolerations, volumes, sidecars.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Dynamic allocation works&lt;/strong&gt; via shuffle tracking, since there is no external shuffle service on Kubernetes in the YARN sense.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;pre&gt;&lt;code class=&quot;language-mermaid&quot;&gt;flowchart TB
  S[&quot;spark-submit&quot;] --&amp;gt; A[&quot;API server&quot;]
  A --&amp;gt; D[&quot;driver pod&quot;]
  D --&amp;gt;|&quot;ExecutorPodsAllocator&amp;lt;br/&amp;gt;creates pods&quot;| A
  A --&amp;gt; E1[&quot;executor pod&quot;]
  A --&amp;gt; E2[&quot;executor pod&quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;178. What is the difference between Standalone, YARN and Kubernetes for Spark?&lt;/summary&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt; &lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Standalone&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;YARN&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Kubernetes&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Provided by&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Spark itself&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Hadoop&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Kubernetes&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Grants containers&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Master&lt;/code&gt; daemon&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;ResourceManager, via &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ApplicationMaster&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;API server, via the driver&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Isolation&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Process-level&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Containers, cgroups&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Pods, full container isolation&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Multi-tenancy&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Limited&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Queues, mature&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Namespaces, quotas&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Dependency shipping&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Jars on a shared path&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Jars, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Container images&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Best for&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Dedicated Spark clusters&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Existing Hadoop estates&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Cloud-native, mixed workloads&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;How to choose, honestly:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Standalone&lt;/strong&gt; when the cluster runs only Spark and you want the least machinery. It is simple and it works, but it has the weakest multi-tenancy.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;YARN&lt;/strong&gt; when you already have Hadoop. It is the most mature for queue-based sharing and for Kerberos, and the tooling around it is well understood.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Kubernetes&lt;/strong&gt; when you are already running Kubernetes, want per-job container images, and want Spark to share a cluster with non-Spark workloads.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;And note Mesos is gone.&lt;/strong&gt; It was removed in the 4.x line, so a candidate
listing four options is working from older material.&lt;/p&gt;

  &lt;p&gt;The interview-relevant point beyond the table: the choice changes deployment and
sharing, but &lt;strong&gt;not&lt;/strong&gt; how Spark itself schedules. The driver, the stages and the
tasks behave identically, because only &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SchedulerBackend&lt;/code&gt; differs. That is worth
saying, because it reframes the question as an operations decision rather than a
performance one.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;179. How do you share a cluster between Spark applications?&lt;/summary&gt;

  &lt;p&gt;At two levels, and keeping them distinct is the answer.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Between applications&lt;/strong&gt;, the cluster manager arbitrates:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;YARN&lt;/strong&gt;: queues with capacity or fair scheduling, so each team or pipeline gets a guaranteed share.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Kubernetes&lt;/strong&gt;: namespaces with resource quotas, and optionally a scheduler that understands gang scheduling.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Standalone&lt;/strong&gt;: limited, mostly &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.cores.max&lt;/code&gt; per application.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;Dynamic allocation matters here, because an application holding idle executors
is holding capacity another application could use.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Within one application&lt;/strong&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.scheduler.mode&lt;/code&gt; governs how concurrent jobs
share slots:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.scheduler.mode&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;FAIR &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.scheduler.allocation.file&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;/etc/spark/fairscheduler.xml
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;sc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;setLocalProperty&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.scheduler.pool&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;interactive&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Default is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FIFO&lt;/code&gt;, under which a long job submitted first starves a short one
submitted second. That is the wrong behaviour for a notebook server or a Thrift
server serving many users from one &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkSession&lt;/code&gt;, which is exactly where &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;FAIR&lt;/code&gt;
and pools belong.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The distinction to state plainly:&lt;/strong&gt; one application cannot share executors with
another, because executors are per-application. So between-application sharing is
about who gets containers, and within-application sharing is about who gets task
slots in the containers you already hold.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;180. How do you pass configuration to a Spark application, and what is the precedence?&lt;/summary&gt;

  &lt;p&gt;Four mechanisms, and the precedence, highest first:&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SparkConf&lt;/code&gt; in code&lt;/strong&gt;, set before the session is created.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-submit&lt;/code&gt; flags&lt;/strong&gt;, including &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--conf&lt;/code&gt; and the named ones like &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--executor-memory&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A properties file&lt;/strong&gt; named with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--properties-file&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-defaults.conf&lt;/code&gt;&lt;/strong&gt; in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SPARK_HOME/conf&lt;/code&gt;.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;SparkSession&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;builder&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;config&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;400&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# beats --conf
&lt;/span&gt;    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;getOrCreate&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;nt&quot;&gt;--properties-file&lt;/span&gt; /etc/spark/job.conf              &lt;span class=&quot;nt&quot;&gt;--load-spark-defaults&lt;/span&gt;              &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.sql.shuffle.partitions&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;200 app.jar
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--load-spark-defaults&lt;/code&gt; makes Spark read &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark-defaults.conf&lt;/code&gt; &lt;strong&gt;as well as&lt;/strong&gt; the
file given by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--properties-file&lt;/code&gt;, which otherwise replaces it. That is useful
when you want cluster-wide defaults plus job-specific overrides.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The traps worth naming:&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--conf&lt;/code&gt; after the application jar&lt;/strong&gt; is passed to your application, not to Spark. No warning.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Options that only apply at JVM start&lt;/strong&gt;, notably &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.extraJavaOptions&lt;/code&gt; in client mode, cannot be set from code.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Executor configs set after the session exists&lt;/strong&gt; do not retroactively change running executors.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;To see what actually applied, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--verbose&lt;/code&gt; prints the resolved configuration and
its source, and the Environment tab of the UI is authoritative at runtime.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;181. How do you add third-party libraries?&lt;/summary&gt;

  &lt;p&gt;Four mechanisms, each for a different situation:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Mechanism&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;For&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Maven coordinates, resolved with transitive dependencies at submit time&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Local or remote jars, added to driver and executor classpaths&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--py-files&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Python &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.zip&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.egg&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.py&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--files&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Arbitrary files placed in each working directory&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--packages&lt;/span&gt; org.apache.hudi:hudi-spark3.5-bundle_2.12:1.2.0 &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--jars&lt;/span&gt; /opt/lib/custom-udfs.jar &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--files&lt;/span&gt; /etc/spark/conf/log4j2.properties &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  app.py
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;For production, prefer not resolving at submit time.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--packages&lt;/code&gt; reaches
out to Maven Central on every run, which makes your job depend on network
availability and on a repository staying up, and makes the resolved set
non-deterministic if a version range is involved. Bake dependencies into an
assembly jar, or into a container image on Kubernetes.&lt;/p&gt;

  &lt;p&gt;Two related notes:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Conflicts are the usual pain.&lt;/strong&gt; If your version collides with one Spark ships, Spark’s usually wins. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-verbose:class&lt;/code&gt; tells you which JAR won, and shading in your assembly is the robust fix.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--jars&lt;/code&gt; does not expand directories,&lt;/strong&gt; and URLs must be comma-separated. The &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;local:&lt;/code&gt; scheme means “already present on every node”, which avoids copying large jars.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;For PySpark specifically, a virtual environment archive shipped with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--archives&lt;/code&gt; is the cleanest way to get consistent Python dependencies on every
executor.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;182. What is the history server and why do you need it?&lt;/summary&gt;

  &lt;p&gt;The Spark UI is served by the driver, so &lt;strong&gt;it dies when the application ends&lt;/strong&gt;.
The history server replays event logs to provide the same UI for completed
applications.&lt;/p&gt;

  &lt;p&gt;Two parts. The application writes a log:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.eventLog.enabled&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.eventLog.dir&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;hdfs:///spark-events &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.eventLog.compress&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;And a separately running server reads that directory:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nb&quot;&gt;export &lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;SPARK_HISTORY_OPTS&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;-Dspark.history.fs.logDirectory=hdfs:///spark-events&quot;&lt;/span&gt;
&lt;span class=&quot;nv&quot;&gt;$SPARK_HOME&lt;/span&gt;/sbin/start-history-server.sh
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;Why you need it is simple:&lt;/strong&gt; almost all investigation happens after the fact. A
job that failed at 03:00 leaves you with only the driver’s stdout unless event
logging was on. Enabling it is close to mandatory in production, and it is the
single cheapest operational improvement for a Spark platform.&lt;/p&gt;

  &lt;p&gt;Operational details worth knowing:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Logs grow,&lt;/strong&gt; so enable compression and set retention (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.history.fs.cleaner.enabled&lt;/code&gt;).&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A very large log is slow to replay,&lt;/strong&gt; which happens with jobs that had hundreds of thousands of tasks.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;On YARN,&lt;/strong&gt; the ResourceManager’s tracking URL redirects to the history server once an application finishes, so links keep working.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;The thing that goes wrong most often is enabling &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.eventLog.dir&lt;/code&gt; on a path
the history server cannot read, so logs are written and never shown.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;183. How do you secure a Spark cluster?&lt;/summary&gt;

  &lt;p&gt;Four layers, and a good answer covers more than one.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Authentication between Spark components.&lt;/strong&gt;&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.authenticate&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.authenticate.secret&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&amp;lt;secret&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;On YARN and Kubernetes the secret can be distributed by the cluster manager
rather than being passed on a command line.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. Encryption in transit.&lt;/strong&gt; RPC encryption with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.network.crypto.enabled&lt;/code&gt;, and separately encryption of shuffle and cached
data spilled to disk with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.io.encryption.enabled&lt;/code&gt;. TLS for the UI and the
history server.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;3. Authorisation and identity.&lt;/strong&gt; Kerberos on Hadoop, with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--principal&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--keytab&lt;/code&gt; so long-running jobs can renew delegation tokens. On Kubernetes,
service accounts and RBAC.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;4. Isolation.&lt;/strong&gt; YARN queues or Kubernetes namespaces, so one tenant cannot
exhaust another’s capacity.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Then the mistakes to avoid, which is where practical knowledge shows:&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Secrets in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;extraJavaOptions&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--conf&lt;/code&gt;&lt;/strong&gt; are visible in the Environment tab of the UI and in process listings. Use a secrets mechanism instead.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The UI has no authentication by default.&lt;/strong&gt; It should not be reachable from an untrusted network; put it behind a proxy with authentication, using &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.ui.filters&lt;/code&gt;.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Event logs contain SQL text and configs,&lt;/strong&gt; so the log directory needs the same access control as the data.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;184. How do you route Spark traffic through an HTTP proxy?&lt;/summary&gt;

  &lt;p&gt;JVM proxy system properties, passed through the same &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;extraJavaOptions&lt;/code&gt;
mechanism as any other JVM flag.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.driver.extraJavaOptions=-Dhttp.proxyHost=proxy.internal -Dhttp.proxyPort=3128 -Dhttps.proxyHost=proxy.internal -Dhttps.proxyPort=3128 -Dhttp.nonProxyHosts=localhost|127.0.0.1|*.internal&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.executor.extraJavaOptions=-Dhttp.proxyHost=proxy.internal -Dhttp.proxyPort=3128 -Dhttps.proxyHost=proxy.internal -Dhttps.proxyPort=3128 -Dhttp.nonProxyHosts=localhost|127.0.0.1|*.internal&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  app.jar
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Three practical points:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Set it on both sides.&lt;/strong&gt; Driver-side covers dependency resolution and metadata calls; executor-side covers whatever your code fetches from tasks.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;http.nonProxyHosts&lt;/code&gt; matters as much as the proxy itself.&lt;/strong&gt; Without it, intra-cluster traffic tries to go through the proxy, which is slow at best and broken at worst. The separator is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;|&lt;/code&gt;, not a comma.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;https.nonProxyHosts&lt;/code&gt; does not exist;&lt;/strong&gt; the HTTP one covers both.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;The reason this question appears in a Spark interview&lt;/strong&gt; is that it is really a
question about whether you understand the delivery mechanism. Proxy settings,
logging configuration, stack size, GC flags and heap dump options all arrive the
same way: through &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.extraJavaOptions&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.extraJavaOptions&lt;/code&gt;. Learning one teaches the rest.&lt;/p&gt;

  &lt;p&gt;And the client-mode caveat applies here too: the driver JVM already exists, so
use &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--driver-java-options&lt;/code&gt; if you are setting it for a client-mode driver.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;185. What should you monitor in production?&lt;/summary&gt;

  &lt;p&gt;Monitor structural signals, not total job duration, because duration moves for
many innocent reasons.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;For batch jobs:&lt;/strong&gt;&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Signal&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Task duration skew, max versus median per stage&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Detects skew before it becomes a failure&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Shuffle spill&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The early sign that partitions are too large&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;GC time per executor&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Predicts heartbeat timeouts and slowdowns&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Executor loss count and reason&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Distinguishes container kills from crashes&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Input file count&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Rising file counts predict planning slowdowns&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Stage retry count&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;A stage running twice means lost shuffle output&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;For streaming jobs:&lt;/strong&gt;&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Signal&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Why&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Processing rate versus input rate&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The definition of keeping up&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;State store row count&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Growth without plateau means a watermark problem&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Consumer lag&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The business-visible measure of freshness&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Batch duration versus trigger interval&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Batches overrunning their window&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;How to collect it:&lt;/strong&gt; event logs plus the history server for post-mortem, and
Spark’s metrics system (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.metrics.conf&lt;/code&gt;) exporting to Prometheus or
StatsD for live dashboards. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;StreamingQueryListener&lt;/code&gt; for streaming.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The reason to frame it this way in an interview:&lt;/strong&gt; anyone can alert on
“job took too long”. Alerting on spill, skew ratio and state growth means you
find out before the job fails, and you know what to do when the alert fires.
That is the difference between monitoring and having a dashboard.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;scenario-based-questions&quot;&gt;Scenario-based questions&lt;/h2&gt;

&lt;p&gt;These separate people who have run Spark in production from people who have read about it. Each is a symptom; the answer is a way of looking, not a fact to recall. Practise saying these out loud.&lt;/p&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;186. A job that took 20 minutes now takes 2 hours, and nothing was deployed. How do you investigate?&lt;/summary&gt;

  &lt;p&gt;No code changed, so something changed in the &lt;strong&gt;data&lt;/strong&gt; or the &lt;strong&gt;cluster&lt;/strong&gt;. Work
through it in this order, because it is cheapest first.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Compare the physical plan against a previous run.&lt;/strong&gt; This finds the most
common cause by far: a broadcast join that no longer broadcasts, because the
dimension table grew past &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.autoBroadcastJoinThreshold&lt;/code&gt;. One join
strategy change can account for the entire regression.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;explain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;formatted&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# and the SQL tab of the history server for the old run
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;2. Check input size and file count separately.&lt;/strong&gt; Ten times the files with the
same bytes changes planning cost and partition packing, which looks like
slowness with no plan change.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;3. Check for new skew.&lt;/strong&gt; On the slowest stage, max versus median task duration
and shuffle read. A newly hot key, or a surge of null keys, concentrates work.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;4. Check the executors tab&lt;/strong&gt; for loss and retries. A stage that now runs twice
because an executor keeps dying doubles the time with no plan difference.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;5. Check cluster contention.&lt;/strong&gt; A noisy neighbour on a shared cluster, or fewer
executors granted than before.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;6. Only then look at configs,&lt;/strong&gt; which did not change, but might now be wrong
for the new data volume.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The answer that scores:&lt;/strong&gt; name the lost broadcast first and say why you would
look there before anything else. It is the single most likely cause of a
step-change regression with unchanged code.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;187. A stage has 200 tasks; 198 finish in seconds and 2 run for an hour. What do you do?&lt;/summary&gt;

  &lt;p&gt;That is skew, and the first thing to do is confirm it rather than assume it.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Confirm.&lt;/strong&gt; On the stage page compare max and median for &lt;strong&gt;shuffle read size&lt;/strong&gt;,
not just duration:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Duration&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Shuffle read&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Cause&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Skew. Fix the distribution&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Max » median&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Roughly even&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Environment. Slow node, GC, bad disk&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;If shuffle read is even, stop treating it as skew and look at the executors tab
for that host.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Then, if it is skew:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Check whether AQE could fire.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.skewJoin.enabled&lt;/code&gt; is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;true&lt;/code&gt;, but a partition must be over 5x the median &lt;strong&gt;and&lt;/strong&gt; over 256 MB. Two tasks running an hour on a job whose partitions are 80 MB will never trigger it. Lowering &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;skewedPartitionThresholdInBytes&lt;/code&gt; may be the whole fix.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Broadcast the small side,&lt;/strong&gt; if there is one. No shuffle, no skew.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Salt the hot key,&lt;/strong&gt; which is the general answer.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Split the hot keys out&lt;/strong&gt; and union, if there are only a few.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;And say what you would not do:&lt;/strong&gt; enable speculation. The duplicate task has
the same rows and is equally slow, so it burns a second slot for nothing and
disguises a data problem as flaky infrastructure.&lt;/p&gt;

  &lt;p&gt;Also check the join key’s null count. Nulls all hash together, and a column that
is 40% null produces exactly this picture.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;188. Your job writes 50,000 tiny Parquet files. Why, and how do you fix it?&lt;/summary&gt;

  &lt;p&gt;It is arithmetic. Every task that holds rows for a partition value writes a file
into that value’s directory:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;files = tasks x partition values
      = 200 shuffle partitions x 250 cities
      = up to 50,000
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Each task has a few rows for almost every city, so it opens a writer for almost
every city.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The fix is to make each value live in one task,&lt;/strong&gt; by repartitioning on the same
columns you partition by:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;repartition&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;overwrite&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;parquet&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;That gives 250 files instead of 50,000.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Then check three related things:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Is the partition column the right cardinality?&lt;/strong&gt; 250 is fine; 50,000 distinct values is not, and directory-per-value is the wrong layout. Consider a coarser column, or bucketing.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Cap file size for the big partitions.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.files.maxRecordsPerFile&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;0&lt;/code&gt;, splits one enormous partition into several sensible files.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Watch for skew from the repartition itself.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;repartition(&quot;city_id&quot;)&lt;/code&gt; hash-partitions, so a dominant city becomes one large task. That may be an acceptable trade for the file layout, or may need salting.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;Why it matters beyond tidiness:&lt;/strong&gt; every later read pays listing cost, per-file
open cost (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;openCostInBytes&lt;/code&gt;, default &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;4MB&lt;/code&gt; each), and worse compression. The
write is slow too, because 50,000 files on object storage is 50,000 PUTs.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;189. The driver runs out of memory on a job that only aggregates. What happened?&lt;/summary&gt;

  &lt;p&gt;“Only aggregates” means the result should be small, so something is pulling data
back or the driver is tracking too much.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Check these in order:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Is there a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;collect()&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;toPandas()&lt;/code&gt;, or a wide &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show()&lt;/code&gt;?&lt;/strong&gt; Even a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;show(1000)&lt;/code&gt; on a very wide row is a lot of data. This is the most common answer.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Is something being broadcast?&lt;/strong&gt; The driver must assemble a broadcast side before sending it, so a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;broadcast()&lt;/code&gt; hint on a large table fails on the driver, not the executors. Check the plan for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastHashJoin&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BroadcastExchange&lt;/code&gt; and the size of what feeds it.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;How many tasks?&lt;/strong&gt; The driver holds metrics and state per task. A job with hundreds of thousands of tasks, from tiny input files or an enormous shuffle partition count, needs real memory just for bookkeeping.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Is the plan deep or very wide?&lt;/strong&gt; Analysis holds the tree in driver memory; a loop of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;withColumn&lt;/code&gt; calls or a 5,000-column schema adds up.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Is there a driver-side loop accumulating results?&lt;/strong&gt; Appending each iteration’s output to a Python list is a classic.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;get&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.driver.maxResultSize&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# 1g
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The distinction worth drawing:&lt;/strong&gt; a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;maxResultSize&lt;/code&gt; error is Spark protecting
you with a clear named failure, while an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt; means the data
arrived some other way, usually a broadcast. So the error type narrows the
search.&lt;/p&gt;

  &lt;p&gt;And note &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.driver.memory&lt;/code&gt; defaults to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;1g&lt;/code&gt;, which is a coordinator’s
budget.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;190. Executors keep being killed for exceeding memory limits. What is your sequence?&lt;/summary&gt;

  &lt;p&gt;&lt;strong&gt;1. Confirm which failure it is,&lt;/strong&gt; because the two have different fixes:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Message&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Means&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Container killed by YARN/Kubernetes for exceeding limits&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The &lt;strong&gt;process total&lt;/strong&gt; exceeded heap plus overhead&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;memoryOverhead&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;java.lang.OutOfMemoryError: Java heap space&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;The &lt;strong&gt;heap&lt;/strong&gt; filled&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Partitions, then &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;executor.memory&lt;/code&gt;&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;A container kill is not a JVM error, and reading the message rather than
assuming is the first step.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. Raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memoryOverhead&lt;/code&gt;.&lt;/strong&gt; Overhead holds off-heap buffers,
Netty’s direct buffers, metaspace, thread stacks, and &lt;strong&gt;the Python workers&lt;/strong&gt;.
PySpark jobs hit this far more often for exactly that reason, and can be killed
with a perfectly healthy heap.&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.executor.memoryOverhead&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;4g
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;3. Check for skew,&lt;/strong&gt; because one oversized partition is a very common root
cause that presents as a memory shortage. Fixing the distribution fixes the
memory.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;4. Raise the partition count&lt;/strong&gt; so each task’s footprint shrinks. This scales
with your data, whereas a memory bump does not.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;5. Reduce cores per executor,&lt;/strong&gt; which gives each running task a larger share
of the same pool.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;6. Raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.memory&lt;/code&gt;,&lt;/strong&gt; last.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The judgement to voice:&lt;/strong&gt; raising memory first usually just moves the failure
to next month’s data volume. Steps 3 and 4 fix it structurally.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;191. A `FetchFailedException` keeps failing your job. What is the real problem?&lt;/summary&gt;

  &lt;p&gt;Almost never the fetch settings. The blocks were written; whatever was serving
them can no longer do so.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Look at the executors tab, not the network config.&lt;/strong&gt; The question is which
executor was serving those blocks and why it stopped:&lt;/p&gt;

  &lt;table&gt;
    &lt;thead&gt;
      &lt;tr&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Finding&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Root cause&lt;/th&gt;
        &lt;th style=&quot;text-align: left&quot;&gt;Fix&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Killed for exceeding memory&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Overhead, skew, or partition size&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Those, not fetch retries&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Partition too large&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;More partitions&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Alive but very high GC time&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Too busy to serve&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Memory, or fewer cores&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Node lost&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Hardware or preemption&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Retries, and an external shuffle service&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Disk full&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;Spill filled local disk&lt;/td&gt;
        &lt;td style=&quot;text-align: left&quot;&gt;More disk, or less spill&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;

  &lt;p&gt;&lt;strong&gt;What Spark does on its own:&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DAGScheduler&lt;/code&gt; treats it as lost map output and
&lt;strong&gt;resubmits the upstream stage&lt;/strong&gt; to regenerate it. So the job often recovers
slowly, and the tell is a stage that runs twice.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The structural fix&lt;/strong&gt; is the &lt;strong&gt;external shuffle service&lt;/strong&gt;, or shuffle
tracking, so shuffle output is served by a node-level daemon that outlives the
executor:&lt;/p&gt;

  &lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; spark.shuffle.service.enabled&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;true&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;What not to do:&lt;/strong&gt; raise &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.shuffle.io.maxRetries&lt;/code&gt; and
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.network.timeout&lt;/code&gt; and call it fixed. That makes the job take longer to
fail, and hides the executor deaths that are the actual problem.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;192. You have a 2 TB fact table and a 3 GB dimension table to join. How?&lt;/summary&gt;

  &lt;p&gt;3 GB is 300 times the 10 MB broadcast threshold, so a broadcast is out &lt;strong&gt;as
things stand&lt;/strong&gt;. The work is to change that, or to make the shuffle good.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. Try to make the dimension small.&lt;/strong&gt; This is the highest-value step and the
most often skipped. Filter and project it first:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;dim&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;dimension&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;active&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;select&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;region&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;        &lt;span class=&quot;c1&quot;&gt;# 3 GB may become 40 MB
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;A 3 GB dimension is usually 3 GB because of columns and rows the query never
needs. If this gets it under the threshold, the problem disappears.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. If it cannot shrink, accept a sort-merge join&lt;/strong&gt; and make it well-shaped:
filter the fact side first, size the shuffle with
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.sql.adaptive.advisoryPartitionSizeInBytes&lt;/code&gt;, and handle skew on the join
key.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;3. If this join is recurring, bucket both tables&lt;/strong&gt; on the join key with the
same bucket count. That removes the shuffle permanently, at the cost of a one-off
write and some rigidity.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;4. If the fact table is partitioned on the join key&lt;/strong&gt; and the dimension is
filtered, &lt;strong&gt;dynamic partition pruning&lt;/strong&gt; may already be limiting the fact scan.
Check for &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dynamicpruningexpression&lt;/code&gt; in the plan.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;And the thing not to do:&lt;/strong&gt; hint &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BROADCAST&lt;/code&gt; on 3 GB. The driver must collect it
first, so you convert a slow join into a driver &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OutOfMemoryError&lt;/code&gt;, which is
harder to diagnose.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;193. Your streaming query&apos;s state grows until the job dies. Why?&lt;/summary&gt;

  &lt;p&gt;A stateful operation whose state is never released. In practice one of four
causes.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;1. No watermark on a stateful operation.&lt;/strong&gt; Without one, no window is ever
final, so nothing is dropped.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;2. The watermark column is not event time.&lt;/strong&gt; The classic mistake is using the
source’s ingest timestamp:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# wrong: Kafka&apos;s timestamp is broker ingest time
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;timestamp&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10 minutes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# right: event time from the payload
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;events&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withWatermark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;10 minutes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;If the watermark column advances with processing time rather than event time it
still moves, so this can look like it is working while behaving wrongly for late
data.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;3. The watermark is attached in the wrong place.&lt;/strong&gt; It must be applied to the
stream &lt;strong&gt;before&lt;/strong&gt; the aggregation that needs it. Applied after, it does nothing.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;4. The aggregation is unbounded by construction.&lt;/strong&gt; A non-windowed &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupBy&lt;/code&gt;
keeps one entry per key forever, and no watermark can expire a key. That one
needs a restructure, usually into a windowed aggregation, not a config.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Confirm which it is from the progress metrics:&lt;/strong&gt;&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;query&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;lastProgress&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;stateOperators&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;])&lt;/span&gt;   &lt;span class=&quot;c1&quot;&gt;# numRowsTotal over time
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;State that &lt;strong&gt;plateaus&lt;/strong&gt; is healthy. State that climbs monotonically is the
signature. Then check the event-time gap in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;lastProgress&lt;/code&gt;, which tells you
whether the watermark is advancing at all.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;194. A nightly job silently produced half the expected rows. How do you find out why?&lt;/summary&gt;

  &lt;p&gt;Silent wrong results point at &lt;strong&gt;semantics&lt;/strong&gt;, not performance, and Spark has a
handful of well-known ways to lose rows quietly.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The candidates, most common first:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;An inner join dropping non-matches&lt;/strong&gt;, including rows whose key is null, because null never equals null. Half the rows is a very plausible join-coverage figure.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;union&lt;/code&gt; matching by position&lt;/strong&gt; where the two sides’ column orders differ. This corrupts rather than drops, but produces nonsense that a later filter then drops.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A filter against a sometimes-null column.&lt;/strong&gt; &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;col != &apos;x&apos;&lt;/code&gt; is null, not true, for null rows, so they are excluded.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A changed upstream schema,&lt;/strong&gt; so a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;select&lt;/code&gt; picks up a renamed column as null and a downstream filter removes it.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;ANSI mode on a Spark 4 upgrade,&lt;/strong&gt; now raising or changing results where invalid casts previously yielded null.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Dynamic partition overwrite&lt;/strong&gt; replacing more or fewer partitions than intended.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;The method, which matters more than the list:&lt;/strong&gt; instrument row counts at each
stage and find where they fall off.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;for&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;d&lt;/span&gt; &lt;span class=&quot;ow&quot;&gt;in&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;[(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;raw&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;raw&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;filtered&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;filtered&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;joined&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;joined&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)]:&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;d&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;())&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;Then for the join specifically:&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;raw&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;join&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;dim&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;left_anti&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# rows that found no match
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;raw&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;city_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;isNull&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;count&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;      &lt;span class=&quot;c1&quot;&gt;# null keys
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;The habit to convey:&lt;/strong&gt; for a silent correctness problem, reach for counts and
anti-joins, not the UI. The UI tells you about speed, not about semantics.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;195. You must deduplicate a CDC feed to the latest row per key. How?&lt;/summary&gt;

  &lt;p&gt;A window function is the idiomatic batch answer.&lt;/p&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Window&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;from&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;pyspark.sql.functions&lt;/span&gt; &lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;row_number&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;desc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;w&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;Window&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;partitionBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;trip_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;orderBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;desc&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;updated_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;

&lt;span class=&quot;n&quot;&gt;latest&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;changes&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;row_number&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;().&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;over&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;w&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;where&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;drop&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rn&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;That shuffles once, by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trip_id&lt;/code&gt;.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The things to check, which is what the question is really about:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Is the key skewed?&lt;/strong&gt; The window partitions by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;trip_id&lt;/code&gt;, so one enormous key is one task. Check the distribution first.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Is the ordering column unique enough?&lt;/strong&gt; Ties in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;updated_at&lt;/code&gt; make the winner arbitrary. Add a tiebreaker such as an offset or a sequence number, or the result is non-deterministic between runs.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Are deletes represented?&lt;/strong&gt; A CDC feed usually carries an operation column, and the latest row for a key may be a delete, which should remove the row rather than keep it.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;latest&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;op&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;!=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;D&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;Alternatives worth naming:&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dropDuplicates([&quot;trip_id&quot;])&lt;/code&gt;&lt;/strong&gt; is cheaper but keeps an arbitrary row, not the latest. Only correct if the feed is already ordered, which it usually is not.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;For a stream&lt;/strong&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dropDuplicates&lt;/code&gt; with a watermark bounds the state, which is the streaming equivalent.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;In a lakehouse table&lt;/strong&gt;, this is what an upsert does natively: Hudi with a record key and an ordering field applies exactly this rule on write, so the deduplication becomes the table’s job rather than the query’s.&lt;/li&gt;
  &lt;/ul&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;196. A query works on a sample but fails at full scale with spill and no progress. What now?&lt;/summary&gt;

  &lt;p&gt;The shape that works at one scale rarely fails at another for a config reason.
It fails because &lt;strong&gt;partition sizes scaled with the data&lt;/strong&gt; and the per-task
working set outgrew its memory.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The arithmetic to check first:&lt;/strong&gt;&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;per-task execution memory ~= (heap - 300MB) x spark.memory.fraction / cores
                          ~= (16g - 300MB) x 0.6 / 4
                          ~= 2.4 GB

partition size = total shuffle bytes / partition count
               = 4 TB / 200
               = 20 GB per task        &amp;lt;- far too large
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;200 partitions was fine for the sample and is absurd at full scale.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Then, in order:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Raise the partition count, or lower the advisory size,&lt;/strong&gt; so partitions land in the low hundreds of megabytes.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.shuffle.partitions&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;4000&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;spark&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;conf&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;set&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;128MB&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Look for skew that the sample hid.&lt;/strong&gt; A sample of 1% of rows may contain none of the hot keys, so a job that looked balanced is not.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check for an aggregation whose cardinality scaled.&lt;/strong&gt; A &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;groupBy&lt;/code&gt; with 1,000 groups in the sample and 50 million at full scale is a different operation.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Check whether something cached no longer fits,&lt;/strong&gt; and is now evicting execution memory on every stage.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;The framing that scores:&lt;/strong&gt; say explicitly that a sample validates &lt;em&gt;logic&lt;/em&gt;, not
&lt;em&gt;shape&lt;/em&gt;, and that partition sizing is the first thing to recompute at a new
scale.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;197. You need to read 10 TB but only one day of it. The job reads everything. Why?&lt;/summary&gt;

  &lt;p&gt;The filter is not pruning, and there are only a few possible reasons.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Check the plan first.&lt;/strong&gt; Two fields answer it:&lt;/p&gt;

  &lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;PartitionFilters: [isnotnull(dt#42), (dt#42 = 2026-09-17)]     &amp;lt;- pruning works
PushedFilters:    [IsNotNull(started_at), GreaterThan(...)]     &amp;lt;- source filtering works
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;If your date predicate appears in &lt;strong&gt;neither&lt;/strong&gt;, nothing is being skipped.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The causes:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;The table is not partitioned on the column you filter.&lt;/strong&gt; Filtering &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;started_at&lt;/code&gt; on a table partitioned by &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;dt&lt;/code&gt; prunes nothing, because Spark cannot know the relationship. Filter the partition column.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The predicate is wrapped in a function.&lt;/strong&gt; This is the most common, and it is subtle:&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;c1&quot;&gt;# cannot prune: the column is inside a function
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;to_date&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-17&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;c1&quot;&gt;# can prune
&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;filter&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;2026-09-17&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;amp;&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;col&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;started_at&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;lt;&lt;/span&gt;  &lt;span class=&quot;s&quot;&gt;&quot;2026-09-18&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;The type does not match.&lt;/strong&gt; Comparing a string partition column to a date literal, or vice versa, can prevent pruning depending on the cast direction.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;The filter is applied after a join or aggregation,&lt;/strong&gt; so it is no longer adjacent to the scan and cannot be pushed through.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;A UDF in the predicate,&lt;/strong&gt; which is opaque to Catalyst.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;And the structural note:&lt;/strong&gt; for a table where this keeps happening, a lakehouse
format prunes on column statistics rather than only directory names, which makes
the layout less brittle.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;198. Two jobs writing the same table produce corrupt or missing data. What is wrong?&lt;/summary&gt;

  &lt;p&gt;A directory of Parquet files has &lt;strong&gt;no commit protocol&lt;/strong&gt;, so concurrent writers
have no way to coordinate and a failed writer has no way to roll back.&lt;/p&gt;

  &lt;p&gt;What goes wrong concretely:&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;&lt;strong&gt;Interleaved output.&lt;/strong&gt; Two jobs writing the same partition both add files, and readers see the union, which is neither job’s intended result.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Partial output on failure.&lt;/strong&gt; A job that dies halfway leaves the files it already wrote, with nothing marking them as incomplete.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;overwrite&lt;/code&gt; racing &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;append&lt;/code&gt;.&lt;/strong&gt; The overwrite deletes files the appender is adding, or vice versa.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Readers seeing a moving target,&lt;/strong&gt; because there is no snapshot isolation: a scan that lists files, then reads them, can have the list change underneath it.&lt;/li&gt;
  &lt;/ul&gt;

  &lt;p&gt;&lt;strong&gt;The two fixes, and they are different in kind:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Serialise the writers.&lt;/strong&gt; An orchestration-level lock, or a single job. Correct, and limiting.&lt;/li&gt;
    &lt;li&gt;&lt;strong&gt;Use a table format with atomic commits.&lt;/strong&gt; Hudi, Iceberg or Delta add a commit protocol over the same Parquet files: a reader sees a consistent snapshot, a failed write commits nothing, and concurrent writers are arbitrated by the format rather than by luck.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;write&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;iceberg&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;append&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;save&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;prod.db.trips&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;p&gt;&lt;strong&gt;This is the structural answer and worth stating as such:&lt;/strong&gt; it is not a Spark
tuning problem, it is a missing transaction boundary. Lakehouse formats exist
precisely for this, and saying so shows you understand why they were built rather
than just that they exist.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;199. A `groupBy` on a high-cardinality column is unbearably slow. Options?&lt;/summary&gt;

  &lt;p&gt;First ask whether the aggregation is needed at that cardinality, because the
cheapest fix is usually to not do it.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;Then, in order:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;Is the output actually consumed at that granularity?&lt;/strong&gt; An aggregation to 50 million groups that then feeds a join on a coarser key can often be reordered: join first, aggregate second.&lt;/p&gt;
    &lt;/li&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;Is an approximation acceptable?&lt;/strong&gt; Exact &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;countDistinct&lt;/code&gt; requires a second shuffle; the approximate version does not:&lt;/p&gt;
    &lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;approx_count_distinct&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;rsd&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;mf&quot;&gt;0.02&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;     &lt;span class=&quot;c1&quot;&gt;# instead of countDistinct
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;ol&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;Is the shuffle sized for the cardinality?&lt;/strong&gt; 50 million groups across 200 partitions is 250,000 groups per task, and the hash map for that may not fit. Raise the partition count so each task’s map is smaller.&lt;/p&gt;
    &lt;/li&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;Is there skew within the groups?&lt;/strong&gt; High cardinality and skew often coexist: millions of small groups plus a handful of enormous ones. Two-phase aggregation with a salt handles the large ones:&lt;/p&gt;
    &lt;/li&gt;
  &lt;/ol&gt;

  &lt;div class=&quot;language-python highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;df&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;withColumn&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;rand&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;32&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;cast&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;int&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&quot;salt&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;fare&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;p&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;
   &lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;groupBy&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;rider_id&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;agg&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;F&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;sum&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;p&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;alias&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&quot;total&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;  &lt;/div&gt;

  &lt;ol&gt;
    &lt;li&gt;&lt;strong&gt;Is the output written partitioned by that column?&lt;/strong&gt; Then high cardinality is also a small-files problem, and the layout needs rethinking.&lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;The framing:&lt;/strong&gt; high cardinality is not itself a problem; the problem is a
per-task hash map that does not fit, and the lever is partition count before
memory.&lt;/p&gt;

&lt;/details&gt;

&lt;details class=&quot;qa&quot;&gt;
  &lt;summary&gt;200. Your team wants to move a Spark job&apos;s source table from Hudi to Iceberg. What do you consider?&lt;/summary&gt;

  &lt;p&gt;First separate the layers, because the question conflates them. &lt;strong&gt;Spark is the
compute engine; Hudi and Iceberg are table formats.&lt;/strong&gt; The job’s Spark logic
barely changes. What changes is write semantics, read reach and operations.&lt;/p&gt;

  &lt;p&gt;&lt;strong&gt;The questions that actually decide it:&lt;/strong&gt;&lt;/p&gt;

  &lt;ol&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;What is the write pattern?&lt;/strong&gt; High-frequency keyed upserts are what Hudi’s record index is built for: a key lookup finds its file group directly. Iceberg has no record index, so a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;MERGE&lt;/code&gt; plans a join whose cost scales with how much of the table it touches. If the job is CDC ingestion, this is the dominant consideration.&lt;/p&gt;
    &lt;/li&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;Which engines must read it?&lt;/strong&gt; Iceberg has the widest warehouse support, and if a warehouse that only reads Iceberg is the reason for the move, that is a legitimate and sufficient reason.&lt;/p&gt;
    &lt;/li&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;Who operates the table services?&lt;/strong&gt; Both need compaction and cleanup, but they are exposed differently: Hudi ships them as configurable services that can run inline or async, Iceberg as procedures you schedule (&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rewrite_data_files&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;expire_snapshots&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;remove_orphan_files&lt;/code&gt;).&lt;/p&gt;
    &lt;/li&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;What is the migration mechanism?&lt;/strong&gt; Iceberg has &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;snapshot&lt;/code&gt; and &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;migrate&lt;/code&gt; procedures for in-place adoption of existing Parquet. A full rewrite of a large table is a real cost.&lt;/p&gt;
    &lt;/li&gt;
    &lt;li&gt;
      &lt;p&gt;&lt;strong&gt;What breaks?&lt;/strong&gt; Incremental reads, time travel syntax and concurrency configuration all differ, so downstream consumers need changes even though the Spark job does not.&lt;/p&gt;
    &lt;/li&gt;
  &lt;/ol&gt;

  &lt;p&gt;&lt;strong&gt;And the option people forget:&lt;/strong&gt; if the answer is “both, for different
readers”, Apache XTable converts metadata in place without copying data, so one
set of Parquet files can be read as either. That reframes the decision from a
migration to a conversion.&lt;/p&gt;

&lt;/details&gt;

&lt;h2 id=&quot;how-to-use-this-in-an-actual-interview&quot;&gt;How to use this in an actual interview&lt;/h2&gt;

&lt;p&gt;Three habits matter more than recall.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Say what it depends on.&lt;/strong&gt; Most Spark questions have a “depends” in them, and
naming the dependency &lt;em&gt;is&lt;/em&gt; the answer. “Should you cache?” depends on whether
the dataset is reused and whether producing it was expensive. An answer that
names the condition beats one that picks a side.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Reach for the mechanism, not the knob.&lt;/strong&gt; “Raise the memory” is a weak answer
to a spill question. “The partitions are too large for the memory each task
gets, so I would raise parallelism first and check for skew” is a strong one.
The knob comes last, after the cause.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Refuse to invent numbers.&lt;/strong&gt; If you do not know a default, say you would check
the Environment tab or the configuration reference. Interviewers notice
confident wrong defaults far more than an honest “I would look it up”, and one
invented number makes everything else you said suspect.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;Two hundred questions is a lot of surface, and it collapses into four ideas.&lt;/p&gt;

&lt;p&gt;Work is &lt;strong&gt;lazy&lt;/strong&gt; until an action asks for a result, which is what allows the
whole plan to be optimised rather than each call executed as it arrives.
&lt;strong&gt;Partitions&lt;/strong&gt; are the unit of parallelism, so nearly every performance question
is really about how many you have and how evenly the data sits across them.
&lt;strong&gt;Shuffles&lt;/strong&gt; are the expensive thing, and stage boundaries fall exactly where
they occur, which is why counting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Exchange&lt;/code&gt; nodes tells you so much. And the
&lt;strong&gt;driver&lt;/strong&gt; holds all the state that cannot be recomputed, which is why it is the
one process with no redundancy.&lt;/p&gt;

&lt;p&gt;Derive an answer from those four and you will handle questions this list does
not contain, which is the actual goal. The scenario section is where that shows:
nobody can memorise the answer to “it was 20 minutes and now it is 2 hours”,
because the answer is a way of looking rather than a fact.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/configuration.html&quot;&gt;Spark configuration reference&lt;/a&gt; for every property, its default and the version it appeared in&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/rdd-programming-guide.html&quot;&gt;RDD programming guide&lt;/a&gt; for lazy evaluation, persistence and shared variables&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://spark.apache.org/docs/latest/sql-performance-tuning.html&quot;&gt;SQL performance tuning&lt;/a&gt; for adaptive query execution and join hints&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheSparkArchitecture/&quot;&gt;Apache Spark architecture&lt;/a&gt; for the execution model behind these answers&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheSparkCheatSheet/&quot;&gt;Apache Spark: the complete cheat sheet&lt;/a&gt; for the config surface in one page&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Spark, Apache Hadoop, Apache Hive, Apache Parquet, Apache ORC, Apache Avro, Apache Kafka, Apache Arrow, Apache Hudi, Apache Iceberg, Apache XTable (incubating), Apache Mesos, Apache Log4j and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries. Delta Lake is a trademark of the Linux Foundation. Kubernetes is a registered trademark of the Linux Foundation.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Spark" /><category term="Spark" /><category term="Interview" /><category term="Reference" /><category term="Scenarios" /><summary type="html">How this is organised Spark fundamentals RDDs DataFrames, Datasets and Spark SQL Architecture and execution Partitioning and shuffle Memory, caching and persistence Joins, Catalyst and adaptive execution Performance tuning and troubleshooting Structured Streaming Deployment, cluster managers and operations Scenario-based questions How to use this in an actual interview Conclusion References Trademarks TL;DR 200 questions in 11 sections, each answered properly: the direct answer, the mechanism underneath, a worked example, and the trap. Click any question to open it. The last 15 are scenario questions: a symptom, and how to reason to the cause. Practise those out loud, because they are what separate people who have run Spark in production. Where a default surprises people the number is given, because a confidently wrong default is worse in an interview than an honest “I would look it up”. The answers say where the textbook version is wrong: AQE skew handling needs a partition over 256 MB before it fires, input partitions do not come from file count, accumulators double-count outside actions, and coalesce(1) can make an entire pipeline single-threaded. No speedup ratios anywhere. Where performance is the point, the answer names the mechanism and leaves the measuring to you. Interview lists tend to collect questions without checking the answers, so they repeat things that were true in Spark 1.6 and quote defaults that changed three releases ago. They also tend to answer in two lines, which is enough to recognise a term and not enough to defend it when someone asks “why”. This one is built the other way round. The answers came first, from source and documentation at v4.2.0, and each one is written to survive a follow-up question: what the mechanism is, what the numbers actually are, and where the common version of the answer is wrong. Use it two ways. Read a section end to end to find the gaps in your own model, or open a single question when you want the defensible version of something you already half know. The sections run roughly from basics to advanced, and the scenario section at the end is the one worth rehearsing aloud. A note on what is deliberately absent: there are no benchmark numbers or speed multipliers. Where performance is the point, the answer explains the mechanism that makes one path cheaper and leaves the measuring to you, on your data and your cluster. How this is organised Section Questions Focus Spark fundamentals 1-20 Vocabulary and the execution model RDDs 21-40 Lineage, partitioning, shared variables DataFrames, Datasets and Spark SQL 41-62 Catalyst, plans, formats Architecture and execution 63-84 Driver, executors, schedulers Partitioning and shuffle 85-104 The main source of slowness Memory, caching and persistence 105-122 The unified memory model Joins, Catalyst and adaptive execution 123-142 Strategy selection and AQE Performance tuning and troubleshooting 143-160 Diagnosis before knobs Structured Streaming 161-175 Watermarks, state, guarantees Deployment, cluster managers and operations 176-185 Running it for real Scenario-based questions 186-200 Symptom to cause The diagrams render when you open a question, so nothing is drawn until you ask for it. Spark fundamentals Start here. These come up in the first ten minutes of almost every interview, and the answers below give you the version you can defend rather than the version everyone recites. 1. What is Apache Spark? Apache Spark is a distributed engine for large-scale data processing. You describe a computation over a dataset, and Spark splits both the data and the work across a cluster of machines, coordinating the whole thing from a single process called the driver. Three properties define it: Distributed execution. A dataset is divided into partitions, and each partition is processed independently, in parallel, on whichever machine has capacity. Lazy evaluation. Operations build a plan rather than running immediately, which lets the engine optimise the whole computation before any of it starts. Fault tolerance through lineage. Spark records how each partition was derived, so a lost one is rebuilt rather than the job being restarted. It ships APIs for Scala, Java, Python and R, plus libraries for SQL, structured streaming, machine learning and graph processing, all on the same execution engine. The framing that lands well in an interview: Spark is a compute engine, not a storage system and not a table format. It reads from HDFS, S3, JDBC or Kafka, and writes back out. Confusing Spark with Hudi, Iceberg or Delta is a common mistake, and being crisp about the boundary is a good signal. 2. Why use Spark instead of a single machine? Because one of two things has become true: the data no longer fits the memory, CPU or disk of one machine, or it fits but takes unacceptably long to process serially. Spark’s answer to both is partitioning. A 1 TB dataset might become 8,000 partitions of roughly 128 MB, and if the cluster has 200 cores then 200 of them are processed at a time. 1 TB input | v split into partitions +------+------+------+ +--------+ | P1 | P2 | P3 | ... | P8000 | +------+------+------+ +--------+ | | v processed in parallel v across 200 cores | v result The part candidates usually miss is the cost. Distribution is not free: there is serialization, network transfer during shuffles, scheduling overhead per task, and the coordination cost of the driver. For a dataset that fits comfortably on one machine, a single-process tool such as pandas, DuckDB or Polars will routinely beat a Spark cluster, because Spark’s overhead is not amortised. A good answer names the crossover point rather than claiming Spark is always faster. Spark earns its keep when the work genuinely exceeds one machine, or when you need fault tolerance across a long-running job. 3. What is the difference between Spark and Hadoop MapReduce? The structural difference is where intermediate results live and how much of the computation the engine can see at once. MapReduce runs one map phase and one reduce phase, and writes the intermediate result to disk, usually replicated, between them. A multi-step algorithm becomes a chain of MapReduce jobs, each re-reading the previous one’s output from disk. Spark keeps intermediate data in memory where it can, and expresses an entire chain of operations as one plan before running any of it. That gives it two advantages MapReduce structurally cannot have:   MapReduce Spark Intermediate data Written to disk between stages Kept in memory where possible Unit of work One map plus one reduce An arbitrary DAG of stages Optimisation scope Per job The whole plan, via Catalyst Iterative workloads Re-reads input each pass Can cache and reuse APIs Map and reduce SQL, DataFrames, streaming, ML, graph The iterative case is where the gap is widest. Ten passes of an algorithm in MapReduce means ten round trips through disk; in Spark the dataset can be cached once and reused. The honest framing for an interview: the advantage is in-memory intermediate data plus whole-plan optimisation, not a fixed speed multiplier. The often-quoted “100x” comes from a specific iterative benchmark and does not describe a typical batch job. Quote a number only if you measured it on your own data, and otherwise describe the mechanism. 4. What is lazy evaluation? Transformations do not compute anything when you call them. Each one records what to do and returns a new dataset describing the result. Nothing executes until an action asks for a value. The RDD programming guide states it directly: All transformations in Spark are lazy, in that they do not compute their results right away. Instead, they just remember the transformations applied to some base dataset. Why it matters is the important half. Because Spark holds the whole chain before running it, Catalyst can rewrite it: push a filter down into the scan so fewer rows are read, drop columns nobody selected, fuse adjacent operations into one generated loop, and choose a join strategy knowing what comes next. None of that is possible for an engine that executes each call as it arrives. flowchart TB T[&quot;transformations called:&amp;lt;br/&amp;gt;read, filter, withColumn, groupBy&quot;] T --&amp;gt; P[(&quot;plan recorded&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;0 jobs, 0 tasks&amp;lt;/b&amp;gt;&quot;)] P -.-&amp;gt;|&quot;nothing has run&quot;| W[&quot;cluster is idle&quot;] A[&quot;an action is called:&amp;lt;br/&amp;gt;count()&quot;] --&amp;gt; R[&quot;&amp;lt;b&amp;gt;1 job&amp;lt;/b&amp;gt;: Catalyst optimises&amp;lt;br/&amp;gt;the whole plan, then runs it&quot;] P --&amp;gt; R There are two consequences to be ready for: A dataset is recomputed every time an action needs it, unless you persist it. The guide is explicit: “each transformed RDD may be recomputed each time you run an action on it.” Errors surface at the action, not at the transformation. A typo in a column name in a filter may only raise when you call count(), which is why analysis errors seem to appear far from their cause. 5. What is the difference between a transformation and an action? A transformation creates a new dataset from an existing one and runs nothing. An action returns a value to the driver or writes to storage, and that is what submits a job.   Transformation Action Examples map, filter, select, groupBy, join, repartition count, collect, show, first, take, write, foreach Returns A new dataset A value to the driver, or a write Triggers execution No Yes, one job Transformations divide further, and the distinction drives everything about performance: Narrow transformations, where each input partition feeds at most one output partition, so the work pipelines inside a single task: map, filter, mapPartitions. Wide transformations, where an output partition draws from many input partitions, so data must move across the network: groupByKey, join, distinct, repartition. The count of actions tells you how many jobs you will run, and the count of wide transformations tells you how many stages each job will have. One trap worth naming: show() and take(n) are actions, so calling them during development runs the whole chain up to that point. A notebook with ten show() calls has run ten jobs, and if nothing is cached, much of the work is repeated each time. 6. How can you prove that transformations are lazy? Count the jobs. Chaining transformations submits none; the action submits one. sc = spark.sparkContext rdd = sc.parallelize(range(4), 2) doubled = rdd.map(lambda x: x * 2).filter(lambda x: x &amp;gt; 2) print(len(sc.statusTracker().getJobIdsForGroup() or [])) # 0 doubled.count() print(len(sc.statusTracker().getJobIdsForGroup() or [])) # 1 Two transformations, zero jobs. One action, one job. A second demonstration is more visual: put a side effect in a transformation and watch when it happens. acc = sc.accumulator(0) counted = sc.parallelize(range(100), 4).map(lambda x: (acc.add(1), x)[1]) print(acc.value) # 0, the map has not run counted.count() print(acc.value) # 100, it ran when the action asked The same trick exposes the other half of laziness. Call count() a second time and the accumulator reads 200, for the same 100 rows, because the uncached dataset was recomputed from scratch. Add .cache() and it stays at 100. In an interview, the accumulator version is the stronger answer, because it demonstrates laziness and recomputation in one go. 7. What is a partition? A partition is the unit of parallelism: a slice of the dataset that one task processes on one core. A stage runs exactly one task per partition, so the partition count is the maximum parallelism available to that stage. flowchart TB subgraph S[&quot;one stage&quot;] P1[&quot;partition 0&quot;] --&amp;gt; T1[&quot;task 0&quot;] P2[&quot;partition 1&quot;] --&amp;gt; T2[&quot;task 1&quot;] P3[&quot;partition 2&quot;] --&amp;gt; T3[&quot;task 2&quot;] P4[&quot;partition 3&quot;] --&amp;gt; T4[&quot;task 3&quot;] end T1 --&amp;gt; E[&quot;executors run&amp;lt;br/&amp;gt;as many as they have slots&quot;] T2 --&amp;gt; E T3 --&amp;gt; E T4 --&amp;gt; E Three numbers follow from this, and almost every tuning question is one of them: How many partitions. Too few and cores sit idle; too many and per-task scheduling overhead dominates. A useful target is partitions in the low hundreds of megabytes, with several tasks per core so stragglers average out. How evenly the data is spread. One partition holding most of the rows is skew, and because a task cannot be split, that one task sets the stage duration. Where they come from. On read, from file splitting; after a shuffle, from spark.sql.shuffle.partitions or adaptive coalescing. Check the number rather than assume it: print(df.rdd.getNumPartitions()) 8. What is `SparkSession` and how does it relate to `SparkContext`? SparkSession is the entry point for the DataFrame and SQL APIs, and it is what modern code creates. SparkContext is the older, lower-level entry point for RDDs and for cluster coordination. from pyspark.sql import SparkSession spark = (SparkSession.builder .appName(&quot;trips-ingest&quot;) .config(&quot;spark.sql.shuffle.partitions&quot;, &quot;400&quot;) .getOrCreate()) sc = spark.sparkContext # the SparkContext inside it   SparkSession SparkContext Introduced 2.0 1.0 Gives you DataFrames, SQL, catalog, streaming RDDs, broadcasts, accumulators Relationship Wraps one SparkContext One per application A SparkSession wraps exactly one SparkContext, and one application has one SparkContext. getOrCreate() returns the existing one rather than making a second, which is why it is the idiomatic call. Two details interviewers probe: Creating the session is what starts negotiating for executors. Before it, nothing has been requested from the cluster manager. newSession() gives an isolated SQL session, with its own temporary views and SQL configs, sharing the same SparkContext and therefore the same executors. That is how a multi-tenant server such as the Thrift server isolates users. 9. What languages does Spark support, and does the choice affect performance? Scala, Java, Python and R, with a community-maintained C# binding outside the project. For DataFrame and SQL work the choice barely affects performance, because operations in any language compile to the same logical plan and execute in the JVM. It matters in two places: RDD code, where the logic itself is a closure in your language. A Python RDD map ships every row to a Python worker process and back. Python UDFs, which sit inside a plan that is otherwise all JVM. Rows must be serialized out to Python and results serialized back, and because Catalyst cannot see inside the function, it cannot push filters through it or fuse it into generated code. # stays in the JVM, fully optimisable df.filter(F.col(&quot;fare_amount&quot;) &amp;gt; 50) # leaves the JVM per batch, breaks codegen across this point @F.pandas_udf(&quot;double&quot;) def adjust(v: pd.Series) -&amp;gt; pd.Series: return v * 1.1 The ordering to remember, best first: built-in functions and SQL, then pandas (vectorised) UDFs which amortise the crossing over an Arrow batch, then plain Python UDFs which pay it per row. Scala and Java additionally get the typed Dataset API. Python does not, since it has no compile-time types to check. 10. What is a job, a stage and a task? Three nested units of work, each created by a different component. Term Definition Created by Job All the work for one action The action call Stage A group of tasks with no shuffle between them DAGScheduler, cut at shuffle boundaries Task One partition of one stage, on one core TaskSchedulerImpl flowchart TB A[&quot;action: write()&quot;] --&amp;gt; J[&quot;1 job&quot;] J --&amp;gt; S0[&quot;stage 0&amp;lt;br/&amp;gt;read, filter, partial aggregate&quot;] S0 --&amp;gt;|&quot;shuffle&quot;| S1[&quot;stage 1&amp;lt;br/&amp;gt;final aggregate, write&quot;] S0 --&amp;gt; T0[&quot;8 tasks&amp;lt;br/&amp;gt;one per input partition&quot;] S1 --&amp;gt; T1[&quot;200 tasks&amp;lt;br/&amp;gt;one per shuffle partition&quot;] Read it as a hierarchy: a job has one or more stages, and a stage has one task per partition. The arithmetic is worth internalising, because it is how you predict what the UI will show: Jobs equals the number of actions you called. Stages per job equals the number of shuffles, plus one. Tasks per stage equals that stage’s partition count, unrelated to how many executors you have. A nuance that catches people: a single write can produce more than one job, because some writers run extra jobs for committing or for computing statistics. “One action, one job” is the right model, with that exception noted. 11. What is a DAG in Spark? A directed acyclic graph of dependencies between datasets. Directed because every edge points from an input to an output; acyclic because nothing can depend on itself, which is what makes the graph safe to walk. Spark builds it as you chain transformations, and DAGScheduler uses it for two jobs: Cutting stages. It walks the graph backwards from the action and starts a new stage at every shuffle boundary, because everything narrow can pipeline into the stage it is already in. Recovery. Because each node records how it was derived, a lost partition can be rebuilt by replaying just the path that produced it. flowchart TB A[&quot;read trips&quot;] --&amp;gt; B[&quot;filter&quot;] C[&quot;read cities&quot;] --&amp;gt; D[&quot;filter&quot;] B --&amp;gt; J[&quot;join&quot;] D --&amp;gt; J J --&amp;gt; K[&quot;groupBy city&quot;] J --&amp;gt; L[&quot;groupBy rider&quot;] K --&amp;gt; M[&quot;write by city&quot;] L --&amp;gt; N[&quot;write by rider&quot;] The DAG is the reason Spark is not restricted to a map phase and a reduce phase: any shape of dependency graph is expressible, so a job can branch, rejoin and have many stages. You can print it for an RDD with toDebugString(), and for a DataFrame the physical plan from explain(&quot;formatted&quot;) is the equivalent. In the UI, the DAG visualisation on the job page shows the same thing graphically, with each box a stage. 12. What is the driver? The driver is the process that holds the SparkSession and makes every decision. In order, it: Creates the SparkSession, and with it the SparkContext, SparkEnv and the RPC endpoints executors will call back on. Requests executors from the cluster manager through SchedulerBackend. Turns your code into a logical plan, has Catalyst optimise it, and selects a physical plan. Cuts the plan into stages in DAGScheduler, at every ShuffleDependency. Places individual tasks on executors via TaskSchedulerImpl and TaskSetManager, preferring the best available locality. Tracks every attempt, retrying failures up to spark.task.maxFailures, default 4. Maintains BlockManagerMaster, the index of every cached and shuffle block in the cluster. Receives results, bounded in total by spark.driver.maxResultSize, default 1g. Serves the UI on port 4040 and writes the event log the history server replays. What it does not do is process your data. It is a coordinator. Two consequences follow, and both are common interview questions: spark.driver.memory defaults to 1g, which is a coordinator’s budget. Anything that pulls data back, collect() or toPandas(), needs more, or needs not to do that. The driver is the one process with no redundancy. The plan, the scheduler state and the block index exist nowhere else, so if the driver dies the application dies, whereas losing an executor costs only recomputable work. 13. What is an executor? An executor is a JVM launched for one specific application that runs tasks and stores blocks. The Spark source describes it in a line: a “Spark executor, backed by a threadpool to run tasks”. Its lifecycle: Registers. CoarseGrainedExecutorBackend sends RegisterExecutor and waits for RegisteredExecutor before accepting work. Until that round trip finishes, a granted container is doing nothing. Accepts LaunchTask messages and runs each in a pool thread. Concurrency is spark.executor.cores, which defaults to 1 on YARN and to every core on the machine in standalone mode. Reads input, runs the generated code, and for a shuffle map stage writes shuffle output to local disk. Reports back with StatusUpdate per task and a heartbeat every spark.executor.heartbeatInterval, default 10s. Miss enough and the driver declares it lost. Stores and serves blocks through its BlockManager, which is what makes a cached DataFrame reusable and lets the next stage fetch shuffle data from its peers. The contract is deliberately narrow: an executor never makes a scheduling decision, never sees the query plan, and never coordinates with another executor about what to run. It receives tasks, runs them, and serves bytes. That narrowness is what makes executors disposable. Lose one and its tasks are retried elsewhere and its cached partitions are recomputed from lineage. Executors are also per-application: two applications never share one, which is why memory is reserved per application rather than pooled. 14. What is a cluster manager, and which ones does Spark support? A cluster manager is a separate service whose only job is to decide who gets containers. At Spark 4.x the supported options are Standalone, YARN and Kubernetes. Cluster manager What grants the container What Spark runs there Standalone Master, tracking registered Worker daemons The worker launches an executor JVM per grant YARN The ResourceManager, asked by Spark’s ApplicationMaster through YarnAllocator An executor per allocated container Kubernetes The API server. The driver’s own ExecutorPodsAllocator creates the pods An executor per pod Mesos support was removed in the 4.x line. At v4.2.0 the resource-managers/ directory in the source tree contains only kubernetes and yarn, so the three rows above are the whole list. The division worth internalising is that the cluster manager never schedules your tasks. It hands out containers; executors register back with the driver; and from that point the driver drives everything. That single fact explains a lot of otherwise puzzling behaviour: Why an under-provisioned driver throttles a large cluster. Why YARN’s own scheduler settings change what you are given but never how your tasks are distributed. Why Kubernetes needs no Spark daemon at all: the driver talks to the API server directly. Kubernetes is the clearest illustration of the split, because the component granting resources and the component scheduling work are visibly different things. 15. Is Spark a master-slave architecture? In the general sense yes: one process coordinates and many do the work. But Spark’s own vocabulary is worth getting exactly right, because “master” is overloaded and people conflate three different things with it. Term What it actually is What it is not Driver The coordinator: holds the plan, schedules every task Not called the master anywhere in Spark --master A URL naming the cluster manager (yarn, k8s://…, local[*]) Not a machine, and not the driver Standalone Master A daemon that tracks workers and grants resources Standalone mode only; YARN and Kubernetes have their own Worker A machine, or the standalone daemon on it Not an executor; a worker hosts executors Executor A JVM doing the work Not a machine Spark has also moved away from the older terminology in its own scripts. At v4.2.0 there is sbin/start-workers.sh and conf/workers.template, while start-slaves.sh and slaves.template are gone, and the standalone documentation speaks of “a master and workers”. Where the analogy breaks is the interesting part, and it is a strong thing to volunteer. In a classic master-worker system the master assigns work. In Spark the cluster manager does not assign your tasks: it grants containers, and the driver schedules into them. The coordinator role is split, resources from one component and work from another. 16. What is `spark-submit`? The script that launches an application against any supported cluster manager through one uniform interface, so the same jar runs on YARN, Kubernetes or standalone with only --master changing. spark-submit \ --class org.apache.spark.examples.SparkPi \ --master yarn \ --deploy-mode cluster \ --executor-memory 4g \ --executor-cores 4 \ --conf spark.sql.shuffle.partitions=400 \ examples/jars/spark-examples.jar 1000 The options fall into three groups: what to run (--class, the jar), where to run it (--master, --deploy-mode), and how much to ask for (--executor-memory, --executor-cores, --num-executors). The mistake to know: every Spark-facing option must come before the application jar. Anything after the jar is passed to your application’s main as an argument instead, so a misplaced --conf is silently ignored by Spark and handed to your code. Configuration precedence, highest first: Values set on SparkConf in your code. Flags passed to spark-submit. spark-defaults.conf. --properties-file names an alternative defaults file, and --load-spark-defaults makes Spark read spark-defaults.conf as well as that file. --verbose prints where each value came from, which settles most “why is my config not applying” questions. 17. What is the difference between client and cluster deploy mode? Where the driver runs. That is the whole difference, and everything else follows from it. flowchart TB subgraph C[&quot;client mode&quot;] C1[&quot;submitting machine&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;driver here&amp;lt;/b&amp;gt;&quot;] --&amp;gt; C2[&quot;cluster manager&quot;] C2 --&amp;gt; C3[&quot;executors in the cluster&quot;] C1 &amp;lt;--&amp;gt;|&quot;all scheduling traffic&quot;| C3 end subgraph K[&quot;cluster mode&quot;] K1[&quot;submitting machine&amp;lt;br/&amp;gt;exits after submit&quot;] --&amp;gt; K2[&quot;cluster manager&quot;] K2 --&amp;gt; K3[&quot;&amp;lt;b&amp;gt;driver in the cluster&amp;lt;/b&amp;gt;&quot;] K3 &amp;lt;--&amp;gt; K4[&quot;executors in the cluster&quot;] end   Client Cluster Driver runs In the submitting process In a container in the cluster Console output Attached to your terminal In the driver’s log Submitting machine Must stay up for the whole job Can disconnect Network Driver-executor traffic crosses to your machine Stays inside the cluster Suited to Shells, notebooks, debugging Production jobs Two practical consequences: Client mode from a laptop over a VPN is slow and fragile, because every scheduling message and every collected result crosses that link. Cluster mode keeps it local. In client mode the driver JVM already exists by the time your code runs. So spark.driver.extraJavaOptions set in SparkConf arrives too late to affect the JVM. Use --driver-java-options or a properties file instead. This catches people trying to add a logging config or a heap dump flag. 18. What is the default parallelism, and what sets it? There are two separate settings, and confusing them is a classic slip. spark.default.parallelism governs RDD operations. Its documented default depends on the operation: for a distributed shuffle such as reduceByKey it is the largest number of partitions in the parent RDD, and for an operation with no parent, such as parallelize, it follows the cluster manager, which on local mode is the number of cores. spark.sql.shuffle.partitions governs DataFrame and SQL shuffles, and it defaults to 200 regardless of your data or your cluster. print(spark.conf.get(&quot;spark.sql.shuffle.partitions&quot;)) # 200   spark.default.parallelism spark.sql.shuffle.partitions Applies to RDD operations DataFrame and SQL shuffles Default Depends on parent and cluster manager 200 So if you are tuning a DataFrame job and you change spark.default.parallelism, nothing happens, which is a frequent source of confusion. The other thing to know is that input read parallelism is governed by neither: it comes from file splitting via spark.sql.files.maxPartitionBytes. 19. What is `spark.sql.shuffle.partitions` and why does the default cause trouble? It is the number of partitions produced by a DataFrame or SQL shuffle, and it defaults to 200. The trouble is that 200 is a constant, while the right value depends on your data volume and your core count. Both directions hurt: Too few for the data. 200 partitions over 1 TB means 5 GB per task, which will spill to disk and may fail outright. Too many for the data. 200 partitions over 100 MB means 500 KB per task, so scheduling and task startup dominate and 200 near-empty files get written. Mismatched to cores. With 32 cores, 200 tasks is a sensible several-waves-per-core; with 2,000 cores, most sit idle. A reasonable target is partitions in the low hundreds of megabytes, and a total that is a small multiple of your core count so stragglers average out. Adaptive query execution changes the calculus. With spark.sql.adaptive.enabled and spark.sql.adaptive.coalescePartitions.enabled both true by default, Spark writes 200 shuffle partitions and then merges the small ones at runtime toward spark.sql.adaptive.advisoryPartitionSizeInBytes, default 64MB. spark.conf.set(&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;, &quot;128MB&quot;) So in modern Spark the better habit is to set the advisory size and leave the partition count high, letting coalescing do the arithmetic, rather than guessing a single number for every stage in the query. 20. Which Spark version introduced Log4j 2, and why does it matter? Spark 3.3.0. The configuration file became log4j2.properties and the JVM flag became -Dlog4j.configurationFile=. It matters because the syntax changed completely, and most logging guides on the internet predate it. Log4j 1 used a flat log4j. prefix: # Log4j 1, ignored by Spark 3.3.0 and later log4j.rootLogger=INFO, console log4j.logger.org.apache.spark=WARN Log4j 2 uses named components: # Log4j 2 rootLogger.level = info rootLogger.appenderRef.stdout.ref = console appender.console.type = Console appender.console.name = console appender.console.layout.type = PatternLayout appender.console.layout.pattern = %d{HH:mm:ss} %-5level %logger{1} - %msg%n logger.spark.name = org.apache.spark logger.spark.level = warn At v4.2.0 the only template Spark ships is conf/log4j2.properties.template; the Log4j 1 template is gone. So a config written in the old syntax is not an error you will see, it is a file that is silently ignored, which is the worst kind of failure. Ship it to both sides, since driver and executor answer different questions: spark-submit \ --files /etc/spark/conf/log4j2.properties \ --conf &quot;spark.driver.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot; \ --conf &quot;spark.executor.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot; \ app.jar RDDs The original API. Interviewers still ask, because the concepts underneath the DataFrame API are these, and because RDD questions expose whether you understand lineage and partitioning or just the syntax. 21. What is an RDD? A Resilient Distributed Dataset: an immutable, partitioned collection of records that can be operated on in parallel, carrying enough information to rebuild any lost partition. Unpack the name, because each word is a design decision: Resilient. Spark records how each partition was derived, not just the data. Lose one and it is recomputed from its parents rather than the job failing. Distributed. The collection is split into partitions spread across executors. Dataset. A collection of records, with no schema and no interpretation of their contents. It is also immutable. Every transformation produces a new RDD rather than modifying the old one, which is what makes lineage a reliable recovery record: a parent cannot have changed underneath you. rdd = spark.sparkContext.parallelize([(&quot;sf&quot;, 24.5), (&quot;nyc&quot;, 61.75)], 2) totals = rdd.reduceByKey(lambda a, b: a + b) The trade against DataFrames is visibility. Because an RDD holds opaque objects, Spark cannot see inside your closures, so there is no schema, no column pruning, no predicate pushdown and no Catalyst. You get control and lose optimisation, which is why the DataFrame API is the default for structured data. 22. What are the five properties of an RDD? From RDD.scala, every RDD is characterised by five things, three required and two optional: A list of partitions. getPartitions, which defines the parallelism. A function for computing each partition. compute, which is the actual work. A list of dependencies on other RDDs. getDependencies, which is the lineage. Optionally, a partitioner for key-value RDDs, such as HashPartitioner. Optionally, preferred locations for each partition, such as the HDFS block locations. The first three make the RDD work; the last two make it fast, and they are the ones worth talking about: The partitioner is what lets Spark skip a shuffle. If two RDDs share a partitioner on the join key, matching keys are already co-located, so the join is narrow. Preferred locations are what data locality is built on. TaskSchedulerImpl tries to place a task where its input already is, which is why locality levels exist. rdd = spark.sparkContext.parallelize(range(10), 4) print(rdd.getNumPartitions()) # 1 print(rdd.partitioner) # 4, None for a non-pair RDD Naming all five, and then saying which two are optional and why they matter, is a much stronger answer than listing them. 23. How does Spark achieve fault tolerance for RDDs? Through lineage: the chain of transformations from a source to the dataset in hand. Because Spark recorded how to build every partition, not just the partition itself, a lost one is rebuilt on a surviving executor. lineage = (spark.sparkContext.parallelize(range(10), 2) .map(lambda x: x + 1) .filter(lambda x: x % 2 == 0)) print(lineage.toDebugString().decode()) Two things make this cheaper than it sounds: Recovery is per partition, not per job. Losing one executor costs the partitions it held, which are recomputed while everything else stands. Only the path matters. Spark replays the branch that produced the lost partition, not the whole graph. flowchart TB S[&quot;source&quot;] --&amp;gt; A[&quot;map&quot;] --&amp;gt; B[&quot;filter&quot;] --&amp;gt; C[&quot;partition 3 lost&quot;] C -.-&amp;gt;|&quot;recompute just this path&quot;| D[&quot;partition 3 rebuilt&amp;lt;br/&amp;gt;on another executor&quot;] The limits are worth volunteering: Shuffle output is the exception. If the shuffle files a completed stage wrote die with their executor, DAGScheduler resubmits that entire stage, because the next stage has nothing to fetch. That is a stage rerun rather than a task retry, and it is why a job can appear to go backwards. Long lineages get expensive to replay, which is what checkpointing exists to fix: it writes the data to reliable storage and truncates the chain. A non-deterministic transformation breaks the guarantee, because a recomputed partition may not match the lost one. 24. What is the difference between a narrow and a wide dependency? In a narrow dependency each parent partition feeds at most one child partition, so the work can pipeline inside a single task with no data movement. In a wide dependency a child partition draws from many parents, which requires a shuffle. flowchart TB subgraph N[&quot;narrow: map, filter&quot;] P0[&quot;P0&quot;] --&amp;gt; C0[&quot;C0&quot;] P1[&quot;P1&quot;] --&amp;gt; C1[&quot;C1&quot;] P2[&quot;P2&quot;] --&amp;gt; C2[&quot;C2&quot;] end subgraph W[&quot;wide: groupByKey, join&quot;] Q0[&quot;P0&quot;] --&amp;gt; D0[&quot;C0&quot;] Q0 --&amp;gt; D1[&quot;C1&quot;] Q1[&quot;P1&quot;] --&amp;gt; D0 Q1 --&amp;gt; D1 Q2[&quot;P2&quot;] --&amp;gt; D0 Q2 --&amp;gt; D1 end   Narrow Wide Classes OneToOneDependency, RangeDependency ShuffleDependency Examples map, filter, flatMap, mapPartitions, union groupByKey, reduceByKey, join, distinct, repartition Data movement None Across the network Stage Pipelines into the current stage Creates a boundary On failure Recompute one parent partition May need to recompute many, or rerun the stage The recovery difference is the part people miss. A lost narrow partition needs one parent partition recomputed. A lost partition after a wide dependency may need data from every parent partition, which is far more expensive, and is another reason shuffles are costly beyond their immediate network cost. coalesce is the interesting edge case: it is narrow, because several parent partitions feed one child without a shuffle, which is exactly why it can only reduce the count and may leave partitions uneven. 25. Where exactly does Spark cut a stage? At every ShuffleDependency in the lineage, and nowhere else. DAGScheduler walks the graph backwards from the action. Each time it crosses a ShuffleDependency it ends the current stage and starts a new one; everything narrow is absorbed into the stage it is already in. flowchart TB subgraph S0[&quot;stage 0&quot;] R[&quot;read&quot;] --&amp;gt; F[&quot;filter&quot;] --&amp;gt; W[&quot;withColumn&quot;] --&amp;gt; PA[&quot;partial aggregate&quot;] end PA --&amp;gt;|&quot;ShuffleDependency&quot;| SH[(&quot;shuffle files&amp;lt;br/&amp;gt;on local disk&quot;)] subgraph S1[&quot;stage 1&quot;] SH --&amp;gt; FA[&quot;final aggregate&quot;] --&amp;gt; O[&quot;write&quot;] end The practical consequences: A long chain of narrow operations costs no extra stages. Twenty chained filter and withColumn calls still run in one stage, fused by whole-stage codegen into a single loop. The stage count is the shuffle count plus one, per job. Counting Exchange nodes in a physical plan gives you the same number. Every stage but the last is a ShuffleMapStage, whose purpose is to produce shuffle output. The last is a ResultStage, which computes what the action asked for. Stages also enforce a barrier: stage 1 cannot start until stage 0 has finished writing all its output, because a reduce task needs its slice from every map task. That is why one straggler in stage 0 delays the entire next stage, and why skew is so expensive. 26. What is the difference between `reduceByKey` and `groupByKey`? reduceByKey combines values on the map side before the shuffle; groupByKey shuffles every value and combines afterwards. # prefer this: combines locally first pairs.reduceByKey(lambda a, b: a + b) # shuffles every value across the network pairs.groupByKey().mapValues(sum) flowchart TB subgraph R[&quot;reduceByKey&quot;] A1[&quot;(&apos;sf&apos;,1) (&apos;sf&apos;,1) (&apos;sf&apos;,1)&quot;] --&amp;gt; A2[&quot;local combine&amp;lt;br/&amp;gt;(&apos;sf&apos;,3)&quot;] --&amp;gt; A3[&quot;shuffle&amp;lt;br/&amp;gt;1 record&quot;] end subgraph G[&quot;groupByKey&quot;] B1[&quot;(&apos;sf&apos;,1) (&apos;sf&apos;,1) (&apos;sf&apos;,1)&quot;] --&amp;gt; B3[&quot;shuffle&amp;lt;br/&amp;gt;3 records&quot;] --&amp;gt; B4[&quot;combine after&quot;] end With a hot key, the difference is the number of records crossing the network: one per key per partition for reduceByKey, versus every record for groupByKey. groupByKey also has a correctness-adjacent risk: all values for one key must fit in memory on the reduce side, so a hot key can cause an OutOfMemoryError that reduceByKey would not. The subtlety that makes a strong answer: a map-side combine disqualifies Spark’s two faster shuffle writers, because they cannot combine. So reduceByKey shuffles less data through a more expensive writer. It is still almost always the right choice, because network volume dominates, but the trade-off is real and worth naming. Use groupByKey only when you genuinely need every value, for example to collect them into a list, and even then consider aggregateByKey which combines into a buffer as it goes. 27. What is the difference between `map` and `mapPartitions`? map applies your function once per element. mapPartitions applies it once per partition, handing you an iterator over that partition’s elements and expecting an iterator back. # once per row: a connection per row, which is fatal rdd.map(lambda r: lookup(open_conn(), r)) # once per partition: one connection amortised over thousands of rows def enrich(rows): conn = open_conn() for r in rows: yield lookup(conn, r) conn.close() rdd.mapPartitions(enrich) Use mapPartitions when there is per-partition setup worth amortising: a database or HTTP connection, loading a model, or compiling a regex. Two cautions: Do not materialise the partition. list(rows) inside the function loads the whole partition into memory and defeats the streaming design. Use a generator, as above. Return an iterator, not a single value. Returning a list works but holds everything in memory. mapPartitionsWithIndex additionally gives you the partition number, which is the standard trick for inspecting how data is distributed: print(rdd.mapPartitionsWithIndex( lambda i, rows: [(i, sum(1 for _ in rows))]).collect()) That prints rows per partition, which is how you confirm skew on an RDD. 28. What is the difference between `map` and `flatMap`? map returns exactly one output per input. flatMap returns an iterable per input and flattens the results, so it can return zero, one or many. lines = sc.parallelize([&quot;a b c&quot;, &quot;d e&quot;]) lines.map(lambda s: s.split()).collect() # [[&apos;a&apos;,&apos;b&apos;,&apos;c&apos;], [&apos;d&apos;,&apos;e&apos;]] two elements, each a list lines.flatMap(lambda s: s.split()).collect() # [&apos;a&apos;,&apos;b&apos;,&apos;c&apos;,&apos;d&apos;,&apos;e&apos;] five elements The “zero” case is the one worth knowing, because it makes flatMap a combined map and filter: # parse, and silently drop anything unparseable, in one pass rdd.flatMap(lambda s: [parse(s)] if valid(s) else []) Both are narrow transformations, so neither causes a shuffle, and both pipeline into the surrounding stage. The DataFrame equivalent of flatMap for a column is explode, which turns an array column into one row per element. 29. What is the difference between `repartition` and `coalesce`? repartition does a full shuffle to reach any target count and balances the result. coalesce merges existing partitions without a full shuffle, so it can only reduce the count and may leave partitions uneven.   repartition(n) coalesce(n) Shuffle Full None by default Direction Up or down Down only Balance Even Possibly uneven Dependency Wide, so a new stage Narrow df.coalesce(10).write.parquet(path) # cheap, before a write df.repartition(400).groupBy(&quot;k&quot;).count() # balanced, before heavy work df.repartition(&quot;city_id&quot;) # hash-partitioned by a column The trap is what coalesce does upstream, and it is a favourite interview question. Because coalesce is narrow, it does not create a stage boundary, so the reduced parallelism propagates backwards into the stage feeding it. coalesce(1) before a heavy transformation can make that entire transformation run in a single task. flowchart TB subgraph C[&quot;coalesce(2): merge, no shuffle&quot;] A1[&quot;P0 10 MB&quot;] --&amp;gt; M1[&quot;P0 + P1&amp;lt;br/&amp;gt;30 MB&quot;] A2[&quot;P1 20 MB&quot;] --&amp;gt; M1 A3[&quot;P2 5 MB&quot;] --&amp;gt; M2[&quot;P2 + P3&amp;lt;br/&amp;gt;45 MB&quot;] A4[&quot;P3 40 MB&quot;] --&amp;gt; M2 end subgraph R[&quot;repartition(2): shuffle, balanced&quot;] B1[&quot;P0 10 MB&quot;] --&amp;gt; SH[(&quot;shuffle&quot;)] B2[&quot;P1 20 MB&quot;] --&amp;gt; SH B3[&quot;P2 5 MB&quot;] --&amp;gt; SH B4[&quot;P3 40 MB&quot;] --&amp;gt; SH SH --&amp;gt; N1[&quot;37 MB&quot;] SH --&amp;gt; N2[&quot;38 MB&quot;] end So if you need one output file, repartition(1) pays for a shuffle but keeps the upstream stage parallel. coalesce(1) is only safe when the work before it is trivial. 30. What is the difference between `cache` and `persist`? cache() is exactly persist() with the default storage level. persist(level) lets you choose the level. df.cache() # default level df.persist(StorageLevel.MEMORY_AND_DISK_SER) # explicit The default differs by API, which is the part that surprises people: API Default level Behaviour when it does not fit rdd.cache() Memory only Partitions are dropped and recomputed df.cache() Disk and memory, deserialized Partitions spill to disk So an RDD cache that exceeds memory silently recomputes, which looks like the cache doing nothing at all. A DataFrame cache spills instead, which is slower than memory but still avoids recomputation. If an RDD cache seems to have no effect, that asymmetry is usually why. Both are lazy: they mark the dataset and populate it on the next action. The exception is SQL’s CACHE TABLE, which is eager by default. Release it explicitly when you are done, because cached blocks hold storage memory for the life of the application and squeeze execution memory from every later stage: df.unpersist() spark.catalog.clearCache() # everything 31. What are the RDD storage levels? The levels combine three choices: memory or disk, serialized or not, and replicated or not. Level Memory Disk Serialized Replicas MEMORY_ONLY Yes No No 1 MEMORY_ONLY_SER Yes No Yes 1 MEMORY_AND_DISK Yes Spills No 1 MEMORY_AND_DISK_SER Yes Spills Yes 1 DISK_ONLY No Yes Yes 1 *_2 variants as above as above as above 2 OFF_HEAP Off-heap No Yes 1 How to choose: Serialized forms are far more compact, so more fits in memory, at the cost of CPU to deserialize on every read. Worth it when memory is the constraint. _2 replicas put a copy on a second node, so losing an executor does not force recomputation. Worth it when recomputation is very expensive; otherwise it doubles the memory cost. DISK_ONLY is right when recomputing is more expensive than reading back, which is the case after a long or costly lineage. OFF_HEAP avoids garbage collection pressure but needs spark.memory.offHeap.enabled, which defaults to false, and a size. Read the storage tab of the UI to check what actually happened. A dataset showing well below 100% in memory is being partly recomputed or read from disk on every use, which usually means the cache is not paying for itself. 32. What are broadcast variables? A broadcast variable is a read-only value sent to each executor once and cached there, rather than serialized into every task. The guide describes them as keeping “a read-only variable cached on each machine rather than shipping a copy of it with tasks”. sc = spark.sparkContext cities = sc.broadcast({&quot;sf&quot;: &quot;San Francisco&quot;, &quot;nyc&quot;: &quot;New York&quot;}) resolved = sc.parallelize([&quot;sf&quot;, &quot;nyc&quot;, &quot;sf&quot;], 2).map(lambda c: cities.value[c]) print(resolved.collect()) # [&apos;San Francisco&apos;, &apos;New York&apos;, &apos;San Francisco&apos;] Without the broadcast, that dictionary is captured by the closure and serialized with every task. With 10,000 tasks and a 10 MB lookup table, that is 100 GB of serialization instead of one copy per executor. flowchart TB subgraph W[&quot;captured variable&quot;] D1[&quot;driver&quot;] --&amp;gt;|&quot;copy per task&amp;lt;br/&amp;gt;x 10,000&quot;| T1[&quot;tasks&quot;] end subgraph B[&quot;broadcast&quot;] D2[&quot;driver&quot;] --&amp;gt;|&quot;one copy per executor&amp;lt;br/&amp;gt;x 20&quot;| E2[&quot;executors&quot;] --&amp;gt; T2[&quot;all tasks read locally&quot;] end Practical notes: Read-only. Mutating .value on an executor changes only that copy and nothing comes back. Call .unpersist() when finished, or .destroy() to release it permanently, if it is large and no longer needed. This is the same machinery behind a broadcast hash join, which is why spark.sql.autoBroadcastJoinThreshold exists and why broadcasting something large pressures the driver, which must assemble it first. 33. What are accumulators, and what is the catch? Accumulators are write-only-from-tasks counters that the driver can read. Tasks add to them; only the driver should read the value. errors = sc.accumulator(0) def parse(line): try: return json.loads(line) except ValueError: errors.add(1) return None parsed = rdd.map(parse).filter(lambda x: x is not None) parsed.count() print(errors.value) The catch is the guarantee, and it is the whole question. The guide is explicit: For accumulator updates performed inside actions only, Spark guarantees that each task’s update to the accumulator will only be applied once, i.e. restarted tasks will not update the value. In transformations, users should be aware of that each task’s update may be applied more than once if tasks or job stages are re-executed. This is not theoretical, and it follows directly from lazy evaluation: acc = sc.accumulator(0) counted = sc.parallelize(range(100), 4).map(lambda x: (acc.add(1), x)[1]) counted.count(); print(acc.value) # 100 counted.count(); print(acc.value) # 200, for the same 100 rows The uncached dataset is recomputed for the second action, so the accumulator increments again. Add .cache() and it stays at 100. A task retry has the same effect. So use accumulators for diagnostics you can tolerate being approximate, and never as the source of a number the output depends on. If you need an exact count, compute it as data with an aggregation. 34. What is a partitioner, and when does it help? A partitioner is the function mapping a key to a partition number. Spark ships two: HashPartitioner, which uses hash(key) % numPartitions. The default for reduceByKey and friends. RangePartitioner, which samples the data to find range boundaries so that partitions are roughly equal and ordered. Used by sortByKey and by a global orderBy. It helps because a known partitioner lets Spark skip a shuffle. If two RDDs share the same partitioner on the join key, matching keys are already co-located, so the join becomes a narrow dependency. part = 100 a = rdd_a.partitionBy(part).cache() # note the cache b = rdd_b.partitionBy(part).cache() joined = a.join(b) # no shuffle The cache is not optional. Without persisting, the partitioning is recomputed each time the RDD is used, so you pay the shuffle you were trying to avoid. You can inspect and set it: print(pairs.partitioner) # None until you set one tagged = pairs.partitionBy(HashPartitioner(100)) The DataFrame equivalents are repartition(col) for the same effect within a job, and bucketing for the effect to persist across jobs, recorded in the metastore. 35. What does `toDebugString` tell you? It prints the lineage of an RDD as an indented tree: each level is an RDD, with its type, its id and its partition count. lin = (sc.parallelize(range(100), 4) .map(lambda x: (x % 5, x)) .reduceByKey(lambda a, b: a + b)) print(lin.toDebugString().decode()) (4) PythonRDD[3] at RDD at PythonRDD.scala:53 [] | MapPartitionsRDD[2] at mapPartitions at ... [] | ShuffledRDD[1] at partitionBy at ... [] +-(4) PairwiseRDD[0] at reduceByKey at ... [] | ParallelCollectionRDD ... [] How to read it: The number in parentheses is the partition count at that level. Indentation changes, marked by +-, are shuffle boundaries. Count them and you have the stage count minus one. Read bottom up: the source is at the bottom, the RDD you called it on is at the top. It is the fastest way to answer “how many stages will this take” for RDD code, and to spot an accidental shuffle. For DataFrames the equivalent is df.explain(&quot;formatted&quot;), where each Exchange is the same signal. 36. When should you still use RDDs? When you need something the structured APIs do not expose. The honest list is short: Custom partitioning logic beyond hash or range, for example routing by a business rule. Per-partition resource handling via mapPartitions, although the DataFrame API has mapInPandas for much of this now. Genuinely unstructured data with no schema to impose: raw text, binary records, custom parsing where a row shape only emerges after processing. Low-level control over lineage and checkpointing, in iterative algorithms. For everything with a schema, the DataFrame API wins, and the reason is structural rather than a matter of taste: Catalyst can see a DataFrame plan and cannot see inside an RDD closure. That means no column pruning, no predicate pushdown, no join strategy selection and no whole-stage codegen for RDD code. Tungsten’s compact binary rows also avoid materialising JVM objects, which RDD code cannot avoid. A good answer also notes that RDDs did not go away. DataFrames are built on them: df.rdd gives you the underlying RDD, and the scheduler works entirely in RDD terms. So understanding RDDs is still how you understand what a DataFrame job actually does. 37. What is the difference between `collect` and `take`? collect() brings every row to the driver. take(n) returns the first n rows and tries to read as little as possible to get them. rows = df.collect() # everything, bounded only by driver memory head = df.take(5) # 5 rows, usually from one partition df.show(5) # take plus formatting take(n) starts by scanning one partition. If that yields fewer than n rows it scans more, increasing the number it tries each round. So on a large dataset it usually touches a tiny fraction of the data. collect() is bounded by spark.driver.maxResultSize, default 1g, and by the driver heap, spark.driver.memory, default 1g. Exceeding the first raises an error naming the config; exceeding the second is an OutOfMemoryError. This is the classic way to kill a driver, and the answer an interviewer wants is what to do instead: To inspect, use show(), take() or limit(). To count, use count(), which aggregates rather than transferring rows. To export, use write so executors write in parallel and only a status returns. To hand data to Python, toPandas() has the same problem as collect(); prefer writing out or using mapInPandas to keep the work distributed. 38. What is the difference between `reduce` and `fold`? Both aggregate an RDD to a single value using a binary function. The difference is the zero value. sc.parallelize([1, 2, 3, 4]).reduce(lambda a, b: a + b) # 10 sc.parallelize([1, 2, 3, 4]).fold(0, lambda a, b: a + b) # 10 sc.parallelize([]).reduce(lambda a, b: a + b) # raises sc.parallelize([]).fold(0, lambda a, b: a + b) # 0 So fold is defined on an empty RDD and reduce is not. The trap is that the zero value must be a true identity, because Spark applies it once per partition as well as when combining partitions. With four partitions and a zero of 1 for addition, you would add 1 five times, not once. aggregate generalises both: it takes a zero, a function to fold a partition into an accumulator, and a function to combine accumulators. That is what you need when the accumulator type differs from the element type, such as computing a mean: total, n = sc.parallelize([1, 2, 3, 4]).aggregate( (0, 0), lambda acc, x: (acc[0] + x, acc[1] + 1), # fold within a partition lambda a, b: (a[0] + b[0], a[1] + b[1])) # combine partitions print(total / n) Note also that all three are actions, returning to the driver, whereas reduceByKey is a transformation returning a distributed RDD. 39. Why are RDD operations on key-value pairs special? Because only pair RDDs can be partitioned by key, and key-partitioning is what enables the optimisations that matter. Being a pair RDD unlocks: partitionBy, and therefore shuffle-free joins between co-partitioned RDDs. Map-side combines: reduceByKey, aggregateByKey, combineByKey. Key-wise operations: groupByKey, join, cogroup, lookup, sortByKey. pairs = rdd.map(lambda r: (r.city_id, r.fare_amount)) # now a pair RDD pairs.reduceByKey(lambda a, b: a + b) In Scala these arrive by implicit conversion on RDD[(K, V)] through PairRDDFunctions, which is why they are not on the RDD class itself. In Python any RDD whose elements are 2-tuples qualifies. The distinction matters in an interview because it explains why the API is shaped this way: partitioning is defined on keys, so anything that benefits from co-location has to be expressed as a pair. The DataFrame equivalent is that these optimisations attach to columns you group or join on, which is why repartition(col) and bucketing exist. 40. What happens if a task fails? It is retried, and if retries are exhausted the stage and then the job fail. The sequence: The task fails and reports back to the driver. TaskSetManager reschedules it, preferring a different executor if the failure looked executor-specific. Retries continue up to spark.task.maxFailures, default 4, counted per task across the stage. On the fourth failure the stage fails, and the job fails with it. Two related mechanisms are worth naming: Blacklisting, now called excludeOnFailure. If one executor or node keeps failing tasks, Spark can stop scheduling onto it, which prevents a single bad machine from burning through the retry budget of every task. Speculation. spark.speculation, default false, duplicates tasks that are running much slower than their peers and takes whichever finishes. It helps with a slow node and hurts with skew, because the duplicate is equally slow. The distinct case is losing shuffle output rather than a task: flowchart TB T1[&quot;task attempt 1 fails&quot;] --&amp;gt; T2[&quot;attempt 2, preferably&amp;lt;br/&amp;gt;on another executor&quot;] T2 --&amp;gt; T3[&quot;attempt 3&quot;] T3 --&amp;gt; T4[&quot;attempt 4&quot;] T4 --&amp;gt; F[&quot;spark.task.maxFailures reached:&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;stage fails, job fails&amp;lt;/b&amp;gt;&quot;] T2 -.-&amp;gt;|&quot;same executor keeps failing&quot;| X[&quot;excludeOnFailure:&amp;lt;br/&amp;gt;stop scheduling there&quot;] T2 -.-&amp;gt;|&quot;slow, not failing&quot;| SP[&quot;speculation may&amp;lt;br/&amp;gt;duplicate the task&quot;] That is a stage rerun, and it is why a job can appear to go backwards in the UI. An external shuffle service avoids it by serving shuffle blocks independently of the executor that wrote them. DataFrames, Datasets and Spark SQL The API you should reach for by default. These questions separate people who use it from people who understand what it does underneath. 41. What is a DataFrame? A distributed collection of rows organised into named columns, with a schema Spark knows at plan time. That last part is the whole point. df = spark.read.parquet(&quot;s3a://lakehouse-prod/warehouse/trips&quot;) df.printSchema() Because Spark knows the schema and the operations, a DataFrame plan goes through Catalyst, which can: Prune columns nobody selected, so they are never read from disk. Push predicates into the scan, so rows are filtered at the source. Choose a join strategy based on estimated sizes. Fuse operators into a single generated loop via whole-stage codegen. Store rows in Tungsten’s binary format, avoiding JVM objects entirely. An RDD of the same data gets none of that, because its records are opaque objects and its closures are opaque functions. Two clarifications interviewers like: A DataFrame in Scala is Dataset[Row]. It is not a separate type; Row is the untyped record. It is still immutable and lazy. withColumn returns a new DataFrame, and nothing runs until an action. 42. What is a Dataset, and how does it differ from a DataFrame? A Dataset is a typed distributed collection that still runs through Spark SQL’s optimised engine. It gives you compile-time type safety and your own case classes instead of generic rows. case class Trip(tripId: String, fareAmount: Double, cityId: String) val trips: Dataset[Trip] = spark.read.parquet(path).as[Trip] trips.filter(_.fareAmount &amp;gt; 50).map(_.cityId) // typed, checked at compile time In Scala, DataFrame is simply Dataset[Row], so they are the same machinery with different static types.   DataFrame Dataset Element type Row, untyped Your class, typed Errors caught At analysis, runtime At compile time Available in All four languages Scala and Java only Encoder Built in Generated for your type Python has no typed Dataset API, and the reason is worth stating: the API exists to give compile-time checking, and Python has no compile step to check in. In PySpark, DataFrame is what you get. The cost of typed Datasets is real too: a lambda like _.fareAmount &amp;gt; 50 is opaque to Catalyst in the same way an RDD closure is, so it cannot be pushed down. Typed filter and map trade optimisation for type safety, which is why even in Scala, column expressions are often preferred for hot paths. 43. Compare RDD, DataFrame and Dataset.   RDD DataFrame Dataset Level Low Structured Typed structured Schema None Yes Yes, from your type Catalyst optimisation No Yes Yes, for column expressions Whole-stage codegen No Yes Yes Tungsten binary format No Yes Yes Type safety Compile time (Scala) Runtime Compile time Python support Yes Yes No typed API Serialization Java or Kryo Tungsten encoders Tungsten encoders How to choose, in one line each: DataFrame or SQL for almost everything. It is the fastest path because Catalyst can see it all. Dataset in Scala or Java when compile-time safety on a domain type is worth losing pushdown through typed lambdas. RDD only for custom partitioning, per-partition resource handling, or data with no schema. The evolution is worth a sentence: RDDs came first, DataFrames added a schema and the optimiser, and Datasets added types back on top of the optimiser. They are layered, not alternatives, and df.rdd drops down a level whenever you need it. The strong closing point: hand-written RDD code loses to DataFrame code on structured data not because the author wrote it badly, but because the engine is allowed to rewrite the DataFrame version and is not allowed to touch the RDD one. 44. What is Catalyst? Catalyst is Spark SQL’s query optimiser: the component that turns a query you wrote into a physical plan the engine runs. It is a rule-based tree transformation framework. Plans are trees, rules are functions from tree to tree, and the optimiser applies batches of rules until the tree stops changing. That design is why Spark SQL is extensible: a new optimisation is a new rule, and you can add your own through session extensions. flowchart TB A[&quot;SQL or DataFrame&quot;] --&amp;gt; B[&quot;unresolved&amp;lt;br/&amp;gt;logical plan&quot;] B --&amp;gt;|&quot;catalog&quot;| C[&quot;analysed&amp;lt;br/&amp;gt;logical plan&quot;] C --&amp;gt;|&quot;rules&quot;| D[&quot;optimised&amp;lt;br/&amp;gt;logical plan&quot;] D --&amp;gt;|&quot;strategies + cost&quot;| E[&quot;physical plan&quot;] E --&amp;gt;|&quot;codegen&quot;| F[&quot;JVM bytecode&quot;] What Catalyst gives you in practice, in rough order of value: Column pruning, so unread columns are never touched. Predicate pushdown, so filtering happens at the source. Join strategy selection, so a small side gets broadcast. Constant folding and expression simplification. Whole-stage code generation, producing tight loops rather than operator-by-operator iteration. The idea underneath all of it is lazy evaluation. Catalyst can only optimise a plan it can see in full, which is exactly what laziness provides. 45. What are Catalyst&apos;s phases? Five, and naming them in order with what each does is a strong answer. Parse. SQL text or DataFrame calls become an unresolved logical plan: a tree with relation and column names that have not been checked against anything. Analyse. The analyzer resolves table names, column names, data types and function calls against the catalog. This is where AnalysisException comes from, and why a typo in a column name surfaces here rather than at parse time. Logical optimisation. Rule batches rewrite the plan: predicate pushdown, column pruning, constant folding, boolean simplification, filter and projection reordering, subquery flattening. The output is still logical, meaning it says what, not how. Physical planning. Strategies turn each logical operator into one or more physical ones, for example a logical join into a broadcast hash join, shuffle hash join or sort-merge join. Costs choose between candidates. Code generation. Whole-stage codegen fuses the operators in a stage into one generated Java method, compiled with Janino, so intermediate rows never become objects. You can see each stage: df.explain(&quot;extended&quot;) # parsed, analysed, optimised, physical, in that order The distinction to be crisp about is logical versus physical. Logical optimisation is algebraic and knows nothing about the cluster; physical planning is where partitioning, shuffles and join implementations are decided. 46. What is predicate pushdown? Moving a filter as close to the data as possible, so that fewer rows are ever read or carried up the plan. There are two levels, and distinguishing them is a good signal: Within the plan. Catalyst moves a Filter below a Project or through a join, so downstream operators see fewer rows. Into the data source. For Parquet or ORC, the predicate is handed to the reader, which skips entire row groups using footer statistics. For JDBC, it becomes a WHERE clause in the SQL sent to the database. You can confirm it in the plan: (spark.read.parquet(path) .filter(F.col(&quot;fare_amount&quot;) &amp;gt; 50) .select(&quot;city_id&quot;) .explain(&quot;formatted&quot;)) FileScan parquet [fare_amount#19,city_id#21] DataFilters: [isnotnull(fare_amount#19), (fare_amount#19 &amp;gt; 50.0)] PushedFilters: [IsNotNull(fare_amount), GreaterThan(fare_amount,50.0)] ReadSchema: struct&amp;lt;fare_amount:double,city_id:string&amp;gt; PushedFilters is the line that matters: those predicates reached the file format. spark.sql.parquet.filterPushdown defaults to true. What blocks it is the practically useful half: Wrapping the column in a function. to_date(ts) = &apos;2026-09-17&apos; cannot push down; ts &amp;gt;= &apos;...&apos; AND ts &amp;lt; &apos;...&apos; can. A UDF in the predicate, because Catalyst cannot see inside it. Filtering after a non-deterministic operation, which would change semantics. 47. What is column pruning? Reading only the columns the query actually needs, and dropping the rest as early as possible in the plan. On a columnar format such as Parquet or ORC, unread columns are never touched on disk at all, so a table with 200 columns queried for 3 reads roughly 1.5% of the bytes. On a row format such as JSON or CSV the whole row must still be read and parsed, so pruning helps less. Confirm it by reading ReadSchema in the plan: ReadSchema: struct&amp;lt;fare_amount:double,city_id:string&amp;gt; Two columns, not the whole table. What defeats it: select(&quot;*&quot;), explicitly asking for everything. df.rdd or a typed Dataset lambda, which forces Spark to materialise the whole row because it cannot see which fields you touch. Caching before selecting. df.cache() caches the columns the plan needed at that point, so caching a wide DataFrame and then selecting three columns stores all of them. The practical habit: select the columns you need immediately after reading, not at the end of the pipeline. It costs nothing and it is the difference between reading 1.5% and 100% of a wide table. 48. What is whole-stage code generation? An optimisation where Catalyst fuses all the operators in a stage into a single generated Java method, rather than running each operator as a separate iterator that passes rows to the next. Without it, a scan-filter-project chain is three objects calling next() on each other, with a virtual call and an intermediate row per step. With it, Spark emits roughly the loop you would write by hand: // conceptually, for filter + project over a scan while (scan.hasNext()) { InternalRow row = scan.next(); double fare = row.getDouble(0); if (!row.isNullAt(0) &amp;amp;&amp;amp; fare &amp;gt; 50.0) { out.write(row.getUTF8String(1)); } } You can see it in the plan. Operators marked with * and a number share one generated stage: *(1) HashAggregate(keys=[city_id#21], ...) +- Exchange hashpartitioning(city_id#21, 200) +- *(2) HashAggregate(keys=[city_id#21], ...) +- *(2) Project [city_id#21] +- *(2) Filter (fare_amount#19 &amp;gt; 50.0) +- *(2) FileScan parquet Everything marked *(2) was fused into one method. What breaks the fusion is the useful part to know: a Python UDF, because rows must leave the JVM; some complex expressions; and plans that exceed the generated-method size limit, at which point Spark falls back to the interpreted path. Losing codegen is a large part of why Python UDFs cost what they do. 49. How do you read a query plan? Start with the mode that matches the question: df.explain(&quot;formatted&quot;) # the readable one: tree plus per-node details df.explain(&quot;extended&quot;) # parsed, analysed, optimised, physical df.explain(&quot;cost&quot;) # with statistics, where available df.explain(True) # same as extended Then read the physical plan bottom up, because that is execution order: scans at the bottom, then filters and projections, then exchanges, then the final operator. What to look for, in the order that usually matters: In the plan Means Exchange A shuffle, therefore a stage boundary. Count them PushedFilters Predicates that reached the data source ReadSchema Which columns are actually read BroadcastHashJoin The small side is being broadcast SortMergeJoin Both sides shuffled and sorted *(n) Whole-stage codegen; same n means fused CartesianProduct Almost always a mistake AdaptiveSparkPlan isFinalPlan=false AQE will re-plan at runtime The last row matters for reading plans in modern Spark. Before execution the plan is wrapped in AdaptiveSparkPlan with isFinalPlan=false, meaning what you see is the planned shape. To see what actually ran, look at the SQL tab of the UI after the query finishes, where the final plan shows the strategies AQE settled on. 50. What is the difference between `select` and `withColumn`? select projects a set of columns, replacing the projection entirely. withColumn adds or replaces one column, keeping everything else. df.select(&quot;trip_id&quot;, (F.col(&quot;fare_amount&quot;) * 1.1).alias(&quot;fare_with_fee&quot;)) df.withColumn(&quot;fare_with_fee&quot;, F.col(&quot;fare_amount&quot;) * 1.1) For one or two columns the difference is style. For many it is performance, and this is a real production issue: # 50 chained calls: a 50-level deep plan, slow to analyse and optimise for i in range(50): df = df.withColumn(f&quot;c{i}&quot;, F.col(&quot;x&quot;) + i) # one projection: a flat plan df = df.select(&quot;*&quot;, *[(F.col(&quot;x&quot;) + i).alias(f&quot;c{i}&quot;) for i in range(50)]) Each withColumn adds a Project node. Catalyst will usually collapse them, but the analysis and optimisation passes still walk the deep tree first, and with enough levels you get very slow planning or a StackOverflowError from the recursive walk. The rule of thumb: a handful of withColumn calls is fine and more readable; dozens should become one select. If planning time is a noticeable fraction of your job, count the withColumn calls first. 51. What is the difference between a temporary view and a global temporary view? Scope and lifetime. df.createOrReplaceTempView(&quot;trips&quot;) # this SparkSession only df.createGlobalTempView(&quot;trips_global&quot;) # all sessions in the app spark.sql(&quot;SELECT * FROM global_temp.trips_global&quot;)   Temporary view Global temporary view Visible to The creating SparkSession Every session in the application Database The current one global_temp, always qualified Lives until The session ends The application ends Neither persists metadata: both disappear when the process does. For something that survives, you need a catalog table via saveAsTable, which writes to the metastore. The reason global temp views exist is multi-session applications. newSession() gives an isolated SQL session sharing the same SparkContext, which is how the Thrift server isolates users. A global temp view is how you deliberately share something across those sessions. Worth noting: a view stores the query, not the result. Reading a view re-executes its plan. If you want the result stored, cache it or write a table. 52. What is a UDF, and why avoid it? A user-defined function: your own logic registered as a callable expression. @F.udf(&quot;double&quot;) def add_fee(fare): return fare * 1.1 if fare else None df.withColumn(&quot;total&quot;, add_fee(&quot;fare_amount&quot;)) Avoid it when a built-in exists, for three compounding reasons: Catalyst cannot see inside it. It is a black box, so no predicate can be pushed through it, no constant folded, no expression simplified. It breaks whole-stage codegen across that point, so the tight generated loop becomes iterator-by-iterator. In PySpark it leaves the JVM. Rows are serialized to a Python worker and results serialized back, per row for a plain UDF. Almost everything people write a UDF for exists already: # instead of a UDF F.when(F.col(&quot;fare&quot;) &amp;gt; 100, &quot;high&quot;).otherwise(&quot;mid&quot;) F.regexp_extract(&quot;path&quot;, r&quot;city_id=(\w+)&quot;, 1) F.coalesce(&quot;a&quot;, &quot;b&quot;, F.lit(0)) F.date_format(&quot;started_at&quot;, &quot;yyyy-MM-dd&quot;) The ordering to remember: built-in functions, then SQL expressions, then a pandas (vectorised) UDF, then a Scala UDF if you are on the JVM, and a plain Python UDF last. If you must write one, keep it narrow and apply it after filtering, so it sees as few rows as possible. 53. What is a pandas UDF and when does it help? A vectorised UDF that receives an Arrow batch as a pandas Series and returns one, rather than being called per row. import pandas as pd from pyspark.sql.functions import pandas_udf @pandas_udf(&quot;double&quot;) def adjust(fare: pd.Series) -&amp;gt; pd.Series: return fare * 1.1 The cost of a Python UDF is dominated by crossing the JVM-Python boundary and by per-row interpreter overhead. A pandas UDF amortises the crossing over a whole batch and lets the work happen in vectorised NumPy or pandas code, so the per-row cost falls sharply. It still breaks whole-stage codegen, because the data still leaves the JVM. So the ordering is: built-ins beat pandas UDFs, and pandas UDFs beat plain Python UDFs. Variants worth knowing: Series to Series, the scalar case above. Iterator of Series, when you need expensive per-partition setup such as loading a model once. Grouped map, via df.groupBy(...).applyInPandas(fn, schema), which hands you a whole group as a DataFrame. Watch memory: one group must fit. mapInPandas, which streams batches of an entire DataFrame without grouping. Arrow is what makes this work, and it is also what makes toPandas() faster when enabled. 54. How do you handle nulls correctly? Use null-aware operators, because in SQL semantics any comparison with null is null, not false. df.filter(F.col(&quot;city_id&quot;).isNull()) df.filter(F.col(&quot;city_id&quot;).isNotNull()) df.filter(F.col(&quot;a&quot;).eqNullSafe(F.col(&quot;b&quot;))) # null == null is true here df.na.fill({&quot;fare_amount&quot;: 0.0, &quot;city_id&quot;: &quot;unknown&quot;}) df.na.drop(subset=[&quot;trip_id&quot;]) df.withColumn(&quot;city&quot;, F.coalesce(&quot;city_id&quot;, F.lit(&quot;unknown&quot;))) The traps that produce silently wrong results: col == value never matches nulls. A filter meant to exclude one city also drops every row whose city is null. An inner join drops null keys entirely, because null never equals null. Aggregations skip nulls. count(&quot;col&quot;) counts non-null values while count(&quot;*&quot;) counts rows, and avg ignores nulls rather than treating them as zero. A large number of null keys is skew, because they all hash to one partition. And one version-specific change to know: from Spark 4.0 spark.sql.ansi.enabled is on by default, so invalid casts and overflows now raise instead of quietly producing null. That converts a class of silent null-generation into a visible error, which is usually an improvement even when it breaks a job on upgrade. 55. What changed with ANSI mode in Spark 4? It became the default. The migration guide is explicit: Since Spark 4.0, spark.sql.ansi.enabled is on by default. What changes with it on: Operation Legacy behaviour ANSI behaviour CAST(&apos;abc&apos; AS INT) null Raises an error Integer overflow Wraps silently Raises an error Division by zero null Raises an error Reserved keywords Permitted as identifiers Enforced To restore the old behaviour: spark.conf.set(&quot;spark.sql.ansi.enabled&quot;, &quot;false&quot;) There is also a SPARK_ANSI_SQL_MODE environment variable, which the source reads when computing the default. The right framing for an interview is that this converts silent data corruption into loud failure. Under the old behaviour a malformed value became null, flowed through the pipeline, and quietly changed a downstream aggregate. Under ANSI the job fails and you find out. So when a job breaks on upgrade, treat it as a finding rather than a regression: something in the data was already not what the schema claimed. Fix the cast explicitly with try_cast where null genuinely is the right answer, rather than disabling ANSI globally. 56. What is the difference between `union` and `unionByName`? union matches columns by position. unionByName matches them by name. a = spark.createDataFrame([(1, &quot;sf&quot;)], [&quot;id&quot;, &quot;city&quot;]) b = spark.createDataFrame([(&quot;nyc&quot;, 2)], [&quot;city&quot;, &quot;id&quot;]) a.union(b).show() # id=nyc, city=2 &amp;lt;- silently wrong a.unionByName(b).show() # correct This is one of the few Spark behaviours that produces silently wrong data rather than an error, because the types happened to be compatible. If the types had differed you would have got an analysis error and noticed. unionByName also handles schema differences: a.unionByName(b, allowMissingColumns=True) # missing columns become null Without that flag, a name present on one side and not the other is an error, which is usually what you want. The recommendation is to default to unionByName, and to reach for union only when you have deliberately aligned the schemas and want positional semantics. In a code review, a bare union on two DataFrames built by different code paths is worth questioning. Note also that both are narrow transformations: they do not shuffle, they just concatenate partitions. Neither deduplicates, unlike SQL’s UNION, which is union followed by distinct. 57. How do you write partitioned output, and what is the trap? partitionBy writes a directory per distinct value, which lets later readers prune whole directories. (df.write .partitionBy(&quot;city_id&quot;) .mode(&quot;overwrite&quot;) .parquet(&quot;s3a://lakehouse-prod/warehouse/trips&quot;)) trips/city_id=sf/part-00000-....parquet trips/city_id=nyc/part-00000-....parquet The trap is file count, and it is arithmetic: every task that holds rows for a partition value writes a file into it. With 200 shuffle partitions and 250 cities you can get up to 50,000 files. The fix is to repartition by the same columns first, so each value is held by one task: (df.repartition(&quot;city_id&quot;) .write.partitionBy(&quot;city_id&quot;) .mode(&quot;overwrite&quot;) .parquet(path)) Other things to get right: Cardinality. Partition on a low-cardinality column. Partitioning by a timestamp or a user id creates a directory per value, which makes listing slower than scanning. Dynamic overwrite. spark.sql.sources.partitionOverwriteMode set to dynamic makes overwrite replace only the partitions present in the data, instead of the whole table. File size. spark.sql.files.maxRecordsPerFile, default 0 meaning unlimited, caps rows per file when one partition is huge. 58. What is the difference between `partitionBy` and `bucketBy`? Both pre-organise data on write, but they solve different problems.   partitionBy bucketBy Layout A directory per value A fixed number of files per partition Good for Pruning on read Avoiding a shuffle on join or aggregate Cardinality Low Any, hashed into n buckets Requires a metastore No Yes, so saveAsTable only Read benefit Skips directories Skips the shuffle (df.write .bucketBy(64, &quot;rider_id&quot;) .sortBy(&quot;rider_id&quot;) .mode(&quot;overwrite&quot;) .saveAsTable(&quot;lakehouse.trips_bucketed&quot;)) Bucketing works because the bucket count and key are recorded in the metastore, so when two tables are bucketed the same way on the join key, Spark knows matching rows are already co-located and skips the exchange. That is the only way to avoid a shuffle on a large-to-large join. The conditions are strict, and naming them is a good answer: both sides bucketed, on the same column, with the same number of buckets. Mismatched bucket counts means Spark shuffles anyway. The usual pattern is to combine them: partitionBy a date for pruning, and bucketBy a join key within each partition. 59. What are the save modes? Four, set with .mode(): Mode If the target exists error or errorifexists Raise. The default append Add new files alongside overwrite Replace ignore Do nothing, silently df.write.mode(&quot;append&quot;).parquet(path) The two that need care: overwrite on a partitioned table replaces the whole table by default, even if your DataFrame only contains one day. Setting spark.conf.set(&quot;spark.sql.sources.partitionOverwriteMode&quot;, &quot;dynamic&quot;) makes it replace only the partitions present in the data, which is almost always what an incremental job wants. append has no transactional guarantee on plain Parquet. Two concurrent appends can interleave, and a failure mid-write leaves partial files with no way to roll back, because a directory of Parquet files has no commit protocol. That is the structural reason lakehouse formats exist: Hudi, Iceberg and Delta add atomic commits over the same files. ignore is worth flagging in review, because it silently does nothing, which hides a logic error rather than surfacing it. 60. Why is Parquet usually the right format? Three properties that compound: Columnar. Columns are stored separately, so reading 3 of 200 columns touches roughly 1.5% of the bytes. Row formats must read and parse the whole row. Statistics per row group. Each row group carries min, max and null counts per column, so a pushed-down predicate can skip whole groups without decoding them. Compression that works. A column holds like-typed, often similar values, so dictionary and run-length encoding are very effective, which reduces both storage and read time. It also carries a schema, including nested structures, so there is no inference step and no ambiguity about types. spark.read.parquet(path) # schema comes from the files spark.sql.parquet.filterPushdown defaults to true, so the statistics are used by default. When it is not the right choice: Write-once-read-never logs, where a cheaper row format is fine. Row-level random access, which no scan format does well. Very small datasets, where the per-file overhead dominates. Interchange with tools that cannot read it, where CSV wins on ubiquity alone. ORC is broadly comparable and stronger in some Hive-centric stacks; the columnar argument is the same. Avro is row-oriented and better for streaming records and schema evolution on the write path, which is why Hudi uses it for log blocks. 61. How do you read from JDBC in parallel? Give Spark a column to split on, plus bounds and a partition count. Without them the entire table is read by a single task, whatever your cluster size. trips = (spark.read.format(&quot;jdbc&quot;) .option(&quot;url&quot;, &quot;jdbc:postgresql://db.internal:5432/rides&quot;) .option(&quot;dbtable&quot;, &quot;public.trips&quot;) .option(&quot;user&quot;, &quot;etl&quot;) .option(&quot;partitionColumn&quot;, &quot;trip_seq&quot;) .option(&quot;lowerBound&quot;, &quot;1&quot;) .option(&quot;upperBound&quot;, &quot;40000000&quot;) .option(&quot;numPartitions&quot;, &quot;16&quot;) .option(&quot;fetchsize&quot;, &quot;10000&quot;) .load()) Spark turns this into 16 queries with disjoint ranges on trip_seq. The rules: partitionColumn must be numeric, date or timestamp. A string primary key will not work, which catches people with UUID keys. All four options go together. Setting numPartitions alone does nothing. The bounds are for splitting, not filtering. Rows outside them are still read, in the first and last partitions, so badly chosen bounds create skew rather than dropping data. fetchsize controls JDBC round trips and often matters more than partitioning for a moderate table. And the caution to volunteer: 16 parallel connections is 16 concurrent queries against a production database. Parallelism here is a load decision on the source system, not just a Spark setting. 62. What is the difference between `cache` on a DataFrame and a temporary view? They do unrelated things. Caching stores a computed result; a view stores a query. df.createOrReplaceTempView(&quot;trips&quot;) # names a plan df.cache() # stores the data spark.sql(&quot;CACHE TABLE trips&quot;) # names a plan AND stores the data So reading a temporary view re-executes its plan every time. If the plan is expensive and you read it repeatedly, you want caching as well, which is what CACHE TABLE does. One behavioural difference worth knowing: CACHE TABLE is eager by default, populating immediately, while df.cache() is lazy and populates on the next action. CACHE LAZY TABLE makes the SQL form lazy. Two related notes: Caching is by plan, not by name. Two DataFrames with identical plans share a cache entry, which is why caching then re-deriving the same query can hit the cache unexpectedly. A cached DataFrame is pinned until released. unpersist() or spark.catalog.clearCache(); otherwise it holds storage memory for the life of the application and squeezes execution memory from every later stage. Architecture and execution How a line of code becomes work on a cluster. Depth here is the strongest signal an interviewer gets. 63. Walk through what happens when you submit a Spark application. Ten steps, and being able to narrate them in order is the answer. flowchart TB A[&quot;&amp;lt;b&amp;gt;Startup&amp;lt;/b&amp;gt; (1-4)&amp;lt;br/&amp;gt;submit, driver starts,&amp;lt;br/&amp;gt;executors requested and registered&quot;] B[&quot;&amp;lt;b&amp;gt;Plan&amp;lt;/b&amp;gt; (5-7)&amp;lt;br/&amp;gt;code builds a plan, an action submits a job,&amp;lt;br/&amp;gt;Catalyst optimises, DAGScheduler cuts stages&quot;] C[&quot;&amp;lt;b&amp;gt;Execute&amp;lt;/b&amp;gt; (8-9)&amp;lt;br/&amp;gt;TaskSchedulerImpl places tasks,&amp;lt;br/&amp;gt;executors run them and write shuffle output&quot;] D{&quot;more stages?&quot;} E[&quot;&amp;lt;b&amp;gt;Finish&amp;lt;/b&amp;gt; (10)&amp;lt;br/&amp;gt;results to the driver,&amp;lt;br/&amp;gt;or written by the executors&quot;] A --&amp;gt; B --&amp;gt; C --&amp;gt; D D --&amp;gt;|&quot;yes&quot;| C D --&amp;gt;|&quot;no&quot;| E Two details that make the answer stronger: Step 5 is where laziness lives. Everything between the session and the first action is plan construction, so a job that fails at step 6 may have a bug written many lines earlier. Step 10 forks. collect() pulls every row back to the driver; write has each executor write its own partition and return only a status. Same job shape, completely different driver cost. Steps 8 and 9 repeat per stage, and that loop is where adaptive query execution intervenes, because between two passes it has real statistics from the stage that just finished. 64. What does the driver actually do, step by step? Nine responsibilities, in roughly the order they happen: Creates the SparkSession, bringing up SparkContext, SparkEnv, the block manager and the RPC endpoints executors call back on. Asks for executors. SchedulerBackend requests resources from whichever cluster manager --master named. Turns code into a plan. Unresolved, then analysed against the catalog, then optimised by Catalyst, then a physical plan chosen by cost. Cuts the plan into stages. DAGScheduler walks the RDD lineage and breaks it at every ShuffleDependency, producing a TaskSet per stage. Places every task. TaskSchedulerImpl and TaskSetManager decide which executor runs which task, preferring the best available locality. Tracks every attempt. Retries up to spark.task.maxFailures, default 4; speculates slow tasks if enabled; resubmits stages whose shuffle output was lost. Keeps the block index. BlockManagerMaster knows where every cached partition and shuffle block lives, so executors can fetch from each other. Receives results, bounded in total by spark.driver.maxResultSize, default 1g. Serves the UI on port 4040 and writes the event log the history server replays. The two facts that follow: spark.driver.memory defaults to 1g, which is a coordinator’s budget and not a data-processing one; and the driver is the one process with no redundancy, because items 3 to 7 exist nowhere else. 65. What does an executor do, step by step? Much less than the driver, deliberately. Registers. CoarseGrainedExecutorBackend sends RegisterExecutor and waits for RegisteredExecutor before accepting work. Until that round trip completes, a granted container is idle. Accepts LaunchTask and runs each task in a thread from its pool. Concurrency is spark.executor.cores, which defaults to 1 on YARN and to all cores in standalone mode. Reads input, runs the generated code, and on a shuffle map stage writes partitioned output to local disk. Reports back with StatusUpdate per task and a heartbeat every spark.executor.heartbeatInterval, default 10s. Miss enough and the driver declares it lost. Stores and serves blocks through its BlockManager, which makes a cached DataFrame reusable and lets the next stage fetch shuffle data from peers. The contract is narrow on purpose: an executor never makes a scheduling decision, never sees the query plan, and never coordinates with another executor about what to run. It receives tasks, runs them, serves bytes. That is exactly what makes executors disposable. Lose one and its tasks are retried elsewhere with its cached partitions recomputed from lineage. It is also why executors are per-application rather than shared: memory and slots are reserved for one application’s work. 66. What does the cluster manager do? One thing: decide who gets containers. It is a resource manager, not a work scheduler. Cluster manager What grants the container What Spark runs there Standalone Master, tracking registered Worker daemons The worker launches an executor JVM per grant YARN The ResourceManager, asked by Spark’s ApplicationMaster through YarnAllocator An executor per allocated container Kubernetes The API server. The driver’s own ExecutorPodsAllocator creates pods An executor per pod Kubernetes is the clearest illustration of the split, because there is no Spark cluster-manager daemon at all: the driver talks to the API server directly and asks for pods. The component granting resources and the component scheduling work are simply different things, however they are packaged. Two defaults follow from this being a separate concern: spark.dynamicAllocation.enabled is false, so absent a decision your application holds its executors from first request to exit, idle or not. And Mesos was removed in the 4.x line, so the three rows above are the whole list. The practical consequence people find surprising: tuning YARN’s scheduler changes what you are given, never how your tasks are distributed across what you were given. That is the driver’s job. 67. What is `DAGScheduler` responsible for? Stage-level scheduling. Given a job, it: Walks the RDD lineage backwards from the action. Cuts a new stage at every ShuffleDependency, absorbing all narrow dependencies into the current stage. Determines the order stages must run in, and which can run in parallel. Submits each ready stage to TaskScheduler as a TaskSet. Handles FetchFailedException by resubmitting the upstream stage, because lost shuffle output cannot be re-fetched. Computes preferred locations for tasks from the RDD’s getPreferredLocations. The distinction from TaskScheduler is the point of the question: DAGScheduler decides what stages exist and in what order; TaskSchedulerImpl decides which executor runs which individual task. One is about the shape of the job, the other about placement. flowchart TB J[&quot;job&quot;] --&amp;gt; D[&quot;DAGScheduler&amp;lt;br/&amp;gt;cuts stages at shuffles&quot;] D --&amp;gt; TS[&quot;TaskSet per stage&quot;] TS --&amp;gt; T[&quot;TaskSchedulerImpl&amp;lt;br/&amp;gt;places each task&quot;] T --&amp;gt; B[&quot;SchedulerBackend&quot;] --&amp;gt; E[&quot;executors&quot;] The stage-resubmission behaviour is the one to volunteer, because it explains a confusing UI: a job that appears to go backwards, re-running a stage that had already completed, is DAGScheduler rebuilding shuffle output that died with an executor. 68. What is `TaskSchedulerImpl` responsible for? Task-level scheduling: taking a TaskSet and deciding which executor runs each task in it. Its concerns: Placement by locality. It offers each task the best available locality level, waiting briefly (spark.locality.wait) before settling for a worse one. Retries. Failed tasks are rescheduled up to spark.task.maxFailures, default 4, preferring a different executor when the failure looked executor-specific. Speculation. If enabled, duplicating tasks that lag their peers. Excluding bad executors. If one executor or node keeps failing tasks, it can stop scheduling there so a single bad machine does not burn every task’s retry budget. Fair or FIFO ordering between concurrent TaskSets, per spark.scheduler.mode, default FIFO. It works with a TaskSetManager per stage, which holds the per-task state, and talks to the cluster through SchedulerBackend. The reason to know the split: when tasks are not being placed where you expect, the question is a TaskSchedulerImpl and locality question. When stages are not in the shape you expect, it is a DAGScheduler question. Knowing which component owns which symptom is most of debugging. 69. What is `SchedulerBackend`? The single component that talks to the cluster manager, with an implementation per manager. It is the boundary between Spark’s scheduling and the outside world. Its job: Request and release executors. Including the dynamic-allocation calls to scale up and down. Receive executor registration and tell the scheduler how many slots exist. Launch tasks onto executors, and relay their status updates back. Implementations include CoarseGrainedSchedulerBackend as the common base, with YARN, Kubernetes and standalone specialisations, plus LocalSchedulerBackend for local[*]. “Coarse-grained” is worth explaining because the name is opaque: the executor is acquired once and held for the application’s lifetime, running many tasks. The alternative, fine-grained acquisition per task, existed under Mesos and is gone. This is the layer that makes Spark portable. The whole scheduler above it is identical on every cluster manager, and only SchedulerBackend changes. That is why the same jar runs on YARN and Kubernetes with nothing but --master different. 70. What is a `ShuffleMapStage` versus a `ResultStage`? Every stage but the last in a job is a ShuffleMapStage; the last is a ResultStage.   ShuffleMapStage ResultStage Output Shuffle files on local disk The action’s result Purpose Feed the next stage Answer the action Consumed by The next stage’s fetch The driver, or a write Rerunnable Yes, and resubmitted if output is lost Yes The reason the distinction exists in the scheduler is recovery. ShuffleMapStage output is registered with MapOutputTracker on the driver, so Spark knows where every map output block lives. If an executor dies and takes blocks with it, DAGScheduler knows which stage to resubmit and which partitions of it are missing. A ResultStage has nothing registered, because its output goes to the driver or to storage. This is also why the UI shows the final stage differently, and why a ShuffleMapStage can be skipped: if its output is still registered from a previous job, Spark reuses it rather than recomputing. Those grey “skipped stages” in the UI are a feature, not a problem, and they are what makes reusing a shuffle across two actions cheap. 71. What are the task locality levels? Five, best first: Level Meaning PROCESS_LOCAL The data is in this executor’s memory already NODE_LOCAL On the same machine, another executor or local disk or HDFS NO_PREF No preference, for example a JDBC source RACK_LOCAL On the same rack ANY Anywhere TaskSchedulerImpl offers each task the best level available, and if nothing at that level is free it waits spark.locality.wait, default 3s, before settling for the next level down. flowchart LR A[&quot;task ready&quot;] --&amp;gt; B{&quot;PROCESS_LOCAL&amp;lt;br/&amp;gt;slot free?&quot;} B --&amp;gt;|&quot;yes&quot;| R[&quot;run&quot;] B --&amp;gt;|&quot;no&quot;| C[&quot;wait spark.locality.wait&quot;] C --&amp;gt; D{&quot;NODE_LOCAL free?&quot;} D --&amp;gt;|&quot;yes&quot;| R D --&amp;gt;|&quot;no&quot;| E[&quot;degrade further, to ANY&quot;] The modern caveat is the useful part. Locality was designed for HDFS, where compute ran on the machines holding the blocks. Reading from S3 or another object store means no partition has a meaningful preferred location, so most tasks are NO_PREF or ANY and the waiting buys nothing. On such clusters, lowering spark.locality.wait can measurably reduce scheduling delay. Where locality still matters is cached data: a PROCESS_LOCAL hit on a cached partition avoids a network fetch entirely. 72. Where does the input partition count come from? Not from the number of files, which is the assumption that costs people the most time. FilePartition.maxSplitBytes decides the split size first: Math.min(defaultMaxSplitBytes, Math.max(openCostInBytes, bytesPerCore)) defaultMaxSplitBytes is spark.sql.files.maxPartitionBytes, default 128MB. openCostInBytes is spark.sql.files.openCostInBytes, default 4MB, charged per file so many small files are packed together rather than each getting a task. bytesPerCore is the total size divided by spark.sql.files.minPartitionNum, which falls back to the default parallelism. The consequence is measurable. Write the same data as 1, 8 and 16 Parquet files, then read each back: Files written Partitions on read 1 1 8 2 16 2 File count did not determine partition count in any of them, because the files were small enough to pack. Two directions follow: a directory of small files collapses into far fewer partitions than it has files, and a single file larger than the split size is divided into several. So read the number rather than predicting it: print(df.rdd.getNumPartitions()) 73. How many tasks will a stage have? One per partition of that stage. Nothing else affects it. Where that partition count comes from depends on the stage: The first stage takes it from input splitting, so spark.sql.files.maxPartitionBytes and file sizes. A stage after a shuffle takes it from spark.sql.shuffle.partitions, default 200, or whatever adaptive coalescing reduced it to at runtime. After an explicit repartition(n), it is n. It is not related to the number of executors, and that confusion is the reason the question gets asked. With 200 tasks and 10 executors of 4 cores each, you have 40 slots, so the stage runs in 5 waves. Tasks queue for slots; they are not divided among executors up front. The arithmetic worth having ready: slots = num_executors x spark.executor.cores waves = ceil(tasks / slots) A stage with fewer tasks than slots leaves cores idle. A stage with thousands of very short tasks spends its time in scheduling. The target is several waves of tasks each doing a meaningful amount of work, which is what “partitions in the low hundreds of megabytes” is really a proxy for. 74. What happens when an executor is lost? The work is redone, and the reason it can be redone is lineage. Recovery is per partition, not per job: losing one executor costs the partitions it held, which are rebuilt on whichever executors remain while everything else stands. Its running tasks are retried elsewhere, counting against spark.task.maxFailures. Cached blocks are recomputed from lineage, unless the storage level replicated them (MEMORY_ONLY_2 and friends), in which case the replica is promoted. Shuffle output is the special case worth volunteering: flowchart LR A[&quot;stage 0 completed&quot;] --&amp;gt; B[&quot;executor dies with&amp;lt;br/&amp;gt;its shuffle files&quot;] B --&amp;gt; C[&quot;stage 1 fetch fails&amp;lt;br/&amp;gt;FetchFailedException&quot;] C --&amp;gt; D[&quot;DAGScheduler resubmits&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;stage 0&amp;lt;/b&amp;gt;&quot;] That is a stage rerun rather than a task retry, and it is why a job can appear to go backwards in the UI. An external shuffle service avoids it by serving shuffle blocks from a node-level daemon that outlives the executor, which is also why dynamic allocation wants one. The contrast with the driver is the closing point: executors hold only recomputable state, so they are disposable; the driver holds the plan, the scheduler and the block index, so it is not. 75. Why does losing the driver kill the application, but losing an executor not? Because of what each holds that exists nowhere else.   Driver Executor Holds Plan, scheduler state, block index, SparkContext Task threads, cached blocks, shuffle files Recoverable from elsewhere No Yes, from lineage or a peer On failure Application ends Tasks retried, partitions recomputed An executor’s contents are either derivable (cached partitions, from lineage) or reproducible (shuffle output, by rerunning the stage). So Spark can always reconstruct them. The driver’s contents are not derivable from anything. It is the only process that knows what the job is, which stages remain, which tasks succeeded, and where every block lives. There is no second copy, so there is nothing to recover from. What you can do about it: Cluster deploy mode puts the driver inside the cluster, where the manager can restart it. On standalone, --supervise restarts a failed driver. Structured Streaming checkpoints let a restarted driver resume from recorded offsets and state, which is the closest thing to driver fault tolerance in practice. Keep the driver unloaded. Most driver failures are self-inflicted: collect() on a large result, a broadcast that is not small, or hundreds of thousands of tasks to track. 76. What is the external shuffle service, and why use it? A daemon running on each node that serves shuffle blocks on behalf of the executors that wrote them. spark.shuffle.service.enabled defaults to false. Without it, shuffle output lives inside the executor process, so the executor must stay alive for as long as anything might fetch from it. With it, the blocks are on the node’s disk and served by the daemon, so the executor can exit freely. flowchart TB subgraph N[&quot;without the service&quot;] E1[&quot;executor&amp;lt;br/&amp;gt;holds shuffle files&quot;] --&amp;gt;|&quot;must stay alive&quot;| F1[&quot;reduce tasks fetch&quot;] end subgraph W[&quot;with the service&quot;] E2[&quot;executor&amp;lt;br/&amp;gt;can exit&quot;] --&amp;gt; D2[&quot;node shuffle service&amp;lt;br/&amp;gt;serves the files&quot;] --&amp;gt; F2[&quot;reduce tasks fetch&quot;] end Why it matters: Dynamic allocation needs it, or an equivalent. Otherwise Spark cannot release an idle executor that still holds shuffle data someone might want. The alternative is spark.dynamicAllocation.shuffleTracking.enabled, default true, which tracks which executors hold live shuffle blocks and keeps only those. It reduces FetchFailedException, because an executor dying no longer destroys its shuffle output. It offloads serving work from the executor JVM, which is otherwise doing your computation. The cost is an extra daemon to deploy and operate per node, which on YARN means an auxiliary service in the NodeManager. 77. What is dynamic allocation? Adding and removing executors according to pending work, rather than holding a fixed set. spark.dynamicAllocation.enabled defaults to false, so absent a decision your application holds its executors from first request to exit, idle or not. How it behaves: Scales up when tasks are pending, adding executors exponentially up to maxExecutors. Scales down when an executor has been idle for executorIdleTimeout. Respects minExecutors as a floor, and initialExecutors at start. --conf spark.dynamicAllocation.enabled=true --conf spark.dynamicAllocation.minExecutors=2 --conf spark.dynamicAllocation.maxExecutors=100 The shuffle problem is the part to know. An idle executor may still hold shuffle blocks a later stage needs, so releasing it would cause fetch failures. Two solutions: an external shuffle service, or spark.dynamicAllocation.shuffleTracking.enabled, which defaults to true and keeps executors that hold live shuffle data. When it helps: interactive and bursty workloads, notebooks, multi-tenant clusters, and jobs whose parallelism varies a lot between stages. When it hurts: short jobs, where the ramp-up costs more than it saves, and jobs with strict latency needs, where waiting for executors is worse than holding them. Note also that setting --num-executors alongside it is contradictory: the static value only acts as an initial size. 78. What is speculative execution? Re-running tasks that are running much slower than their peers, and taking whichever attempt finishes first. spark.speculation defaults to false. Spark watches the distribution of task durations within a stage. Once a configurable fraction of tasks has completed, any task running more than spark.speculation.multiplier times the median is duplicated on another executor. When it helps: a genuinely slow machine. A failing disk, a noisy neighbour, a node with thermal throttling. The duplicate runs elsewhere and finishes normally. When it hurts, and this is the answer interviewers want: data skew. If the task is slow because its partition holds ten times the rows, the duplicate has exactly the same work to do and is equally slow. You have now consumed two slots to get one result no sooner. Worse, it can mask the skew, because the symptom looks like flaky infrastructure rather than an uneven distribution. So the diagnostic order is: check whether slow tasks also have larger shuffle read. If they do, it is skew and speculation is the wrong tool. If durations vary while input sizes are equal, it is the environment and speculation helps. One more caution: with a non-idempotent sink, two attempts writing the same output can duplicate data. Speculation assumes writes are safe to repeat. 79. What is the default scheduler mode within an application? spark.scheduler.mode defaults to FIFO. That governs how concurrent jobs within one application share executor slots, which only matters when you submit jobs from multiple threads or serve multiple users from one SparkSession. Mode Behaviour FIFO Jobs run in submission order; the first takes all the slots it can use FAIR Slots are shared round-robin between active jobs Under FIFO, a large job submitted first can starve a small interactive query submitted second, because it holds every slot until it is done. That is precisely the wrong behaviour for a notebook server or a Thrift server, where a quick query should not wait behind an hour-long batch. --conf spark.scheduler.mode=FAIR --conf spark.scheduler.allocation.file=/etc/spark/fairscheduler.xml FAIR also supports pools with weights and minimum shares, so you can give an interactive pool priority over a batch pool. A job joins a pool by setting a thread-local property: sc.setLocalProperty(&quot;spark.scheduler.pool&quot;, &quot;interactive&quot;) Keep the levels distinct: this is scheduling within an application. Sharing between applications is the cluster manager’s job, with YARN queues or Kubernetes quotas. 80. How do you set JVM options for the driver and executors? Two configs, one per side: spark-submit \ --conf &quot;spark.driver.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot; \ --conf &quot;spark.executor.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties -verbose:class&quot; \ app.jar They are the delivery mechanism for almost every JVM-level change: logging configuration, -verbose:class for class-loading problems, -Xss for stack size, heap dump flags, proxy properties, and GC settings. The client-mode trap is the part that catches people. In client mode the driver JVM is already running by the time your SparkConf executes, so a driver JVM option set in code arrives too late to have any effect. Use --driver-java-options, or a properties file, or set it before the JVM starts. Two more notes: Do not set -Xmx here. Heap comes from spark.driver.memory and spark.executor.memory; setting it in extra options conflicts with them. Avoid secrets. The Environment tab of the UI displays configs, so a password in extraJavaOptions is visible to anyone who can reach the UI. To confirm a flag took effect, the Environment tab is authoritative, and --verbose on submit prints where each value came from. 81. What is `spark.driver.maxResultSize` and why does it exist? A cap on the total serialized size of results returned to the driver, default 1g. Exceeding it aborts the job with an error naming the config. It exists because without it, collect() on a large dataset would simply exhaust the driver heap and produce an OutOfMemoryError, which is harder to diagnose and can take the application down mid-flight. The cap converts that into a clear, early, named failure. spark.conf.get(&quot;spark.driver.maxResultSize&quot;) # 1g Note it counts the total across all tasks in an action, not per task, and it is checked as results arrive, so a job can fail partway through collecting. The right response is almost never to raise it. If you are hitting a 1 GB limit, the question is why a gigabyte of rows needs to be in the driver: Writing out? Use write, so executors write in parallel and only statuses return. Inspecting? show(), take() or limit(). Counting or aggregating? Do it as an aggregation, which returns one row. Handing to Python? toPandas() has the same problem; consider mapInPandas to keep the work distributed. Setting it to 0 disables the check, which trades a clear error for an OutOfMemoryError. Raising it is defensible only when you genuinely need a large result on the driver and have sized spark.driver.memory accordingly. 82. What port does the Spark UI use, and what happens after the application ends? spark.ui.port defaults to 4040. If that port is taken, Spark increments and tries 4041, 4042 and so on, which is why a second concurrent application appears on a different port. The UI dies with the application. It is served by the driver, so when the driver exits there is nothing left to serve it. That is a problem, because most investigation happens after a job has failed or finished. The solution is two-part: --conf spark.eventLog.enabled=true --conf spark.eventLog.dir=hdfs:///spark-events The driver writes an event log, and a separately running history server reads that directory and replays the logs, giving you the same UI for completed applications. Without event logging, a job that failed overnight leaves you with only the driver’s stdout, which is why enabling it is close to mandatory in production. Two practical notes: event logs grow, so they need retention and ideally compression (spark.eventLog.compress); and on YARN the ResourceManager UI links to the history server, so the “tracking URL” for a finished application lands there rather than at a dead port 4040. 83. How do broadcast variables and accumulators relate to the driver and executors? They are the two sanctioned ways to move data between driver and executors, one in each direction. A task runs in another JVM, so an ordinary variable captured by a closure is serialized, shipped and then discarded. Updates to it never come back, which is why both mechanisms exist. flowchart LR D[&quot;driver&quot;] --&amp;gt;|&quot;broadcast: one copy per executor&quot;| E[&quot;executors&quot;] E --&amp;gt;|&quot;accumulator: merged updates&quot;| D Broadcast, outward. A read-only value cached once per executor rather than serialized per task. The saving is per-task-count: with 10,000 tasks and a 10 MB table, one copy per executor instead of 10,000 copies. Accumulators, inward. Tasks add, the driver reads. The guarantee is exactly-once only for updates inside actions; in a transformation, a recompute or retry applies the update again. What neither does: give you shared mutable state. There is no way for two tasks to coordinate through a variable, which is deliberate. If tasks need to agree on something, that is what a shuffle is for. The common mistake is closing over a large object without broadcasting it, which silently multiplies serialization cost by the task count and often shows up as inexplicably slow task launch. 84. What is Spark Connect? A client-server architecture where the client builds an unresolved logical plan and sends it to a remote Spark server over gRPC, instead of the client being the driver. Traditionally your application is the driver: it holds the SparkContext, it must be a JVM (or a JVM plus a Python process), and it must be network-close to the executors. Spark Connect splits that. flowchart LR C[&quot;thin client&amp;lt;br/&amp;gt;Python, Scala, Go, JVM&quot;] --&amp;gt;|&quot;unresolved plan&amp;lt;br/&amp;gt;over gRPC&quot;| S[&quot;Spark Connect server&amp;lt;br/&amp;gt;holds the real driver&quot;] S --&amp;gt; E[&quot;executors&quot;] What it buys: Thin clients. No local JVM, no cluster-side dependencies, so a notebook, an IDE or a web service can drive a cluster. Isolation. User code crashing or leaking memory no longer takes down the driver, because it is not in the driver. Upgradeability. Client and server versions are decoupled, so the cluster can move independently. Multi-language. The protocol, not the JVM, is the interface, which is how non-JVM clients become possible. The trade is that anything depending on being inside the driver does not work: SparkContext internals, RDD APIs in general, and code that assumes JVM colocation. So it is a DataFrame and SQL interface, which covers most modern usage. Partitioning and shuffle The single biggest source of Spark performance problems, and therefore of interview questions. Expect to be pushed for detail here. 85. What is a shuffle? A redistribution of data across partitions so that rows which must be processed together end up in the same partition. It has two halves: The map side writes each record into a bucket for its target partition, sorts or spills as needed, and writes partitioned output to local disk, along with an index of where each partition’s bytes start. The reduce side fetches its own slice from every map output, over the network, then merges them. flowchart TB subgraph M[&quot;map side, stage 0&quot;] M1[&quot;task 0&quot;] --&amp;gt; F1[(&quot;file + index&quot;)] M2[&quot;task 1&quot;] --&amp;gt; F2[(&quot;file + index&quot;)] M3[&quot;task 2&quot;] --&amp;gt; F3[(&quot;file + index&quot;)] end subgraph R[&quot;reduce side, stage 1&quot;] F1 --&amp;gt; R1[&quot;task 0 fetches&amp;lt;br/&amp;gt;its slice from all 3&quot;] F2 --&amp;gt; R1 F3 --&amp;gt; R1 F1 --&amp;gt; R2[&quot;task 1&quot;] F2 --&amp;gt; R2 F3 --&amp;gt; R2 end Three consequences worth stating: It always touches disk, even when nothing spills, because map output is written to local disk before being served. Shuffle is not an in-memory operation. It is all-to-all. With m map tasks and r reduce tasks there are m x r block fetches, which is why very high parallelism on both sides is its own cost. It creates a barrier. Stage 1 cannot start until stage 0 has finished writing every partition, so one straggler delays everything. 86. What triggers a shuffle? Any wide dependency. In practice: Operation Why groupBy, groupByKey Rows with the same key must meet join without a broadcast Both sides must be co-partitioned on the key distinct, dropDuplicates Duplicates must meet to be removed repartition By definition orderBy, sort Global ordering needs range partitioning reduceByKey, aggregateByKey Combines locally first, but still shuffles Window functions Unless already partitioned by the same key What does not shuffle: map, filter, select, withColumn, union, coalesce, and a join where one side is broadcast. The reliable way to count them is the plan, because every shuffle is an Exchange: df.explain(&quot;formatted&quot;) Exchange hashpartitioning(city_id#21, 200), ENSURE_REQUIREMENTS The description tells you the key and the target partition count, so you can see not just that it shuffles but on what. The optimisation this enables: if two consecutive operations shuffle on the same key, Spark needs only one exchange. So aggregating and joining on the same column is much cheaper than on different columns, and that is often something you can arrange. 87. Why is a shuffle expensive? Five costs, and naming several of them is the difference between a shallow and a deep answer. Serialization and deserialization. Every record crossing the boundary must be encoded and decoded. Disk I/O. Map output is always written to local disk, and spills add more. Network transfer. Every reduce task pulls from every map task, so the fetch pattern is all-to-all. Sorting or merging. Depending on the writer path, records are sorted on the map side and merged on the reduce side. The stage barrier. No reduce task can start before every map task has finished, so the slowest map task sets the floor. The barrier is the one people forget, and it is why skew is so damaging: a single slow map task holds up the entire downstream stage, wasting every other slot. There is also a scaling cost: with m map and r reduce tasks there are m x r blocks to track and fetch. Very high parallelism on both sides turns into metadata and connection overhead, which is one reason “just raise the partition count” has a limit. What to do about it, in order: eliminate the shuffle (broadcast, or bucket the tables); reduce the data entering it (filter and project first); reduce the number of shuffles (aggregate and join on the same key); then size its partitions sensibly. 88. What are Spark&apos;s shuffle writers? SortShuffleManager selects between three writer paths, checked cheapest first: Writer Chosen when Behaviour Bypass merge sort No map-side combine, and partition count below spark.shuffle.sort.bypassMergeThreshold Writes one file per partition then concatenates. No sorting Unsafe (serialized) sort No map-side combine, and the serializer supports relocating serialized objects Sorts serialized records in binary form, so no deserialization Sort Everything else Sorts deserialized records, spilling as needed. Most general, most expensive The condition that disqualifies both fast paths is the same: a map-side combine. And a map-side combine is exactly what reduceByKey, aggregateByKey and a partial aggregation do. So there is a real tension worth articulating: reduceByKey -&amp;gt; less data shuffled, through the slowest writer groupByKey -&amp;gt; more data shuffled, possibly through a faster writer reduceByKey still usually wins, because network volume dominates writer efficiency, and because groupByKey risks an OutOfMemoryError on a hot key. But knowing why the trade exists, rather than reciting “always use reduceByKey”, is what a deeper question is probing for. For DataFrames you rarely choose directly: Catalyst inserts partial aggregation automatically, which is a map-side combine, so aggregations take the sort path. 89. What is data skew? An uneven distribution of rows across partitions, so some tasks have far more work than others. The cause is almost always the data itself: one key dominates. A city_id column where 60% of trips are in one city, a customer_id where a single enterprise account has millions of rows, or a null key that every unmatched row shares. flowchart TB subgraph E[&quot;even&quot;] A1[&quot;task 0: 1M rows&quot;] --- A2[&quot;task 1: 1M&quot;] --- A3[&quot;task 2: 1M&quot;] --- A4[&quot;task 3: 1M&quot;] end subgraph S[&quot;skewed&quot;] B1[&quot;task 0: 100K&quot;] --- B2[&quot;task 1: 100K&quot;] --- B3[&quot;task 2: &amp;lt;b&amp;gt;3.7M&amp;lt;/b&amp;gt;&quot;] --- B4[&quot;task 3: 100K&quot;] end Why it is so costly: a task is the unit of work and cannot be split. One task with ten times the rows takes roughly ten times as long, and because of the stage barrier, every other slot sits idle waiting for it. A 200-task stage can be limited by one task. The signatures to recognise: One or two tasks with far longer duration than the median, in the same stage. Those same tasks showing much larger shuffle read. A stage that is 95% complete for a long time. An OutOfMemoryError on one executor while others are fine. Skew is a data problem that presents as a performance problem, which is why config tuning rarely fixes it. 90. How do you detect skew? Two places: the UI, and the data. In the UI, open the stage page and read the task metrics summary, which gives min, 25th percentile, median, 75th and max for duration and shuffle read. A median of 3 seconds with a max of 40 minutes is skew, and the shuffle read column confirms it is data volume rather than a slow machine. That distinction matters: Duration Shuffle read Diagnosis Max » median Max » median Skew. Fix the distribution Max » median Roughly even Environment. A slow node, GC, or a bad disk In the data, count rows per key and look at the head of the distribution: (df.groupBy(&quot;city_id&quot;).count() .orderBy(F.desc(&quot;count&quot;)) .show(20, truncate=False)) For an RDD, count rows per partition directly: print(rdd.mapPartitionsWithIndex( lambda i, rows: [(i, sum(1 for _ in rows))]).collect()) And for a join, remember to check both sides plus the null count on the key, since nulls all hash together: df.filter(F.col(&quot;city_id&quot;).isNull()).count() 91. How do you fix skew? In order of preference, cheapest and least invasive first. 1. Let AQE handle it. spark.sql.adaptive.skewJoin.enabled defaults to true and splits skewed partitions automatically. Check the thresholds first, because they are strict: it needs a partition larger than 5x the median and larger than 256 MB. 2. Broadcast the small side, if there is one. No shuffle means no skew. trips.join(F.broadcast(cities), &quot;city_id&quot;) 3. Salt the hot key. Spread it across N synthetic sub-keys, join, then aggregate the salt away. This is the general answer when the hot key is genuinely large. 4. Separate the hot keys. Process them with a different strategy and union the results. More code, but optimal when there are only a handful. hot = [&quot;sf&quot;] a = trips.filter(F.col(&quot;city_id&quot;).isin(hot)).join(F.broadcast(cities), &quot;city_id&quot;) b = trips.filter(~F.col(&quot;city_id&quot;).isin(hot)).join(cities, &quot;city_id&quot;) result = a.unionByName(b) 5. Filter nulls out of the join key if they carry no meaning, since they all land in one partition. What not to do: enable speculation. The duplicate task has the same rows and is equally slow, so it burns a second slot for nothing. 92. How does salting work in practice? Add a random suffix to the hot key on the fact side, and replicate the dimension side once per possible suffix so every match still exists. from pyspark.sql import functions as F N = 16 facts = trips.withColumn(&quot;salt&quot;, (F.rand() * N).cast(&quot;int&quot;)) dims = (cities .withColumn(&quot;salt&quot;, F.explode(F.array([F.lit(i) for i in range(N)])))) joined = facts.join(dims, [&quot;city_id&quot;, &quot;salt&quot;]).drop(&quot;salt&quot;) flowchart TB A[&quot;all &apos;sf&apos; rows&amp;lt;br/&amp;gt;one partition&quot;] --&amp;gt; B[&quot;salt 0..15&quot;] B --&amp;gt; C[&quot;16 partitions:&amp;lt;br/&amp;gt;sf-0, sf-1, ... sf-15&quot;] D[&quot;cities row &apos;sf&apos;&quot;] --&amp;gt; E[&quot;replicated x16&amp;lt;br/&amp;gt;sf-0 ... sf-15&quot;] C --&amp;gt; F[&quot;join, now spread&amp;lt;br/&amp;gt;across 16 tasks&quot;] E --&amp;gt; F The trade is explicit: the dimension side is N times larger. With N=16 and a 1 MB dimension that is 16 MB, which is nothing. With a 3 GB dimension it is 48 GB, which is not. So N stays small, and salting suits a large-fact-to-small-dimension shape. For an aggregation rather than a join, salting is two-phase and cheaper, because nothing is replicated: (trips .withColumn(&quot;salt&quot;, (F.rand() * N).cast(&quot;int&quot;)) .groupBy(&quot;city_id&quot;, &quot;salt&quot;).agg(F.sum(&quot;fare_amount&quot;).alias(&quot;part&quot;)) .groupBy(&quot;city_id&quot;).agg(F.sum(&quot;part&quot;).alias(&quot;total&quot;))) Aggregate per salted key, then aggregate the partials. The first pass spreads the hot key over N tasks; the second is tiny. 93. What are the adaptive skew join settings and their defaults? Config Default spark.sql.adaptive.enabled true spark.sql.adaptive.skewJoin.enabled true spark.sql.adaptive.skewJoin.skewedPartitionFactor 5.0 spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes 256MB Both conditions must hold. A partition is treated as skewed only when it is larger than 5 times the median and larger than 256 MB. That conjunction is the answer to “AQE skew handling is on but nothing happened”. On a job whose partitions are all under 256 MB, skew handling never fires no matter how uneven they are. A stage with a 2 MB median and a 100 MB outlier is 50x skewed and completely untouched. # make it fire on a smaller job spark.conf.set(&quot;spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes&quot;, &quot;32MB&quot;) When it does fire, Spark splits the skewed partition into several smaller reduce tasks and replicates the matching partition from the other side so each split still finds its matches. That is salting, done automatically at runtime. Two limits worth knowing: it applies to joins, so a skewed groupBy is not covered, and it needs the shuffle to have actually happened, so it cannot help a skewed read. 94. What is adaptive query execution? Re-planning parts of a query at runtime, using real statistics from stages that have already finished, instead of relying on planning-time estimates. It is on by default since Spark 3.2: spark.sql.adaptive.enabled is true. Three things it does: Optimisation What it fixes Coalescing shuffle partitions A static 200 that is wrong for this data Switching join strategy An estimate that said “large” when the side is actually small Splitting skewed partitions One task with far more rows than its peers flowchart TB S[&quot;a shuffle stage completes:&amp;lt;br/&amp;gt;real sizes are now known&quot;] S --&amp;gt; A[&quot;&amp;lt;b&amp;gt;coalesce partitions&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;merge the small ones&quot;] S --&amp;gt; B[&quot;&amp;lt;b&amp;gt;switch join strategy&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;sort-merge to broadcast&quot;] S --&amp;gt; C[&quot;&amp;lt;b&amp;gt;split skewed partitions&amp;lt;/b&amp;gt;&amp;lt;br/&amp;gt;one big task into several&quot;] Why it works so well: planning-time estimates on derived data are usually wrong. Spark can estimate the size of a table it has statistics for, but after three filters and a join, the estimate is a guess. After the shuffle, it is a measurement. Reading it: before execution the plan shows AdaptiveSparkPlan isFinalPlan=false, so explain() gives you the planned shape. To see what actually ran, look at the SQL tab after the query finishes. The main caveat is that AQE works at shuffle boundaries, so a query with no shuffle gets nothing from it, and the first stage is always planned blind. 95. What does AQE partition coalescing do? After a shuffle, it merges small output partitions into fewer, larger tasks. The relevant settings: Config Default spark.sql.adaptive.coalescePartitions.enabled true spark.sql.adaptive.advisoryPartitionSizeInBytes 64MB spark.sql.adaptive.coalescePartitions.minPartitionSize 1MB Spark writes the full spark.sql.shuffle.partitions worth of output, looks at the actual size of each, and then assigns contiguous runs of them to single reduce tasks so each task gets roughly the advisory size. 200 shuffle partitions, 800 MB total -&amp;gt; without coalescing: 200 tasks x 4 MB -&amp;gt; with coalescing: 13 tasks x ~64 MB This is what makes the static default tolerable. Before AQE, 200 had to be tuned per job or you paid for it; now a high value is mostly harmless, because coalescing brings it down. The practical consequence for tuning: set the advisory size, not the partition count. spark.conf.set(&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;, &quot;128MB&quot;) That expresses the intent (“I want partitions about this big”) and lets Spark do the arithmetic per stage, which is better than one number for a query whose stages differ in size. Note it only merges; it never splits. A stage whose partitions are too large needs a higher spark.sql.shuffle.partitions, or skew handling. 96. How do you choose the number of shuffle partitions? Work from two constraints and take whichever is larger. Constraint 1, partition size. Aim for partitions in the low hundreds of megabytes of shuffled data. Too large and you spill; too small and scheduling dominates. partitions = total shuffle bytes / target partition size = 600 GB / 128 MB = ~4,800 Constraint 2, core count. You want a small multiple of your total cores, so every core gets several tasks and stragglers average out. cores = num_executors x spark.executor.cores = 50 x 4 = 200 partitions = 200 x 3 = 600 (a floor, not a target) With AQE, the better move is to express the first constraint directly and let coalescing handle the rest: spark.conf.set(&quot;spark.sql.shuffle.partitions&quot;, &quot;2000&quot;) # generous spark.conf.set(&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;, &quot;128MB&quot;) Set the count high enough that no partition is too big, and let AQE merge the small ones down. That handles a query whose stages differ in volume, which a single static number cannot. The diagnostic loop: if the stage page shows spill, partitions are too large. If it shows thousands of tasks lasting milliseconds, they are too small. Those two symptoms are the feedback signal, not a formula. 97. What is the small files problem, and how do you avoid it? Many tiny files make every later read slow, for reasons that are not about total bytes: Listing cost. Planning must enumerate the files, which on object storage means many API calls. Per-file open cost. Spark charges spark.sql.files.openCostInBytes, default 4MB, per file when computing splits, precisely because opening one is not free. Metadata overhead. Each Parquet file has a footer to read. Compression loss. Small files compress worse, because dictionaries and encodings have less to work with. Where they come from, and the fix for each: Cause Fix High write parallelism coalesce(n) before the write partitionBy on many values repartition(same cols) first Streaming with a short trigger Longer trigger, or periodic compaction Over-partitioning Partition on a lower-cardinality column (df.repartition(&quot;city_id&quot;) .write.partitionBy(&quot;city_id&quot;) .mode(&quot;overwrite&quot;).parquet(path)) You can also cap file size rather than count: spark.conf.set(&quot;spark.sql.files.maxRecordsPerFile&quot;, &quot;5000000&quot;) which defaults to 0, meaning unlimited. The structural fix for a continuously written table is a lakehouse format, because Hudi, Iceberg and Delta all provide compaction as a managed table service rather than something you bolt on. 98. What is the difference between `repartition(n)` and `repartition(col)`? repartition(n) distributes rows round-robin into n partitions, producing even sizes and no relationship between a row’s value and its partition. repartition(col) hash-partitions by that column, so all rows sharing a value land together. df.repartition(400) # even, value-agnostic df.repartition(&quot;city_id&quot;) # by value, into spark.sql.shuffle.partitions df.repartition(400, &quot;city_id&quot;) # by value, into 400   repartition(n) repartition(col) Distribution Even by construction Even only if the key is uniform Co-locates by value No Yes Use before Heavy per-row work needing balance A write partitioned by that column, or a join or aggregate on it The catch with repartition(col) is skew. Hash partitioning a column where one value dominates puts all those rows in one partition, by design. So it fixes file layout and can remove a later shuffle, while potentially creating the very skew you were trying to avoid. Check the distribution before reaching for it. The most common correct use is immediately before a partitioned write, so each output directory is produced by one task rather than all of them. The second is before several operations that key on the same column, so one exchange serves them all instead of each operator shuffling separately. 99. Does `orderBy` shuffle, and how? Yes, and it is more expensive than most shuffles because of how it partitions. A global sort needs range partitioning: partition 0 gets the lowest values, partition 1 the next, and so on, so that concatenating the sorted partitions yields a globally sorted result. But Spark does not know the value distribution in advance, so it must sample the data first to pick range boundaries. flowchart LR A[&quot;sample the data&amp;lt;br/&amp;gt;&amp;lt;i&amp;gt;an extra job&amp;lt;/i&amp;gt;&quot;] --&amp;gt; B[&quot;compute range boundaries&quot;] B --&amp;gt; C[&quot;shuffle by range&quot;] C --&amp;gt; D[&quot;sort within each partition&quot;] That sampling job is why orderBy can show an unexpected extra job in the UI before the real work starts. If you only need ordering within partitions, which is often enough (for example to make a file’s contents ordered, or for a window already partitioned by the same key), use the cheaper form: df.sortWithinPartitions(&quot;started_at&quot;) # no range partitioning, no sampling Two related notes: A RangePartitioner handles skew badly when many rows share a value, since boundaries cannot split a single value. orderBy before a limit is a special case Spark optimises with TakeOrderedAndProject, which avoids a full global sort by taking the top n per partition and merging. 100. How can you join without a shuffle? Three ways, and each removes the shuffle for a different reason. 1. Broadcast the small side. Every executor gets the whole small table, so each large-side partition can join locally. No redistribution needed. trips.join(F.broadcast(cities), &quot;city_id&quot;) Automatic when the estimated size is below spark.sql.autoBroadcastJoinThreshold, default 10485760 (10 MB). 2. Bucket both tables the same way. If both were written with bucketBy on the join key with the same bucket count, matching rows are already co-located in corresponding files, and the metastore records that fact. (df.write.bucketBy(64, &quot;rider_id&quot;).sortBy(&quot;rider_id&quot;) .saveAsTable(&quot;lakehouse.trips_bucketed&quot;)) This is the only shuffle-free option for a genuinely large-to-large join. 3. Reuse an existing partitioning. If a DataFrame is already hash-partitioned on the join key, from a previous repartition(col) or an earlier shuffle on the same key, Catalyst can satisfy the join’s requirement without a new exchange. With RDDs this is explicit co-partitioning plus cache. flowchart TB A[&quot;shuffle needed?&quot;] --&amp;gt; B{&quot;one side small?&quot;} B --&amp;gt;|&quot;yes&quot;| C[&quot;broadcast: no shuffle&quot;] B --&amp;gt;|&quot;no&quot;| D{&quot;both bucketed&amp;lt;br/&amp;gt;same key, same count?&quot;} D --&amp;gt;|&quot;yes&quot;| E[&quot;bucketed join: no shuffle&quot;] D --&amp;gt;|&quot;no&quot;| F{&quot;already partitioned&amp;lt;br/&amp;gt;on the key?&quot;} F --&amp;gt;|&quot;yes&quot;| G[&quot;reuse: no shuffle&quot;] F --&amp;gt;|&quot;no&quot;| H[&quot;sort-merge join: shuffle&quot;] 101. What is the shuffle fetch side, and which knobs matter? The reduce side pulling its blocks from every map output. It is the half that crosses the network, and the knobs govern how aggressively it does so. Config Governs spark.reducer.maxSizeInFlight How much data may be in flight per reduce task spark.reducer.maxReqsInFlight Concurrent fetch requests spark.shuffle.io.maxRetries Retries per failed fetch spark.shuffle.io.retryWait Wait between retries spark.network.timeout The general network timeout, default 120s Raising the in-flight limits increases throughput and memory pressure; lowering them is gentler on a loaded cluster. But the important answer is that fetch-side tuning is rarely the fix. A FetchFailedException almost always means the executor holding those blocks is gone, was killed for exceeding memory, or is too overwhelmed to serve. Raising retries makes the job take longer to fail rather than succeed. So the diagnostic order is: Find the executor that was serving those blocks and why it died. The executors tab shows loss reasons. If it was killed for memory, fix that: overhead, skew, or partition size. Consider an external shuffle service, so shuffle output survives the executor. Only then touch fetch settings, and only with evidence of genuinely slow but healthy transfers. 102. What causes `FetchFailedException`? The reduce side could not get shuffle blocks it needs. The block was written, but whatever was serving it is no longer able to. The causes, most common first: The executor died. Killed by the cluster manager for exceeding memory limits, or crashed with an OutOfMemoryError. Its shuffle files went with it. The executor is alive but overwhelmed, so fetch requests time out while it is busy with GC or computation. The node died, taking its local disk with it. Disk full, so the map side could not write or the service cannot read. Genuine network problems, which are the rarest in practice. flowchart LR A[&quot;FetchFailedException&quot;] --&amp;gt; B[&quot;look at the EXECUTORS tab,&amp;lt;br/&amp;gt;not the network config&quot;] B --&amp;gt; C{&quot;why did it die?&quot;} C --&amp;gt;|&quot;killed for memory&quot;| D[&quot;overhead, skew,&amp;lt;br/&amp;gt;or partition size&quot;] C --&amp;gt;|&quot;OOM&quot;| E[&quot;partition too large&quot;] C --&amp;gt;|&quot;still alive&quot;| F[&quot;GC pressure or overload&quot;] What Spark does automatically: DAGScheduler treats it as lost map output and resubmits the upstream stage to regenerate it. So the job often recovers, just slowly, which is why you may see a stage run twice. What to do: fix the executor deaths. An external shuffle service also helps directly, because shuffle blocks are then served by a node-level daemon that outlives the executor. 103. What is shuffle spill, and what does it tell you? Spill is data written to disk because it did not fit in execution memory. The stage page reports it as two numbers: spill (memory), the in-memory size of what was spilled, and spill (disk), the serialized size actually written. It happens on either side: the map side while sorting and buffering output, or the reduce side while merging fetched blocks and aggregating. What it means: the per-task working set exceeds the execution memory each task gets. That memory is roughly the unified pool divided by the number of concurrently running tasks on that executor. executor memory 16g - 300 MB reserved x spark.memory.fraction 0.6 -&amp;gt; ~9.6 GB unified pool / spark.executor.cores 4 -&amp;gt; ~2.4 GB per running task So the fixes, in order: More partitions, so each task’s share is smaller. This is usually the right answer and costs nothing but a config. Fewer cores per executor, which gives each running task a larger slice of the same pool. Check for skew, because one oversized partition spills while its peers do not. More memory, last, because it treats the symptom. A little spill is not an emergency; Spark is designed to spill rather than fail. Heavy spill relative to shuffle size means you are paying disk I/O for work that should have been in memory. 104. Why can `coalesce(1)` be dangerous? Because coalesce is a narrow dependency, so it creates no stage boundary, and the reduced parallelism propagates backwards into the stage that feeds it. flowchart TB subgraph C[&quot;coalesce(1): ONE stage, 1 task total&quot;] R1[&quot;read 200 partitions&quot;] --&amp;gt; M1[&quot;expensive transform&quot;] --&amp;gt; Z1[&quot;coalesce(1)&quot;] --&amp;gt; W1[&quot;write 1 file&quot;] end subgraph R[&quot;repartition(1): TWO stages&quot;] R2[&quot;read 200 partitions&quot;] --&amp;gt; M2[&quot;expensive transform&amp;lt;br/&amp;gt;&amp;lt;b&amp;gt;200 tasks&amp;lt;/b&amp;gt;&quot;] --&amp;gt; S2[(&quot;shuffle&quot;)] --&amp;gt; W2[&quot;write 1 file&amp;lt;br/&amp;gt;1 task&quot;] end With coalesce(1), the read and the expensive transform are in the same stage as the single-partition write, so the whole pipeline runs in one task on one core. A job that should take minutes takes hours, and the cluster sits idle. repartition(1) inserts a shuffle, which costs a full redistribution but keeps the upstream stage at full parallelism. So the rule: Situation Use Reducing partitions after cheap work coalesce(n) Reducing to a very small number after expensive work repartition(n) Needing balance, or more partitions repartition(n) A middle path is often best: coalesce(200) to trim file count without a shuffle, rather than collapsing to one. And if you need exactly one file, ask whether you actually do, because a single-file requirement usually comes from a downstream tool that could read a directory instead. Memory, caching and persistence Where the errors are most alarming and the fixes least obvious. Interviewers use this area to find out whether you understand the memory model or just raise numbers until it works. 105. How is executor memory divided? UnifiedMemoryManager does the arithmetic in a fixed order: Subtract 300 MB of reserved memory, a constant for Spark’s own overhead. Take spark.memory.fraction of what remains, default 0.6, as the unified pool shared by execution and storage. What is left, 40% of the post-reserved heap, is user memory: your own data structures, UDF state, and Spark internal metadata. --executor-memory 16g (16,384 MB) - 300 MB reserved -&amp;gt; 16,084 MB usable x 0.6 memory.fraction -&amp;gt; 9,650 MB unified pool of which storageFraction 0.5 -&amp;gt; 4,825 MB storage floor remaining 40% -&amp;gt; 6,434 MB user memory + memoryOverhead -&amp;gt; ~18,022 MB requested from the cluster flowchart TB C[&quot;container: heap + overhead&quot;] --&amp;gt; H[&quot;heap 16g&quot;] C --&amp;gt; O[&quot;overhead, off-heap and native&quot;] H --&amp;gt; R[&quot;300 MB reserved&quot;] H --&amp;gt; U[&quot;unified pool, 60%&amp;lt;br/&amp;gt;execution + storage&quot;] H --&amp;gt; M[&quot;user memory, 40%&quot;] U --&amp;gt; E[&quot;execution: shuffles, joins, sorts&quot;] U --&amp;gt; S[&quot;storage: cached blocks, broadcasts&quot;] Two things people miss: the pool is shared between execution and storage combined, which is usually less than expected; and the container asks the cluster manager for heap plus spark.executor.memoryOverhead, which is why a 16g executor consumes about 18g of cluster capacity. Off-heap is separate and off by default: spark.memory.offHeap.enabled is false. 106. What is the difference between execution and storage memory? They are two uses of one pool.   Execution memory Storage memory Used for Shuffles, joins, sorts, aggregations Cached blocks, broadcast data Lifetime The duration of a task Until evicted or unpersisted If it runs short Spills to disk Evicts blocks, or spills if the level allows Floor None spark.memory.storageFraction, default 0.5 They borrow from each other, which is what “unified” means. If nothing is cached, execution can use the whole pool. If no shuffle is running, cached blocks can fill it. spark.memory.storageFraction is not a partition; it is the floor below which cached blocks will not be evicted. Cached data can grow past it when execution is idle, but cannot be pushed below it. Before Spark 1.6 these were static pools, and the tuning pain of that is why unification happened: a job that cached nothing still had memory reserved for caching it could not use. The consequence for tuning: spark.memory.fraction is rarely worth changing, because raising it steals from user memory, which UDFs and your own objects need. If you are tempted to raise it, the real problem is usually partition size. 107. Can execution and storage evict each other? No, and the asymmetry is the whole answer. Execution can evict cached blocks, down to the storage floor. Storage can never evict execution. flowchart LR E[&quot;execution needs memory&quot;] --&amp;gt;|&quot;evicts cached blocks&amp;lt;br/&amp;gt;down to storageFraction&quot;| S[&quot;storage&quot;] S -.-&amp;gt;|&quot;never evicts&quot;| E The reason is correctness and progress: a task that cannot get execution memory can only spill or fail, whereas a cached block that is evicted can simply be recomputed from lineage. So Spark prioritises the thing that cannot recover. The practical consequences, and these are the interview-worthy part: Caching a large DataFrame cannot fail a shuffle. Execution will take the memory it needs. But it can make a shuffle spill, because the cache occupied the pool first and execution had to reclaim it, and because whatever stays above the floor is unavailable. So adding a cache() can make a job slower, which is a counterintuitive result worth being able to explain. And an under-populated cache is the worst outcome: you paid to write it, execution evicted most of it, and now every use partly recomputes. Check the storage tab. A cached dataset showing 40% in memory is costing you without paying you back, and either needs MEMORY_AND_DISK, a serialized level, or not to be cached at all. 108. What is the difference between heap and off-heap memory? On-heap memory is allocated inside the JVM heap and managed by the garbage collector. Off-heap is allocated outside it, through sun.misc.Unsafe, and is not subject to GC.   On-heap Off-heap Managed by JVM garbage collector Spark, explicitly Config spark.executor.memory spark.memory.offHeap.size Enabled Always spark.memory.offHeap.enabled, default false GC pressure Yes No Risk Long GC pauses Leaks are not reclaimed automatically Why off-heap exists: a large heap full of small objects makes full garbage collections slow, and GC pauses stall the executor. Storing data off-heap in a compact binary form removes it from the collector’s view entirely. This is what Tungsten is built on. Spark’s binary row format stores rows as contiguous bytes with a known layout, so it can be read field by field without constructing JVM objects, and can live off-heap. --conf spark.memory.offHeap.enabled=true --conf spark.memory.offHeap.size=4g When to bother: very large executors where GC time is a visible fraction of task time, or heavy shuffle and aggregation workloads. Note that off-heap size counts toward the container total, so it must fit inside what the cluster manager granted, which usually means accounting for it in memoryOverhead. 109. What is executor memory overhead? Memory requested from the cluster manager in addition to the JVM heap, to cover everything that is not heap: Off-heap allocations and Tungsten buffers. The Python worker process, for PySpark. Native libraries, and the JVM’s own non-heap regions such as metaspace, thread stacks and code cache. Netty’s direct buffers for shuffle transfer. spark.executor.memoryOverhead defaults to a fraction of executor memory with a minimum floor, so a 16g executor requests roughly 18g in total. --conf spark.executor.memory=16g --conf spark.executor.memoryOverhead=3g Why it is the answer to a specific failure. When the cluster manager kills a container for exceeding its memory limit, that is the total being exceeded, not the heap. A JVM OutOfMemoryError means the heap; a container kill means the total. They have different fixes, and telling them apart is the skill: Symptom Cause Fix java.lang.OutOfMemoryError: Java heap space Heap exhausted More partitions, or more executor.memory Container killed by YARN or Kubernetes for exceeding limits Total exceeded Raise memoryOverhead PySpark jobs need noticeably more overhead than JVM ones, because every executor runs Python workers whose memory is entirely outside the heap. A PySpark job being killed while the heap looks healthy is the classic case. 110. What causes a driver `OutOfMemoryError`? Something is pulling data into the driver, or the driver is tracking too much metadata. The causes, most common first: collect() or toPandas() on a large result. This is by far the most common. A broadcast that is not small. The driver must assemble the whole table before sending it, so broadcast() on a multi-gigabyte side fails on the driver. A huge number of tasks. The driver tracks metrics and state per task, so a job with hundreds of thousands of tasks needs real memory just for bookkeeping. A deep or wide query plan. Analysis and optimisation hold the plan tree in driver memory; thousands of chained withColumn calls or very wide schemas add up. Accumulating results in a loop, appending to a driver-side list across iterations. spark.driver.memory defaults to 1g, which is a coordinator’s budget. The distinction from spark.driver.maxResultSize, default 1g, is worth making: that config aborts the job with a clear named error before the heap is exhausted. So a maxResultSize error is Spark protecting you, and an OutOfMemoryError is what happens when the data arrived some other way, such as through a broadcast. The fix is usually structural, not a larger driver: write instead of collect, aggregate instead of transfer, and check the plan for an accidental broadcast. 111. What causes an executor `OutOfMemoryError`? A single task needing more heap than it can get. The usual sources: A partition too large. The task must hold its working set, and if that exceeds its share of the unified pool plus what can spill, it fails. Skew. One key concentrating rows into one task, so that task alone is oversized. A high-cardinality aggregation, where the hash map of groups grows large. groupByKey on a hot key, because all values for one key must fit on the reduce side. A UDF materialising too much, for example list(rows) inside mapPartitions, or an applyInPandas group that does not fit. Too many cores per executor, so the pool is divided too many ways. The arithmetic that explains most cases: per-task execution memory ~= unified pool / concurrent tasks on the executor ~= 9.6 GB / 4 cores ~= 2.4 GB So the fixes in order: More partitions. Halving partition size halves the working set, and this is usually the correct answer. Check and fix skew, since one bad partition is invisible in an average. Fewer cores per executor, which gives each task a bigger slice of the same pool. A serialized storage level if caching is competing. More executor memory, last. Raising memory first often just moves the failure to a larger dataset next month. 112. When should you cache? When both conditions hold: the dataset is used more than once, and producing it was expensive. base = (spark.read.parquet(path) .filter(F.col(&quot;started_at&quot;) &amp;gt;= &quot;2026-09-01&quot;) .join(dims, &quot;city_id&quot;)) # expensive: a scan and a shuffle base.cache() base.count() # populates by_city = base.groupBy(&quot;city_id&quot;).count() by_rider = base.groupBy(&quot;rider_id&quot;).count() # reuses the cache Without the cache, base is recomputed for each aggregation, including the scan and the join. Good candidates: A cleaned, filtered base dataset feeding several outputs. An iterative algorithm reading the same data each pass. A dataset used by both branches of a self-join or a union. Anything you will query interactively in a notebook. And cache deliberately, then verify. Call an action to populate it, and check the storage tab: if it is not fully in memory, the cache may be costing more than it saves. Release it when done: base.unpersist() One subtlety: caching is by plan, not by variable. Two DataFrames with identical plans share the cache entry, which is occasionally surprising and occasionally very useful. 113. When should you not cache? Four cases where it costs more than it saves. Used once. You pay to write the cache and gain nothing. This is the most common mistake, and in a notebook it is everywhere. Cheap to recompute. A plain scan of a small Parquet file is faster to redo than to serialise into memory. Memory already tight. The cache occupies the unified pool, execution evicts it, and you end up partly recomputing anyway while having made every shuffle more likely to spill. Immediately before one big shuffle. The cache and the shuffle want the same pool, and the shuffle is the thing that cannot recover. There is also a correctness-adjacent case: caching a non-deterministic DataFrame pins one particular result, which may be what you want or may hide a bug. A rand() or a current_timestamp() in the plan behaves differently cached than uncached. The anti-pattern to name explicitly: # caching everything, which is caching nothing usefully df1.cache(); df2.cache(); df3.cache(); df4.cache() Each competes for the same pool, most get evicted, and every stage now spills more. Cache the one dataset that is genuinely reused, populate it, verify it in the storage tab, and unpersist it when the reuse is over. 114. What are the DataFrame storage levels worth knowing? Four, and the choice is a memory-versus-CPU trade. Level When MEMORY_AND_DISK The default for df.cache(). Spills rather than losing blocks MEMORY_AND_DISK_SER Memory is tight. More fits, at deserialization cost DISK_ONLY Recomputation is more expensive than reading back MEMORY_ONLY Rarely for DataFrames; the RDD default, and it silently drops what does not fit from pyspark import StorageLevel df.persist(StorageLevel.MEMORY_AND_DISK_SER) The serialized variants are the useful lever. A deserialized cache stores rows close to their in-memory representation, which is fast to read and large. A serialized cache is far more compact, so more of the dataset stays in memory, at the cost of CPU to decode on every read. When the storage tab shows a cache only partly resident, serialized is usually the better trade. The _2 replicated variants put a copy on a second node so an executor loss does not force recomputation. They double the memory cost, so they are for the case where recomputation is genuinely very expensive. Note DataFrame caching already stores data in a compressed columnar form internally, controlled by spark.sql.inMemoryColumnarStorage.compressed, so the gap between deserialized and serialized is smaller than for RDDs. 115. How do you uncache, and why does it matter? df.unpersist() # one dataset, blocking=False by default spark.catalog.clearCache() # everything spark.sql(&quot;UNCACHE TABLE trips&quot;) # a named table It matters because cached blocks hold storage memory for the lifetime of the application, not the lifetime of the variable. Python’s garbage collector reclaiming a DataFrame object does not release the blocks on the executors: Spark’s block manager still holds them. So in a long-running application, a notebook, a streaming job, or a multi-stage pipeline, forgotten caches accumulate. Every later stage then has less execution memory, spills more, and runs slower, with no obvious cause. The symptom is a pipeline that gets slower toward the end for no reason visible in the plan. The habit worth building: base.cache() base.count() # ... the reuse that justified the cache ... base.unpersist() Check what is actually held in the storage tab of the UI, which lists every cached dataset with its size and memory fraction. Anything there that you no longer read is pure cost. One note on unpersist(blocking=True): it waits for the blocks to actually be removed, which is worth using when you need the memory freed before the next stage rather than eventually. 116. Is `cache()` an action? No. cache() and persist() are lazy: they mark the dataset as one to store, and the blocks are written during the next action that computes it. df.cache() # nothing happens yet df.count() # computes, and populates the cache on the way df.groupBy(...) # now reads from the cache This matters in two ways: The first action pays the cost. It computes the dataset and writes the cache, so it is slower than the uncached version. The saving starts with the second use. If you never call an action, nothing is cached, which is a silent no-op people hit when they cache and then only inspect the schema. The idiom is to force population deliberately with a cheap action, usually count(), so the cost lands where you expect rather than inside the first real query. The exception is SQL. CACHE TABLE is eager by default: CACHE TABLE trips; -- populates immediately CACHE LAZY TABLE trips; -- behaves like df.cache() That asymmetry between the DataFrame API and SQL is a favourite detail to probe, because it is easy to assume they behave the same. 117. What is checkpointing, and how does it differ from caching? Checkpointing writes a dataset to reliable storage and truncates its lineage. Caching stores a copy while keeping the lineage intact. spark.sparkContext.setCheckpointDir(&quot;hdfs:///checkpoints&quot;) df = df.checkpoint() # eager by default for DataFrames   Cache Checkpoint Stored where Executor memory or local disk Reliable storage, HDFS or S3 Lineage Kept Truncated Survives executor loss Only with _2 levels Yes Survives application end No The files do Cost Cheap A full write to reliable storage The reason lineage truncation matters: with a long chain of transformations, a single lost partition means replaying the whole chain, and the plan itself grows until analysis is slow or overflows the stack. Checkpointing cuts the chain, so recovery reads a file instead of recomputing a hundred steps. Where you need it: iterative algorithms that build lineage per iteration, and Structured Streaming, where checkpointing is mandatory and stores offsets and state. localCheckpoint() truncates lineage using executor storage instead of reliable storage: much faster, but lost if the executor is lost, so it trades durability for speed. A common pattern is to do both, cache so the write is not recomputed, then checkpoint. 118. Why would a long lineage be a problem even if the data fits? Because lineage is a data structure held on the driver, and it grows whether or not the data does. Three separate problems: Planning cost. Every action re-analyses and re-optimises the plan. Catalyst walks the tree repeatedly, so a tree with thousands of nodes makes planning a measurable fraction of the job. In a loop, planning time grows each iteration. Stack depth. Many Catalyst rules are recursive over the tree. A deep enough plan overflows the thread stack, producing a java.lang.StackOverflowError that looks nothing like a data problem. Recovery cost. One lost partition replays the entire chain that produced it. The classic trigger is a loop: for i in range(200): df = df.withColumn(f&quot;c{i}&quot;, F.col(&quot;x&quot;) + i) # 200 levels deep or an iterative algorithm where each pass depends on the last. The remedies: Checkpoint periodically, every few iterations, which truncates the chain. Collapse the operations. One select with 200 expressions instead of 200 withColumn calls. Raise -Xss on the side that threw, as a stopgap for the stack overflow specifically. The tell that distinguishes this from other slowness: the job spends its time before any task starts. If the stages are quick but the gap between them is long, you are looking at planning, not execution. 119. What is the default serializer, and should you change it? spark.serializer defaults to org.apache.spark.serializer.JavaSerializer. Java serialization is general and slow, and produces large output. Kryo is substantially more compact and faster: --conf spark.serializer=org.apache.spark.serializer.KryoSerializer --conf spark.kryo.registrationRequired=true conf.registerKryoClasses(Array(classOf[Trip], classOf[City])) Registering your classes matters: unregistered classes are written with their full class name per record, which throws away much of the benefit. registrationRequired=true turns a missed registration into an error rather than silent bloat. Where it does and does not help is the important half: Workload Effect of Kryo RDDs of custom objects Large Shuffle of RDD data Large Caching with a _SER level Large Broadcast variables Moderate DataFrame and SQL operations Small DataFrames mostly use Tungsten encoders, which are generated per schema and already compact, so they bypass spark.serializer for row data. That is why “set Kryo” is standard advice for RDD-era code and much less impactful for modern DataFrame pipelines. So: set it if you have RDD-heavy code or serialized caching. If your job is pure DataFrame, expect little and look elsewhere. 120. What is Tungsten? Spark’s execution backend, aimed at making the JVM behave more like hand-written memory-managing code. Three parts: Binary row format. UnsafeRow stores a row as a contiguous block of bytes with a fixed-size region for primitives and offsets into a variable-length region. Fields are read by offset arithmetic, with no objects constructed. Explicit memory management. Spark allocates and tracks memory itself, on or off heap, rather than relying on the garbage collector for data. Whole-stage code generation. Operators in a stage are fused into one generated Java method, so there is no per-row virtual call chain. Why it matters, concretely: a JVM object per row costs a header, pointer indirection, and GC attention. A million-row partition becomes a million objects. UnsafeRow makes it one byte array plus offsets. JVM object row : header + pointers + boxed fields, GC-visible UnsafeRow : contiguous bytes, read by offset, GC-invisible This is the technical answer to “why do DataFrames beat RDD code”. It is not that the DataFrame API is better written; it is that RDDs must materialise your objects because Spark does not know their shape, while a DataFrame’s schema lets Tungsten use a compact layout and generate code against it. It also explains why a Python UDF is expensive: it forces rows out of that binary format, across a process boundary, and back. 121. How do you diagnose GC pressure? Read the GC Time column in the executors tab of the UI, and compare it to task time. A rough guide: consistently above 10% of task time is worth investigating, and above 20% is a problem. The causes, and the fix for each: Cause Fix Many small objects, typically RDDs or Python UDFs Move to DataFrames; serialized storage levels Too much data per executor More, smaller executors rather than few huge ones A very large heap Full collections take longer; consider several executors instead Deserialized caching MEMORY_AND_DISK_SER, which stores far more compactly Long-lived cached data plus heavy execution Unpersist what is not reused To see what the collector is actually doing, log it: --conf &quot;spark.executor.extraJavaOptions=-verbose:gc -XX:+PrintGCDetails -XX:+PrintGCTimeStamps&quot; Two structural points worth making: Off-heap storage removes data from the collector’s view entirely, which is the real fix for GC-bound shuffle and aggregation work. Very large heaps are not free. A 64g executor can spend a long time in a full collection, which is one reason the usual advice is several medium executors rather than one enormous one. And distinguish GC pressure from spill: GC is CPU spent on memory management, spill is disk I/O from insufficient memory. They often appear together, but the remedies differ. 122. What does the storage tab of the UI tell you? For every cached dataset: its name, storage level, the number of cached partitions, the fraction held in memory, and the sizes in memory and on disk. The single most useful number is the memory fraction. What it tells you: Fraction in memory Meaning 100% The cache is working as intended Partial, with a MEMORY_AND_DISK level The remainder is being read from disk each time Partial, with MEMORY_ONLY The remainder is being recomputed each time Not listed at all Nothing was cached, usually because no action ran after cache() That third row is the trap: an RDD cached MEMORY_ONLY that only half fits silently recomputes half the partitions on every use, which looks like the cache having no effect at all. What to do with the information: Partial residency means either switch to a serialized level so more fits, accept MEMORY_AND_DISK, cache a smaller projection, or do not cache. Several datasets listed when you only meant to cache one is the “cache everything” anti-pattern, and they are competing for the same pool. An empty tab on a job you expected to use a cache means the cache() call never populated. It is also the place to confirm an unpersist() actually released the blocks rather than just dropping your reference. Joins, Catalyst and adaptive execution Join strategy is where most real tuning happens, and where an interviewer can tell in one answer whether you have read a physical plan before. 123. What join strategies does Spark have? Five, and Catalyst picks one per join based on size estimates, join type and hints. Strategy How it works Shuffle Good when Broadcast hash join Ship the small side to every executor, build a hash table locally None One side is small Shuffle hash join Shuffle both, build a hash table per partition from the smaller side Both sides One side much smaller, but not broadcastable Sort-merge join Shuffle both, sort each partition, merge Both sides Both sides large Broadcast nested loop Broadcast one side, compare every pair One side Tiny input, or a non-equi condition Cartesian product Full cross product Both sides Explicit cross joins only The selection order is roughly: can it broadcast, then can it shuffle-hash, then sort-merge, and for non-equi conditions only the nested-loop options are available at all. flowchart TB A[&quot;equi-join?&quot;] --&amp;gt;|&quot;no&quot;| N[&quot;BroadcastNestedLoop&amp;lt;br/&amp;gt;or Cartesian&quot;] A --&amp;gt;|&quot;yes&quot;| B{&quot;one side &amp;lt;&amp;lt;br/&amp;gt;autoBroadcastJoinThreshold?&quot;} B --&amp;gt;|&quot;yes&quot;| BH[&quot;BroadcastHashJoin&quot;] B --&amp;gt;|&quot;no&quot;| C{&quot;one side small enough&amp;lt;br/&amp;gt;to hash per partition?&quot;} C --&amp;gt;|&quot;yes&quot;| SH[&quot;ShuffleHashJoin&quot;] C --&amp;gt;|&quot;no&quot;| SM[&quot;SortMergeJoin&quot;] The last two rows of the table are the ones to notice in a plan: seeing CartesianProduct or BroadcastNestedLoopJoin where you expected an equi-join almost always means the join condition is not what you think. 124. What is a broadcast hash join and when does Spark choose it? Spark sends the entire smaller side to every executor, where each builds a hash table in memory. Each partition of the large side then joins locally, so the large side is never shuffled. flowchart TB D[&quot;driver collects&amp;lt;br/&amp;gt;the small side&quot;] --&amp;gt; E1[&quot;executor 1&amp;lt;br/&amp;gt;hash table&quot;] D --&amp;gt; E2[&quot;executor 2&amp;lt;br/&amp;gt;hash table&quot;] D --&amp;gt; E3[&quot;executor 3&amp;lt;br/&amp;gt;hash table&quot;] L1[&quot;large partition&quot;] --&amp;gt; E1 L2[&quot;large partition&quot;] --&amp;gt; E2 L3[&quot;large partition&quot;] --&amp;gt; E3 Spark chooses it automatically when the estimated size of one side is below spark.sql.autoBroadcastJoinThreshold, default 10485760, which is 10 MB. Set it to -1 to disable automatic broadcasting. spark.conf.get(&quot;spark.sql.autoBroadcastJoinThreshold&quot;) # 10485760 Why it is the fastest option: no shuffle at all on the large side, which removes the network transfer, the disk write and the stage barrier. The costs, which a good answer names: The driver assembles it first. The small side is collected to the driver before being broadcast, so a too-large broadcast fails on the driver, not the executors. Memory per executor. Every executor holds a full copy, so 200 MB broadcast across 50 executors is 10 GB of cluster memory. It is bounded by join type. A full outer join cannot be broadcast, and for a left outer join only the right side can be. 125. How do you force or prevent a broadcast? from pyspark.sql.functions import broadcast joined = trips.join(broadcast(cities), &quot;city_id&quot;) SELECT /*+ BROADCAST(c) */ * FROM trips t JOIN cities c ON t.city_id = c.city_id The available hints, and what each forces: Hint Effect BROADCAST Broadcast that side, regardless of the estimate MERGE Force a sort-merge join SHUFFLE_HASH Force a shuffle hash join SHUFFLE_REPLICATE_NL Force a replicated nested loop To prevent broadcasting globally: spark.conf.set(&quot;spark.sql.autoBroadcastJoinThreshold&quot;, &quot;-1&quot;) When forcing is legitimate: Spark’s estimate is wrong, usually because statistics are missing or the side is derived through several transformations, and you know it is small. When it backfires: hinting BROADCAST on something genuinely large. The driver must collect it first, so you move the failure from a slow join to a driver OutOfMemoryError, which is harder to diagnose. A hint overrides the safety check, so it is a claim you are making about the data. A better habit than hinting is often to fix the estimate: ANALYZE TABLE ... COMPUTE STATISTICS, or filter and project the small side first so it is genuinely below the threshold. And with AQE enabled, a runtime switch to a broadcast often happens without a hint at all. 126. What is `spark.sql.broadcastTimeout`? How long Spark will wait for a broadcast to be built and distributed before failing the query. The default is 300 seconds. The error is explicit about the config, which is a clue people sometimes miss: it tells you to either raise the timeout or disable broadcast joins. spark.conf.get(&quot;spark.sql.broadcastTimeout&quot;) # 300 Why raising it is usually the wrong move. Hitting a five-minute timeout to build what should be a small hash table means the side is not small. The sequence of events is: Spark estimated it as broadcastable, began collecting it to the driver, and that collection is taking minutes. So the diagnosis order: Check the actual size of the side being broadcast. If it is hundreds of megabytes, the estimate was wrong. Check whether a hint forced it. A BROADCAST hint overrides the size check entirely. Check for a missing filter. A side that should be small after filtering is being broadcast before the filter, or the filter cannot push down. Then consider the timeout, legitimately, if the table is genuinely small but the cluster is slow or contended. The alternative to raising it is to remove the broadcast and let the join shuffle, which is slower per-query but predictable: spark.conf.set(&quot;spark.sql.autoBroadcastJoinThreshold&quot;, &quot;-1&quot;) 127. Why is sort-merge join the default for large joins? Because its memory requirement does not scale with the size of either side. A hash join must hold one side’s hash table in memory per partition. A sort-merge join sorts both sides, then walks them together with two cursors, so it only needs buffers for the merge, not the whole partition. left (sorted): a a b c d ... right (sorted): a b b d e ... ^ ^ advance the smaller cursor, emit matches And the sort can spill to disk, so a partition larger than memory still completes rather than failing. spark.sql.join.preferSortMergeJoin defaults to true, which biases the planner toward it over shuffle hash join.   Sort-merge Shuffle hash Memory Merge buffers, spillable A whole hash table per partition Extra work Sorting both sides None Fails on a huge partition No, spills Possibly So the trade is CPU for robustness: you pay the sort in exchange for bounded memory. For two large tables that is the right trade, which is why it is the default. A useful corollary: if both sides are already sorted on the join key, for example from bucketing with sortBy, the sort is skipped and sort-merge becomes very cheap. 128. What is a shuffle hash join, and when is it better than sort-merge? Both sides are shuffled on the join key, then for each partition Spark builds a hash table from the smaller side and probes it with the larger. It beats sort-merge when one side is small enough to hash per partition but too large to broadcast, because it skips the sort entirely. sort-merge : shuffle both, sort both, merge shuffle hash : shuffle both, hash the small side, probe The conditions Spark requires before choosing it: The join is an equi-join. One side is estimated small enough that a per-partition hash table fits, judged against spark.sql.autoBroadcastJoinThreshold multiplied by the number of shuffle partitions. spark.sql.join.preferSortMergeJoin is false, or the cost model prefers it. Because preferSortMergeJoin defaults to true, Spark picks it less often than you might expect, and the usual reason is exactly that conservatism: sort-merge cannot run out of memory, and shuffle hash can. To force it when you know the shape: SELECT /*+ SHUFFLE_HASH(c) */ * FROM trips t JOIN cities c ON t.city_id = c.city_id The honest summary for an interview: it occupies a narrow band between broadcast and sort-merge, and in modern Spark, AQE converting a planned sort-merge into a broadcast at runtime covers much of that band more safely. 129. What is the difference between a bucketed join and a broadcast join? Both avoid a shuffle, for entirely different reasons and at different scales.   Broadcast join Bucketed join Avoids the shuffle by Copying the small side everywhere Pre-arranging both sides on disk Size limit One side must be small None Set up Nothing, automatic Write both tables bucketed, in advance Needs a metastore No Yes, so saveAsTable Cost Memory per executor A one-off write, plus rigidity A broadcast join is a runtime decision about a small side. A bucketed join is a storage layout decision that pays off on every future join. (trips.write.bucketBy(64, &quot;rider_id&quot;).sortBy(&quot;rider_id&quot;) .mode(&quot;overwrite&quot;).saveAsTable(&quot;lakehouse.trips_b&quot;)) (riders.write.bucketBy(64, &quot;rider_id&quot;).sortBy(&quot;rider_id&quot;) .mode(&quot;overwrite&quot;).saveAsTable(&quot;lakehouse.riders_b&quot;)) spark.table(&quot;lakehouse.trips_b&quot;).join(spark.table(&quot;lakehouse.riders_b&quot;), &quot;rider_id&quot;) The conditions are strict, and naming them is the good answer: both sides bucketed, on the same column, with the same number of buckets. A mismatch in bucket count means Spark shuffles anyway, silently, and you have paid the write cost for nothing. So bucketing is the only shuffle-free route for large-to-large joins, and its drawback is inflexibility: the bucket count is baked in, and a join on a different key gets no benefit. 130. Why might a broadcast join not happen even though the table is small? Because Spark broadcasts on an estimate, and the estimate can be wrong or unavailable. The reasons, most common first: No statistics. For a file-based table Spark falls back to the total file size, which can be far larger than the in-memory size after filtering, or unavailable for some sources. The side is derived. After a filter and a join, the estimate is a computed guess. Spark multiplies selectivity factors, and those compound badly. The filter cannot push down, so the estimate is of the unfiltered table. Compression. A 50 MB Parquet file may be 500 MB in memory, and Spark’s estimate accounts for that, so a “small” file can exceed the threshold. The join type forbids it. A full outer join cannot be broadcast, and for outer joins only the nullable side can be. The fixes, in order of preference: ANALYZE TABLE lakehouse.cities COMPUTE STATISTICS FOR ALL COLUMNS; cities_small = cities.filter(F.col(&quot;active&quot;)).select(&quot;city_id&quot;, &quot;name&quot;) trips.join(F.broadcast(cities_small), &quot;city_id&quot;) And the modern answer: adaptive query execution largely solves this. Because AQE re-plans after the shuffle stage completes, it knows the actual size and can convert a planned sort-merge join into a broadcast. So on Spark 3.2 and later, a missing broadcast at plan time often becomes a broadcast at runtime, which is why the SQL tab shows a different plan than explain() did. 131. What is cost-based optimisation and is it on? Cost-based optimisation uses table and column statistics to make planning decisions that rule-based optimisation cannot: which join order is cheapest, and which physical strategy to pick. spark.sql.cbo.enabled defaults to false. Even when enabled it needs statistics to exist: ANALYZE TABLE lakehouse.trips COMPUTE STATISTICS FOR ALL COLUMNS; spark.conf.set(&quot;spark.sql.cbo.enabled&quot;, &quot;true&quot;) spark.conf.set(&quot;spark.sql.cbo.joinReorder.enabled&quot;, &quot;true&quot;) What it can do that rules cannot: reorder a multi-way join so the most selective join happens first, which changes the size of every intermediate result. Why it is off by default, and why that is defensible: CBO depends on statistics being present, current and representative. In a lakehouse where tables are written continuously, statistics go stale quickly, and a confidently wrong estimate is worse than no estimate. Collecting column statistics on a large table is also itself an expensive scan. And adaptive execution has taken over much of its job using real runtime numbers rather than stored estimates, which do not go stale. So the modern answer to “should I enable CBO” is usually: make sure AQE is on, and reach for CBO only for complex multi-way joins over stable tables where join order is the problem. 132. What does AQE do about join strategy? After a shuffle stage completes, AQE knows the actual size of each side, so it can replace the planned strategy with a better one. The main conversion is demoting a sort-merge join to a broadcast hash join. flowchart LR A[&quot;plan time:&amp;lt;br/&amp;gt;estimate says 800 MB&amp;lt;br/&amp;gt;-&amp;gt; SortMergeJoin&quot;] --&amp;gt; B[&quot;stage runs&quot;] B --&amp;gt; C[&quot;actual: 6 MB&quot;] C --&amp;gt; D[&quot;re-plan:&amp;lt;br/&amp;gt;BroadcastHashJoin&quot;] This is the single most valuable thing AQE does, and the reason is structural: planning-time estimates on derived data are usually wrong. Spark can estimate a base table it has statistics for, but after two filters and a join the number is a product of guessed selectivities. After the shuffle, it is a measurement. The relevant setting is spark.sql.adaptive.autoBroadcastJoinThreshold, which if unset falls back to the static spark.sql.autoBroadcastJoinThreshold. Two consequences worth knowing: explain() lies about the final plan. Before execution the tree is AdaptiveSparkPlan isFinalPlan=false, so what you read is the planned shape. The SQL tab after the query shows what ran. There must be a shuffle for AQE to act at. A query with no exchange gets no adaptive help, and the first stage is always planned blind. A related optimisation follows automatically: once the join becomes a broadcast, the already-written shuffle can be read locally rather than over the network. 133. What is a local shuffle reader? An AQE optimisation that avoids network traffic when a join strategy changes at runtime. The situation: Spark planned a sort-merge join, so both sides were shuffled. Then AQE discovered one side is small and converted the join to a broadcast. The shuffle files already exist, and with a broadcast join each task no longer needs a specific partition from every map output; it just needs the rows. So instead of fetching them over the network in the shuffle pattern, each executor reads the shuffle blocks it wrote locally. spark.sql.adaptive.localShuffleReader.enabled defaults to true. without: every reduce task fetches from every map output (network) with : each executor reads its own local shuffle blocks (local disk) In the plan it appears as LocalShuffleReader above the exchange. The reason to know it exists is mainly for reading plans: seeing LocalShuffleReader tells you AQE changed a join strategy after the shuffle had already been written, which is a useful signal that your plan-time estimates were wrong. That in turn tells you statistics might be worth collecting, or that a filter should be pushed earlier so the plan is right the first time. 134. How do you read whether AQE actually changed the plan? Compare the planned tree with what actually ran, because they are different objects. Before execution, explain() shows the planned shape wrapped in an adaptive node: AdaptiveSparkPlan isFinalPlan=false +- SortMergeJoin [city_id#21], [city_id#40], Inner :- Sort [city_id#21 ASC], false, 0 : +- Exchange hashpartitioning(city_id#21, 200) ... After execution, the same query’s entry in the SQL tab of the UI shows isFinalPlan=true and the strategies AQE settled on. You can also re-run explain() on an already-executed DataFrame and get the final plan. What to look for as evidence AQE acted: Sign Meaning isFinalPlan=true You are looking at what ran BroadcastHashJoin where the plan said SortMergeJoin A runtime strategy switch LocalShuffleReader Follows such a switch CustomShuffleReader or coalesced partition counts Partition coalescing happened A stage with far fewer tasks than spark.sql.shuffle.partitions Coalescing again The practical habit: never conclude anything about a modern Spark query from explain() alone. It tells you the starting point. The SQL tab tells you the result, and when the two differ, the difference is exactly what AQE contributed. 135. What is the difference between a semi join and an anti join? Both filter the left side by whether a match exists on the right, and neither returns any right-side columns. trips.join(cities, &quot;city_id&quot;, &quot;left_semi&quot;) # rows WITH a match trips.join(cities, &quot;city_id&quot;, &quot;left_anti&quot;) # rows WITHOUT a match   Left semi Left anti Returns Left rows that match Left rows that do not match Columns Left only Left only Duplicates One left row per left row, regardless of how many right matches Same SQL equivalent WHERE EXISTS or IN WHERE NOT EXISTS Why they are better than the alternatives, which is the point of the question: Against an inner join plus select: a semi join stops at the first match, so a left row matching 100 right rows is emitted once. An inner join emits it 100 times, and then you have to deduplicate. Against a left_outer plus isNull filter for the anti case: the outer join carries all the right-side columns through the shuffle before you discard them. So semi and anti joins are both correct where the alternatives need a distinct, and cheaper, because less data crosses the shuffle. The null behaviour is worth knowing: for a left anti join, a null key on the left never matches, so those rows are returned, which is usually what you want but occasionally surprising. 136. How do you handle a join key with nulls? Null never equals null in SQL semantics, so a null key matches nothing. Three consequences and their handling: 1. Inner joins silently drop null keys. If the null means “not applicable”, that is correct. If it means “unknown but should match”, you must say so: a.join(b, a.city_id.eqNullSafe(b.city_id)) # null == null is true here 2. Null keys are a skew source. Every null hashes to the same partition, so a column that is 30% null puts 30% of the rows in one task. If they carry no meaning, drop them before the join: a.filter(F.col(&quot;city_id&quot;).isNotNull()).join(b, &quot;city_id&quot;) Or replace them with a distributed sentinel, which is salting for nulls: a.withColumn(&quot;join_key&quot;, F.coalesce(&quot;city_id&quot;, F.concat(F.lit(&quot;__null_&quot;), (F.rand() * 16).cast(&quot;int&quot;)))) 3. Outer joins produce nulls too, so after a left outer join you cannot distinguish “matched with a null value” from “did not match” by looking at the column. Add a marker before joining if you need to tell them apart: b_marked = b.withColumn(&quot;_matched&quot;, F.lit(True)) a.join(b_marked, &quot;city_id&quot;, &quot;left&quot;).withColumn( &quot;had_match&quot;, F.coalesce(&quot;_matched&quot;, F.lit(False))) The habit: check the null count on every join key before writing the join. It answers both the correctness and the skew question at once. 137. What causes a Cartesian product by accident? A join condition Spark cannot treat as an equality, so it has no key to partition on and must compare every pair. The usual causes: A non-equi condition. ON a.ts BETWEEN b.start AND b.end has no equality, so there is nothing to hash. The condition moved into a where after a cross join. Semantically equivalent SQL, but if Spark cannot push it into the join it evaluates the product first. A join on an expression that Catalyst cannot match to a key, for example ON cast(a.id AS string) = b.id. A missing condition entirely, such as a.join(b) with no key, or a multi-column join where one predicate was forgotten. In the plan it shows as CartesianProduct or BroadcastNestedLoopJoin: CartesianProduct :- FileScan parquet [...] +- FileScan parquet [...] How to catch it: read the plan whenever a join is unexpectedly slow. A Cartesian on two 1-million-row tables is a trillion comparisons, so the symptom is a stage that never finishes rather than one that is merely slow. There is a guard: spark.sql.crossJoin.enabled historically had to be set for implicit cross joins, forcing you to be explicit with crossJoin(). For a legitimate range join, the usual technique is to add an equality that buckets both sides, for example joining on a truncated date as well as the range condition, so Spark has a key to partition on and the nested-loop comparison happens only within each bucket. 138. What is dynamic partition pruning? Using the result of a filter on one side of a join to prune partitions of the other side at runtime, rather than scanning all of them. The classic star-schema shape: a large partitioned fact table, joined to a small filtered dimension table. SELECT t.* FROM trips t JOIN cities c ON t.city_id = c.city_id WHERE c.region = &apos;west&apos; Static partition pruning cannot help, because the filter is on c.region while trips is partitioned by city_id. Without DPP, Spark scans every partition of trips and discards most of them after the join. With DPP, Spark runs the dimension side first, collects the matching city_id values, and injects them as a partition filter on the fact scan. flowchart LR A[&quot;filter cities&amp;lt;br/&amp;gt;region = &apos;west&apos;&quot;] --&amp;gt; B[&quot;city_ids: sf, la, sea&quot;] B --&amp;gt; C[&quot;inject as a partition filter&quot;] C --&amp;gt; D[&quot;scan only those&amp;lt;br/&amp;gt;trips partitions&quot;] The conditions: the fact table must be partitioned on the join key, the join must be an equi-join, and the dimension side must be filtered and broadcastable. In the plan it appears as a dynamicpruningexpression in the fact-side PartitionFilters. The payoff is often the largest single win available on a star schema, because it changes how much data is read rather than how efficiently it is processed. 139. How do you join a large table to a large table efficiently? Broadcasting is off the table, so the work is in reducing what enters the shuffle and making the shuffle well-shaped. In order: 1. Reduce both sides first. Filter and project before joining. This is the largest lever and the most often skipped: t = trips.filter(F.col(&quot;started_at&quot;) &amp;gt;= &quot;2026-09-01&quot;).select(&quot;trip_id&quot;, &quot;rider_id&quot;, &quot;fare_amount&quot;) r = riders.filter(F.col(&quot;active&quot;)).select(&quot;rider_id&quot;, &quot;segment&quot;) Sometimes this alone makes one side broadcastable, which changes the problem entirely. 2. Check whether one side can become small. An aggregation before the join often shrinks it by orders of magnitude. 3. Bucket both tables on the join key with the same bucket count, if this join is a recurring pattern. That removes the shuffle permanently. 4. Size the shuffle. Aim for partitions in the low hundreds of megabytes, and set spark.sql.adaptive.advisoryPartitionSizeInBytes rather than guessing a count. 5. Handle skew on the join key, since a large-to-large join is where skew hurts most. AQE skew handling first, then salting. 6. Avoid re-shuffling. If you then aggregate on the same key, one exchange serves both. If you aggregate on a different key, you pay a second shuffle, so consider whether the order can change. The one thing not to do is hint BROADCAST and hope. 140. What is the `Exchange` node in a plan? A shuffle. Every Exchange in a physical plan is one redistribution of data, and therefore one stage boundary. Its description carries the useful detail: Exchange hashpartitioning(city_id#21, 200), ENSURE_REQUIREMENTS, [plan_id=141] Exchange rangepartitioning(started_at#30 ASC NULLS FIRST, 200), ... Exchange SinglePartition, ... Partitioning Means hashpartitioning(cols, n) Hash on those columns into n partitions. The usual case rangepartitioning(cols, n) Range partitioning, from a global sort SinglePartition Everything into one partition. A red flag unless intended The ENSURE_REQUIREMENTS tag means the exchange was inserted because an operator above it required that partitioning, which is the normal reason. How to use it: Count them to count your shuffles, and therefore your stages. Read the key to see whether consecutive operations shuffle on the same column, in which case one exchange can serve both. Watch for SinglePartition, which usually comes from a global orderBy, a coalesce(1), or a window with no partitionBy. That last one is a common accidental performance disaster: a window function without partitionBy sends the entire dataset through one task. 141. What is `ReusedExchange`? A shuffle whose output is read by more than one branch of the plan, rather than being computed twice. It appears when the same subtree occurs in two places, for example in a self-join, a union of two aggregations over the same base, or a query referencing the same CTE twice. :- Exchange hashpartitioning(city_id#21, 200) : +- HashAggregate(...) +- ReusedExchange [city_id#40], Exchange hashpartitioning(city_id#21, 200) Seeing it is good news: Catalyst recognised the common subtree and is reusing the shuffle files rather than recomputing the stage. In the UI this is also what produces “skipped stages”, shown in grey, which are not a problem but a saving. The interesting case is its absence. If you expected reuse and do not see it, the two branches differ in some way you did not intend: A different filter, even a logically equivalent one written differently. A different column projection, so the plans are not identical. A non-deterministic expression such as rand() or current_timestamp(), which makes the subtrees non-equivalent by definition. When reuse does not happen and you want it, cache() on the shared base is the explicit way to force one computation, at the cost of storage memory. Checking for ReusedExchange first tells you whether the cache is even necessary. 142. How do you reduce the number of shuffles in a query? Six techniques, roughly in order of payoff. 1. Aggregate and join on the same key. If both operations need the same partitioning, one exchange satisfies both. Reordering a pipeline so the keys line up can halve the shuffles. 2. Repartition once, deliberately. If several operators key on the same column, one repartition(col) up front means each of them finds the partitioning already satisfied. 3. Replace distinct plus join with a semi join. distinct is a shuffle; a semi join achieves the same filtering in the join’s own shuffle. trips.join(active_riders, &quot;rider_id&quot;, &quot;left_semi&quot;) # not distinct + inner join 4. Broadcast what can be broadcast. A broadcast join has no exchange at all, so reducing a dimension until it fits under the threshold removes a shuffle outright. 5. Avoid unnecessary global ordering. orderBy costs a range-partitioning shuffle plus a sampling job. If you only need order within files, use sortWithinPartitions. 6. Bucket recurring join keys, so the shuffle is paid once at write time rather than on every read. Then verify, because this is all measurable: df.explain(&quot;formatted&quot;) # count the Exchange nodes before and after The mindset worth conveying: do not think of shuffles as something to tune, think of them as something to count, and then ask whether each one is earning its place. Performance tuning and troubleshooting What an interviewer really wants to know: can you find the bottleneck, or do you guess at knobs. 143. How do you approach a slow Spark job? Find the bottleneck before changing anything. The order matters more than any individual technique. Open the SQL tab and find the slowest operator. The per-node timing tells you where the work is, which is often not where you assumed. Open the stage page for that stage and read the task duration distribution: min, median, max. Classify it. Almost every slow stage is one of four things: Signal Diagnosis Max task time » median, with larger shuffle read Skew Large shuffle read and write across all tasks Too much shuffle Spill reported Partitions too large Thousands of tasks, each milliseconds long Too many partitions Check the plan for structural mistakes before tuning: a missing broadcast, an accidental Cartesian, a filter that did not push down, columns you did not need. Change one thing, re-run, and compare the same stage. The answer that impresses is the negative one: say what you would not do first. You would not raise executor memory, not enable speculation, and not change spark.sql.shuffle.partitions until you knew which of the four it was. Tuning without a diagnosis is how jobs acquire a page of configs that nobody can justify. 144. What does the Spark UI tell you, tab by tab? Tab What it answers Jobs Which action is slow, how many stages, which stages were skipped Stages Task duration and shuffle distribution, spill, the DAG for that stage SQL The final physical plan and per-operator row counts and time Storage What is cached, and what fraction is actually in memory Executors Per-executor task counts, GC time, failures, and loss reasons Environment The configs actually in effect How to use them together, which is the real skill: Start in SQL for a DataFrame job, because it maps operators to time. Starting in Stages tells you where but not what. Stages gives you the distribution. The summary metrics table with percentiles is the single most valuable view in the UI, because it separates skew from uniform slowness. Executors answers “why did it fail”. Loss reasons live here: killed for memory, heartbeat timeout, or exited. Environment settles arguments. If someone insists a config is set, this is the authoritative answer. Storage is where caches are exposed as a cost rather than assumed to be a benefit. And remember the UI dies with the application, which is why event logging plus a history server is close to mandatory: most investigations happen after the job. 145. A few tasks take far longer than the median. What is it? Skew, in almost every case. Confirm it and then fix the distribution. Confirm: on the stage page, compare the max and median for both duration and shuffle read size. The second column is what distinguishes the two possible causes: Duration Shuffle read Cause Max » median Max » median Skew: the task has more data Max » median Roughly even Environment: slow node, GC, bad disk If it is the second, look at the executors tab for GC time and at which host the slow tasks ran on; a single bad machine shows up as all slow tasks on one executor. If it is skew, fix it in this order: Check whether AQE skew handling could fire. It needs a partition over 5x the median and over 256 MB, so on a smaller job it silently never triggers. Broadcast the small side if there is one. Salt the hot key. Split the hot keys out and union. And do not enable speculation. The duplicate task has the same rows and is equally slow, so it consumes a second slot for no benefit and masks the real cause by making it look like flaky infrastructure. 146. The stage shows large spill. What do you change? Spill means the per-task working set exceeded the execution memory that task could get. So reduce the working set, or increase the share. The arithmetic that frames the fix: per-task execution memory ~= unified pool / concurrent tasks per executor ~= (heap - 300MB) x 0.6 / spark.executor.cores Changes in order of preference: More partitions, so each task handles less. This is usually the right answer, costs nothing, and scales. spark.conf.set(&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;, &quot;64MB&quot;) spark.conf.set(&quot;spark.sql.shuffle.partitions&quot;, &quot;2000&quot;) Check for skew. One oversized partition spills while its peers do not, and no amount of memory fixes a distribution problem. Fewer cores per executor, which gives each running task a larger slice of the same pool. Going from 5 cores to 3 raises per-task memory by two thirds. More executor memory, last, because it treats the symptom and costs cluster capacity. Context worth adding: some spill is normal and by design. Spark spills rather than failing, which is a feature. The question is proportion: spill of a few percent of shuffle size is fine, spill several times the shuffle size means you are paying disk I/O for work that should have been in memory. 147. Planning takes longer than execution. What is wrong? The job is spending its time on the driver before tasks start. Three usual causes: 1. Too many input files. Planning must list them and compute splits, which on object storage is many API calls. Tens of thousands of small files can take minutes before a single task runs. print(spark.read.parquet(path).inputFiles().__len__()) 2. A deep plan. Hundreds of chained withColumn calls, or a loop that rebuilds a DataFrame, produce a very deep tree that Catalyst walks repeatedly. Collapse them into one select, or checkpoint to truncate. 3. Very wide schemas. Thousands of columns, or deeply nested structs, make every analysis and optimisation pass expensive. How to tell it apart from execution slowness: in the Jobs tab, look at the gap between the job being submitted and its first stage starting. In the SQL tab, a long total duration with short stage durations points the same way. The extreme version is a StackOverflowError from the recursive tree walk, which is the same problem at a larger scale. The related structural fix for the file-count case is a lakehouse format, because Hudi, Iceberg and Delta keep file metadata in their own manifests rather than requiring a directory listing, which is one of the main reasons they exist. 148. There are thousands of tasks each lasting milliseconds. What is wrong? Too many partitions, so per-task overhead dominates the actual work. Each task carries real fixed costs: serialization of the task description, scheduling on the driver, JVM thread setup, reading a partition’s metadata, and reporting results. At a few milliseconds of work per task, those costs are the job. It also loads the driver, which must track every task’s state and metrics. A job with hundreds of thousands of tasks can exhaust driver memory on bookkeeping alone. The fixes depend on where the partitions came from: Source Fix A shuffle Lower spark.sql.shuffle.partitions, or let AQE coalesce Many small input files Compact them, or raise spark.sql.files.maxPartitionBytes An explicit repartition(n) Lower n Before a write coalesce(n) With AQE on, coalescing should already be merging small shuffle partitions toward spark.sql.adaptive.advisoryPartitionSizeInBytes, default 64MB. So if you are seeing thousands of tiny tasks after a shuffle, check that AQE is enabled and that coalescing is not disabled. The target to state: partitions in the low hundreds of megabytes, and a total that is a small multiple of your core count. Tasks lasting well under a second are a signal you have overshot. 149. How do you size executors? Favour several medium executors over a few enormous ones or many tiny ones. A common starting point is 4 to 5 cores per executor, and there are two reasons: HDFS client contention. Concurrent tasks in one JVM share the same client, and throughput stops improving past roughly that point. Garbage collection. A very large heap makes full collections slow, and a GC pause stalls every task on that executor. Then memory follows from partition size: per-task execution memory ~= (heap - 300MB) x 0.6 / cores Size the heap so that is comfortably larger than your largest partition’s working set, and add spark.executor.memoryOverhead for off-heap, Python workers and native libraries. --executor-cores 4 --executor-memory 16g --conf spark.executor.memoryOverhead=3g The failure modes at each extreme: Shape Problem Few huge executors (e.g. 32 cores, 200g) Slow full GC, HDFS contention, coarse scheduling granularity Many tiny executors (1 core, 2g) No memory sharing between tasks, more overhead copies, more broadcast copies And state the caveat: these are starting points, not laws. The right answer depends on your partition sizes and workload, and the honest thing to say is that you would start here, measure spill and GC, and adjust. 150. How do you decide `--num-executors` versus dynamic allocation? Pick one. Setting both is contradictory, and Spark resolves it by treating the static number as merely the initial size.   Static sizing Dynamic allocation Config --num-executors spark.dynamicAllocation.enabled=true Suits Predictable batch jobs Bursty, interactive, multi-tenant Cost model Reserves capacity for the whole run Releases idle capacity Startup All at once Ramps up Needs Nothing Shuffle tracking or an external shuffle service Use static sizing when the job’s parallelism is roughly constant, you know the shape from experience, and predictable performance matters more than cluster efficiency. A nightly batch job is the archetype. Use dynamic allocation when parallelism varies a lot between stages, the application is long-lived and often idle (a notebook, a Thrift server), or the cluster is shared and holding idle executors is antisocial. --conf spark.dynamicAllocation.enabled=true --conf spark.dynamicAllocation.minExecutors=2 --conf spark.dynamicAllocation.maxExecutors=200 The requirement to name: an idle executor may still hold shuffle blocks a later stage needs. So dynamic allocation needs either an external shuffle service, or spark.dynamicAllocation.shuffleTracking.enabled, which defaults to true and keeps executors holding live shuffle data. The case against dynamic allocation for short jobs: the ramp-up costs more than the elasticity saves. 151. What does a container killed for exceeding memory limits mean? The cluster manager killed the JVM because the process total exceeded what was requested, which is heap plus overhead. It is not a JVM OutOfMemoryError, and the distinction drives the fix. Symptom Means Fix java.lang.OutOfMemoryError: Java heap space The heap filled More partitions, then more executor.memory Container killed by YARN or Kubernetes for exceeding limits The total exceeded More memoryOverhead What lives in overhead: off-heap buffers, Netty’s direct buffers for shuffle, the JVM’s metaspace and thread stacks, native libraries, and the Python worker processes for PySpark. That last one is why PySpark jobs hit this far more often than JVM jobs: Python memory is entirely outside the heap, so a job with heavy Python UDFs can have a perfectly healthy heap and still be killed. The sequence: Confirm which it is from the message. This determines everything. Raise spark.executor.memoryOverhead, generously for PySpark. Check for skew, because one oversized partition is a common root cause that presents as a memory shortage. Raise partition count so each task’s footprint shrinks. Raise executor memory, last. The point to make: raising memory first often just moves the failure to a larger dataset next month, whereas fixing the partition size or the skew fixes it structurally. 152. What is `spark.network.timeout` and when do you raise it? The default timeout for network interactions between Spark components, 120s. It acts as the default for several more specific timeouts, including RPC lookups and shuffle fetch waits. spark.conf.get(&quot;spark.network.timeout&quot;) # 120s When raising it is legitimate: genuinely long operations on healthy infrastructure. Very large shuffle fetches, a heavily contended cluster, or cross-availability-zone traffic. When raising it is the wrong answer, which is most of the time: the timeout is reporting that something is dead or overwhelmed, not causing the problem. An executor stuck in a long garbage collection cannot answer heartbeats; an executor that was killed cannot serve shuffle blocks. Raising the timeout makes the job take longer to notice, not longer to succeed. So the diagnosis order: Look at the executors tab. Did an executor die, and why? Check GC time. An executor spending 40% of its time in GC will miss heartbeats, and the fix is memory or partition size, not the timeout. Check for container kills, which point at overhead. Only then consider the timeout, and pair it with spark.executor.heartbeatInterval, which must stay well below it. A related note: spark.executor.heartbeatInterval defaults to 10s, and it must be considerably smaller than the network timeout or executors get declared lost spuriously. 153. How do you debug a `NoClassDefFoundError` or a version conflict? Find out which JAR each class is actually loaded from, rather than reasoning about what should be on the classpath. --conf &quot;spark.driver.extraJavaOptions=-verbose:class&quot; --conf &quot;spark.executor.extraJavaOptions=-verbose:class&quot; That prints a line per class load with its source JAR, turning a guess about shading into a direct answer: [Loaded com.fasterxml.jackson.databind.ObjectMapper from file:/opt/spark/jars/jackson-databind-2.15.2.jar] The usual causes: Two versions on the classpath, one from Spark’s jars/ and one from your assembly. Spark’s usually wins, which is why your newer version appears absent. A provided dependency not actually provided at runtime. A transitive dependency you did not know you had. The fixes, in order: Shade the conflicting dependency in your assembly, which is the robust answer: relocate the package so there is no collision. Align versions with what Spark ships, which is the simplest when possible. spark.driver.userClassPathFirst and spark.executor.userClassPathFirst, which put your jars ahead of Spark’s. Effective but risky, because it can break Spark’s own dependencies. --jars or --packages for genuinely missing classes. The habit to convey: -verbose:class converts an argument into a fact. It is noisy, so filter it, but it answers the question definitively. 154. What causes a `java.lang.StackOverflowError` in Spark? A recursive walk over a structure deeper than the thread’s stack. In Spark that structure is almost always the query plan or the RDD lineage, not your code’s own recursion. The plan gets deep from: Hundreds of chained withColumn or filter calls. A loop that rebuilds a DataFrame each iteration. Deeply nested structs or very wide schemas. A long iterative lineage with no checkpoint. The stack trace is the tell: if the frames are Catalyst classes, an Avro or Parquet schema walker, or TreeNode methods, it is the plan. Two fixes, and the right one depends on which side threw: 1. Raise the stack on the side that threw, as a direct remedy: --conf &quot;spark.driver.extraJavaOptions=-Xss64m&quot; --conf &quot;spark.executor.extraJavaOptions=-Xss64m&quot; Note in client mode the driver JVM already exists, so use --driver-java-options. 2. Make the plan shallower, which is the structural fix: Collapse many withColumn calls into one select. checkpoint() periodically inside an iterative loop, truncating lineage. The judgement to express: if you find yourself needing hundreds of megabytes of stack, the plan itself is the problem and raising -Xss is buying time rather than fixing it. 155. How do you use custom logging to see what a job did? Write a Log4j 2 config, ship it with --files, and point both sides at it. rootLogger.level = warn rootLogger.appenderRef.stdout.ref = console appender.console.type = Console appender.console.name = console appender.console.layout.type = PatternLayout appender.console.layout.pattern = %d{HH:mm:ss} %-5level %logger{1} - %msg%n logger.sql.name = org.apache.spark.sql.execution logger.sql.level = debug spark-submit \ --files /etc/spark/conf/log4j2.properties \ --conf &quot;spark.driver.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot; \ --conf &quot;spark.executor.extraJavaOptions=-Dlog4j.configurationFile=log4j2.properties&quot; \ app.jar Three details that make it work: The value is a bare filename, not a path, because --files places the file in each container’s working directory. Set both sides. The driver logs planning and scheduling; executors log task-level work. Setting one and forgetting the other looks like the feature not working. Spark 3.3.0 and later is Log4j 2. A config in the old log4j.rootLogger= syntax is silently ignored, not rejected. Useful namespaces to raise selectively: org.apache.spark.sql.execution for plan and codegen detail, org.apache.spark.scheduler for stage and task decisions, org.apache.spark.storage for block and cache activity. Keep the root at warn and raise one namespace, otherwise the signal is buried. And never leave DEBUG on in production. 156. How do you confirm a config actually took effect? Three ways, in descending authority: 1. The Environment tab of the UI. It lists the Spark properties actually in force for the running application. This is the authoritative answer and settles most arguments. 2. At runtime, for SQL configs: print(spark.conf.get(&quot;spark.sql.shuffle.partitions&quot;)) Note this works for spark.sql.* configs but raises for many core ones, which are read from SparkConf at startup: print(spark.sparkContext.getConf().get(&quot;spark.executor.memory&quot;, &quot;(default)&quot;)) 3. --verbose on submit, which prints the resolved configuration and where each value came from. The cases where a config silently does not apply, which is the real content of this question: Case Why Driver JVM options in client mode The JVM already started spark.executor.* set after the session exists Executors are already running spark.default.parallelism on a DataFrame job Wrong config; you want spark.sql.shuffle.partitions A --conf placed after the application jar Passed to your app, not to Spark That last one is worth emphasising: it is not an error, it is an argument to your main, so Spark never sees it and nothing warns you. 157. What is the difference between `spark.sql.files.maxPartitionBytes` and `spark.sql.shuffle.partitions`? They control parallelism at two different points, and reaching for the wrong one is a common waste of time.   spark.sql.files.maxPartitionBytes spark.sql.shuffle.partitions Controls Input partitions when reading files Output partitions of a shuffle Default 128MB 200 Affects The first stage Every stage after a shuffle Units Bytes per partition A count flowchart LR A[&quot;files on storage&quot;] --&amp;gt;|&quot;maxPartitionBytes&amp;lt;br/&amp;gt;decides split size&quot;| B[&quot;stage 0 tasks&quot;] B --&amp;gt;|&quot;shuffle&quot;| C[&quot;spark.sql.shuffle.partitions&amp;lt;br/&amp;gt;decides task count&quot;] C --&amp;gt; D[&quot;stage 1 tasks&quot;] So if the read stage has too few tasks, lowering maxPartitionBytes gives you more. If the stage after a shuffle has the wrong number, spark.sql.shuffle.partitions or AQE coalescing is the lever. Note the units differ in a way that matters: one is a size and the other a count. The size one adapts automatically as data grows; the count one does not, which is exactly why a static 200 ages badly and why AQE coalescing exists. And remember maxPartitionBytes is only one input to the split calculation. spark.sql.files.openCostInBytes, default 4MB, is charged per file, so with many small files the packing behaviour dominates and lowering maxPartitionBytes changes nothing. 158. How do you speed up a job that writes many partitions? Repartition by the same columns you partition by, so each output directory is written by one task instead of all of them. (df.repartition(&quot;city_id&quot;, &quot;dt&quot;) .write.partitionBy(&quot;city_id&quot;, &quot;dt&quot;) .mode(&quot;overwrite&quot;) .parquet(path)) The arithmetic that explains why. Without the repartition, every task may hold rows for every partition value, so it opens a writer per value: files = tasks x partition values (worst case) = 200 x 250 = 50,000 small files With the repartition, each value lives in one task: files = partition values = 250 That also fixes the write itself, not just later reads: 50,000 open file handles and 50,000 small footers is slow to produce, and on object storage it is thousands of PUT requests. Other levers: spark.sql.files.maxRecordsPerFile, default 0 (unlimited), caps rows per file so one huge partition splits into several properly sized files rather than one enormous one. Dynamic partition overwrite, so a job writing one day does not rewrite the table: spark.conf.set(&quot;spark.sql.sources.partitionOverwriteMode&quot;, &quot;dynamic&quot;) Reconsider the partition column. If it has thousands of values, directory-per-value is the wrong layout and a lower-cardinality column, or bucketing, is better. 159. What are the highest-value things to check before tuning configs? Six checks, each of which typically outweighs any config change. Are you reading more than you need? Columns you never select, rows you filter after the join instead of before, partitions you could have pruned. Check ReadSchema and PartitionFilters in the plan. Is the plan what you think? An accidental CartesianProduct, a SortMergeJoin where a broadcast was possible, a SinglePartition exchange from a window without partitionBy. Are there small files? Thousands of tiny inputs make planning dominate execution. Is there skew? Max versus median task time on the slowest stage. Is something cached that should not be? The storage tab: a partly resident cache is pure cost. Is a UDF doing work a built-in could do? Every UDF is an optimisation barrier. flowchart TB A[&quot;slow job&quot;] --&amp;gt; B[&quot;read less?&quot;] --&amp;gt; C[&quot;plan correct?&quot;] --&amp;gt; D[&quot;file count?&quot;] D --&amp;gt; E[&quot;skew?&quot;] --&amp;gt; F[&quot;caches?&quot;] --&amp;gt; G[&quot;UDFs?&quot;] --&amp;gt; H[&quot;only now: configs&quot;] The reason to put it this way in an interview: it shows you treat tuning as diagnosis rather than ritual. A job with a page of carefully tuned configs and a missing broadcast is slower than a job with default configs and a correct plan. Configs are the last 20%, and the plan is the first 80%. 160. How do you benchmark a change honestly? Change one thing, run at representative scale, and compare the stage you expected to change rather than total wall clock. The rules that keep a benchmark honest: One variable at a time. Two changes together tell you nothing about either. Representative data. A change that helps on 1 GB may do nothing at 1 TB, and skew often only appears at scale. Repeat runs. A single run on a shared cluster measures your neighbours as much as your change. Compare the right metric. If you changed shuffle partitions, compare that stage’s duration and spill, not the whole job, which includes unrelated stages. Account for warm caches. The second read of the same data is faster for reasons unrelated to your change: page cache, warm JVMs, and Spark’s own caches. And the thing not to do, which is worth saying explicitly: do not publish or rely on wall-clock timings from a laptop or a demo dataset. They are dominated by JVM warm-up and OS page cache, and they do not transfer to a cluster. A timing harness is fine; presenting its local output as a result is not. What to report instead: the mechanism, and the structural numbers that are deterministic. “Spill dropped from 40 GB to zero and the stage went from 200 tasks to 2,000” is reproducible and explains itself. “It got 3x faster on my machine” does not. Structured Streaming Enough to show you understand the model, not just the API. Watermarks and state are where these questions go. 161. What is Structured Streaming? A stream processing model where a stream is treated as an unbounded table that rows are appended to, and a query over it is re-executed incrementally as data arrives. flowchart TB A[&quot;input stream&quot;] --&amp;gt; B[&quot;unbounded input table&amp;lt;br/&amp;gt;rows appended&quot;] B --&amp;gt; C[&quot;your query,&amp;lt;br/&amp;gt;the same as batch&quot;] C --&amp;gt; D[&quot;result table,&amp;lt;br/&amp;gt;updated each trigger&quot;] D --&amp;gt; E[&quot;sink, per output mode&quot;] The consequence that makes it valuable: you write the same DataFrame operations as for batch. The engine works out how to maintain the result incrementally, keeping whatever state that requires. events = (spark.readStream.format(&quot;kafka&quot;) .option(&quot;kafka.bootstrap.servers&quot;, &quot;kafka-broker1:9092&quot;) .option(&quot;subscribe&quot;, &quot;trips&quot;) .load()) (events.selectExpr(&quot;CAST(value AS STRING)&quot;) .writeStream.format(&quot;parquet&quot;) .option(&quot;checkpointLocation&quot;, &quot;s3a://lakehouse-prod/checkpoints/trips&quot;) .option(&quot;path&quot;, &quot;s3a://lakehouse-prod/warehouse/trips_raw&quot;) .outputMode(&quot;append&quot;) .start()) By default it executes as micro-batches: the engine runs a small batch job per trigger, which is why the same Catalyst machinery and the same UI apply. The model is what you should lead with in an interview, because it explains everything else: output modes exist because the result table can change in different ways, and watermarks exist because maintaining the result incrementally requires bounded state. 162. What is the difference between Structured Streaming and the old DStream API? DStreams are the original RDD-based API; Structured Streaming is the DataFrame-based replacement.   DStreams Structured Streaming Built on RDDs DataFrames and Catalyst Optimisation None Full Catalyst and Tungsten Time semantics Processing time only Event time, with watermarks Late data Manual handling Watermarks and allowed lateness End-to-end guarantee At-least-once in practice Exactly-once with a replayable source and idempotent sink Batch and stream code Different APIs The same API Status Legacy Current The two differences that matter most in practice are event time and the unified API. DStreams only knew when the engine saw a record, so correct windowing over event time was the application’s problem. And because DStream code was a separate API, a batch backfill of the same logic had to be written twice. New work should use Structured Streaming. The right way to say this in an interview is that DStreams are not merely older but architecturally unable to offer event-time correctness or plan optimisation, because they operate on opaque RDDs. The one thing DStreams retained for a while was some lower-level control over batch composition, which the newer API has largely absorbed through triggers and foreachBatch. 163. What are the output modes? Three, and which are legal depends on your query. Mode Writes each trigger Legal for append Only new rows that will never change Queries with no aggregation, or aggregations with a watermark update Only rows whose value changed Most aggregations complete The entire result table Aggregations only .outputMode(&quot;append&quot;) The logic behind the restrictions is worth explaining, because it shows you understand the model: append requires that a row, once emitted, is final. For a plain projection that is always true. For a windowed aggregation it is only true once the watermark has passed the window’s end, which is why append on an aggregation requires a watermark. update emits changed rows, so it works for aggregations without needing finality, but the sink must be able to handle updates to keys it has seen. complete rewrites everything every trigger, so it requires keeping the whole result table in state forever. It does not scale, and it is only appropriate for small result sets such as a dashboard aggregate. The common mistake is choosing complete for convenience, then finding state grows without bound. The other is expecting append to emit aggregation results immediately, when in fact nothing appears until the watermark closes the window. 164. What is a trigger, and what are the options? A trigger controls when the engine runs the next micro-batch. Trigger Behaviour Default, unspecified Start the next batch as soon as the previous finishes processingTime=&quot;1 minute&quot; Start a batch on a fixed interval availableNow=True Process all available data in as many batches as needed, then stop continuous=&quot;1 second&quot; Experimental continuous processing, with limited operation support .trigger(processingTime=&quot;1 minute&quot;) .trigger(availableNow=True) How to choose: The default gives lowest latency and continuous resource use. It also produces many small files if the sink is file-based, because it writes per batch. A fixed interval is the usual production choice, because it batches work into sensibly sized writes and makes resource use predictable. If a batch takes longer than the interval, the next starts immediately, so the interval is a floor, not a guarantee. availableNow is how you run a streaming query as a scheduled batch job: it consumes everything outstanding using the streaming checkpoint for offsets, then exits. That gives you exactly-once bookkeeping with batch economics, and it replaced the older once=True. The practical trade to name: shorter triggers mean lower latency and more small files; longer triggers mean fewer, larger files and higher latency. For a lakehouse sink, the file-count consequence usually dominates the latency preference. 165. What is event time versus processing time? Event time is when the thing happened, carried in the record. Processing time is when the engine saw it. event time 10:00:05 (the trip started) ingest time 10:00:07 (Kafka received it) processing time 10:04:30 (Spark&apos;s batch picked it up) They diverge for ordinary reasons: a mobile client was offline, a broker rebalanced, a consumer lagged, or a batch was delayed. Correctness depends on event time. A trip that happened at 09:59 belongs in the 09:55-10:00 window no matter when it arrives, and aggregating by processing time would attribute it to the wrong window and produce a different answer on every replay. The mistake to watch for, and interviewers do ask about it: using the source’s own timestamp as if it were event time. Kafka’s timestamp column is broker ingest time, not when the event occurred: # wrong: this is ingest time events.withWatermark(&quot;timestamp&quot;, &quot;10 minutes&quot;) # right: event time from the payload (events .select(F.from_json(F.col(&quot;value&quot;).cast(&quot;string&quot;), schema).alias(&quot;t&quot;)) .select(&quot;t.*&quot;) .withWatermark(&quot;started_at&quot;, &quot;10 minutes&quot;)) The practical test is replayability: if you reprocess yesterday’s data today, an event-time query gives the same answer and a processing-time query does not. 166. What is a watermark and what does it actually do? A watermark is a moving threshold on event time that declares how late data may be. It is computed as the maximum event time seen so far, minus the delay you allow. (events .withWatermark(&quot;started_at&quot;, &quot;10 minutes&quot;) .groupBy(F.window(&quot;started_at&quot;, &quot;5 minutes&quot;), &quot;city_id&quot;) .agg(F.count(&quot;*&quot;).alias(&quot;trips&quot;))) It does two things, and naming both is the strong answer: Finalises windows, so their results can be emitted in append mode. Until the watermark passes a window’s end, that window might still change, so nothing can be appended. Bounds state. Once a window can no longer change, the engine drops the state it was holding for it. Without that, state grows for the life of the query. flowchart TB A[&quot;max event time seen&amp;lt;br/&amp;gt;10:30&quot;] --&amp;gt; B[&quot;minus 10 min delay&quot;] B --&amp;gt; C[&quot;watermark 10:20&quot;] C --&amp;gt; D[&quot;windows ending before 10:20:&amp;lt;br/&amp;gt;emit and drop state&quot;] C --&amp;gt; E[&quot;records older than 10:20:&amp;lt;br/&amp;gt;dropped as too late&quot;] The trade is explicit: a longer watermark tolerates later data and holds more state; a shorter one is cheaper and drops more. That is a business decision about how much lateness matters, not a tuning parameter. The signature of getting it wrong is state size climbing without plateau in lastProgress, which is the number to monitor. 167. Does a watermark do anything on a non-aggregating stream? Essentially nothing, and recognising that is a good sign. withWatermark gives the engine two capabilities: finalise results so they can be appended, and drop state that can no longer change. A pass-through stream, a projection, a filter, a from_json, keeps no state and has no results to finalise. So there is nothing for the watermark to do. # the watermark here is inert (events .withWatermark(&quot;started_at&quot;, &quot;10 minutes&quot;) .select(&quot;trip_id&quot;, &quot;city_id&quot;) .writeStream.format(&quot;parquet&quot;).outputMode(&quot;append&quot;).start()) Seeing one there usually means it was copied from an aggregation example. Where it does matter is any stateful operation: Operation Watermark needed Windowed aggregation Yes, for append mode and to bound state dropDuplicates on a stream Yes, or the dedup set grows forever Stream-stream join Yes, on both sides, plus a time condition flatMapGroupsWithState Yes, to expire state Projection, filter, from_json No The one subtle exception is worth mentioning: a watermark also drops individual records older than the threshold, so on a non-aggregating stream it could silently discard late rows without giving you anything in return. So it is not merely useless there, it can be actively harmful. 168. What is checkpointing in Structured Streaming? A required storage location where the engine records what it has processed and the state it holds, so a restarted query resumes exactly where it stopped. .option(&quot;checkpointLocation&quot;, &quot;s3a://lakehouse-prod/checkpoints/trips_by_window&quot;) What it contains: Offsets, a write-ahead log of which source offsets each batch covered, written before the batch runs. Commits, marking which batches completed. State, the keyed state for stateful operations, versioned per batch. Metadata, including the query id. The offsets-before-execution ordering is what makes recovery deterministic: on restart the engine knows which batch was in flight and re-runs it over exactly the same input range, which is the basis of the exactly-once story. The rules: Every streaming query needs one. Without it the query cannot recover, and file sinks will refuse to run. One per query. Two queries sharing a location will corrupt each other. It must be durable and shared. Local disk fails when the driver moves, so use HDFS or object storage. It is not disposable. Deleting it means reprocessing from the source’s earliest offset, or losing data, depending on startingOffsets. That last point is the operational trap: people delete a checkpoint to “reset” a query and either reprocess everything or skip data. 169. How does Structured Streaming achieve exactly-once? By combining three things, and it is a property of the whole pipeline, not of the engine alone. A replayable source. One that can be re-read from a recorded offset: Kafka, or files. A source you cannot rewind, such as a plain socket, cannot give exactly-once. Checkpointed offsets. The engine writes which offsets a batch covers before running it, so a restart re-runs the identical input range. An idempotent sink. One where writing the same batch twice has the same effect as writing it once, usually by keying the write on the batch id. flowchart TB A[&quot;replayable source&amp;lt;br/&amp;gt;Kafka offsets&quot;] --&amp;gt; B[&quot;checkpoint offsets&amp;lt;br/&amp;gt;before the batch&quot;] B --&amp;gt; C[&quot;run the batch&quot;] C --&amp;gt; D[&quot;idempotent sink&amp;lt;br/&amp;gt;dedupes on batch id&quot;] D --&amp;gt; E[&quot;exactly-once end to end&quot;] The honest framing: if any of the three is missing, you get at-least-once. A sink that appends without deduplication will duplicate on retry no matter what the engine does. Spark’s file sinks handle this with a commit log, which is why they are exactly-once, and foreachBatch puts the responsibility on you. The interview-grade version of the answer is exactly that distinction: the engine guarantees it will re-present the same batch, and the sink must guarantee that re-presenting is harmless. Saying “Spark gives exactly-once” without the sink condition is the answer that gets probed. 170. What is `foreachBatch` and why is it useful? A sink that hands you each micro-batch as an ordinary DataFrame, letting you do anything batch code can do. def upsert(batch_df, batch_id): (batch_df.write.format(&quot;hudi&quot;) .options(**hudi_options) .mode(&quot;append&quot;) .save(table_path)) (events.writeStream .foreachBatch(upsert) .option(&quot;checkpointLocation&quot;, ckpt) .start()) Why it matters: streaming sinks support a limited set of operations, while a batch DataFrame supports everything. So foreachBatch is how you reach: MERGE into a lakehouse table, which is the standard way to drive upserts from a stream into Hudi, Iceberg or Delta. Multiple sinks from one stream, writing the same batch twice without reading the source twice. JDBC writes, and any sink with no streaming implementation. Batch-only operations on the micro-batch, such as a join to a static table with a strategy that streaming would not choose. Two responsibilities it transfers to you: Idempotency. The same batch_id can be re-presented after a failure, so your write must tolerate that. The batch_id argument exists precisely so you can deduplicate on it. Caching if you use the batch twice. Writing batch_df to two sinks without caching recomputes it, which for a Kafka source means re-reading. So foreachBatch is the escape hatch that makes streaming practical for lakehouse ingestion, at the cost of owning the exactly-once contract yourself. 171. What happens if you change a streaming query&apos;s code and restart it? It depends on whether the change is compatible with the checkpointed state. The engine validates on restart and fails rather than producing wrong results. Usually safe: Adding or removing a projection column. Changing a filter predicate. Changing the sink’s output path. Changing the trigger interval. Adding a new stateless operation. Usually not safe: Changing the aggregation keys, since the state is keyed by them. Changing the output mode. Adding or removing a stateful operation, which changes the state layout. Changing the watermark column. Changing the source in a way that changes offset semantics, such as a different topic. incompatible change -&amp;gt; restart fails, or requires a new checkpoint new checkpoint -&amp;gt; reprocess from startingOffsets, or skip data The operational consequence, which is the real content of this question: for an incompatible change you need a migration plan, not just a deploy. The usual options are to start a new query with a new checkpoint and backfill the gap with a batch job, or to run both old and new in parallel and cut over. This is why streaming state should be treated as a schema: changing it is a migration. Teams that treat a streaming job as stateless code get caught by this the first time they need to add a grouping key. 172. How do you handle late data? Decide how late is tolerable, express that as the watermark, and then decide what happens to anything later. .withWatermark(&quot;started_at&quot;, &quot;30 minutes&quot;) Data arriving within 30 minutes of the maximum event time seen is included in its correct window. Data arriving later is dropped from the stateful aggregation, because the state for its window has been released. The trade is direct: Watermark Late data State Short, minutes More dropped Small, cheap Long, hours or days More included Large, expensive So the choice is a business question: how much does a late record matter, and is a slightly wrong aggregate acceptable in exchange for bounded cost? If you cannot afford to drop it, the standard pattern is two paths: The streaming aggregation with a modest watermark, for timely approximate results. The raw stream written as-is, with no watermark and no aggregation, so nothing is lost. Then a periodic batch job recomputes the affected windows from the raw data and corrects the serving table. That is the lambda-shaped compromise, and in a lakehouse it is usually a MERGE that overwrites the recomputed windows. You can also monitor what you are losing: the difference between raw record counts and aggregated counts over the same period tells you whether the watermark is set sensibly. 173. What are stateful operations, and which are they? Any operation whose result depends on data seen across triggers, so the engine must carry something between batches. Operation State held Windowed aggregation A partial aggregate per window per key groupBy aggregation without windows A running aggregate per key, forever dropDuplicates The set of keys already seen Stream-stream join Buffered rows from both sides awaiting matches flatMapGroupsWithState, transformWithState Whatever you choose to keep Stateless by contrast: select, filter, withColumn, from_json, and a stream-to-static join. All of the stateful ones need a watermark to bound state, except a non-windowed groupBy, which is unbounded by construction: a running count per key keeps one entry per key forever, and no watermark can help because the key never expires. That is worth knowing, because it is the one case where the answer is “restructure the query”, not “add a watermark”. State lives in a state store, checkpointed per batch, and it is the main scaling dimension of a streaming job. lastProgress reports stateOperators with row counts and memory used, which is the number to watch: print(query.lastProgress[&quot;stateOperators&quot;]) Plateauing state means the watermark is doing its job. Monotonically growing state is the signature of a missing watermark, an over-generous one, or an unbounded aggregation. 174. What is special about a stream-stream join? Both sides are unbounded, so the engine must buffer rows from each side while waiting for a match that may not have arrived yet. That makes it the most state-hungry operation in Structured Streaming. Two requirements follow: Watermarks on both sides, so the engine knows when a row can no longer find a match and its state can be dropped. A time constraint in the join condition, bounding how far apart matching events can be. (impressions.withWatermark(&quot;imp_time&quot;, &quot;2 hours&quot;) .join( clicks.withWatermark(&quot;click_time&quot;, &quot;3 hours&quot;), F.expr( &quot;imp_id = click_imp_id AND &quot; &quot;click_time &amp;gt;= imp_time AND &quot; &quot;click_time &amp;lt;= imp_time + interval 1 hour&quot;))) Without the time condition the engine must keep every row forever, because any future row might match. Outer joins add a further subtlety worth volunteering: a non-match cannot be emitted immediately, because a match might still arrive. The engine must wait until the watermark proves no match is possible, so outer-join results appear delayed by the watermark, not at the trigger where the row arrived. The practical advice: prefer a stream-to-static join where you can, joining the stream to a dimension table. It is stateless, needs no watermark, and is usually what the requirement actually calls for. 175. How do you monitor a streaming query? Through the query’s own progress objects, and through the Streaming tab of the UI. print(query.status) # what it is doing right now print(query.lastProgress) # metrics for the last completed batch The numbers that matter, in priority order: Metric What it tells you inputRowsPerSecond vs processedRowsPerSecond Whether you are keeping up stateOperators[].numRowsTotal Whether state is bounded batchDuration against your trigger interval Whether batches fit in their window durationMs breakdown Where the time goes: addBatch, getBatch, walCommit Source endOffset minus committed Consumer lag The two alerts worth having: Processing rate below input rate, sustained. That is falling behind, and lag grows without bound until you add resources or reduce work. State size growing without plateau. That is the watermark signature, and it ends in an executor OutOfMemoryError hours or days later. For programmatic alerting, attach a listener rather than polling: spark.streams.addListener(my_listener) # onQueryProgress, onQueryTerminated The thing to say about alerting on duration: batch duration alone is a poor signal, because it moves with input volume for innocent reasons. The ratio of processing rate to input rate is the structural signal, and state size is the one that predicts a failure before it happens. Deployment, cluster managers and operations The practical half that candidates often cannot answer, which makes it a good differentiator. 176. How do you run Spark on YARN? --master yarn, with the deploy mode choosing where the driver lives. export HADOOP_CONF_DIR=/etc/hadoop/conf spark-submit \ --master yarn \ --deploy-mode cluster \ --queue analytics \ --num-executors 50 --executor-cores 4 --executor-memory 16g \ --conf spark.executor.memoryOverhead=3g \ --class com.example.TripsIngest trips.jar What happens: Spark starts an ApplicationMaster in the cluster, which requests containers from the ResourceManager through YarnAllocator. Each granted container becomes an executor. In cluster mode the driver runs inside the ApplicationMaster container; in client mode the AM only handles resource requests while the driver stays on your machine. Things that are YARN-specific: HADOOP_CONF_DIR or YARN_CONF_DIR must be set, or Spark cannot find the ResourceManager. --queue places the application in a scheduler queue, which is how YARN arbitrates between applications. Container kills for exceeding memory are reported by the NodeManager, and the fix is memoryOverhead. The external shuffle service runs as a NodeManager auxiliary service. Kerberos delegation tokens are obtained by the driver and renewed for long-running jobs, which needs --principal and --keytab. The tracking URL in the ResourceManager UI links to the Spark UI while running, and to the history server afterwards. 177. How do you run Spark on Kubernetes? --master k8s://https://&amp;lt;api-server&amp;gt;:&amp;lt;port&amp;gt;, with a container image that has Spark in it. spark-submit \ --master k8s://https://k8s.internal:6443 \ --deploy-mode cluster \ --name trips-ingest \ --conf spark.kubernetes.container.image=registry.internal/spark:4.2.0 \ --conf spark.kubernetes.authenticate.driver.serviceAccountName=spark \ --conf spark.executor.instances=50 \ --class com.example.TripsIngest \ local:///opt/app/trips.jar The architecture is distinctive: the driver runs in a pod, and the driver’s own ExecutorPodsAllocator creates executor pods by talking to the API server. There is no Spark cluster-manager daemon at all. What that implies: A service account with permission to create pods is required, which is what the serviceAccountName config is for. The image is the dependency mechanism. local:// paths refer to files inside the image, which is the repeatable way to ship code. Pod templates let you set anything Kubernetes supports that Spark has no config for: node selectors, tolerations, volumes, sidecars. Dynamic allocation works via shuffle tracking, since there is no external shuffle service on Kubernetes in the YARN sense. flowchart TB S[&quot;spark-submit&quot;] --&amp;gt; A[&quot;API server&quot;] A --&amp;gt; D[&quot;driver pod&quot;] D --&amp;gt;|&quot;ExecutorPodsAllocator&amp;lt;br/&amp;gt;creates pods&quot;| A A --&amp;gt; E1[&quot;executor pod&quot;] A --&amp;gt; E2[&quot;executor pod&quot;] 178. What is the difference between Standalone, YARN and Kubernetes for Spark?   Standalone YARN Kubernetes Provided by Spark itself Hadoop Kubernetes Grants containers Master daemon ResourceManager, via ApplicationMaster API server, via the driver Isolation Process-level Containers, cgroups Pods, full container isolation Multi-tenancy Limited Queues, mature Namespaces, quotas Dependency shipping Jars on a shared path Jars, --packages Container images Best for Dedicated Spark clusters Existing Hadoop estates Cloud-native, mixed workloads How to choose, honestly: Standalone when the cluster runs only Spark and you want the least machinery. It is simple and it works, but it has the weakest multi-tenancy. YARN when you already have Hadoop. It is the most mature for queue-based sharing and for Kerberos, and the tooling around it is well understood. Kubernetes when you are already running Kubernetes, want per-job container images, and want Spark to share a cluster with non-Spark workloads. And note Mesos is gone. It was removed in the 4.x line, so a candidate listing four options is working from older material. The interview-relevant point beyond the table: the choice changes deployment and sharing, but not how Spark itself schedules. The driver, the stages and the tasks behave identically, because only SchedulerBackend differs. That is worth saying, because it reframes the question as an operations decision rather than a performance one. 179. How do you share a cluster between Spark applications? At two levels, and keeping them distinct is the answer. Between applications, the cluster manager arbitrates: YARN: queues with capacity or fair scheduling, so each team or pipeline gets a guaranteed share. Kubernetes: namespaces with resource quotas, and optionally a scheduler that understands gang scheduling. Standalone: limited, mostly spark.cores.max per application. Dynamic allocation matters here, because an application holding idle executors is holding capacity another application could use. Within one application, spark.scheduler.mode governs how concurrent jobs share slots: --conf spark.scheduler.mode=FAIR --conf spark.scheduler.allocation.file=/etc/spark/fairscheduler.xml sc.setLocalProperty(&quot;spark.scheduler.pool&quot;, &quot;interactive&quot;) Default is FIFO, under which a long job submitted first starves a short one submitted second. That is the wrong behaviour for a notebook server or a Thrift server serving many users from one SparkSession, which is exactly where FAIR and pools belong. The distinction to state plainly: one application cannot share executors with another, because executors are per-application. So between-application sharing is about who gets containers, and within-application sharing is about who gets task slots in the containers you already hold. 180. How do you pass configuration to a Spark application, and what is the precedence? Four mechanisms, and the precedence, highest first: SparkConf in code, set before the session is created. spark-submit flags, including --conf and the named ones like --executor-memory. A properties file named with --properties-file. spark-defaults.conf in SPARK_HOME/conf. spark = (SparkSession.builder .config(&quot;spark.sql.shuffle.partitions&quot;, &quot;400&quot;) # beats --conf .getOrCreate()) spark-submit --properties-file /etc/spark/job.conf --load-spark-defaults --conf spark.sql.shuffle.partitions=200 app.jar --load-spark-defaults makes Spark read spark-defaults.conf as well as the file given by --properties-file, which otherwise replaces it. That is useful when you want cluster-wide defaults plus job-specific overrides. The traps worth naming: A --conf after the application jar is passed to your application, not to Spark. No warning. Options that only apply at JVM start, notably spark.driver.extraJavaOptions in client mode, cannot be set from code. Executor configs set after the session exists do not retroactively change running executors. To see what actually applied, --verbose prints the resolved configuration and its source, and the Environment tab of the UI is authoritative at runtime. 181. How do you add third-party libraries? Four mechanisms, each for a different situation: Mechanism For --packages Maven coordinates, resolved with transitive dependencies at submit time --jars Local or remote jars, added to driver and executor classpaths --py-files Python .zip, .egg or .py --files Arbitrary files placed in each working directory spark-submit \ --packages org.apache.hudi:hudi-spark3.5-bundle_2.12:1.2.0 \ --jars /opt/lib/custom-udfs.jar \ --files /etc/spark/conf/log4j2.properties \ app.py For production, prefer not resolving at submit time. --packages reaches out to Maven Central on every run, which makes your job depend on network availability and on a repository staying up, and makes the resolved set non-deterministic if a version range is involved. Bake dependencies into an assembly jar, or into a container image on Kubernetes. Two related notes: Conflicts are the usual pain. If your version collides with one Spark ships, Spark’s usually wins. -verbose:class tells you which JAR won, and shading in your assembly is the robust fix. --jars does not expand directories, and URLs must be comma-separated. The local: scheme means “already present on every node”, which avoids copying large jars. For PySpark specifically, a virtual environment archive shipped with --archives is the cleanest way to get consistent Python dependencies on every executor. 182. What is the history server and why do you need it? The Spark UI is served by the driver, so it dies when the application ends. The history server replays event logs to provide the same UI for completed applications. Two parts. The application writes a log: --conf spark.eventLog.enabled=true --conf spark.eventLog.dir=hdfs:///spark-events --conf spark.eventLog.compress=true And a separately running server reads that directory: export SPARK_HISTORY_OPTS=&quot;-Dspark.history.fs.logDirectory=hdfs:///spark-events&quot; $SPARK_HOME/sbin/start-history-server.sh Why you need it is simple: almost all investigation happens after the fact. A job that failed at 03:00 leaves you with only the driver’s stdout unless event logging was on. Enabling it is close to mandatory in production, and it is the single cheapest operational improvement for a Spark platform. Operational details worth knowing: Logs grow, so enable compression and set retention (spark.history.fs.cleaner.enabled). A very large log is slow to replay, which happens with jobs that had hundreds of thousands of tasks. On YARN, the ResourceManager’s tracking URL redirects to the history server once an application finishes, so links keep working. The thing that goes wrong most often is enabling spark.eventLog.dir on a path the history server cannot read, so logs are written and never shown. 183. How do you secure a Spark cluster? Four layers, and a good answer covers more than one. 1. Authentication between Spark components. --conf spark.authenticate=true --conf spark.authenticate.secret=&amp;lt;secret&amp;gt; On YARN and Kubernetes the secret can be distributed by the cluster manager rather than being passed on a command line. 2. Encryption in transit. RPC encryption with spark.network.crypto.enabled, and separately encryption of shuffle and cached data spilled to disk with spark.io.encryption.enabled. TLS for the UI and the history server. 3. Authorisation and identity. Kerberos on Hadoop, with --principal and --keytab so long-running jobs can renew delegation tokens. On Kubernetes, service accounts and RBAC. 4. Isolation. YARN queues or Kubernetes namespaces, so one tenant cannot exhaust another’s capacity. Then the mistakes to avoid, which is where practical knowledge shows: Secrets in extraJavaOptions or --conf are visible in the Environment tab of the UI and in process listings. Use a secrets mechanism instead. The UI has no authentication by default. It should not be reachable from an untrusted network; put it behind a proxy with authentication, using spark.ui.filters. Event logs contain SQL text and configs, so the log directory needs the same access control as the data. 184. How do you route Spark traffic through an HTTP proxy? JVM proxy system properties, passed through the same extraJavaOptions mechanism as any other JVM flag. spark-submit \ --conf &quot;spark.driver.extraJavaOptions=-Dhttp.proxyHost=proxy.internal -Dhttp.proxyPort=3128 -Dhttps.proxyHost=proxy.internal -Dhttps.proxyPort=3128 -Dhttp.nonProxyHosts=localhost|127.0.0.1|*.internal&quot; \ --conf &quot;spark.executor.extraJavaOptions=-Dhttp.proxyHost=proxy.internal -Dhttp.proxyPort=3128 -Dhttps.proxyHost=proxy.internal -Dhttps.proxyPort=3128 -Dhttp.nonProxyHosts=localhost|127.0.0.1|*.internal&quot; \ app.jar Three practical points: Set it on both sides. Driver-side covers dependency resolution and metadata calls; executor-side covers whatever your code fetches from tasks. http.nonProxyHosts matters as much as the proxy itself. Without it, intra-cluster traffic tries to go through the proxy, which is slow at best and broken at worst. The separator is |, not a comma. https.nonProxyHosts does not exist; the HTTP one covers both. The reason this question appears in a Spark interview is that it is really a question about whether you understand the delivery mechanism. Proxy settings, logging configuration, stack size, GC flags and heap dump options all arrive the same way: through spark.driver.extraJavaOptions and spark.executor.extraJavaOptions. Learning one teaches the rest. And the client-mode caveat applies here too: the driver JVM already exists, so use --driver-java-options if you are setting it for a client-mode driver. 185. What should you monitor in production? Monitor structural signals, not total job duration, because duration moves for many innocent reasons. For batch jobs: Signal Why Task duration skew, max versus median per stage Detects skew before it becomes a failure Shuffle spill The early sign that partitions are too large GC time per executor Predicts heartbeat timeouts and slowdowns Executor loss count and reason Distinguishes container kills from crashes Input file count Rising file counts predict planning slowdowns Stage retry count A stage running twice means lost shuffle output For streaming jobs: Signal Why Processing rate versus input rate The definition of keeping up State store row count Growth without plateau means a watermark problem Consumer lag The business-visible measure of freshness Batch duration versus trigger interval Batches overrunning their window How to collect it: event logs plus the history server for post-mortem, and Spark’s metrics system (spark.metrics.conf) exporting to Prometheus or StatsD for live dashboards. StreamingQueryListener for streaming. The reason to frame it this way in an interview: anyone can alert on “job took too long”. Alerting on spill, skew ratio and state growth means you find out before the job fails, and you know what to do when the alert fires. That is the difference between monitoring and having a dashboard. Scenario-based questions These separate people who have run Spark in production from people who have read about it. Each is a symptom; the answer is a way of looking, not a fact to recall. Practise saying these out loud. 186. A job that took 20 minutes now takes 2 hours, and nothing was deployed. How do you investigate? No code changed, so something changed in the data or the cluster. Work through it in this order, because it is cheapest first. 1. Compare the physical plan against a previous run. This finds the most common cause by far: a broadcast join that no longer broadcasts, because the dimension table grew past spark.sql.autoBroadcastJoinThreshold. One join strategy change can account for the entire regression. df.explain(&quot;formatted&quot;) # and the SQL tab of the history server for the old run 2. Check input size and file count separately. Ten times the files with the same bytes changes planning cost and partition packing, which looks like slowness with no plan change. 3. Check for new skew. On the slowest stage, max versus median task duration and shuffle read. A newly hot key, or a surge of null keys, concentrates work. 4. Check the executors tab for loss and retries. A stage that now runs twice because an executor keeps dying doubles the time with no plan difference. 5. Check cluster contention. A noisy neighbour on a shared cluster, or fewer executors granted than before. 6. Only then look at configs, which did not change, but might now be wrong for the new data volume. The answer that scores: name the lost broadcast first and say why you would look there before anything else. It is the single most likely cause of a step-change regression with unchanged code. 187. A stage has 200 tasks; 198 finish in seconds and 2 run for an hour. What do you do? That is skew, and the first thing to do is confirm it rather than assume it. Confirm. On the stage page compare max and median for shuffle read size, not just duration: Duration Shuffle read Cause Max » median Max » median Skew. Fix the distribution Max » median Roughly even Environment. Slow node, GC, bad disk If shuffle read is even, stop treating it as skew and look at the executors tab for that host. Then, if it is skew: Check whether AQE could fire. spark.sql.adaptive.skewJoin.enabled is true, but a partition must be over 5x the median and over 256 MB. Two tasks running an hour on a job whose partitions are 80 MB will never trigger it. Lowering skewedPartitionThresholdInBytes may be the whole fix. Broadcast the small side, if there is one. No shuffle, no skew. Salt the hot key, which is the general answer. Split the hot keys out and union, if there are only a few. And say what you would not do: enable speculation. The duplicate task has the same rows and is equally slow, so it burns a second slot for nothing and disguises a data problem as flaky infrastructure. Also check the join key’s null count. Nulls all hash together, and a column that is 40% null produces exactly this picture. 188. Your job writes 50,000 tiny Parquet files. Why, and how do you fix it? It is arithmetic. Every task that holds rows for a partition value writes a file into that value’s directory: files = tasks x partition values = 200 shuffle partitions x 250 cities = up to 50,000 Each task has a few rows for almost every city, so it opens a writer for almost every city. The fix is to make each value live in one task, by repartitioning on the same columns you partition by: (df.repartition(&quot;city_id&quot;) .write.partitionBy(&quot;city_id&quot;) .mode(&quot;overwrite&quot;) .parquet(path)) That gives 250 files instead of 50,000. Then check three related things: Is the partition column the right cardinality? 250 is fine; 50,000 distinct values is not, and directory-per-value is the wrong layout. Consider a coarser column, or bucketing. Cap file size for the big partitions. spark.sql.files.maxRecordsPerFile, default 0, splits one enormous partition into several sensible files. Watch for skew from the repartition itself. repartition(&quot;city_id&quot;) hash-partitions, so a dominant city becomes one large task. That may be an acceptable trade for the file layout, or may need salting. Why it matters beyond tidiness: every later read pays listing cost, per-file open cost (openCostInBytes, default 4MB each), and worse compression. The write is slow too, because 50,000 files on object storage is 50,000 PUTs. 189. The driver runs out of memory on a job that only aggregates. What happened? “Only aggregates” means the result should be small, so something is pulling data back or the driver is tracking too much. Check these in order: Is there a collect(), toPandas(), or a wide show()? Even a show(1000) on a very wide row is a lot of data. This is the most common answer. Is something being broadcast? The driver must assemble a broadcast side before sending it, so a broadcast() hint on a large table fails on the driver, not the executors. Check the plan for BroadcastHashJoin or BroadcastExchange and the size of what feeds it. How many tasks? The driver holds metrics and state per task. A job with hundreds of thousands of tasks, from tiny input files or an enormous shuffle partition count, needs real memory just for bookkeeping. Is the plan deep or very wide? Analysis holds the tree in driver memory; a loop of withColumn calls or a 5,000-column schema adds up. Is there a driver-side loop accumulating results? Appending each iteration’s output to a Python list is a classic. spark.conf.get(&quot;spark.driver.maxResultSize&quot;) # 1g The distinction worth drawing: a maxResultSize error is Spark protecting you with a clear named failure, while an OutOfMemoryError means the data arrived some other way, usually a broadcast. So the error type narrows the search. And note spark.driver.memory defaults to 1g, which is a coordinator’s budget. 190. Executors keep being killed for exceeding memory limits. What is your sequence? 1. Confirm which failure it is, because the two have different fixes: Message Means Fix Container killed by YARN/Kubernetes for exceeding limits The process total exceeded heap plus overhead memoryOverhead java.lang.OutOfMemoryError: Java heap space The heap filled Partitions, then executor.memory A container kill is not a JVM error, and reading the message rather than assuming is the first step. 2. Raise spark.executor.memoryOverhead. Overhead holds off-heap buffers, Netty’s direct buffers, metaspace, thread stacks, and the Python workers. PySpark jobs hit this far more often for exactly that reason, and can be killed with a perfectly healthy heap. --conf spark.executor.memoryOverhead=4g 3. Check for skew, because one oversized partition is a very common root cause that presents as a memory shortage. Fixing the distribution fixes the memory. 4. Raise the partition count so each task’s footprint shrinks. This scales with your data, whereas a memory bump does not. 5. Reduce cores per executor, which gives each running task a larger share of the same pool. 6. Raise spark.executor.memory, last. The judgement to voice: raising memory first usually just moves the failure to next month’s data volume. Steps 3 and 4 fix it structurally. 191. A `FetchFailedException` keeps failing your job. What is the real problem? Almost never the fetch settings. The blocks were written; whatever was serving them can no longer do so. Look at the executors tab, not the network config. The question is which executor was serving those blocks and why it stopped: Finding Root cause Fix Killed for exceeding memory Overhead, skew, or partition size Those, not fetch retries OutOfMemoryError Partition too large More partitions Alive but very high GC time Too busy to serve Memory, or fewer cores Node lost Hardware or preemption Retries, and an external shuffle service Disk full Spill filled local disk More disk, or less spill What Spark does on its own: DAGScheduler treats it as lost map output and resubmits the upstream stage to regenerate it. So the job often recovers slowly, and the tell is a stage that runs twice. The structural fix is the external shuffle service, or shuffle tracking, so shuffle output is served by a node-level daemon that outlives the executor: --conf spark.shuffle.service.enabled=true What not to do: raise spark.shuffle.io.maxRetries and spark.network.timeout and call it fixed. That makes the job take longer to fail, and hides the executor deaths that are the actual problem. 192. You have a 2 TB fact table and a 3 GB dimension table to join. How? 3 GB is 300 times the 10 MB broadcast threshold, so a broadcast is out as things stand. The work is to change that, or to make the shuffle good. 1. Try to make the dimension small. This is the highest-value step and the most often skipped. Filter and project it first: dim = (dimension .filter(F.col(&quot;active&quot;)) .select(&quot;city_id&quot;, &quot;region&quot;)) # 3 GB may become 40 MB A 3 GB dimension is usually 3 GB because of columns and rows the query never needs. If this gets it under the threshold, the problem disappears. 2. If it cannot shrink, accept a sort-merge join and make it well-shaped: filter the fact side first, size the shuffle with spark.sql.adaptive.advisoryPartitionSizeInBytes, and handle skew on the join key. 3. If this join is recurring, bucket both tables on the join key with the same bucket count. That removes the shuffle permanently, at the cost of a one-off write and some rigidity. 4. If the fact table is partitioned on the join key and the dimension is filtered, dynamic partition pruning may already be limiting the fact scan. Check for dynamicpruningexpression in the plan. And the thing not to do: hint BROADCAST on 3 GB. The driver must collect it first, so you convert a slow join into a driver OutOfMemoryError, which is harder to diagnose. 193. Your streaming query&apos;s state grows until the job dies. Why? A stateful operation whose state is never released. In practice one of four causes. 1. No watermark on a stateful operation. Without one, no window is ever final, so nothing is dropped. 2. The watermark column is not event time. The classic mistake is using the source’s ingest timestamp: # wrong: Kafka&apos;s timestamp is broker ingest time events.withWatermark(&quot;timestamp&quot;, &quot;10 minutes&quot;) # right: event time from the payload events.withWatermark(&quot;started_at&quot;, &quot;10 minutes&quot;) If the watermark column advances with processing time rather than event time it still moves, so this can look like it is working while behaving wrongly for late data. 3. The watermark is attached in the wrong place. It must be applied to the stream before the aggregation that needs it. Applied after, it does nothing. 4. The aggregation is unbounded by construction. A non-windowed groupBy keeps one entry per key forever, and no watermark can expire a key. That one needs a restructure, usually into a windowed aggregation, not a config. Confirm which it is from the progress metrics: print(query.lastProgress[&quot;stateOperators&quot;]) # numRowsTotal over time State that plateaus is healthy. State that climbs monotonically is the signature. Then check the event-time gap in lastProgress, which tells you whether the watermark is advancing at all. 194. A nightly job silently produced half the expected rows. How do you find out why? Silent wrong results point at semantics, not performance, and Spark has a handful of well-known ways to lose rows quietly. The candidates, most common first: An inner join dropping non-matches, including rows whose key is null, because null never equals null. Half the rows is a very plausible join-coverage figure. union matching by position where the two sides’ column orders differ. This corrupts rather than drops, but produces nonsense that a later filter then drops. A filter against a sometimes-null column. col != &apos;x&apos; is null, not true, for null rows, so they are excluded. A changed upstream schema, so a select picks up a renamed column as null and a downstream filter removes it. ANSI mode on a Spark 4 upgrade, now raising or changing results where invalid casts previously yielded null. Dynamic partition overwrite replacing more or fewer partitions than intended. The method, which matters more than the list: instrument row counts at each stage and find where they fall off. for name, d in [(&quot;raw&quot;, raw), (&quot;filtered&quot;, filtered), (&quot;joined&quot;, joined)]: print(name, d.count()) Then for the join specifically: raw.join(dim, &quot;city_id&quot;, &quot;left_anti&quot;).count() # rows that found no match raw.filter(F.col(&quot;city_id&quot;).isNull()).count() # null keys The habit to convey: for a silent correctness problem, reach for counts and anti-joins, not the UI. The UI tells you about speed, not about semantics. 195. You must deduplicate a CDC feed to the latest row per key. How? A window function is the idiomatic batch answer. from pyspark.sql import Window from pyspark.sql.functions import row_number, desc, col w = Window.partitionBy(&quot;trip_id&quot;).orderBy(desc(&quot;updated_at&quot;)) latest = (changes .withColumn(&quot;rn&quot;, row_number().over(w)) .where(col(&quot;rn&quot;) == 1) .drop(&quot;rn&quot;)) That shuffles once, by trip_id. The things to check, which is what the question is really about: Is the key skewed? The window partitions by trip_id, so one enormous key is one task. Check the distribution first. Is the ordering column unique enough? Ties in updated_at make the winner arbitrary. Add a tiebreaker such as an offset or a sequence number, or the result is non-deterministic between runs. Are deletes represented? A CDC feed usually carries an operation column, and the latest row for a key may be a delete, which should remove the row rather than keep it. latest.filter(col(&quot;op&quot;) != &quot;D&quot;) Alternatives worth naming: dropDuplicates([&quot;trip_id&quot;]) is cheaper but keeps an arbitrary row, not the latest. Only correct if the feed is already ordered, which it usually is not. For a stream, dropDuplicates with a watermark bounds the state, which is the streaming equivalent. In a lakehouse table, this is what an upsert does natively: Hudi with a record key and an ordering field applies exactly this rule on write, so the deduplication becomes the table’s job rather than the query’s. 196. A query works on a sample but fails at full scale with spill and no progress. What now? The shape that works at one scale rarely fails at another for a config reason. It fails because partition sizes scaled with the data and the per-task working set outgrew its memory. The arithmetic to check first: per-task execution memory ~= (heap - 300MB) x spark.memory.fraction / cores ~= (16g - 300MB) x 0.6 / 4 ~= 2.4 GB partition size = total shuffle bytes / partition count = 4 TB / 200 = 20 GB per task &amp;lt;- far too large 200 partitions was fine for the sample and is absurd at full scale. Then, in order: Raise the partition count, or lower the advisory size, so partitions land in the low hundreds of megabytes. spark.conf.set(&quot;spark.sql.shuffle.partitions&quot;, &quot;4000&quot;) spark.conf.set(&quot;spark.sql.adaptive.advisoryPartitionSizeInBytes&quot;, &quot;128MB&quot;) Look for skew that the sample hid. A sample of 1% of rows may contain none of the hot keys, so a job that looked balanced is not. Check for an aggregation whose cardinality scaled. A groupBy with 1,000 groups in the sample and 50 million at full scale is a different operation. Check whether something cached no longer fits, and is now evicting execution memory on every stage. The framing that scores: say explicitly that a sample validates logic, not shape, and that partition sizing is the first thing to recompute at a new scale. 197. You need to read 10 TB but only one day of it. The job reads everything. Why? The filter is not pruning, and there are only a few possible reasons. Check the plan first. Two fields answer it: PartitionFilters: [isnotnull(dt#42), (dt#42 = 2026-09-17)] &amp;lt;- pruning works PushedFilters: [IsNotNull(started_at), GreaterThan(...)] &amp;lt;- source filtering works If your date predicate appears in neither, nothing is being skipped. The causes: The table is not partitioned on the column you filter. Filtering started_at on a table partitioned by dt prunes nothing, because Spark cannot know the relationship. Filter the partition column. The predicate is wrapped in a function. This is the most common, and it is subtle: # cannot prune: the column is inside a function df.filter(F.to_date(&quot;started_at&quot;) == &quot;2026-09-17&quot;) # can prune df.filter((F.col(&quot;started_at&quot;) &amp;gt;= &quot;2026-09-17&quot;) &amp;amp; (F.col(&quot;started_at&quot;) &amp;lt; &quot;2026-09-18&quot;)) The type does not match. Comparing a string partition column to a date literal, or vice versa, can prevent pruning depending on the cast direction. The filter is applied after a join or aggregation, so it is no longer adjacent to the scan and cannot be pushed through. A UDF in the predicate, which is opaque to Catalyst. And the structural note: for a table where this keeps happening, a lakehouse format prunes on column statistics rather than only directory names, which makes the layout less brittle. 198. Two jobs writing the same table produce corrupt or missing data. What is wrong? A directory of Parquet files has no commit protocol, so concurrent writers have no way to coordinate and a failed writer has no way to roll back. What goes wrong concretely: Interleaved output. Two jobs writing the same partition both add files, and readers see the union, which is neither job’s intended result. Partial output on failure. A job that dies halfway leaves the files it already wrote, with nothing marking them as incomplete. overwrite racing append. The overwrite deletes files the appender is adding, or vice versa. Readers seeing a moving target, because there is no snapshot isolation: a scan that lists files, then reads them, can have the list change underneath it. The two fixes, and they are different in kind: Serialise the writers. An orchestration-level lock, or a single job. Correct, and limiting. Use a table format with atomic commits. Hudi, Iceberg or Delta add a commit protocol over the same Parquet files: a reader sees a consistent snapshot, a failed write commits nothing, and concurrent writers are arbitrated by the format rather than by luck. (df.write.format(&quot;iceberg&quot;).mode(&quot;append&quot;).save(&quot;prod.db.trips&quot;)) This is the structural answer and worth stating as such: it is not a Spark tuning problem, it is a missing transaction boundary. Lakehouse formats exist precisely for this, and saying so shows you understand why they were built rather than just that they exist. 199. A `groupBy` on a high-cardinality column is unbearably slow. Options? First ask whether the aggregation is needed at that cardinality, because the cheapest fix is usually to not do it. Then, in order: Is the output actually consumed at that granularity? An aggregation to 50 million groups that then feeds a join on a coarser key can often be reordered: join first, aggregate second. Is an approximation acceptable? Exact countDistinct requires a second shuffle; the approximate version does not: F.approx_count_distinct(&quot;rider_id&quot;, rsd=0.02) # instead of countDistinct Is the shuffle sized for the cardinality? 50 million groups across 200 partitions is 250,000 groups per task, and the hash map for that may not fit. Raise the partition count so each task’s map is smaller. Is there skew within the groups? High cardinality and skew often coexist: millions of small groups plus a handful of enormous ones. Two-phase aggregation with a salt handles the large ones: (df.withColumn(&quot;salt&quot;, (F.rand() * 32).cast(&quot;int&quot;)) .groupBy(&quot;rider_id&quot;, &quot;salt&quot;).agg(F.sum(&quot;fare&quot;).alias(&quot;p&quot;)) .groupBy(&quot;rider_id&quot;).agg(F.sum(&quot;p&quot;).alias(&quot;total&quot;))) Is the output written partitioned by that column? Then high cardinality is also a small-files problem, and the layout needs rethinking. The framing: high cardinality is not itself a problem; the problem is a per-task hash map that does not fit, and the lever is partition count before memory. 200. Your team wants to move a Spark job&apos;s source table from Hudi to Iceberg. What do you consider? First separate the layers, because the question conflates them. Spark is the compute engine; Hudi and Iceberg are table formats. The job’s Spark logic barely changes. What changes is write semantics, read reach and operations. The questions that actually decide it: What is the write pattern? High-frequency keyed upserts are what Hudi’s record index is built for: a key lookup finds its file group directly. Iceberg has no record index, so a MERGE plans a join whose cost scales with how much of the table it touches. If the job is CDC ingestion, this is the dominant consideration. Which engines must read it? Iceberg has the widest warehouse support, and if a warehouse that only reads Iceberg is the reason for the move, that is a legitimate and sufficient reason. Who operates the table services? Both need compaction and cleanup, but they are exposed differently: Hudi ships them as configurable services that can run inline or async, Iceberg as procedures you schedule (rewrite_data_files, expire_snapshots, remove_orphan_files). What is the migration mechanism? Iceberg has snapshot and migrate procedures for in-place adoption of existing Parquet. A full rewrite of a large table is a real cost. What breaks? Incremental reads, time travel syntax and concurrency configuration all differ, so downstream consumers need changes even though the Spark job does not. And the option people forget: if the answer is “both, for different readers”, Apache XTable converts metadata in place without copying data, so one set of Parquet files can be read as either. That reframes the decision from a migration to a conversion. How to use this in an actual interview Three habits matter more than recall. Say what it depends on. Most Spark questions have a “depends” in them, and naming the dependency is the answer. “Should you cache?” depends on whether the dataset is reused and whether producing it was expensive. An answer that names the condition beats one that picks a side. Reach for the mechanism, not the knob. “Raise the memory” is a weak answer to a spill question. “The partitions are too large for the memory each task gets, so I would raise parallelism first and check for skew” is a strong one. The knob comes last, after the cause. Refuse to invent numbers. If you do not know a default, say you would check the Environment tab or the configuration reference. Interviewers notice confident wrong defaults far more than an honest “I would look it up”, and one invented number makes everything else you said suspect. Conclusion Two hundred questions is a lot of surface, and it collapses into four ideas. Work is lazy until an action asks for a result, which is what allows the whole plan to be optimised rather than each call executed as it arrives. Partitions are the unit of parallelism, so nearly every performance question is really about how many you have and how evenly the data sits across them. Shuffles are the expensive thing, and stage boundaries fall exactly where they occur, which is why counting Exchange nodes tells you so much. And the driver holds all the state that cannot be recomputed, which is why it is the one process with no redundancy. Derive an answer from those four and you will handle questions this list does not contain, which is the actual goal. The scenario section is where that shows: nobody can memorise the answer to “it was 20 minutes and now it is 2 hours”, because the answer is a way of looking rather than a fact. References Spark configuration reference for every property, its default and the version it appeared in RDD programming guide for lazy evaluation, persistence and shared variables SQL performance tuning for adaptive query execution and join hints Apache Spark architecture for the execution model behind these answers Apache Spark: the complete cheat sheet for the config surface in one page Trademarks Apache Spark, Apache Hadoop, Apache Hive, Apache Parquet, Apache ORC, Apache Avro, Apache Kafka, Apache Arrow, Apache Hudi, Apache Iceberg, Apache XTable (incubating), Apache Mesos, Apache Log4j and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries. Delta Lake is a trademark of the Linux Foundation. Kubernetes is a registered trademark of the Linux Foundation.</summary></entry><entry><title type="html">Spark and Hudi logging: making a write explain itself</title><link href="https://rangareddy.github.io/SparkHudiCustomLogging/" rel="alternate" type="text/html" title="Spark and Hudi logging: making a write explain itself" /><published>2026-09-15T09:00:00+05:30</published><updated>2026-09-15T09:00:00+05:30</updated><id>https://rangareddy.github.io/SparkHudiCustomLogging</id><content type="html" xml:base="https://rangareddy.github.io/SparkHudiCustomLogging/">&lt;ul id=&quot;markdown-toc&quot;&gt;
  &lt;li&gt;&lt;a href=&quot;#why-is-none-of-this-hudi-specific-configuration&quot; id=&quot;markdown-toc-why-is-none-of-this-hudi-specific-configuration&quot;&gt;Why is none of this Hudi-specific configuration?&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#architecture-where-the-config-goes-and-who-reads-it&quot; id=&quot;markdown-toc-architecture-where-the-config-goes-and-who-reads-it&quot;&gt;Architecture: where the config goes and who reads it&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#which-hudi-loggers-actually-fire&quot; id=&quot;markdown-toc-which-hudi-loggers-actually-fire&quot;&gt;Which Hudi loggers actually fire?&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#what-does-the-signal-look-like&quot; id=&quot;markdown-toc-what-does-the-signal-look-like&quot;&gt;What does the signal look like?&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#the-config-that-keeps-the-signal&quot; id=&quot;markdown-toc-the-config-that-keeps-the-signal&quot;&gt;The config that keeps the signal&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#which-side-do-hudi-logs-come-out-of&quot; id=&quot;markdown-toc-which-side-do-hudi-logs-come-out-of&quot;&gt;Which side do Hudi logs come out of?&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#sending-hudi-to-its-own-file&quot; id=&quot;markdown-toc-sending-hudi-to-its-own-file&quot;&gt;Sending Hudi to its own file&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#turning-up-one-subsystem-at-a-time&quot; id=&quot;markdown-toc-turning-up-one-subsystem-at-a-time&quot;&gt;Turning up one subsystem at a time&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#production-tips&quot; id=&quot;markdown-toc-production-tips&quot;&gt;Production tips&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#frequently-asked-questions&quot; id=&quot;markdown-toc-frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#conclusion&quot; id=&quot;markdown-toc-conclusion&quot;&gt;Conclusion&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#references&quot; id=&quot;markdown-toc-references&quot;&gt;References&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;#trademarks&quot; id=&quot;markdown-toc-trademarks&quot;&gt;Trademarks&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt;&lt;/p&gt;

  &lt;ul&gt;
    &lt;li&gt;Hudi logs through slf4j with class-name loggers, so every knob is an &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.*&lt;/code&gt; package in your Log4j 2 config. Nothing Hudi-specific is needed to capture them.&lt;/li&gt;
    &lt;li&gt;On a two-commit Merge-on-Read write, &lt;a href=&quot;https://github.com/apache/hudi/blob/master/hudi-common/src/main/java/org/apache/hudi/common/table/HoodieTableConfig.java&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieTableConfig&lt;/code&gt;&lt;/a&gt; alone produced 101 lines saying it loaded the same properties file. It is the first thing to silence.&lt;/li&gt;
    &lt;li&gt;The five loggers worth keeping print the instant lifecycle: requested, inflight, committed, and what a reader reconciled. That is the same timeline your table is built on.&lt;/li&gt;
    &lt;li&gt;A tuned config took the same job from 899 lines to 204 without losing a single lifecycle line.&lt;/li&gt;
    &lt;li&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;additivity = false&lt;/code&gt; is what sends Hudi to its own file instead of duplicating it into the console.&lt;/li&gt;
  &lt;/ul&gt;
&lt;/blockquote&gt;

&lt;p&gt;A Hudi write goes wrong in a way Spark’s own logs cannot explain. The stage
succeeded, the job did not fail, and the table is somehow missing the update you
just wrote. Or the write took far longer than the one before it and nothing in
the Spark UI says why, because the time went somewhere the UI does not model:
index lookup, log file reads, a metadata table commit.&lt;/p&gt;

&lt;p&gt;The answers are in Hudi’s logs. The problem is that a small write emits hundreds
of lines and most of them repeat that a properties file was loaded.&lt;/p&gt;

&lt;p&gt;This post is the filter. It covers which Hudi loggers actually fire, which to
keep, and how to read what survives. The generic mechanism, how a Log4j 2 file
reaches the driver and the executors, is in the
&lt;a href=&quot;/SparkTroubleshootingPlaybook/&quot;&gt;Spark JVM playbook&lt;/a&gt;;
this is the Hudi-specific layer on top.&lt;/p&gt;

&lt;h2 id=&quot;why-is-none-of-this-hudi-specific-configuration&quot;&gt;Why is none of this Hudi-specific configuration?&lt;/h2&gt;

&lt;p&gt;Because Hudi does not have a logging system of its own. &lt;a href=&quot;https://github.com/apache/hudi/blob/master/hudi-common/src/main/java/org/apache/hudi/common/table/HoodieTableMetaClient.java&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieTableMetaClient&lt;/code&gt;&lt;/a&gt;
and every other class does this:&lt;/p&gt;

&lt;div class=&quot;language-java highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;org.slf4j.Logger&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;org.slf4j.LoggerFactory&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;kd&quot;&gt;private&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;final&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Logger&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;LOG&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;LoggerFactory&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;getLogger&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nc&quot;&gt;HoodieTableMetaClient&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;class&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;slf4j with a class-name logger. The consequence is the useful part: &lt;strong&gt;every
logger name is a fully qualified class name&lt;/strong&gt;, so you can address any subsystem
by its package. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.client.transaction&lt;/code&gt; is the locking code,
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.common.table.log&lt;/code&gt; is the log file reader, and so on.&lt;/p&gt;

&lt;p&gt;One version note that catches people moving from older guides. Spark 3.3.0
switched to Log4j 2, and at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;v4.2.0&lt;/code&gt; the only template shipped is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;conf/log4j2.properties.template&lt;/code&gt;; the Log4j 1 template is gone. If a guide
tells you to edit &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j.properties&lt;/code&gt; with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j.rootLogger=&lt;/code&gt;, it predates that
change and the file will be ignored.&lt;/p&gt;

&lt;h2 id=&quot;architecture-where-the-config-goes-and-who-reads-it&quot;&gt;Architecture: where the config goes and who reads it&lt;/h2&gt;

&lt;p&gt;One file reaches two kinds of JVM, and each hosts different Hudi classes.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;log4j2-hudi.properties
        |
        |  --files ships it into every container&apos;s working directory
        |
        +--&amp;gt; driver JVM      -Dlog4j.configurationFile=log4j2-hudi.properties
        |      BaseHoodieWriteClient, ActiveTimelineV2,
        |      HoodieBackedTableMetadataWriter, TransactionManager
        |
        +--&amp;gt; executor JVMs   -Dlog4j.configurationFile=log4j2-hudi.properties
               HoodieMergeHandle, HoodieAppendHandle, HoodieCreateHandle,
               index classes, log record readers
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Log4j 2 reads the file once per JVM at startup, so the same config governs both
sides and there is no Hudi-side reload. What differs is only which classes are
loaded in each JVM, which is why the two logs answer different questions.&lt;/p&gt;

&lt;h2 id=&quot;which-hudi-loggers-actually-fire&quot;&gt;Which Hudi loggers actually fire?&lt;/h2&gt;

&lt;p&gt;Rather than guess, run a write with every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi&lt;/code&gt; logger at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INFO&lt;/code&gt;,
print the logger name in the pattern, and count. On a Merge-on-Read table with
one insert and one update, two commits in total:&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: right&quot;&gt;Lines&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Logger&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;What it is&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;101&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;common.table.HoodieTableConfig&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;“Loading table properties from …”, repeated per access&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;70&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;client.transaction.lock.InProcessLockProvider&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Lock acquire and release&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;61&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;common.table.view.FileSystemViewManager&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;File system view construction&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;56&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;client.transaction.TransactionManager&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Transaction state changes&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;44&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;common.table.log.HoodieLogFormat$WriterBuilder&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Log file writer setup&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;42&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;common.table.HoodieTableMetaClient&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Table initialisation and reload&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;35&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;common.table.timeline.versioning.v2.ActiveTimelineV2&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Instant state transitions&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;26&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;metadata.HoodieBackedTableMetadataWriter&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Metadata table commits&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;25&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;client.BaseHoodieWriteClient&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;Commit lifecycle&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: right&quot;&gt;27&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;common.table.log.BaseHoodieLogRecordReader&lt;/code&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;strong&gt;What a reader reconciled&lt;/strong&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The shape is the point. The four noisiest loggers describe plumbing that is
working correctly, and the three in bold are the ones that answer questions.
&lt;a href=&quot;https://github.com/apache/hudi/blob/master/hudi-common/src/main/java/org/apache/hudi/common/table/HoodieTableConfig.java&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieTableConfig&lt;/code&gt;&lt;/a&gt; produced more lines than the commit lifecycle, the timeline
and the log reader combined.&lt;/p&gt;

&lt;h2 id=&quot;what-does-the-signal-look-like&quot;&gt;What does the signal look like?&lt;/h2&gt;

&lt;p&gt;Three loggers carry almost all the diagnostic value.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BaseHoodieWriteClient&lt;/code&gt; gives you the commit, by instant:&lt;/strong&gt;&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;INFO BaseHoodieWriteClient - Committing 20260917105313836 action deltacommit
INFO BaseHoodieWriteClient - Committed 20260917105313836
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Two lines, and the second one is the one that matters. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Committing&lt;/code&gt; without a
matching &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Committed&lt;/code&gt; is a write that did not land, which is exactly the case
where the Spark job looks successful and the table disagrees.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ActiveTimelineV2&lt;/code&gt; gives you the state machine&lt;/strong&gt; described in the
&lt;a href=&quot;/ApacheHudiArchitecture/&quot;&gt;Hudi architecture post&lt;/a&gt;,
printed as it happens:&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;INFO ActiveTimelineV2 - Creating a new instant [==&amp;gt;20260917105313836__deltacommit__REQUESTED]
INFO ActiveTimelineV2 - Marking instant complete [==&amp;gt;20260917105313836__deltacommit__INFLIGHT]
INFO ActiveTimelineV2 - Created new file for toInstant: .hoodie/timeline/20260917105313836_20260917105315303.deltacommit
INFO ActiveTimelineV2 - Completed [==&amp;gt;20260917105313836__deltacommit__INFLIGHT]
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;Read the filename on the third line. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;20260917105313836_20260917105315303&lt;/code&gt; is
the start time and the completion time, which is the naming the timeline uses
for a finished instant. If your logs stop after &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;REQUESTED&lt;/code&gt; or &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INFLIGHT&lt;/code&gt;, you
have found where the write stopped, and the instant time tells you exactly which
files to look at under &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.hoodie/&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BaseHoodieLogRecordReader&lt;/code&gt; tells you what a Merge-on-Read reader had to
reconcile:&lt;/strong&gt;&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;INFO BaseHoodieLogRecordReader - Ordered instant times seen [20260917105313836]
INFO BaseHoodieLogRecordReader - Targeted instants that are rolled back are []
INFO BaseHoodieLogRecordReader - Total valid instants found are 1
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;That count is read-side merge work made visible. When snapshot queries slow down
on a Merge-on-Read table, this number climbing across runs is compaction falling
behind, and it moves before any query gets slow enough to notice.&lt;/p&gt;

&lt;h2 id=&quot;the-config-that-keeps-the-signal&quot;&gt;The config that keeps the signal&lt;/h2&gt;

&lt;p&gt;Everything above in one file. Save it as &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j2-hudi.properties&lt;/code&gt;:&lt;/p&gt;

&lt;div class=&quot;language-properties highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;py&quot;&gt;status&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;warn&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;rootLogger.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;warn&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;rootLogger.appenderRef.stdout.ref&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;console&lt;/span&gt;

&lt;span class=&quot;py&quot;&gt;appender.console.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;Console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;console&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.target&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;SYSTEM_OUT&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.layout.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;PatternLayout&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.console.layout.pattern&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;%d{HH:mm:ss} %-5level %logger{1} - %msg%n&lt;/span&gt;

&lt;span class=&quot;c&quot;&gt;# the write lifecycle: which instant, and whether it committed
&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;logger.hudiclient.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.hudi.client.BaseHoodieWriteClient&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudiclient.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;info&lt;/span&gt;

&lt;span class=&quot;c&quot;&gt;# instant state transitions: requested -&amp;gt; inflight -&amp;gt; completed
&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;logger.huditimeline.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.hudi.common.table.timeline&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.huditimeline.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;info&lt;/span&gt;

&lt;span class=&quot;c&quot;&gt;# what a Merge-on-Read reader reconciled
&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;logger.hudilog.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.hudi.common.table.log&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudilog.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;info&lt;/span&gt;

&lt;span class=&quot;c&quot;&gt;# noise: repeats the same property load on every access
&lt;/span&gt;&lt;span class=&quot;py&quot;&gt;logger.huditableconfig.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.hudi.common.table.HoodieTableConfig&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.huditableconfig.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;warn&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudifsview.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.hudi.common.table.view&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudifsview.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;warn&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudilock.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.hudi.client.transaction&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudilock.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;warn&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;On the same two-commit job that produced 899 lines, this produces &lt;strong&gt;204&lt;/strong&gt;, with
every lifecycle line intact. &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieTableConfig&lt;/code&gt; drops from 101 lines to 4 and
the locking chatter to none.&lt;/p&gt;

&lt;p&gt;Ship it the same way as any Spark logging config:&lt;/p&gt;

&lt;div class=&quot;language-bash highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;spark-submit &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--files&lt;/span&gt; /etc/spark/conf/log4j2-hudi.properties &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.driver.extraJavaOptions=-Dlog4j.configurationFile=log4j2-hudi.properties&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--conf&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;spark.executor.extraJavaOptions=-Dlog4j.configurationFile=log4j2-hudi.properties&quot;&lt;/span&gt; &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;--class&lt;/span&gt; com.example.TripsIngest &lt;span class=&quot;se&quot;&gt;\&lt;/span&gt;
  trips-ingest.jar
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--files&lt;/code&gt; puts the file in each container’s working directory, which is why the
value of &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-Dlog4j.configurationFile&lt;/code&gt; is a bare filename rather than a path. In
client mode the driver JVM already exists by the time your code runs, so use
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--driver-java-options&lt;/code&gt; for the driver side instead.&lt;/p&gt;

&lt;h2 id=&quot;which-side-do-hudi-logs-come-out-of&quot;&gt;Which side do Hudi logs come out of?&lt;/h2&gt;

&lt;p&gt;Both, and they say different things. It is worth knowing which log to open.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Runs on&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Loggers&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Answers&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Driver&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;BaseHoodieWriteClient&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ActiveTimelineV2&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieBackedTableMetadataWriter&lt;/code&gt;, &lt;a href=&quot;https://github.com/apache/hudi/blob/master/hudi-client/hudi-client-common/src/main/java/org/apache/hudi/client/transaction/TransactionManager.java&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;TransactionManager&lt;/code&gt;&lt;/a&gt;&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Did the commit land, what did the timeline do, did the metadata table update&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Executor&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;a href=&quot;https://github.com/apache/hudi/blob/master/hudi-client/hudi-client-common/src/main/java/org/apache/hudi/io/HoodieMergeHandle.java&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieMergeHandle&lt;/code&gt;&lt;/a&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieAppendHandle&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieCreateHandle&lt;/code&gt;, index and log reader classes&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Which handle ran, what a key tagged to, what a reader merged&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The split follows the architecture: the driver owns the timeline and the commit,
the executors do the record-level work. So &lt;strong&gt;a question about whether a write
committed is a driver-log question, and a question about why one partition was
slow is an executor-log question&lt;/strong&gt;. Setting &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.extraJavaOptions&lt;/code&gt;
and forgetting the driver, or the reverse, is why half the expected lines never
appear.&lt;/p&gt;

&lt;h2 id=&quot;sending-hudi-to-its-own-file&quot;&gt;Sending Hudi to its own file&lt;/h2&gt;

&lt;p&gt;On a busy job Hudi’s output is interleaved with Spark’s. A dedicated appender
separates them:&lt;/p&gt;

&lt;div class=&quot;language-properties highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;py&quot;&gt;appender.hudifile.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;RollingFile&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;hudifile&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.fileName&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;/var/log/spark/hudi.log&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.filePattern&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;/var/log/spark/hudi-%d{yyyy-MM-dd}-%i.log.gz&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.layout.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;PatternLayout&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.layout.pattern&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;%d{ISO8601} %-5level %logger - %msg%n&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.policies.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;Policies&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.policies.size.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;SizeBasedTriggeringPolicy&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.policies.size.size&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;50MB&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.strategy.type&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;DefaultRolloverStrategy&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;appender.hudifile.strategy.max&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;5&lt;/span&gt;

&lt;span class=&quot;py&quot;&gt;logger.hudi.name&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;org.apache.hudi&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudi.level&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;info&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudi.additivity&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;false&lt;/span&gt;
&lt;span class=&quot;py&quot;&gt;logger.hudi.appenderRef.hudifile.ref&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;hudifile&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;&lt;strong&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;additivity = false&lt;/code&gt; is the line that does the work.&lt;/strong&gt; Without it a logger
sends its output to its own appender &lt;em&gt;and&lt;/em&gt; to the root appender, so everything
lands in both the file and the console. With it, the same run put 871 Hudi lines
in &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hudi.log&lt;/code&gt; and left 4 on the console.&lt;/p&gt;

&lt;p&gt;Two cautions. On YARN and Kubernetes an absolute path writes inside the
container, so use a mounted path or let log aggregation collect it. And the
rolling policy is not optional at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;INFO&lt;/code&gt; on a busy writer: without
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SizeBasedTriggeringPolicy&lt;/code&gt; and a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;max&lt;/code&gt;, the file grows until the disk is the
problem you are debugging.&lt;/p&gt;

&lt;h2 id=&quot;turning-up-one-subsystem-at-a-time&quot;&gt;Turning up one subsystem at a time&lt;/h2&gt;

&lt;p&gt;Once the baseline is quiet, raise exactly one package for the question you have.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Question&lt;/th&gt;
      &lt;th style=&quot;text-align: left&quot;&gt;Raise to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DEBUG&lt;/code&gt;&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Why is tagging slow, and which index is running&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.index&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Is compaction being scheduled, and over what&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.table.action.compact&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Is the metadata table keeping up&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.metadata&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Who holds the lock, and for how long&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.client.transaction&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;What is a Merge-on-Read reader merging&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.common.table.log&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;Which handle wrote a file&lt;/td&gt;
      &lt;td style=&quot;text-align: left&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi.io&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;The first row is the one to reach for most often, because an unset
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.index.type&lt;/code&gt; resolves to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;SIMPLE&lt;/code&gt; on Spark, whose cost scales with the
table rather than the batch. The
&lt;a href=&quot;/ApacheHudiCheatSheet/&quot;&gt;Hudi cheat sheet&lt;/a&gt; has the
index options and the rest of the config surface.&lt;/p&gt;

&lt;h2 id=&quot;production-tips&quot;&gt;Production tips&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;Keep the tuned config as the default&lt;/strong&gt;, not something you switch on during an incident. The lifecycle lines are what let you answer questions after the fact.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Set both driver and executor options.&lt;/strong&gt; Each answers different questions, and a missing one looks like a missing feature.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Never leave &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;org.apache.hudi&lt;/code&gt; at &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;DEBUG&lt;/code&gt; in production.&lt;/strong&gt; Raise one package for one investigation, then put it back.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Give Hudi its own file with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;additivity = false&lt;/code&gt;,&lt;/strong&gt; and always pair it with a size policy and a &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;max&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Alert on &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Committing&lt;/code&gt; without &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;Committed&lt;/code&gt;.&lt;/strong&gt; That pair, matched by instant time, is the cheapest write-succeeded check you can build.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Log the instant time into your own job output.&lt;/strong&gt; It is the join key between your scheduler, the Spark UI and everything under &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;.hoodie/&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;frequently-asked-questions&quot;&gt;Frequently asked questions&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;Why does &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j.properties&lt;/code&gt; no longer work?&lt;/strong&gt;
Spark moved to Log4j 2 in 3.3.0. The file is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j2.properties&lt;/code&gt;, the flag is
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;-Dlog4j.configurationFile=&lt;/code&gt;, and the syntax is &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;logger.&amp;lt;key&amp;gt;.name&lt;/code&gt; rather than
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;log4j.logger.&amp;lt;name&amp;gt;&lt;/code&gt;. Spark 4.x ships only the Log4j 2 template.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Do I need a Hudi-specific logging dependency?&lt;/strong&gt;
No. Hudi logs through slf4j, and the Spark bundle already brings a binding.
Configuring Log4j 2 is enough.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;My executor logs are empty even though the driver’s are fine.&lt;/strong&gt;
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;spark.executor.extraJavaOptions&lt;/code&gt; is probably unset, or the file was not shipped
with &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;--files&lt;/code&gt;, so the path resolves on the submitting machine only.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Can I change levels without restarting?&lt;/strong&gt;
Add &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;monitorInterval&lt;/code&gt; to the config and Log4j 2 re-reads it periodically. It is
useful on a long streaming job, and pointless on a batch job that will finish
before the interval elapses.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Why is the same properties file loaded so many times?&lt;/strong&gt;
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieTableConfig&lt;/code&gt; logs on each access rather than caching the message. It is
harmless, and it is the single biggest source of noise, which is why the config
above pins it to &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;WARN&lt;/code&gt;.&lt;/p&gt;

&lt;h2 id=&quot;conclusion&quot;&gt;Conclusion&lt;/h2&gt;

&lt;p&gt;The reason Hudi logs feel useless by default is not that they lack information.
It is that the lines carrying the state machine are outnumbered roughly four to
one by lines confirming that plumbing worked. Silence the plumbing and what
remains is a readable account of the write: an instant created, moved to
inflight, and completed with a filename that carries both its start and
completion time.&lt;/p&gt;

&lt;p&gt;That account is worth keeping on by default. Hudi’s failure modes are quiet
ones, a write that never committed or a Merge-on-Read table whose logs are
outgrowing compaction, and neither raises an exception in the Spark job. The
lifecycle lines are the record that lets you answer, afterwards, which instant
did what.&lt;/p&gt;

&lt;p&gt;The general mechanism is not Hudi’s at all, which is the useful part. Once you
can address any subsystem by its package name, the same file tunes Spark,
Hudi and whatever else is on the classpath, and the only decision left is which
question you are asking today.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://hudi.apache.org/docs/configurations/&quot;&gt;Hudi configuration reference&lt;/a&gt; for every &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;hoodie.*&lt;/code&gt; key mentioned here&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://logging.apache.org/log4j/2.x/manual/configuration.html&quot;&gt;Log4j 2 properties configuration&lt;/a&gt; for appenders, additivity and rollover&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://github.com/apache/hudi/blob/master/hudi-common/src/main/java/org/apache/hudi/common/table/HoodieTableMetaClient.java&quot;&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;HoodieTableMetaClient.java&lt;/code&gt;&lt;/a&gt;, showing the slf4j logger every Hudi class uses&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/SparkTroubleshootingPlaybook/&quot;&gt;A Spark JVM playbook&lt;/a&gt; for how a Log4j 2 file reaches the driver and the executors&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;/ApacheHudiArchitecture/&quot;&gt;Apache Hudi architecture&lt;/a&gt; for the timeline and instant states these logs narrate&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;trademarks&quot;&gt;Trademarks&lt;/h2&gt;

&lt;p&gt;Apache Hudi, Apache Spark, Apache Log4j, Apache Hadoop, Apache Parquet, Apache Avro and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.&lt;/p&gt;</content><author><name>Ranga Reddy</name></author><category term="Hudi" /><category term="Hudi" /><category term="Spark" /><category term="Logging" /><category term="Log4j" /><category term="Troubleshoot" /><summary type="html">Why is none of this Hudi-specific configuration? Architecture: where the config goes and who reads it Which Hudi loggers actually fire? What does the signal look like? The config that keeps the signal Which side do Hudi logs come out of? Sending Hudi to its own file Turning up one subsystem at a time Production tips Frequently asked questions Conclusion References Trademarks TL;DR Hudi logs through slf4j with class-name loggers, so every knob is an org.apache.hudi.* package in your Log4j 2 config. Nothing Hudi-specific is needed to capture them. On a two-commit Merge-on-Read write, HoodieTableConfig alone produced 101 lines saying it loaded the same properties file. It is the first thing to silence. The five loggers worth keeping print the instant lifecycle: requested, inflight, committed, and what a reader reconciled. That is the same timeline your table is built on. A tuned config took the same job from 899 lines to 204 without losing a single lifecycle line. additivity = false is what sends Hudi to its own file instead of duplicating it into the console. A Hudi write goes wrong in a way Spark’s own logs cannot explain. The stage succeeded, the job did not fail, and the table is somehow missing the update you just wrote. Or the write took far longer than the one before it and nothing in the Spark UI says why, because the time went somewhere the UI does not model: index lookup, log file reads, a metadata table commit. The answers are in Hudi’s logs. The problem is that a small write emits hundreds of lines and most of them repeat that a properties file was loaded. This post is the filter. It covers which Hudi loggers actually fire, which to keep, and how to read what survives. The generic mechanism, how a Log4j 2 file reaches the driver and the executors, is in the Spark JVM playbook; this is the Hudi-specific layer on top. Why is none of this Hudi-specific configuration? Because Hudi does not have a logging system of its own. HoodieTableMetaClient and every other class does this: import org.slf4j.Logger; import org.slf4j.LoggerFactory; private static final Logger LOG = LoggerFactory.getLogger(HoodieTableMetaClient.class); slf4j with a class-name logger. The consequence is the useful part: every logger name is a fully qualified class name, so you can address any subsystem by its package. org.apache.hudi.client.transaction is the locking code, org.apache.hudi.common.table.log is the log file reader, and so on. One version note that catches people moving from older guides. Spark 3.3.0 switched to Log4j 2, and at v4.2.0 the only template shipped is conf/log4j2.properties.template; the Log4j 1 template is gone. If a guide tells you to edit log4j.properties with log4j.rootLogger=, it predates that change and the file will be ignored. Architecture: where the config goes and who reads it One file reaches two kinds of JVM, and each hosts different Hudi classes. log4j2-hudi.properties | | --files ships it into every container&apos;s working directory | +--&amp;gt; driver JVM -Dlog4j.configurationFile=log4j2-hudi.properties | BaseHoodieWriteClient, ActiveTimelineV2, | HoodieBackedTableMetadataWriter, TransactionManager | +--&amp;gt; executor JVMs -Dlog4j.configurationFile=log4j2-hudi.properties HoodieMergeHandle, HoodieAppendHandle, HoodieCreateHandle, index classes, log record readers Log4j 2 reads the file once per JVM at startup, so the same config governs both sides and there is no Hudi-side reload. What differs is only which classes are loaded in each JVM, which is why the two logs answer different questions. Which Hudi loggers actually fire? Rather than guess, run a write with every org.apache.hudi logger at INFO, print the logger name in the pattern, and count. On a Merge-on-Read table with one insert and one update, two commits in total: Lines Logger What it is 101 common.table.HoodieTableConfig “Loading table properties from …”, repeated per access 70 client.transaction.lock.InProcessLockProvider Lock acquire and release 61 common.table.view.FileSystemViewManager File system view construction 56 client.transaction.TransactionManager Transaction state changes 44 common.table.log.HoodieLogFormat$WriterBuilder Log file writer setup 42 common.table.HoodieTableMetaClient Table initialisation and reload 35 common.table.timeline.versioning.v2.ActiveTimelineV2 Instant state transitions 26 metadata.HoodieBackedTableMetadataWriter Metadata table commits 25 client.BaseHoodieWriteClient Commit lifecycle 27 common.table.log.BaseHoodieLogRecordReader What a reader reconciled The shape is the point. The four noisiest loggers describe plumbing that is working correctly, and the three in bold are the ones that answer questions. HoodieTableConfig produced more lines than the commit lifecycle, the timeline and the log reader combined. What does the signal look like? Three loggers carry almost all the diagnostic value. BaseHoodieWriteClient gives you the commit, by instant: INFO BaseHoodieWriteClient - Committing 20260917105313836 action deltacommit INFO BaseHoodieWriteClient - Committed 20260917105313836 Two lines, and the second one is the one that matters. Committing without a matching Committed is a write that did not land, which is exactly the case where the Spark job looks successful and the table disagrees. ActiveTimelineV2 gives you the state machine described in the Hudi architecture post, printed as it happens: INFO ActiveTimelineV2 - Creating a new instant [==&amp;gt;20260917105313836__deltacommit__REQUESTED] INFO ActiveTimelineV2 - Marking instant complete [==&amp;gt;20260917105313836__deltacommit__INFLIGHT] INFO ActiveTimelineV2 - Created new file for toInstant: .hoodie/timeline/20260917105313836_20260917105315303.deltacommit INFO ActiveTimelineV2 - Completed [==&amp;gt;20260917105313836__deltacommit__INFLIGHT] Read the filename on the third line. 20260917105313836_20260917105315303 is the start time and the completion time, which is the naming the timeline uses for a finished instant. If your logs stop after REQUESTED or INFLIGHT, you have found where the write stopped, and the instant time tells you exactly which files to look at under .hoodie/. BaseHoodieLogRecordReader tells you what a Merge-on-Read reader had to reconcile: INFO BaseHoodieLogRecordReader - Ordered instant times seen [20260917105313836] INFO BaseHoodieLogRecordReader - Targeted instants that are rolled back are [] INFO BaseHoodieLogRecordReader - Total valid instants found are 1 That count is read-side merge work made visible. When snapshot queries slow down on a Merge-on-Read table, this number climbing across runs is compaction falling behind, and it moves before any query gets slow enough to notice. The config that keeps the signal Everything above in one file. Save it as log4j2-hudi.properties: status = warn rootLogger.level = warn rootLogger.appenderRef.stdout.ref = console appender.console.type = Console appender.console.name = console appender.console.target = SYSTEM_OUT appender.console.layout.type = PatternLayout appender.console.layout.pattern = %d{HH:mm:ss} %-5level %logger{1} - %msg%n # the write lifecycle: which instant, and whether it committed logger.hudiclient.name = org.apache.hudi.client.BaseHoodieWriteClient logger.hudiclient.level = info # instant state transitions: requested -&amp;gt; inflight -&amp;gt; completed logger.huditimeline.name = org.apache.hudi.common.table.timeline logger.huditimeline.level = info # what a Merge-on-Read reader reconciled logger.hudilog.name = org.apache.hudi.common.table.log logger.hudilog.level = info # noise: repeats the same property load on every access logger.huditableconfig.name = org.apache.hudi.common.table.HoodieTableConfig logger.huditableconfig.level = warn logger.hudifsview.name = org.apache.hudi.common.table.view logger.hudifsview.level = warn logger.hudilock.name = org.apache.hudi.client.transaction logger.hudilock.level = warn On the same two-commit job that produced 899 lines, this produces 204, with every lifecycle line intact. HoodieTableConfig drops from 101 lines to 4 and the locking chatter to none. Ship it the same way as any Spark logging config: spark-submit \ --files /etc/spark/conf/log4j2-hudi.properties \ --conf &quot;spark.driver.extraJavaOptions=-Dlog4j.configurationFile=log4j2-hudi.properties&quot; \ --conf &quot;spark.executor.extraJavaOptions=-Dlog4j.configurationFile=log4j2-hudi.properties&quot; \ --class com.example.TripsIngest \ trips-ingest.jar --files puts the file in each container’s working directory, which is why the value of -Dlog4j.configurationFile is a bare filename rather than a path. In client mode the driver JVM already exists by the time your code runs, so use --driver-java-options for the driver side instead. Which side do Hudi logs come out of? Both, and they say different things. It is worth knowing which log to open. Runs on Loggers Answers Driver BaseHoodieWriteClient, ActiveTimelineV2, HoodieBackedTableMetadataWriter, TransactionManager Did the commit land, what did the timeline do, did the metadata table update Executor HoodieMergeHandle, HoodieAppendHandle, HoodieCreateHandle, index and log reader classes Which handle ran, what a key tagged to, what a reader merged The split follows the architecture: the driver owns the timeline and the commit, the executors do the record-level work. So a question about whether a write committed is a driver-log question, and a question about why one partition was slow is an executor-log question. Setting spark.executor.extraJavaOptions and forgetting the driver, or the reverse, is why half the expected lines never appear. Sending Hudi to its own file On a busy job Hudi’s output is interleaved with Spark’s. A dedicated appender separates them: appender.hudifile.type = RollingFile appender.hudifile.name = hudifile appender.hudifile.fileName = /var/log/spark/hudi.log appender.hudifile.filePattern = /var/log/spark/hudi-%d{yyyy-MM-dd}-%i.log.gz appender.hudifile.layout.type = PatternLayout appender.hudifile.layout.pattern = %d{ISO8601} %-5level %logger - %msg%n appender.hudifile.policies.type = Policies appender.hudifile.policies.size.type = SizeBasedTriggeringPolicy appender.hudifile.policies.size.size = 50MB appender.hudifile.strategy.type = DefaultRolloverStrategy appender.hudifile.strategy.max = 5 logger.hudi.name = org.apache.hudi logger.hudi.level = info logger.hudi.additivity = false logger.hudi.appenderRef.hudifile.ref = hudifile additivity = false is the line that does the work. Without it a logger sends its output to its own appender and to the root appender, so everything lands in both the file and the console. With it, the same run put 871 Hudi lines in hudi.log and left 4 on the console. Two cautions. On YARN and Kubernetes an absolute path writes inside the container, so use a mounted path or let log aggregation collect it. And the rolling policy is not optional at INFO on a busy writer: without SizeBasedTriggeringPolicy and a max, the file grows until the disk is the problem you are debugging. Turning up one subsystem at a time Once the baseline is quiet, raise exactly one package for the question you have. Question Raise to DEBUG Why is tagging slow, and which index is running org.apache.hudi.index Is compaction being scheduled, and over what org.apache.hudi.table.action.compact Is the metadata table keeping up org.apache.hudi.metadata Who holds the lock, and for how long org.apache.hudi.client.transaction What is a Merge-on-Read reader merging org.apache.hudi.common.table.log Which handle wrote a file org.apache.hudi.io The first row is the one to reach for most often, because an unset hoodie.index.type resolves to SIMPLE on Spark, whose cost scales with the table rather than the batch. The Hudi cheat sheet has the index options and the rest of the config surface. Production tips Keep the tuned config as the default, not something you switch on during an incident. The lifecycle lines are what let you answer questions after the fact. Set both driver and executor options. Each answers different questions, and a missing one looks like a missing feature. Never leave org.apache.hudi at DEBUG in production. Raise one package for one investigation, then put it back. Give Hudi its own file with additivity = false, and always pair it with a size policy and a max. Alert on Committing without Committed. That pair, matched by instant time, is the cheapest write-succeeded check you can build. Log the instant time into your own job output. It is the join key between your scheduler, the Spark UI and everything under .hoodie/. Frequently asked questions Why does log4j.properties no longer work? Spark moved to Log4j 2 in 3.3.0. The file is log4j2.properties, the flag is -Dlog4j.configurationFile=, and the syntax is logger.&amp;lt;key&amp;gt;.name rather than log4j.logger.&amp;lt;name&amp;gt;. Spark 4.x ships only the Log4j 2 template. Do I need a Hudi-specific logging dependency? No. Hudi logs through slf4j, and the Spark bundle already brings a binding. Configuring Log4j 2 is enough. My executor logs are empty even though the driver’s are fine. spark.executor.extraJavaOptions is probably unset, or the file was not shipped with --files, so the path resolves on the submitting machine only. Can I change levels without restarting? Add monitorInterval to the config and Log4j 2 re-reads it periodically. It is useful on a long streaming job, and pointless on a batch job that will finish before the interval elapses. Why is the same properties file loaded so many times? HoodieTableConfig logs on each access rather than caching the message. It is harmless, and it is the single biggest source of noise, which is why the config above pins it to WARN. Conclusion The reason Hudi logs feel useless by default is not that they lack information. It is that the lines carrying the state machine are outnumbered roughly four to one by lines confirming that plumbing worked. Silence the plumbing and what remains is a readable account of the write: an instant created, moved to inflight, and completed with a filename that carries both its start and completion time. That account is worth keeping on by default. Hudi’s failure modes are quiet ones, a write that never committed or a Merge-on-Read table whose logs are outgrowing compaction, and neither raises an exception in the Spark job. The lifecycle lines are the record that lets you answer, afterwards, which instant did what. The general mechanism is not Hudi’s at all, which is the useful part. Once you can address any subsystem by its package name, the same file tunes Spark, Hudi and whatever else is on the classpath, and the only decision left is which question you are asking today. References Hudi configuration reference for every hoodie.* key mentioned here Log4j 2 properties configuration for appenders, additivity and rollover HoodieTableMetaClient.java, showing the slf4j logger every Hudi class uses A Spark JVM playbook for how a Log4j 2 file reaches the driver and the executors Apache Hudi architecture for the timeline and instant states these logs narrate Trademarks Apache Hudi, Apache Spark, Apache Log4j, Apache Hadoop, Apache Parquet, Apache Avro and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and other countries.</summary></entry></feed>